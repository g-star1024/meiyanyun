# DELIVERY-P5-B61（2026-09-18）· 角色管理页 RBAC 可视化（04-backlog L50）

> **批次性质**：事实翻转的**零代码纵深勾销批**（先例 B53/B54/B60）。台账登记「后端 RBAC 已就绪，缺前端管理页」，卡0 代码侧只读侦察即发现前后端整套角色管理能力（四管理页 3219 行＋真实 store 609 行五端点无 mock＋router 四路由＋nav 权限中台分组＋RbacAdminController 全套后端端点＋PermissionMatrix 298×8 播种）均已真实存在；随后 curl/PG/Chrome 三轨三角真验全绿，确认登记与现网不符系历史时点漂移。处置＝订正 02-modules L110、勾销 04-backlog L50，**零代码改动、零 commit、数字一律不变**。
>
> **决策依据（用户四阶段授权逐字）**：「1、先处理第二层近线，可以全部开始；2、然后处理core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径 / 状态机 / 选型决策 按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入C. 大阶段 / 专项开工」。本批为第①阶段第二层近线第二批（B60 之后），「可以全部开始」覆盖 B61 无需再请示开工；卡0 事实翻转后的处置（全真→台账订正勾销、发现真缺口→拆纵深卡）按 B60 §7 逐字预告口径执行，属「按推荐来」授权范围。
>
> **卡0 四条裁断**：
> ① 前端角色管理四页（角色 /admin/roles 850 行、权限矩阵 /admin/permissions 697 行、员工 /admin/staff 686 行、组织 /admin/org 986 行，共 **3219 行**）均为真实 API 驱动、无 mock 回退；router L170-175 四路由、nav.ts L353-362「权限中台」菜单分组齐备——「缺前端管理页」登记不成立。
> ② 后端 RbacAdminController 自定义角色 CRUD／权限全量覆写／员工七端点全套真实，权限门控前后端双层（前端 v-if 按码显隐＋后端中文 403）；PermissionMatrix 298 个被引用码×8 角色矩阵与 RbacDataInitializer 四表播种对账一致——「后端 RBAC 已就绪」属实且前端已接线。
> ③ 三轨真验（网关 curl 四角权限＋PG 四表行数＋Chrome 三账号三角渲染/门控）全绿，无任何功能缺口需拆卡；六项历史包袱（§5）均为兼容/诚实预留性质，非阻断、非缺口，登记不施工。
> ④ 因此本批不产生代码 commit：以三轨证据链订正台账时点漂移即完成闭合（同 B53 卡8b/B60 纵深口径，零代码无 commit）；前端样式零改（本批无任何前端改动）。
>
> **提交清单（代码侧）**：**零代码、无 commit**（本批唯一提交为批末 docs 原子提交：本 DELIVERY＋台账六册＋哨兵，`git add -u docs/` 与 `git add -f` 本文件，hash 由下批 PREV 锚点登记）。
>
> **铁律对齐**：-1 不臆造（全部结论有 curl/PG/Chrome 逐轨证据，无一处凭台账文字想当然）；0 每卡开工先读哨兵＋六册（本卡开工 13:10 心跳留痕）；1 API 契约只加不改（本批零代码，契约零触碰）；2 DataScope/权限不回退（真验重点即四角越权矩阵）；3 中文错误（role:create 越权中文 403 实证）；4 数字不虚增（零代码批数字一律不变，三证勾稽）；5 测试数据还原（本批只读，无写入无还原项，§5 如实说明）；6 三轨真验（§4 全绿）；7 前端样式零改；8 一卡一 commit（零代码无 commit，沿 B50 卡8b 先例）；9/10 台账六册只追加不改写历史事实（02 L110 旧注保留不抹、尾接订正）；11 哨兵 ACTIVE 开工/DORMANT 闭合＋文末存档；安全：只读侦察无密钥外泄。

## 1. 背景与记账口径

04-backlog L50 登记：「角色管理页（RBAC 可视化配置）｜来源 permission-matrix｜依赖：后端 RBAC 已就绪，缺前端管理页｜建议批次：组织收口批」。02-modules L110 同口径：域⑦「角色 RBAC / 权限矩阵」行缺口列＝「角色管理页待补（Backlog）」（状态 ✅，M7/B19，真实依据 PermissionMatrix）。

B60 §7（DELIVERY-P5-B60 L114）逐字预告 B61 施工口径：「卡0 只读侦察 org PermissionMatrix 八角色×权限码播种现状、role:view/edit/staff-role 绑定与既有 /api/org/roles 端点、前端角色管理页/入口现状后拆卡；前端样式零改只换数据源」。卡0 代码侧只读侦察于开工当日完成，事实即翻转：前端四页与后端全套均已存在。经 curl/PG/Chrome 三轨三角真验全部取证后，定案为零代码纵深勾销。

**记账口径（数字一律不变，同 B53/B54/B60 纵深批）**：本批无新页面、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解。仪表盘维持 **✅109/166≈66%、🔧1、⬜55 约33%**；域⑦ 组织与权限维持 **11✅/0🔧/13⬜**；域⑧ 平台与基建维持 **34✅/2🔧/31⬜**。仅勾销 04-backlog L50 一行（删除线＋闭合长注＋末列 ✅ B61）、订正 02-modules L110 尾列（旧注作为当时时点事实保留不抹、尾接 B61 订正长注）。三证勾稽（完成度＝状态计数＝时间线批次）一致：本批不产生 ✅ 增量，只消解一条与现网不符的历史登记。

## 2. 影响面只读核实（卡0 侦察结论）

### 2.1 前端侧（四页 3219 行真实 API 驱动，无 mock）

- `frontend/src/views/T1RolesView.vue`（**850 行**）：角色列表/抽屉、成员关联、**全量 334 chip 权限矩阵对照**（L436-454，绿 success＝该角色已授权、灰 disabled＝未授权，非越权渲染）；按钮门控：新建角色 L299-305 `role:create`、编辑 L353-376 `role:edit`（内置八角色 builtin 恒 disabled）、删除 L372-376 `role:delete`、保存 L579-584 按权禁用、成员授权 L484-488 `role:assign`。
- `frontend/src/views/T1PermissionsView.vue`（**697 行**）：权限矩阵/SoD 冲突/角色差异/字段级四 Tab。
- `frontend/src/views/T1StaffView.vue`（**686 行**）：员工与主兼岗角色管理；`frontend/src/views/T1OrgView.vue`（**986 行**）：组织树与编制。
- `frontend/src/stores/t1Rbac.ts`（**609 行**）：封装五个真实端点（角色列表/角色矩阵/权限全集/角色权限覆写/角色成员），**无 mock 回退分支**；`frontend/src/api/org.ts` 角色段配套；`router/index.ts` L170-175 四路由（/admin/roles、/admin/permissions、/admin/staff、/admin/org）；`nav.ts` L353-362「权限中台」菜单分组（按权限码整组显隐）。
- 会话与守卫机制：持久化键 `meiyun_session`（整个 SessionInfo JSON，非裸 token）；`loadSession` 要求 token＋roles 非空数组（stores/auth.ts L331-341）；路由守卫 beforeEach（router/index.ts L252-273）身份判定＝`isAuthenticated || currentRoles.length>0`，无身份跳 `/login?redirect=`，权限不足跳 `/no-auth?from=&need=`（need＝permissionForPath 映射）。can()＝超管或 perms 含 `*` 或含该权限码（auth.ts L421-425）。真实 UI 登录是唯一被守卫认可的进入方式。

### 2.2 后端侧（RbacAdminController 全套端点＋矩阵＋播种）

org-service RbacAdminController 提供：自定义角色 CRUD（role:create/edit/delete）、角色权限全量覆写（rbac:edit）、权限全集/角色矩阵只读（role:view/permission:view/rbac:view）、员工角色绑定七端点（staff 段，门控 rbac:view/rbac:edit，无 staff:* 细码）。后端双层门控：细粒度码缺失即中文 403（与前端 v-if 双保险）。PermissionMatrix 定义 8 内置角色×权限码矩阵；RbacDataInitializer 对 role_def/permission_def/role_permission/staff_role 四表播种。org 库走 Hibernate ddl-auto=update 既有惯例（本批零改动）。

## 3. 零代码纵深勾销——无代码改动

本批无代码改动。能力存在性以 §4 三轨真验逐轨取证替代施工；台账原登记「缺前端管理页」属历史时点漂移（M7/B19 后端 RBAC 落地时的登记，前端四页系后续批次已补齐但未回写勾销），按铁律 9/10「只追加不改写历史事实」处置：04 L50 勾销、02 L110 旧注保留不抹仅尾接订正。零代码无 commit 先例：B50 卡8b（SE101 GROUP 全域口径核对，03-timeline commit 列「（卡8b 零代码无 commit）」）。

## 4. 三轨真验（全绿，逐轨留证）

### 4.1 网关 curl 轨（seed 栈 18443，四账号×五端点＋写门控）

- 四角真实登录（POST /api/org/auth/login，{loginName,password:"meiyun123"}，-k）：SE101 超管、E011 区域经理（华东）、E005 店长兼 OPERATOR、E002 前台，均 200 取真实会话。
- 五读端点权限分布符合矩阵：GET /api/org/roles、/role-matrix、/admin/permissions、/admin/role-permissions、/admin/role-members——SE101 200、E011 200（REGION_MGR 278 码含 role:view/rbac:view/permission:view/role:edit）、E005 按门控放行只读（STORE_MGR+OPERATOR 并集 236 码含三 view＋rbac:edit，**无** role:create/edit/delete）、E002 403（FRONT_DESK 30 码无任一管理 view 码）、无 token 401。
- role-members 数据域三体量对账：E011 区域视角 18566B（成员 12，华东 SST01/02/06 关联）、E005 门店视角 4685B、E002 空数组 `[]` 2B——DataScope 数据域收窄真实生效（空是正确收窄非 bug）。
- 写门控：E005/E002 POST role:create → 中文 **403**（前端按钮不在 DOM 之外的后端第二道保险实证）；持 rbac:edit 调写端点过鉴权闸、落到参数校验 **400**（证明门控后真实业务逻辑在跑，非假端点）。

### 4.2 PG 轨（meiyun_seed org 库四表行数与结构对账）

- `role_def` **8** 内置角色，data_scope 列带 CHECK 约束；**无 parent_id 列**（所谓「父角色继承树」仅为历史 UI 概念，后端恒一层，见 §5 ③）。
- `permission_def` **334** 个权限码定义＝**298** 个被七角色引用的去重并集＋**36** 个诚实预留码（ai* 系列、部分 :approve、notification:view、permission:diff 等，先定义待后续业务接线）。
- `role_permission` **686** 行七角色授权：REGION_MGR **278**、STORE_MGR **234**、CONSULTANT **29**、DOCTOR **31**、FRONT_DESK **30**、OPERATOR **17**、FINANCE **67**；SUPER_ADMIN 的 `*` 通配**不落库**（前端 can()/后端超管判定双通道补，见 §5 ⑥）。
- `staff_role` 双列复合 PK 共 **48** 行：43 行主角色已全 canonical 码＋**4** 行 ROLE_REGION/ROLE_GROUP_* 老码兼岗（E011-E014，历史数据兼容，见 §5 ①）。

### 4.3 Chrome 轨（dev server 5175，pageId=4，真实 UI 登录三账号三角）

- **E011（REGION_MGR，全读写三角）**：/admin/roles 真实渲染，成员关联 **12** 与 curl 18566B 对账一致；选中 DOCTOR 角色 chip 区显示 **334** 总码／**31** 绿（success＝授权，恰等 PG DOCTOR 31 行）／**112** 模块组／**95** 空组（灰 disabled＝未授权的诚实呈现，非缺陷）；内置八角色编辑/删除恒 disabled、「新建角色」按钮在 DOM（持 role:edit/create）。/admin/permissions 矩阵页：**8** 角色／**112** 模块组／**334** 码／**10** 条真实 SoD 职责分离冲突（complaint/transfer/refund/cardcancel 四模块「审批权 × 同模块提交权」组合，矩阵真实计算非摆设）。/admin/staff 员工 **13** 人；/admin/org 组织 **11** 节点、编制 **0**（诚实空态，不造数）。
- **E005（STORE_MGR+OPERATOR，只读三角）**：角色页成员 **0**（数据域空，与 curl `[]` 2B 对账）；「新建角色」按钮**不在 DOM**（v-if 门控）；新建抽屉仅常驻占位节点 w0/h0 不可见；三 view 页可进、写动作全收敛。
- **E002（FRONT_DESK，拦截三角）**：访问 /admin/roles 被路由守卫拦截跳 `/no-auth?from=/admin/roles&need=role:view`；左侧 nav「权限中台」菜单**整组隐藏**；证明前端菜单/路由/后端三层门控一致。
- 控制台与网络：四页 console **零 error、零 warn**（唯一发现为登录表单 3 个字段缺 id/name 属性的 a11y issue×3，非本批功能域，登记打磨项见 §5）；Network 面板五端点全部真实 200/403 响应，**无 mock 回退、无静态 json**。

## 5. 如实说明

### 5.1 六项历史包袱判定（均非阻断、非功能缺口，登记不施工）

1. **staff_role 4 行老码兼岗**（E011-E014 的 ROLE_REGION/ROLE_GROUP_* 码）：seed 历史数据，角色元素的 roleCode 已全 canonical，老码仅存于兼岗绑定行，读取链路兼容；主角色 43 行无老码。属数据层历史兼容，不影响四页功能与门控，登记待数据治理专项统一，本批不动。
2. **role-members 响应 Map 兼吐老码键**：为兼容①的历史兼岗行，成员聚合对老码键不丢弃，前端按并集正常呈现；契约刻意保留，非 bug。
3. **父角色继承树无后端列**：role_def 无 parent_id，权限恒一层（无继承）；「继承树」是早期 UI 文案/概念残留，当前页面未展示误导性树状继承（矩阵平铺），后续如做角色继承需独立设计，登记远期。
4. **36 个预留权限码**：permission_def 诚实预定义未引用码（ai*、:approve 等），矩阵中以灰 chip 呈现，供后续业务接线；不是脏数据。
5. **SELF/BRAND data_scope 就近映射**：历史角色 data_scope 枚举在个别查询处就近映射处理，行为正确，技术上属可收敛的小重复，登记技术债不施工。
6. **超管 `*` 不落库**：SUPER_ADMIN 全权限由前端 can() 与后端超管判定双通道补，role_permission 无 SUPER_ADMIN 行（686 行为七业务角色）；设计如此（避免通配落库后被覆写界面误改），非缺口。

### 5.2 打磨项（非阻断）

登录页表单 3 个字段缺少 id/name 属性，浏览器报可访问性（a11y）建议×3。不影响登录功能与本批 RBAC 域，登记前端打磨项，建议平台治理批随登录页统一补 a11y 属性（与 B50 卡5 全局壳四门控同域但不同事，不并入本批）。

### 5.3 测试数据还原

本批为**纯只读真验批**：curl 全部 GET/越权写均被 403/400 拦截未落库，PG 仅 SELECT，Chrome 仅页面浏览与表单显隐验证（未提交任何写操作）。**零写入、零测试残留，无还原项**；浏览器会话于真验后登出清理。

### 5.4 与台账旧登记的关系

04 L50/02 L110「缺前端管理页」为 M7/B19 后端落地时点的真实状态，属历史时点漂移而非误记；按铁律 9/10，02 L110 旧注原文保留不抹、尾接 B61 订正长注（同 L117 B26→B57→B59→B60 层层订正样板）；04 L50 按勾销样板加删除线＋闭合长注，不删除原行。

## 6. 进度落账

批末六册＋本 DELIVERY＋哨兵同一 docs 原子提交（docs/ 被 gitignore：已跟踪文件 `git add -u docs/`，本新文件 `git add -f docs/DELIVERY-P5-B61-2026-09-18.md`）：

- **04-backlog L50**：事项加删除线，依赖列改「**✅ P5-B61 已闭合（2026-09-18，详见 DELIVERY-P5-B61-2026-09-18）**：卡0 事实翻转——前端四页 3219 行＋store 609 行五真实端点无 mock＋router/nav 齐备＋RbacAdminController 全套，curl/PG/Chrome 三轨三角全绿（334 码/686 授权/48 兼岗、四角 200/403/401、三账号读写拦三角、零代码数字不变）」，末列改「✅ B61」。
- **02-modules L110**：尾列「角色管理页待补（Backlog）」原文保留，尾接「；**P5-B61 订正（2026-09-18，零代码纵深，旧注作为当时时点事实保留不抹）**：前端角色/权限矩阵/员工/组织四页 T1 共 3219 行＋t1Rbac.ts 609 行五真实端点＋router L170-175＋nav L353-362 权限中台分组＋后端 RbacAdminController 全套均真实存在，三轨三角实证（334/686/48、无 parent_id、四角门控），数字不变，详见 DELIVERY-P5-B61-2026-09-18」。
- **00-history**：L6 前插 B61「**最近更新**」块（零代码批：卡0 翻转、三轨证据、勾销 L50＋订正 L110、数字不变），现 L6 B60 块首改「上一批 B60：」前缀（内容全保留）。
- **01-dashboard**：L19 B60 bullet 前倒序插入 B61 bullet（数字格 ✅109/🔧1/⬜55 一字不动）；L40 域⑦ `| ⑦ | 组织与权限 | 11 | 0 | 13 |` 行尾接 B61 注（11/0/13 不变）。
- **03-timeline**：L91 B60 行后正序追加 B61 六列行（commit 列「零代码无 commit」，沿 B50 卡8b 写法先例；数字列列勾销 L50＋订正 L110、各项数字不变）。
- **索引 DEVELOPMENT-ROADMAP.md**：通读确认无「交付文档清单」区段，本批索引无需随动；**HANDOFF-AUTO.md**：STATUS 置 DORMANT、心跳 17:06 闭合、BATCH/CARD 换全绿结论、PREV 更新 B60 docs 锚 `6072b49`，人读区当前批次首条（B60 bullet）前加 B61 闭合 bullet，以标题锚 `## P5-B60 闭合存档（2026-09-18）` 为界在其前加「P5-B61 闭合存档」块。

## 7. 下一批

**B62 客户运营批**（04-backlog L69 自动降级引擎＋L70 会员权益真实联动＋L44 撞单合并）：卡0 只读侦察——L69 降级：B25 LevelMonthlyJob 只升不降现状、计算周期事实表/保护月/降级审计/KPI 口径缺口盘点；L70 权益：等级阈值/benefits 配置与订单计价、权益核销链路接线点；L44 撞单：客户查重/合并规则与 ES 融合检索（B30/B32/B48）复用面。按「一卡一 commit+push、前端样式零改只换数据源、三轨真验、测试数据还原」施工。

队列（用户四阶段授权第①阶段第二层近线）：B62→B63（L83-86 财务四行：通用异常中心/异常账务登记/预收合规监控/进项税）→B64（L87 M4 多级审批/L90 SOP 多模板/L91 随访分析/L75 EMR 模板集团运营）→B65 营销散项→B66 报表数据（R03-R09/R02 维度/毛利率/REVENUE 聚合/月结）→B67 测试体系（L88 造数工具化/L89 audit 保留选项/seed Flyway 专项）。近线全收后转第②阶段 **B68**：core 库 24 行营销演示残留仿 B57 备份＋物理删除；再第③阶段 **B69**：L133 REJECTED 回转、L140 transfer 渠道、L138 XLSX/PDF 选型（含竞品调研，按推荐拍板）；全部完成转第④阶段 **C 大阶段/专项开工**。哨兵本批末置 DORMANT，后续接力按「中断恢复指引」三条件核验（心跳停滞≥15min 无并发＋下一批用户已明确拍板＋夜间窗 22:00-06:00；B62 在「第二层近线可以全部开始」总授权内，接力会话核验哨兵后可直接开工）。
