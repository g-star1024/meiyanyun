# DELIVERY-P5-B53 · L141 URGENT 加急源 A3 诚实空态 + 清死码小卡（2026-09-16）

> **批次性质**：用户对 A/B/C/D 四方向正式拍板后的最小卡——A 方向（04-backlog L141 M1 调度 URGENT 加急标记源）选 **A3：不新增任何优先级字段、继续诚实空态，顺手清理前端零引用死 CSS**。全批仅卡1 一处单删一行死码 + 卡2 批末落账，无后端、无契约、无 schema、无页面视觉变更。
> **用户拍板（A 段原文照录）**：「A. L141 M1 调度 URGENT 加急标记源，选 A3 + 顺手清死 CSS，继续诚实空态，不提前造字段。」（同批另拍板 B=L144 排班真实化→P5-B54、C=L133 合并 L46 真实 impersonate→P5-B55、D=L123 报告哈希验真 UI→P5-B56，本卡均不触碰。）
> **提交清单**：style `fb16b53`（卡1 删除零引用死 CSS，1 file changed, 1 deletion，已 push origin/main `17251b7..fb16b53`）；卡2 零代码为本批 docs 原子提交。
> **铁律对齐**：0 先查再写（全站 `urgent` 52 命中逐一甄别、模板零引用核实）、-1 样式/现网保护（只删死码，模板/脚本零改）、7 三轨真验（构建+双栈产物 grep+HTTP 200）、8 一卡一 commit 紧跟 push、9/10 台账分册原子提交、11 哨兵 HANDOFF-AUTO 全程心跳。

## 1. 背景与记账口径

- **L141 登记实质**：M1 调度中心（B49 卡12 已切真）前端曾为「加急」作业预留红色左边框视觉钩子 `.job--urgent`，但 appointment / 工单**均无 priority/urgent 字段**，加急标记无数据源；B49 卡12 去 mock 时已删 `urgentJobs` 死脚本，仅剩这条孤儿 CSS。
- **A3 决策**：不提前造优先级字段（不臆造产品语义、不污染 schema/契约），调度页继续以真实派单状态诚实呈现；在「不造字段」前提下唯一可做的实功是删除这条全站零引用死 CSS。
- **L141 不勾销**：加急源字段缺口属**保留的产品决策**（无源诚实空态），故 04-backlog L141 行**保留登记、不打勾、不删除线**，仅在行尾追加 B53 注记说明「A3 拍板不造字段、仅清死码」；完成度数字一律不变（同 B44/B45/B50/B51/B52 纵深/还债先例）。

## 2. 卡1 影响面只读核实（删除前）

- 前端 `frontend/src/views/M1DispatchView.vue`：`.job--urgent { border-left: 3px solid var(--c-danger-fg); }` 位于 `<style scoped>`，模板（`<template>`）中**无任何** `job--urgent` class 绑定，纯孤儿规则。
- 全站 `urgent` 共 52 处命中，逐一甄别后确认其余全部属于**互不相关的独立功能**：T3 工单优先级、通知中心、交接事项、日结待办等业务各自的 urgent 语义——**严禁触碰**，本卡只删 M1DispatchView 这一条。
- `.job--active` 等相邻真实在用规则保留；删除为**精准单行删除**，不动上下文。

## 3. 卡1 代码改动（style `fb16b53`，1 file -1，已 push）

```diff
- .job--urgent { border-left: 3px solid var(--c-danger-fg); }
```

- 文件：`frontend/src/views/M1DispatchView.vue`（删除原第 246 行单行）。
- 模板（template）、脚本（script）零改动；无后端、无 API 契约、无 DB schema、无权限点、无中文文案变更。

## 4. 三轨真验（全绿）

1. **构建轨**：`pnpm build` exit 0（vite/vue-tsc 20.24s，0 error）；新产物 `M1DispatchView-B-BnlAuT.css`、`M1DispatchView-DfV4LoYr.js`。
2. **产物 grep 轨**：本地新 dist 中 `job--urgent` 计数 = **0**，且 `job--active` 仍在（证明精准单删、未误伤相邻规则）。
3. **双栈部署+HTTP 轨**：docker cp 为叠加复制（不清目标目录），首次部署后发现旧 hash 产物 `M1DispatchView-Cyk7km7L.css` 残留仍含死类；改为对两容器先 `rm -rf /usr/share/nginx/html/*` 再干净 cp，复测：
   - 正式栈容器 `meiyun-frontend`（http://localhost:8080）与 seed 栈 `meiyun-seed-frontend`（http://127.0.0.1:18080）各含 2 个 M1DispatchView 新 hash 产物、全 assets `grep job--urgent` 零命中；
   - 8080 / 18080 双前端 HTTP 均 **200**。
4. **提交轨**：`fb16b53` 已 push，`17251b7..fb16b53 main -> main`。

## 5. 如实说明

- 本卡是**纯死码清理**，不产生任何用户可见行为/视觉变化，亦不闭合加急源缺口；L141 的真实闭合仍有待未来「预约/工单优先级字段」经产品拍板后另卡接线。
- 未触碰其余 51 处 `urgent`（T3 工单/通知/交接/日结待办等独立功能）。
- 本卡无写操作、无 append-only 业务数据、无服务重启依赖（仅静态资源换载）。

## 6. 进度落账

**完成度数字不变**：✅ **108**/166=约65%、🔧 **1**、⬜ **56**（约34%）；域⑧ 34✅2🔧31⬜ 不变。本卡纯死码清理，新增完成模块 **0** 个，勾销 Backlog 登记行 **0** 行（L141 按 A3 保留登记仅注记）。

- **04-backlog**：L141 行尾追加 B53 A3 注记（不勾销、不删除线），末列标注「远期 M1（A3 诚实空态，B53 仅清死码）」。
- **03-timeline**：表底追加 P5-B53 六列行（`fb16b53` / 本文档 / 无新增·无跃迁模块）。
- **02-modules**：M1·调度中心行尾接 B53 死码清理纵深（明示纯死码、不影响模块状态）。
- **00-history**：顶部新增 B53 简报，B52 条降级「上一批 B52」。
- **01-dashboard**：L18 口径流水与 L36 域⑧行双尾接（数字不变口径说明）。

## 7. 下一批

- **P5-B54 排班真实化大批（6 卡，B=L144）**：org-service 新建 `staff_shift` 实体（JPA ddl-auto / meiyun_core，无 Flyway；唯一键 staff_id+shift_date，source=TEMPLATE/OVERRIDE）→ 浏览器 GET/PUT/POST generate-week 端点 + internal resolve（X-Internal-Token 软降级）→ txn DispatchService 派单/改期三处固定窗改排班真源+422 → M2 `/m2-schedule` 接真（模板/样式零改，5 态改名 上午/下午/全天/休息/请假，全员排班+周例+请假联动）→ M1 SLOTS/时间轴以后端为准动态化 → 批末落账。
- 其后 P5-B55（C=L133 合并 L46 真实 impersonate：JWT act/realSub 双 claim+审计 realSub·act+短时效切换 token/版本号回收+前端强制横幅，授权矩阵 超管→店长→员工、总部可跨店、门店员工不可跨店）、P5-B56（D=L123 报告哈希验真 UI：先冻结规范化字节（UTF-8 BOM+CRLF、文件名去下载时刻）→finance report_job 加 content_hash（SHA-256 落库原始字节）→验真端点+前端验真卡）。
