# M1 集团总部管控 · 页面详设（7 要素）

> 定位：多品牌、多区域、多门店连锁集团的统一管控与决策中枢。
> **画布**：ardot `713306548228181` · 页面 `66:1`（P-M1 集团总部管控）
> **审查日期**：2026-08-13 · 16/16 页已产出（Desktop+Tablet 双端），编号映射已对齐画布实际内容
> **合规状态**：金标准骨架全部通过；原 M1-08/09/11 内容错位已通过文档编号重映射解决（画布 66:560/66:655/66:736 实际分别为多租户管理/字段级权限矩阵/操作审计日志），M1-12 已补全（节点 204:1/204:119）
> 桌面侧栏统一 金标准深色侧栏（#14152B，M1 集团模块高亮）；KPI 行 4 列；主区双栏（左主 / 右辅）。

---

## M1-01 集团数据驾驶舱（✅ 已产 · 金标准通过）
**节点**：Desktop `66:157` · Tablet `66:240`

① **页面结构**：桌面 = 侧栏240 + 主区（顶栏64：面包屑「集团 / 驾驶舱」+ 日期范围切换 + 门店范围下拉；内容736 = 顶部 KPI 4列 + 下方双栏：左主 2/3 地图/趋势大图，右辅 1/3 预警/排行榜）；平板 = 顶部60 + 内容流（KPI 卡 ×4 横滑 + 地图卡 + 趋势卡 + 预警卡）+ 底部CTA「导出周报」。

② **展示内容**：全国营业额/客流/成交/复购 4 KPI；门店地图热力；集团近 30 日趋势；异常门店预警榜；品牌/区域排行；目标完成进度环。

③ **核心功能**：【图表】多维趋势 + 地图热力；【筛选】时间范围（日/周/月）、品牌、区域、门店；【其他】预警钻取、报表导出。

④ **实现方式**：复用 `ref 3:18` 侧栏；KPI 卡圆角12+描边#EBEBF2，数字24 Bold；地图用品牌蓝 #6B8AFF 热力 + 异常点 #FF6B9D 脉冲；数据经 `T2-04 数据服务` 取数；范围受集团级 RBAC（仅看授权品牌/区域）。

⑤ **交互功能**：时间范围切换实时刷新；点击异常门店 → 钻取 `M1-02 门店矩阵`；点击排行门店 → 客户画像 `M3-01`；一键导出 PPT/PDF。

⑥ **达成效果**：集团总经理解锁「一屏看全集团」，异常门店 30 秒内定位，决策有实时数据支撑。

⑦ **页面联动**：上游=`T2 数据中台` 汇聚各店；下游=`M1-02`（门店钻取）、`M1-03`（对比）、`M1-13`（周月报生成）、`M1-12`（审计）。横切=`A1-05 经营日报` 自动推送同口径摘要。

---

## M1-02 门店矩阵总览（✅ 已产 · 金标准通过）
**节点**：Desktop `66:316` · Tablet `66:394`

① **页面结构**：顶栏（区域/品牌/状态筛选 + 搜索）；内容 = 上「全国地图/行政区热力」+ 下「门店卡片网格（自动换行，每卡含营业状态徽标/今日业绩/在店客流/异常标记）」；底栏状态条「共 N 家 · 营业 N · 异常 N」。

② **展示内容**：门店卡片（店名、品牌、区域、状态点、今日业绩、客流、异常标签如「客流异常」「证照临期」）；地图按区域聚合营业健康度。

③ **核心功能**：【列表】门店卡片网格；【筛选】区域/品牌/状态/关键词；【其他】状态钻取、异常一键派单、营业/闭店切换。

④ **实现方式**：状态点用 Token（营业青#2ED4BF / 闭店灰 / 异常粉#FF6B9D）；卡片圆角12+描边；切换营业状态写 `T1-04` 审计；派单触发 `T3-02 工单中心`。

⑤ **交互功能**：点击门店卡 → 右侧抽屉展开门店详情（基础信息/今日快照/异常列表）；右上「派单」→ 弹层选接收人 + 工单类型；状态切换二次确认。

⑥ **达成效果**：异常门店 30 秒定位并派单，集团运营一眼掌握所有门店健康度。

⑦ **页面联动**：上游=`M1-04 门店管理` 基础信息；下游=`T3-02 工单`（派单）、`M2-06 门店日报`（单店复盘）；异常标记回写 `M1-12 审计`。

---

## M1-03 多店业绩对比（✅ 已产 · 金标准通过）
**节点**：Desktop `66:422` · Tablet `66:489`

① **页面结构**：顶栏（对比维度选择 + 门店多选器）；内容 = 左主「多门店 KPI 横向对比表（可排序）」+ 右辅「同环比柱状/折线 + 标杆店标注」；支持拖拽列调整对比维度。

② **展示内容**：门店维度对比（业绩/客流/客单价/复购/耗卡）；同环比；标杆店高亮；差距归因下钻。

③ **核心功能**：【图表】柱状+折线组合；【筛选】门店组、维度、周期；【其他】自定义对比组、标杆标注、差距归因下钻。

④ **实现方式**：图表卡圆角12；标杆店用金#FFCB47 高亮；对比数据经 `T2-04`；门店组配置存 `T2` 数据集市。

⑤ **交互功能**：拖拽维度列重排；点击某店某指标 → 下钻该店时段趋势；「设标杆」→ 弹层选基准店；导出对比图。

⑥ **达成效果**：识别高/低绩效门店，支撑标杆复制与帮扶策略。

⑦ **页面联动**：上游=`M6-01 业绩报表`；下游=`M1-06 区域看板`（区域聚合）、`M1-13 周月报`（收录对比结论）。

---

## M1-04 品牌 / 门店管理（✅ 已产 · 金标准通过）
**节点**：Desktop `94:1` · Tablet `94:59`

① **页面结构**：桌面双栏 = 左「品牌树 + 门店列表（树形展开）」+ 右「选中门店详情抽屉/面板（基础信息/证照/营业时间/负责人）」；顶栏品牌切换 + 新增门店按钮；平板 = 列表卡 + 点进去详情卡 + 底部CTA「新增/编辑」。

② **展示内容**：品牌树（多品牌层级）；门店列表（名称/品牌/区域/状态/负责人）；门店详情（地址、证照列表及到期日、营业时间、 seating 容量、负责人）；证照临期预警。

③ **核心功能**：【列表】品牌-门店树；【表单】门店新增/编辑表单（基础信息、证照上传、营业时间）；【筛选】品牌/区域/状态；【其他】门店启停、证照到期提醒。

④ **实现方式**：树用侧栏同级组件；证照到期用金#FFCB47 预警徽标；新增/编辑走抽屉式表单（圆角12）；启停写 `T1-04` 审计 + 影响 `M1-02` 状态；证照 OCR/到期计算服务端。

⑤ **交互功能**：树节点点击展开门店；列表行点击 → 右侧详情抽屉；「编辑」→ 抽屉内嵌表单，实时校验必填；证照到期日历视图悬浮提示。

⑥ **达成效果**：集团资产台账统一，证照合规零遗漏，避免无证经营风险。

⑦ **页面联动**：上游=`M1-05 组织架构`（归属区域/负责人）；下游=`M1-02`（状态同步）、`M2-21 门店设置`（单店参数）、`M1-12`（变更审计）。

---

## M1-05 组织架构与权限（✅ 已产 · 结构合规 · 截图渲染异常）
**节点**：Desktop `94:286` · Tablet `94:380`

① **页面结构**：桌面三栏 = 左「集团-区域-门店三级组织树」+ 中「选中节点下的岗位/角色矩阵」+ 右「成员列表 + 角色授权面板（数据范围/功能权限）」；顶栏组织筛选 + 新增节点。

② **展示内容：组织树节点详情；岗位×角色矩阵（功能权限勾选 + 数据范围如「本店/本区域/全集团」）；成员卡片（姓名/岗位/状态）；权限冲突提示。

③ **核心功能**：【表单】组织节点增删、角色授权（功能权限树 + 数据范围选择）；【列表】成员列表；【其他】成员调动、权限冲突检测。

④ **实现方式**：权限矩阵复用 `T1-02 权限矩阵` 组件；字段级 RBAC 落地（功能权限 + 数据范围）；所有授权写 `T1-04` append-only 审计；敏感授权需 `T3-01 审批中心` 审批。

⑤ **交互功能**：组织树拖拽调整归属；矩阵勾选实时高亮影响范围；数据范围下拉（本店/本区域/全集团/自定义）；成员调动弹层选目标节点。

⑥ **达成效果**：权限收口到角色，跨店数据隔离可控，满足等保三级权限审计。

⑦ **页面联动**：上游=`T1-03 组织架构`（中台同源）；下游=`T1-01 角色管理`、`T1-02 权限矩阵`、`T1-04 审计`；横切=全站数据范围受其约束（如 `M1-01` 仅看授权范围）。

---

## M1-06 区域运营看板（✅ 已产 · 金标准通过）
**节点**：Desktop `94:465` · Tablet `94:559`

① **页面结构**：顶栏（区域选择 + 周期）；内容 = KPI 4列（到店/成交/复发/投诉）+ 双栏（左主 区域指标趋势，右辅 区域负责人排行 + 赛马榜）。

② **展示内容**：按区域聚合的运营指标（到店/成交/复发率/投诉率）；区域负责人绩效排行；区域运营报告入口。

③ **核心功能**：【图表】区域趋势 + 排行条形；【筛选】区域、周期；【其他】负责人绩效对比、报告导出。

④ **实现方式**：排行用品牌蓝#6B8AFF 进度条；数据经 `T2-04`；范围受区域 RBAC。

⑤ **交互功能**：区域切换刷新；点击负责人 → 成员详情（关联 `M1-05`）；导出区域运营报告（PDF）。

⑥ **达成效果**：区域总经理拥有专属作战看板，驱动区域赛马与帮扶。

⑦ **页面联动**：上游=`M1-03`（门店对比聚合）、`M6` 业绩；下游=`M1-13 周月报`、`M1-12 审计`。

---

## M1-07 跨店调配调度（✅ 已产 · 金标准通过）
**节点**：Desktop `94:645` · Tablet `94:739`

① **页面结构**：顶栏（调配类型：专家/设备/物料）；内容 = 左主「需求-供给匹配视图（甘特/地图）」+ 右辅「调配工单流（待审/进行中/已完成）」；底栏「发起调配」CTA。

② **展示内容**：专家/设备/物料的跨店需求与供给；匹配建议；调配工单状态流；到店确认记录。

③ **核心功能**：【流程】发起调配 → 审批流 → 到店确认 → 记录；【列表】工单流；【表单】调配申请单。

④ **实现方式**：工单流复用 `T3-02 工单中心`；审批走 `T3-01`；涉及资产转移联动 `M4-19`；写 `T1-04` 审计。

⑤ **交互功能**：拖拽需求到供给方发起匹配；工单状态实时推进；到店扫码确认。

⑥ **达成效果**：稀缺专家/设备利用率最大化，客户不跨城流失。

⑦ **页面联动**：上游=`M2-05 设备`/`M3 客户`需求；下游=`T3-02 工单`、`M4-19 资产转移`、`M2-13 交接班`。

---

## M1-08 多租户管理（集团租户隔离策略）（✅ 已产 · 金标准通过）
**节点**：Desktop `66:560` · Tablet `66:617`
> **编号对齐说明**：画布此屏实际展示多租户隔离管理（原规划「集团会员体系」未落地，已移至文末规划区）。本文档按画布实际内容对齐编号。

① **页面结构**：顶栏（租户状态筛选 + 品牌筛选 + 「新增租户」）；内容 = 左主「租户卡片网格（租户名/品牌/区域/状态点/隔离策略/到期日/资源配额）」+ 右辅「选中租户详情（数据隔离策略配置/数据范围/资源配额/合规标记）」；底栏「新增租户」。

② **展示内容**：多租户列表（集团下各品牌/区域主体的租户）；隔离策略（数据/权限/资源）；配额与到期；合规标记；异常（配额超限/证照临期）。

③ **核心功能**：【列表】租户卡片网格；【表单】租户新增/编辑（隔离策略、配额、数据范围）；【筛选】状态/品牌/区域；【其他】租户启停、到期预警。

④ **实现方式**：隔离策略写 `T1-04` 审计；数据范围受集团级 RBAC；配额经 `T2` 资源服务；状态点用 Token（营业青#2ED4BF / 异常粉#FF6B9D）；到期用金#FFCB47 预警徽标。

⑤ **交互功能**：卡片点击 → 右侧详情抽屉；「新增/编辑」→ 抽屉内嵌表单实时校验；启停二次确认；配额条形可视化。

⑥ **达成效果**：集团多品牌多主体合规隔离，避免数据越权与资源争抢。

⑦ **页面联动**：上游=`T1-03 组织架构`（中台同源）；下游=`M1-04 门店归属`、`M1-12 审计`；横切=全站数据范围受其约束。

---

## M1-09 字段级权限矩阵（角色权限配置）（✅ 已产 · 金标准通过）
**节点**：Desktop `66:655` · Tablet `66:708`
> **编号对齐说明**：画布此屏实际展示字段级 RBAC 权限矩阵（原规划「加盟商/合伙人管理」未落地，已移至文末规划区）。本文档按画布实际内容对齐编号。

① **页面结构**：桌面三栏 = 左「角色树（集团/区域/门店角色层级）」+ 中「功能权限矩阵（模块×操作 勾选，如 查看/编辑/导出/双签/审批）」+ 右「字段级数据范围（本店/本区域/全集团/自定义）」；顶栏角色筛选 + 「新增角色」。

② **展示内容**：角色树节点；功能权限勾选矩阵（按模块聚合）；字段级数据范围配置；权限冲突/越权提示；敏感操作（双签/审批）高亮。

③ **核心功能**：【表单】角色授权（功能权限树 + 字段级数据范围）；【列表】角色列表；【其他】角色克隆、权限冲突检测、敏感授权审批触发。

④ **实现方式**：权限矩阵复用 `T1-02 权限矩阵` 组件；字段级 RBAC 落地（功能权限 + 数据范围）；所有授权写 `T1-04` append-only 审计；敏感授权（如跨店数据、双签配置）需 `T3-01 审批中心` 审批。

⑤ **交互功能**：角色树点击展开；矩阵勾选实时高亮影响范围；数据范围下拉（本店/本区域/全集团/自定义）；冲突提示标红；克隆快速建角色。

⑥ **达成效果**：权限收口到角色，跨店数据隔离可控，满足等保三级权限审计。

⑦ **页面联动**：上游=`T1-01 角色管理`、`T1-02 权限矩阵`（中台同源）；下游=`T1-04 审计`；横切=全站数据范围受其约束（如 `M1-01` 仅看授权范围）。

---

## M1-10 供应链统采 ★增强（✅ 已产 · 金标准通过）
**节点**：Desktop `94:1185` · Tablet `94:1279`

① **页面结构**：顶栏（目录/订单/分拨 Tab）；内容 = 左主「集采目录 + 统采订单列表」+ 右辅「到货与分拨状态 + 价格对标」；底栏「发起统采」。

② **展示内容**：集采目录、统采订单、到货与分拨、供应商绩效、价格对标。

③ **核心功能**：【表单】统采下单、分店分拨配置；【列表】订单/到货；【图表】价格对标；【其他】供应商绩效。

④ **实现方式**：统采单经 `T3-01 审批`；分拨联动 `M2-11 物料申领`/`M2-02 库存`；价格对标经 `T2-04`；写 `T1-04`。

⑤ **交互功能**：目录选品加入统采单；分拨比例可视化分配；供应商评分卡。

⑥ **达成效果**：集团集采降本，门店耗材成本可追踪。

⑦ **页面联动**：上游=`M2-02 库存`（缺货触发统采）；下游=`M2-11 申领`、`M2-12 报损`、`M6-06 成本`。

---

## M1-11 操作审计日志（WORM 审计视图）（✅ 已产 · 金标准通过）
**节点**：Desktop `66:736` · Tablet `66:814`
> **编号对齐说明**：画布此屏实际展示 WORM 操作审计日志（原规划「合并财务报表」归入 M6 财务域，不单列）。本文档按画布实际内容对齐编号。

① **页面结构**：顶栏（日志类型/时间/操作人/关键词筛选）；内容 = 左主「操作日志表格（时间/操作人/模块/动作/结果/门店）」+ 右辅「敏感操作审计流（双签/退卡/划扣）+ 合规项清单 + 告警」；底栏「导出证据链」。

② **展示内容**：全量操作日志；敏感操作（双签/退卡/划扣/调权限）审计流；合规项清单（等保/证照/EMR）；异常告警。

③ **核心功能**：【列表】日志检索（多条件）；【筛选】类型/时间/操作人/门店；【其他】异常告警、合规项跟踪、导出证据链。

④ **实现方式**：日志源 `T1-04`（append-only，不可改删）；双签/退卡/划扣记录关联 `M4-14`/`M4-16`；导出生成防篡改哈希证据链；等保三级检索留痕。

⑤ **交互功能**：日志行 → 详情抽屉（含前后值 diff）；敏感流时间轴钻取；合规项打勾跟踪；一键导出 ZIP 证据包。

⑥ **达成效果**：满足医疗行业审计与等保 2.0，事故全可追溯、可举证。

⑦ **页面联动**：上游=全站写操作（`T1-04` 汇聚）；横切=所有敏感页（M4-14/16/17/19/20、M2-12、M1-09）均回流此页；下游=`M1-12 审计与合规`（合规总览）。

---

## M1-12 审计与合规（✅ 已产 · 金标准通过）
**节点**：Desktop `204:1` · Tablet `204:119`

① **页面结构**：顶栏（日志类型/时间/关键词筛选 + 模块筛选 + 时间筛选）；内容 = KPI 4列（今日操作/预警/异常/合规率）+ 左主「WORM 存证说明卡 + 审计日志总览表格（示例：张敏导出成功/王磊退款双签/系统修改待审）+ 分页」+ 右辅「敏感操作审计流（双签退卡/大额划扣/权限变更）+ 合规项清单（等保三级 4 项 checklist）」；底栏「导出审计包」粉色 CTA。

② **展示内容**：全量操作日志；敏感操作（双签/退卡/划扣/调权限）审计流；合规项清单（等保/证照/EMR）；异常告警。

③ **核心功能**：【列表】日志检索（多条件）；【筛选】类型/时间/操作人/门店；【其他】异常告警、合规项跟踪、导出证据链。

④ **实现方式**：日志源 `T1-04`（append-only，不可改删）；双签/退卡/划扣记录关联 `M4-14`/`M4-16`；导出生成防篡改哈希证据链；等保三级检索留痕。

⑤ **交互功能**：日志行 → 详情抽屉（含前后值 diff）；敏感流时间轴钻取；合规项打勾跟踪；一键导出 ZIP 证据包。

⑥ **达成效果**：满足医疗行业审计与等保 2.0，事故全可追溯、可举证。

⑦ **页面联动**：上游=全站写操作（`T1-04` 汇聚）；横切=所有敏感页（M4-14/16/17/19/20、M2-12、M1-05）均回流此页。

---

## M1-13 经营周报 / 月报（✅ 已产 · 金标准通过）
**节点**：Desktop `134:2` · Tablet `134:78`

① **页面结构**：顶栏（周期选择 + 模板切换 + 「生成」）；内容 = 自动生成的报告视图（指标卡 + 图表 + 结论文字）+ 订阅推送配置卡；底栏「推送/归档」。

② **展示内容**：自动生成集团经营报告（核心指标/图表/结论建议）；订阅与推送状态；历史归档列表。

③ **核心功能**：【表单】报告模板配置、定时生成、多渠道推送；【列表】历史归档；【其他】订阅管理。

④ **实现方式**：模板引擎服务端；数据经 `T2-04`；推送走 `T3-03 消息`（企微/邮件）；生成动作写 `T1-04`；结论可引用 `A1-05` 生成。

⑤ **交互功能**：模板可视化配置；定时规则设置；一键推送 + 归档；历史版本对比。

⑥ **达成效果**：管理层免手工报表，决策有数据支撑。

⑦ **页面联动**：上游=`M1-01/03/06`、`M6` 报表；下游=`T3-03 推送`、`M1-12 审计`；横切=`A1-05 经营日报` 同引擎。

---

## M1-14 营销资源中台 ★增强（✅ 已产 · 金标准通过）
**节点**：Desktop `134:116` · Tablet `134:197`

① **页面结构**：顶栏（素材/模板/券池 Tab）；内容 = 左主「集团统一营销素材 + 活动模板库」+ 右辅「下发到店进度 + 效果回收」；底栏「分发」。

② **展示内容**：集团统一营销素材/活动模板/券池；下发到店进度；效果回收。

③ **核心功能**：【列表】素材/模板；【表单】分发配置（选门店/时段）；【图表】效果回收；【其他】券池统管。

④ **实现方式**：素材复用 `M5-13`；券池联动 `M5-02`；分发经 `T3-03`；效果经 `T2-04`；写 `T1-04`。

⑤ **交互功能**：模板一键复用下发；分发进度条；效果漏斗回收。

⑥ **达成效果**：营销资产集团复用，门店执行标准化。

⑦ **页面联动**：上游=`M5-13 素材`/`M5-02 券`；下游=`M5`（门店活动）、`M2-16 拓客`；横切=`A1-10 内容生成`。

---

## M1-15 数据大屏（展厅）★远期（✅ 已产 · 暗色无侧栏模式正确）
**节点**：Desktop `134:253` · Tablet `134:312`

① **页面结构**：全屏模式（无侧栏）；轮播式大屏（业绩数字 + 全国地图 + 荣誉墙 + 实时滚动）；右下「模式切换/脱敏」浮动控件。

② **展示内容**：面向投资人/参观的实时数据大屏（集团业绩/门店地图/荣誉/排名）。

③ **核心功能**：【图表】实时大屏；【其他】大屏模式切换、数据脱敏、轮播。

④ **实现方式**：数据经 `T2-04` 实时流；脱敏规则受 `A1-17 隐私合规` + 展厅模式 RBAC；不暴露客户级明细。

⑤ **交互功能**：自动轮播；手动切屏；脱敏开关实时生效。

⑥ **达成效果**：品牌实力的可视化展厅资产。

⑦ **页面联动**：上游=`M1-01`（同口径指标）；横切=`A1-17 隐私合规`（脱敏兜底）。

---

## M1-16 系统设置（✅ 已产 · 金标准通过）
**节点**：Desktop `94:1545` · Tablet `94:1639`

① **页面结构**：桌面双栏 = 左「设置分组导航（参数树）」+ 右「选中分组配置表单」；顶栏「保存」；平板 = 分组卡列表 + 配置卡 + 底部CTA「保存」。

② **展示内容**：集团级参数（营业时间模板/品牌色/通知渠道/数据范围/环境切换）。

③ **核心功能**：【表单】全局配置表单（分组展开）；【其他】环境切换、操作留痕。

④ **实现方式**：表单复用样板输入组件（圆角8/描边）；环境切换需 `T3-01` 审批；所有保存写 `T1-04`；影响范围提示条（如「将影响全部门店」）。

⑤ **交互功能**：分组树切换；必填/格式实时校验；影响范围前置提示；保存二次确认。

⑥ **达成效果**：集团配置统一收口，避免各店各自为政。

⑦ **页面联动**：下游=全站（参数生效）；横切=`T1`（配置存储）、`T1-04 审计`、`M2-21 门店设置`（单店覆盖集团默认）。

---

## 📋 规划中页面（画布暂未包含，待增强/远期设计）

> 以下 3 个页面为 M1 原始规划页，因画布 M1-08/09/11 位置已分配给出租户管理 / 字段级权限矩阵 / 操作审计日志，故未单独建屏。规划意图保留如下，建议重新归属，避免功能遗漏：

- **集团会员体系**（等级权益 + 跨店消费轨迹）→ 建议归入 **M3 客户域**（M3-03 等级体系 / M3-04 积分），M1 不再单列。
- **加盟商 / 合伙人管理**（签约 + 分成 + 保证金 + 考核）→ 建议归入 **M6 财务域**（M6-05 分账 / M4-20 合同），M1 不再单列。
- **合并财务报表**（三大报表 + 抵消明细）→ 建议归入 **M6 财务域**（M6 已有报表体系），经 `T3` Outbox 镜像金蝶/用友，**仅镜像不碰资金**；M1 不再单列。
