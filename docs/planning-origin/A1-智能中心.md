# A1 AI 中心 · 页面详设（7 要素）

> 定位：AI 能力总入口与运营后台。已产 1 页（A1-01 智能中心旗舰屏，双端）；未产 23 页。本文件对 A1-01~24 全给 7 要素详设。
> **AI 红线**：隐私合规 `A1-17`（脱敏/本地隔离/等保）、敏感词 `A1-04` 实时拦截、模型可解释可审计、动作受控（审批/灰度）。

---

## A1-01 AI 智能中心（✓ 已产 · 双端）

① **页面结构**：顶栏（能力入口切换）；内容 = 能力卡片网格（画像/复购/话术/日报/分群/敏感词…各 AI 能力入口）+ 全局效果概览 KPI + 待办（审批/告警）；平板 = 能力卡 + KPI 卡 + 底部CTA「查看待办」。

② **展示内容**：AI 能力矩阵入口；全局调用/效果概览；待办（模型审批/告警/效果复核）。

③ **核心功能**：【列表】能力卡片；【图表】全局效果；【其他】入口跳转、待办处理。

④ **实现方式**：能力卡片用紫#8B5CF6 tint（#F1ECFF）；待办联动 `A1-14 审批`/`A1-20 告警`；调用经 `T4` 中台；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：卡片 → 跳对应 AI 页；效果概览下钻；待办一键处理。

⑥ **达成效果**：AI 能力一屏入口，运营可观测。

⑦ **页面联动**：下游=全 A1-02~24 各页；横切=`T4 中台`、`A1-17 隐私`、`M1-12 审计`。

---

## A1-02 客户画像引擎（未产）

① **页面结构**：顶栏（客户/模型选择）；内容 = 画像模型输出卡（标签/分群/价值分）+ 特征权重解释 + 效果回看；底栏「应用到分群」。

② **展示内容**：画像模型输出（标签/分群/价值分）；特征权重（可解释）；历史效果。

③ **核心功能**：【图表】特征权重/分群；【其他】模型预览、效果回看、一键应用。

④ **实现方式**：模型 `T4`；输出落 `T2-03` 标签；可解释性（SHAP/权重）；隐私 `A1-17`；写 `T1-04`。

⑤ **交互功能**：特征权重钻取；分群预览；效果曲线回看；一键应用到 `M3-13`/`M3-05`。

⑥ **达成效果**：画像可解释，顾问敢用。

⑦ **页面联动**：上游=`T2 数据`、`T4 模型`；下游=`M3-05 标签`、`M3-13 分群`、`M3-01 画像`；横切=`A1-17`（隐私）。

---

## A1-03 复购预测（未产）

① **页面结构**：顶栏（周期 + 项目筛选）；内容 = 复购概率榜（客户/项目/时机）+ 推荐时机卡 + 任务下发；底栏「批量建任务」。

② **展示内容**：复购概率榜（客户/项目/推荐时机/概率）；推荐依据。

③ **核心功能**：【图表】概率榜；【其他】预测圈选、任务下发、转化回收。

④ **实现方式**：模型 `T4`/`A1-09`；圈选 `T2-03`；任务 `M3-07`/`T3-02`；任务 `M5-03`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：榜排序；推荐时机钻取；一键建跟进/推送任务；转化回看。

⑥ **达成效果**：复购前置触发，LTV 提升。

⑦ **页面联动**：上游=`T4`、`M3-06 旅程`；下游=`M3-07 跟进`、`M4-18 复诊`、`M5-03 推送`；横切=`A1-09`。

---

## A1-04 敏感词检测（未产）

① **页面结构**：顶栏（渠道筛选 + 「词库管理」）；内容 = 命中记录流（咨询/话术/推送文案）+ 拦截统计 + 词库卡；底栏「编辑词库」。

② **展示内容**：敏感词命中记录（渠道/上下文/处置）；拦截统计；词库（分级/同义词）。

③ **核心功能**：【列表】命中记录；【表单】词库管理；【其他】实时拦截、误报标注。

④ **实现方式**：实时拦截嵌入咨询/话术/推送（`M4-09`/`M5-03`/`M3-08`）；词库 `T2`；误报标注回流训练 `A1-13`；写 `T1-04`；合规受红线。

⑤ **交互功能**：命中记录钻取上下文；词库分级编辑；同义词批量；误报标注。

⑥ **达成效果**：合规话术兜底，降舆情风险。

⑦ **页面联动**：上游=`M4-09 咨询`、`M5-03 推送`、`M3-08 关怀`；下游=`A1-13 训练数据`（误报回流）、`M1-12 审计`；横切=`T4`（模型）。

---

## A1-05 经营日报（未产）

① **页面结构**：顶栏（周期 + 「生成」+ 订阅）；内容 = AI 经营摘要卡（指标/异常/建议）+ 推送状态；底栏「推送/归档」。

② **展示内容**：AI 生成每日摘要（核心指标/异常点/行动建议）；推送与订阅状态。

③ **核心功能**：【图表】摘要图表；【表单】模板/订阅；【其他】定时生成、推送。

④ **实现方式**：生成引用 `T2-04` + 摘要模型 `T4`；同源 `M1-13`/`M2-06`/`M2-20`；推送 `T3-03`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：生成自动填充；建议可采纳（→ 任务）；订阅管理；历史对比。

⑥ **达成效果**：管理层每日 3 分钟掌握全局。

⑦ **页面联动**：上游=`M6-01`、`M1-01`、`M2-06`；下游=`T3-03 推送`、`M1-13 周月报`、`M3-07 任务`；横切=`T4`。

---

## A1-06 智能话术（未产）

① **页面结构**：顶栏（场景筛选：破冰/升单/异议）；内容 = 话术库卡（场景/效果评分）+ 插入预览 + 反馈；底栏「插入咨询工作台」。

② **展示内容**：场景化话术库（破冰/升单/异议处理）；效果评分；插入预览。

③ **核心功能**：【列表】话术库；【其他】话术推荐、效果反馈、插入 `M4-09`。

④ **实现方式**：话术 `T4`/`A1-11 知识库`；推荐嵌入 `M4-09`（实时）；合规 `A1-04` 过滤；反馈回流 `A1-13`；写 `T1-04`。

⑤ **交互功能**：场景筛选；话术卡片预览；「插入」→ `M4-09` 沟通框；效果评分反馈。

⑥ **达成效果**：新人即战力，升单率提升。

⑦ **页面联动**：上游=`A1-11 知识库`、`M4-09 咨询`；下游=`M4-09`（插入）、`A1-13 训练`（反馈）；横切=`A1-04`（合规）。

---

## A1-07 AI 客服 ★增强（未产）

① **页面结构**：顶栏（会话筛选 + 人工/AI 切换）；内容 = AI 接待会话流（意图/回复/知识命中）+ 转人工按钮 + 知识命中卡；底栏「转人工」。

② **展示内容**：AI 接待会话；意图识别；知识命中；转人工记录。

③ **核心功能**：【其他】会话托管、意图识别、人工协同；【列表】会话列表。

④ **实现方式**：会话 `T4`/`A1-11`；转人工 `M4-09`/`T3-03`；合规 `A1-04`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：会话流查看；一键转人工（带上下文）；知识命中钻取；满意度评价。

⑥ **达成效果**：夜间/高峰咨询不漏接。

⑦ **页面联动**：上游=`A1-11 知识库`；下游=`M4-09 咨询`、`M3-07 跟进`、`T3-03 通知`；横切=`A1-04`（合规）。

---

## A1-08 智能排班 ★增强（未产）

① **页面结构**：顶栏（周期 + 「生成建议」）；内容 = 基于预测的排班建议甘特 + 人力成本模拟卡 + 对比（现排/建议）；底栏「采纳并微调」。

② **展示内容**：排班建议（基于客流预测）；人力成本模拟；与原排班对比。

③ **核心功能**：【图表】排班甘特；【表单】人工微调；【其他】成本对比、采纳。

④ **实现方式**：预测 `T4`/`T2`；建议回填 `M2-03`；成本联动 `M6`（工时）；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：建议甘特查看；人工拖拽微调；成本实时对比；采纳 → `M2-03`。

⑥ **达成效果**：人力成本与体验平衡。

⑦ **页面联动**：上游=`T2 预测`、`M2-03 排班`；下游=`M2-03 排班`、`M6-08 提成`；横切=`T4`。

---

## A1-09 流失预警模型（未产）

① **页面结构**：顶栏（风险等级筛选）；内容 = 流失模型分榜（客户/因子/分）+ 干预建议卡 + 任务联动；底栏「联动 M3-09」。

② **展示内容**：流失模型分（客户/关键因子/分）；干预建议；与 `M3-09` 联动。

③ **核心功能**：【图表】因子/分；【其他】模型预览、任务联动 `M3-09`。

④ **实现方式**：模型 `T4`；输出落 `M3-09`；因子可解释；隐私 `A1-17`；写 `T1-04`。

⑤ **交互功能**：因子钻取；模型分排序；一键联动 `M3-09` 干预。

⑥ **达成效果**：流失可预测可干预。

⑦ **页面联动**：上游=`T4`、`M3-06 旅程`；下游=`M3-09 流失`、`M2-17 唤醒`、`M3-07 跟进`；横切=`A1-17`。

---

## A1-10 内容生成 ★增强（未产）

① **页面结构**：顶栏（「生成」+ 类型：文案/海报文案）；内容 = 生成历史卡 + 预览编辑区 + 合规过滤状态；底栏「一键下发 M5」。

② **展示内容**：营销文案/海报文案生成；预览与编辑；合规过滤状态。

③ **核心功能**：【表单】主题生成；【其他】合规过滤（`A1-04`）、一键下发 `M5`。

④ **实现方式**：生成 `T4`；合规 `A1-04` 过滤；下发 `M5-01/05/13`/`T3-03`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：主题输入生成；多版本选择；合规状态标色；一键下发。

⑥ **达成效果**：内容生产提效。

⑦ **页面联动**：上游=`M5-13 素材`、`A1-11 知识`；下游=`M5-01/05/08/09`、`T3-03 推送`；横切=`A1-04`（合规）。

---

## A1-11 知识库（未产）

① **页面结构**：顶栏（搜索 + 「录入知识」）；内容 = 知识检索卡（项目/方案/法规）+ 维护列表（向量化状态）；底栏「录入」。

② **展示内容**：企业知识（项目/方案/法规）检索与维护；向量化状态。

③ **核心功能**：【列表】知识列表；【表单】知识录入；【其他】向量化、引用溯源。

④ **实现方式**：存储 S3/`T4` 向量库；检索 `T2`；溯源 `T1-04`；合规 `A1-17`；联动 `A1-06/07/10`。

⑤ **交互功能**：全文检索高亮；知识录入（结构化）；向量化状态查看；引用溯源。

⑥ **达成效果**：AI 回答有据，降幻觉。

⑦ **页面联动**：下游=`A1-06 话术`、`A1-07 客服`、`A1-10 生成`、`A1-04 合规`；横切=`T4`（向量）。

---

## A1-12 模型管理 / A1-13 训练数据（未产）

① **页面结构**：顶栏（模块切换 模型/数据）；内容 = 模型版本列表（状态/指标）+ 训练数据集卡（标注/质量）+ 评估；底栏「发布/再训练」。

② **展示内容**：模型版本（基础/微调/状态/指标）；训练数据集（规模/标注/质量）；评估指标。

③ **核心功能**：【表单】版本发布、数据标注；【其他】再训练、评估。

④ **实现方式**：模型仓 `T4-01`；训练 `T4-03`/`T2`；评估 `A1-15`；误报/反馈回流（`A1-04/06`）；写 `T1-04`；隐私 `A1-17`（数据隔离）。

⑤ **交互功能**：版本对比；数据标注界面；再训练触发；评估看板。

⑥ **达成效果**：模型持续迭代闭环。

⑦ **页面联动**：上游=`A1-04/06 反馈`、`T2 数据`；下游=`T4 中台`、`A1-15 评估`；横切=`A1-17`（数据合规）。

---

## A1-14 审批流 / A1-15 效果评估 / A1-16 A/B 测试（未产）

① **页面结构**：顶栏（模块切换 审批/评估/实验）；内容 = 审批待办（AI 动作）+ 效果归因卡 + 实验对比（A/B 指标）。

② **展示内容**：AI 动作审批待办；效果归因（曝光/转化/ROI）；A/B 实验对比。

③ **核心功能**：【流程】AI 动作审批；【图表】效果归因、实验对比；【其他】灰度发布。

④ **实现方式**：审批 `T3-01`（AI 动作受控）；归因 `T2-04`；实验 `T4`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：审批通过/驳回（带理由）；效果下钻；实验指标对比；灰度发布。

⑥ **达成效果**：AI 动作受控、可证有效。

⑦ **页面联动**：上游=全 A1 动作；下游=`T3-01 审批`、`T4 中台`、`M5-14 看板`；横切=`A1-17`。

---

## A1-17 隐私合规（未产）

① **页面结构**：顶栏（合规项筛选）；内容 = 数据脱敏配置卡 + 本地隔离说明 + 等保项清单 + 审计导出；底栏「导出合规报告」。

② **展示内容**：脱敏规则（字段级）；本地/私有化隔离状态；等保项清单；审计。

③ **核心功能**：【表单】合规配置（脱敏/权限）；【其他】审计导出、等保项跟踪。

④ **实现方式**：脱敏落地全站（`M3/M4/M6` 隐私字段）；配置受 `T1` RBAC；审计 `T1-04`/`M1-12`；等保三级；写 `T1-04`。

⑤ **交互功能**：脱敏字段级开关；等保项勾选跟踪；合规报告导出（哈希）。

⑥ **达成效果**：AI 合规底线（联动样板等保提示条）。

⑦ **页面联动**：上游=全站隐私字段；下游=`T1-04 审计`、`M1-12 审计`、`M15 大屏`（脱敏）；横切=`T1`（RBAC）。

---

## A1-18 API 网关 ★远期 / A1-19 调用日志 / A1-20 告警中心（未产）

① **页面结构**：顶栏（模块切换 网关/日志/告警）；内容 = API 目录（限流/鉴权）+ 调用明细（时延/错误）+ 告警规则卡。

② **展示内容**：API 目录与配额；调用明细（成功/错误/时延）；告警规则与记录。

③ **核心功能**：【表单】网关管理、告警规则；【列表】调用日志；【其他】检索。

④ **实现方式**：网关 `T4`/Go 网关；日志 `T1-04`（append-only）；告警 `T3-03`/`M2-18`；写 `T1-04`；鉴权 `T1`。

⑤ **交互功能**：API 限流/鉴权配置；日志检索；告警规则编辑；仪表盘。

⑥ **达成效果**：AI 能力可观测可治理。

⑦ **页面联动**：上游=全 A1 调用；下游=`T4 中台`、`T3-03 告警`、`M1-12 审计`；横切=`T1`（鉴权）。

---

## A1-21 权限管理 / A1-22 计费账单 ★增强 / A1-23 市场模板 ★增强 / A1-24 设置（未产）

① **页面结构**：顶栏（模块切换 权限/账单/市场/设置）；内容 = AI 功能权限矩阵 + 用量计费卡 + 模板市场 + 全局配置表单。

② **展示内容**：AI 功能权限（按角色）；用量计费（Token/调用）；模板市场；全局配置。

③ **核心功能**：【表单】授权、计费规则、模板选用、配置；【列表】用量/账单。

④ **实现方式**：权限 `T1`（RBAC）；计费 `T2`/`T4`；模板 `S3`；配置 `T2`；写 `T1-04`；隐私 `A1-17`。

⑤ **交互功能**：权限矩阵勾选；用量下钻；模板预览选用；配置保存。

⑥ **达成效果**：AI 平台化运营。

⑦ **页面联动**：上游=`T1 权限`、`T4 中台`；下游=`T1-04 审计`、`M1-12 审计`、`T3-03 账单`；横切=`A1-17`。
