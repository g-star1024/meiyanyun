# DELIVERY-P5-B60 · 通知近线批（第二层近线首批：04 L35 多渠道适配层实时收口 + L39 全局免打扰管理端可配 + L65 员工个人级免打扰，4 代码卡 4 commit）（2026-09-18 白天）

> **批次性质**：用户 2026-09-18 四阶段授权第一阶段「第二层近线，可以全部开始」的**首批**，台账 04-backlog 三行纵深收口——L35 通知多渠道适配层（四渠道 INBOX/SMS/WECHAT/EMAIL 后端扇出在 B26/B57 已全量落地，残留=铃铛未接实时推送 + Controller 过时注释文案）、L39 全局免打扰时段（NotificationQuietConfig 已实现已接线、唯一消费点 FanoutJob，残留=启用开关与管理端配置口径，yml 默认关闭）、L65 员工个人级免打扰（唯一真功能缺口：实体无时段字段、设置页 GNotifSettingsView 纯 mock 且类别 key 与后端不对齐）。本批四卡：① 个人免打扰时段后端（notify_preference 加 quiet_enabled/quiet_start/quiet_end 三列、偏好契约扩三字段 HH:mm 校验、FanoutJob 全局‖个人双窗判定）；② 消息设置页纯 mock 换真源（五类别对齐、INBOX 列恒 disabled、样式零改）；③ 铃铛 EventSource SSE 实时推送（服务端流 B26 已完备、本批前端订阅）；④ 全局免打扰管理端可配（org external_integration 第 8 行 NOTIFY_GLOBAL_QUIET/QUIET_WINDOW 播种，txn 60s 快照动态窗）。**本批无新页面、无模块状态跃迁，B20/B21/B26 通知模块纵深闭合批。**
> **决策依据**：本批由用户 2026-09-18 明确四阶段授权开工（非夜间自主接力），授权原文逐字：「1、先处理第二层近线，可以全部开始；2、然后处理 core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径/状态机/选型决策按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入 C. 大阶段/专项开工」。本批三处实质技术取舍按「按照你推荐的来」总授权自主定案，依据如下：
> 1. **个人免打扰字段走 ddl-auto=update 加三列而非新建 Flyway**：txn-service 业务表既有惯例即 ddl-auto=update（B59 卡2 customer.anonymized_at、B56 之前多批同范式），notify_preference 为既有表加可空列、双栈重启自动加列、可重入零风险；org-service 因 B57 起已纳 Flyway（external_integration 7 目录由 V34 建），卡4 全局配置新增行自然走 V35 SQL。两轨各循其服务既有惯例，不混用。
> 2. **铃铛实时推送选 SSE 而非 WebSocket**：通知是服务端→客户端的**单向**推送，SseEmitter 服务端在 B26 已完备（/api/txn/notifications/stream，30min 超时/25s 心跳/ready 首帧/?access_token= 握手/Registry push 事件名 notification），前端全仓已有 m1Screen 集团大屏 EventSource 成熟范式可复刻；WebSocket 双向能力对单向场景属过度设计，且需额外会话/连接管理。SSE 经标准 GET 走现有网关路由与 Bearer/query token 鉴权，零新基础设施。
> 3. **全局免打扰管理口径＝B57 external_integration 配置窗口 + yml 运维默认回退**：不新建配置表、不加管理页面——复用 B57 落地的 external_integration 第七目录「接入配置」tab 与 IntegrationCatalog 枚举第二真源，新增第 8 行 NOTIFY_GLOBAL_QUIET（value_kind=QUIET_WINDOW、config_json {start,end}、enabled 开关），管理端权限/加密/审计/内部快照链路全部白拿；txn IntegrationConfigClient 60s 快照已在 B57 落地，resolveQuietWindow 直挂；org 不可用/快照失败/禁用/非法值一律回退 yml `meiyun.notify.quiet.*` 运维默认，保证 fanout 永不因配置故障阻塞。
> 4. **双窗命中语义**：非 INBOX 渠道命中全局或个人任一窗→DEFERRED + next_attempt_at=now+1800s（既有 findRetryable 30 分钟重评自愈，不丢信）；URGENT 豁免穿透（与 B59 合规 CRITICAL 告警必须可达一致）；INBOX 站内信恒 SENT（免打扰免的是打扰，不是留存）。
> **提交清单（4 代码卡 4 commit，全 push origin/main，HEAD=`3a9a308`）**：
> - 卡1 `cbc094f` feat(P5-B60)：个人级免打扰 quiet 三字段+偏好契约 HH:mm 校验+FanoutJob 全局/个人窗口判定，4 files +103/-15
> - 卡2 `191d225` feat(P5-B60)：消息设置页纯 mock 换真源，个人免打扰五类别落库，3 files +221/-45
> - 卡3 `51c6628` feat(P5-B60)：铃铛 SSE 实时推送 EventSource 增量插头+挂载清理，2 files +62/-2
> - 卡4 `3a9a308` feat(P5-B60)：全局免打扰时段管理端可配 external_integration 新增 QUIET_WINDOW 第 8 行，9 files +294/-32
>
> 合计 commit 级文件变更 **18 个（4+3+2+9）+680/-94**（NotificationQuietConfig.java 经卡1/卡4 两 commit、stores/notification.ts 经卡2/卡3 两 commit 演进，唯一文件 17 个）；卡5 为本 docs 原子提交（本 DELIVERY＋六册＋哨兵）。
> **铁律对齐**：0 先查再写（卡0 全链路只读侦察：四渠道适配层/扇出六态/SSE 服务端/双库 notify_preference 物理结构/ddl-auto 配置/GNotifSettingsView mock 实证；B59「铃铛非 SSE」时点注由本批卡3 订正为现状，旧注保留不抹）；1 三处契约（GET/PUT /preferences 扩 quiet 三字段、org integration 第 8 行 config_json {start,end}、内部 snapshot 透传，前后端逐字段对齐）；2 新增写数据循惯例（txn 三列 ddl-auto=update 双库自动加列、org V35 播种 ON CONFLICT 幂等可重入，QUIET_WINDOW value_kind 与 CHECK 先核实）；3 通知标题/拦截文案/400 错误全中文（HH:mm 格式非法、起止不能相同、免打扰时段延后发送）；6 Maven 绝对路径＋双栈换载；7 三轨真验（网关 curl＋PG 物理列＋Chrome，逐卡留证）；8 一卡一 commit 紧跟 push（卡3 TS6133 编译即发现即修复随卡提交）；9/10 台账六册原子提交通读勾稽；11 哨兵全程心跳、批末置 DORMANT。

## 1. 背景与记账口径

- **收口三行 backlog**：L35 通知多渠道适配层——B26 建通知基座（notification/notification_delivery/notify_preference、FanoutJob 60s 扇出、四渠道适配器、delivery 六态、幂等退避）、B57 把三渠道网关 URL 与广告密钥统一收进 external_integration 配置窗口，后端链路已全量；B59 卡1 合规告警复用该基座时实证并澄清了两个残留事实：铃铛无实时推送（前端零 SSE 订阅，实时性＝fanout 60s＋下次挂载拉取）、Controller 注释/400 文案仍称「INBOX 唯一链路/网关未开通」过时。L39 全局免打扰——NotificationQuietConfig 跨午夜算法已实现且 FanoutJob deliver 是唯一消费点，但无启用开关、yml 无 meiyun.notify.* 配置（默认关），管理端不可配。L65 个人级免打扰——notify_preference 实体无任何时段字段、Job 只读全局窗、GNotifSettingsView 整个设置页是纯 mock（但路由、壳子下拉入口、免打扰 UI 骨架齐备），且页内类别 key（approval/workorder/alert/marketing/system）与后端五类别不对齐。
- **卡1 技术口径**：[NotifyPreference.java](file:///Users/huluobo/WorkBuddy/2026-08-15-23-51-02/meiyun-platform/backend/txn-service/src/main/java/com/meiyun/txn/NotifyPreference.java) 加 quietEnabled(boolean default false)/quietStart/quietEnd(varchar(8) HH:mm) 三字段，ddl-auto=update 双库（meiyun_seed/meiyun_core）随重启自动加列；[NotificationController.java](file:///Users/huluobo/WorkBuddy/2026-08-15-23-51-02/meiyun-platform/backend/txn-service/src/main/java/com/meiyun/txn/NotificationController.java) GET/PUT /preferences 契约扩三字段，无偏好行时内存回落 quietEnabled=false/22:00/08:00 不落库（PUT 才 upsert），normalizeHhmm 校验 25:00 等非法值→400 中文、起止相等→400（相等等于空窗，语义上等于不限时段，避免误配置静默全天静默）；[NotificationQuietConfig.java](file:///Users/huluobo/WorkBuddy/2026-08-15-23-51-02/meiyun-platform/backend/txn-service/src/main/java/com/meiyun/txn/NotificationQuietConfig.java) 抽 static bizNow()/inWindow(start,end,now) 共用跨午夜算法（start>end 跨午夜、s==e 空窗恒 false、null/非法容错 false）；[NotificationFanoutJob.java](file:///Users/huluobo/WorkBuddy/2026-08-15-23-51-02/meiyun-platform/backend/txn-service/src/main/java/com/meiyun/txn/NotificationFanoutJob.java) deliver 改为 globalQuiet||personalQuiet 双窗（pref 已在入参零扩参），URGENT 豁免、仅非 INBOX 渠道判定、DEFERRED 文案区分「全局/个人免打扰时段，延后发送」、next_attempt_at=now+1800s，retryOne 重评自动覆盖。
- **卡4 技术口径**：org 库 V35 Flyway 给 external_integration value_kind CHECK 增 QUIET_WINDOW 并播种第 8 行 NOTIFY_GLOBAL_QUIET（config_json `{"start":"22:00","end":"08:00"}`、enabled=false、ON CONFLICT 幂等）；IntegrationCatalog 增第 8 枚举、IntegrationService upsert 走 QUIET_WINDOW 分支（HH:mm 正则、双空卫语、起止相同 400、assertCanEnable 统一闸口、parseWindow）；内部快照 /internal/integrations/snapshot 8 行 configJson/enabled 透传；txn IntegrationConfigClient.resolveQuietWindow 60s 快照解析，失败/禁用/非法回空由 NotificationQuietConfig 回退 yml meiyun.notify.quiet.*（application.yml 落运维默认）；前端 integration.ts 扩 QUIET_WINDOW，CInput 支持 type=time（样式零改），T3IntegrationView 接入配置抽屉双 time input。
- **完成度记账（纵深收口批，数字一律不变）**：本批无新页面（设置页/铃铛/接入配置 tab 均为既有页面挂真）、无 ⬜→✅ 状态跃迁、无 🔧 成因消解：
  - 总数 **✅109/166=约66%、🔧1、⬜55（约33%）全部不变**；
  - 域归属：**域⑦ 组织与权限 11✅0🔧13⬜ 不变**；**域⑧ 平台与基建 34✅2🔧31⬜ 不变**（B20/B21/B26 通知既有模块纵深闭合；接入配置窗口 B57 已建，本批增枚举行不新增模块）。勾销 04-backlog **L35/L39/L65 三行**（删除线+闭合长注+末列 ✅ B60；L35 注中明示真实运营商账号接入仍留 L64 远期项）。三证勾稽（完成度=状态计数=时间线批次），同 B50-B59 纵深批先例。

## 2. 影响面只读核实（卡0 侦察结论）

- **通知全链路实证**：生产源两条——ApprovalSlaJob（APPROVAL/URGENT 审批超时）与 B59 InternalNotificationController `POST /api/txn/internal/compliance-alert`（@RequirePerm internal:notify，WARN→STORE_MGR/WARNING、CRITICAL→REGION_MGR+SUPER_ADMIN/URGENT）；两源落 notification 表（uk idem_key、idx recipient）→NotificationFanoutJob `@Scheduled(fixedDelay=60_000L, initialDelay=30_000L)` 限批 100 findUnfanned（not-in 子查询）→NotifyPreference（员工×类别 uk(staff_id,category)、channels text 逗号串 INBOX/SMS/WECHAT/EMAIL、退订落 INBOX SKIPPED）→notification_delivery 逐渠道短事务幂等 ensurePending（uk notification_id+channel，六态 PENDING/SENT/FAILED/SKIPPED/DEFERRED/DEAD）。
- **deliver 顺序实证**：INBOX 恒 SENT 且 streamRegistry.push 后 return（先于免打扰判定）；非 INBOX 先全局 quietConfig.inQuietWindow（URGENT 豁免/Asia/Shanghai/跨午夜）→DEFERRED+1800s；SMS/WECHAT/EMAIL 三适配器 POST 网关 URL 经 IntegrationConfigClient 60s 快照从 org external_integration NOTIFY_*_GATEWAY 解析、@Value 兜底，未配 SKIPPED、4xx DEAD、其余 FAILED 指数退避 30s 起/帽 1800s/20 次 DEAD；findRetryable 自愈 PENDING/FAILED/DEFERRED；TxnSchedulerConfig 独立 4 线程调度池。
- **SSE 服务端实证（B26 已完备）**：NotificationStreamController `GET /api/txn/notifications/stream`（SseEmitter 30min 超时/25s 心跳/ready 首帧/?access_token= query 握手）+Registry push 事件名 notification；前端零订阅（全前端 grep EventSource 仅 m1Screen.ts 集团大屏），范式＝api/screen.ts screenStreamUrl()+stores/m1Screen.ts connectStream。B59 DELIVERY §5 据此澄清「铃铛非 SSE」——该注记作为当时时点事实保留，本批卡3 上线后在 02-modules L117 同处追加现状订正，不抹旧文。
- **双库物理结构实证**：双库 `\d notify_preference` 六列 id/category/channels/is_enabled/staff_id/updated_at，无 quiet 列；meiyun_core 偏好 2 行全 APPROVAL、notification 515/delivery 515 全 SENT；meiyun_seed 偏好 2 行（E009/E014 APPROVAL）、notification 39。txn application.yml 实证 spring.jpa.hibernate.ddl-auto=update（加列零迁移 SQL、双栈重启自动加）、无 meiyun.notify.* 配置。
- **前端设置页实证**：GNotifSettingsView 纯 mock 但路由（router L213）/壳子下拉入口（CShellDesktop L110）/免打扰 UI 块齐备；类别 key 为 approval/workorder/alert/marketing/system，与后端 APPROVAL/CUSTOMER/INVENTORY/MARKETING/SYSTEM 五类不对齐（NotificationView 已全真源五类对齐，可参照）；INBOX 为必达渠道，矩阵中须恒 disabled 不可关。

## 3. 代码改动（按 commit）

### 卡1 `cbc094f`（4 files +103/-15）个人级免打扰后端

- **NotifyPreference.java +21/-1**：新增 `@Column(name="quiet_enabled") boolean quietEnabled=false`、`quiet_start`/`quiet_end` String（varchar(8)，可空），默认值保证历史行加载为关闭态。
- **NotificationQuietConfig.java +29/-约 4**：抽出 `static LocalTime bizNow()`（LocalTime.now(BIZ_ZONE)）与 `static boolean inWindow(String start,String end,LocalTime now)`——null/空白/非法容错返回 false、start.equals(end) 空窗恒 false、start>end 走跨午夜两段判定；实例方法保持 URGENT 豁免与快照窗解析入口，全局与个人共用同一算法零分叉。
- **NotificationController.java +56/-约 8**：GET /preferences 出参每类别带 quietEnabled/quietStart/quietEnd，无库行内存回落 false/22:00/08:00（只读不落库）；PUT /preferences 携三字段时 normalizeHhmm 严格 `^([01]\d|2[0-3]):[0-5]\d$` 校验，非法→400「免打扰时间格式非法，应为 HH:mm（如 22:00）」、起止相等→400「免打扰起止时间不能相同（相同等于空窗，不限时段）」；校验通过才 upsert（PUT 首次写库建行）；同步订正类注释与旧 400 文案中「INBOX 唯一链路/网关未开通」过时表述。
- **NotificationFanoutJob.java +12/-1**：deliver 内非 INBOX 分支判定改 `globalQuiet || personalQuiet`（personal 窗由入参 pref 三字段调静态 inWindow，零扩参零新查询）；命中且非 URGENT→status=DEFERRED、note 按命中源写「全局免打扰时段，延后发送」/「个人免打扰时段，延后发送」、nextAttemptAt=now+1800s；URGENT 与 INBOX 路径不变；retryOne 经同一 deliver 重评，窗口过后自动恢复发送。

### 卡2 `191d225`（3 files +221/-45）消息设置页纯 mock 换真源

- **api/notification.ts +15/-约 3**：偏好 DTO 与 PUT body 扩 quietEnabled/quietStart/quietEnd 三字段；预埋 notificationStreamUrl(token)（拼 /api/txn/notifications/stream?access_token=，卡3 消费）。
- **stores/notification.ts +64/-约 4**：新增 quiet 状态切片与 saveQuiet（类别×三字段串行保存、乐观更新失败回滚、后端中文 errMsg toast 三防），fetchPreferences 一并拉三字段。
- **GNotifSettingsView.vue +187/-约 40**：类别 key 由 approval/workorder/alert/marketing/system 修正为 **APPROVAL/CUSTOMER/INVENTORY/MARKETING/SYSTEM** 五类别与后端对齐；渠道矩阵与免打扰时段全部绑真源 store，删除 mock 脚本；INBOX 列恒 disabled（必达渠道，不可关）；免打扰起止 time input 前端先做 HH:mm/相等校验（中文 toast）后提交；**模板样式零改**（沿用既有设计系统与布局）。

### 卡3 `51c6628`（2 files +62/-2）铃铛 SSE 实时推送

- **stores/notification.ts +54/-1**：connectStream/disconnectStream——守卫防重（已连不重复握手）、无 token 不握手、open 收 ready 首帧置 streamReady、监听 "notification" 事件 JSON.parse 后按 id 去重 unshift(adapt) 头插 items 并刷 unreadCount（parse 失败 console.error 不影响流）；onerror 仅在「未 ready 且 readyState=CLOSED」（典型 401 坏 token）时主动 close 防死循环刷 401，其余掉线交浏览器原生重连；disconnect 清理 EventSource 与标志位。
- **CShellDesktop.vue +10/-1**：onMounted 调 connectStream（补 import onUnmounted）、onUnmounted disconnectStream，随壳挂载/卸载管理流生命周期。

### 卡4 `3a9a308`（9 files +294/-32）全局免打扰管理端可配

- **V35__notify_global_quiet.sql（新，23 行）**：org 库 external_integration value_kind CHECK 增 'QUIET_WINDOW'（以 DO 块改约束方式可重入），播种 NOTIFY_GLOBAL_QUIET 第 8 行（name 全局免打扰时段、value_kind=QUIET_WINDOW、config_json `{"start":"22:00","end":"08:00"}`、enabled=false、ON CONFLICT (code) DO NOTHING 幂等）。
- **IntegrationCatalog.java +5/-1**：第 8 枚举 NOTIFY_GLOBAL_QUIET（code/名称/value_kind=QUIET_WINDOW/内部快照可见/管理端可见等元数据）。
- **IntegrationService.java +120/-约 10**：upsert QUIET_WINDOW 分支——HH:mm 正则校验双值、双空卫语、起止相同 400、assertCanEnable 统一闸口（非法窗不可启用）、parseWindow 解析 config_json；test/保存返回文案含「约 60 秒生效」（快照 TTL 如实告知）。
- **IntegrationConfigClient.java +40/-约 3**：resolveQuietWindow 读 60s 快照中 NOTIFY_GLOBAL_QUIET 行，解析失败/禁用/非法/缺失一律回空 Optional（永不外抛）。
- **NotificationQuietConfig.java +42/-约 3**：判定优先级＝URGENT 豁免→IntegrationConfigClient 快照动态窗→异常/无窗回退 yml `meiyun.notify.quiet.*` 运维默认。
- **application.yml +7**：meiyun.notify.quiet.enabled/start/end 运维默认（默认关闭/22:00/08:00）。
- **integration.ts +14/-约 2**：前端枚举与 DTO 扩 QUIET_WINDOW/configJson {start,end}。
- **CInput.vue +2/-1**：type 联合补 "time"（原生 time input，样式零改）。
- **T3IntegrationView.vue +73/-约 8**：接入配置抽屉第 8 行——双 input[type=time]、跨午夜提示文案、启用复选框；前端先做相等/格式拦截（不发 POST、红条中文），合法保存经既有 /api/org/integrations/{code} 提交，成功绿条+关抽屉；无 integration:edit 权限配置按钮 disabled 置灰（SE001 店长实测）。

## 4. 三轨真验（全绿，逐轨留证）

1. **构建/部署轨**：卡1 txn-service、卡4 org-service+txn-service `mvn package -DskipTests` EXIT=0 出 fat jar，seed 栈 docker cp 换载＋restart healthy；卡2 `pnpm build` 20.55s EXIT=0 零 TS 错；卡3 `pnpm build` 23.58s EXIT=0（首编译报 TS6133 streamReady 只写未读，onerror 条件加 !streamReady 后通过）；V35 seed org 库 Flyway success；5175 vite dev server 全程存活供 Chrome 轨。
2. **PG/物理轨（双库）**：
   - 卡1 ddl-auto=update 双库 notify_preference 自动加 quiet_enabled/quiet_start/quiet_end 三列实证（\d 物理列名逐字核对）；原六列 id/category/channels(text 逗号串)/is_enabled/staff_id/updated_at 与 uk(staff_id,category) 不变；
   - 卡2 GET 无偏好行只内存回落不落库、PUT 后才 upsert 经物理行 count 证实；
   - 卡3 PG delivery id87 INBOX SENT attempt=1（SSE 推送与 fanout 落库并行不悖）；
   - 卡4 全天窗 00:00-23:59 fanout 行为：通知 id88 NORMAL 三非 INBOX 渠道全 DEFERRED、note「全局免打扰时段，延后发送」、next_attempt_at=now+1800s，INBOX SENT；id89 URGENT 三渠道 SKIPPED（穿透到适配器、dev 无网关 URL）；id90 无偏好仅 INBOX SENT；关窗（回 false/22:00-08:00）后新插 id91 NORMAL 三渠道 SKIPPED 而非 DEFERRED，证明快照 ≤60s 动态刷新恢复；旧 DEFERRED 因 1800s 重评间隔保持（设计如此，非缺陷）。
3. **curl/HTTP 轨**：
   - 卡1 偏好契约四角：GET 五类回落 false/22:00/08:00、PUT 正常落库、seed 偏好临时置 00:00-23:59 后 WARNING id84→INBOX SENT+SMS DEFERRED「个人免打扰时段，延后发送」、URGENT id85→INBOX SENT+SMS SKIPPED 穿透；
   - 卡2 网关四角：GET 五类回落、PUT MARKETING 全量返回正确、25:00→400「格式非法，应为 HH:mm」、起止相同→400 中文跨午夜提示；
   - 卡3 `curl -N '.../api/txn/notifications/stream?access_token='`：首帧 `event:ready` data `{"staffId":"SE001"}`、两次 `:hb` 注释心跳；插通知 id86 后 60s 内收到 `event:notification` 完整实体帧（id/recipient/category/level/title/content/link/bizRef:null/sender/idemKey/read/createdAt ISO）；
   - 卡4 管理端写口径（SE101 超管 POST /api/org/integrations/NOTIFY_GLOBAL_QUIET）：25:00→400、起止相同→400、全天窗启用→200 config_json 透传、test/保存两态文案含「约 60 秒生效」；内部快照 X-Internal-Token 8 行 configJson/enabled 透传、关窗即时回落。
4. **浏览器 UI 轨（Chrome DevTools MCP，5175 seed 隔离上下文）**：
   - 卡2（SE001 店长）：渠道总开关串行 PUT 五类落 WECHAT（SYSTEM 保 SMS）、矩阵 INBOX 列恒 disabled 必达、免打扰 25:00 toast 中文前端拦截、23:30/07:15 五类 quiet=t 落库、刷新完整回显、控制台 0 error/0 warn；
   - 卡3：首拉 REST 角标=1（id86），插第二条 id87 URGENT 后**无刷新角标实时 1→2**、store items 头插 [87,86,42]、unreadCount=2、reload 后 ids=[87,86,42] hasDuplicate=false（id 去重实证防重试重复推）、控制台零消息；
   - 卡4：SE001 第 8 行配置按钮 disabled 置灰（integration:view 无 edit 权限正确）；登 SE101 后按钮启用、抽屉双 input[type=time]/跨午夜提示/启用复选框齐全；起止相同点保存→红条「免打扰起止时间不能相同（相同等于空窗，不限时段）」抽屉不关且 list_network_requests 实证**无 POST**（前端拦截）；合法 22:00/08:00 未启用保存→绿条+POST 200（reqid435）+抽屉关闭。
5. **提交轨**：cbc094f→191d225→51c6628→3a9a308 逐一 commit 紧跟 push origin/main；批末 HEAD=`3a9a308`（其下为卡3 docs 留痕 `d059342`、卡3 代码 `51c6628`）。

## 5. 如实说明

- **B59「铃铛非 SSE」时点注的订正口径（铁律 0/台账只追加不改写）**：B59 DELIVERY §5 与 02-modules L117 记载「铃铛无 EventSource、无轮询，实时性＝fanout 60s＋挂载拉取」是 2026-09-18 凌晨的真实时点事实；本批卡3 起前端订阅已上线，铃铛对 INBOX 新通知为 SSE 实时头插+角标自增。旧注**保留不抹**，在 02-modules L117 同处尾接 B60 卡3 现状订正；本批改变的是前端订阅层，服务端 SseEmitter/Registry 在 B26 即已完备。
- **免打扰不免留存**：INBOX 站内信在任何免打扰窗内恒 SENT（deliver 先判 INBOX 后判窗），免打扰只延迟 SMS/WECHAT/EMAIL 等「会响」的渠道；DEFERRED 不丢信——findRetryable 每轮重评，窗口结束后 30 分钟内自动补发；URGENT（审批超时升级、合规 CRITICAL）穿透免打扰，保证高危必达。这两条语义在个人窗与全局窗上完全一致。
- **全局窗配置生效时延如实告知**：管理端保存后经 60s 快照 TTL 生效（非实时），故文案明示「约 60 秒生效」；田间实测关窗后新单 ≤60s 恢复 SKIPPED/发送即此机制。yml 运维默认是最后回退——org 服务不可用、快照拉取失败、配置非法三态下 fanout 不阻塞、按 yml 默认关窗处理（通知不被配置故障静默吞没）。
- **ddl-auto 与 Flyway 双轨的选择**：卡1 三列走 txn ddl-auto=update 而非 Flyway，系因 txn 业务表既有惯例即 ddl-auto（B59 customer 加列同范式）、加可空列可重入零风险；org external_integration 自 B57 V34 起已纳 Flyway，卡4 新枚举行循 V35。两服务各循既有惯例，未制造新的迁移风格分裂。
- **卡3 TS6133 小坑（如实留痕）**：首版 onerror 条件未引用 streamReady，vue-tsc 报「只写未读」TS6133；构建期即发现，onerror 加 !streamReady 判定（仅未握手成功即 CLOSED 才主动摘除，握手成功后的临时掉线交浏览器原生重连）后随卡3 同一 commit 提交，无独立修复 commit。

**测试数据还原（铁律：测试数据必须还原，2026-09-18 批末逐项 SQL 复核）**：

- meiyun_seed：notification 测试 id84-91（含 SSE id86/87、fanout id88-91）共删净，回 **39 行/max id 42** 基线；notification_delivery 删 13 行测试扇出，回 **39 行/max id 41**；notify_preference 测试行 SE999/QUIETTEST 删除，回每库 **E009/E014 两行 APPROVAL** 基线（三表测试行 count 全 0）；
- external_integration NOTIFY_GLOBAL_QUIET 还原创播种态 enabled=**false**/config_json `{"end":"08:00","start":"22:00"}`；
- 卡2 五类渠道/quiet 串行改动均在测试偏好行上，随测试行删除归零；meiyun_core 零 SE001/测试污染（双库偏好各 E009/E014 基线核对）；
- 浏览器 localStorage meiyun_token 已清；/tmp/seed_tok_se101.txt 等临时 token 文件不入库、无 mock 开关/临时 cron 残留。

## 6. 进度落账

**完成度数字（纵深收口批不变）**：✅ **109**/166=约**66%**、🔧 **1**、⬜ **55**（约33%）全部不变；**域⑦ 组织与权限 11✅0🔧13⬜ 不变；域⑧ 平台与基建 34✅2🔧31⬜ 不变**。新增完成页面/模块 **0 个**；勾销 04-backlog **L35/L39/L65 三行**（删除线+闭合长注+末列 ✅ B60；L35 注中 SSE 实时推送到卡3 收口、真实运营商账号接入保留远期指 L64），订正 02-modules L117 铃铛表述（追加 B60 现状，B59 旧注保留）。

- **04-backlog**：L35（多渠道适配层+实时推送）、L39（全局免打扰时段）、L65（员工个人级免打扰）三行勾销（删除线+逐条闭合长注：四 commit、双窗语义、SSE 范式、第 8 行配置；末列 ✅ B60）；L64 真实运营商接入不勾销保持远期。
- **03-timeline**：表底追加 P5-B60 六列行（L91，日期 2026-09-18、四 commit cbc094f/191d225/51c6628/3a9a308、DELIVERY-P5-B60-2026-09-18、闭合列写三行勾销+02 订正+数字 109/1/55 与域⑦ 11/0/13、域⑧ 34/2/31 不变）。
- **02-modules**：L117 消息通知中心行尾接（卡1 个人 quiet 三列+双窗 DEFERRED、卡2 设置页真源五类别、卡3 铃铛 EventSource SSE——B59「非 SSE」注为当时时点事实保留、本注追加现状订正、卡4 全局窗 external_integration QUIET_WINDOW 管理端可配）。
- **01-dashboard**：L12-15 数字一字不动；倒序 bullet 首条插 B60（四卡、三行勾销、数字不变、DELIVERY 指引）；L40 域⑦ 数字格 11/0/13 不动、单元格尾接 B60 B26 通知纵深闭合精简注。
- **00-history**：顶部插 B60「最近更新」简报块（四 commit、三行勾销+SSE 订正、数字不变、DELIVERY 指引），B59 完整长文保留加「上一批 B59：」前缀下沉。
- 索引＋六册＋本 DELIVERY＋哨兵同一 commit 原子提交（新文件 DELIVERY `git add -f`，已跟踪册 `git add -u docs/`），提交后通读勾稽（109/55/166、域⑦ 11/13、域⑧ 34/31 与 ③ 时间线一致；04 L35/L39/L65 闭合注与末列一致）。

## 7. 下一批

- **P5-B61 角色管理页 RBAC 可视化（04 L50）**：第二层近线第二批改走组织与权限域——卡0 只读侦察 org 角色/权限矩阵现状（PermissionMatrix 八角色×权限码播种、role:view/edit 权限、staff-role 绑定链路）后拆卡施工，前端样式零改只换数据源，一卡一 commit+push、三轨真验、数据还原、批末台账原子提交。
- **第二层近线后续队列（用户已授权「可以全部开始」，按序推进）**：B62 客户运营三行（L69 自动降级/L70 权益联动/L44 撞单合并）→B63 财务四行（L83-86 异常中心/异常账务/预收合规/进项税）→B64 诊疗四行（L87 M4 多级审批/L90 SOP/L91 随访分析/L75 EMR 模板）→B65 营销散项→B66 报表数据（R03-R09/R02 维度/毛利率/REVENUE 聚合/月结）→B67 测试体系（造数工具化/audit 选项/seed Flyway 专项）。
- **第二、三阶段衔接口径**：第二层近线全部闭合后，按用户授权转 B68（core 库 24 行营销演示残留，仿 B57 备份+物理删除，对应 04 L100）→B69（B 类产品口径/状态机/选型决策：L133 REJECTED 回转、L140 transfer 渠道、L138 XLSX/PDF 选型含竞品调研，按推荐口径先行竞品调研选最合适方案）→全部完成后转 C 大阶段/专项开工。
- 哨兵批末置 **DORMANT**；本批为用户白天明确授权批次，后续批次同属「第二层近线可以全部开始」授权范围，接续会话按中断恢复指引核验心跳后即可 ACTIVE 续跑，不臆造授权外范围。
