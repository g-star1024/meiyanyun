# DELIVERY-P5-B62（2026-09-19）· 客户运营纵深批（04-backlog L69 自动降级引擎＋L70 会员权益联动＋L44 撞单合并）

> **批次性质**：第二层近线**客户运营三卡纵深批**（非零代码批）。04-backlog 登记的三项客户域纵深缺口全部经卡0 只读侦察确认为**真实缺口**（非 B61 式事实翻转），按风险递增＋依赖排序拆三卡施工：卡1 会员自动降级引擎＋月消费事实表（补「当前只升不降」）、卡2 会员等级折扣真实计价联动（补「discount 配置零消费方」）、卡3 撞单合并**期1**候选发现只读端点（合并写期与 8 张物理 FK 子表迁移留存后续期）。三卡均为既有 ✅ 模块（会员等级/客户档案）的纵深挂载，**无新页面、无 ⬜→✅ 模块状态跃迁，数字一律不变**。
>
> **决策依据（用户四阶段授权逐字）**：「1、先处理第二层近线，可以全部开始；2、然后处理core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径 / 状态机 / 选型决策 按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入C. 大阶段 / 专项开工」。本批为第①阶段第二层近线第三批（B60/B61 之后），「可以全部开始」覆盖 B62 无需再请示开工；降级口径（连续月/保护期/只降一级）、折扣计价口径（后端权威/BigDecimal 分位）、撞单期边界（只读候选发现、写期留存）全部按「按推荐来」授权由卡0 侦察定案，零返工。
>
> **卡0 裁断（硬结论，定卡序＝卡1→卡2→卡3）**：
> ① **L69 降级引擎代码零存在**：`downgrade_protect_months` 已落库默认 3、校验 0~36 但零消费；LevelMonthlyJob cron `0 5 0 1 * ?` 无时区、autoUpgrade 只升不降（无向下分支）；`customer.total_spend` 是死字段（全仓 setTotalSpend 零命中，禁复用）；无客户×月消费事实表（txn 仅有界实时窗与门店维 RevenueMonthly）；手工降级端点可复用；通知中心只发员工无客户短信通路；审批仅 REFUND/CARD_CANCEL/REQUISITION/LOSS_REPORT 无等级类——自动降级定案为引擎判定＋站内信告知＋审计，不引入客户短信/等级审批流。
> ② **L70 discount 零计价消费方**：member_level.discount 五级种子（1.00/0.95/0.90/0.85/0.80）进了 MemberLevelDTO 但前端 adaptLevel 丢弃；三条下单链路（order/retail-order/emr-sign）信前端单价/模板价，订单无原价/折扣/等级快照列；benefits 纯 List<String> 文案，无定义/实例/核销三表，360 权益区静态占位。定案：卡2 只做 discount 真实计价＋订单等级快照＋360 权益区接真；**benefits 权益核销链路无真实核销底座，本批不做，L70 半项闭合留存**。
> ③ **L44 撞单仅同店/公海手机号精确硬拒绝**：synchronized 单 JVM，(store_code,phone) 无唯一索引（仅 idx_customer_phone/idx_customer_store），跨店同号允许建档；`customer:merge` 权限壳已在 PermissionMatrix L186 但无端点；前端 CustomerGraph/CustomerMerge 纯 Pinia mock；**8 张表有真实物理 FK 且全 NO ACTION 不可延迟**（customer_tag_rel/member_card/appointment/consultation/txn_order/writeoff_record/repurchase/push_record）——合并写必须先迁子表后删主档，风险最高，故卡3 最后；且卡3 引用迁移清单必须纳入卡1 新建月消费事实表（无物理 FK，合并时按 customer_id 聚合处理唯一冲突）避免返工。卡3 期边界＝只做候选发现只读能力，写期/审计/前端留存。
>
> **提交清单（代码侧，三 feat commit，全 push origin/main）**：
> - 卡1 `003d71e`（2026-09-18 22:13）`feat(customer): 会员自动降级引擎+月消费事实表（B62卡1，04 L69）`——19 files +1231/-50（含 docs/HANDOFF-AUTO.md 10 行卡1 哨兵留痕，纯业务代码 18 文件）。
> - 卡2 `59a79f8`（2026-09-19 02:08）`feat(txn,customer): 会员等级折扣计价联动（B62卡2/04 L70）`——18 files +989/-78（含 M4FlowController `cashier:view OR emr:create` 403 缺陷顺带修复）。
> - 卡3 `34bcfeb`（2026-09-19 04:40）`feat(P5-B62 卡3): 撞单合并期1候选发现只读端点 GET /api/customer/merge-candidates`——7 files +333/-1。
> - 卡3 接力期另有两条 docs 心跳提交 `d584333`/`4b0384a`（哨兵留痕，非业务代码，不计入 03-timeline commit 列）。
>
> **铁律对齐**：-1 不臆造（卡0 全部硬结论经双库 psql/全仓 grep 实证，死字段/物理 FK/权限壳逐条坐实）；0 每卡开工先读哨兵＋六册；1 API 三处一致（卡2 internal level-discount/level-catalog、卡3 merge-candidates 契约均前后端逐字段对齐）；2 DataScope/权限不回退（卡3 pair 双侧 canReadOwned、CONSULTANT 有 view 无 merge 必须 403 为真验重点）；3 中文错误（403「需要 customer:merge」、422 中文计价文案）；4 数字不虚增（纵深批数字一律不变）；5 前端真 API（卡2 折后预估/360 权益区全部真源、提交仍传原价后端权威）；6 双栈构建部署（mvn package＋pnpm build＋双栈真名容器换载 healthy）；7 三轨真验（§4 全绿）；8 一卡一 commit＋紧跟 push（三 feat 独立 hash 均在 origin/main）；9/10 台账六册只追加不改写历史（02 旧注保留尾接、04 L69 整行勾销、L70 半项、L44 期1 注记）；11 哨兵 ACTIVE/心跳/批末存档；12 OCR 门禁（卡3 7 文件 CLI 不可用，人工逐文件审查并载明于 commit message）；安全：候选 DTO 手机号一律掩码前3后4、最小披露，死字段 total_spend/visit_count 禁读。

## 1. 背景与记账口径

04-backlog 三行登记：
- **L69**「自动降级引擎（连续未达标/保护期/客户通知/审批策略）｜B23 移交｜当前只升不降；需计算周期事实表、保护月逻辑、降级审计与 KPI 口径；引擎上线前本月降级 KPI 仅统计手工降级｜客户运营自动化批」。
- **L70**「客户详情会员权益真实联动（discount 实际计价 + benefits 可享权益）｜B23 新增｜等级阈值/权益配置已就绪；需订单计价与权益核销链路接入｜交易计价收口批」。
- **L44**「撞单合并｜business-flows｜客户查重/合并规则｜客户域批」。

B61 §7 逐字预告 B62 施工口径：「卡0 只读侦察——L69 降级：B25 LevelMonthlyJob 只升不降现状、计算周期事实表/保护月/降级审计/KPI 口径缺口盘点；L70 权益：等级阈值/benefits 配置与订单计价、权益核销链路接线点；L44 撞单：客户查重/合并规则与 ES 融合检索复用面。按一卡一 commit+push、前端样式零改只换数据源、三轨真验、测试数据还原施工」。卡0 侦察于 2026-09-18 17:23 起完成（哨兵 MACHINE:BATCH 全量存档硬结论），20:29 进卡1。

**记账口径（数字一律不变，纵深批）**：本批无新页面、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解。仪表盘维持 **✅109/166≈66%、🔧1、⬜55 约33%**；域①客户维持 **8✅/0🔧/10⬜**；域⑦ **11✅/0🔧/13⬜**、域⑧ **34✅/2🔧/31⬜** 不动。台账处置：04-backlog **L69 整行勾销**（删除线＋闭合长注＋末列 ✅ B62）；**L70 半项闭合**（discount 三计价点真实计价＋订单等级快照＋360 权益区接真，benefits 核销链路无底座留存，阶段性注记不整行勾销）；**L44 期1 注记不勾销**（候选发现只读能力就位，合并写期与 8 张物理 FK 子表迁移留存）。三证勾稽（完成度＝状态计数＝时间线批次）一致：本批不产生 ✅ 增量，只闭合/推进三条既有登记。

## 2. 影响面只读核实（卡0 侦察硬结论）

### 2.1 降级引擎域（卡1 缺口实证）

- **只升不降**：LevelMonthlyJob cron `0 5 0 1 * ?`（每月1日 00:05，无 zone），autoUpgrade 逻辑无任何向下分支；`level_rule_config.downgrade_protect_months` 列已存在、默认 3、CHECK 0~36，但全仓零消费。
- **死字段**：`customer.total_spend`（及 visit_count）全仓 setTotalSpend 零命中（仅 SQL 种子写），自动升级累计若复用即为脏源——卡1 新建事实表并把 autoUpgrade 从死字段切到事实表累计 net，total_spend 列保留不写不读。
- **无周期事实表**：txn 仅有 `/internal/paid-orders`、`/internal/refunded-orders` 实时窗端点与门店维 RevenueMonthly，无客户×月粒度事实；自动积分引擎 AutoPoints 的游标幂等范式（闭合月游标 state 表＋fixedDelay Job＋故障不推进游标）可复刻。
- **手工降级/通知/审批现状**：POST /api/customer/{id}/level + adjustCustomerLevel 手工调级可复用；customer_level_history 无表（手工调级无留痕）；通知中心只发员工 STAFF_ID（无客户短信通路）；审批类型仅 REFUND/CARD_CANCEL/REQUISITION/LOSS_REPORT，无等级类。定案不新建客户短信/等级审批流（按推荐授权）。

### 2.2 折扣计价域（卡2 缺口实证）

- member_level 五级种子 discount=1.00/0.95/0.90/0.85/0.80，进 MemberLevelDTO 但前端 adaptLevel 丢弃该字段；后端零计价消费方。
- 三条下单链路：普通开单 createOrder、零售 createRetailOrder、EMR 签署 signEmr，价格全信前端传入单价/模板价；TxnOrder 无 member_level/member_tier/member_discount/original_amount/discount_amount 列，OrderItem 无原价/折扣列，无等级快照。
- 门店会员价 store.memberPriceFen 不挂等级、txn 不调用；卡2 定案折扣**不与门店会员价叠加**（等级折扣为唯一会员优惠口径，门店会员价留存不接）。
- benefits 纯 List<String> 文案，无权益定义/实例/核销三表；CustomerProfileView L1010-1013 权益区静态占位。核销链路无真实底座，卡2 不做，留存 04 L70。

### 2.3 撞单合并域（卡3 缺口实证）

- 查重仅 findFirstByStoreCodeAndPhone（同店）＋findFirstByStoreCodeIsNullAndPhone（公海）手机号精确硬拒绝，synchronized 仅单 JVM；psql 实证 (store_code,phone) 无任何唯一/部分唯一索引（仅 idx_customer_phone/idx_customer_store），跨店同号允许建档＝合并目标场景。
- **8 张物理 FK 子表全 NO ACTION（upd/dec 均 NO ACTION）不可延迟**：customer_tag_rel/member_card/appointment/consultation/txn_order/writeoff_record/repurchase/push_record——合并写必须先迁子表（UPDATE customer_id 指向主档）后删副档；points_ledger/card_ledger append-only 无 FK，不改写、读时 alias 解析；member_card 被 customer 与 txn 双服务映射须定单写入方；customer_monthly_spend（卡1 新建）无物理 FK，合并按 customer_id 聚合并处理 (customer_id,period_month) 唯一冲突（net_fen 相加）。
- ID 列长实证：customer/ai_*/多数业务表 varchar(16)、followup 系 32、customer_grant/grant_deduction/channel_returnback 64（纠正子代理 ai=255 推测）；主档不新建 ID。
- 权限壳：`customer:merge` 已在 PermissionMatrix L186（ALL_PERMISSIONS 码定义，生成器 backend/db/gen_perm_matrix.mjs←frontend/src/stores/auth.ts，勿手改），真正授权仅 REGION_MGR L493/STORE_MGR L772＋SUPER_ADMIN 通配；customer:view 七角色普授不可用——候选暴露跨客户 PII 配对，CONSULTANT 有 view 无 merge 必须 403。
- DataScope：canReadStore(null) 对 REGION/STORE/SELF 全 false（仅 GROUP 全量）→公海撞单组（store_code IS NULL）仅超管可见；候选对必须 pair 级双侧 canReadOwned 过滤，任一侧不可读整对丢弃，不泄露对侧存在性。
- 前端 stores/customer.ts links/merges/detectDuplicate（mock 直 APPROVED）/proposeMerge/seedGraph（C-210/C-211 假客户）/dismissLink 纯 mock；CustomerMergeView 338 行强耦合 mock（硬编码 KPI +42/94.2%、假等级、假 compareRows）；api/customer.ts 零 merge 函数。期1 不接前端。
- ES outbox/relay/reconcile 管道可复用但字段不足、orphan 不删，留写期。

## 3. 施工（分卡）

### 3.1 卡1 `003d71e`——会员自动降级引擎＋月消费事实表（19 files +1231/-50）

**Flyway V36（customer 库，双栈共享迁移链）三表一列**：
- `customer_monthly_spend`：客户×UTC 月事实表，paid_fen/refund_fen/net_fen（净消费=支付−退款，充值剔除、退款全抵），唯一键 (customer_id,period_month)，**无物理 FK**（便于后续合并聚合）。
- `customer_level_history`：等级变更史，change_type 四态 LEVEL_INIT/AUTO_UPGRADE/AUTO_DOWNGRADE/MANUAL，留痕原级/新级/事实月份/原因。
- `monthly_spend_state`：闭合月游标（幂等断点，txn 拉数故障游标不推进）。
- `level_rule_config` 增 `auto_downgrade` 开关列（既有 downgrade_protect_months 接入消费）。

**引擎口径（按推荐授权定案）**：
- MonthlySpendService 聚合＋独立 fixedDelay Job＋手动触发端点：仅闭合月回填（不碰当月未闭合数据），按 **UTC 日界**（与积分引擎同口径），upsert 幂等，充值剔除、退款全抵。
- **autoDowngrade**：M=上一闭合月；连续 3 个月净消费低于本级保级线；保护期锚最近一次调级月、默认保护 3 个月（downgrade_protect_months）；每客户每次只降一级；幂等重放短路（已落 AUTO_DOWNGRADE 史不重复）。
- **autoUpgrade**：从死字段 total_spend 切换为事实表累计 net 口径（total_spend 列保留不写不读）；升级 38 人实证见 §4。
- 告知：txn 新增 `POST /internal/level-downgrade-alert`（@RequirePerm("internal:notify")，按店路由 STORE_MGR，idemKey `LEVEL:` 前缀幂等，公海客户只审计不发信）；手动双端点 auto-upgrade/auto-downgrade（权限 level:edit）。
- 配套：LevelInitService（LEVEL_INIT 初始化史）、LevelHistoryRecorder、LevelDowngradeNotifier；record 组件与 jsonList 两处既有缺陷顺带修复。

### 3.2 卡2 `59a79f8`——会员等级折扣计价联动（18 files +989/-78）

**后端 customer 侧**：
- `GET /internal/level-discount`（X-Internal-Token，@RequirePerm("internal:customer-directory")，批量 ≤50 客户；等级缺失兜底 NORMAL/1.00；最小披露只回折扣相关字段）。
- `GET /member-levels/catalog`（权限 customer:view）＋LevelCatalogDTO（折扣率/权益/升级条件，供 360 权益区真源）。

**后端 txn 侧（后端权威计价）**：
- TxnOrder 加 5 列 member_level/member_tier/member_discount/original_amount/discount_amount；OrderItem 加 2 列 original_unit_price/discount_amount（JPA ddl-auto=update，老行 NULL 诚实留空）。
- MemberDiscountClient/Service：请求级缓存（同单内不重复调）、4xx 透传、**5xx/连接失败 fail-closed 抛 502 回滚下单**（不静默降级成全价导致错单）。
- createOrder/createRetailOrder/signEmr 三计价点统一 BigDecimal HALF_UP：折后单价=round(原价×discount, 分位)；**CARD_SALE（购卡）不参与折扣**；不与门店会员价叠加。
- 读模型纯加字段（不破坏旧契约）；CREATE/ORDER 审计携带原价/折后/等级快照。
- **顺带修复** M4FlowController getOrder 权限缺陷：原仅 cashier:view 致 EMR 医生（emr:create）看诊订单 403，改 `{cashier:view, emr:create}` OR 放行。
- MemberDiscountClientTest 7 纯单测：五级折扣价 20999/19948/18900/17849/16798、4xx 透传、5xx fail-closed、连接失败回滚。

**前端（真 API，样式零改）**：新建 stores/level.ts＋api 扩 customer.ts/order.ts；Prescription/Consultation/DoctorWorkbench 三视图折后预估＋原价划线展示；CustomerProfile360 权益区接 catalog 真源（折扣率/benefits 文案/升级条件）；**提交仍传原价，折后由后端权威计算**（前端仅展示，不信前端价）。

### 3.3 卡3 `34bcfeb`——撞单合并期1·候选发现只读端点（7 files +333/-1）

- Repository：findDuplicatePhoneRows 原生 @Query，三层 regexp_replace 归一化（去空白/连字符/括号→去 `^+?86`→去 `^86`，与既有建档查重同口径）＋`anonymized_at IS NULL`（已匿名客户排除）＋归一化号 HAVING COUNT(*)>1 收敛后拉全部命中行。
- MergeCandidateService：组内 N>2 两两配对；组分型 POOL（全 NULL 店）/SAME_STORE（全非空且同店）/CROSS_STORE（其余含混合）；逐对**双侧 canReadOwned**（storeCode,ownerStaffId）过滤，任一侧不可读整对丢弃；RefNameResolver 经 org/store name-map 富化门店名/owner 名，远程异常 catch 降级空 Map 不阻断；排序 groupType(POOL→SAME_STORE→CROSS_STORE)＋较早 createdAt＋pairId；score 恒 0.95（手机号归一精确命中常量，不伪造算法分），matchReasons=["PHONE"]。
- DTO：MergeCandidatePairDTO{pairId,groupType,matchReasons,score,sideA/sideB:Side{customerId,name,maskPhone,level,storeCode,storeName,ownerStaffId,ownerStaffName,createdAt}}；pairId=确定性 "MC-"+minCustomerId+"-"+maxCustomerId 纯读模型不落库；手机号一律 maskPhone 前3后4（138****8000），不回显明文/归一化号；**禁读死字段** total_spend/visit_count，期1 不带消费/到店字段（无真实到店源防伪造）。
- Controller：`GET /api/customer/merge-candidates` 挂 CustomerController，@RequirePerm("customer:merge")，无业务参（客户量级 100/双栈可接受）；只读不审计（与 list/search 一致）、不写表、无 Flyway、无新权限码。
- 测试：customer-service src/test 原为空、pom 仿 txn 补 spring-boot-starter-test（test scope）；MergeCandidateServiceTest 8 纯单测（不起 Spring、中文方法名：pair 两两组合/组分型/双侧过滤/排序等）。
- **铁律 12 OCR**：OCR CLI 本机不可用，7 文件（6 源码＋pom）已经人工逐文件审查（无密钥/无 PII 硬编码/无无关改动），审查结论载明于 `34bcfeb` commit message。

## 4. 三轨真验（全绿，逐轨留证）

### 4.1 卡1 三轨

- **curl**：手动 auto-upgrade/auto-downgrade 端点 level:edit 权限四角；降级告知 internal 端点无/错 token 401、网关外 /api/customer/internal/** 裸 404；公海客户降级只审计不发信。
- **PG（meiyun_seed 造数全还原）**：txn 聚合得 **375 行**月消费事实，事实表∀订单三角对账平；降级预演 **78 人**——接口返回 78＝customer_level_history AUTO_DOWNGRADE 78 行＝降级通知 78 条＝通知幂等唯一键 78＝审计 items 78；降级后各客户等级与事实 100% 对账平；autoUpgrade 切事实表后升级 **38 人**；升/降各自**重放 0 动作**（幂等短路＋游标不重复推进）。验完还原基线：等级复位、三新表测试行清空、78 通知/2 审计归零。
- **core 栈**：V36 结构双库建成；meiyun_core 仅留 V36 表结构与历史审计，**零测试数据污染**。

### 4.2 卡2 三轨

- **单测**：MemberDiscountClientTest 7 用例全绿——黑卡 SC001（0.80）原价 20999 分→折后 **16798**、折前 20999、优惠 **4201**；五级折后价 20999/19948/18900/17849/16798 逐档对账；4xx 透传、5xx fail-closed 502、连接失败回滚下单。
- **curl/PG（seed）**：三计价点（开单/零售/EMR 签署）折后单价 BigDecimal HALF_UP 分位落库，TxnOrder 5 列＋OrderItem 2 列快照完整；CARD_SALE 购卡不打折实证；客户折扣服务 5xx 时下单整体回滚（无脏单）；member-levels/catalog 五档真源。
- **Chrome**：Prescription/Consultation/DoctorWorkbench 折后预估＋原价划线；360 权益区折扣率/benefits/升级条件真源渲染；控制台零 error；前端提交原价、后端权威折后实证（篡改前端价不影响落库）。M4 EMR 医生看诊订单不再 403。
- **还原对账（seed）**：测试订单/通知等造数全清，五项基线计数 612/1111/5/320/1 全命中；core 零污染。

### 4.3 卡3 三轨

- **构建部署**：mvn -pl customer-service -am package BUILD SUCCESS（meiyun-security ClientIpTest 6 绿、MergeCandidateServiceTest 8 绿），fat jar 48,649,653 字节 unzip 实证含 MergeCandidateService/$Pair/DuplicatePhoneRow/MergeCandidatePairDTO/$Side 五 class；双栈真名容器 meiyun-customer-service/meiyun-seed-customer-service docker cp＋restart 均 healthy。
- **curl 角色矩阵（prod 8443 七角）**：无 token/错 token **401×2**；SE101 超管/E011 区域/E005 店长 **200×3**（基线空集 []）；E002 FRONT_DESK、E004 CONSULTANT **403×2**，中文「需要 customer:merge」（坐实有 view 无 merge 必须拦）；seed 18443 基线 SE001/SE101/E011 200 []、无 token 401。
- **PG 造数四视角（仅 meiyun_seed）**：先 pg_dump data-only 备份 `/tmp/customer_bak_b62card3.sql` 容器＋宿主双份 16695 字节；造 TESTMC01-07 共 7 行（同店 SST01 归一化变体：139空格9000-0001↔+8613990000001；跨店 13990000002↔139-9000-0002；公海 8613990000003↔(139) 9000 0003；TESTMC06 已匿名验证被排除）。四视角＝SE101 超管 **3 pair**（排序 POOL→SAME_STORE→CROSS_STORE）／E011 华东 **2 pair** 无 POOL（公海仅超管坐实）／SE001 SST01 仅 SAME_STORE（跨店对整对丢弃不泄露存在性）／SE005 SST02 **0 pair**；pairId=MC-min-max、score 0.95、["PHONE"]、掩码 139****000x、门店名富化上海徐汇店/上海浦东店全对；补验 owner 富化：UPDATE TESTMC01.owner_staff_id=SE002→ownerStaffName「林咨询」打通（其余侧 null 正常）。
- **还原/core 零污染**：8 张子表引用=0→DELETE 7 行，customer 回 **100**、门店分布 24/12/21/25/18 全命中基线、匿名 1、公海 0；还原后 seed/prod 端点复测均 []；core TESTMC=0、总数 13 未变、会话期间零审计写入（最新审计 id658=02:00 SYSTEM INSPECT 早于接管）。期1 无前端改动，Chrome 轨以 curl 角色矩阵＋PG 四视角替代（期边界内不接前端）。

### 4.4 审计哈希链验链（双口径并陈，append-only 不修复）

- **实时口径（本批复验，为准）**：core `GET audit verify` 报 `{"ok":false,"brokenAtId":258,"total":630}`；全表 max(id)=658、count=630，缺 28 号。PG `lag()` 实测**十处缺口**：173→177 缺3、247→258 缺10、300→302 缺1、378→380 缺1、380→382 缺1、518→520 缺1、521→525 缺3、646→650 缺3、650→654 缺3、654→657 缺2，合计 **28** 枚；verify 只报首处断点故停在 258。
- **历史口径（台账既载，并陈）**：B50 卡6 `aecf97f` 登记 prod breaks=[380,520]/569（两处历史断链，均早于 B48 `4bfb658` 部署窗口；B27 冒烟删 257-300、B29 L106「冒烟清理删行不回退序列」已留档），seed breaks=[]/117。两口径都指向历史既存缺号断链，非本批三卡造成。
- 处置：按 append-only 铁律**仅登记不修复**；本批 seed verify ok total=320 brokenAtId=null；core 会话期间零审计写入，断链存量与接管前一致。

## 5. 如实说明

### 5.1 L70 benefits 半项留存

卡2 闭合 discount 实际计价（三计价点＋订单等级快照＋360 权益区折扣/升级条件真源），但 **benefits 可享权益的核销链路未做**：benefits 当前仅是等级上的 List<String> 文案，无权益定义/实例/核销三表、无核销业务端点，360 权益区仅能真源展示文案与升级条件。核销属于独立大项（需权益产品模型＋核销写链路＋与订单/疗程/卡券的勾稽），04 L70 按阶段性注记半项闭合、不整行勾销，留存交易计价/权益专项批。

### 5.2 L44 撞单合并期1 边界

卡3 仅交付**候选发现只读能力**（发现/分型/数据域/掩码/富化/排序＋权限壳端点＋8 纯单测），明确**不包含**：合并写逻辑（保留/丢弃主档、字段冲突取舍）、合并审批流（现审批四类型无 MERGE）、合并审计哈希链、8 张物理 FK 子表迁移执行、points/card_ledger 读时 alias、member_card 双服务单写入方定案、customer_monthly_spend 合并聚合、ES outbox orphan 处置、前端 CustomerGraph/CustomerMerge 接真（现 338 行纯 mock）。卡0 已把迁移序列硬结论全量存档于哨兵 MACHINE:CARD 供写期直接取用。04 L44 加注记不勾销，02 L19 撞单合并行状态保持 ⬜。

### 5.3 历史断链双口径

§4.4 实时 brokenAtId=258/total=630 与台账历史 [380,520]/569 看似冲突，实为 verify「只报首处断点」与不同时点存量两个口径，十处缺号 28 枚＝max658−count630 已对账坐实，均为 B27/B29 冒烟窗口历史删行，append-only 不修。本 DELIVERY 以实时口径为准、历史口径并陈。

### 5.4 B57 DELIVERY「待写」观察项

03-timeline L88 P5-B57 行交付文档列仍为「DELIVERY-P5-B57（待写）」系既有遗留（B57 闭合存档详尽载于哨兵，但独立 DELIVERY 文件未补）。本批不擅动他批台账，登记为观察项；是否补写由后续批次或用户决定。

### 5.5 测试数据还原

三卡造数全部在 meiyun_seed 栈（卡3 含 pg_dump data-only 双份备份），验完精准 DELETE 还原（禁 TRUNCATE）：卡1 等级/三新表/78 通知/2 审计归零、卡2 五项基线 612/1111/5/320/1、卡3 customer=100 分布 24/12/21/25/18。双库保留本批正式 schema（V36 三表一列、TxnOrder/OrderItem 七列 ddl-auto、merge-candidates 只读端点），meiyun_core 零测试数据污染；审计 append-only 只读留档；/tmp 备份与 token 不入库。

## 6. 进度落账

批末六册＋本 DELIVERY＋哨兵同一 docs 原子提交（docs/ 被 gitignore：已跟踪 `git add -u docs/`，本新文件 `git add -f docs/DELIVERY-P5-B62-2026-09-19.md`）：

- **04-backlog L69**：整行加删除线，依赖列改「**✅ P5-B62 卡1 已闭合（2026-09-19，003d71e，详见 DELIVERY-P5-B62-2026-09-19）**：V36 customer_monthly_spend/customer_level_history/monthly_spend_state 三表＋auto_downgrade 列；UTC 闭合月游标事实聚合、连续3月低于保级线/保护期锚最近调级月默认3月/每次只降一级/幂等重放短路、autoUpgrade 切事实表累计 net（total_spend 死字段停用）、降级告知 internal 端点（按店 STORE_MGR/idemKey LEVEL:/公海只审计）、手动双端点 level:edit；seed 预演降级78人=history=通知=唯一键=审计、升级38人、重放0动作、全还原」，末列改「✅ B62」。
- **04-backlog L70**：不整行勾销，尾接阶段性注记「**◑ P5-B62 卡2 discount 半项已闭合（2026-09-19，59a79f8，详见 DELIVERY）**：internal level-discount（批量≤50/缺失兜底/fail-closed）＋member-levels/catalog，TxnOrder 5 列＋OrderItem 2 列等级快照，createOrder/createRetailOrder/signEmr 三计价点 BigDecimal HALF_UP（CARD_SALE 不参与、不与门店会员价叠加），前端折后预估/原价划线＋360 权益区接真（提交传原价后端权威），黑卡 20999→16798；**benefits 权益核销链路无定义/实例/核销底座，留存后续专项，本行不勾销**」。
- **04-backlog L44**：不勾销，尾接「**◑ P5-B62 卡3 期1 候选发现只读端点已就位（2026-09-19，34bcfeb，详见 DELIVERY）**：GET /api/customer/merge-candidates（customer:merge，三层手机号归一/POOL·SAME_STORE·CROSS_STORE 分型/pair 双侧 canReadOwned/掩码前3后4/score 0.95/owner 富化降级/8 纯单测），prod 七角 401×2·200×3·403×2、seed 四视角 3/2/1/0 pair、造数全还原 core 零污染；**合并写期（8 张物理 FK 子表迁移/审批流/合并审计/前端接真）留存，本行不勾销**」。
- **02-modules L16 会员等级行**：状态保持 ✅，旧注（B23 五档/手工调级/只升不降/定时批处理列 Backlog）保留不抹，尾接 B62 卡1＋卡2 长注（自动降级引擎闭合＋autoUpgrade 切事实表＋discount 三计价点真实计价＋订单等级快照＋360 权益区接真，benefits 核销留存）。
- **02-modules L19 撞单合并行**：状态保持 ⬜ 不翻 ✅，尾接卡3 期1 候选发现只读能力就位阶段性注记（与 04 L44 同口径，写期留存）。
- **00-history**：L6 前插 B62「**最近更新**：2026-09-19（P5-B62 客户运营纵深批：卡1 自动降级引擎 003d71e、卡2 折扣计价 59a79f8、卡3 撞单期1 34bcfeb；L69 整行勾销、L70 discount 半项 benefits 留存、L44 期1 注记；数字不变 ✅109/🔧1/⬜55）」巨型行；现 L6 B61 行首改「> 上一批 B61：」前缀下沉（内容全保留）。
- **01-dashboard**：L19 B61 bullet 前倒序插入 B62 bullet（三卡三 commit、L69 勾销/L70 半项/L44 期1、数字格 ✅109/🔧1/⬜55 一字不动）；域① 8/0/10 不变（纵深挂既有行，不新增尾注，避免域行臃肿——批细节以 02/04 注记为准）。
- **03-timeline**：L92 B61 行后正序追加 B62 六列行（commit 列仅列三 feat `003d71e`/`59a79f8`/`34bcfeb`，docs 心跳 d584333/4b0384a 不计；交付文档 DELIVERY-P5-B62-2026-09-19；末列纵深批数字不变、L69 勾销/L70 半项/L44 期1）。
- **索引 DEVELOPMENT-ROADMAP.md**：通读确认纯索引无随动。
- **HANDOFF-AUTO.md**：机读五行转 **BATCH=P5-B63、CARD=B63 卡0（04 L83-86 财务四行只读侦察）、STATUS 保持 ACTIVE**（遵从本自主队列用户第5步「卡片闭合后哨兵改写为下一张卡并保持 ACTIVE；全队列闭合才 DONE」，不沿用 B61 批末 DORMANT 旧例）；HEARTBEAT 取批末实时北京时间；PREV 锚本批 docs hash（提交后回填）；人读区顶部加 B62 闭合 bullet；以「## P5-B61 闭合存档」为界在其前加「## P5-B62 闭合存档」七要素块。

## 7. 下一批

**B63 财务收口批**（04-backlog L83-86 四行）卡0 只读侦察起步：
- **L83 通用异常中心**（跨域异常单据统一归集与处置工作台）：BOM 异常等现为各域分散后端 only；财务税/日结/异常页 B24 已切真但无统一处置中心——盘点各域异常单据类型/状态/端点与归集面。
- **L84 异常账务处置登记**（长短款/错账登记与审批闭环）：财务台账真实但缺异常账务专门登记审批流——盘点台账与审批四类型复用面。
- **L85 预收合规监控**（合规指标/挂账时长监控预警）：核销/预收明细 B24 已切真可读，缺合规监控规则与预警——盘点预收/核销数据源与预警 Job 范式（可复刻 ComplianceInspectionJob）。
- **L86 进项税抵扣链路**：当前发票/税页仅覆盖销项与台账视角，进项抵扣诚实未实现——盘点发票/税域实体与缺口边界。

按「卡0 只读侦察定缺口真伪与卡序→一卡一 commit+push→前端样式零改只换数据源→curl+PG+Chrome 三轨真验→测试数据还原→批末六册落账」施工。

队列（用户四阶段授权第①阶段第二层近线）：B63（本批）→B64（L87 M4 多级审批/L90 SOP 多模板/L91 随访分析/L75 EMR 模板集团运营）→B65 营销散项→B66 报表数据（R03-R09/R02 维度/毛利率/REVENUE 聚合/月结）→B67 测试体系（L88 造数工具化/L89 audit 保留选项/seed Flyway 专项）。近线全收后转第②阶段 **B68**：core 库 24 行营销演示残留仿 B57 备份＋物理删除；再第③阶段 **B69**：L133 REJECTED 回转、L140 transfer 渠道、L138 XLSX/PDF 选型（含竞品调研，按推荐拍板）；全部完成转第④阶段 **C 大阶段/专项开工**。
