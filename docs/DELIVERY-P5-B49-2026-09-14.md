# DELIVERY-P5-B49 · M1 集团屏批交付文档（2026-09-14）

> 批次：P5-B49（C 方向 · M1 集团管控屏）
> 拍板来源：用户 2026-09-14 白天「先 A（技术债还债收口）再 C（M1 集团屏，与已闭环的单店主线衔接最自然）」
> 状态：批中文档，随卡推进逐章补全，批末定稿随 docs 原子提交入库

---

## 一、批概要

- 入口台账：02-modules L131「M1 集团其余 14 页 ⬜」
- 目标：M1 集团屏 mock 页逐组接真（去 seed/硬编码），集团多店对比后端聚合走跨店例外域（铁律 -1-D，DataScope GROUP/BRAND 域）
- 完成度影响：本批每闭合一页/一组，94/166 完成度递增（B48 纯还债批数字不变系正常）

## 二、卡1 侦察：M1 全量现状三清单（本卡已闭合）

### 2.1 前端现状（router/index.ts L65-L82 + config/nav.ts，17 条 M1 路由）

| # | 菜单名 | 路由 | 组件 | 判定 | 证据 |
|---|---|---|---|---|---|
| 1 | 经营概览 | /m1 | M1OverviewView.vue (134 行) | mock | m1Overview.ts seed 空函数，KPI/趋势/排行全硬编码 |
| 2 | 指标矩阵 | /m1-matrix | M1MatrixView.vue (238 行) | mock | m1Matrix.ts rand() 伪随机 |
| 3 | 门店对标 | /m1-compare | M1CompareView.vue (170 行) | mock | m1Compare.ts rand(seed)，selectedIds 硬编码 |
| 4 | 数据大屏 | /m1-screen | M1ScreenView.vue (206 行) | mock | m1Screen.ts realtime 静态数组 |
| 5 | 门店主数据 | /m1-tenant | M1TenantView.vue (444 行) | mock | m1Tenant.ts seed 本地造数 |
| 6 | 区域管理 | /m1-region | M1RegionView.vue (307 行) | mock | m1Region.ts seed 仅调 tenantStore.seed() |
| 7 | 采购供应链 | /m1-procurement | M1ProcurementView.vue (450 行) | **半真实** | 库存台账已接 @/api/consumable；供应商/PO 仍 loadWorkflowDemo() |
| 8 | 品牌品类 | /m1-brand | M1BrandView.vue (534 行) | ✅真实 | m1Brand.ts listBrands/listCategories/listSkus（@/api/projectMaster） |
| 9 | 营销活动 | /m1-marketing | M1MarketingView.vue (362 行) | ✅真实 | m1Marketing.ts api.listCampaigns（@/api/marketing） |
| 10 | 调度中心 | /m1-dispatch | M1DispatchView.vue (269 行) | mock | m1Dispatch.ts seed 本地 |
| 11 | 合规中心 | /m1-compliance | M1ComplianceView.vue (296 行) | mock | m1Compliance.ts seed 本地 |
| 12 | 审计日志 | /m1-audit-log | M1AuditLogView.vue (202 行) | mock | m1Audit.ts seed 本地生成 |
| 13 | 健康度巡检 | /m1-health | M1HealthView.vue (205 行) | mock | m1Health.ts seed 本地 |
| 14 | 标准作业SOP | /m1-sop | M1SopView.vue (316 行) | mock | m1Sop.ts seed 本地 |
| 15 | 目标管理 | /m1-target | M1TargetView.vue (209 行) | mock | m1Target.ts seed=mk() 工厂造数 |
| 16 | 报表中心 | /m1-report | M1ReportView.vue (214 行) | mock | m1Report.ts seed=mkTemplates/mkJobs |
| 17 | 系统设置 | /m1-settings | SettingsView.vue（共享组件 537 行） | mock（config 驱动） | 非 M1 专属，复用全站设置页 |

汇总：**真实 3（含半真实 1）+ M1 专属 mock 13 + 共享 1 = 17**。台账「14 页待开发」= 13 mock + /m1-settings，数量吻合。
结构性发现：①侧栏无独立「集团管控」分组，M1 页散落工作台/门店运营/营销中心/管理后台 4 域；②路由 meta 只有 domain 无 title，标题在 nav.ts PAGE_TITLES；③m1Org.ts 真实但服务 T1OrgView（T1 权限中台），不属 M1。

### 2.2 后端供给对照（78 个 Controller 全量摸排）

| 页面 | 判定 | 证据与缺口 |
|---|---|---|
| 审计日志 | **需扩展（供给最完备，成本最低）** | audit-service AuditController 已有 GET /api/audit、/verify 验链、outbox stats/retry（B48 卡1 刚补强写入侧三道防线）。缺口：findAll() 全表无过滤无分页、字段缺 IP/风险等级、前端模块字典与 bizType 未对齐 |
| 区域管理 | 读已有/写需新建 | 读：GET /api/org/tree（集团→六区→23 门店）、/api/org/regions、/api/stores/regions/dist 齐备。写：OrgAdminController L17-19 明示「新建区域/门店不支持，入 Backlog」刻意收窄 |
| 门店主数据 | 读已有/写需新建 | 读：GET /api/stores、/api/stores/{code}、/api/org/org-units?orgType=门店。写收窄同上；Store 实体仅 6 字段（code/name/region/nature/status/openDate） |
| 经营概览/指标矩阵/门店对标 | **需扩展（一个聚合端点闭合三页）** | 原料齐备：/api/finance/revenue（门店×月，GROUP 域全量）、/ledger、/cost、/api/org/regions；无任何 overview/dashboard/group 聚合端点。新建集团聚合 BFF 端点（finance-service 宿主，照抄 DataScope GROUP 域逐行收敛模式）即可三页同闭 |
| 标准作业SOP | 需扩展（较完备） | txn FollowupSopController 模板/节点/启停/看板闭环完整，但系「术后随访 SOP」单场景语义；M1 SOP 页按随访 SOP 语义接真即可 |
| 报表中心 | 需新建（有底子） | 无报表模板/任务域；finance 已有 FinanceExportService+8 个 CSV 导出端点可作生成引擎底子 |
| 数据大屏 | 需新建（有底子） | SSE 基础设施（NotificationStreamController registry/心跳/握手）+订单数据源 GET /api/txn/order 可复用；需新增订单流 topic+大屏聚合端点 |
| 采购页缺口（供应商+PO） | 需新建 | 无 Supplier/PurchaseOrder 实体；供应商仅 Consumable 表自由文本字段；采购仅 stock-in 流水。挂 store-service consumable 包下新建 |
| 调度中心 | 需新建 | 仅 RbacAdminController.transferStaff 员工调动可蹭；无调度单域 |
| 合规中心 | 需新建 | 仅 ai-service PrivacyController（限 AI 隐私）；无集团合规域 |
| 健康度巡检 | 需新建 | 无业务端点 |
| 目标管理 | 需新建 | 无实体无端点 |

跨店机制：meiyun-security DataScope 五级（SELF/STORE/REGION/GROUP/BRAND）已成熟，GROUP/BRAND 全量读即跨店通道；范例=FinanceController 聚合层按门店域逐行收敛、OrgController.staff REGION 域回收。**无独立集团聚合模块**，M1 聚合端点照此模式落地。

### 2.3 分组序列定案（按闭合成本+衔接自然度升序）

| 卡 | 内容 | 后端动作 | 前端动作 | 依据 |
|---|---|---|---|---|
| 卡2 | 审计日志 /m1-audit-log | GET /api/audit 加过滤（bizType/actor/时间/关键字）+分页 | m1Audit store 接真去 seed | 供给最完备，B48 卡1 刚闭环写入侧，趁热打铁 |
| 卡3 | 区域管理+门店主数据 /m1-region+/m1-tenant | 无需改（读侧齐备；写侧维持 Backlog 收窄口径） | 两 store 接真读视图 | 读接口现成，两页同源（org/tree+stores）一并闭合 |
| 卡4 | 集团聚合端点+概览/矩阵/对标 /m1+/m1-matrix+/m1-compare | finance-service 新建集团聚合端点（GROUP 域逐行收敛） | 三 store 接真 | 一端点闭合三页，性价比最高 |
| 卡5 | 采购页补全（供应商+PO） /m1-procurement | store-service consumable 包下新建 supplier+PO 域 | m1Procurement 供应商/PO 接真 | 半真实页补全，单店采购链延伸 |
| 卡6 | 标准作业SOP /m1-sop | 复用 FollowupSopController（随访 SOP 语义） | m1Sop store 接真 | 后端闭环已在，前端模板语义对齐即可 |
| 卡7 | 数据大屏 /m1-screen | 订单流 SSE topic+大屏聚合端点 | m1Screen 接真 | SSE 底子复用 |
| 卡8+ | 目标/合规/健康度/报表/调度（五页全新域） | 逐域建表+端点 | 逐 store 接真 | 缺口最大，批内按序推进或视批幅拆分 |

口径说明：/m1-settings 系共享 SettingsView（全站设置），不计入 M1 闭合页数；台账「14 页」按 13 个 M1 专属页闭合即视为本批目标达成，settings 维持现状。

---

## 三、逐卡交付记录（批中补全）

### 卡2 审计日志 /m1-audit-log（已闭合，feat `43f52ca`）

**改动清单（6 文件 +421/-155）**

后端 audit-service（3 文件）：
- `AuditController.java`：新增 GET /api/audit/page（bizType/actor/keyword/from/to/page/size 六参，OffsetDateTime 反序列化）+ GET /api/audit/facets 两端点，均 @RequirePerm("audit:view")；原六端点（含 GET /api/audit 全链 List）零改动——铁律 1 契约零破坏，m5Settings.ts L237 消费回归实测 553 条键不变。
- `AuditService.java`：+search（JPA Specification：bizType/actor 精确等值、createdAt 范围、keyword 对 action/target/actor/payload 四字段 lower-like（jsonb 列 `root.get("payload").as(String.class)` cast），id 倒序，size 夹 1~200）+ PageResult record + facets（JdbcTemplate 聚合 total/last24/actors/bizTypes 带计数）；verifyChain() 维持首处失配即返回口径。
- `AuditRepository.java`：+JpaSpecificationExecutor<AuditLog>。

前端（3 文件）：
- `api/audit.ts`（新建）：AuditLogRow/AuditPageResult/AuditFacets 等类型（facets bizTypes 键名 `count` 与后端契约一致）+ pageAuditLogs/getAuditFacets/verifyAuditChain。
- `stores/m1Audit.ts`：去 seed 接真，63 项 BIZTYPE_LABEL 全量中文映射、SENSITIVE_RE 敏感字段派生、listStaff 姓名缓存。
- `views/M1AuditLogView.vue`：整页重写——KPI 四卡（总量/24h/操作人/模块数+链完整性卡点击重验）、63 项带计数模块下拉、五过滤+七列表格、au-pager 服务端分页、详情侧栏（prev/cur 双哈希+payload JSON 美化+append-only 页脚注）。

**三轨真验（对账一致）**
1. 构建：backend mvn package + frontend npm run build（vue-tsc）双 exit 0；frontend 烘焙式镜像重建+gateway 重启生效。
2. curl 经网关 8443 十项：/page 首页 first_id=571（AI_MODEL）、分页 28 页（20/页）、size=999 截断 200；facets={"total":553,"last24":72,"actors":25,"bizTypes":63 项}；五过滤逐项——bizType=ROLE 20、actor=E011 152、keyword=E014 106、from=2026-09-14(+08) 47、ROLE+E011 组合 15。
3. PG（docker exec meiyun-pg psql meiyun_core）七项对账与 curl 全一致：total=553、last24=72、distinct actor=25、distinct bizType=63。
4. Chrome 经 nginx 8080 /m1-audit-log：E011（REGION_MGR）登录态 KPI/下拉/表格/分页/详情侧栏全活，console 零 error。

**断链事实记录（铁律 0 如实口径）**
- GET /api/audit/verify 实测 `{"ok":false,"brokenAtId":380,"total":553}`，页面 KPI 如实显「断链 #380」。
- PG lag() 全链扫描确认两处真实断链：**#380（2026-09-12 11:49）与 #520（2026-09-13 20:01）**，id 379/381 序列空洞（回滚迹象）。
- git log 证实 B48 审计修复（`4bfb658`）部署于 2026-09-14 09:41，两处断链均早于修复属历史并发窗口，修复后无新增断链。
- audit_log append-only 不可篡改，历史存量保持原状（与 04-backlog id=380 保留口径一致）；verifyChain 首处失配即返回只报 #380、#520 不被报告为已知口径，「全量断链清单返回」增强已登记 04-backlog（平台治理批）。

### 卡3 区域管理+门店主数据 /m1-region+/m1-tenant（已闭合，feat `494c272`）

**改动清单（6 前端文件 +248/-653，后端零改动）**

- `api/org.ts`（铁律 1 只加不改）：Store 接口扩 `region?/nature?/status?/openDate?: string|null` 四可选字段；新增 `StoreRegionDist` 七字段接口（region/openCnt/ownCnt/jointCnt/buildingCnt/closedCnt/total）+ `listStoreRegionDist()`；原端点零改动。
- `stores/m1Tenant.ts`（全覆写 107 行）：去 seed/create/update/setStatus 只读化；`listStores()` 取 `res.data`；STATUS_FROM_CN 三值映射（营业中→OPERATING/筹建中→SETTING_UP/已关店→SUSPENDED，未知值 console.warn+归 SUSPENDED、标签用 statusText 原文）；CITY_PREFIXES 16 城常量表门店名前缀派生城市（未匹配 '—'）；导出 byRegion=Record<string,Tenant[]>、natures=string[] 动态键。
- `stores/m1Region.ts`（全覆写 110 行）：双数据源 Promise.all（getOrgTree+listStoreRegionDist）；区域短名匹配键 `orgName.replace(/事业部$/,'')`；tree 过滤 orgTypeCode==='REGION' 按 sortNo 排序；leaderName 全 null→'—'；dist miss→EMPTY_STATS；withStats computed 合并。
- `views/M1TenantView.vue`（全覆写 184 行）：删新建/编辑弹层/停用确认/ops 列/canEdit/v-perm；7 列只读表（code/name/region/city/nature/status/openDate）；筛选=区域（byRegion 动态键）/状态/性质（natures 动态）/关键词；KPI 四卡含已关店；CTable emptyText 三态（加载中…/error/无匹配门店）+error 重试；页脚注（数据源 /api/stores+DataScope 随登录人数据域+写操作收窄 Backlog 只读）。
- `views/M1RegionView.vue`（全覆写 160 行）：删全部写按钮/两弹层/覆盖城市 chips/月营收目标；KPI 四卡=运营中区域/区域总数/门店总数/营业中门店；六区卡体=经理/门店数（营业中）/直营·联营/筹建·关店；empty 三态+error 重试；页脚注（org/tree+regions/dist 集团聚合视角不受数据域收窄+写侧收窄只读）。
- `config/domain-home.ts`：L315 '/m1-tenant'→'门店档案总览'、L316 '/m1-region'→'六区组织框架'（原 '6 家门店'/'2 个区域' 与真实 DataScope 数字冲突，中性化）。

**关键定性（铁律 0 如实口径）**
- DataScope：E011=REGION_MGR（region=华东），`GET /api/stores` 实测仅返回华东 6 店系 REGION 域**正确收窄**（非 bug）；`/api/stores/regions/dist` 返回六区 23 店全量统计（聚合通道豁免）；`/api/org/tree` 返回集团→六区→23 店全量树。门店页随登录人数据域呈现、区域页用 tree+dist 集团视角，页脚注如实载明。
- 写侧收窄定案：后端无需改（读侧齐备）；OrgAdminController 明示「新建区域/门店不支持，入 Backlog」；两 store/两 view 删全部写入口，页面只读+页脚注说明，与既有 Backlog 收窄口径一致。
- 派生口径：城市=门店名前缀匹配 16 城常量表；区域短名=`replace(/事业部$/,'')` 作 dist 匹配键（Chrome 实证六区全部命中，无 EMPTY_STATS 全 0 卡）。

**三轨真验（对账一致）**
1. 构建：frontend npm run build（vue-tsc）exit 0（首败 4 处 TS 错——client.get 返回 AxiosResponse 未取 .data，修 `distRows.data ?? []`/`tree.data?.children ?? []` 后过，21.31s）；frontend 烘焙式镜像重建+gateway 重启生效。
2. curl 经网关 8443 三端点：/api/stores 华东 6 店字段吻合（筹建中 openDate=null）；/api/stores/regions/dist 六区统计（华东 6/5/3/2/1/0，华北 5/closed1，华南 4，华中 4，西南 3/closed1，西北 1，total 和=23）；/api/org/tree 六区 ACTIVE、sortNo 1-6、leaderName 全 null。
3. PG（docker exec meiyun-pg psql meiyun_core）对账：store 表 23=营业中 18+筹建中 3+已关店 2；六区 6/5/4/4/3/1 与 dist 逐区一致。
4. Chrome 经 nginx 8080：E011 登录 /m1-tenant 6 行、KPI 5/1/0/6=dist 华东行、城市派生全对（上海/南京/杭州）、筹建中 openDate='—'、页脚注正确；/m1-region 6 卡 sortNo 序、KPI 6/6/23/18=PG 基线、逐卡统计与 dist 全吻合、经理 '—'；两页 console 零 error 零 warn。

### 卡4 集团经营概览/指标矩阵/门店对标 /m1+/m1-matrix+/m1-compare（已闭合，feat `f672a80`）

**改动清单（8 文件 +407/-154）**

- `backend/finance-service/.../FinanceController.java`（488 行）：新增 GET /api/finance/group-overview 集团聚合端点（**铁律 -1-D 跨店例外域**：M1 集团多店对比屏不受门店锁定限，SKILL.md L94-97）；DataScope.storeSpec("storeCode") 按登录人数据域解析可见门店集合，LinkedHashMap 聚合保持门店序、parallel 空安全；默认期口径「已出月报门店数最多的月份（**并列取最新，随数据域而定**）」。
- `frontend/src/api/finance.ts`（741 行，铁律 1 只加不改）：+GroupMonthTotal/GroupOverviewView 两接口 + listGroupOverview()，原端点零改动。
- `stores/m1Overview.ts`（143）/m1Matrix.ts（150）/m1Compare.ts（154）三 store 全量重写：去 mock 接真，null 显「—」不伪造，fen2wan 分→万换算，score 权重归一+负截 0。
- `views/M1OverviewView.vue`（140）/M1MatrixView.vue（253）/M1CompareView.vue（177）：23 处接线+三页页脚注（数据源/默认期口径/无源指标「—」口径如实载明）。

**关键定性（铁律 0 如实口径）**

- E011 华东域默认期 2026-09 系**域收窄正确非 bug**：华东仅 ST-SH-001 有月报（2026-09 行），「已出月报门店数最多」在华东域内命中 2026-09，与集团全域口径（2026-07 五店）不同系数据域不同所致，页脚注如实载明。
- **口径修正记录（自发现自修复）**：三页页脚注/注释原硬编码「当前 2026-07」4 处，随默认期规则化一并修为 generic 表述（「已出月报门店数最多月份·并列取最新，随数据域而定」），杜绝月份漂移再犯——教训：脚注/注释不得硬编码易漂移事实，一律写规则口径。
- PG 对账基线（单位分）：revenue_monthly 7 行 5 店——2026-07 五店（ST-SH-001 280000000/146720000/133280000/grossRate 0.476）；2026-09 两店（ST-BJ-001 20000/0/20000/1.000；ST-SH-001 506000/1512500/-1006500/-1.989 负毛利如实呈现）；无 2026-08。
- **Backlog 三项登记 04-backlog**：①无 GROUP 域账号（SUPER_ADMIN=集团域但 staff 0 行，集团全量域真验缺口）；②新客/复购/治疗人次/满意度/活跃客户/利用率/mom/yoy 等无月度数据源，待 txn/org 域月度聚合投影；③revenue_monthly 覆盖不足（仅 7 行 5 店、无 2026-08，依赖月结流程推广）。

**三轨真验（全绿）**

1. 构建：vue-tsc 0 error + npm run build ×2 exit 0；frontend 烘焙镜像重建+docker cp 生效。
2. curl 经网关+PG 对账：/finance/group-overview 与 PG revenue_monthly 精确吻合（ST-SH-001 2026-07：280000000 分/146720000/133280000/grossRate 0.476 逐项一致）。
3. Chrome 经 nginx 8080 三页矩阵：2026-07↔2026-09 双向切换（280/47.6 与全「—」两态正确）、/m1 hero 0.5万元（506000 分→0.5 万，fen2wan 正确）、/m1-compare score 0 归一+500/0.5、55/-198.9（负毛利不截断如实呈现）；三页 console 零 error 零 warn。

**仪表盘推进**：97→**100/166=约60%**，⬜ 67→**64**；域⑧ 23✅→**26✅**、42⬜→**39⬜**；M1 集团屏 3/17→**6/17** 切真（3 真实+6 已切真+8 mock）。

---

### 卡5 采购补全 /m1-procurement（已闭合，feat `a108203`）

**改动清单（15 文件 +1425/-80）**

- 后端 store-service 新建 `procurement` 包 13 文件：Supplier/SupplierRepository/SupplierController、PurchaseOrder/PurchaseOrderItem/PurchaseOrderRepository/PurchaseOrderItemRepository、GoodsReceipt/GoodsReceiptRepository、PoNoGenerator、PurchaseOrderService、PurchaseOrderController、ProcurementDataInitializer（种子 4 供应商（1 停用）/6 PO 六态/2 收货单、审计种子 21 条）。
- 前端：新建 `api/procurement.ts`（9 端点+DTO 镜像）、`stores/m1Procurement.ts` 重写 +112/-80 接真实端点，**M1ProcurementView 零改动切真**。

**六态状态机与驳回/作废规则**

DRAFT→SUBMITTED→APPROVED/REJECTED→PARTIAL→RECEIVED；驳回回 DRAFT 并带 rejectNote；作废仅允许 DRAFT/SUBMITTED 两态；重复提交/越态流转 409/422 中文报错。

**三级审批阈值**

按采购单金额（元）：门店 ≤5000 / 区域 ≤50000 / 集团 >50000；审批动作落审计。

**库存联动与移动均价**

收货入库联动 consumable 库存，移动均价重算实验证：HC-001 (99×1500+100×1200)/199=**1348 分**；批次号 `PO<poNo>-R%02d-<lineNo>` 幂等（同单同行重复收货不重复入库）；PG 对账库存 SST02 HC-001 50→90、SST03 HC-002 30→80、HC-006 20→120。

**权限与数据域**

resolveReadStoreCode 三段守卫与 consumable 端点逐条一致：不带过滤=全域、越权过滤=空数组、详情/动作 404 不泄露存在性。

**部署（Docker 双栈）**

后端 docker cp fat jar→`/app/app.jar`+restart；前端 docker cp dist→nginx 免重启。

**三轨真验（全绿）**

1. 构建：mvn package + vue-tsc 0 error + vite build exit 0。
2. curl 经网关全分支矩阵：201 创建/409 重复/400 参数/422×4 越态/404×2 不泄露/403 越权，报错全中文。
3. Chrome 经 nginx 8080 三 tab（供应商/采购单/收货）console 零 error 零 warn；audit_outbox 无新增。

**真验数据留存口径**

SUP-T1/PO1 RECEIVED/PO2 CANCELLED/PO3 DRAFT/PO4 APPROVED（含 GHOST-1 行）append-only 留存，**04-backlog 不新增行**（比照 B47 先例）。

**🔧2→1 勾稽说明**

M1 集团 🔧 行半成品成因=procurement 供应商/PO 前端 mock，本卡补全后成因消解：✅ 100→**101/166=约61%**、🔧 2→**1**（<1%）、⬜ 64→**63**（约38%）；参照 B37 营销设置先例——🔧 勾销经 ✅+1 体现，域表 🔧 列维持历史计数差口径不动。域⑧ 26✅→**27✅**、39⬜→**38⬜**，大表新增「M1 · 采购管理」独立 1 行；M1 集团屏 6/17→**7/17** 切真（3 真实+7 已切真+7 mock）。

---

## P5-B49 卡6：M1 标准作业SOP /m1-sop 整页去 mock（feat `d4c6453`）

**交付**：store-service 新建 sop 域 9 文件（`com.meiyun.store.sop`：SopTemplate 61/SopTemplateStep 39/SopTask 79 三实体+三 Repository+SopService 460+SopController 162+SopDataInitializer 164 种子），**JPA ddl-auto 自动建表、无 Flyway migration**；前端新增 `api/sop.ts` 111 行、`stores/m1Sop.ts` 切真实 API（合计 11 文件 +1283/-10）。

**SopController 八端点**：`GET /templates` 模板列表、`POST /templates` 新建模板、`POST /templates/{id}/publish` 发布、`GET /tasks` 任务列表、`POST /tasks` 按模板生成任务、`POST /tasks/{id}/start` 开始、`POST /tasks/{id}/steps/{stepRef}/toggle` 步骤逐项勾选、`POST /tasks/{id}/complete` 完成。

**状态机**：模板（新建→publish 发布）；任务（按模板生成→start 开始→步骤逐项 toggle 勾选→complete 完成）。

**三轨真验全绿**：curl 经网关状态机 16 断言+权限三角（员工/店长/越权）9 断言+审计 17 行落库；SopDataInitializer 种子 PG 对账一致；Chrome 经 nginx 8080 双 tab console 零 error/warn；mvn+vue-tsc+vite build 全绿。真验数据沿用批内留存口径（种子模板+任务保留，供批末复核）。

**数字勾稽**：标准作业SOP 1 页 ⬜→✅，✅101→**102**（102/166=约61%），⬜63→**62**（约37%），🔧1 不动；大表域⑧ 新增「M1 · 标准作业SOP」独立 1 行、27✅→**28✅**、38⬜→**37⬜**，M1 集团其余页 7→**6**；M1 集团屏 7/17→**8/17** 切真（3 真实+8 已切真+6 mock）。

**backlog 新登记 2 项**：①卡5 采购列表越权观察项（列表按请求头上下文过滤，修复=store_id 强制过滤+越权用例补强，去向 B49 批末或 B50 首卡）；②ai-service pg-ai seed 库 Flyway V17-V29 悬置（去向 seed 库版本化专项）。

## P5-B49 卡7：M1 数据大屏 /m1-screen 整页去 mock（feat `815f006`）

### 侦察确权（六数据块 → 真实数据源，全部落位）

页面：`frontend/src/views/M1ScreenView.vue`（206 行深色大屏）+ `stores/m1Screen.ts`（80 行 mock 六数据块）。主战场 txn-service。

1. **今日营收/成交单/客单价/hourly 趋势** ← `txn_order`：status='已收款' + createdAt 落北京自然日（复用 InternalDailyController daily-metrics 口径先例）；客单价=营收/成交单；hourly=已收款金额按小时分桶（0-23 全量出参，前端取营业时段渲染）。
2. **今日到店** ← `arrival`（arrivedAt 落当日行数）；**在院治疗** ← `arrival` status IN (TRIAGED,CALLED) 当日行数。
3. **今日预约** ← `appointment`（apptDate=当日 AND status<>'已取消'）。
4. **品类占比** ← `order_item.item_name` → `product_sku.name` 精确匹配（txn 新增 StoreProjectClient 经 X-Internal-Token 调 `GET /api/stores/skus` 空参全量；内部 token=GROUP 全域+perms[*]，meiyun-security AuthInterceptor L43-48 放行 brand:view）→ `serviceCategory` 五枚举（INJECTION/LASER/SKINCARE/BODY/EXAM）映射中文（注射美容/光电美肤/皮肤护理/形体管理/检测咨询），null/未命中→「其他」。**无需 join 品类表，一次调用+60s 缓存**。
5. **门店营收排行** ← `txn_order` 已收款按 storeCode 聚合 top5（`DataScope.storeSpec("storeCode")` 收敛，卡4 group-overview 先例）；门店名前端 join `GET /api/stores`（org 域 listStores，卡4 同例，端点不跨域 join）。
6. **实时成交流** ← `order_payment` 流水 SSE 推送。**mock 的 channel（获客渠道）列诚实改为支付方式 payMethod**（订单无获客渠道字段；payMethod 五值 cash/card/wxpay/alipay/balance → 现金/刷卡/微信/支付宝/余额）。
7. **客户名脱敏** ← customer `GET /api/customer/name-map?ids=`（@RequirePerm internal:name-map，仅内部身份可调，CustomerController L351 已有）服务端脱敏为「王**」后出参。

### 设计定案

- **端点 A（快照）**：`GET /api/txn/screen/overview`（@RequirePerm("screen:view")，PermissionMatrix 已有 4 角色）→ `{kpis:[{key,label,valueFen|value,unit,deltaPct}], hourly:[{hour,amountFen}], categoryShare:[{name,pct,color?}], storeRanks:[{storeCode,amountFen}], notes}`。deltaPct=较昨日同口径（昨日为 0 → null，前端不显示箭头）；金额统一「分」出参（fen 后缀键名，卡4 一致）。
- **端点 B（流）**：`GET /api/txn/screen/stream`（SSE，TEXT_EVENT_STREAM）——握手/心跳/cleanup 全量复用 NotificationStreamController 模板：`?access_token=` 查询参数握手（EventSource 无法设 Authorization 头）、25s comment 心跳帧（规避代理空闲超时）、30min 整体超时、统一 cleanup、建连先发 `ready` 事件。
- **扇出（零侵入收款主链路）**：新建 `ScreenOrderFanoutJob`（NotificationFanoutJob 同构纯新增）`@Scheduled(fixedDelay=3000)` 轮询 `order_payment`（OrderPaymentRepository 新增 `findByCreatedAtAfterOrderByCreatedAtAsc`，只加不改）watermark 内存游标；关联 txn_order（findByOrderNoIn 已有）+ name-map 脱敏富化 → 新建 `ScreenStreamRegistry.broadcast`（NotificationStreamRegistry 同构）推 `name="order-paid"` 事件；payload 带 paymentId，前端 Set 去重。重启不补历史（大屏定位可接受，如实标注）。
- **口径单一来源**：KPI/hourly/品类/排行只由 overview 出参（前端 30s 定时重取）；SSE 仅驱动实时成交流顶插（>8 pop，维持 mock 交互节奏），**不就地累加 KPI**（防口径漂移）。
- **前端**：新增 `api/screen.ts`（client.ts 系，TOKEN_KEY='meiyun_token'；request.ts 已确认孤儿零引用）；`m1Screen.ts` 去 mock 接真（保留六数据块形状，RealtimeOrder.channel 语义改支付方式）；M1ScreenView.vue 接线 + EventSource（`/api/txn/screen/stream?access_token=`+getToken()）；排行 bar 归一分母改动态 max；品类 donut 图例动态渲染；页脚口径注如实标注。
- **网关/代理零改动**：Go `httputil.NewSingleHostReverseProxy` 对 text/event-stream 原生即时 flush；vite http-proxy 透传；/api/txn 路由既有（域⑦ 通知流已通）；25s 心跳兜底。
- **风险如实标注**：①itemName→SKU 名称精确匹配为尽力而为（同名跨品牌取首命中；售卡单 itemName=卡项名不在 SKU 目录 → 归「其他」，后续批次可扩展 catalog_product 映射）；②营收/hourly 按订单 createdAt 落日口径（非支付完成时点，与 daily-metrics 先例一致）；③扇出延迟 ≤3s+调度抖动；④品类占比基数=当日已收款订单子项金额合计。

**交付闭合**（feat `815f006` 已推送，13 文件 +848/-79）：后端 txn-service 新建 screen 包 5 新 4 改——新建 ScreenController（GET /api/txn/screen/overview @RequirePerm("screen:view")+GET /api/txn/screen/stream SSE）、ScreenOverviewService（六 KPI：revenue/paidCount/avgTicket/apptToday/occupancy/alertCount，deltaPct 昨日 0→null 不伪造箭头、hourly 0-23 全量分桶、品类 itemName→SKU 精确匹配→serviceCategory 五枚举、门店 top5 DataScope.storeSpec("storeCode")）、ScreenStreamRegistry（同构 NotificationStreamRegistry：建连 ready+25s comment 心跳+30min 超时统一 cleanup）、ScreenOrderFanoutJob（fixedDelay=3000/initialDelay=15000，watermark 内存游标+recentIds cap500、name-map 脱敏富化、order-paid 八键 payload 带 paymentId、重启不补历史如实标注）、StoreProjectClient（X-Internal-Token、SKU→品类映射 60s 缓存）；改 OrderPaymentRepository +findByCreatedAtAfterOrderByCreatedAtAsc（铁律 1 只加不改）等 4 改；无 Flyway 零表结构变更。前端 1 新 3 改：api/screen.ts 新建（client.ts 系，TOKEN_KEY='meiyun_token'）、m1Screen.ts 去 mock（RealtimeOrder.channel 语义改支付方式）、M1ScreenView.vue 接线+EventSource（30s 定时重取 overview、SSE 仅顶插成交流 >8 pop 不就地累加 KPI 防口径漂移、排行 bar 归一分母改动态 max、品类 donut 图例动态渲染、页脚口径注如实标注）、frontend/nginx.conf SSE 专块。

**真验中修复两缺陷**：①P0 SSE 认证串扰——Spring SSE 异步线程归还 Tomcat 池后 plain ThreadLocal 残留串前手身份；修复=AuthInterceptor implements AsyncHandlerInterceptor+afterConcurrentHandlingStarted 清理+403 分支即时清理+两 SSE controller 握手局部变量化；复验种子栈 42+主栈 12=54 样本全绿。②nginx SSE 缓冲——location /api/ 默认 proxy_buffering on 攒帧；修复=新增 `location ~ ^/api/.+/stream$` 专块（proxy_buffering off/proxy_cache off/http_version 1.1/Connection ''/read_timeout 3600s/send_timeout 3600s）双栈 reload。

**三轨真验全绿**：curl 权限三角 E002 403/E011 200；种子 PG 跨夜对账 paid_orders=0/appt_today=0/yesterday=7/deltaPct=-100.0/null 不伪造箭头；Chrome PG 直插三笔 SCR20260915001/002/003（append-only）实时顶插「16:32 上海浦东店 许** 热玛吉眼周 现金 ¥12,800」+KPI 联动 revenue=2,358,000 分/paidCount=3/avgTicket=786,000 分/storeRanks top2/categoryShare 其他 100%，console 零 error 零 warn；mvn package+vue-tsc 0 error+vite build exit 0 双栈部署。

**数字勾稽**：✅102→**103**（103/166=约62%），⬜62→**61**（约37%），🔧1 不动；域⑧ 28✅→**29✅**、37⬜→**36⬜**（🔧 列 2 维持历史计数差口径不动）；大表新增「M1 · 数据大屏」独立 1 行；M1 集团其余页 6→**5**；M1 集团屏 8/17→**9/17** 切真（3 真实+9 已切真+5 mock）。

**backlog 新登记 1 项**：成交流时间 UTC 原文显示未转 Asia/Shanghai（m1Screen.ts `paidAt.slice(11,16)` 直接截取 UTC 字符串，页面「16:32」实为北京时间 00:32；修复=复用 utils/datetime.ts 时区工具，去向 B49 批末或 B50 首卡）。

---

## P5-B49 卡8+：M1 其余五页全新域侦察确权（序列照 §2.3 定案：目标→合规→健康度→报表→调度）

### 五页侦察确权（mock 数据域 → 后端归属，全部落位）

1. **卡8 /m1-target 目标管理**（首卡，finance 域衔接最自然）：前端 `m1Target.ts`（132 行）——TargetLine 单实体：ownerType 三级（GROUP/REGION/STORE 分解树 children 串联）、metric 五枚举（REVENUE/NEW_CUSTOMER/REPURCHASE_RATE/PROCEDURE_COUNT/SATISFACTION）、period 三枚举（MONTH/QUARTER/YEAR）、approval 四态状态机（DRAFT→PENDING→APPROVED/REJECTED）、weight 权重加权综合达成（仅算 APPROVED 且 GROUP 行）、progress=current/target 百分比四档成色（≥100 ACHIEVED/≥85 ON_TRACK/≥60 AT_RISK/余 BEHIND）。后端：finance-service 新建 target 域（平铺风格：BizTarget+Repository+Service+Controller+DataInitializer 种子）；§2.2 判定「需新建，无实体无端点」。端点需求：GET 列表（ownerType/metric/period/approval 过滤）+POST 新建+PUT 更新进度+POST submit/approve/reject。**首卡收窄**：目标值与实际值均手工维护（与 mock 语义一致）；REVENUE 实际值自动聚合 RevenueMonthly 增强登记 backlog。DataScope GROUP 域读全量，写侧权限按角色收窄。
2. **卡9 /m1-compliance 合规中心**：前端 `m1Compliance.ts`（154 行）——双实体：CheckItem（六类 QUALIFICATION/CONSENT/DRUG_TRACE/PRIVACY/AD/INFECTION × 四态 PASS/WARN/FAIL/PENDING、按门店维度、recheck 复检 FAIL/WARN→PENDING→PASS）+ AuditLog（六动作 IMPERSONATE_START/END、EXPORT_DATA、PERMISSION_CHANGE、SENSITIVE_VIEW、CONFIG_CHANGE × 三级风险）+ impersonate 会话管理（必须填理由全程留痕）。后端：audit-service 扩展（卡2 基建复用）——新建 compliance_check 表+端点；审计留痕部分复用 audit_log 表（bizType 新枚举）+卡2 GET /page 五过滤直接可用。**收窄**：真实代操作会话（org-service 身份切换）登记 backlog，首卡仅审计留痕真实化。
3. **卡10 /m1-health 健康度巡检**：前端 `m1Health.ts`（190 行）——双实体：TenantHealth（六维 SAFETY/SERVICE/FINANCE/COMPLIANCE/STAFF/EQUIPMENT 评分 0-100 带权重 SAFETY/COMPLIANCE×2，overall 加权总分四档 ≥85 HEALTHY/≥70 WARNING/余 CRITICAL）+ HealthIssue（OPEN→PROCESSING→RESOLVED/IGNORED 四态、HIGH/MEDIUM/LOW 三级、assignee/dueAt/resolution）+ rerun 重新巡检按未解决严重问题动态重算分数。后端：store-service 新建 health 包（procurement/sop 同模式）：health_score+health_issue 两表 ddl-auto+种子+Controller；§2.2 判定「需新建，无业务端点」。
4. **卡11 /m1-report 报表中心**：前端 `m1Report.ts`（165 行）——ReportTemplate 9 模板（六类 REVENUE/CUSTOMER/OPERATION/FINANCE/COMPLIANCE/STAFF、period 六枚举、dimensions/metrics 数组、subscribed 订阅）+ ReportJob 生成历史（READY/GENERATING/FAILED 三态、format XLSX/PDF/CSV、rowCount/fileSize）+ SAMPLE_DATA 两模板假预览 + **mock setTimeout 假生成（必须剔除）**。后端：finance-service 新建 report 域（模板表+任务表）；§2.2 底子=FinanceExportService+8 个 CSV 导出端点可作生成引擎。**首卡收窄**：generate 真实异步生成 CSV（XLSX/PDF 登记 backlog）；预览 R01 门店营收日报接 order_payment 聚合、R02 月度经营接 RevenueMonthly，其余模板预览收窄登记。
5. **卡12 /m1-dispatch 调度中心**（最复杂，批末或转 B50）：前端 `m1Dispatch.ts`（193 行）——三实体：Resource（DOCTOR/ROOM/DEVICE 含 workStart/workEnd 班次时段+ON/OFF）、Job（待派单 PENDING/ASSIGNED、NORMAL/URGENT、durationMin/preferredDoctor）、Assignment（resource×时段占用块 SCHEDULED/IN_PROGRESS/DONE）+ 派单冲突检测（半小时格 22 格 9:00-20:00）+ 利用率=已占用/可用时长。后端：跨三服务组装——房间/设备主数据在 store-service（room/equipment 包已有）、医生排班 org-service、预约工单 txn-service appointment；§2.2 判定「需新建，无调度单域，仅 RbacAdminController.transferStaff 可蹭」。新建 dispatch 域（宿主倾向 txn-service 近 appointment 源）；写侧派单/释放+冲突检测服务端化。视批幅拆分 B50 候选。

**后端包结构现状（侦察实证）**：finance-service 单文件平铺（FinBudget/RevenueMonthly/FinanceController/FinanceExportService 等同级）；store-service 已分包（bom/catalog/consumable/equipment/masterdata/pricelist/procurement/project/room/sop——卡5/卡6 新建包同模式）；audit-service 单文件小服务（AuditController/Service/Repository+Outbox 中继，卡2 /page+/facets 已建）。

---

## P5-B49 卡8：目标管理 /m1-target 设计定稿（设计素材三读+API/种子先例补齐后定案）

### 设计素材确权（全部已读原文）

1. **Controller 范式**（FinConfigController.java 107 行）：类级 `@RequirePerm("finance:view")` 兜底只读、写端点方法级注解覆盖；`SecurityContext.currentStaffName()` 取操作人；薄 Controller，校验/幂等/审计/中文错误收敛 Service。
2. **权限矩阵免改**（PermissionMatrix.java 实证）：`target:view`（集团财务/运营/合规等四角色）、`target:edit`（三角色）、`target:approve`（两角色）已分布多角色，卡8 直接引用，**不动矩阵、不同步角色**。
3. **前端接线点**（M1TargetView.vue 209 行）：`onMounted(() => tg.seed())` 唯一入口；`tg.approve/reject/submit/updateProgress` 四操作；`auth.can('target:edit')`/`auth.can('target:approve')` 门控已备；树渲染靠 `children: string[]` 按字符串业务 id 三级嵌套（group→region→store），`selId` 默认 'G1'——**后端必须保留可读业务 id**。
4. **API 层先例**（api/finance.ts 741 行）：分模块注释带 + interface 定义 + `client.get/post` 导出函数，新建 `api/target.ts` 同风格，**只加不改** finance.ts。
5. **种子先例**（CommissionDataInitializer.java 242 行）：`@Order(40)` + ApplicationRunner + `@Transactional`；**栈门控**（JDBC URL 含 `meiyun_seed` 才播种，prod 跳过防 FK 冲撞）；`count()>0` 幂等跳过；固定 SEED 业务 id；中文 log。

### 后端定案（finance-service 平铺六文件）

- **表 biz_target**：`id` BIGSERIAL PK；`target_id` VARCHAR(32) UNIQUE（**保留 mock 可读 id**：G1/G1-E/G1-N/G1-S/T01-R/T02-R/G2-G5，前端树零适配）；`owner_id/owner_name/owner_type`（GROUP/REGION/STORE）；`metric` 五枚举（REVENUE/NEW_CUSTOMER/REPURCHASE_RATE/PROCEDURE_COUNT/SATISFACTION）；`period` 三枚举（YEAR/QUARTER/MONTH）+`period_label`（2026 / 2026-Q3 / 2026-08）；`target_value/current_value` NUMERIC(14,2) + `unit`（万元/人/%/人次——**管理指标非资金流水，不走「分」红线**，与 mock 手工维护语义一致）；`weight` INT；`approval` 四态；`children_ids` VARCHAR 逗号串（对齐 mock `children: string[]`，免关联表）；`submitted_by/at`、`approved_by/at`、`reject_reason`；审计四件 `created_by/updated_by/created_at/updated_at`。
- **审计复用 FinanceAuditRecorder**（实现期微调：不自建 biz_target_log 表）——REST 直发 audit-service audit_log（bizType=`BIZ_TARGET`、txnNo=target_id、action=CREATE/SUBMIT/APPROVE/REJECT/UPDATE_PROGRESS），失败同事务落 audit_outbox 中继补偿，与发票/预算写操作完全同轨。
- **端点**（TargetController，挂 `/api/finance/targets`，类级 `target:view`）：
  - `GET /finance/targets?ownerType&metric&period&approval` → List\<TargetLineDTO\>（有 `target:view` 即可读全量——target 是集团管理对象非门店流水，行级 DataScope 收窄登记 backlog）；
  - `POST /finance/targets`（`target:edit`，idemKey 幂等照发票先例，新建默认 DRAFT）；
  - `PUT /finance/targets/{targetId}/progress`（`target:edit`，**仅 APPROVED 可更新**——与前端 canEdit∧APPROVED 门控对齐）；
  - `POST /finance/targets/{targetId}/submit`（`target:edit`，DRAFT→PENDING）；
  - `POST /finance/targets/{targetId}/approve`（`target:approve`，PENDING→APPROVED）；
  - `POST /finance/targets/{targetId}/reject`（`target:approve`，PENDING→REJECTED，reason 必填）；
  - 状态机前置态不符一律 422 中文错误（天然幂等）。
- **种子 BizTargetDataInitializer**：`@Order(45)`（Commission 40 之后）、meiyun_seed 门控、count>0 跳过；照 m1Target mock 全量 10 行——G1 集团营收年度 38000/23800 万元（children 三区域）、G1-E 华东 18000/11900（children T01-R 杭州 10000/6800 + T02-R 上海静安 8000/5100）、G1-N 华北 11000/5900、G1-S 华南 9000/6000、G2 新客 Q3 4200/1980 人、G3 复购率 Q3 45/41 % **PENDING**、G4 满意度 2026-08 95/93 %、G5 华南治疗人次 Q3 3000/0 人次 **DRAFT**（覆盖审批中/草稿两样本态）。
- **backlog 登记**：REVENUE 实际值自动聚合 RevenueMonthly；REJECTED 再提交；区域岗行级 DataScope 收窄。

### 前端定案（两文件，view 零改动）

- **api/target.ts 新建**：`TargetLineDTO`（字段与后端对齐，children: string[]）+ `listTargets/createTarget/updateTargetProgress/submitTarget/approveTarget/rejectTarget` 六函数，风格照 finance.ts。
- **m1Target.ts 重写去 mock**：state `lines` 改空数组；`seed()` 改调 `listTargets()` 填充（**签名保留**，view 的 `onMounted(tg.seed)` 不动）；四操作改调 API 成功后重新 fetch；`statusOf/overallProgress` 等派生计算为纯函数原样保留。

---

## P5-B49 卡8：M1 目标管理 /m1-target 整页去 mock（feat `1f432a2`）

**交付**：finance-service 新建 target 域（`BizTarget` 表：metric 五枚举×ownerType GROUP/REGION/STORE 三级×progress 百分比×approval 四态/驳回理由/目标期间，`ddl-auto` 建表无 Flyway）；`TargetController` 七端点挂 `/api/finance/targets`：`GET /`（ownerType/metric/period/approval 四过滤）、`POST /`（target:edit，idemKey 幂等 `duplicated=true`，targetId 缺省生成 `T-XXXXXXXX`）、`PUT /{id}/progress` **body=`{value}`**（仅 APPROVED）、`POST /{id}/submit`（DRAFT→PENDING）、`POST /{id}/approve`（target:approve，PENDING→APPROVED）、`POST /{id}/reject` **body=`{reason}` 必填**（PENDING→REJECTED）；前置态不符 400、字段校验 422、不存在 404、越权 403「无操作权限：需要 target:xxx」、无 token 401（全中文）；审计复用 `FinanceAuditRecorder`（bizType=BIZ_TARGET、txnNo=target_id，action=CREATE/SUBMIT/APPROVE/REJECT/UPDATE_PROGRESS，payload 带 old/new+reason，失败落 audit_outbox 中继）；`BizTargetDataInitializer` `@Order(45)` meiyun_seed 门控种子 10 行（2 GROUP+3 REGION+5 STORE，DRAFT/PENDING/APPROVED/REJECTED 四态覆盖）形成三级分解树。前端 `api/target.ts` 新建 79 行+`m1Target` 重写 171 行（toLine 转换器/seed() loaded 门控/replaceLine 局部更新/四操作 async+useToast，reject 内 window.prompt）。8 文件 +820/-54。

**真验中修复一 view 遗留缺陷**（破设计章「view 零改动」字面，如实标注）：mock 时代 `storeTargets`/`regionTargets` 各自含独立 GROUP 行作树头，切真后 GROUP 行统一挂树头致 G2/G3/G4 重复渲染（12 行）；`M1TargetView.vue` 新增 `treeRoots` computed 过滤独立行修复（主树 v-for 改绑 treeRoots，9 行正确）。

**三轨真验全绿**：①curl 14 项——列表 10 行+四过滤（ownerType=GROUP 4 行/metric=REVENUE 6 行/period=QUARTER [G2,G3,G5]/approval=PENDING [G3]）、新建 `T-5E944395`+idemKey 重放 `duplicated=true`、{value} 契约（曾误传 currentValue 422）、submit/approve/reject/进度全流转、越态 400×2+校验 422×3+404+权限三角（E011 冯区域 target 三件套/E005 李店长无 approve 403/E012 褚财务仅 view POST 403/无 token 401）；②PG `biz_target` 12 行+`audit_log` BIZ_TARGET 7 条+主栈空表门控；③Chrome E011 手工 session——9 行树 console 零 error/warn+UI 三态操作（G1 进度 63→64%/G3 批准 66→70%/G3 驳回回 66%）+PG 复核三行落库。`mvn`+`vue-tsc` 0 error、`vite build` 双栈部署。真验数据 `TRUNCATE biz_target`+重播恢复纯净 10 行（双栈隔离）。

**数字勾稽**：✅103→**104**（104/166≈63%），⬜61→**60**，🔧1 不动；域⑧ 29✅→**30✅**、36⬜→**35⬜**；模块册新增「M1 · 目标管理」独立 1 行、M1 集团其余页 5→**4**；M1 集团屏 9/17→**10/17** 切真（3 真实+10 已切真+4 mock）。

**Backlog 新登记**：①REVENUE 实际值应自动聚合 RevenueMonthly（首卡收窄为手工维护）→后续批次（目标域增强）；②REJECTED 终态不可再提交→后续批次（目标域增强）；③target 列表行级 DataScope 未收窄（有 target:view 即可读全量，区域岗应按域收窄）→B49 批末或 B50 首卡。

---

## P5-B49 卡9：合规中心 /m1-compliance 设计定稿（设计素材三读+API/种子先例补齐后定案）

### 设计素材确权（全部已读原文）

1. **Controller/Service 范式**（AuditController 115 行 + AuditService 192 行）：类级 `@RequestMapping("/api/audit")`+方法级 `@RequirePerm`；取操作人 `DataScope.currentActor()`（AuditController outboxRetry 实证）；audit_log 为 SHA-256 链式 append-only（pg_advisory_xact_lock 串行化+payload canonicalize+微秒截断），**只增不改、不扩列**。
2. **权限矩阵免改**（PermissionMatrix 实证）：`compliance:view`（L47）/`compliance:edit`（L167）/`audit:view`（L48）均已存在并分布就绪（SUPER_ADMIN=`*`、REGION_MGR/STORE_MGR 三码全有、FINANCE 仅 view 无 edit），**不动矩阵、不同步角色**。
3. **前端接线点**（M1ComplianceView 296 行 + m1Compliance store 154 行）：`onMounted(cp.seed)` 唯一入口；`cp.recheck(id, ok, checker, remark)` 的 checker 传 auth.user.name——**切真后 checker 由后端取登录人，前端签名保留传参被忽略**；`auth.can('compliance:edit')` 门控复检按钮、`auth.isSuper` 门控超管代操作；审计 tab 时间线依赖 `log.risk/action/target/ip/detail` 五字段（由 payload 解析供给）；`CheckItem.id` mock 为 string，view 仅作 `:key` 与传参，**切真改 number 零适配**。
4. **API 层先例**（api/audit.ts 65 行，卡2）：`pageAuditLogs({bizType,page,size})` 只读三件套直接复用；新建 `api/compliance.ts` 同风格（分模块注释带+interface+client.get/post），**只加不改** audit.ts。
5. **种子先例**（BizTargetDataInitializer 119 行，卡8）：`@Order(45)`+ApplicationRunner+`@Transactional`；JDBC URL 含 `meiyun_seed` 门控、count>0 幂等跳过、中文 log——audit-service 无 DataInitializer，卡9 新建首个。
6. **Flyway 占号确权**（全库 db/migration 实证）：V1-V30 全占（customer V1/V2/V3/V5/V9、finance V4/V6/V7/V8、audit V10/V30、marketing V11-V14、ai V15-V29），**新表迁移取 V31**；application.yml 已配 out-of-order:true + `*:missing`/`*:future` 忽略，与他服务共享 flyway_schema_history 无冲撞。

### 后端定案（audit-service 平铺新增 5 文件+1 迁移）

- **表 compliance_check**（Flyway `V31__compliance_check.sql`，CREATE TABLE IF NOT EXISTS）：`id` BIGSERIAL PK；`category` VARCHAR(16) NOT NULL **CHECK 六枚举**（QUALIFICATION/CONSENT/DRUG_TRACE/PRIVACY/AD/INFECTION）；`title` VARCHAR(128) NOT NULL；`requirement` VARCHAR(256) NOT NULL；`store_name` VARCHAR(64) NOT NULL（mock 纯文本含「全集团」，不建外键）；`status` VARCHAR(8) NOT NULL **CHECK 四态**（PASS/WARN/FAIL/PENDING）；`last_check_at` TIMESTAMPTZ NOT NULL；`checker` VARCHAR(32) NOT NULL；`evidence` VARCHAR(256)；`due_date` DATE；`remark` VARCHAR(512)；`created_at/updated_at` TIMESTAMPTZ NOT NULL DEFAULT now()。ddl-auto=update 与 Flyway 双轨照 V10 先例（IF NOT EXISTS 幂等）。
- **审计留痕口径定案**（侦察待决点）：audit_log **不扩列**——ip/risk/target 落 payload jsonb（`{"target","ip","detail","risk"}` 四键，RECHECK 另带 checkId/oldStatus/newStatus）；bizType 新枚举 **`COMPLIANCE`**；动作沿用六枚举（IMPERSONATE_START/END、EXPORT_DATA、PERMISSION_CHANGE、SENSITIVE_VIEW、CONFIG_CHANGE）**+RECHECK**（复检留痕，前端 AUDIT_ACTION_LABEL 补「合规复检」）。审计 tab 数据源=卡2 `GET /api/audit/page?bizType=COMPLIANCE`（五过滤直接可用）。
- **端点**（ComplianceController 新建，挂 `/api/audit/compliance`——网关 /api/audit→8084 既有路由零改动，nginx 通用块零改动）：
  - `GET /checks?category&status`（类级 `compliance:view`）→ List\<CheckItemDTO\>（有 view 即可读全量：合规检查是集团管控对象非门店流水，行级收窄随 target 同项 backlog 一并评估）；
  - `POST /checks/{id}/recheck`（方法级 `compliance:edit`）body=`{pass:boolean 必填, remark?}`：status→PASS/FAIL、last_check_at=now、checker=`DataScope.currentActor()`、remark 非空覆盖；同事务 `AuditService.append`（bizType=COMPLIANCE、txnNo=check id、action=RECHECK、payload 带 checkId/title/storeName/oldStatus/newStatus/remark/risk=newStatus==FAIL?HIGH:LOW）——同服务同库直注入，不走 HTTP/outbox；404 不存在/422 校验/403 越权/401 无 token 全中文。
- **impersonate 收窄定案**：`startImpersonate/endImpersonate` **仅审计留痕真实化**——前端直调既有 `POST /api/audit`（append，超管 `*` 天然过 `audit:view`），payload 四键照 mock 文案；activeSession 保持前端内存单会话（与 mock 语义一致）；真实身份切换（org-service 会话置换）登记 backlog。ip 前端如实写 `"web"`（浏览器取不到真实客户端 IP，网关侧采集登记 backlog）。
- **种子 ComplianceDataInitializer**（audit-service 首个）：`@Order(45)`、meiyun_seed 门控、`compliance_check.count>0` 幂等跳过（审计种子同门控绑定）；**12 条 CheckItem 照 mock 全量**（六类×四态覆盖：PASS×7/WARN×2/FAIL×2/PENDING×1，last_check_at=now().minusHours(h) 换算，evidence/dueDate/remark 照原文）；**6 条审计经 `AuditService.append` 真实写入**（保哈希链完整：SENSITIVE_VIEW MEDIUM / IMPERSONATE_START HIGH / PERMISSION_CHANGE MEDIUM / IMPERSONATE_END MEDIUM / EXPORT_DATA LOW / CONFIG_CHANGE HIGH，payload 四键照 mock 文案含 10.12.x.x 演示 ip，createdAt=now 如实「初始化留痕」语义）。
- **backlog 登记**：①真实代操作会话（org-service 身份切换+会话置换）→后续批次；②审计真实客户端 IP 采集（网关注入 X-Real-IP→audit append 链路）→后续批次。

### 前端定案（两文件，view 零改动）

- **api/compliance.ts 新建**：`CheckItemDTO`（id:number，字段与后端对齐）+ `listChecks({category?,status?})`/`recheckCheck(id,{pass,remark?})`；审计侧复用 api/audit.ts `pageAuditLogs`；append 自实现 `appendComplianceAudit({txnNo?,action,payload})`（audit.ts 卡2 仅导出只读三件套未含 append，本文件内 `client.post('/audit', ...)`，只加不改）。
- **m1Compliance.ts 重写去 mock**（154→约 180 行）：state `items/auditLogs` 空数组+`loaded` 门控；`seed()` 签名保留——并行 `listChecks()`+`pageAuditLogs({bizType:'COMPLIANCE',size:50})`，`toCheckItem` 直映、`toAuditLog` 解析 payload 四键（缺省 ip='—'/risk='LOW'）；`stats/itemsByCategory/categoryScore` 纯函数原样保留；`recheck(id, ok, _checker, remark)` 改调 API（checker 参数签名保留、后端忽略改取登录人），成功局部更新行+刷新 auditLogs（RECHECK 留痕可见）；`startImpersonate/endImpersonate` 改调 appendComplianceAudit（activeSession 内存单会话门控/理由必填/时长计算全保留），成功刷新 auditLogs；`AUDIT_ACTION_LABEL` 补 `RECHECK:'合规复检'`；`addAudit` 内部化改名 `refreshAudits`（非 view 契约——view 仅调 recheck/start/end/seed）。

---

## P5-B49 卡9：合规中心 /m1-compliance 整页去 mock（feat `b7baee5`）

**交付**：audit-service 平铺新增 5 文件+1 迁移——`compliance_check` 表（Flyway `V31__compliance_check.sql`，CREATE TABLE IF NOT EXISTS 与 ddl-auto=update 双轨照 V10 先例：category CHECK 六枚举 QUALIFICATION/CONSENT/DRUG_TRACE/PRIVACY/AD/INFECTION、status CHECK 四态 PASS/WARN/FAIL/PENDING、store_name 纯文本不外键、checker/evidence/due_date/remark、created_at/updated_at）；`ComplianceController` 挂 `/api/audit/compliance`（网关既有 /api/audit→8084 路由零改动、nginx 通用块零改动）：`GET /checks?category&status`（类级 `compliance:view`，有 view 即可读全量——合规检查是集团管控对象非门店流水，行级收窄随 target 同项 backlog 一并评估）、`POST /checks/{id}/recheck`（方法级 `compliance:edit`，body=`{pass:boolean 必填, remark?}`：status→PASS/FAIL、last_check_at=now、remark 非空覆盖；同事务 `AuditService.append`——bizType=COMPLIANCE、txnNo=check id、action=RECHECK 新动作码、payload 八键 checkId/title/storeName/oldStatus/newStatus/remark+risk（newStatus==FAIL→HIGH、PASS→LOW）+ip 如实 `"web"`，同服务同库直注入不走 HTTP/outbox；404 不存在/422 校验/403 越权/401 无 token 全中文）；audit_log **不扩列**（target/ip/detail/risk 四键落 payload jsonb）；`ComplianceDataInitializer`（audit-service 首个）`@Order(45)` meiyun_seed 门控、count>0 幂等跳过，种子 12 条 CheckItem 照 mock 全量（PASS×7/WARN×2/FAIL×2/PENDING×1，last_check_at 按 mock 小时数回推）+6 条审计经 `AuditService.append` 真实写入保哈希链完整。前端 `api/compliance.ts` 新建（CheckItemDTO id:number+listChecks/recheckCheck+appendComplianceAudit，审计只读复用卡2 api/audit.ts `pageAuditLogs` 只加不改）+`m1Compliance.ts` 重写切真（seed 签名保留并行双拉 listChecks+pageAuditLogs bizType=COMPLIANCE size=50、recheck 局部替换+void refreshAudits、start/endImpersonate **仅审计留痕真实化**收窄——activeSession 保持前端内存单会话与 mock 语义一致、AUDIT_ACTION_LABEL 补 RECHECK:'合规复检'），view 零改动。8 文件 +590/-67。

**设计微调如实标注**：取操作人由设计章 `DataScope.currentActor()` 改 `SecurityContext.currentStaffName()`——返回中文姓名贴 mock checker 显示口径，curl 实证 checker=「冯区域」（E011）/「李店长」（E005）；审计 append 链路 actor 口径不受影响。

**部署中修复种子库 Flyway 历史遗留**：种子栈 `up frontend` 按 depends_on 连带拉起 meiyun-seed-ai-service 报 Flyway V26 checksum mismatch（Applied: null / Resolved: 582052037）——种子库 flyway_schema_history 历史遗留 11 行 checksum NULL（V0 基线+V17-V26 ai-service 全部迁移），主库对应行正常（主栈 ai-service healthy 实证）；从主库 SELECT 生成 UPDATE 语句管道回填种子库 V17-V26 共 10 行（null_left=0）→ ai-service healthy → frontend Started，种子栈 bundle hash=index-8OUan0Df 与本次构建产物一致实证新前端生效。V0 基线行保留 NULL 不碍 validate。

**前端构建修复一缺陷**：m1Compliance.ts `recheck` 为兼容 view 契约保留的 checker 占位参数（切真后后端改取登录人）落盘时误用原名触发 TS6133 noUnusedParameters，改 `_checker` 下划线前缀豁免（注释保留语义），vue-tsc+vite build 全绿。

**三轨真验全绿**：①curl 权限四角全贴合矩阵——E011 REGION_MGR 全通（list 12 条+recheck id=10 PENDING→FAIL checker=冯区域+audit page total=7 RECHECK id=148 最前 payload 八键 risk=HIGH ip=web）、E005 STORE_MGR 全通（recheck id=3 WARN→PASS checker=李店长）、E012 FINANCE list 200 但 recheck 403（仅 view 无 edit）、E002 FRONT_DESK 双 403；category/status 过滤正确；②PG 对账 check id=10 落库 FAIL、链式哈希 id=148.prevHash==id=147.curHash 连续（7e5ccd3ae25159b2）；③Chrome 复用 E011 会话——12 行渲染+统计卡勾稽（67%/8/1/3、六类得分卡全对）+console 零 error 零 warn、UI 复检 id=12 全链路（弹窗默认复检通过→填说明→提交→行局部替换+统计卡 67%→75% 联动 PASS 8→9）+审计 tab 时间线「合规复检/低风险/FAIL → PASS/操作人：冯区域/IP web」。种子库终态 compliance_check 12 行（PASS×9/WARN×1/FAIL×2，id=3/10/12 三条真验翻转 append-only 留痕）、audit_log COMPLIANCE 9 条（6 种子+3 RECHECK）。

**数字勾稽**：✅104→**105**（105/166≈63%），⬜60→**59**，🔧1 不动；域⑧ 30✅→**31✅**、35⬜→**34⬜**；模块册新增「M1 · 合规中心」独立 1 行、M1 集团其余页 4→**3**；M1 集团屏 10/17→**11/17** 切真（3 真实+11 已切真+3 mock）。

**Backlog 新登记**：①impersonate 真实身份切换（org-service 会话置换，本卡仅审计留痕真实化收窄）→平台批；②审计真实客户端 IP 采集（网关注入 X-Real-IP→audit append 链路，现如实写 `"web"`）→平台批。

---

## P5-B49 卡10：健康度巡检 /m1-health 设计定稿

**mock 域摸清**（m1Health.ts 189 行+M1HealthView.vue 205 行）：`TenantHealth`（tenantId/tenantName/region+`scores[6]`（dimension 六枚举 SAFETY/SERVICE/FINANCE/COMPLIANCE/STAFF/EQUIPMENT×score 0-100×weight——SAFETY/COMPLIANCE 权重 2 余 1）+lastCheckedAt/nextCheckAt/inspector）×5 店 T01-T05；`HealthIssue`（id/tenantId/dimension/severity HIGH/MEDIUM/LOW/title/detail/status OPEN/PROCESSING/RESOLVED/IGNORED/assignee/dueAt/createdAt/resolvedAt/resolution）×7 条四态覆盖（OPEN×3/PROCESSING×3/RESOLVED×1）；KPI 五卡（集团均分=加权总分全店均值、健康 ≥85/预警 70-84/严重 <70 三档计数、待整改=OPEN+PROCESSING）；view 交互五操作——`seed()` 双拉、门店选中（前端 selId 客户端 filter tenantIssues）、`startIssue`（OPEN→PROCESSING）、`resolveIssue`（抽屉填 resolution→RESOLVED）、`ignoreIssue`（OPEN/PROCESSING→IGNORED）、`rerun`（按未决高/中危扣分重算六维+日期滚动）；`canEdit=auth.can('health:edit')||isSuper`。

**后端**：store-service 新建 `health` 包（同卡5 procurement/卡6 sop 分包模式，**ddl-auto=update 建表无 Flyway** 照卡5/卡6 先例）平铺 10 文件——三实体三表：`health_check`（门店巡检头：id BIGSERIAL PK、store_code VARCHAR(32) UNIQUE、last_checked_at/next_check_at DATE、inspector VARCHAR(64)、created_at/updated_at）、`health_score`（六维分数行：id PK、store_code、dimension VARCHAR(16) @Enumerated(STRING) 六枚举、score INT、weight INT、UNIQUE(store_code,dimension)）、`health_issue`（整改任务：id PK、store_code、dimension、severity VARCHAR(8) 三档、title VARCHAR(128)、detail VARCHAR(512)、status VARCHAR(16) 四态、assignee VARCHAR(64)、due_at DATE、created_at/resolved_at/resolution VARCHAR(512)）；`HealthService`（issue 状态机+rerun 服务端真实重算）；`HealthController` 挂 `/api/stores/health`（网关 `/api/stores`→8081 前缀通配**零改动**、nginx 通用块零改动）五端点：`GET /checks`（`health:view`，join Store 表组装 TenantHealthDTO——tenantId=store_code、tenantName/region 取真实主数据、scores 按 store_code 分组，按 store_code 排序）、`GET /issues?storeCode&status`（`health:view`，全量返回前端客户端 filter 保持 view 零改动、可选过滤附带）、`POST /issues/{id}/start`（`health:edit`，OPEN→PROCESSING）、`POST /issues/{id}/resolve` body=`{resolution 必填}`（`health:edit`，PROCESSING→RESOLVED+resolved_at=now+resolution——收窄：mock 无状态门控，真实化贴合 UI 流程仅 PROCESSING 可解决，422 其他状态）、`POST /issues/{id}/ignore`（`health:edit`，OPEN/PROCESSING→IGNORED）、`POST /checks/{storeCode}/rerun`（`health:edit`，**mock 前端假重算算法服务端化**：openHigh/openMid=该店 severity=HIGH/MEDIUM 且 status IN (OPEN,PROCESSING) 计数，每维 score=max(40,min(98,s-openHigh×8-openMid×3+(无未决则+4)))，last_checked_at=today、next_check_at=+7d、inspector=SecurityContext.currentStaffName() 中文姓名照卡9 实证口径）；错误形态 404/422/403/401 全中文。

**审计**：`HealthAuditRecorder` 同构 ConsumableAuditRecorder（REST POST /api/audit 五键 bizType=HEALTH/txnNo=issue id 或 storeCode（RERUN）/actor=currentStaffName()/action=START/RESOLVE/IGNORE/RERUN/payload json 关键字段——RESOLVE 带 resolution、RERUN 带 openHigh/openMid/新均分，失败同事务落 audit_outbox 由 audit-service 中继补偿不阻断主流程）。

**种子**：`HealthDataInitializer` @Order(80) meiyun_seed 门控、health_check count>0 幂等跳过——门店映射**照卡6 定案** T01-T05→SST01-SST05（SST06 杭州西湖店无种子档案不出现列表，如实标注）；5 店巡检头+30 行六维分数照 mock 分值/权重全量，日期重定 2026-09 区间（lastCheckedAt 09-06~09-10、nextCheckAt 09-16~09-20 保持未来，inspector 照 mock 王/李/张/陈/赵质控）；7 条 issue 照 mock 映射（I01/I02/I05→SST03、I03/I04→SST02、I06→SST05、I07→SST04），createdAt 重定 09-01~09-10、dueAt 09-16~09-30、I06 RESOLVED 终态 resolvedAt=09-12+resolution 照 mock 原文；**种子全部 Repository 直建终态不落审计**（与卡6 差异如实标注：卡6 走 Service 同路径因 SOP 步骤勾选派生复杂，health 四态简单直建；审计从真验操作起始）。

**前端**：`api/health.ts` 新建（TenantHealthDTO/HealthIssueDTO+getChecks/getIssues/startIssue/resolveIssue/ignoreIssue/rerunCheck）；`m1Health.ts` 重写切真——tenants/issues 空数组+`loaded` 门控，`seed()` 签名保留并行双拉（toTenant 直映后端已组装 tenantName/region），overall/scoreOf/statusOf/五 KPI computed 纯函数原样保留，四操作改 async 调 API+局部替换（rerun 返回更新后 TenantHealth 整行替换；`rerun(tenantId, _inspector)` inspector 参数签名保留下划线豁免、后端取登录人，照卡9 checker 先例）；**view 零改动**（view 仅调 seed/startIssue/resolveIssue/ignoreIssue/rerun/scoreOf/statusOf，签名全保留）。

**权限**：**零改动**——PermissionMatrix 已预置 health:view/edit（REGION_MGR L354/L474、STORE_MGR L632/L756 全通、FINANCE L968 仅 view 无 edit、FRONT_DESK 无）；权限四角预期同卡9：E011/E005 全通、E012 list 200 写 403、E002 双 403。

**数字勾稽预判**：✅105→**106**（106/166≈64%），⬜59→**58**，🔧1 不动；域⑧ 31✅→**32✅**、34⬜→**33⬜**；模块册新增「M1 · 健康度巡检」独立 1 行、M1 集团其余页 3→**2**；M1 集团屏 11/17→**12/17** 切真（3 真实+12 已切真+2 mock）。

---

## P5-B49 卡10：健康度巡检 /m1-health 整页去 mock（feat `177a32e`）

**交付**：store-service 新建 `health` 包 9 文件（照卡5 procurement/卡6 sop 分包模式，ddl-auto=update 建表无 Flyway）——三实体三表：`health_check`（**store_code VARCHAR(32) 作 PK**，last_checked_at/next_check_at DATE、inspector、created_at/updated_at）、`health_score`（id PK、store_code、dimension 六枚举、score、weight、UNIQUE(store_code,dimension)）、`health_issue`（id PK、store_code、dimension、severity 三档、title、detail、status 四态 OPEN/PROCESSING/RESOLVED/IGNORED、assignee、due_at、created_at/resolved_at/resolution）；三 Repository；`HealthService` 224 行（issue 状态机+rerun 服务端真实重算）；`HealthController` 挂 `/api/stores/health`（网关 /api/stores→8081 前缀通配零改动、nginx 零改动）六端点：`GET /checks`（`health:view`，join Store 组装 TenantHealthDTO——tenantId=store_code、tenantName/region 取真实主数据）、`GET /issues?storeCode&status`（`health:view`）、`POST /issues/{id}/start`（`health:edit`，OPEN→PROCESSING）、`POST /issues/{id}/resolve` body=`{resolution 必填}`（`health:edit`，PROCESSING→RESOLVED+resolved_at=now+resolution，422 其他状态）、`POST /issues/{id}/ignore`（`health:edit`，OPEN/PROCESSING→IGNORED）、`POST /checks/{storeCode}/rerun`（`health:edit`，**mock 前端假重算算法服务端化**：openHigh/openMid=该店 HIGH/MEDIUM 且 OPEN/PROCESSING 计数，每维 score=max(40,min(98,s-openHigh×8-openMid×3+(无未决+4)))，last_checked_at=today、next_check_at=+7d Asia/Shanghai、inspector=SecurityContext.currentStaffName() 中文姓名照卡9 实证口径）；404/422/403/401 全中文。审计**复用既有 `ConsumableAuditRecorder`**（bizType=HEALTH_CHECK/HEALTH_ISSUE、action=RERUN/START/RESOLVE/IGNORE、txnNo=issue id 或 storeCode，失败同事务落 audit_outbox 由 audit-service 中继补偿不阻断主流程）。`HealthDataInitializer` `@Order(80)` meiyun_seed 门控、health_check count>0 幂等跳过——5 店巡检头（T01-T05→SST01-SST05 照卡6 定案映射）+30 行六维分照 mock 分值/权重全量+7 条 issue 四态覆盖，Repository 直建终态不落审计（审计从真验操作起始）。前端 `api/health.ts` 新建 58 行（TenantHealthDTO/HealthIssueDTO+六端点函数）+`m1Health.ts` 重写切真（tenants/issues 空数组+loaded 门控、seed 签名保留并行双拉、overall/scoreOf/statusOf/五 KPI computed 纯函数原样保留、四操作 async 调 API+局部替换/refreshIssues、rerun 返回整行 splice 替换+KPI 联动、inspector 参数签名保留下划线豁免照卡9 checker 先例）+M1HealthView selId 一行微调。12 文件 +808/-90。

**设计微调如实标注**（五处）：①设计章（docs commit `cbd118f`）称 health 包「平铺 10 文件」含新建 `HealthAuditRecorder` 同构 ConsumableAuditRecorder——实现**复用既有 `com.meiyun.store.consumable.ConsumableAuditRecorder`** 零新建，实 **9 文件**（bizType 取 HEALTH_CHECK/HEALTH_ISSUE 两新值）；②`health_check` 以 **store_code 作 PK**（设计为 id BIGSERIAL PK+store_code UNIQUE）——一店一巡检头语义更直接，rerun 按 storeCode 定位免二次查询；③issue 前端 DTO id 格式 **"I%02d"**（设计未写明）——后端 parseRef strip 'I' 前缀转 long，NumberFormatException→400「任务编号非法」中文；④M1HealthView **selId 'T01'→'SST01'** 一行微调（设计章称 view 零改动）——默认选中门店须对齐真实 store_code 否则首屏空态；⑤设计章 L357 写「五端点」实为**六端点**（GET /checks、GET /issues、start、resolve、ignore、rerun）——笔误更正。

**部署**：双栈后端 docker cp fat jar+restart、前端 docker cp dist 免重启；冒烟种子栈 health_check=5/health_score=30/health_issue=7 落库、主栈 meiyun_core 门控 0 行、GET /checks 与 GET /issues 双端点无 token 401。

**三轨真验全绿**：①curl 权限四角全贴合矩阵——E011 REGION_MGR/E005 STORE_MGR 全通（checks 5 店+issues 7 条+四操作）、E012 FINANCE list 200 仅 view 写 403、E002 FRONT_DESK 双 403 中文；rerun 加分方向实证（curl SST01，openHigh=0/openMid=0 → bonus+4 clamp98：六维 [92,88,90,95,86,84]→[96,92,94,98,90,88]，overall=94=752/8，last_checked_at=2026-09-15/next=2026-09-22、inspector=冯区域）；②PG 对账三表落库+audit_log HEALTH_CHECK/HEALTH_ISSUE 对账（payload jsonb 需 payload::text cast 查询）+主栈门控 0 行；③Chrome E011 会话——5 店列表+SST03 切换+issue 三态按钮分支+I05 开始处理局部替换+UI 重新巡检扣分方向实证（SST03 I02+I05 两 HIGH 未决 delta=2×8=16、bonus=0：六维 [62,75,58,70,68,55]→[46,59,42,54,52,40]，55-16=39→clamp40，overall=393/8=49.125→49；左列该店 65→49、集团均分 81→78（(94+78+49+87+83)/5=78.2→78））console 零 error/warn；mvn package+vue-tsc 0 error+vite build 双栈全绿。

**数字勾稽**：✅105→**106**（106/166≈64%），⬜59→**58**，🔧1 不动；域⑧ 31✅→**32✅**、34⬜→**33⬜**；模块册新增「M1 · 健康度巡检」独立 1 行、M1 集团其余页 3→**2**；M1 集团屏 11/17→**12/17** 切真（3 真实+12 已切真+2 mock）——与设计章预判逐项一致。

**Backlog 新登记**：无。

---

## P5-B49 卡11：报表中心 /m1-report 设计定稿

**mock 域摸清**（m1Report.ts 165 行+M1ReportView.vue 214 行）：`ReportTemplate`×9（R01-R09：id/name/category 六枚举 REVENUE 营收/CUSTOMER 客户/OPERATION 运营/FINANCE 财务/COMPLIANCE 合规/STAFF 人效/desc/period 六枚举 DAY/WEEK/MONTH/QUARTER/YEAR/RANGE/dimensions[]/metrics[]/lastRunAt?/subscribed——订阅初值 R01/R02/R04/R06/R08=true 共 5）——R01 门店营收日报（DAY，门店×支付方式→营收/客单价/笔数）、R02 月度经营分析（MONTH，区域×门店×项目品类→营收/成本/毛利率/环比）、R03 新客转化漏斗、R04 RFM 分层、R05 疗程消耗、R06 应收账龄、R07 退款纠纷、R08 合规月报、R09 员工业绩；`ReportJob`×5（id/templateId/templateName/category/period 期段串/status READY/GENERATING/FAILED/format XLSX/PDF/CSV/createdAt/createdBy/rowCount?/fileSize?/error?）——J01(R02/XLSX/READY)、J02(R06/PDF/READY)、J03(R01/XLSX/READY)、J04(R08/PDF/FAILED error「部分门店数据未同步，请重试」)、J05(R09/XLSX/READY)；`SAMPLE_DATA` 仅 R01/R02 假预览（headers+rows）；store 方法 seed()（seeded 门控）/catFilter/filtered/toggleSubscribe/**generate(templateId,period,format)=setTimeout 1500ms 假生成（Math.random 假 rowCount/fileSize，必剔）**/**retry(jobId)=setTimeout 1200ms 假重试（必剔）**/preview 读 SAMPLE_DATA/subscribedCount；view KPI 四卡（模板数/订阅数/READY 数/FAILED 数）、selId='R02'、`canExport=auth.can('report:export')||isSuper`、periodOptions 硬编码五档（必剔改动态）、**download=mock blob 假下载（text/plain 内容=templateName+period，必剔改真实端点）**。

**后端**：finance-service 平铺新建 report 域（照卡8 BizTarget 五件套先例，**ddl-auto=update 建表无 Flyway**）——两实体两表：`report_template`（id VARCHAR(8) PK 'R01'-'R09'、name VARCHAR(64)、category VARCHAR(16) @Enumerated(STRING) 六枚举、description VARCHAR(256)、period VARCHAR(16) 六枚举、dimensions/metrics VARCHAR(512) 逗号串存储、subscribed BOOLEAN、last_run_at TIMESTAMP）；`report_job`（id VARCHAR(8) PK 'J%02d' 序列、template_id、template_name、category、period VARCHAR(32) 期段串、status VARCHAR(16) 三态、format VARCHAR(8)、created_at、created_by、row_count INT、file_size INT、error VARCHAR(256)、**content BYTEA——CSV 真实内容落库**：容器文件系统不持久，DB 持久卷保下载可重放不伪造）；`ReportService`（generate 落 job GENERATING+触发异步、retry FAILED→GENERATING 重触发、subscribe 切换、preview/generateCsv 数据源分派）；`ReportAsyncRunner` 独立组件 @Async 执行真实生成（FinanceApplication 加 @EnableAsync——**避免 Service 自调用 @Async 失效**，成功→READY+content+rowCount+fileSize+template.last_run_at 回写、失败→FAILED+error 中文）；`ReportController` 挂 `/api/finance/report`（网关 `/api/finance`→8087 前缀通配 router.go L32 **零改动**、nginx 通用块零改动）类级 `@RequirePerm("report:view")` 七端点：`GET /templates?category`、`GET /jobs?status&category`、`POST /templates/{id}/subscribe`（均类级 report:view）、`POST /generate` body=`{templateId,period,format}`（`report:export`，format≠CSV→400「首卡仅支持 CSV 真实生成，XLSX/PDF 已登记 backlog」中文收窄、模板数据源未建→422「该模板数据源待建」中文收窄、期段必填校验）、`POST /jobs/{id}/retry`（`report:export`，仅 FAILED 可重试 422 其他状态）、`GET /jobs/{id}/download`（`report:export`，content bytea 真实返回 Content-Disposition 附件——content IS NULL 历史种子→404「历史文件未留存，请重新生成」中文）、`GET /templates/{id}/preview?period=`（report:view，R01/R02 真实聚合返回 headers+rows，其余 7 模板 422 中文收窄前端 catch→null 走既有空态）；404/400/422/403/401 全中文。**数据源定案**：R01 门店营收日报→`FinanceAggregationService.ledger(storeCode,from,to)` 真实流水按门店×支付方式聚合（order_payment 源，dimensions/metrics 对齐 mock）；R02 月度经营分析→`RevenueMonthlyRepository` 本库直读（storeCode+periodMonth/revenue/cost/grossProfit/grossRate，join 门店主数据补区域，环比=对上期 revenue 计算；**项目品类型维度数据源未建如实收窄不含**）；R03-R09 七模板 generate/preview 收窄 422 登记 backlog。CSV 生成复用 `FinanceExportService` 工具模式（UTF-8 BOM+中文表头+CRLF+RFC 4180 转义+report() 拼「名称-yyyyMMdd-HHmmss.csv」）。

**审计**：复用 `FinanceAuditRecorder`（照卡8 BizTarget 先例零新建）——bizType=REPORT、action=GENERATE/RETRY/DOWNLOAD/SUBSCRIBE、txnNo=job id 或 template id、payload json 关键字段（GENERATE 带 templateId/period/format、DOWNLOAD 带 fileSize、SUBSCRIBE 带 subscribed 终值），失败同事务落 audit_outbox 由 audit-service 中继补偿不阻断主流程。

**种子**：`ReportDataInitializer` @Order(85) meiyun_seed 门控、report_template count>0 幂等跳过——9 模板照 mock 全量（id/name/category/desc/period/dimensions/metrics 原值、subscribed 照 mock 五 true、last_run_at 照 mock 串转 timestamp）；5 job 照 mock 全量映射（J01-J05 id/template_id/template_name/category/period/status/format/created_at 重定 2026-08/09 区间、created_by 照 mock、J04 error 照 mock 原文），**content 全部 NULL 不伪造**（历史任务记录留存、文件重新生成后真实落库，download 走 404 提示路径如实）；种子全部 Repository 直建终态不落审计（照卡10 先例，审计从真验操作起始）。

**前端**：`api/report.ts` 新建（ReportTemplateDTO/ReportJobDTO/PreviewData+七端点函数）；`m1Report.ts` 重写切真——templates/jobs 空数组+`loaded` 门控，`seed()` 签名保留并行双拉（templates+jobs），catFilter/filtered/subscribedCount/KPI computed 纯函数原样保留，`toggleSubscribe`→POST 后局部替换，**剔 generate/retry 双 setTimeout**→generate async POST 后轮询 `GET /jobs`（1s×10 次至目标 job 终态，真实异步语义保留 UI GENERATING 态），retry 同路径，**剔 SAMPLE_DATA 假预览**→preview(templateId,period) 调真实端点 catch→null；M1ReportView 微调——**download 剔 mock blob 改 fetch 真实端点 blob 另存**（带 Authorization 头，filename 取 Content-Disposition）、periodOptions 动态生成（近 7 日+近 6 月+近 2 季对齐模板 period 枚举）、**format 选择收窄仅 CSV 可选**（XLSX/PDF 禁用+tooltip 中文说明）、selId='R02' 保留、canExport 已就位零改动。

**权限**：**零改动**——PermissionMatrix 已预置（SUPER_ADMIN='*' 通配 L319-321；report:view：REGION_MGR L344、STORE_MGR L622、CONSULTANT L861、OPERATOR L944、FINANCE L960；report:export：REGION_MGR L491、STORE_MGR L769、FINANCE L985）；权限四角预期：E011 REGION_MGR/E005 STORE_MGR/**E012 FINANCE 三角全通**（FINANCE 双权限齐备与卡9/卡10 写 403 不同，如实标注）、E002 FRONT_DESK 双 403 中文。

**数字勾稽预判**：✅106→**107**（107/166≈64.5%），⬜58→**57**，🔧1 不动；域⑧ 32✅→**33✅**、33⬜→**32⬜**；模块册新增「M1 · 报表中心」独立 1 行、M1 集团其余页 2→**1**；M1 集团屏 12/17→**13/17** 切真（3 真实+13 已切真+1 mock）。**Backlog 预判登记**：①R03-R09 七模板数据源（新客漏斗/RFM/疗程/应收账龄/退款/合规月报/人效逐批建）；②XLSX/PDF 生成引擎；③R02 项目品类型维度。

---

## P5-B49 卡11 M1 报表中心（/m1-report）交付（2026-09-15）

### 交付清单（feat `01c7af1`，13 文件 +1241/-162，已推送）

- 后端 10 文件（finance-service 平铺包 report 域）：ReportTemplate/ReportTemplateRepository/ReportJob/ReportJobRepository/ReportService/ReportController/ReportAsyncRunner/ReportCsvBuilder/ReportDataInitializer 9 新增 + FinanceApplication M。
- 前端 3 文件：api/report.ts 新增、stores/m1Report.ts M（重写切真）、views/M1ReportView.vue M。
- report_template 9 模板种子 + report_job 5 任务种子（content BYTEA 真实 CSV 落库可重放；种子行 content NULL 不伪造历史文件）。
- ReportController 挂 /api/finance/report 类级 report:view 七端点：templates/jobs/subscribe/generate/retry/download/preview；ReportAsyncRunner @Async 真实生成。
- R01→FinanceAggregationService.ledger 真实流水门店×支付方式聚合；R02→RevenueMonthlyRepository 直读 join 补区域+环比（项目品类维度收窄）；R03-R09 七模板 422 收窄登记 backlog。
- 审计复用 FinanceAuditRecorder bizType=REPORT；异步上下文恢复 asyncRunner 显式传 SecurityContext。

### 设计微调如实标注

E012 FINANCE 角色 report:view+report:export 双权限齐备全通——与卡9/卡10「写 403」不同，如实标注：报表域读+导双权限均授予 FINANCE，权限零改动。

### 部署

mvn 全绿 45M；双栈部署+冒烟：种子 9 模板/5 任务 content NULL、主栈门控 0、双栈无端 401。

### 三轨真验全绿

1. curl 权限四角：E011 200 / E002 403 / 无端 401 / 超管 SE101·E014 通；另 E012 FINANCE 双权限齐备全通（见上设计微调）。
2. PG 数值对账：微信支付净额 2980=3980−1000、支付宝 6800、现金 12800。
3. retry 成功路径：J90 FAILED→GENERATING→READY（异步上下文恢复 asyncRunner 显式传 SecurityContext）。
4. audit 哈希链官方验链 ok:true total:105。
5. Chrome UI 轨：9 模板卡 / R01+R02 真实 preview / J03 content NULL 种子 download 404 toast「历史文件未留存，请重新生成」（MutationObserver 同帧注入实证）/ 真实 CSV 195B text/csv / R03 常驻 gen--note 提示块（无生成按钮无 preview）/ E002 路由守卫 403 重定向 /no-auth?need=report:view+集团治理菜单整组隐藏。

### 如实标注清单（累计 16 项）

1. UTC 日界口径（R01 汇总按 UTC 日界，与业务自然日可能差 ±1）。
2. transfer→「其他」保守口径（mapPayMethod 未直通，影响 R01 支付方式分布）。
3. 毛利率小数输出 vs 表头（%）（gross_rate 未 ×100）。
4. 种子库补插演示数据（SCR20260914099/RT20260914001/revenue_monthly 7 行+report_job J90 手工演示+J91 UI 生成）。
5. dev-login 已关，验证用 meiyun123。
6. E001=DOCTOR 非超管。
7. 超管=SE101/E014。
8. R02 区域收窄正确非 bug。
9. download 无属主校验（同租户任意 report:view 持有者可下载）。
10. 异步上下文恢复（asyncRunner 显式传 SecurityContext）。
11. 前端 CSV-only（format 选项剔除 XLSX/PDF）。
12. 种子行 content NULL，download 404 文案「历史文件未留存，请重新生成」。
13. download 404 先于 audit，不写链。
14. toast 2800ms 生命周期 < MCP 往返，观测须 MutationObserver 同帧注入。
15. R03-R09 为常驻 gen--note 提示块非 toast。
16. commit `01c7af1` 消息 typo「bob 错误体」应为「blob 错误体」，已推送不改写历史，登记在案。

### 数字勾稽（与设计章预判逐项一致）

✅106→**107**/166=约64%（「约64%」不变照卡10 先例）；⬜58→**57**；🔧1 不动；域⑧ 32✅→**33✅** 2🔧 33⬜→**32⬜**；大表新增「M1 · 报表中心」独立 1 行；M1 其余页 2→**1**；M1 集团屏 12/17→**13/17** 切真（3 真实+13 已切真+1 mock）。

### Backlog 新登记 5 项

R03-R09 七模板数据源待建／XLSX·PDF 导出格式／R02 区域+项目品类维度扩展／mapPayMethod transfer 渠道直通／R02 毛利率 % 格式化——详见 04-backlog。
