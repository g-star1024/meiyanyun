# M7 权限与安全 · 阶段交付总结报告（m7-6 / m7-7）

> 时间：2026-09-02
> 里程碑：M7 权限与安全（P0）
> 鉴权落点（已拍板）：统一拦截器 + 注解（meiyun-security 公共模块），不引入 Spring Security 过滤器链
> 角色来源（已拍板）：本里程碑一并落地登录态 / JWT
> 本报告范围：**m7-6 前端登录态**（收尾验收）+ **m7-7 前端权限真源对齐**（本轮全部完成）

---

## 一、本阶段交付内容

### 1.1 m7-6 前端登录态（已闭合）

| 项 | 内容 | 验收 |
| -- | ---- | ---- |
| 登录页 | `LoginView.vue`：工号+密码登录（PBKDF2，默认密码 meiyun123）、8 角色快捷登录（dev-login）、redirect 回跳、中文错误提示 | E2E 通过 |
| 路由守卫 | 三段式：未认证 → `/login?redirect=`；已认证权限不足 → `/no-auth`；已登录访问 /login → /my-workbench；/m 小程序放行 | E2E 通过 |
| 会话持久化 | 双键 localStorage：`meiyun_token`（裸 JWT）+ `meiyun_session`（SessionInfo）；main.ts 挂载前同步 restoreSession | E2E 通过 |
| axios 拦截器 | 请求注入 Bearer（公共路径除外）；响应 401 → clearAuth() 清双键 + 跳登录页 | E2E 通过（失效 token → 双键 null + 重定向） |
| 顶栏接线 | CShellDesktop 角色切换器 → `auth.loginAs(role)`（真实 dev-login 换发 JWT，离线兜底）；退出登录 → logout + /login | E2E 通过（王前台→财务 E012 褚财务总监，64 权限 finance:* 全系列，侧栏导航同步收敛） |

### 1.2 m7-7 前端权限真源对齐（本轮，全部完成）

| # | 改造点 | 文件 | 内容 |
| - | ------ | ---- | ---- |
| 1 | 权限矩阵服务端化 | `stores/auth.ts` + `main.ts` | 新增 `matrix` state 与 `loadMatrix()`：启动异步拉 `GET /api/org/auth/permissions`（319 权限码 / 8 角色 rolePermissions）；未登录 ?as= 离线模式**优先用服务端 rolePermissions**，拉取失败静默回退硬编码 ROLE_PERMISSIONS；main.ts 挂载前预热、不阻塞渲染 |
| 2 | 花名册对齐真源 | `stores/auth.ts` DEV_ROSTER | 假 staff-* 假人全部替换为后端 dev-login 实际返回的 E 系列：SUPER_ADMIN=E014 蒋IT、REGION_MGR=E011 冯区域、STORE_MGR=E005 李店长(ST-SH-001)、CONSULTANT=E004 赵咨询师、DOCTOR=E001 刘治疗师、FRONT_DESK=E002 王前台、OPERATOR=E013 卫运营、FINANCE=E012 褚财务总监 |
| 3 | 登录页脱外壳 | `App.vue` | 新增 `isAuthPage`（route.meta.public）裸布局分支：/login 不再套 CShellDesktop 侧栏/顶栏 |
| 4 | 旧角色码 bug 修复 | `AppointmentCreateView.vue` | 医生下拉过滤 `roleCode === 'ROLE_DOCTOR'`（永远为空）→ `'DOCTOR' && medicalLicensed === true`（排除治疗师等无资质 DOCTOR）；门店/员工加载改 `Promise.allSettled` 独立容错，门店服务 500 不再拖垮医生下拉 |
| 5 | 工号示例修正 | `LoginView.vue` | placeholder「如 SE101」→「如 E002」 |
| 6 | 姓名映射对齐 | `config/staff.ts` + ApprovalView + MyWorkbenchView | 新增 ROSTER 全量 19 人映射（E001-E014 + SE101-SE105）；DOCTORS 仅列执业资质医生；两处 STAFF_NAME_EXTRA 保留 mock 旧 ID + cashier/system，E/SE 工号统一由 staffName() 解析 |

### 1.3 运行时真源备注（与源码不一致的处置）

- 仓库源码种子为 SE001-SE024（`db/seed/01_master.sql`）+ SE101-SE105（RbacDataInitializer），但 18096 联调实例运行库实际为 **E001-E014 + SE101-SE105 共 19 人**（dev-login 按工号升序每角色取第一人）。
- 处置原则：**以运行时 API 为真源**，前端 DEV_ROSTER/ROSTER 全部对齐 E 系列；E 系列种子在 org-service 源码中未 grep 到来源（运行库历史数据），记录在案，待 m7-9 验收时与后端种子统一核对。

---

## 二、E2E 验证证据（Chrome DevTools，vite 5173 → org 18096）

| 用例 | 结果 |
| ---- | ---- |
| 登录页裸布局 | ✅ /login 无侧栏/顶栏，整页渐变卡片；placeholder 显示「请输入工号（如 E002）」 |
| 权限矩阵启动拉取 | ✅ performance 资源记录 2 次 `/api/org/auth/permissions` 200（/login 与跳转后各一） |
| 离线模式服务端真源 | ✅ `?as=FINANCE` 客户端权限 **64 项**，与服务端 rolePermissions.FINANCE 数量/内容逐项一致（含 finance:reconcile、customer:view；不含 emr:edit）；顶栏显示「褚财务总监」 |
| 快捷登录身份 | ✅ 点「门店店长」→ dev-login 返回 E005 李店长 / STORE_MGR / ST-SH-001 / 233 权限，跳 /my-workbench |
| 医生下拉 | ✅ /appointment/new 操作医生下拉 = 不指定 / **陈医生(E003) / 孙医生(E008) / 郑医生(E010)**，三位执业资质医生；刘治疗师(E001, medicalLicensed=false) 正确排除 |
| 门店服务容错 | ✅ /api/stores 在 org 直连实例 500（归其他服务）时，医生下拉仍正常填充，无 toast 误报 |
| 构建 | ✅ `vue-tsc --noEmit` 0 error；`vite build` exit 0（18.1s） |
| 旧码清扫 | ✅ 全前端 grep `ROLE_(DOCTOR|CONSULTANT|…)` 零匹配 |

---

## 三、M7 整体进度

| 工作项 | 状态 | 说明 |
| ------ | ---- | ---- |
| m7-1 链路设计（RBAC+JWT+注解方案） | ✅ | 已拍板 |
| m7-2 org-service RBAC 播种 | ✅ | staff/role/permission 种子 |
| m7-3a meiyun-security 公共模块 | ✅ | 统一拦截器 + @RequirePerm 注解 + JWT 解析 |
| m7-3b AuthController login/dev-login/permissions | ✅ | 三端点 + ROLE_SCOPE |
| m7-4 全站 @RequirePerm 七服务 | ✅ | 401/403/200 抽验全绿 |
| m7-6 前端登录态 | ✅ | 本报告 1.1 |
| m7-7 前端权限真源对齐 | ✅ | 本报告 1.2 |
| **m7-5 后端数据权限 data_scope** | ⬜ 待做 | SELF/STORE/REGION/GROUP 列表过滤注入；审批中心写动作细化鉴权 |
| **m7-8 脱敏扩面 + 越权用例** | ⬜ 待做 | 敏感字段全站脱敏扫描；横向/纵向越权 curl 用例集；服务层 cmd.operator() 收敛为 JWT 登录人断言 |
| **m7-9 M7 DoD 验收** | ⬜ 待做 | DoD 八条逐条验收 + 种子真源核对（E/SE 不一致）+ 里程碑交付报告 |

---

## 四、遗留与风险

1. **后端种子两套口径**：源码 SE 系列 vs 运行库 E 系列，m7-9 前需统一（建议以运行库 E 系列回写种子或全栈重灌），否则新环境部署后 dev-login 第一人会变。
2. **离线 mock 假人未清**：69 处 staff-* 假 ID 散落各演示页 mock 种子（consultation/customer/order/t1Rbac 等），不影响登录与权限真源，随各域去 mock 批次自然替换。
3. **/api/stores 归属**：门店主数据端点在全网关拓扑下由其他服务提供，org 直连联调时 500 属预期；前端已容错。
4. 联调临时实例 org-service（18096）仍在运行；vite dev 已停止。
