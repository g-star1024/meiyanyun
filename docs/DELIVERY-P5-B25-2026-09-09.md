# P5-B25 交付：域①客户域自动化三件套（标签自动化规则 / 消费自动积分 / 等级定时批处理）

> 交付日期：2026-09-09
> 范围：客户域（customer-service）自动化 + 1 个 txn-service 新增只读内部端点（非 B24 文件）
> 关联路线台账：DEVELOPMENT-ROADMAP（域① 5→8 ✅、⬜13→10；整体 54→57/166=34%）
> 代码状态：首版 `00a06e3`（customer 12 + txn 1，+1133）经审核通过；**运行态闭环补丁**（V9 改名 + 3 个运行态缺陷修复 + cron 外部化）见 §4.3，代码/docs 提交见 §六。双模块 `mvn -o compile` 通过。
> 运行态状态：**§4.2 五项冒烟已在 docker 双栈（meiyun_core 正式栈 + meiyun_seed 种子栈）全部跑通并取证**（2026-09-09），冒烟改动库数据已全量还原（§4.4）。

---

## 一、目标与边界

### 1.1 本批闭合的三张卡（Backlog 257 / 258 / 259）

| 模块 | Backlog | 核心能力 | 复用基础 |
|---|---|---|---|
| 标签自动化规则 | 257 | 规则引擎（TA###）+ 定时扫描打/撤标 + 5 类条件 + 审计 | 标签定义/人工打标（B23 已闭环） |
| 消费自动积分 | 258 | 拉取已收款订单自动发分、已退款流水回退积分，幂等不重复 | 积分账户/流水 + clientToken 幂等（B23） |
| 等级定时批处理 | 259 | 每月 1 号 cron 触发「只升不降」自动升级 + 汇总审计 | 自动升级服务 autoUpgrade()（B23） |

### 1.2 边界红线（铁律 / 红线约束）

- **B24 隔离**：严格不触碰 B24 文件——txn-service 的 `InternalFinanceController` / `ConsultPlan` / `PlanRevision` / `FinanceFlowDTO`、finance-service 全套、customer-service 的 `InternalCardController` 及 Card* 文件均未改动。txn-service 仅**新增 1 个独立文件** `InternalOrderController`（只读内部端点），与 B24 零耦合。
- **写接口四件套**：校验 + 幂等 + 审计 + 中文错误，全部沿用既有范式（`adjustPoints` / `assignTag` 既有实现）。
- **审计 payload 必为合法 JSON**（jsonb）：所有新增审计均手工拼 JSON 并转义。
- **幂等**：发分 clientToken=`AUTOPOINTS:{orderNo}`、回退=`AUTOPOINTS_REFUND:{orderNo}`，复用 `PointsLedger.client_token` 唯一判重；标签打标受复合主键约束 + `Conflict` 捕获。
- **不碰资金池**：自动积分只动 `customer.points` 与积分池月度累计，绝不触达资金/储值余额（CARD_SALE 售卡订单已排除，见 §2.2）。
- **双端/Token 不变**：纯后端自动化，无前端页面改动，不涉及 UI Token 与样式。

---

## 二、接口与状态契约

### 2.1 标签自动化规则（base path：`/api/customer/tag-rules`）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/tag-rules` | `tag:view` | 规则列表（分页，priority 排序） |
| POST | `/tag-rules` | `tag:edit` | 新建规则（四件套校验 + 目标标签存在性） |
| PUT | `/tag-rules/{ruleId}` | `tag:edit` | 更新规则（局部字段） |
| DELETE | `/tag-rules/{ruleId}` | `tag:edit` | 删除规则 |
| POST | `/tag-rules/{ruleId}/run` | `tag:edit` | 手动执行单条规则（即时扫描全部客户） |
| POST | `/tag-rules/run-all` | `tag:edit` | 手动执行全部启用规则 |

条件类型 `condition_type`：`CONSUME_GTE`（累计消费≥元）/ `VISIT_GTE`（到店次数≥）/ `POINTS_GTE`（积分≥）/ `LEVEL_IN`（等级命中逗号清单）/ `CHANNEL_EQ`（渠道=）。
效果 `effect`：`ASSIGN`（命中打标）/ `REVOKE`（命中撤标）；`revoke_when_unsatisfied=true` 时 ASSIGN 规则在条件不再满足时自动撤标。
审计：`TAG_AUTO` / CREATE·UPDATE·DELETE·RULE_RUN·RULE_RUN_ALL。

### 2.2 消费自动积分（引擎，无新增业务 HTTP 端点；定时任务驱动）

- 数据源（新增 txn 只读内部端点，见 §2.4）：`GET /api/txn/internal/paid-orders?from=&to=`、`GET /api/txn/internal/refunded-orders?from=&to=`。
- 积分折算：`points = floor( (amount分/100) × point_rule.earn_rate × level_rule_config.points_multiplier )`，两乘数缺省各为 1。
- 发分：`adjustPoints(customerId, +pts, "消费自动积分（订单 X）", "AUTOPOINTS:{orderNo}")`。
- 回退：`adjustPoints(customerId, -pts, "退款回退积分（退款单 X）", "AUTOPOINTS_REFUND:{orderNo}")`；余额不足时跳过（留人工调平，不制造负分）。
- 排除 `biz_kind=CARD_SALE` 订单（售卡/充值不计入消费积分，防充值循环积分）；排除在积分引擎发分侧强制执行（`AutoPointsService` 循环内跳过），txn 投影端点保持通用、不绑业务策略。
- 游标：`auto_points_state`（单行 id=1）记录 `last_run_at`，断点续跑、不重复扫描；txn 不可用则跳过本轮、下轮自愈。
- 审计：`POINTS` / AUTO_POINTS（有实际发/退分时）。

### 2.3 等级定时批处理（引擎，复用既有手动端点逻辑）

- 触发：`@Scheduled(cron = "${meiyun.level-monthly.cron:0 5 0 1 * ?}")`（默认每月 1 号 00:05）。cron 外部化为配置项 `meiyun.level-monthly.cron`，经 `MEIYUN_LEVEL_MONTHLY_CRON` 环境变量覆盖（docker-compose.app.yml 已显式声明该变量并带默认值），生产默认值不变；冒烟可临时覆盖为近未来时刻而无需改码/重建。
- 收敛：读 `level_rule_config.auto_upgrade`，关闭则跳过本轮。
- 执行：调用既有 `CustomerService.autoUpgrade()`（只升不降），升级>0 时落 `LEVEL` / AUTO_UPGRADE 汇总审计（payload 与手动端点 `POST /api/customer/member-levels/auto-upgrade` 完全一致）。
- 审计 actor：定时任务统一记 `SYSTEM`。

### 2.4 新增 txn 内部只读端点（base path：`/api/txn/internal`）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/paid-orders?storeCode=&from=&to=` | `internal:finance-flow` | 已收款订单精简投影（orderNo/customerId/storeCode/amount分/status/bizKind/createdAt） |
| GET | `/refunded-orders?storeCode=&from=&to=` | `internal:finance-flow` | 已退款流水精简投影（txnNo/orderNo/customerId/storeCode/refundAmt分/createdAt） |

- 权限复用 `internal:finance-flow`（系统身份 X-Internal-Token 已具备，无需新增权限种子）。
- **与 B24 `InternalFinanceController` 相互独立、互不依赖**，仅新增只读投影，不修改任何 B24 文件。

---

## 三、改动清单

### 3.1 标签自动化规则（customer-service，新文件 + 迁移）

| 文件 | 类型 | 说明 |
|---|---|---|
| `TagAutoRule.java` | 新增实体 | 规则表 `tag_auto_rule`（rule_id/name/enabled/effect/target_tag_id/condition_type/condition_value/priority/revoke_when_unsatisfied/时间戳） |
| `TagAutoRuleRepository.java` | 新增 | `findByEnabledTrueOrderByPriorityAsc` + `maxTaId()` |
| `TagAutoRuleService.java` | 新增 | 规则 CRUD（四件套）、条件求值（5 类纯函数）、`runRule`/`runAll` 分页扫描打/撤标 |
| `TagAutoRuleController.java` | 新增 | REST 端点（§2.1）+ 审计 |
| `TagAutoRuleJob.java` | 新增 | `@Scheduled(fixedDelay=60s)` 定时扫描启用规则，限批 50、单条 try-catch、无方法级事务 |
| `db/migration/V9__customer_automation.sql` | 新增迁移 | 建 `tag_auto_rule` + `auto_points_state`（IF NOT EXISTS 可重入）。**版本号由 V6 改 V9**：全库共享 flyway_schema_history 全局递增，V4–V8 已被 finance/card 占用（运行态 `flyway_schema_history` 核实），取首个空号 V9 |

### 3.2 消费自动积分（customer-service，新文件 + 配置）

| 文件 | 类型 | 说明 |
|---|---|---|
| `AutoPointsState.java` | 新增实体 | 游标单行 `auto_points_state` |
| `AutoPointsStateRepository.java` | 新增 | JpaRepository |
| `AutoPointsService.java` | 新增 | 拉取订单/折算积分/幂等发分回退/游标推进/服务间调用（X-Internal-Token） |
| `AutoPointsJob.java` | 新增 | `@Scheduled(fixedDelay=5min)` 驱动 scan()，有变动落 AUTO_POINTS 审计 |
| `application.yml` | 修改 | 新增 `txn.service.url`（默认 `http://localhost:8083`） |

### 3.3 等级定时批处理（customer-service，新文件）

| 文件 | 类型 | 说明 |
|---|---|---|
| `LevelMonthlyJob.java` | 新增 | 每月1号 cron，调用 `autoUpgrade()`，autoUpgrade 开关收敛，汇总审计 |

### 3.4 txn-service 新增只读内部端点（新文件，非 B24）

| 文件 | 类型 | 说明 |
|---|---|---|
| `txn-service/.../InternalOrderController.java` | 新增 | `/paid-orders`、`/refunded-orders` 只读投影（复用 `internal:finance-flow` 权限） |

### 3.5 路线台账更新

| 文件 | 类型 | 说明 |
|---|---|---|
| `docs/DEVELOPMENT-ROADMAP.md` | 修改 | 域① 5→8 ✅/⬜13→10；整体 54→57/166=34%；新增 B25 时间线行；3 个 Backlog 项标记 ✅ B25 |

---

## 四、验证（运行态真实取证，2026-09-09 docker 双栈）

> 环境：单 PG 容器 `meiyun-pg`（PG15.7），正式栈 `docker-compose.app.yml`（库 `meiyun_core`），种子栈 `docker-compose.seed.yml`（库 `meiyun_seed`，独立网络/容器/端口）。容器日志时间为 UTC，DB 时间为北京（+8h），下文证据时间以 DB/日志原文为准。后端 Dockerfile 仅 COPY 宿主 fat-jar，改码后须先 `mvn -o package` 再 `DOCKER_BUILDKIT=0 docker compose -f docker-compose.app.yml build customer-service txn-service` 并重启容器。

### 4.1 编译验证

- `mvn -o -pl customer-service,txn-service -am compile` ✅（补丁修复后复编译，退出码 0，无 ERROR）。
- 首版 18 个新类全部产出 `.class`；补丁改动 `AutoPointsService` / `InternalOrderController` / `LevelMonthlyJob` 均重新产出。

### 4.2 §清单五项冒烟结果（DELIVERY §4.2 原清单，全部跑通）

**① Flyway 建表（双库）**
- `meiyun_core`：customer-service 启动日志 `Successfully validated 10 migrations` → `Migrating schema "public" to version "9 - customer automation"` → `Successfully applied 1 migration ... now at version v9`；`flyway_schema_history` installed_rank=10 / version=9 / success=t；`tag_auto_rule`、`auto_points_state` 两表建成（`\d` 列结构齐全）。
- `meiyun_seed`：用同一新镜像重建 seed 的 customer/txn 容器后，12:35:31（UTC）同样从 v8 迁移到 v9（installed_rank=10），两表建成，两容器 healthy。**双栈隔离证实**：两库各自独立的 `auto_points_state`、`points_ledger`，互不串数据。

**② 打标 / 撤标（CONSUME_GTE + revoke_when_unsatisfied）**
- 建规则 `TA001`（CONSUME_GTE 阈值命中后向 TG008「VIP黑卡」ASSIGN，revoke_when_unsatisfied=true）→ 手工 `POST /tag-rules/TA001/run` 与 TagAutoRuleJob（60s）扫描，命中客户被打 TG008；调整条件/等级使其不再命中后，下一轮 Job 自动撤标。`audit_log` 落 TAG_AUTO / CREATE·UPDATE·RULE_RUN（248–251，冒烟后已随数据还原理清，见 §4.4）。幂等：重复扫描 `打标0 撤标0`，复合主键约束保证不重复打标。

**③ 已收款订单自动发分 + 幂等**
- 首跑（20:04:49 北京）AutoPointsJob 日志：`扫描5 发分订单3 发分2180 退分订单2 退分15800`。
- 3 笔已收款单（88000 分 / 80000 分 / 50000 分，earn_rate=multiplier=1）→ M005 +880（余额 2380）、M001 +800（43300）、M002 +500（19100）；`points_ledger` 落 client_token=`AUTOPOINTS:{orderNo}` 三行（26/27/28），CARD_SALE 售卡单被排除。
- 幂等：下一轮窗口重扫，`points_ledger` 仍 6 行（部分唯一索引 `uq_points_ledger_client_token` 兜底不重复加分）。

**④ 已退款流水回退**
- 存量 2 笔 REFUNDED 退款（对应原单 M001 1,500,000 分、80,000 分）→ 回退 M001 −15000（28300）、−800（27500），client_token=`AUTOPOINTS_REFUND:{orderNo}`（流水 29/30）。
- 增量验证（冒烟3）：手工插 REFUNDED 退款单 `RF20260909-SMOKE1`（原单 OD20260905-000002 / M002 / 50000 分），下一轮（20:14:49）增量窗口只处理新单 → M002 −500（19100→18600），游标增量推进、历史单不重放（流水 31）。

**⑤ 等级月批只升不降（近未来 cron）**
- `MEIYUN_LEVEL_MONTHLY_CRON='0 30 12 * * *'` 覆盖（经 docker-compose.app.yml environment 透传，容器 `printenv` 已确认注入），12:30:00（UTC，即北京 20:30）LevelMonthlyJob 触发，日志 `升级1人`：M005（total_spend 手工置 6000.00 ≥ 银卡阈值 5000，且当前序严格更低）由 `普通→银卡`，仅升一档；`audit_log` 落 LEVEL / AUTO_UPGRADE（id=256，actor=SYSTEM，payload 与手工端点一致）。
- 只升不降证实：黑卡 M003/M010（total_spend 不达 100000 阈值）**未被降级**；autoUpgrade 仅在序严格更低时升级。
- 验证后已恢复默认生产 cron（不传环境变量，容器回退 `0 5 0 1 * ?`）重启。

**种子栈普适性附加证据**：新镜像在第二份更大数据集 meiyun_seed 首跑 `扫描509 发分订单509 发分1480504 退分0`，无 UnexpectedRollbackException / ERROR，证明三处缺陷修复在更大真实数据量下同样成立。

### 4.3 运行态暴露并修复的三个真实缺陷（铁律 7：拒绝假交付）

冒烟不是走过场——首跑即暴露代码缺陷，已修代码、重建镜像、重新验证：

- **缺陷①（P1）退款投影取错客户列**：`InternalOrderController.refundedOrders` 原把 `txn_refund.customer`（**客户姓名冗余列**，如"数据域越权测试顾客"）当作 customerId 返回，customer-service 据此 `findById(姓名)` 必然 NotFound。修复：批量预载原单 `orderRepo.findAllById(orderNos)`（防 N+1），由 `txn_order.customer_id`（真实 M 编号）解析客户 ID；退款视图 customerId 改取原单。paid-orders 端点无需改（订单表 customer_id 本就是 M 编号）。
- **缺陷②（P1）大事务毒化整批 UnexpectedRollbackException**：`AutoPointsService.scan()` 原包单一方法级 `@Transactional`，循环内调他 bean 的 `adjustPoints`（REQUIRED 加入同一事务）；缺陷① 的 NotFound 运行时异常即便被 catch 静默吞掉，**共享事务仍被标记 rollback-only**，外层提交抛 `UnexpectedRollbackException` 导致整批发/退分全量回滚（且 NotFound 原无 warn，日志无线索）。修复：移除 scan() 方法级事务，每笔 `adjustPoints` 各自独立事务提交，单笔失败（NotFound/Unprocessable）只跳过并补 `log.warn`，绝不毒化整批；游标推进随最后一次独立提交落库。
- **缺陷③ 幂等重放虚增计数 + 重复落审计**：扫描窗口用 `last_run_at` 的本地日期（当天 00:00 起），同日数据每轮被反复重扫；`adjustPoints` 幂等命中是**正常返回 `AdjustResult(ledger, created=false)`**（非抛 Conflict），而 scan() 未检查 `created()` 仍计数，使 AutoPointsJob 据虚增计数重复写 AUTO_POINTS 审计（实测 253/254/255 三条重复；唯一索引保证 ledger 未重复扣分）。修复：发分/退分两段均仅在 `result.created()` 时累加计数；修复后 20:47:38 周期 `扫描0 发分0 退分0`、不再产生新审计。

配套测试设施改进（生产行为不变）：`LevelMonthlyJob` cron 外部化为 `${meiyun.level-monthly.cron:0 5 0 1 * ?}`，`docker-compose.app.yml` customer-service environment 显式声明 `MEIYUN_LEVEL_MONTHLY_CRON`（带生产默认值，未声明的宿主 env 不会被 compose 透传，此为首次注入失败的教训）。

### 4.4 冒烟改库数据的全量还原（诚实标注）

冒烟在 `meiyun_core` 上临时改动/造数，验证后已在单事务内全量还原并经下一 Job 周期复核（20:47:38 `扫描0 全0`，流水不重造）：

| 对象 | 冒烟改动 | 还原动作 | 还原后核实 |
|---|---|---|---|
| customer M005 | 普通→银卡、points 1500→2380、total_spend 置 6000 | update 回 points=1500/level='普通'/total_spend=NULL（NULL 为遗留种子基线，`db/seed_m3.sql` 无该列） | 1500 / 普通 / NULL ✅ |
| customer M001 | points 42500→27500（+800−15000−800） | update 回 42500（等级仍钻石，冒烟未动） | 42500 / 钻石 ✅ |
| customer M002 | 净变化 0（+500−500） | 无需还原 | 18600 / 金卡 ✅ |
| points_pool id=1 | gained_month +2180、redeemed_month +16300 | 校正回 gained=1,286,500 / redeemed=795,000（total_issued 8,640,000 冒烟本就不动） | 一致 ✅ |
| points_ledger | 新增 AUTOPOINTS% 6 行（26–31） | 删除该 6 行（恰为连续尾部，max 回到 13） | `AUTOPOINTS%` count=0 ✅ |
| customer_tag_rel TG008 | 冒烟规则误打 M002/M006、原 M010 被撤 | 补回 M010、删 M002/M006（基线见 seed_m3.sql，TG008 原仅 M010） | 仅 M010 ✅ |
| tag_auto_rule | 建 TA001 | 删除 | count=0 ✅ |
| txn_refund | 造 RF20260909-SMOKE1 | 删除（无 FK、无 outbox/fund_entry 残留，删前已核查 count=0） | count=0 ✅ |
| audit_log | 248–256（TAG_AUTO/POINTS/LEVEL 冒烟行） | 删除该连续尾部段 | max 回到 247（合法 PLAN 业务行），哈希链不断 ✅ |
| auto_points_state | 游标推进至当天、计数 | **保留 state 行**（删除会致下次从 2000 年全量回填、重造已删流水）；保留当天游标且计数为 0，历史单不在窗口内 | last_run_at=20:47:38、计数全 0 ✅ |

两栈 `MEIYUN_DEV_LOGIN` 均确认为 `false`。meiyun_seed 为一次性测试库（有 `scripts/reset-seed-db.sh` 可彻底清库），其 509 单首跑数据属测试库正常产物，未做逐笔还原。

---

## 五、本批明确不做（移交 Backlog）

- **售卡退款积分回退的对称排除**：发分侧已排除 `biz_kind=CARD_SALE`（审核补入）；但退款投影 `RefundedOrderView` 不返回 bizKind，通用退款入口亦不按 bizKind 拦截。若未来产生售卡退款，回退侧会尝试扣减积分（多数被「余额不足跳过」兜住，但客户有其它来源积分时可能误扣）。后续给退款投影 join 订单带 bizKind，回退侧对称跳过 CARD_SALE。
- **自动降级引擎（260）**：本轮仅做「只升不降」批处理；降级引擎（保护期/连续未达标/KPI）仍列 Backlog，待独立设计。
- **客户详情会员权益真实联动（261）**：discount 实际计价 + benefits 核销链路接入，属交易计价收口批，不在本批。
- **前端页面**：本批为纯后端自动化，无 UI 改动；规则管理界面为后续前端批。
- **通知/企微推送**：批处理报告仅落审计（actor=SYSTEM），未扩展通知中心推送（留待通知批）。

---

## 六、提交记录

- 首版按「先交付 + 审核」分两个提交落库：代码 `00a06e3`（customer 12 + txn 1，13 文件 +1133）、docs `37f6007`（本报告 + 路线台账，docs 在 .gitignore 内须 `git add -f`）。
- 审核补入 1 处修复：`AutoPointsService` 发分循环强制跳过 `biz_kind=CARD_SALE`（原仅文档声明、代码未拦）。
- **运行态闭环补丁（本轮）**：在 docker 双栈执行 §4.2 五项冒烟，首跑暴露并修复三个真实运行态缺陷（§4.3）——①`InternalOrderController.refundedOrders` 客户列错取姓名（改由原单 `txn_order.customer_id` 批量解析）；②`AutoPointsService.scan()` 去方法级大事务，逐笔独立提交消除 UnexpectedRollbackException 毒化并补 NotFound 告警；③发/退分仅在 `AdjustResult.created()` 时计数，消除同日窗口重扫虚增计数/重复审计。另将 `LevelMonthlyJob` cron 外部化为 `meiyun.level-monthly.cron`（compose 显式声明 `MEIYUN_LEVEL_MONTHLY_CRON`，默认值不变），迁移 V6→V9 避让全库共享版本号。补丁代码提交见 `git log -1 -- backend/customer-service/src/main/java/com/meiyun/customer/AutoPointsService.java`，docs 提交哈希见 `git log -1 -- docs/DELIVERY-P5-B25-2026-09-09.md`（自引用不硬编码以免哈希漂移）。
- 运行态结论：**§4.2 五项冒烟全部跑通并取证（§4.2），冒烟改库数据已全量还原（§4.4），双模块复编译退出码 0**。本批从「离线编译 + 待运行态验证」升级为「docker 双栈真实闭环」。
