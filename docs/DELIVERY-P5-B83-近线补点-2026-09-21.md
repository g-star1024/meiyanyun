# DELIVERY-P5-B83 · M3 近线运营补点施工批（3 卡）＋ ✅110→112 数字跃迁（2026-09-21）

> 批次：P5-B83 近线运营补点施工批（M3 首批施工批）
> 范围：02-modules 三行 mock/占位 → 真实——L66 疗程跟踪（卡1）→ L118 交接班（卡2）→ L175 通用页（卡3）
> 授权：用户 M3 三点拍板（2026-09-21，PLAN-M3-CANDIDATE-SCOPE-2026-09-21 §6）——①同意 M3 不做 L22 平台十页留待 M5；②组织树二批（兼岗模型 L34/L36/L37/L38）M3 期间并行；③B84 撞单合并涉及 8 张物理 FK 子表迁移，开工前先评审写期设计文档
> 结果：三卡四 commit 全部 push origin/main，三轨真验全绿；**批末数字跃迁 ✅110→112/127≈88%、⬜16→14≈11%、🔧1 不动**

## 1. 背景与记账口径

- **批次性质**：M3 近线运营补点施工批，零新页面/零新菜单/零新路由——三卡全部挂载既有视图，只换数据源（template/style 零改动铁律）。
- **唯一计数底座**：02-modules.md 大表模块行口径 **127**（B82 起唯一权威）。批中不跃迁，批末统一跃迁（01-dashboard L22 载明「批中不跃迁、批末目标 109→112 不变」——实际批末起点 110，因 B82 校准后 109＋B83 卡1 前已有 L66 翻 ✅ 计入，见 §6 勾稽）。
- **卡0 规划闭合**：PLAN-M3-CANDIDATE-SCOPE-2026-09-21 六章＋§6 用户拍板结论（docs `98ac04c`＋回填 `0301d5a` 已 push），纯规划无数字跃迁。
- **提交序列**：卡1 feat `91f1921`（11 文件）→ 卡1 fix `f9a9e09` → 卡1 docs 批中回写 `4fc4b89` → 卡2 feat `dbbb8bb`（11 files +1139/-205）→ 卡3 feat `42cfa1c`（2 files +179/-22）→ 批末 docs 原子提交（六册＋本 DELIVERY＋哨兵，`git add -f`）。

## 2. 影响面只读核实（卡0/卡前侦察结论）

- **L66 疗程跟踪**：customer-service 卡域已有 member_card/card_ledger/writeoff_record 三表，缺 expires_at 演示数据与 course-track 聚合读端点；前端 CourseTrackView 纯 mock。种子缺口＝110 张疗程卡 expires_at 全空、无 CONSUME 流水与核销记录。
- **L118 交接班**：store-service 零 handover 实现；前端 HandoverView＋stores/handover.ts 纯本地 mock。工作区有在途资产（handover 包八文件未跟踪未编译未验证），卡2 先核可用度再补写落地。
- **L175 通用页**：全局搜索 GSearchView 纯 mock（四分类假数据）、GHelpView 静态不过滤、反馈入口悬空；403 与 /closed-loop 闭环演示此前已具备真实能力，不在本卡施工面。手机号明文属 A1-17 脱敏合规缺口，须内联掩码。
- **权限码**：三卡零新增——course:view（宽码 6 角色⊃course:track 菜单窄码 3 角色且均持 view，拍板维持现状）、handover/store 读码沿用既有、search 全局搜索挂 customer 域既有码。

## 3. 代码改动（卡1–卡3，按 commit）

### 卡1 `91f1921`（11 文件）＋ fix `f9a9e09` — L66 疗程跟踪切真

- **种子**（setup-seed-db.sh B83 段，门控幂等）：110 张疗程卡 expires_at 回填（在用 26 张 30 天内/60 张活跃、已用完 19＋已退卡 5 全过期）；304 条 card_ledger CONSUME 流水＋304 条 writeoff_record 核销（WO-SEED 幂等）。
- **G1** `GET /api/customer/cards/course-track`（course:view，DataScope.canReadStore 越权空）：trackStatus 服务端推导 ACTIVE/EXPIRING≤30天/FINISHED/FROZEN，native query JOIN customer 无 N+1，phoneMask 脱敏。
- **G2** `GET /api/txn/writeoffs/by-card/{cardNo}`（course:view，越权/不存在 404「数据不存在或无权查看」）：operatorName org-service name-map 解析降级 null。
- **前端**：CourseTrackView script 重写接真（template/style 零改）＋api/customer.ts +27／api/writeoff.ts +21 各 1 函数，详情弹层懒加载按卡缓存，本地只读适配 asset.txnsOf 保模板形状。
- **fix `f9a9e09`**：G1 缺 storeCode 裸 500 改 400 中文「门店编码不能为空」；另修 Hibernate 6 native query 投影 Instant（OffsetDateTime 报 Cannot project）。

### 卡2 `dbbb8bb`（11 files +1139/-205）— L118 交接班切真

- **store-service handover 包八文件**：HandoverSession/HandoverSnapshot/HandoverException 三实体＋四仓储＋HandoverService 聚合服务（在途资产核实补写落地）。
- **读端点四件套**：`GET /api/store/handover/current-session`、`/sessions`、`/sessions/{id}/snapshot`、`/records`——当前班次/交班快照资金＋客情＋预约＋库存异动**四域聚合**＋差异阈值核对＋异常落库。
- **前端三件套**：api/handover.ts 86 行新建＋stores/handover.ts 344 行重写切真＋HandoverView.vue +44/-44 适配（template/style 零改动，mock 全废），金额 bigint 存分。

### 卡3 `42cfa1c`（2 files +179/-22）— L175 通用页切真

- **GSearchView.vue +156/-22**：全局搜索**四源聚合**（顾客/疗程卡/服务/产品）＋searchSeq 防竞态＋250ms 防抖＋**maskPhone 内联掩码** `p.replace(/^(\d{3})\d{4}(\d{4})$/, '$1****$2')`（A1-17 合规修复）。
- **GHelpView.vue +23**：quickStart/faqs/videos 三 computed 真实过滤＋意见反馈入口跳 /workorders 真实工单页。
- 两页 template/style 零改动。

## 4. 三轨真验（全绿）

**卡1（11 项 curl＋PG＋Chrome）**：正角 SST01=21 卡四态 11/7/2/1＋phoneMask 全脱敏；status=EXPIRING 过滤 7 行 daysLeft 4-24；缺参/空参 400「门店编码不能为空」；越权 SST02 200[]；无 token 401；G2 越权/不存在 404；SE006 SELF 正角本店 12 行。PG 对账 SST01 COURSE 卡 21=21＋种子三键 110/304/304。Chrome 18080：KPI 18/7/2/57、21 行完整渲染 EXPIRING 优先、弹层 5 条核销「林咨询 -1 次/条」、console 0 error、5 请求全 200。

**卡2**：mvn 编译＋双栈 deploy.sh 换载＋curl 端点权限矩阵（越权/缺参 400/无 token 401）＋PG 种子对账＋Chrome 账密登录页面真渲——会话·快照·核对·记录四区全渲染、console 0 error。

**卡3**：curl 经网关 18443 四源聚合端点核对＋浏览器搜索四分类·帮助三 computed 过滤·反馈跳 /workorders 全绿、console 0 error。

**双栈部署**：dist 双前端容器 docker cp 换载免重启（见 §5 教训）。

## 5. 如实说明（教训与偏差）

- **双前端容器环境混淆（本批最大教训）**：`meiyun-frontend`（宿主 **8080**，dev 库）与 `meiyun-seed-frontend`（宿主 **18080**，seed 库，与 curl 18443 网关同数据源）双容器并存，浏览器验证误开 8080 致会话丢失/登录失败假象（dev 库无 SE001 报「开发期登录已关闭」），一度误判为前端缺陷。**定案：部署与验证统一 18080 seed 栈，双栈 docker cp 换载**；已回写 01/03/00 三册与本 DELIVERY 留存。
- **手机号明文差异与 maskPhone 修复（A1-17）**：卡3 真验发现全局搜索顾客结果手机号明文渲染，与卡1 G1 phoneMask 口径不一；按 A1-17 脱敏铁律内联 maskPhone 掩码 `***` 修复，同 commit 闭合不另立 fix。
- **环境类偏差留痕**：navigate_page 缺 pageId 工具调用参数坑；快捷登录不填充改账密手工登录；/closed-loop-demo 404（实际路由 /closed-loop）；「新手推荐 · 4 篇」静态文案不随过滤变（按样式零改动铁律不修，留痕）。
- **数据处置**：三卡均只读/种子幂等，无测试污染需还原；审计 append-only 保留。
- **哨兵滞后**：批中两次接力（16:04 卡1 闭合后未及时翻哨兵、16:35 接力拍补真验），批末本提交统一修正为 DORMANT 并存档全链。

## 6. 进度落账（数字勾稽）

| 口径 | 批前 | 批后 | 说明 |
|---|---|---|---|
| ✅ 已完成 | 110 | **112** | +2＝L118 交接班（卡2）＋L175 通用页（卡3）翻 ✅ |
| 🔧 部分 | 1 | 1 | 不动（唯一 🔧＝02 L140 C 端移动端） |
| ⬜ 未开始 | 16 | **14** | -2，近线 ⬜ 构成删交接班（域⑦1）与通用页（域⑧1） |
| 合计 | 127 | 127 | 模块行口径不变 |
| 完成率 | 约87% | **约88%** | M3 完成度 86%→约88% |

**分域跃迁**：域⑦ 组织与权限 21✅/0🔧/1⬜ → **22✅/0🔧/0⬜（⬜ 清零）**；域⑧ 平台与基建 35✅/1🔧/5⬜ → **36✅/1🔧/4⬜**。余六域不动。

**02-modules 翻行**：L118 交接班 ⬜→✅（`dbbb8bb`）、L175 通用页 ⬜→✅（`42cfa1c`）；L174 A1 增强池占位行保持 ⬜ 不动。01-dashboard 八处（头部表/批末行/两口径/近线构成×2/分域表×2/⑧注记）、03-timeline 卡2/卡3/批总三行、00-history 批末条、04-backlog L10 注记均已同步回写。

## 7. 下一批

- **B84 撞单合并写期**：8 张物理 FK 子表迁移，**开工前先评审写期设计文档**（用户拍板③）；期1 候选发现只读端点 B62 卡3 `34bcfeb` 已就位。
- **组织树二批并行专项**：兼岗模型 L34/L36/L37/L38（用户拍板②，M3 期间并行）。
- 后续：B85 资产转移/合同 → B86 转介绍，M3 完成度目标约91%（115/127）。
