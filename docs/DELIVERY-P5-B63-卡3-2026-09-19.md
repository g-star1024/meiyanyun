# DELIVERY-P5-B63 卡3（2026-09-19）· 预收合规监控账龄扫描 Job＋规则/事件/站内信/审计全链（04-backlog L85）

> **批次性质**：第二层近线**财务收口纵深批第三卡**（P5-B63 四卡之卡3，非零代码批，四卡中唯一**中风险**卡——finance 服务首引 `@EnableScheduling` 定时调度）。04-backlog L85 登记的「预收合规监控（合规指标/挂账时长监控预警）」：核销/预收明细 B24 已切真可读，但全平台**零合规监控规则实体、零预警事件模型、零账龄扫描 Job**。本卡交付规则可配（阈值/级别/启用开关/门店白名单）＋每日账龄扫描＋事件幂等落库与自动回转＋仅新发 OPEN 外呼 txn 既有 internal 告警通路（复用 NotificationFanoutJob 60s 扇出既有通知管道）＋全轮审计 append-only。前端守**无新页面契约**：仅既有 FinPrepayView 监管页将真实 `PM-` 事件并入告警区，本地三类派生告警原样保留；规则管理仅后端端点，**不接管理 UI**。**无新页面、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解，数字一律不变**。
>
> **决策依据（用户四阶段授权逐字）**：「1、先处理第二层近线，可以全部开始；2、然后处理core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径 / 状态机 / 选型决策 按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入C. 大阶段 / 专项开工」。本卡为第①阶段第二层近线第四批 B63 第三卡（B60/B61/B62/卡1/卡2 之后），「可以全部开始」覆盖开工无需再请示；统一读模型口径（三规则数据源/阈值单位/级别映射）、事件状态机（OPEN/RESOLVED 幂等回转）、双栈调度形态（不引新开关、双 Job 各扫各库沿用惯例）、默认安全三层（纯 DDL 零种子＋enabled 默认 false＋白名单 NULL=全店）、外呼软降级策略全部按「按推荐来」授权由卡0 侦察与卡3 施工定案，零返工。
>
> **卡0 裁断（2026-09-19 07:17 闭合，硬结论，卡序＝卡1 L84→卡2 L83→卡3 L85→卡4 L86，风险递增＋依赖）**：
> ① **卡3 为四卡中唯一中风险卡**：finance-service 此前无任何调度引用，本卡**首引 `@EnableScheduling`**，卡序据此置后于卡1/卡2（两者均纯请求链路，无定时面），须专项验证双栈调度不互相重复扫描/重复告警，以及 Job 开关与规则启用的**默认安全态**——新装环境零规则/规则默认停用，Job 空跑只落 0 命中审计不外呼。
> ② **可复用底座全部实证存在**：txn 既有 internal 告警通路 `/api/txn/internal/compliance-alert`（X-Internal-Token 系统身份，网关 `/internal/**` 外网裸 404）与 NotificationFanoutJob 60s 扇出管道现成；customer 服务已有 02:00 巡检 Job 可作 cron 错峰参照；前端 FinPrepayView 监管页与 financeCore store、api/finance.ts 槽位真实，原告警全为本地派生（`PR-`/`PD-`/`PC-` 三前缀），接真只需并入真实事件不得造数。
> ③ **卡序依赖已满足**：卡1（V37/异常账务，`3d9d399`）、卡2（异常中心三源归集，`d413186`）先行闭合后卡3 开工；Flyway 版号 V38 经侦察确认空版可占（卡1 用 V37）；卡4 L86 进项税抵扣全链新建最大置后，本卡 V38 不影响 V39 后续版迁移。
>
> **提交清单（代码侧，一 feat commit，已 push origin/main）**：
> - 卡3 `c958169`（2026-09-19）`feat(P5-B63 卡3): 预收合规监控账龄扫描 Job＋规则/事件/站内信/审计全链 L85`——**16 files +1327/-3**（finance-service 9 新 4 改、frontend 3 改）。
> - 卡3 过程心跳 docs commit `7407d89`/`2099f92`/`b26b476`（非业务代码，不计入 03-timeline commit 列）。
>
> **铁律对齐**：-1 不臆造（三规则数据源经双库 psql 与全仓 grep 实证：fund_entry RF-DEPOSIT 净沉淀、txn 已退款流水、customer 休眠卡本金余额，空态诚实不造数）；0 开工先读哨兵＋六册（三枚接力心跳 7407d89/2099f92/b26b476 留痕，六册 6/6 读齐锁定回写落点）；1 API 三处一致（规则 type DEPOSIT_AGE/REFUND_PENDING/DORMANT_CARD、level HIGH/MEDIUM/LOW、事件 status OPEN/RESOLVED 在 V38 CHECK 约束、后端契约、前端 finance.ts 类型三处对齐）；2 DataScope/权限不回退（规则写门店白名单越域 404 不泄露存在性、overview 数据域逐店收敛、E 系无码账号 403、internal 端外网裸 404/直连无 token 401）；3 中文错误（409「规则编码已存在」、422 阈值/级别非法中文提示、404「规则不存在或无权查看」）；4 数字不虚增（纵深批数字一律不变）；5 前端真 API（FinPrepayView 真实 `PM-` 事件并入告警顶部，prepayPool 独立 ref 软容错绝不灌入 4 KPI）；6 双栈构建部署（mvn package finance -am＋pnpm build＋双栈真名容器换载 healthy，core/seed 各扫各库）；7 三轨真验（curl 经网关＋PG 双库＋Chrome 双隔离＋外呼软降级专项，§4 全绿）；8 一卡一 commit＋紧跟 push（c958169 已在 origin/main，本地远端一致）；9/10 台账五册只追加不改写历史（02 旧注尾接、04 L85 整行勾销）；11 哨兵 ACTIVE/心跳/批末存档；12 OCR 门禁 CLI 不可用转人工（正则全扫＋逐文件目检，结论见 §4.7）；安全：internal 端点 X-Internal-Token 系统身份、网关外裸 404、默认安全三层（V38 纯 DDL 零 INSERT＋enabled 默认 false＋白名单 NULL=全店）、外呼失败软降级不回滚扫描事务、测试数据单事务物理精准还原禁 TRUNCATE＋双份备份、审计 append-only 全保留。**本 DELIVERY 及全部提交元数据不出现任何凭据字面值，仅概述占位配置类型。**

## 1. 背景与记账口径

04-backlog L85 登记：「预收合规监控（合规指标/挂账时长监控预警）｜核销/预收明细 B24 已切真可读，缺合规监控规则与预警｜平台/交易收口批」。卡2 DELIVERY §7 逐字预告卡3 施工口径：「finance-service 首引 `@EnableScheduling` 账龄扫描 Job＋预警规则实体（阈值/级别/启用开关），扫描 prepay 预收池按账龄与余额派生合规预警；复用 txn 既有 internal 告警通路 `/api/txn/internal/compliance-alert` 落合规告警单。WARN→STORE_MGR、CRITICAL→REGION_MGR＋SUPER_ADMIN；幂等键前缀防重复告警；复用 txn NotificationFanoutJob 60s 扇出既有通知管道；闲置端点与既有 FinPrepayView 页当前三类本地派生告警换真源（样式零改）。中风险点：首引定时调度，须验证双栈调度互斥与 Job 开关默认态」。卡0 只读侦察于 2026-09-19 07:17 零代码闭合；卡3 当日 19:36 起首轮冒烟扫描，代码单 commit `c958169` 当日 push 完成。

**记账口径（数字一律不变，纵深批）**：本卡无新页面（FinPrepayView 为既有监管页）、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解；规则/事件为**全新监控域**而非业务模块状态翻转，故仪表盘维持 **✅109/🔧1/⬜55=166（约66%）**；域⑥ 财务维持 **11✅/0🔧/1⬜**。台账处置仅一项：04-backlog **L85 整行勾销**（删除线＋闭合长注＋末列 ✅ B63）；L86 一行保持开放，为卡4 目标。本卡验证产生测试写入（3 规则/3 事件/7 通知/7 投递及 14 行审计），业务测试数据**事后单事务物理精准还原**（禁 TRUNCATE、双份备份留盘不入库），审计 append-only 全保留（口径见 §4.6/§5.1）。

## 2. 影响面只读核实（卡0/施工前侦察硬结论）

- **预收池真实但监控为零**：fund_entry 中 RF-DEPOSIT 充值分录与卡余额流水真实存在（B24 已切真可读），但全仓**零规则实体、零扫描调度、零合规事件表**——「挂账时长/超额/长期未核销」在数据层可算、在产品层无人算。
- **finance 首引调度**：finance-service 此前无 `@EnableScheduling`/`@Scheduled` 任何引用；customer 服务已有 02:00 巡检 Job，本卡 cron 默认 **02:20 Asia/Shanghai** 与其错峰；cron 走全键 `meiyun.security.prepay-monitor.cron`，env `MEIYUN_PREPAY_MONITOR_CRON` 可覆盖。
- **外呼与扇出底座现成**：txn 既有 `POST /api/txn/internal/compliance-alert`（系统身份＋X-Internal-Token，外呼分级落合规告警单），NotificationFanoutJob 60s 扫描扇出站内信，卡3 不新建通知管道，只在「新发 OPEN 事件」时外呼一次。
- **前端槽位真实但告警全本地派生**：FinPrepayView 监管页现存三类本地告警（账龄 `PR-`/待退 `PD-`/休眠卡 `PC-` 前缀），4 KPI 与 5 项 checks 为既有完成度数字载体；接真策略＝真实 `PM-` 事件**并入告警区顶部**，本地三告警原样保留，**prepayPool 数据绝不灌入 4 KPI**（防数字虚增）。
- **Flyway 版号**：卡1 已占 V37（fin_abnormal_bill），双库 flyway 37/38 迁移史核实 V38 空版可占，卡4 进项税抵扣后续版＝V39。
- **权限码**：overview `finance:prepay:view`；规则读 `finance:settings:view`、规则写 `finance:settings:edit`；internal 触发端 `internal:prepay-monitor-run`（仅 8087/18087 服务间直连，不面向登录用户）。网关 Go 路由对 `/api/finance/internal/**` 外网一律裸 404。

## 3. 施工（单卡，16 files +1327/-3，commit `c958169`）

### 3.1 DDL：Flyway V38 纯 DDL 零种子（76 行新，双库可重入）

`V38__prepay_monitor.sql` 双表：

- **prepay_monitor_rule**：`code` varchar(16) PK、rule_name varchar(64)、`type` varchar(16)（CHECK ∈ DEPOSIT_AGE/REFUND_PENDING/DORMANT_CARD）、store_code varchar(16) NULL（**门店白名单，NULL=全店**）、threshold_value bigint（CHECK >0）、threshold_unit varchar(8)（CHECK ∈ DAY/FEN）、`level` varchar(8)（CHECK ∈ HIGH/MEDIUM/LOW）、`enabled` boolean NOT NULL **default false**、remark varchar(256)、created_by/updated_by varchar(16) default 'system'、created_at/updated_at；索引 pkey＋idx_enabled/idx_store/idx_type。
- **prepay_monitor_event**：event_id bigint generated always as identity PK、idem_key varchar(96)（**uk_idem 全唯一**，形如 `PM:{code}:{store}:R{轮次}`）、rule_code/store_code/`type`/`level`/title varchar(128)/content varchar(500)、amount_fen bigint default 0（CHECK >=0）、biz_ref varchar(32)、`status` varchar(8) default 'OPEN'（CHECK ∈ OPEN/RESOLVED）、fired_at/resolved_at NULL/created_at；关键约束 **uk_open UNIQUE (rule_code, store_code) WHERE status='OPEN'**（同规则同店同时只允许一条 OPEN）＋idx_fired/idx_status/idx_store。
- 迁移**零 INSERT**：新装双库规则表恒空，Job 空跑只落审计不外呼——默认安全第一层。

### 3.2 域模型与仓储（4 新，共 201 行）

- **PrepayMonitorRule.java（83 行）**：规则实体，type/unit/level 枚举约束在实体与 DDL 双侧；`type` 建后不可变（更新端点拒绝改型，防历史事件语义漂移）。
- **PrepayMonitorEvent.java（81 行）**：事件实体，幂等键＋OPEN/RESOLVED 生命周期字段。
- **PrepayMonitorRuleRepository.java（10 行）**：启用规则、按 code/店查询。
- **PrepayMonitorEventRepository.java（27 行）**：idem_key 命中查询、OPEN 部分唯一索引兜底冲突捕获、按规则/店批量回转 RESOLVED。

### 3.3 核心服务 PrepayMonitorService.java（740 行新）

- **runOnce 扫描主链**：拉 enabled 规则→按 type 分三路 evaluate→命中且无同规则同店 OPEN 则幂等落事件（依赖 uk_open 部分唯一索引并发兜底）→**仅新发 OPEN** 外呼 txn `/api/txn/internal/compliance-alert`（外呼异常 log.warn 软降级，**不回滚扫描事务**）；既有 OPEN 本轮不再命中则批量回转 RESOLVED；轮次 idem 设计支持规则再编辑后重新触发。
- **三规则数据源（全部只读投影，不造数）**：
  - DEPOSIT_AGE：fund_entry RF-DEPOSIT 净沉淀 native 投影 `depositAgeScan`（FundEntryRepository +21），按最宽阈值单扫后内存二次过滤，账龄取最早充值日；
  - REFUND_PENDING：txn finance-flows 已退款流水按店聚合待核销金额；
  - DORMANT_CARD：customer 卡投影休眠态卡，余额口径取**本金不含赠金**。
- **overview 只读视图**：4 KPI 数字不由此供给；告警列表与 5 项 checks 数据源收敛，逐店 DataScope。
- **规则写四件套**：重复 code→**409**；阈值/级别/单位非法→**422**；门店越域/不存在→**404**（不泄露门店是否存在）；更新时改 type→**422**（type 不可变）。
- **审计**：每轮扫描（含 0 命中）落 `biz_type=COMPLIANCE`/`action=PREPAY_MONITOR`，txn_no=`PM-SCAN-{bizDate}`；规则增改落 PREPAY_RULE_CREATE/UPDATE（txn_no=规则号 PM-Tn）。
- **外呼分级**：HIGH→告警级别 CRITICAL，派 REGION_MGR＋SUPER_ADMIN；MEDIUM/LOW→WARN，派全量 STORE_MGR；bizRef `PM{yyMMdd}:{code}:{store}` **日粒度幂等**防重复打扰。

### 3.4 调度与启动（2 改，共 41 行）

- **PrepayMonitorJob.java（39 行新）**：`@Scheduled(cron = "${meiyun.security.prepay-monitor.cron:0 20 2 * * ?}", zone = "Asia/Shanghai")` 薄壳兜底，委托 runOnce，吞异常只记录（调度线程不被业务异常毒化）。
- **FinanceApplication.java（+2）**：首引 `@EnableScheduling`，保留既有 `@EnableAsync` 不动。
- **application.yml（+3）**：cron 全键默认值＋env `MEIYUN_PREPAY_MONITOR_CRON` 覆盖位。

### 3.5 端点（2 新，共 106 行）

- **PrepayMonitorController.java（74 行新）**：`/api/finance/prepay-monitor`——GET overview（`finance:prepay:view`）＋规则列表/新建/更新（读 `finance:settings:view`、写 `finance:settings:edit`），逐店 DataScope 收敛，错误全中文。
- **InternalPrepayMonitorController.java（32 行新）**：`POST /api/finance/internal/prepay-monitor/run`（`internal:prepay-monitor-run`＋X-Internal-Token 系统身份），供联调/运维手动触发一轮扫描；经网关访问裸 404，直连无 token 401。

### 3.6 既有面改动（2 改，共 27 行）

- **FinanceAggregationService.java（+6）**：`fetchCards` 降为包级可见供监控服务复用卡投影，不改既有聚合语义。
- **FundEntryRepository.java（+21）**：新增 `depositAgeScan` native 投影（RF-DEPOSIT 净沉淀＋最早充值日），既有查询零改动。

### 3.7 前端三件（3 改，共 +136/-3，style 零改）

- **api/finance.ts（+84）**：2 个 type（规则/事件）＋4 个 interface＋5 个薄封装（overview、规则列表/建/改、internal run 不经前端）。
- **stores/financeCore.ts（+39 混合）**：新增 openAlerts/prepayPool 两个独立 ref，各自独立 try/catch 软容错（监控域异常不波及既有财务聚合）；**4 KPI computed 零改，prepayPool 绝不灌 KPI**。
- **FinPrepayView.vue（+13 混合）**：真实 `PM-` 事件并入告警区顶部，本地 `PR-`/`PD-`/`PC-` 三告警原样保留；4 KPI/5 checks 模板与 **scoped style 零改**。
- pnpm build（vue-tsc --noEmit && vite build）EXIT=0。

## 4. 三轨真验（全绿，逐轨留证）

### 4.1 构建部署

mvn package finance -am BUILD SUCCESS；pnpm build EXIT=0；scripts/deploy.sh 双栈换载（meiyun-finance、meiyun-seed-finance 及前端），换载后容器 healthy。双栈 Job 同 cron 各扫各库：core/seed 物理隔离，不互调、不重复告警（沿用双栈惯例，不引新开关——见 §5.2）。

### 4.2 curl 轨（经网关 8443/18443，禁绕过）

- **白名单规则 run**：仅含授权门店的启用规则经 internal 直连带 token 触发后，rule/event 按预期落库；非白名单门店不产生事件。
- **幂等链**：OPEN 事件同规则同店重复命中被 uk_open 部分唯一索引兜底，再跑 **0 新事件 0 重复站内信**；规则条件解除后既有 OPEN 批量回转 **RESOLVED**；轮次 idem 支持重新启用后再开新事件。
- **规则写四件套边界**：重复 code→**409 中文**；阈值非正/单位与 type 不匹配/级别非法→**422 中文**；门店越域→**404 不泄露存在性**；更新请求携带不同 type→**422**（type 不可变）。
- **权限与 internal 安全**：无权限账号 overview→403；经网关访问 `/api/finance/internal/prepay-monitor/run`→裸 **404**；服务直连无 X-Internal-Token→401，带系统身份→200。
- **双栈数据域收敛**：prod/seed 双栈规则、事件、通知各自落本库，无交叉污染。

### 4.3 PG 轨（双库，meiyun-pg:5433）

- 验证窗口：core 库落 3 规则（PM-T1/T2/T3，含 E012/E011 两操作人建改）、3 事件；V38 CHECK/uk_idem/uk_open 索引经实际冲突尝试验证有效；flyway 双库 V37/V38 均 success=t。
- 外呼链路：7 条测试通知（id 638-644）与 7 条投递经 txn NotificationFanoutJob 扇出落库，bizRef 呈 `PM…` 日粒度前缀。
- seed 库：规则/事件/PM 审计全程 **0**（双栈物理隔离实证）。

### 4.4 Chrome 轨（8080 正式栈，双隔离会话）

- FinPrepayView 监管页：真实 **PM- 告警并入告警区顶部**，本地 PR-/PD-/PC- 三告警原样保留；**4 KPI/5 checks 数字零漂移**（✅109/🔧1/⬜55、域⑥ 11/0/1 载体不变）；规则停用/事件回转后告警区实时收敛。
- 双隔离会话 console error/warn 均 **0 条**；scoped style 零改，无新页面、无新路由。

### 4.5 软降级专项（txn 外呼不可达）

模拟 txn compliance-alert 不可达：扫描主链**仍正常提交**（事件照落、审计照写），外呼侧 log.warn 软降级不回滚、不抛入调度线程；txn 恢复后下一轮 OPEN 幂等保护不产生重复信。另验证 financeCore 两新 ref 独立容错：监控域接口异常时既有财务聚合页 200 不受累。

### 4.6 测试数据单事务物理精准还原（禁 TRUNCATE，双份备份留盘）

备份目录 `scripts/backup-b63-prepay-20260919/`（`.gitignore:96` 规则 `backup-*/`，**留盘不入库**），9 份 CSV：

| 文件 | wc -l | 数据行 | 用途 |
| --- | --- | --- | --- |
| snapshot_notification.csv | 642 | 641 | 通知表卡前全量快照（201160B） |
| snapshot_notification_delivery.csv | 642 | 641 | 投递表卡前全量快照（223097B） |
| precise_notifications.csv | 8 | 7（id 638-644） | 本卡测试通知精准切片（2731B） |
| precise_deliveries.csv | 8 | 7 | 同批 7 条扇出投递切片（2795B） |
| snapshot_prepay_monitor_rule.csv | 4 | 3（PM-T1/T2/T3） | 规则表快照 |
| precise_rules.csv | 4 | 3 | 测试规则精准切片（与快照同尺寸＝测试行全删） |
| snapshot_prepay_monitor_event.csv | 4 | 3 | 事件表快照 |
| precise_events.csv | 4 | 3 | 测试事件精准切片（1321B，同理全删） |
| audit_retained.csv | 12 | **11（id 720-730）** | 正式验证窗口审计切片（5452B，append-only 不还原仅留证） |

- **还原动作**：单事务内按外键顺序 DELETE——3 规则/3 事件/7 通知/7 投递（精准 id 清单，**禁 TRUNCATE**）；审计表零删除。
- **勾稽（四条独立证据交叉闭合）**：
  1. `641 snapshot − 7 precise = 634`：notification 与 notification_delivery 双表现表均 **634**，双表对齐；
  2. rule/event 快照与精准切片**同尺寸**（均 4 行含表头）＝测试行全删，双表现表 **0**；
  3. `notification.biz_ref::text LIKE 'PM%'` 残留 **0**；
  4. seed 库 PM 审计/规则/事件全 **0**。
- **审计 append-only 终态**：audit_log 全表 **704 行**、max id=732（历史缺号为既往批次已登记断链，不修）；本卡 PM 段 **id 719-732 共 14 行**全部保留（口径见 §5.1）。

### 4.7 OCR／敏感信息门禁（铁律12）

ocr/gitleaks/trufflehog CLI 本机不可用，按铁律12 转人工：对 16 个改动文件执行敏感信息正则全扫＋逐文件人工目检——无新增密钥/无 PII/无硬编码真实 IP/无调试残留/无无关改动；仅复用既有本地联调占位配置（数据源口令、JWT 签名密钥、服务间内部令牌、回环服务地址四类既有占位），生产全部经环境变量注入，**本卡零新增任何凭据字面值**。本 DELIVERY、台账与 commit message 均只概述占位类型，不复述任何明文。

## 5. 如实说明

### 5.1 审计 14／13／11 三个数字的成因（append-only 全保留）

本卡审计段 biz_type 恒为 **COMPLIANCE（域）**，子型在 **action 列**（PREPAY_MONITOR/PREPAY_RULE_CREATE/PREPAY_RULE_UPDATE）；扫描日/规则号在 txn_no 列。14 行明细：

| id | 时间 | txn_no/action |
| --- | --- | --- |
| 719 | 19:36:19 | PM-SCAN 首冒烟扫描（SYSTEM） |
| 720 | 19:45:04 | PM-T1 规则创建（E012） |
| 721 | 19:45:32 | PM-T2 规则创建（E011） |
| 722 | 19:45:56 | PM-T1 更新 |
| 723 | 19:46:09 | PM-T1 更新 |
| 724 | 19:46:09 | 扫描 |
| 725 | 19:46:32 | 扫描 |
| 726 | 19:47:18 | PM-T1 更新 |
| 727 | 19:47:18 | 扫描 |
| 728 | 19:47:39 | PM-T1 更新 |
| 729 | 19:47:39 | 扫描 |
| 730 | 19:48:15 | PM-T3 规则创建（E012） |
| 731 | 19:48:15 | 扫描 |
| 732 | 19:53:38 | **还原后末次 0 命中确认扫描** |

合计 **7 扫描/3 创建/4 更新＝14 行（PG 终态实存）**；备份切片 `audit_retained.csv` 留存正式验证窗口 **id 720-730 共 11 行**（719 早于切片时点、731/732 晚于切片时点）；早前台账中间文案曾出现「13 行」，系 id 732 末次确认扫描落库前口径，批末已在 04-backlog L85 更正为 14/11 双口径并陈。**id 732 不是污染**：它是业务数据全部归零后 Job 仍健康执行、0 命中也如实落审计的正向回归证据，故与全表一并 append-only 保留。

### 5.2 双栈 Job 双跑不引新开关

prod/seed 双栈 finance 同 cron 各启 Job，经双库物理连接各扫各库（规则、事件、通知完全不交叉，seed 全 0 实证）。沿用项目双栈既有惯例（与 customer 巡检 Job 同形态），**不新增「调度总开关」配置项**；安全语义由三层默认态承担：V38 零种子＋enabled 默认 false＋白名单 NULL 才全店（显式配置才生效），新装/误启环境不会产生任何告警外呼。

### 5.3 规则管理只有后端端点，不接管理 UI

按卡0 契约与「无新页面」约束，规则 CRUD 仅交付 `/api/finance/prepay-monitor` 后端端点（供运维/后续管理页使用），本卡**不新建规则管理页、不在既有页面塞入规则表单**；FinPrepayView 只消费 overview/事件读模型。规则配置当前经 internal/授权接口操作，管理 UI 留存后续 backlog 决策。

### 5.4 本地三告警保留与 KPI 隔离

FinPrepayView 原有 `PR-`/`PD-`/`PC-` 三类本地派生告警**不删除**（真实 PM- 事件仅在其上方并入），避免无规则启用期监管页空白，也不擅自改变既有演示语义；但真实监控数据走独立 ref 软容错，**绝不进入 4 KPI computed**——纵深批完成度数字零漂移由代码结构保证而非仅靠约定。

### 5.5 cron 错峰与 env 覆盖

默认 `0 20 2 * * ?`（每日 02:20 Asia/Shanghai）刻意避开 customer 02:00 巡检；双端 yml 均可用环境变量 `MEIYUN_PREPAY_MONITOR_CRON` 覆盖（含压测/联调改为高频或暂停的运维需求），不改代码、不改迁移。

### 5.6 金额与余额口径

后端金额一律存分（bigint/fen，CHECK >=0），前端按元展示；DORMANT_CARD 规则余额口径明确取卡内**本金余额不含赠金**（赠金为营销负债，不属于预收合规挂账口径），该口径在服务投影与前端展示两侧一致。

## 6. 进度落账

批末台账五分册＋本 DELIVERY＋哨兵同一 docs 原子提交（docs/ 被 gitignore：已跟踪 `git add -u docs/`，本新文件 `git add -f docs/DELIVERY-P5-B63-卡3-2026-09-19.md`；备份目录 `backup-*/` 不 add）：

- **04-backlog L85**：整行加删除线，闭合长注（V38 双表/uk_open/三规则数据源/Job 02:20/外呼分级日粒度幂等/软降级/写四件套边界/internal 404/前端无新页面/三轨/单事务还原 3+3+7+7/9 CSV 留盘/审计 14 行双口径/数字不变），末列改「✅ B63」；L86 一行零改动保持开放。
- **02-modules L100（域⑥「税 / 卡余额 / 日结 / 异常」行）**：状态保持 ✅、批次列不动，卡2 句号收口后尾接 P5-B63 卡3 闭合长注（c958169/16 files/首引调度/V38/三规则/幂等事件/外呼软降级/无新页面/三轨/物理还原/审计保留/L85 收口/数字不变）。不新增行、不翻状态。
- **00-history**：顶部「最近更新」换卡3 巨型行（c958169 战果＋L85 勾销＋数字不变）；原卡2 行首加「> 上一批 B63 卡2：」前缀下沉（内容全保留）。
- **01-dashboard**：卡2 bullet 前倒序插入卡3 bullet；数字格 ✅109/🔧1/⬜55=166 与 L43 域⑥ 11/0/1 一字不动。
- **03-timeline**：卡2 行（尾句「转卡3」原文保留不改写历史）后正序追加 P5-B63 卡3 六列行（commit 列仅 c958169；过程心跳 7407d89/2099f92/b26b476 注明不计；交付文档 DELIVERY-P5-B63-卡3-2026-09-19；末列 L85 勾销＋数字不变＋卡序卡1✅→卡2✅→卡3✅→卡4 L86＋哨兵转卡4）。
- **索引 DEVELOPMENT-ROADMAP.md**：通读确认纯索引无随动。
- **HANDOFF-AUTO.md**：STATUS 保持 ACTIVE（队列未全闭合）；HEARTBEAT 取批末实时北京时间（2026-09-19 20:xx CST），旧 18:16 心跳降为历史；CARD 由卡3 🔨 L85 改写为卡4 ⬜ L86 进项税抵扣链路（卡3 标 ✅＋c958169）；PREV 前置卡3 锚（docs hash 由卡4 接力回填，沿 B62/卡1/卡2 先例）；人读区顶部倒序插入卡3 闭合 bullet（数字 ✅109/🔧1/⬜55=166、域⑥ 11/0/1 一字不动）；在「## P5-B63 卡2 闭合存档（2026-09-19）」前插入「## P5-B63 卡3 闭合存档（2026-09-19）」八要素块。

## 7. 下一批

**卡4 = 04-backlog L86 进项税抵扣链路**（B63 四卡之末卡、最大全链新建）：

- **建设目标**：进项发票实体→认证（勾选确认）→抵扣（进项税额抵减销项）→申报全链；Flyway **V39** 迁移（V38 后续版，版号已核空）。
- **收口缺口**：FinTaxView 税盘页 `inputDeduct`（进项抵扣额）当前**恒 0**、FinInvoiceView 发票页**无进项 tab**——当前发票/税页仅覆盖销项与台账视角，进项抵扣诚实未实现。
- **中风险点**：税基/抵扣口径属 B 类产品口径决策（授权「按推荐来＋竞品调研」），开工前须先做只读侦察与口径定案（认证有效期、不得抵扣情形、申报期状态机），再动 V39。
- 卡4 闭合后 P5-B63 四卡全绿（L84✅/L83✅/L85✅/L86✅），B63 置 DONE，按序转 **B64 诊疗四行**。

按「卡0 硬结论直接取用→一卡一 commit+push→前端样式零改只换数据源→curl+PG+Chrome 三轨真验→测试数据单事务还原→批末五分册落账」继续施工。队列顺序：卡1 L84✅→卡2 L83✅→卡3 L85✅→**卡4 L86（下一张）**，四卡全闭合后 B63 批置 DONE 并转 B64；其后 B65 营销散项/B66 报表数据/B67 测试体系，再 B68 core 库 24 行营销残留仿 B57 备份+物理删除、B69 B 类产品口径/状态机/选型决策＋竞品调研，全部完成转 C 大阶段。
