# 美研云门店中台 · 整体开发规划与进度台账（DEVELOPMENT-ROADMAP）

> 本文件是**唯一的整体进度活台账**（铁律 9）。每批交付 commit+push 后、汇报前必须同步更新。
> 状态判定以**真实交付**为据：✅ 必须「真实端点经网关 200 + 前端接真实 API（非 mock）+ 交付文档验证记录」三者齐备；🔧 半成品（后端有端点前端 mock，或前后端均实但无交付文档验证）；⬜ 未开始（mock/占位）；⏸ 暂缓。
> 旧 `DEVELOPMENT_PLAN.md`（2026-09-01）仅作历史参考，不再回写。
>
> **基线**：P5-B24 已交付（2026-09-09，财务边角两卡 + 诊疗主轴三卡：财务四页去 mock/发票 CSV、卡余额时间线/核销双签、咨询草稿接诊、treat-start/treat-done 治疗全链路、我的工作台计数真实化）。
> **最近更新**：2026-09-13（P5-B46 卡2 交付：**AI 智能中心首页 A1HomeView 整页去 mock 一整页——四 KPI/12 能力卡调用量/待办告警全部接真，83→84/166=约51%（⬜ 81→80）、域⑧ 9✅2🔧56⬜→10✅2🔧55⬜、A1 5/15→6/15 页（实测余 10 个 mock 视图）**，feat `8bee59d`，2 文件 +175/-32）。纯前端切真、无新后端端点：按三类 AI 治理权限分组门控——canGateway（aiGateway:view）Promise.allSettled 并发拉 logKpi/monthlyBill/listAlerts 供「今日 AI 调用/今日成功率」两 KPI 与活跃告警待办，canGovern（aiGovern:view）拉 approvals?status=PENDING 以分页 **totalElements** 为「待审批」KPI 真值（>0 显「待处理」红色趋势），canAdmin（aiAdmin:view）拉 sensitive/hits/stats 供「敏感词拦截（今日）」KPI 与词库待办；无权限不发请求、KPI 显「—」。12 张能力卡保留产品元数据静态结构，capCalls computed 仅对有 ai_invoke_log 的六功能（profile/churn/scripts/content/scheduling/govern）显「本月 N 次调用」、敏感词卡显「今日拦截 N 次」，无日志功能诚实隐藏不伪造；待办流=待审批前 5（fmtAgo(appliedAt) 跳 /ai/govern）+今日敏感词命中条+active=true 告警前 5，空态显「暂无待办，各项指标运行正常」。utils/datetime.ts 新增 fmtAgo（刚刚/N 分钟前/N 小时前/N 天前/超 30 天回落本地日期）。**修复两处缺陷**：onMounted govern 分支曾越权兜底调 logKpi（无 aiGateway:view 必 403）改为纯审批 totalElements；模板残留已删字段 cap.calls 改 capCalls[cap.key]。三轨真验：curl 五端点真值（KPI 今日 2/成功率 50%/P99 60959ms/activeAlerts 3、bill profile 月 6 次 5284 tokens、PENDING 审批 totalElements 0、敏感词今日 2/总 2）；PG 八项对账（ai_invoke_log.invoked_at、ai_sensitive_hit.hit_at 均按北京时间日界）与 API 全一致；Chrome E011 全权限登录后 4 KPI=2/2/50%/0、画像卡「本月 6 次调用」、敏感词卡「今日拦截 2 次」、待办 4 条（1 敏感词条+3 条 active 告警，非 active 调用量规则正确隐藏），9 个 XHR 全 200 零 403、console 零 error/warn、全页截图。npm run build（含 vue-tsc）exit 0 后重建 frontend 静态镜像部署。A1ContentView（渠道内容生成）因 invoke 缺渠道字段口径放下一卡做透，其余 9 页/实测 10 视图、EMBEDDING/VISION、多供应商、token 账单环比、ddl-auto validate、告警事件持久化、全站时区推广登记 Backlog，详见 DELIVERY-P5-B46-2。
> 上一批 B46 卡1：2026-09-13（P5-B46 卡1 交付：**敏感词词库管理+命中留痕落库+敏感词检测页去 mock 一整页，AI 中心前端时区统一工具与 8 表 updated_at 触发器同卡落地，82→83/166=50%（⬜ 82→81）、域⑧ 8✅2🔧57⬜→9✅2🔧56⬜**，feat `d944a01`）。后端 ai-service：V19 共享 Flyway 链——新建 ai_sensitive_hit 命中表 14 列（hit_id 序列/hit_at timestamptz default now()/word_id 可空/word/category/feature_code/staff_id·staff_name/store_code/context_snippet(500)/false_positive default f/marked_by/marked_at，CHECK category IN BANNED/INJECTION，hit_at DESC·word_id·false_positive 三业务索引）+ai_sensitive_word 补 updated_at；函数 ai_set_updated_at() 为 8 张 AI 表挂 UPDATE 触发器（同值更新亦刷新；ai_quota/ai_feature_binding 不挂）。SensitiveWordController 六端点（类级 /api/ai/sensitive+aiAdmin:view）：GET /words 全量 List（命中数 countGroupByWord 内存聚合，**非 Spring Page、无 keyword 参数**）、POST /words 与 POST /words/{id}（aiAdmin:edit，重复词中文 400、changed=false 空操作幂等不写审计）、GET /hits 真分页（category 过滤、size 夹 1~200、hit_id 倒序）、GET /hits/stats（今日按北京时间日界）、POST /hits/{id}/mark-fp（幂等重复标注不写审计）；AI_SENSITIVE_WORD CREATE/UPDATE、AI_SENSITIVE_HIT MARK_FALSE_POSITIVE 全动作审计 payload 合法 JSON。拦截链接入 FeatureInvokeService 出站链 L142（Key 解密后、配额 check 前）：BANNED/INJECTION 两类 400 中文拦截语+命中词首尾脱敏（≤2 字保留首字加 *），SensitiveHitRecorder @Transactional(REQUIRES_NEW)、snippet 截断 500，保证 400 同时命中行独立提交。前端新增 utils/datetime.ts（UTC OffsetDateTime→浏览器本地时区，分钟/秒级两格式化，null/非法显 —），AI 中心 4 文件 7 处时间 slice 替换；A1SensitiveView 整页去 mock（4 KPI、命中 tab 词类三态过滤+分页+误报标注、词库 tab 抽屉 CRUD、重复词 400 保留），api/ai.ts 增 6 client。三轨真验：curl 401/重复词 400/changed=false/分页+stats/两类 400 脱敏/mark-fp 幂等；PG V19 迁移+触发器+审计 JSON 对账；Chrome KPI 2/2/2、15 词启用 13、过滤三态、本地时区显示。**口径说明**：历史盘点「A1 余 11 页」与本卡实测 nav 中 12 个 mock A1 视图存在 1 页口径差（后续按 12 视图切页，见 DELIVERY-P5-B46 §7），本卡为整页 ⬜→✅ +1 不受影响。EMBEDDING/VISION 多能力、多供应商适配、token 账单环比、marketing ddl-auto validate、告警事件持久化、全站时区推广（余约 21 处 slice）登记 Backlog，详见 DELIVERY-P5-B46。
> 上一批 B45：2026-09-13（B45 交付：**审批申请入口+联动启用+效果评估/A·B 实验实体化一卡，A1GovernView 页内纵深闭合不新增页面，82/166=49%、⬜82、域⑧ 8✅2🔧57⬜ 均不变**，勾销 §④ B45 Backlog）。后端 ai-service：新增 POST /api/ai/approvals apply 申请端点（类型白名单 PROVIDER/MODEL/BINDING、内容非空≤512、MODEL/BINDING 目标必须存在且未启用、同目标 PENDING 幂等 400 中文，权限 aiAdmin:edit 与决策 aiGovern:approve 分离），审批通过后端联动 MODEL→ai_model.enabled=t / BINDING→ai_feature_binding.enabled=t（审计 linkage=MODEL_ENABLED/BINDING_ENABLED）。V18 共享 Flyway 链两表（ai_eval_task 窗口 1~90 CHECK+指标 JSON 快照+结案结论；ai_experiment 对照/实验双分组+CHECK 强制对照≠实验+双侧指标快照+lift_pp+RUNNING/FINISHED），EvalService 八端点（evals/experiments 各 list/create/refresh/conclude + eval-stats），指标按 append-only ai_invoke_log **北京时间窗口**聚合调用量/成功率/P99/token/成本，lift=实验成功率−对照成功率 pp（HALF_UP 2 位），KPI 平均提升仅统计 FINISHED 且 lift 非空实验，全动作审计、结案冻结。前端新增 AiApplyDrawer 共享申请抽屉（模型行「上架申请」、绑定抽屉「提交审批」），A1Govern 效果评估/A·B 实验两 tab 从占位实体化。feat 提交 `f74d9c8`（17 文件 +1474/-36）。**真验修复一处 Vue 响应式缺陷**：KPI 卡片 kpis 原为 loadKpi 内 toKpis 一次性快照、实验结案只刷 evalStats 不重算致「平均效果提升」停留旧值（后端实回 0.00），拆 baseKpi ref+kpis computed 派生。三轨真验：Chrome 两类申请→审批通过→联动启用（benchmark/churn enabled=t）、评估 91 天拦截/7 天创建指标快照（5 调用/80%/54.09s/5284 tokens）/prompt 结案冻结、实验同模型拦截/异模型 -80 与 +80 两案/prompt 结案、KPI 均值正确显 0 pp；PG ai_approval #5#6 APPROVED、audit 452–460 含 linkage 且 AI 区段 79 条哈希链连续；curl 登录与模型价格置空 changed:true。业务转化/ROI 无埋点不伪造、前端 UTC→本地时区统一转 B46，详见 DELIVERY-P5-B45。
> 上一批 B44：2026-09-12（B44 交付：**AI 出站治理二期闭环一卡，纵深闭合不新增页面，82/166=49%、⬜82、域⑧ 8✅2🔧57⬜ 均不变**，勾销 §④ B44 Backlog，commit `336b7f8`/`ad0219a`）。V17 共享 Flyway 链三表（ai_quota 三级配额 FEATURE/MODEL/GLOBAL 日/月额度 NULL=不限+uk scope+target；ai_sensitive_word BANNED/INJECTION 两类 13 词种子；ai_alert_rule 五规则种子）；配额表无计数器列，用量以 append-only ai_invoke_log 按北京时间日/月窗实时 count 为唯一真相，MODEL 维度无 DB 行时 list 返回 quotaId=null 虚拟默认视图、首次 save 建行（跨供应商同 modelCode 合并计数）。拦截链接在 Key 解密后、prompt 组装前：**敏感词 screen（400 中文+命中词首尾脱敏回显，总开关 sensitive_check 缺省开）→ 配额 check（FEATURE→MODEL→GLOBAL 三级，429 中文「已用 X/Y 次」）**，被拦截请求不写 ai_invoke_log、不占用量、不产生费用；告警刻意无 @EnableScheduling，请求时按当日日志实时评估 LATENCY_P99/ERROR_RATE/CALL_COUNT/SUCCESS_RATE/QUOTA_WATERMARK 五指标（compare_op `>`/`<`，三态已停用/告警/正常，activeCount 联动网关 KPI activeAlerts）；写接口四件套（scope 白名单/额度非负/全字段相等 changed=false 短路不写冗余审计/AI_QUOTA 全动作审计 payload 合法 JSON，NULL 额度序列化空串）。前端：A1Admin 新增第 6 个「配额管理」tab（三级 8 行：六功能+模型+全局，日/月额度与实时用量、水位三档色、调整抽屉留空=不限），A1Gateway 告警 tab 五规则真实化。三轨真验：curl 敏感词两类 400 脱敏（「刷**利」「忽*****令」）、灰度 403 先于敏感词、配额临时置 0 触发 429 且拦截零落日志、恢复后真实 200（54736ms/2097 tokens）、水位 100% 告警 active；PG 三表+AI_QUOTA 审计 4 次对账；Chrome 网关告警红色 P99+KPI activeAlerts=1、管理页 6 tab/配额 8 行/调整抽屉回填全对。观察项：P99 阈值 200ms 对推理模型偏严（今日 1 条成功日志致持续 active，非缺陷，阈值分级登记治理）、告警事件持久化与真实通知下发留后续，详见 DELIVERY-P5-B44。同日 B43：六功能真实 invoke 出口闭环，81→82/166=49%（POST /api/ai/features/{code}/invoke 角色矩阵鉴权+真实火山方舟 ark-code-latest 调用+ai_invoke_log 成败全量沉淀+网关 KPI/日志真实化，详见 DELIVERY-P5-B43）。
> 同日 B42：2026-09-12（B42 交付：**后台 AI 中心模型配置地基一卡，A1 远期阶段正式开工，77→81/166=49%（⬜ 87→83），域⑧ 平台与基建 4✅→8✅、62⬜→58⬜**。用户拍板接入火山方舟（OpenAI Chat Completions 兼容：`https://ark.cn-beijing.volces.com/api/coding/v3` + 模型 `ark-code-latest`，密钥用户自持、页面录入、AES-GCM 加密落库），采纳「一期 Spring RestClient 薄适配层、零重型 AI SDK」推荐方案。**新建第 8 个后端服务 ai-service**（Spring Boot 3.2.5/Java17/端口 8089/包 com.meiyun.ai，reactor 纳管）：V15 共享 Flyway 链建 AI 平台七表（ai_provider 供应商含 api_key_cipher 密文+掩码、ai_model 模型含能力/价格/conn_status、ai_feature_binding 功能绑定+灰度、ai_feature_role 功能灰度矩阵、ai_invoke_log 调用日志、ai_approval 审批单、ai_global_cfg 全局配置单行）；LlmClient OpenAI 兼容薄适配（Bearer+/chat/completions、usage 三 token 解析、ping 连通性测试）；providers/models/features/cfg/logs/approvals 六大组真实端点，写接口四件套（校验/幂等/全动作审计/中文错误），ApiKeyCipher AES-GCM 主密钥 env AI_CONFIG_SECRET、密文与明文不进审计与日志、页面只回掩码与 hasApiKey；审批联动 MODEL 通过→模型 enabled=t、BINDING 驳回→linkage=NONE，重复决策 400 中文防护。**网关新增第 8 前缀 `/api/ai`**（router.go + AI_SERVICE_URL env），两份 compose 补 ai-service 服务块（app 8089/seed 18089 双栈双库，gateway depends_on healthy）。前端：新增 /ai/providers「模型接入」整页（供应商+模型 CRUD、真实连通性测试、Key 掩码录入）+ nav/router/auth 三处注册（aiAdmin:view/aiAdmin:edit、REGION_MGR 补 aiGovern:approve 权限码并同步 org-service PermissionMatrix 重签 JWT）；**A1Gateway/A1Admin/A1Govern 三页全部去 mock 接真**（KPI/调用日志、配置/功能角色矩阵/账单、审批列表/通过驳回决策）；修复 client.ts 双 axios 实例约定踩坑（api/client.ts 拦截器不剥离 data，api/ai.ts 19 端点统一 `.then(r=>r.data)`）。真验：网关 curl 全端点 + PG/审计哈希链对账 + 火山方舟假 Key 连通 FAIL 为预期（待用户录入真实 Key 转 SUCCESS）+ Chrome DevTools MCP 四页面浏览器真验（providers/gateway/admin/govern，console 零 error/warn；审批两行完整渲染含意见与 decidedBy）。占位数据（volc-ark 供应商/模型/6 绑定/2 已完结审批）保留不删，真实 Key 录入后连通即转 SUCCESS。后续 B43–B46 登记 Backlog（真实功能 invoke+调用日志、配额限流敏感词、审批申请 UI+效果评估/A·B 实体化、A1 其余 11 页+多供应商多能力+计费）。详见 DELIVERY-P5-B42）。
> 上一批 B41：2026-09-12（B41 交付：16 张营销属表 Flyway 纳管一卡，**纯基础设施/技术债，完成模块数不变 77/166=46%、🔧 2 不变**，勾销 B38 登记 Backlog「其余 16 张营销属表分批纳管」，commit `3166d97`。B38 接通四服务共享 Flyway 版本链时仅纳管 marketing_cfg/auto_grant_state 两表（V11），其余 16 表仍靠 ddl-auto=update（能加列不能加宽/补约束，B32/B37 已实证其运行态风险）。本卡按现网 `pg_dump --schema-only` 物理结构 1:1 转写（不用 JPA 实体理想态，保留中文枚举 CHECK、live_session 无时区 TIMESTAMP、push_record FK→customer+注释、TEXT、全部唯一约束与 4 索引），分三批：**V12** 券赠金条线 8 表（coupon_template/coupon_grant/campaign/coupon_writeoff_record/coupon_writeoff_chain/customer_grant/grant_rule/grant_deduction，B39 origin_biz_ref 新列随批收录）、**V13** 渠道素材违禁词 3 表（channel_returnback/marketing_asset/forbidden_word）、**V14** 海报短视频直播触达 5 表（poster_template/poster_record/short_video/live_session/push_record，PushSchemaInitializer 历史自愈的 push_type 扩宽/dedup_key 直接固化目标态）。一律 CREATE TABLE/INDEX IF NOT EXISTS，不对存量表 ALTER（缺列仍交 Hibernate update 净兜底）。双场景真验：①现网 backend/ 目录 mvn -pl marketing-service clean package（fat-jar unzip 实证含 V12~V14）→compose build→force-recreate，日志「Successfully applied 3 migrations … now at version v14」，共享链扩至 **V0–V14 共 15 行全 success**（rank13/14/15，33/11/12ms），16 表全部 NOTICE skipping、迁移前后真实 count 逐表对照零变更（writeoff 16 含 SST 11、push 10、channel_returnback 0 等；n_live_tup 为陈旧估计须以 count(*) 为准），服务 healthy；②全新库 scratch b41_fresh_scratch 裸回放 V1→V3（customer 历史 DROP+建 3 表）→V12（3 表跳过+5 表新建，实证 V3/V12 双服务版本链共存与执行顺序无关）→V13→V14 全路径：V14 首报 relation "customer" does not exist 为**预期非缺陷**（customer 表由 customer-service Hibernate 建、不在 V1 init，生产 compose 已固化 marketing depends_on customer healthy），补 customer_id VARCHAR16 PK stub 后重放 EXIT=0，16 表+FK+注释核对齐全后 DROP DATABASE。marketing 全 18 张属表达成 100% 纳管；ddl-auto update→validate 评估登记中期治理。详见 DELIVERY-P5-B41）。
> 上一批 B40：2026-09-12（B40 交付：存量 SST 脏数据全库确认与处置一卡，**纯数据治理/技术债，完成模块数不变 77/166=46%、🔧 2 不变**。全库字符/JSON 列扫描定性污染 **11 表 175 行 + audit_log 36 行取证快照**：门店运营域 8 表 **162 行**幽灵主数据（consumable 36/consumable_stock 36/consumable_movement 35/equipment 8/equipment_maintenance 11/treatment_room 9/treatment_bed 16/store_price 11，均 2026-09-06 19:11 system 播种、零外键零预约零订单引用），根因为 store-service 三个 ApplicationRunner 无 meiyun_seed 门控（StoreBomDataInitializer @Order50/StoreMasterDataInitializer @Order60/StorePriceCatalogDataInitializer @Order70，该服务默认数据源即 meiyun_core、无 Flyway、ddl-auto=update）——B36 只给 marketing 两个播种器加了门控，store 侧漏网。卡 `5ef75db` 按 CouponWriteoffDataInitializer 范式给三处补门控（注入 spring.datasource.url，不含 meiyun_seed 即早退；集团主数据——品牌 9/项目配方 9/品牌品类 SKU 目录/卡项目录 8——保持全栈播种不门控）。数据处置四决策（四问请示被 skip 后按铁律 -1 自主采纳推荐项）：①8 表 162 行幽灵数据删前 8 份 CSV + 全表 pg_dump 留档（scripts/backup-b40-sst-20260912/，backup-*/ gitignored 不入库）后单事务物理删除，audit_log 36 行 append-only 保留作风控证据；②11 条脏核销流水全部保留——8 条 0 元 FORGED/DUPLICATE 风控凭证不动，3 条 OK 共 **114000 分=1140 元**（WR20260908-000003 VIP85 552 元、WR-SEED-0001 WATER500 500 元、WR-SEED-0002 NEWBIE88 88 元）挂账等业务指定真实归属门店；③2 条海报资产 AS-SEED-002/009 保留本体，清掉 SST01~04 投放改 scope=ALL、store_codes=[]（原值 JSONL 留档）；④门控即修。真验连环踩坑后闭合：Dockerfile 仅 COPY 宿主预构建 fat-jar，首个镜像构建于 mvn package 之前致容器跑旧 jar，`restart` 不换镜像反触发旧播种器对空表重播 55 行（room9/bed16/equip8/maint11/price11）；修复链=mvn clean package（javap 解压 fat-jar BOOT-INF/classes 复核三类含 meiyun_seed，macOS strings 对 .class 假阴性不可用）→重建镜像→单事务二次删除 55 行→force-recreate，日志三条「非种子库…跳过」齐出、五表零重播、audit_log 保持 36 行、集团数据完好、服务 healthy。WR20260908 流水勘误：该前缀实 8 条，000001~000007 共 7 条脏（SST01）、000008 为 ST-SH-001 干净复验记录；资产幽灵码实为 SST01~04（B36 仅登 SST01），本批一并更正。详见 DELIVERY-P5-B40）。
> 上一批 B39：2026-09-12（B39 交付：退款终审联动赠金逆向回加一卡，**76→77/166=46%**，勾销 §⑤ 大表「退款回加赠金（消费满额规则的逆向链路）」Backlog，commit `5353bd9`。用户拍板三决策：①仅链路①付款回加——退款终审事务内同步逆向回加，远程失败整笔回滚（链路②满赠门槛追回、卡 gift_balance RF 不回补登记 Backlog）；②过期券回加但保持 EXPIRED；③部分退款按赠金→卡本金→法币级联。marketing 侧：`GrantService.refund()` 逆向 FIFO 回加（悲观锁 + `saveAll` 冲突重查），**RF 单号幂等**（uk_gd_ref_grant 唯一约束）+ **origin_biz_ref 原单锚点累计封顶**（sumRefundedByOriginGroupByGrant，超额锁前中文 422「原订单可回赠金不足：可回 X 元，本次需回加 Y 元」），USED→VALID 复活、EXPIRED 保态；grant_deduction 增 origin_biz_ref 列（随 B41 V12 纳管 Flyway）；`POST /api/marketing/internal/grants/refund`（X-Internal-Token）；GRANT_REFUND 审计 actor=system、payload operator=真实终审人。txn 侧：RefundSplit 三段（grantPart=min(原单 grant 实付,退款额)→balancePart 卡本金→cashPart 法币），与 outbox 资金分录同口径——**grant 段退款不产生任何 finance_event**；confirmRefund 终审事务内先 grantClient.refund→cardClient.refundForOrder 后置 REFUNDED/审计/outbox，远程全在置态前，失败回滚单据停留 PENDING_FINANCE，4xx 业务错误透传、5xx/IO 转 502「本笔操作已回滚」。单测 **13 例全绿**（marketing 8 + txn 5）。容器真验四组：①RF20260912-000001 全额混合退款（券复活/双锚流水/仅 1 条法币 RF-REFUND 事件/审计 grantRefund 60000）；②RF20260912-000002 纯赠金段退 50000（券复活、finance_event max(event_id) 零变化实证）；③RF 重放原样返回不双加 + 同 origin 超额 RF20260912-CAP1 422；④停 marketing 终审 502 四项零写入（单据 PENDING_FINANCE/无 REFUND 流水/无事件/仅 SUBMIT 审计），恢复 18s healthy 后重试得 422（故障窗口重试安全）。真验造数 RF...003 悬挂 PENDING_FINANCE 建议 reject 结案，已回加赠金是否冲正报用户拍板，详见 DELIVERY-P5-B39）。
> 上一批 B38：2026-09-12（B38 交付：Flyway 基线批，**纯基建还债，完成模块数不变 76/166=46%、🔧 2 不变**。此前 customer/finance 两服务已接 Flyway、共享 meiyun_core 单库一张 flyway_schema_history 全局版本链（V0=baseline，V1/V2/V3/V5/V9=customer，V4/V6/V7/V8=finance），audit/marketing 两服务仍零依赖零配置、域表全靠 ddl-auto=update——B37 的 txn_no 列宽缺陷（Hibernate update 不会加宽已有列，只能现网手动 ALTER）实证了缺迁移链的运行态风险。卡1 `6f2e734` audit-service 接入 Flyway：pom 增 flyway-core（BOM 管版，Community 9.22.3）、application.yml 同款共享链配置外**自主增开 out-of-order:true**（共享链补洞保险：防 V11 先于 V10 部署时高版本占位把 V10 卡死，启动 WARN「outOfOrder mode is active」为预期），新增 V10__audit_log_baseline.sql 以 `ALTER TABLE IF EXISTS ... ALTER COLUMN TYPE` 补登 biz_type 16→32（B32 手改）、txn_no 24→128（B37 手改）两次历史手动 ALTER（现网重设同宽 no-op；全新库 Flyway 先于 JPA、表不存在时 NOTICE skipping 留给 Hibernate 按实体 length 建），同时校准 db/schema.sql 基线 biz_type 16 漂移。卡2 `2b03c46` marketing-service 接入 Flyway：同款 pom/yml（无 out-of-order），新增 V11__marketing_cfg_baseline.sql 以 `CREATE TABLE IF NOT EXISTS` 纳管 marketing_cfg（15 列含 cfg_id PK default 1）+ auto_grant_state（5 列）两张 JPA 属表，现网 42P07 already exists skipping 数据无损；其余 16 张营销属表维持 JPA update、后续按域分批纳管（已登记 Backlog）。共享链实测扩至 **V0–V11 共 12 行全 success**，下一可用版本号 V12。四轨真验：①现网迁移 no-op 落 history（V10 19ms rank11、V11 18ms rank12）；②数据无损（audit_log 387 行、marketing_cfg 1 行 15 列、auto_grant_state 1 行）；③两服务各自重启与同时重启均 validated 12 migrations、up to date、healthy（实证 audit 类路径只持 V10 却能跨服务校验含 V11 的全链，*:future/*:missing 忽略生效）；④全新库探针 b38_fresh_probe 实测 V10 两条 IF EXISTS NOTICE skipping、V11 建出两表结构与现网 pg_dump 完全一致，验完 DROP DATABASE；audit/marketing mvn test 全绿。**踩坑记录**：backend/Dockerfile 只 COPY 宿主预构建 fat-jar、镜像内不跑 Maven，改 pom 后必须先 `mvn -pl <svc> package` 再 compose build（只 compile 会打包旧 jar 致迁移不执行，卡1 已踩已修）。db/schema.sql 三处校准（biz_type、marketing_cfg 全列+主键、auto_grant_state 缺失）在 gitignored 的 db/ 下不进 git；勾销 Backlog 两行（Flyway 版本化基线、biz_type 漂移），详见 DELIVERY-P5-B38）。
> 上一批 B37：2026-09-12（B37 交付：营销设置收口批两卡，**营销设置 🔧→✅，75→76/166=46%**。仪表盘 🔧 仍显示 2（实测对应 M1 集团/C 端移动端两个远期行），营销设置行自 B34 起游离于历史计数差，本次以 ✅+1 消解；卡2 为「充值赠金」既有 ✅ 行纵深闭合不加行，域⑤ 12✅0🔧6⬜ 数字不动。卡1 `828ac6d` 统一 marketing_cfg 读写 DTO 契约：新增 ConfigView 只读视图与 ConfigCmd 写命令各 11 字段完全对称，周频键统一 weeklyPushLimit（废弃写命令旧 weeklyLimit，前后端破坏性同卡同步），视图剔除 cfgId/referralArrivedReward/referralDealReward/commissionRate 四个多泄列，默认推送/广告渠道数组化，保留内部 get() 供核销兜底/频控/quota；quota `/push/quota` 出参 weeklyLimit 为独立前端契约刻意不动；前端 m5Settings.ts 全 12 键真实页面管理（违禁词另走 /marketing/forbidden-words），B37 前 append-only 旧审计快照 weeklyLimit 读时归一；容器真实 GET/POST/幂等/硬约束/落库/审计取证 + vue-tsc/vitest 50 绿（6 文件 +219/-38）。卡2 `7552665` 消费满额自动发赠金轮询 Job：AutoGrantJob 5 分钟轮询 txn `/internal/paid-orders`（复刻 B25 AutoPoints 拉取范式，**自主决策未走 FUND_ENTRY 事件链路**，与自动积分复用同一事件源），auto_grant_state 单行游标（NULL 首跑自 2000-01-01 UTC 全量回填、日粒度邻轮重叠窗口靠幂等去重），CONSUME_THRESHOLD 启用规则 priority 升序首条命中（一单不叠多档、门槛 >=、applicableStores 空白=全部门店否则精确匹配），双层幂等 idem_key=`RULE:{ruleId}:{orderNo}` + customer_grant.source_biz_ref 订单级跨规则锚点，故障分层（txn 不可用/客户域 502 整轮中止不推进游标、客户不存在 400 跳过推进、CARD_SALE/空客户/非正额跳过），GRANT_ISSUE/RULE 明细审计 + granted>0 补 AUTO-GRANT/SYSTEM 汇总（10 文件 +703/-1，14 例单测全绿）。**附修跨域运行态缺陷**：audit_log.txn_no VARCHAR(24) 装不下 RULE 锚点（实测 40 字符）致明细审计 PG 22001 insert 500、RestAuditRecorder 仅 log.error 不阻断主流程（赠金落账但审计静默丢失，B35 短手工号未触发）——AuditLog 实体 length 24→128 + 现网 ALTER TABLE + db/schema.sql 三处修复（audit-service ddl-auto=update 不会加宽列、无 Flyway）。真验：现存百万门槛规则首跑扫描 5 发放 0（零资金风险）；临时规则 GRT20260912-000002（满 50000 分赠 2000 限 ST-SH-001）全量回填**扫描 5 发放 4**（M001×2/M002/M003 命中、50000 分边界 >= 命中、M005/ST-SH-002 被门店限定排除、expire_at +3 月），双审计落库，三轮 scanned=1/granted=0 幂等零新增；验证后临时规则 DISABLED 行保留、4 笔测试赠金删除、audit_log append-only 凭证留存、auto_grant_state 存活。退款回加赠金逆向链路、marketing_cfg/auto_grant_state/audit_log 的 Flyway 版本化基线、audit_log.biz_type 基线 16 vs 32 漂移登记 Backlog，详见 DELIVERY-P5-B37）。
> 上一批 B36：2026-09-12（B36 交付：B34 技术债清尾批，四卡全部闭合，**纯技术债/测试债批次，完成模块数不变 75/166=45%、🔧 2 不变**。卡1 `8ddac05` SST01 幽灵门店码 5 处残留清零：`frontend/src/stores/storeContext.ts` 默认兜底常量改读真实门店表首家（路径更正：B34 登记误写为 `utils/storeContext.ts`，实际为 `stores/storeContext.ts`）、`CouponWriteoffDataInitializer` 与 `MarketingAssetDataInitializer` 两处 SST 种子加「仅 seed 库门店体系存在时播种」门控（core 库不再写入幽灵码）、`m5Core.ts` mock 值删除（-88 行中 -80 为 mock）。卡2 `3694003` `/internal/first` 首家门店排序口径契约化：StoreRepository 增 `findFirstByOrderByStoreCodeAsc`，端点从「无 ORDER BY 的 JPA 默认」改为 **store_code 字典序升序首家**的确定口径，javadoc/交付文档写明契约，StoreControllerTest 新增断言（多门店固定首家、空库 404/空、X-Internal-Token 鉴权）。卡3 `a43d299` StoreNameResolver 故障分层：新增 `StoreServiceUnavailableException`，重构为 `resolveNames`（宽松：吞故障降级空 Map，仅供券名回显/「不可用即放行」非阻断路径）+ `resolveNamesRequired`（严格：传输/HTTP 故障抛专用异常→写链路转 **503**，200 响应缺码=真不存在仍 **400**）双方法，私有 `fetchNames` 为唯一取数出口；MarketingCfgService 兜底门店保存校验改严格版——store-service 宕机时不再误报「兜底门店不存在 400」误导用户；另两处消费方（CouponWriteoffService 券名回显、MarketingAssetService 资产门店「不可用放行」）经逐个核实后零改动保留降级语义。卡4 `c0d3583` txn-service 测试基建从零建立（pom 增 starter-test），新增 PaymentServiceTest 7 例为 B35 赠金支付资金安全加护栏：散客 grant 400、同单第二笔 409、售卡单 grant 400（售卡禁令先于流水查询）、成功路径 deduct 先于 order_payment 落库的 InOrder 顺序断言、营销域 422/502 透传且事务不记账、grant+现金混合支付尾款不重复扣赠金。验证：marketing-service 全量 **93 测试全绿**、store-service 卡2 测试全绿、txn-service **7 测试全绿**；卡3 真实容器三分支取证（E014 超管 token：假码 ST-XX-999→400、真码 ST-BJ-001→200 changed=true、停 store-service→**503** 旧逻辑误报 400 的实证），验证后 store-service 已重启 healthy、writeoffFallbackStoreCode 已 API 复原 null。**存量污染事实更正**：WR20260908 核销流水实测为 **7 条（000001~000007）**而非 B34 登记的 4 条（000004~000007），另有 WR-SEED 4 条（9/1-9/3）与 marketing_asset SPECIFIED+SST 2 条，全部 append-only 不擅删、待业务确认，详见 Backlog 与 DELIVERY-P5-B36）。
> 上一批 B35：2026-09-12（B35 交付：充值赠金收口——闭合 §⑤ 大表「充值赠金 🔧」（B18 起）与 B26 明示 Backlog「赠金收银台抵扣（VALID→USED）」，营销赠金发放/扣减账本与收银台混合支付全链路贯通，commit `21d90df`。marketing-service 侧：新增 `grant_deduction` 扣减流水实体/仓储（change_type DEDUCT/REFUND、balance_after_fen、biz_ref 锚订单号、幂等键），CustomerGrant 增 USED 状态，`GrantService.deduct()` 实现 **FIFO 多笔赠金按到期先后带锁扣减**（悲观锁防并发超扣）、幂等键防重、超额中文 422「可用赠金不足」、写 GRANT_DEDUCT 合法 JSON 审计；新增 `InternalGrantController` 暴露 `/api/marketing/internal/grants/balance|deduct`（X-Internal-Token 服务间鉴权，不对外）。txn-service 侧：PaymentService 支付方式枚举增 `grant`——散客单无客户号中文 400、同一订单第二笔赠金支付中文 409（赠金仅限首笔且一单一次）、扣减失败透传营销域中文 message；M4FlowController 增 `GET /api/txn/order/{no}/grant-balance`（权限 **cashier:view** 而非 marketing:view——收银 FRONT_DESK 角色无营销权限，走交易域转发鉴权），散客/营销服务不可用**降级 `{available:false}` 不报错**；新增 MarketingGrantClient（RestTemplate + `@Value marketing.service.url` + internal token + 超时/不可达降级）。前端收银台 **template/style 零改动，仅 OrderView script 适配层 7 处**：api/order.ts 增 getOrderGrantBalance 与 GrantBalanceDTO；PAY_METHODS 增「营销赠金」磁贴（复用既有 finance 图标与选中态样式）；选中磁贴查余额并 toast「该客户可用营销赠金 ¥X，不足部分请用其他方式补收」；提交前两道拦截（零余额/超额中文报错）；**混合支付**——赠金不足部分微信/现金等补收，横幅已收待收、支付明细行、KPI 四卡、tab 计数全联动，已收款详情展示各支付方式中文名。基础设施附修：docker-compose.app.yml / seed.yml 补 `MARKETING_SERVICE_URL`（此前 txn→marketing 按默认 127.0.0.1 调用致 Connection refused，服务间 URL 漏配的运行态真实缺陷）。整体 74→**75/166=45%**（🔧 3→2；充值赠金大表域⑤ 已有独立行，本批为 🔧→✅ 状态跃迁**不加行**，域⑤ 11✅1🔧→12✅0🔧 **营销域 🔧 清零**，沿用历史计数差口径）。三轨验证全绿：marketing 模块 **84 测试全绿**；网关 curl 轨（M003 孙女士 OD20260820-000002 ¥1580：grant500+wxpay1080 混合收款、超额 422、散客 400、同单第二笔 409、幂等重放、余额归零 USED、grant_deduction/order_payment/audit_log 落库对账）；浏览器轨 E006 周收银（M001 陈女士 OD20260912-000001 ¥2000：网络 `GET /grant-balance [200] {balanceFen:60000,available:true}`→赠金 600 toast「部分收款成功，待收 ¥1,400」→微信 1400 toast「收款完成」，KPI 待收 1→0/已完成 3→4/今日已收 ¥2,880→¥4,880，落库 PM20260912-000003 grant 60000 + PM...004 wxpay 140000、grant_deduction DEDUCT 60000/余 0、审计 6 条 CREATE/CONFIRM/GRANT_DEDUCT(system)/PAY×2/FUND_ENTRY(system)）。**偏差如实披露**：①浏览器 TRAE-browseruse 扩展通道 Connection refused（os error 61），改用 Chrome DevTools MCP 完成全部浏览器操作；②take_screenshot 的 filePath 仍受 workspace roots 白名单限制无法落盘 docs/，截图以工具内目视 + 可访问性快照文本 + 网络 reqid 存档；③两笔已收款验证单据（OD20260912-000001/OD20260820-000002）与满赠规则 GRT20260912-000001 属真实业务写入，append-only 保留不删，已报用户拍板是否清理；消费满额**自动触发**发赠金（issueByRule 现无消费事件调用方，发赠金仅手工端点）与 txn-service 扣减侧单测覆盖登记 Backlog，详见 DELIVERY-P5-B35）。
> 上一批 B34：2026-09-12（技术债清偿两卡——闭合 §④ 大表「BOM 异常 🔧 后端 only 前端待接」，并修正 §⑤ 券核销行中如实记录的 SST01 幽灵门店码。卡A `73e4b7c` BOM 扣料异常缺料明细全链路贯通：store-service `ConsumableService` 抛 `InsufficientStockException` 携结构化 shortages（skuCode/skuName/needQty/stockQty/unit），`InternalBomController` @ExceptionHandler 转 **422 `{message,shortages[]}`**（选 422 而非 400 以区分「业务前置条件不满足」与「参数校验失败」）；txn-service `StoreConsumableClient.extractShortages` 解析回结构、`BomDeductService.registerFailure` 落 detail_json + 审计 payload 追加 shortageLines（合法 JSON）、`GET /api/txn/bom-exceptions` 透出；前端 InventoryView 渲染「热玛吉专用探头（HC-004）需 1个·现存 0个·缺 1个」，bom.ts 补 `await ctx.loadStores()` 防门店上下文未就绪带错门店码 404；**附修 1 项运行态真实缺陷**：`BomDeductExceptionRepository`/`ProjectBomRepository` 的 `substring(exc_id from N)` 起始位截错致**序号恒为 1**（BEX 前缀 12 字符应从 13 起），修复后 BEX20260911-000001→000002 正常递增。卡B `9a82af6` 券核销兜底门店配置化：`CouponWriteoffService` 硬编码 `FALLBACK_STORE_CODE="SST01"` 而 core 库无此门店（仅存在于 seed 库），致集团账号核销时流水 store_code 指向不存在的主数据、store_name 退化为编码本身，**历史脏数据 WR20260908-000004~000007 四条为实证**；改法为 marketing_cfg 新增 `writeoff_fallback_store_code`(varchar32，ddl-auto)、`MarketingCfgService.save()` 增门店存在性校验（假码中文 400「兜底门店不存在：xxx」、空白归一 null、归一值参与 changed 判定与审计 payload 故同值重复提交不写冗余审计）、`MarketingCfgDataInitializer` **刻意不回填**（core/seed 编码体系不同，任何播种值必有一侧对不上）、`currentStore()` 改三级链（登录门店→配置兜底门店→/internal/first 首家真实门店→**三级落空抛 503 拒绝核销**，不再静默写臆造编码）；前端 /m5-settings 审批流分组新增「券核销兜底门店」CSelect（选项来自 storeContext 真实门店，首项「未配置（自动取门店表首家）」），m5Settings store 六处同步保证 dirty diff 与审计卡一致，**`<style scoped>` diff 0 行**。整体 73→**74/166=45%**（🔧 4→3；BOM 异常大表已有独立行，本批为 🔧→✅ 状态跃迁不加行，卡B 为券核销/营销设置两行纵深，沿用历史计数差口径）。网关 curl 轨（422 含 shortages、假码 400、真码 changed:true、同值 changed:false 且 3 次 POST 仅 2 条审计、三级链分别落 ST-SH-002 上海浦东店/ST-SH-001 上海旗舰店/E005 本店）+浏览器轨（/m5-settings 保存 ST-SH-003 二次确认→saveDisabled 回 true→审计卡新增「核销兜底门店 未配置→ST-SH-003 蒋IT」→落库对账→再核销得 WR20260911-000004 上海虹桥店，**异常态 DUPLICATE 路径同样生效**）双轨全绿；配置项经 API 复原为 NULL（让复原也留审计），4 条核销流水属真实业务写入、4 条审计 append-only 均如实登记不删；**偏差如实披露：take_screenshot 的 filePath 受 workspace roots 白名单限制，docs/ 不在其中，本批无法产出截图，改用可访问性快照文本存档**；SST01 其余 5 处残留（storeContext 默认值/两处播种器/m5Core mock）与 GET/POST 字段名不对称（weeklyPushLimit vs weeklyLimit）登记 Backlog，详见 DELIVERY-P5-B34）。
> 上一批 B33：2026-09-11（组织树写能力整建两卡——闭合 §⑦ 大表「组织树 🔧 待接线收口」。卡A `c257165` 后端：OrgUnit 扩 leader_name/headcount/remark/inactive_reason 四列（ddl-auto），新增 OrgAdminController 三写端点全挂 org:manage——新建（**仅门店下建部门**，parentCode 非门店中文 400，orgCode 服务端 toUpperCase 归一+唯一校验，orgType 固定「部门」不收前端值，region/store_code 由父门店继承推导）、编辑（**缺省=保持/空串=清空/非空=更新** 三态语义，orgCode/orgType 传了也忽略，parentCode **仅部门可下发**、跨店移动联动改写 store_code+region）、启停（停用 reason trim 非空必填否则中文 400，**启用强制清空 inactive_reason**，同态幂等成功且**不重复写审计**）；DataScope 走 assertManageable，**越权返回中文 404 而非 403**（防编码枚举探测）；ORG 四 action（CREATE/UPDATE 含 toStore/DISABLE/ENABLE）合法 JSON 审计；GET /api/org/tree 补四富化字段，**该端点仅 org:view 且不做 DataScope**（组织目录全局可见为既有设计，隔离由写端点兜底）。卡B `079d4b9` 前端：T1OrgView 去 mock 接真实 API 并收窄写边界（树头「新建部门」、上级下拉仅列 6 门店、类型 disabled、编辑态编码/类型置灰、全程无状态字段、状态只走启停弹窗且「确认停用」按钮原因非空才解锁、启用弹窗无原因字段、后端中文 message 直出、提交期禁用防重复），m1Org store 重写 205 行（/tree 裸根 flatten、三命令 async 且成功后强制回源刷新不做乐观更新、仅部门下发 parentCode），**`<style scoped>` diff 0 行**。整体 72→**73/166=44%**（🔧 5→4；组织树大表已有独立行，本批为 🔧→✅ 状态跃迁不加行，三写端点与前端写边界收窄计入该行纵深，沿用历史计数差口径）。网关 curl 19 项断言（编码大写归一/重复编码 400/非门店父 400/三态编辑/跨店联动 region/停用缺因 400/启用清因/同态幂等审计不增/SE001 越权两条中文 404）+浏览器 6 组断言（写边界收窄/必填校验/小写 d-b33ui→D-B33UI/编辑置灰/跨店移动 SST01→SST06/停用带因→启用清因整块消失）双轨全绿；造数全量还原（删 2 部门+带 max(id) 保护谓词删链尾 ORG 审计 id79-88，精确回 org_unit=11/audit_log=36/max=36/ORG=0，verify `{ok:true,total:36}`），门店/大区/集团层级写能力与兼岗上级路由仍留 Backlog，详见 DELIVERY-P5-B33）。
> 上一批 B32：2026-09-11（客户搜索 outbox「DEAD 事件处置台」两卡——闭合 §④ Backlog「DEAD 事件处置台」。卡A `f02384b` 后端：customer_search_event 扩 DISCARDED 终态+resolve_note/resolved_by/resolved_at（ddl-auto），新增 CustomerSearchEventAdminService 三动作状态机——retry（仅 DEAD→PENDING、rc 清零/last_error 清空、PENDING 幂等，SENT/DISCARDED 中文 400）、replay（PENDING/DEAD 当场同步投递 ES，ES 不可用保持 PENDING 走 RETRY）、discard（note trim≥2 字+处置三字段）；GET /api/customer/search-events（status/customerId/eventId 过滤+固定 event_id 倒序真分页）+/stats 四键；reindex 换权 customer:search:admin；CUSTOMER_SEARCH_EVENT 四 action（MANUAL_RETRY/REPLAY/DISCARD/REINDEX）合法 JSON 审计；权限码仅挂 REGION_MGR；附修 GlobalExceptionHandler 漏接 ResponseStatusException 致中文 400/404 被兜 500、audit_log.biz_type varchar(16)→(32)。卡B `dbf8158` 前端：/search-events 处置台整页（KPI 四宫格点击筛选/筛选条事件ID 正整数校验/左表右详情双栏 1108px+380px、选中粉行/last_error 红块/三按钮按状态机禁用/丢弃 modal 行内校验/写后 list+stats 两连刷新/全量重建入口/v-perm.disable），nav 客情洞察组+路由守卫，SE001 无权限菜单隐藏+直访跳 /no-auth。整体 70→**72/166=43%**（仪表盘净增 2：后端处置闭环+处置台整页 ⬜→✅；大表域②新增「检索事件处置台」1 行 7✅→8✅，reindex/三动作/审计为既有「客情登记（B30 实时写 ES）」行纵深增强只改备注，沿用历史计数差口径）。网关 curl 全矩阵（400 中文/404/401/SE001 403/reindex=100/审计合法 JSON）+浏览器双轨取证（中继 Job 活行为：PENDING 经 RETRY rc 封顶 20 转 DEAD；network 23 个全 200、console 零 error/warn、双截图）；造数外科手术式还原（删 4 事件+setval 归 1+按 biz_type 删 8 行测试审计 id42-49，他类审计完好 max=36），ES reindex 对账治理（ES110 vs PG100 漂移 10 文档）仍留 Backlog，详见 DELIVERY-P5-B32）。
> 上一批 B31：2026-09-11（术后随访 SOP 引擎收口两卡——闭合 B30 明示 Backlog「/followup 与 /sop 整页管理 UI、手工建随访端点」。卡A：POST /api/txn/followup 手工建普通随访（门店强制取 JWT、客户经 customer-service 解析不存在中文 400、planDate≥serviceDate、method 白名单默认 PHONE、sopStage=MANUAL、FOLLOWUP/CREATE 审计）+stats 九键（sopPending/sopOverdue 旧两键不动+pending/todayPending/overdue/done/skipped/avgSatisfaction/adverseCount）+keyword 三列 OR LIKE 真分页；随访工作台 FollowupView 整页接真（CPagination/350ms 防抖/三 tab 角标/四 KPI/超期 warnbar/真实客户检索新建弹层/完成·免回访写动作/v-perm）；流水牌待回访列 sopTodos 走真库、schedulePostOpSop 降 async 空 shim 防双排程；C 端「陈美玲」演示种子保留。卡B：SOP 编排九端点（模板节点增改/启停/删/恢复默认五动作 SOP_TEMPLATE 审计，内置节点可停用可改不可删中文 400、停用不参与新批次历史批次不变；批次分页聚合+summary 五键+一键升级 `{"escalated":N}`），FollowupSopEscalator 抽取供 60s Job 与手动共用（补 escalate 后 save 防标记丢失），FollowupScheduler 改读启用节点；SopManagementView 双 tab（批次执行看板四 KPI/warnbar 一键升级↔infobar 已升级态/节点徽标/模板 checkbox 编排/恢复默认）。整体 68→**70/166=42%**（两整页管理 UI 大表新增 2 行，域③ 6✅→8✅ 仅剩复诊召回远期；端点/九键/五动作为既有 SOP 行纵深增强不加行）。附修 **1 项运行态真实缺陷**：d130dfc（空库 AVG 聚合 getResultStream().findFirst() 遇 null NPE 致 /stats 500→getSingleResult）。网关 curl 全路径+浏览器两页双轨取证（一键升级 warnbar→infobar、console 仅 1 a11y 零 error/warn）；冒烟 3 批 12 随访+模板五动作全量还原（setup-seed-db.sh 全量重建，**如实披露脚本 TRUNCATE 含 audit_log、终态 36 行 CONSUMABLE 播种基线**，节点恢复 id1-4、followup/batch=0、SOP-ESC 通知=0），详见 DELIVERY-P5-B31）。
> 上一批 B30：2026-09-11，近线收口四卡——③ 复购提醒前端接线（RepurchaseView 全链路：客户真实检索/目标项目必填+知情同意硬勾选/待签核统计实时/客户·经办·店长三方签核，🔧→✅）；④ EMR 模板库（emr_template 新域+集团 4 种子 EMT-SEED-001..004+本店建/停用+模板库弹层）+框架异常中文 400+列表真分页/统计后端聚合；② 建档实时写 ES（customer_search_event outbox+10s 中继 Job SENT/RETRY/DEAD+连续失败 3 次熔断 30s 降级 DB+读时合并+显式 mapping）；① 术后随访 SOP 引擎（followup_sop_template/node/batch/followup 四表+treat-done AFTER_COMMIT 幂等排程 1/3/7/30 天节点+60s 超期升级 Job 店长幂等通知免打扰+/followup 端点+工作台两演示项切真摘除）。整体 64→**68/166=41%**（复购为 🔧→✅，SOP/实时写 ES/EMR 模板库为 Backlog 闭合 ⬜→✅）。验证中附修 **2 项运行态真实缺陷**：fa549d5（afterCommit 无活动事务致 SOP 排程静默丢写→REQUIRES_NEW 物理事务）、fc97d00（模板库裸 null 参数致 PG 42P18 查询 500→JPQL cast string）。网关 curl 全路径（含事件 SENT/7 次 RETRY 自愈）+浏览器复购 RP20260911-003194 创建→三方签核与模板库 UI 双轨取证（SE002 咨询师 SELF 数据域隔离正向实证），console 零错；audit id 81–93 共 13 行 append-only 保留；冒烟全量还原（PG 当日造数零残留、customer 基线 100、ES 113→110 三漂移槽位如实记录），详见 DELIVERY-P5-B30）。
> 上一批 B29：预约接待尾工批三卡——① 排队智能候补（arrival_waitlist 新域：WL 单号/手机号锚定会员·散客快照两态、WAITING→NOTIFIED→FULFILLED/CANCELLED 状态机、号源释放同事务 FIFO 递补首位+店长站内信幂等、/queue 第三卡+候补登记弹层+智能候补 KPI）；② 候诊超时自动释放号源（ArrivalAutoReleaseJob 60s 扫描「30min 阈值+10min 宽限」、系统释放 LEFT 同事务递补、手工释放按钮接真、重载状态防重入）；③ 到店核销↔预约/划扣自动勾连（APPOINTMENT 手机号命中当日预约，同事务回写 appt_no/wd_no+预约置已到店+自动建到店号+writeoff_desk_task PENDING，核销详情透出勾连单号）。整体 63→64/166=39%，网关 curl 24 项全绿+浏览器两页取证（audit 314–331），冒烟全量还原，详见 DELIVERY-P5-B29。
> 上一批 B28：预约接待收口批三卡——会员到店核销 /m2-checkin（checkin_record 新域、CI 单号、三方式/三态/四异常码、timeline、手机号掩码）、客情登记十类扩展字段后端化（customer 加 9 列、白名单+过敏互斥、360 有值才显示）、triage 改派时间线（triage_reassign 新表）。62→63/166=38%，详见 DELIVERY-P5-B28。
> 上一批 B27：主轴「接待台/候诊分诊 + EMR 电子病历独立域」——arrival/triage/emr_record 三新表、到店五态队列、分诊建 CP 草稿双向回挂、预约签到自动到店、EMR DRAFT→SIGNED→ARCHIVED→修订全状态机+signEmr/treatDone 联动；工作台两演示项摘除（附修 2 项运行态缺陷），详见 DELIVERY-P5-B27。
> 再上一批 B26：通知多渠道（Inbox/SMS/企微/邮件+60s 扇出退避/死信+SSE+免打扰）+赠金高级规则+外部渠道回传骨架，57→60/166=36%，详见 DELIVERY-P5-B26。

---

## ① 总览仪表盘

### 全量口径（页面 / 功能模块粒度，去重约 166 项）

| 状态 | 数量 | 占比 | 说明 |
|---|---|---|---|
| ✅ 已完成 | **84** | 约51% | 三者齐备，真实闭环 |
| 🔧 半成品 | **2** | 1% | 后端有端点/前后端均实但缺验证或前端 mock |
| ⬜ 未开始 | **80** | 约48% | mock/占位，含大量远期独立阶段页面 |
| **合计** | **166** | 100% | |

> 注：P5-B24「两线合并全收」5 卡收口（财务四页去 mock/发票 CSV、卡余额时间线/核销双签投影、咨询草稿/接诊、treat-start/treat-done 治疗全链路、我的工作台计数真实化），页面口径 47→**54/166=33%**。工作台 7 项计数切真（审批/待收款/今日预约/方案单四态），5 项无后端域（候诊接待/病历草稿/术后回访/SOP 超期/复诊提醒）保留演示数据并显式标注「（演示）」、不计入真实总数；方案单状态机 PENDING→ACTIVE→PENDING_REVIEW→APPROVED→READY_PAY→PAID→TREATING→DONE 全链路双栈真实打通。EMR 独立域、术后随访 SOP、沉睡唤醒、通用异常中心、异常账务登记、预收合规监控、进项税抵扣、M4Repurchase 三方双签列 Backlog。详见 DELIVERY-P5-B24。
> 口径备注：大表逐行加总与仪表盘合计存在历史计数差（域④ 大表 ✅ 行粒度多计 1、财务/咨询行合并粒度不同），不影响真实交付结论，后续盘点统一校准。B30 仪表盘净增 4 项：复购提醒 🔧→✅，术后随访 SOP/建档实时写 ES/EMR 模板库三项 Backlog ⬜→✅；大表仅术后随访 SOP 新增 1 行（域③ 4✅1🔧1⬜→6✅0🔧1⬜），实时写 ES 与 EMR 模板库/分页/400 作为既有 ✅ 行（客情登记/客户档案、EMR）的纵深增强只改备注不加行，沿用上述历史计数差口径。B31 仪表盘净增 2 项：闭合 B30 明示 Backlog 的两个整页管理 UI——随访工作台（/followup）、SOP 编排（/sop）⬜→✅，域③大表新增 2 行（6✅→8✅）；手工建随访端点、stats 九键、模板节点五动作/批次聚合/一键升级作为既有「术后随访 SOP」行纵深增强只改备注不加行。B32 仪表盘净增 2 项：DEAD 事件处置后端能力（三动作状态机+五端点+四 action 审计）与检索事件处置台整页（/search-events）⬜→✅；大表仅域②新增「检索事件处置台」1 行（7✅→8✅，转化漏斗仍 ⬜），reindex 换权/三动作/审计作为既有「客情登记（B30 建档实时写 ES）」行纵深增强只改备注不加行，沿用历史计数差口径。B33 仪表盘净增 1 项：**组织树 🔧→✅**（后端三写端点+前端去 mock 收窄写边界，🔧 5→4）；组织树在大表域⑦ 已有独立 1 行，本批为状态跃迁**不新增行**，三写端点与写边界收窄均计入该行纵深，沿用历史计数差口径。B34 仪表盘净增 1 项：**BOM 异常 🔧→✅**（422 缺料明细结构化贯通至页面，🔧 4→3）；BOM 异常在大表域④ 已有独立 1 行，本批为状态跃迁**不新增行**；卡B 券核销兜底门店配置化作为既有「券核销 ✅」与「营销设置 🔧」两行的纵深增强只改备注不加行，沿用历史计数差口径。B35 仪表盘净增 1 项：**充值赠金 🔧→✅**（🔧 3→2）；充值赠金在大表域⑤ 已有独立 1 行，本批为状态跃迁**不新增行**，域⑤ 表头 11✅1🔧→12✅0🔧；大表「营销设置」行文字仍保留 🔧 标记，但其纵深自 B34 起按历史计数差口径计入券核销行、不计入域⑤ 表头 🔧 列，仪表盘剩余 2🔧 为营销设置（将随 B37 营销规则引擎收口）与 M1/移动端远期页，沿用历史计数差口径。B36 为 **B34 技术债清尾纯还债批**（SST01 残留清零/`/internal/first` 排序契约/StoreNameResolver 故障 503-400 分层/txn 测试基建+grant 护栏 7 例），仪表盘数字 **75/166=45%、🔧 2 均不变**，仅勾销 §④ Backlog 4 行并更正存量污染登记，沿用历史计数差口径。B37 仪表盘净增 1 项：**营销设置 🔧→✅（75→76/166=46%）**——该行自 B34 起游离于仪表盘 🔧 计数差中（见 B35 备注），卡1 统一 marketing_cfg 读写 DTO 契约（ConfigView/ConfigCmd 对称、weeklyPushLimit 破坏性改名、全 12 键真实页面+端点管理、容器取证）且卡2 落地 CONSUME_THRESHOLD 消费满额自动发赠金规则引擎（AutoGrantJob 轮询+双层幂等，扫描 5 发放 4/二轮零新增真验），「随营销规则引擎整体收口」条件满足正式勾销；卡2 本身为「充值赠金」既有 ✅ 行的纵深闭合**不加行**。仪表盘 🔧 仍显示 2（M1 集团/C 端移动端两个远期行），营销设置的勾销经 ✅ +1 体现，域表 🔧 列维持历史计数差口径不动，详见 DELIVERY-P5-B37。B38 为 **Flyway 基线纯基建还债批**（audit/marketing 两服务接入全库共享 Flyway 版本链，V10 补登 audit_log 两次历史手动 ALTER、V11 纳管 marketing_cfg/auto_grant_state，校准 db/schema.sql 三处漂移），仪表盘数字 **76/166=46%、🔧 2 均不变**，仅勾销 §④ Backlog 两行（Flyway 版本化基线、biz_type 漂移）并新增「其余 16 张营销属表分批纳管」一行，沿用历史计数差口径，详见 DELIVERY-P5-B38。B39 仪表盘净增 1 项：**退款回加赠金 Backlog ⬜→✅（76→77/166=46%，⬜ 88→87）**——勾销 §⑤ 大表 B37 登记的「退款回加赠金（消费满额规则的逆向链路）」，域⑤ 12✅6⬜→13✅5⬜；终审事务内同步逆向回加/远程失败整笔回滚、RefundSplit 三段分摊、RF 幂等+原单累计封顶、13 单测+四组容器真验均为该行纵深闭合内容不新增行，链路②满赠门槛追回、卡 gift_balance RF 不回补仍留 Backlog，沿用历史计数差口径，详见 DELIVERY-P5-B39。B40 为 **存量 SST 脏数据确认与处置纯数据治理/技术债批**（全库扫描定性 11 表 175 行+audit 36 行快照：8 表 162 行门店幽灵主数据 CSV/pg_dump 留档后单事务物理删除、3 个 store-service 播种器补 meiyun_seed 门控 `5ef75db` 并经重建镜像 force-recreate 实证零重播、2 条海报资产改 scope=ALL、11 条脏核销流水保留其中 3 条 1140 元挂账待业务指定门店），仪表盘数字 **77/166=46%、🔧 2 均不变**，仅更新 §④ Backlog「存量脏数据」行，沿用历史计数差口径，详见 DELIVERY-P5-B40。B41 为 **16 张营销属表 Flyway 纳管纯基建还债批**（V12 券赠金域 8 表含 B39 origin_biz_ref、V13 渠道素材违禁词 3 表、V14 海报短视频直播触达 5 表含 push_record FK→customer；按 pg_dump 现网物理结构 1:1 转写、CREATE TABLE IF NOT EXISTS 现网全 skipping 零数据变更，`3166d97`；共享链扩至 V0–V14 共 15 行全 success，scratch 全新库 V1→V3→V12→V13→V14 全路径回放通过），仪表盘数字 **77/166=46%、🔧 2 均不变**，勾销 B38 新增的「其余 16 张营销属表分批纳管」Backlog 行，marketing 全 18 张属表 100% 纳管，沿用历史计数差口径，详见 DELIVERY-P5-B41。B42 仪表盘净增 4 项：**A1 AI 中心正式开工，模型配置地基 4 页 ⬜→✅（77→81/166=49%，⬜ 87→83）**——新增 /ai/providers 模型接入整页（供应商+模型 CRUD、AES-GCM 密钥、真实连通性测试），既有 A1Gateway/A1Admin/A1Govern 三页由 mock 去真（KPI/调用日志、配置/灰度矩阵/账单、审批列表/通过驳回）；后端为新建 ai-service 第 8 服务（V15 七表、OpenAI 兼容薄适配、/api/ai 网关第 8 前缀、六大组端点四件套），域⑧ 大表原「A1 AI 中心（15 页）⬜」单行细化为 4✅+余 11 页⬜，域⑧ 4✅2🔧62⬜→8✅2🔧58⬜，详见 DELIVERY-P5-B42。B43 仪表盘净增 1 项：**六功能真实 invoke 出口闭环，AI 网关监控 ⬜ 口径纵深勾销（81→82/166=49%，⬜ 83→82）**——新增 POST /api/ai/features/{code}/invoke（六功能角色矩阵鉴权+门店域校验+模型 enabled/CHAT 能力校验+Key 解密+参数覆盖，LlmClient.chat 真实火山方舟 ARK 调用），成功/失败均沉淀 ai_invoke_log（snippet/token 三量/cost_fen/error_code），网关页 KPI/API 目录/调用日志全部真实联动；无新页面，属 B42 四个 ✅ 页面的纵深闭合，域⑧ 大表不新增行只更新既有行备注（沿用历史计数差口径）；真验过程修复四项运行态缺陷（V16 error_code 扩 VARCHAR(512)、invoke 去方法级事务保证出站失败日志独立提交、AiFeatureBinding.updated_at 去掉 insertable=false 改为显式刷新、bindRows 展示字段改名 storeScopeText 消除对原始枚举的污染），观察项：ark-code-latest 为推理模型不完全遵守 max_tokens、后端 OffsetDateTime 序列化为 UTC 前端未转本地时区，两项登记 B46 打磨，详见 DELIVERY-P5-B43。B44 为 **AI 出站治理二期纵深闭合纯治理批**（V17 三表：ai_quota 三级配额 429 中文+ai_sensitive_word 敏感词/越权 400 脱敏+ai_alert_rule 告警五规则请求时实时评估；拦截链 灰度403→敏感词400→配额429，被拦截不落 ai_invoke_log 不占用量；配额无计数器列、用量按 append-only 日志北京时间日/月窗实时 count，MODEL 维度虚拟默认视图首次 save 建行；A1Admin 配额管理 tab+A1Gateway 告警 tab 真实化），配额管理与告警均为既有 4 个 ✅ 页面内 tab，**不新增页面，82/166=49%、⬜ 82、域⑧ 8✅2🔧57⬜ 均不变**，仅勾销 §④ B44 Backlog，沿用历史计数差口径，详见 DELIVERY-P5-B44。B45 为 **审批申请入口+效果评估/A·B 实验实体化纯纵深闭合批**（POST /api/ai/approvals apply 申请端点+通过联动 MODEL/BINDING enabled=t，V18 ai_eval_task/ai_experiment 两表三 CHECK，EvalService 八端点按 append-only ai_invoke_log 北京时间窗口聚合指标、lift pp 与已结案均值，AiApplyDrawer 共享申请抽屉+A1Govern 两 tab 实体化；真验修复 KPI kpis 一次性快照不刷新的 Vue 响应式缺陷——拆 baseKpi ref+kpis computed），申请抽屉与评估/实验均为既有 4 个 ✅ 页面内入口/tab，**不新增页面，82/166=49%、⬜ 82、域⑧ 8✅2🔧57⬜ 均不变**，仅勾销 §④ B45 Backlog，沿用历史计数差口径，详见 DELIVERY-P5-B45。B46 卡1 仪表盘净增 1 项：**敏感词检测整页 ⬜→✅（82→83/166=50%，⬜ 82→81）**——V19 新建 ai_sensitive_hit 命中表（CHECK+三索引）+ai_sensitive_word 补 updated_at+8 张 AI 表 UPDATE 触发器，SensitiveWordController 六端点（词库全量/新建/编辑、命中分页+词类过滤/统计/误报标注，重复词 400 中文、changed=false 与重复标注幂等不写审计），出站拦截链 L142 接入（Key 解密后/配额前，BANNED/INJECTION 两类 400 脱敏，SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库）；前端 utils/datetime.ts 时区统一工具落地（AI 中心 4 文件 7 点位替换），A1SensitiveView 整页去 mock。大表域⑧ 新增「A1 · 敏感词检测」独立 1 行（8✅→**9✅**、57⬜→**56⬜**），时区工具/updated_at 触发器为既有模块纵深增强不加行。**页数口径差说明**：B42 起按「15 页中已交付 4 页、余 11 页」盘点，本卡实测 nav 中 mock A1 视图为 12 个（11 vs 12 差 1，差源为视图与页面粒度不同），后续余页去 mock 按实测 12 视图逐一切页，本卡整页 +1 不受影响，沿用历史计数差口径，详见 DELIVERY-P5-B46 §7。B46 卡2 仪表盘净增 1 项：**AI 智能中心首页整页 ⬜→✅（83→84/166=约51%，⬜ 81→80）**——纯前端切真无新端点，A1HomeView 四 KPI（今日 AI 调用/敏感词拦截/今日成功率/待审批）与能力卡调用量、待办告警流按 aiGateway:view、aiAdmin:view、aiGovern:view 三类权限分组门控接真，无日志功能诚实隐藏；待审批数改用审批分页 totalElements 消除对网关端点的越权依赖，datetime.ts 补 fmtAgo。大表域⑧ 新增「A1 · 智能中心首页」独立 1 行（9✅→**10✅**、56⬜→**55⬜**），A1 页数 5/15→**6/15**（实测 nav 中 mock 视图 12→10）。本卡不涉及时区工具/触发器等基础设施，沿用历史计数差口径，详见 DELIVERY-P5-B46-2。

### 两个口径，避免「完成度错觉」

- **全量页面口径：约51%**——分母含 M1 集团 / M2 门店运营 / M3 客户运营 / A1 AI 中心 / T2 数据 / T4 算力 / C 端移动端等**远期独立阶段**约 **80 个页面**（占 ⬜ 的大头；B42 起 A1 AI 中心已开工，B43 六功能真实 invoke 出口与调用日志沉淀、B44 配额限流/敏感词告警出站治理、B45 审批申请+联动启用与效果评估/A·B 实验实体化均已纵深闭合，B46 卡1 敏感词检测整页去 mock、卡2 智能中心首页整页去 mock 后 6/15 页真实闭环）。这些是与当前门店中台主线并行的后续大阶段，不应算作「主线欠债」。
- **门店中台核心主线口径（交易 + 财务 + 组织权限 + 门店主数据 + 营销 + 客户运营基础 + 诊疗主轴）**：**核心闭环已打通** ✅。退款/退卡/划扣/审批/卡项/资金/对账/月结/成本/提成/权限/主数据链路全部真实落地，**营销 9 页与客户域 4 卡均收口闭合**（券/推送/核销/ROI、积分商城/积分账户/标签/等级真实闭环）；B24 把**咨询→方案→支付→治疗→归档诊疗主轴全链路贯通**，B27 再补齐主轴两端的**接待台/候诊分诊**（到店→分诊→同事务建方案草稿→叫号→完成，预约签到自动到店）与 **EMR 电子病历独立域**（DRAFT→SIGNED→ARCHIVED→修订全状态机 + signEmr/treatDone 病历联动），B28 收口**到店核销 /m2-checkin** 真实闭环，B29 再补**排队智能候补 + 候诊超时自动释放 + 核销↔预约/划扣勾连**，预约与接待域 8✅（仅剩转化漏斗依赖数据分析）；B30 **近线收口四卡**（复购提醒前端接线 🔧→✅、EMR 模板库/真分页/中文 400、建档实时写 ES、术后随访 SOP 引擎），B31 **术后随访 SOP 引擎收口两卡**（随访工作台 /followup 整页接真+手工建随访端点+stats 九键、SOP 编排 /sop 整页+模板节点五动作+批次看板+一键升级，B30 明示 Backlog 两项全勾销），咨询诊疗域 8✅（仅剩复诊召回远期），工作台再摘除「术后回访/SOP 超期」两演示项（5 个演示项已摘除 4 个，仅剩复诊提醒）；B32 **客户搜索 DEAD 事件处置台两卡**（retry/replay/discard 三动作状态机+/search-events 处置台整页+全量重建入口，customer:search:admin 仅授 REGION_MGR，B30 outbox 遗留 DEAD 处置 Backlog 勾销），预约接待域 8✅（仅剩转化漏斗依赖数据分析）；B33 **组织树写能力整建两卡**（后端三写端点：仅门店下建部门/编辑三态语义/部门跨店移动联动 region/启停带停用原因，越权中文 404 防枚举探测；前端 T1OrgView 去 mock 并收窄写边界，组织树 🔧→✅），组织权限域仅剩兼岗上级路由与门店/大区层级写能力留 Backlog；B34 **技术债清偿两卡**（BOM 扣料异常缺料明细 422 结构化贯通至页面 🔧→✅ 并附修 exc_id 序号恒为 1；券核销兜底门店配置化，消灭 core 库不存在的硬编码 SST01 幽灵门店码，改为「登录门店→营销设置配置项→门店表首家→落空抛 503」三级链），交易域 🔧 清零；B35 **充值赠金收口一卡**（grant_deduction 扣减流水+FIFO 带锁扣减/幂等/超额 422/GRANT_DEDUCT 审计，marketing internal 端点 X-Internal-Token，txn 转发查余额 cashier:view 与 grant 支付散客 400/同单第二笔 409，收银台「营销赠金」磁贴+混合支付，template/style 零改动，附修两份 compose 漏配 MARKETING_SERVICE_URL），**营销域 🔧 清零**；B36 **B34 技术债清尾批四卡**（SST01 幽灵门店码 5 处残留清零并加播种器 seed 门控、`/internal/first` 兜底门店 store_code 升序首家契约化+断言测试、StoreNameResolver 远程故障 503 与真不存在 400 严格分层、txn-service 测试基建从零建立并补赠金支付资金安全 7 例护栏），纯还债不增完成模块；B37 **营销设置收口两卡**（卡1 marketing_cfg 读写 DTO 契约统一 ConfigView/ConfigCmd 对称、周频键 weeklyPushLimit 破坏性改名、全 12 键真实页面+端点管理；卡2 消费满额 CONSUME_THRESHOLD 自动发赠金轮询 Job，5 分钟拉 txn 已支付订单+auto_grant_state 游标全量回填+双层幂等，扫描 5 发放 4 真验，附修 audit_log.txn_no 24→128 列宽致审计静默丢失缺陷），**营销设置 🔧→✅**，充值赠金逆向退款回加留交易-营销联动专项；B38 **Flyway 基线批**（audit/marketing 两服务接入全库共享 Flyway 版本链：V10 以 ALTER TABLE IF EXISTS 补登 audit_log biz_type 16→32/txn_no 24→128 两次历史手动 ALTER，V11 以 CREATE TABLE IF NOT EXISTS 纳管 marketing_cfg/auto_grant_state 两表，audit 自主开 out-of-order:true 作共享链补洞保险，四服务 Flyway 通道齐备，共享链 V0–V11 共 12 行全 success，全新库探针结构与现网一致），纯基建还债不增完成模块；B39 **退款终审联动赠金逆向回加一卡**（用户三决策：仅链路①付款回加/过期券保态回加/赠金→本金→法币级联；marketing 逆向 FIFO 回加、RF 幂等+origin_biz_ref 原单累计封顶 422、USED→VALID 复活/EXPIRED 保态，txn RefundSplit 三段与 outbox 同口径、grant 段零 finance_event、终审事务内先远程后置态失败整笔回滚 502，`5353bd9`，13 单测+四组容器真验），**76→77/166=46%**，链路②满赠门槛追回、卡 gift_balance RF 不回补留 Backlog；B40 **存量 SST 脏数据全库确认与处置一卡**（全库扫描 11 表 175 行：8 表 162 行门店幽灵主数据留档后物理删除、3 个漏网 store-service 播种器补 meiyun_seed 门控 `5ef75db` 并实证零重播、2 海报资产 scope 转 ALL、11 脏核销保留其中 1140 元 3 单挂账待业务定门店），纯数据治理不增完成模块；B41 **16 张营销属表 Flyway 纳管一卡**（V12 券赠金域 8 表/V13 渠道素材违禁词 3 表/V14 海报短视频直播触达 5 表，按 pg_dump 现网物理结构 1:1 基线、CREATE TABLE IF NOT EXISTS 现网全 skipping 零数据变更、scratch 全新库全路径回放，共享链扩至 V0–V14 共 15 行全 success，`3166d97`），marketing 全 18 张属表 100% 纳管、纯基建不增完成模块；B42 **后台 AI 中心模型配置地基一卡**（用户拍板火山方舟 OpenAI 兼容接入：新建 ai-service 第 8 服务 8089/V15 七表 Flyway/LlmClient RestClient 薄适配零重型 SDK/网关第 8 前缀 /api/ai/六大组端点四件套/ApiKeyCipher AES-GCM 密钥不落审计日志，前端新增模型接入页 + 网关监控/管理配置/审批治理三页去 mock 共 4 页，REGION_MGR 补 aiGovern:approve 并同步 org-service 重签 JWT，四页面浏览器真验零 console 报错，火山真实 Key 待用户录入转 SUCCESS），**77→81/166=49%、⬜ 87→83、域⑧ 4✅→8✅**，A1 余 11 页与真实 invoke/配额/多供应商留 B43–B46 Backlog；B43 **六功能真实 invoke 出口闭环一卡**（POST /api/ai/features/{code}/invoke：角色矩阵鉴权+门店域+模型 enabled/CHAT 校验+参数覆盖，真实火山方舟 ark-code-latest 调用成功（13013ms/83+414=497 tokens/中文画像内容），成败均沉淀 ai_invoke_log，网关页 KPI 今日调用/成功率/P99/调用日志与管理页本月用量全部真实联动；curl 全状态码矩阵 401/400/403/404/409/502+成功、DB、Chrome 三轨真验，修复 error_code 扩 512（V16）、去方法级事务保失败日志、updated_at 显式刷新、storeScopeText 展示映射污染四项缺陷），**81→82/166=49%、⬜ 83→82**，无新页面（B42 页面纵深闭合）；B44 **出站治理二期一卡**（V17 三表：三级配额 429 中文/敏感词越权 400 脱敏/告警五规则请求时实时评估，被拦截不落日志不占用量，A1Admin 配额管理 tab+A1Gateway 告警 tab 真实化，纯纵深不加页），B45 **审批申请+效果评估/A·B 实验实体化一卡**（apply 端点+通过联动启用、V18 两表三 CHECK、EvalService 八端点按 append-only 日志北京时间窗口聚合、AiApplyDrawer 共享申请抽屉，纯纵深不加页），B46 卡1 **敏感词检测整页去 mock+词库管理/命中留痕一卡**（V19 新建 ai_sensitive_hit 命中表 CHECK+三索引、8 张 AI 表 updated_at 触发器，SensitiveWordController 六端点，六功能出站链 L142 接入 BANNED/INJECTION 两类 400 脱敏、SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库，datetime.ts 时区统一工具 AI 中心 4 文件先落地，**82→83/166=50%、大表新增独立 1 页 5/15**），B46 卡2 **AI 智能中心首页整页去 mock 一卡**（A1HomeView 四 KPI/12 能力卡调用量/待办告警流按 aiGateway:view、aiAdmin:view、aiGovern:view 三权限分组门控全部接真，待审批数取审批分页 totalElements，datetime.ts 补 fmtAgo，纯前端切真无新端点，**83→84/166=约51%、大表新增首页独立 1 行 6/15**，`8bee59d`）；当前 **🔧 仅剩 M1/移动端两个远期行**（仪表盘显示 2）与**近线 ⬜ 补点**（资产转移、疗程跟踪等；marketing ddl-auto update→validate 评估、ES reindex 对账治理、WR/SST 存量脏数据回填待业务确认、推理模型 max_tokens 遵从度与前端 UTC 时区显示打磨仍留 Backlog）。

### 分业务域完成度

| # | 业务域 | ✅ | 🔧 | ⬜ | 主线状态 |
|---|---|---|---|---|---|
| ① | 客户与会员 | 8 | 0 | 10 | **客户基础运营 + 自动化 8 模块收口✅**（档案/积分商城/积分账户/标签/等级 B23；标签自动化/自动积分/等级批处理 B25）；M3 平台为后续 |
| ② | 预约与接待 | 8 | 0 | 1 | 预约/新建/我的工作台✅；**接待台+客情登记 B27 收口✅**（到店五态队列/分诊建 CP 草稿/预约签到自动到店/客情建档自动到店）；**到店核销 B28 收口✅**（/m2-checkin 登记→核销/异常/解除+timeline+客情扩展字段后端化+改派时间线）；**排队候补 B29 收口✅**（arrival_waitlist 智能候补/超时自动释放/核销↔预约勾连）；仅剩转化漏斗（依赖数据分析）⬜ |
| ③ | 咨询与诊疗 | 8 | 0 | 1 | 开方开单✅；咨询/医师工作台✅；**EMR 电子病历 B27 收口✅**（独立域全状态机+修订+方案联动；B30 补模板库/真分页/中文 400）；**复购提醒 B30 全链路收口✅**（知情同意硬前置+三方签核，`7c922cb`）；**术后随访 SOP 引擎 B30 收口、B31 两卡整页收口✅**（四表+幂等排程+超期升级 Job，`40a35ad`/`fa549d5`；B31 随访工作台/SOP 编排两整页+手工建+节点五动作+批次看板+一键升级，`70d2b3e`/`53901f6`）；仅剩复诊召回远期 ⬜ |
| ④ | 收银与交易 | 11 | 0 | 1 | **闭环✅**（收/退/划/批/卡/渠道全通；SLA/手续费收口；B24 治疗状态机贯通 PAID→TREATING→DONE；**BOM 异常 B34 收口✅** 缺料明细 422 结构化贯通页面） |
| ⑤ | 营销与留存 | 13 | 0 | 5 | **营销 9 页收口✅**（B22）+ **赠金高级规则✅/外部渠道回传✅（B26）**+ **充值赠金✅（B35 收银台抵扣闭合，营销域 🔧 清零）**+ **营销设置✅（B37 卡1 DTO 契约收口）+ 消费满额自动发赠金✅（B37 卡2 轮询 Job）+ 退款回加赠金✅（B39 终审联动逆向回加）**；落地页/转介绍 Backlog |
| ⑥ | 财务 | 11 | 0 | 1 | **全主线收口✅**（台账/对账/月结/成本/提成 + B24 发票/预算/设置/日报/卡余额/核销明细全部切真；经营毛利依赖全量收入，远期） |
| ⑦ | 组织与权限 | 10 | 0 | 14 | **闭环✅**（登录/RBAC/主数据）；B26 通知多渠道（SSE/扇出/免打扰）收口；**B33 组织树写能力整建 🔧→✅**；M2 运营为远期 |
| ⑧ | 平台与基建 | 10 | 2 | 55 | 网关/审计/字典/测试体系✅；**B42 A1 AI 中心开工：ai-service 第 8 服务+V15 七表+模型接入页+网关/管理/治理三页去 mock（4✅）**；**B43 六功能真实 invoke 出口闭环（V16+ai_invoke_log 真实沉淀+KPI/日志真实化，纵深闭合不新增页面）**；**B44 出站治理二期闭环（V17 三表：三级配额 429+敏感词/越权 400+告警五规则实时评估，A1Admin 配额管理 tab，纵深闭合不新增页面）**；**B45 审批申请入口+联动启用+效果评估/A·B 实验实体化（V18 两表+apply 端点+EvalService 八端点+AiApplyDrawer，A1Govern 页内纵深闭合不新增页面）**；**B46 卡1 敏感词检测整页去 mock（V19 ai_sensitive_hit 命中表+8 表 updated_at 触发器、SensitiveWordController 六端点、L142 出站拦截链接入 REQUIRES_NEW 独立落库、datetime.ts 时区工具，大表新增独立 1 行）**；**B46 卡2 智能中心首页整页去 mock（三权限分组门控四 KPI/能力卡调用量/待办告警、fmtAgo，大表新增独立 1 行 `8bee59d`）**；M1/T2/T4/移动端为远期 |

> ⑧ 的 ⬜ 55 主要是远期阶段页面群（M1 集团 14、A1 AI 历史口径余 9 页（已交付 6/15；B46 卡1 实测 nav 中 12 个 mock A1 视图，卡2 首页切真后实测余 10 个，视图/页面粒度口径差见 DELIVERY-P5-B46 §7，后续按实测余 10 视图逐一切页）、移动端 20、T2/T4 等 12），非当前主线。

---

## ② 模块清单大表

> 「完成批次」列对应 §③ 时间线；「真实依据」列给出可核验的交付文档或代码位置。

### 域① 客户与会员（customer-service）

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 客户档案 | ✅ | M4/P3 | 客户视图直连 @/api，交付文档 | — |
| 客户标签 | ✅ | P5-B23 | DELIVERY-P5-B23，`9fd5664` | 五分类 CRUD、覆盖汇总、防重打标、客户 360 打标/删标、tagId exists 过滤真实闭环；标签自动化规则列 Backlog |
| 会员等级 | ✅ | P5-B23 | DELIVERY-P5-B23，`9df9e7e` | 五档中文等级、阈值/权益/规则、手工调级、自动升级只升不降、实时人数；定时批处理与自动降级列 Backlog |
| 积分账户 | ✅ | P5-B23 | DELIVERY-P5-B23，`852d2f2` | 客户 360「档案」tab 流水与人工调分；clientToken 幂等、余额非负、全动作审计；消费自动积分列 Backlog |
| 积分商城 | ✅ | P5-B23 | DELIVERY-P5-B23，`64e8fe3` | 商品/规则/兑换/双签审核/拒绝/履约/KPI 真实闭环；持久单号、幂等下单、同事务扣积分扣库存 |
| 撞单合并 | ⬜ | — | business-flows 缺口 | 依赖客户查重规则，Backlog |
| 公海客户 | ⬜ | — | — | 远期 |
| 客诉管理 | ⬜ | — | — | 远期 |
| M3 客户运营平台（旅程/分群/任务/关怀/流失/NPS/导入导出/风控/洞察/设置 10 页） | ⬜ | — | — | **远期独立阶段 M3**，Backlog |

### 域② 预约与接待

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 预约看板 | ✅ | M4 | 视图直连 @/api | — |
| 新建预约 | ✅ | M4 | 视图直连 @/api | — |
| 我的工作台 | ✅ | P5-B24/B27/B30 | DELIVERY-P5-B24，`50d694d`；B27 `a08bf4d`；B30 `40a35ad` | 7 项计数真实；B27 摘除「候诊待接待」「病历草稿」两演示项（改 listArrivals(WAITING)/listEmr(DRAFT) 真实计数）；**B30 再摘除「术后回访」「SOP 超期」两演示项**（/api/txn/followup/stats 真实 sopPending/sopOverdue 计数，60s 超期升级 Job 驱动）；仅剩复诊提醒 1 演示项，依赖营销自动化 Flow |
| 排队候补 | ✅ | P5-B29 | DELIVERY-P5-B29，`b85ee45`、`2e4d010` | arrival_waitlist 新域：WL 单号查库号池、手机号锚定会员/散客快照两态、WAITING→NOTIFIED→FULFILLED/CANCELLED 状态机；号源释放（手工/超时）同事务 FIFO 递补首位+店长站内信幂等（idemKey `WL:<wlNo>:<staffId>`）；/queue 第三卡+候补登记弹层+候补 KPI；超时自动释放见接待台行 |
| 接待台 | ✅ | P5-B27/B28/B29 | DELIVERY-P5-B27，`759990d`、`a08bf4d`；B28 `cac3f37`；B29 `ae646ed`、`2e4d010` | arrival 独立域：AH 单号/queue_no 本店当日连号/四渠道/WAITING→TRIAGED→CALLING→DONE 五态；分诊（CONSULT/MEDICAL/SERVICE）同事务建 consult_plan 草稿双向回挂、叫号/完成；预约签到同事务自动到店；审计 ARRIVAL 全动作；**B28 改派历史时间线（triage_reassign 同事务追加、正序富化、右栏最小内联改派 UI，同资质候选池剔除当前负责人）**；**B29 候诊超时自动释放（ArrivalAutoReleaseJob 60s 扫描「30min 阈值+10min 宽限」、批 50、系统释放 LEFT 同事务 FIFO 递补、手工释放按钮接真、重载状态防重入、`meiyun.queue.auto-release-enabled` 开关收敛）** |
| 客情登记 | ✅ | P5-B27/B28/B30 | DELIVERY-P5-B27，`a08bf4d`；B28 `aa87fcb`；B30 `09f590b` | GuestReg 手机号 watch 查重（searchCustomers+hydrate）、真实建档成功后自动 checkIn 到店并跳接待台；**B28 十类扩展字段后端化（customer 加 9 列：年龄/肤质/诉求/过敏史三列/意向项目/意向等级/预算/沟通要点，白名单+过敏互斥，客户 360 档案 tab 有值才显示 kv 回显）**；**B30 建档实时写 ES 已交付**（customer_search_event outbox 只存 customerId+10s 中继 Job 批 50 FIFO，SENT/RETRY/DEAD 三态，连续失败 3 次熔断 30s 降级 DB，显式 mapping name 分词+keyword 子字段，读时 mergeRecentFromDb 融合+DataScope 数据域过滤；**B32 DEAD 事件处置台已交付**（/search-events retry·replay·discard 三动作+全量重建入口，customer:search:admin 仅授 REGION_MGR，`f02384b`/`dbf8158`，见下「检索事件处置台」行）；ES reindex 对账治理（ES110 vs PG100 漂移 10 文档）仍留 Backlog） |
| 到店核销 | ✅ | P5-B28/B29 | DELIVERY-P5-B28，`8ae7389`；B29 `abbe49e`、`2e4d010` | checkin_record 独立域：CI 单号查库号池、三方式（SCAN/APPOINTMENT/WALKIN）、三态（PENDING/DONE/EXCEPTION）+四异常码（NOT_SELF/ALREADY_DONE/NO_APPOINTMENT/INFO_MISMATCH）、timeline JSON 同事务流水、手机号掩码出域；by-phone 内部目录端点（本店→公海→404）锚定会员/散客快照两态；当日同号 PENDING 幂等；**B29 APPOINTMENT 手机号命中当日预约后同事务四连写（回写 ci.appt_no/wd_no、预约置已到店、自动建到店号 AH、生成 writeoff_desk_task PENDING），核销详情透出勾连单号** |
| 检索事件处置台 | ✅ | P5-B32 | DELIVERY-P5-B32，`f02384b`、`dbf8158` | /search-events：KPI 四宫格点击筛选/状态·客户ID·事件ID 筛选/左表右详情双栏，retry·replay·discard 三动作状态机（DEAD→PENDING rc 清零、当场重放、丢弃 note≥2 字+处置三字段）+中文 400/404+CUSTOMER_SEARCH_EVENT 四 action 审计+全量重建索引入口；customer_search_event 扩 DISCARDED（ddl-auto）；权限 customer:search:admin（仅 REGION_MGR），无权限菜单隐藏+路由守卫 /no-auth；附修异常处理透传、audit biz_type varchar(32) |
| 转化漏斗 | ⬜ | — | — | 依赖数据分析 |

### 域③ 咨询与诊疗

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 开方开单 | ✅ | M4/P3 | 视图直连 @/api | — |
| 咨询工作台 | ✅ | P5-B24 | DELIVERY-P5-B24，`d2d369b` | 方案单草稿保存/接诊真实端点（draft/start），会话区接真实方案单；EMR 独立域列 Backlog |
| 医师工作台 | ✅ | P5-B24/B30 | DELIVERY-P5-B24，`512990d`；B30 `40a35ad` | treat-start/treat-done 全链路（PAID→TREATING→DONE，术前四项/治疗归档），治疗队列切真；**B30 起 treat-done AFTER_COMMIT 幂等触发术后随访 SOP 排程**（物理事务 REQUIRES_NEW，见术后随访 SOP 行） |
| EMR 电子病历 | ✅ | P5-B27/B30 | DELIVERY-P5-B27，`759990d`、`a08bf4d`；B30 `5aa1e69`、`fc97d00` | emr_record 独立域：EM 单号查库号池（修订号 源号-R{n}）、DRAFT→SIGNED→ARCHIVED 全状态机、修订 parent_id/version、FIRST_VISIT/TREATMENT 两型；signEmr/treatDone 同事务幂等落病历并回挂 plan.emr_id/treat_emr_id；门店域全见越权 404；全链路网关+浏览器取证（audit 276–300）；**B30 补齐 EMR 模板库（emr_template 新域+集团 4 种子 EMT-SEED-001..004 只读+本店建/停用两态+模板库弹层九段范文，权限 emr:view/create 复用）、列表真分页（默认 20）/stats 后端聚合、ChineseValidationAdvice 框架异常中文 400；附修 fc97d00（JPQL cast(:type as string) 解决 PG 42P18 查询 500）；集团模板运营管理留 Backlog** |
| 复购提醒 | ✅ | P5-B30 | DELIVERY-P5-B30，`7c922cb` | RepurchaseView 全链路切真：客户 /api/customer/search 真实检索（DataScope 数据域隔离）、目标项目必填+知情同意硬勾选前置、待签核统计实时、客户（预填）/经办/店长三方签核填齐解禁、RP 单号查库序号不回退（冒烟 RP20260911-003194 已还原）、REPURCHASE/CREATE+TRIPLE_SIGN 审计；**ApprovalService 医师/店长/财务多级审批流留 Backlog** |
| 术后随访 SOP | ✅ | P5-B30/B31 | DELIVERY-P5-B30，`40a35ad`、`fa549d5`；DELIVERY-P5-B31，`3825a48`、`4492da4`、`d130dfc`、`70d2b3e`、`53901f6` | followup_sop_template/node/batch/followup 四表；treat-done AFTER_COMMIT→REQUIRES_NEW 物理事务幂等排程（source_plan_id uk+exists 双幂等），默认 1/3/7/30 天节点（微信/电话），种子 SPT-SEED-001 @Order(62) 仅 seed 库；batch_no 号池 `SOP{yyyyMMdd}-%` maxSeqOfDay 不回退；FollowupSopDueJob 60s 扫描批 50 FIFO 超期升级店长幂等通知（idemKey `SOP-ESC:{followupNo}:{staffId}`，免打扰）；/api/txn/followup 五端点（list/stats/get/complete/skip，followup:view/edit）+工作台两演示项摘除；附修 fa549d5（afterCommit 无活动事务致排程静默丢写）；**B31 纵深增强（不加行）：手工建随访 POST（门店强制取 JWT/客户实名解析/planDate≥serviceDate/method 白名单/sopStage=MANUAL/FOLLOWUP·CREATE 审计）、stats 扩九键（+pending/todayPending/overdue/done/skipped/avgSatisfaction/adverseCount，空库 AVG null 修 getSingleResult 的 d130dfc）、keyword 三字段 OR 模糊+固定排序分页；SOP 侧九端点（模板查改/节点增改停删/重置/批次分页聚合/summary 五键/一键升级），SOP_TEMPLATE 五动作审计，FollowupSopEscalator 抽为 DueJob 与一键升级共享组件（escalate 后 save 防标记丢失）** |
| 随访工作台 | ✅ | P5-B31 | DELIVERY-P5-B31，`3825a48`、`4492da4` | /followup 整页接真：FollowupView 列表（九键 KPI/keyword/分页/手工建弹层/完成·跳过流转）+ BoardView 流水牌 sopTodos 真库计数；schedulePostOpSop 降 async 空 shim 防前后端重复排程；C 端 /m/followup「陈美玲」2 条演示种子与 submitByCustomer 保留 |
| SOP 编排 | ✅ | P5-B31 | DELIVERY-P5-B31，`70d2b3e`、`53901f6` | /sop 整页接真：SopManagementView 双 tab（模板节点五动作+增改 dayOffset 0-365/method 白名单/内置可停用可改不可删（删除中文 400）/reset 同构重建；批次看板未完结在前+节点内嵌随访 24 字段）、五键 KPI、超期 warnbar 一键升级（v-perm followup:edit，成功 infobar 静默刷新）；删 SOP mock，router/nav 零改动（既有占位直接接真） |
| 复诊召回 | ⬜ | — | — | 依赖营销自动化，远期 |

### 域④ 收银与交易（txn-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 订单收款 | ✅ | M4/P3 | 网关 200 + 交付文档 | — |
| 整单划扣 | ✅ | B17 | DELIVERY-P5-B17 | 三路径打通 |
| 划扣执行台（双签） | ✅ | B17/B18 | WriteoffDeskService 硬闸门 | L1 双签 |
| 退款（L3 三签） | ✅ | B19 | DELIVERY-P5-B19，fbe65c2 | 店长→区域→财务 |
| 退卡（冻结/终审） | ✅ | B18/B19/B20 | DELIVERY-P5-B18/B19/B20 | 手续费 RF-REVENUE/IN 收入分录 B20 收口；退卡 SLA 经 B21 实证由无差别 SLA Job 覆盖 |
| 审批中心 | ✅ | B19/B20/B21 | ApprovalService 状态机，DELIVERY-P5-B20/B21 | SLA/候选过滤 B20 收口；转交/加签目标人硬校验（存在/在职/阶段角色/自转交/重复加签/加签给指派人）+ 详情指派人/会签人展示 B21 收口；REGION 兼岗路由留 Backlog |
| 会员卡项 / 疗程 | ✅ | B16/B17 | member_card 真实 | — |
| 卡项目录 | ✅ | B15 | DELIVERY-P5-B15 | — |
| 支付渠道 | ✅ | P3 | 渠道账实 B12 | — |
| BOM 异常 | ✅ | B10/B34 | DELIVERY-P5-B34，BomDeductService/InventoryView | **B34 收口**：库存不足 422 `{message,shortages[]}` 结构化贯通（skuCode/skuName/needQty/stockQty/unit）→ detail_json + 审计 shortageLines + 页面缺料明细；附修 exc_id substring 起始位致序号恒为 1 |
| 疗程跟踪 | ⬜ | — | — | 近线缺口 |
| 资产转移 / 合同 | ⬜ | — | business-flows 缺口 | Backlog |

### 域⑤ 营销与留存（marketing-service）

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 优惠券 | ✅ | B22 | DELIVERY-P5-B22 | 券模板/发放/核销真实闭环；8 券种网关+浏览器验证 |
| 精准推送 | ✅ | B22 | DELIVERY-P5-B22，PushService | 写链路四件套：幂等(SHA-256 dedupKey 60s)/审计(PUSH/SEND)/周频控(7天≤3)/违禁词；13 客户真实 |
| 券核销 | ✅ | B22 | DELIVERY-P5-B22，CouponWriteoffService | 三态(FORGED/DUPLICATE/EXPIRED)+折扣(分)+审计(WRITEOFF/BLOCK)；**B34 修正**门店三级兜底为(登录门店→营销设置配置的兜底门店→/internal/first 首家真实门店→三级落空抛 503)，消灭原硬编码 SST01 幽灵码(core 库无此门店，历史脏数据 WR20260908-000004~000007 为证) |
| 营销总览 | ✅ | B22 | DELIVERY-P5-B22 | 触达/漏斗/趋势/渠道排行真实；导出报告/订阅周报占位 Backlog |
| 素材库 | ✅ | B22 | DELIVERY-P5-B22 | 10 素材元数据/标签/授权门店/引用计数；文件二进制(S3/MinIO) Backlog |
| 海报 | ✅ | B22 | DELIVERY-P5-B22，m5Poster | 推荐人下拉接 listStaff 真实在职员工(19名)；模板/漏斗/佣金真实；图片渲染 Backlog |
| 直播/短视频 | ✅ | B22 | DELIVERY-P5-B22 | 7 场直播/挂链成交/漏斗真实；开播写接口/成交回写 Backlog |
| 营销 ROI | ✅ | B22 | DELIVERY-P5-B22，m5Roi | 活动维度 ROI/发券核销统计真实(GET /stats/overview)；渠道区 7 平台+归因模型为外部依赖 Backlog |
| 渠道业绩 | ✅ | B22 | DELIVERY-P5-B22，m5Channel | 7 渠道卡片/接入状态/对账日真实渲染；外部广告平台(美团/抖音/小红书/大众点评/新氧)数据接入 Backlog |
| 营销设置 | ✅ | B34/B37 | DELIVERY-P5-B37，`828ac6d`；B34 `9a82af6` | 不在 B22 9 页内；**B34 纵深**：新增「券核销兜底门店」配置项(marketing_cfg.writeoff_fallback_store_code，写入前门店存在性校验中文 400、空白归一 null、同值不写冗余审计)，前端 /m5-settings 审批流分组真实门店下拉；**B37 卡1 收口（2026-09-12，`828ac6d`）**：GET 改独立 ConfigView 与写命令 ConfigCmd 字段/类型完全对称（周频键统一 weeklyPushLimit 废弃旧 weeklyLimit 破坏性改名前后端同卡同步、渠道字段两端同为数组、视图剔除 cfgId/referral*/commissionRate 四个多泄遗留列、旧审计快照 weeklyLimit 读时归一、quota weeklyLimit 独立契约不动），marketing_cfg 全部 12 键（周频/免打扰/节假日豁免/大额券阈值/推送审批层级/默认推送与投放渠道/核销兜底门店）均由 /m5-settings 真实页面+真实端点管理，容器 GET/POST/幂等/硬约束/落库/审计取证，原「缺管理页」「GET/POST 字段名不对称」两条 🔧 理由均消除 |
| 充值赠金 | ✅ | B18/B26/B35/B37 | DELIVERY-P5-B35/B37，GrantService/InternalGrantController/PaymentService/OrderView/AutoGrantService | B18 账本+发赠金、B26 满赠阶梯/有效期/报表；**B35 收口**：grant_deduction 扣减流水+FIFO 带锁扣减/幂等/超额 422/GRANT_DEDUCT 审计，marketing internal 端点（X-Internal-Token），txn 转发查余额（cashier:view，散客/不可用降级 available:false）+grant 支付（散客 400/同单第二笔 409），收银台「营销赠金」磁贴与混合支付（template/style 零改动）；**B37 卡2 收口（2026-09-12，`7552665`）**：消费满额自动发赠金——AutoGrantJob 5 分钟轮询 txn `/internal/paid-orders`（复刻 B25 AutoPoints 拉取范式，未走 FUND_ENTRY 事件链路）、CONSUME_THRESHOLD 规则 priority 首条命中（一单不叠多档）、双层幂等（idem_key `RULE:{ruleId}:{orderNo}` + source_biz_ref 订单级跨规则锚点）、单行游标 auto_grant_state 日粒度窗口/NULL 首跑全量回填、txn 不可用整轮中止不推进/客户不存在 400 跳过的故障分层、明细 RULE/system + AUTO-GRANT/SYSTEM 汇总双审计，容器真验全量回填扫描 5 发放 4、二轮幂等零新增；**B39 退款回加赠金已闭合（终审联动逆向回加，`5353bd9`，详见 DELIVERY-P5-B39）** |
| 落地页 / 日历 / 转介绍 | ⬜ | — | business-flows 缺口 | 转介绍到期重分配 Backlog；referral mock 待客户域 M3/营销渠道 |
| 随访 / SOP / 关怀 / 召回 | ⬜ | — | — | 依赖私域自动化 Flow，远期；术后随访 SOP 引擎及随访工作台/SOP 编排两整页已由 B30/B31 在诊疗域（txn-service）闭合（见域③ 术后随访 SOP/随访工作台/SOP 编排行），本行指营销侧关怀/沉睡唤醒/复诊召回 Flow，仍远期 |

### 域⑥ 财务（finance-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 资金台账（fund_entry） | ✅ | B1/P3 | FundEntryService.postEntries 幂等 | — |
| 三方对账 | ✅ | B6 | DELIVERY-P3-B6 | — |
| 渠道账实 | ✅ | B12 | DELIVERY-P3-B12 | — |
| 月结封账 | ✅ | B11 | DELIVERY-P3-B11 | 封账月禁重算 |
| 成本结转 | ✅ | B8 | CostCarryService（COST 分录） | — |
| 提成 | ✅ | B9 | DELIVERY-P3-B9 | — |
| CSV 导出 | ✅ | B7 | DELIVERY-P3-B7 | — |
| 发票管理 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84` | 发票列表去 mock 接真实端点，发票 CSV 真实导出（/api/finance/export/invoices.csv） |
| 预算 / 财务设置 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84` | 预算与财务设置页去 mock 切真；进项税抵扣列 Backlog |
| 税 / 卡余额 / 日结 / 异常 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84`、`dfbe14a` | 资金日报去 mock；卡余额时间线切真（customer-service 内部投影 /internal/cards/{cardNo}/ledger）；通用异常中心列 Backlog |
| 财务核销 / 预收 | ✅ | P5-B24 | DELIVERY-P5-B24，`dfbe14a` | 核销双签明细切真（txn-service /internal/finance/writeoff-details，X-Internal-Token），卡台账/核销 CSV 导出；预收合规监控列 Backlog |
| 经营毛利 | ⬜ | — | — | 依赖成本+收入全量，远期 |

### 域⑦ 组织与权限（org-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 登录 / JWT | ✅ | M7 | DELIVERY-M7，/api/org/auth/login | — |
| 员工管理 | ✅ | M7 | staff/staff_role 真实 | — |
| 角色 RBAC / 权限矩阵 | ✅ | M7/B19 | PermissionMatrix | 角色管理页待补（Backlog） |
| 房间床位 | ✅ | B13 | DELIVERY-P4-B13 | — |
| 设备台账 | ✅ | B13 | DELIVERY-P4-B13 | — |
| 项目 / 品牌 | ✅ | P4 | 主数据真实 | — |
| 价目表 | ✅ | B14 | DELIVERY-P5-B14 | — |
| 耗材库存 | ✅ | B8/B13 | 库存真实 | — |
| 组织树 | ✅ | B33 | DELIVERY-P5-B33，OrgAdminController 三写端点 + T1OrgView 去 mock | 部门层级写闭环真实（仅门店下建部门/编辑三态语义/部门跨店移动联动 store_code+region/启停带停用原因，越权中文 404）；**门店/大区/集团层级写能力与兼岗上级路由留 Backlog**，部门只停用不物理删 |
| 消息通知中心 | ✅ | B20/B21 | DELIVERY-P5-B20/B21，NotificationController 6 端点 | 系统内通知真实闭环（铃铛/列表/已读）；通知偏好持久化（notify_preference 表 + GET/PUT 端点 + SLA 催办偏好免打扰双向实证）B21 收口；**多渠道（短信/企微/邮件 dev 网关 + SSE 实时推送 + 免打扰 + 失败重试/死信）B26 收口**，真实运营商网关接入留 Backlog |
| 交接班 | ⬜ | — | — | 近线缺口 |
| M2 门店运营（排班/工单/日结/申购/报损/绩效/周报/巡检/拓客/唤醒/异常等 13 页） | ⬜ | — | — | **远期独立阶段 M2**，Backlog |

### 域⑧ 平台与基建

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 审计链（audit-service） | ✅ | M7 | 审计落库真实 | — |
| 字典管理 | ✅ | M7 | 字典端点真实 | — |
| 国密网关双栈（Go） | ✅ | M7 | 18443/8443 双栈 | — |
| 测试 / 双栈验证体系 | ✅ | 全程 | 每批 DoD（mvn+vue-tsc+双栈+PG 对账） | — |
| M1 集团（brand/procurement/marketing 3 页真实） | 🔧 | — | 3 页直连 | 其余 14 页 mock，**远期 M1** |
| C 端移动端（packages/coupons 2 页真实） | 🔧 | — | mp-uniapp 2 页 | 其余 20 页 mock，**远期移动端** |
| M1 集团其余 14 页 | ⬜ | — | — | 远期 M1 |
| T2 数据分析（4 页） | ⬜ | — | — | 远期 T2 |
| T3 外部集成 | ⬜ | — | — | 远期 T3 |
| T4 AI 算力（4 页） | ⬜ | — | — | 远期 T4 |
| A1 · 模型接入（/ai/providers 供应商与模型配置） | ✅ | P5-B42/B43 | DELIVERY-P5-B42，ai-service | 供应商/模型 CRUD、ApiKey AES-GCM 密文落库+掩码回显、真实连通性测试（火山方舟 OpenAI 兼容）、六功能绑定与角色灰度矩阵；B42 末真实 Key 录入后连通待复核项**已由 B43 真实 invoke 成功闭合**（ark-code-latest，497 tokens）；价格口径统一为元/百万 tokens |
| A1 · AI 网关监控（/ai/gateway 调用日志/KPI） | ✅ | P5-B42/B43/B44 | DELIVERY-P5-B42，A1GatewayView 去 mock；DELIVERY-P5-B43 真实联动；DELIVERY-P5-B44 告警真实化 | KPI/调用日志/账单接 /api/ai/logs* 真实端点；**B43 起 POST /features/{code}/invoke 真实出口贯通，ai_invoke_log 成败全量沉淀，今日调用/成功率/P99/日志全部真实（ark-code-latest 真实 ARK 497 tokens 实证）**；**B44 拦截链接敏感词(400)与三级配额(429)、告警五规则实时评估，告警 tab 与 KPI 活跃告警真实联动** |
| A1 · AI 管理配置（/ai/admin 全局配置/矩阵/账单） | ✅ | P5-B42/B43 | DELIVERY-P5-B42，A1AdminView 去 mock | 全局配置 GET/PUT、功能角色矩阵、KPI/账单真实；B43 功能绑定抽屉新增真实试运行（invoke 结果/token/费用回显，调用后本月用量实时联动）；**B44 新增第 6 个「配额管理」tab（三级配额 8 行+日/月用量额度+水位三档色+调整抽屉，FEATURE→MODEL→GLOBAL 出站前 429 中文）** |
| A1 · AI 审批与效果评估（/ai/govern） | ✅ | P5-B42/B45 | DELIVERY-P5-B42，A1GovernView 去 mock；DELIVERY-P5-B45 申请入口+评估实验实体化 | 审批 Spring Page 真分页+状态过滤+通过/驳回决策（MODEL 通过联动 enabled、BINDING 驳回联动 NONE、重复决策 400 中文），REGION_MGR aiGovern:approve；**B45 补 POST /api/ai/approvals apply 申请入口（模型上架/功能绑定两类申请单，AiApplyDrawer 共享抽屉）+审批通过联动 enabled=t（linkage 审计），效果评估/A·B 实验从占位实体化（V18 两表、八端点、北京时间窗口聚合、lift pp、结案冻结，KPI computed 修复）** |
| A1 · 敏感词检测（/ai/sensitive 词库治理/命中留痕） | ✅ | P5-B46 | DELIVERY-P5-B46，A1SensitiveView 去 mock | 词库 CRUD+命中分页/词类三态过滤/统计/误报标注（SensitiveWordController 六端点）、V19 ai_sensitive_hit 命中落库 CHECK+三索引、六功能出站链 L142 拦截 BANNED/INJECTION 400 中文脱敏、SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库、8 张 AI 表 updated_at 触发器、前端时区工具 datetime.ts |
| A1 · 智能中心首页（/ai 四 KPI/能力卡/待办告警） | ✅ | P5-B46 卡2 | DELIVERY-P5-B46-2，A1HomeView 去 mock，`8bee59d` | 三权限分组门控（aiGateway:view 并发 logKpi/monthlyBill/listAlerts、aiGovern:view approvals?status=PENDING 取分页 totalElements、aiAdmin:view sensitive/hits/stats），无权限 KPI 显「—」且不发请求杜绝越权 403；12 能力卡 capCalls 按 billMap 仅六功能显本月调用量、敏感词卡显今日拦截，无日志功能诚实隐藏；待办=审批前 5（fmtAgo 相对时间，跳 /ai/govern）+敏感词今日命中条+active 告警前 5，空态文案；datetime.ts 增 fmtAgo；纯前端切真无新端点、样式零改动 |
| A1 AI 中心其余 9 页（Prompt 编排/知识库/Embedding/向量/Agent/效果实验等） | ⬜ | — | — | **远期 A1**，B46 分批：~~真实功能 invoke+调用日志（B43 已闭合）~~、~~配额限流敏感词告警（B44 已闭合）~~、~~审批申请 UI+效果评估/A·B 实体化（B45 已闭合）~~、~~敏感词检测页（B46 卡1 已闭合）~~、~~智能中心首页（B46 卡2 已闭合）~~、余页去 mock（已交付 6/15，历史口径余 9 页/实测 nav 中 mock A1 视图 12→10 个，粒度口径差见 DELIVERY-P5-B46 §7）+EMBEDDING/VISION 多能力+多供应商+token 计费账单环比闭环 |
| 通用页（全局搜索/帮助/403/闭环演示） | ⬜ | — | — | 近线/体验项 |

---

## ③ 批次时间线

| 批次 | 日期 | 主题 | commit | 交付文档 | 闭合 / 推进模块 |
|---|---|---|---|---|---|
| M7 | 2026-09-02 | 权限/JWT/数据域/审计/网关/字典 | — | DELIVERY-M7 / M7-5 / M7-8 / M7-9 | 登录、RBAC、审计、字典、网关 |
| M4 | 2026-09-02 | 交易骨架 / 客户 / 预约 / 开单 | — | DELIVERY-M4 | 订单收款、客户档案、预约、开方开单 |
| P3-B1 | 2026-09-05 | 财务接线 / fund_entry 台账 | — | DELIVERY-P3-B1 | 资金台账 |
| P3-B2 | 2026-09-05 | 合规落账 | — | DELIVERY-P3-B2 | 资金台账 |
| P3-B3 | 2026-09-05 | 充值台账 | — | DELIVERY-P3-B3 | 充值/资金 |
| P3-B4 | 2026-09-05 | 财务配置 | — | DELIVERY-P3-B4 | 财务设置半成 |
| P3-B5 | 2026-09-06 | 划扣 G1 | — | DELIVERY-P3-B5 | 划扣基础 |
| P3-B6 | 2026-09-06 | 三方对账 | — | DELIVERY-P3-B6 | 三方对账 ✅ |
| P3-B7 | 2026-09-06 | CSV 导出 | — | DELIVERY-P3-B7 | CSV 导出 ✅ |
| P3-B8 | 2026-09-06 | 成本 / 库存 | — | DELIVERY-P3-B8 | 成本结转、耗材库存 |
| P3-B9 | 2026-09-06 | 提成 | — | DELIVERY-P3-B9 | 提成 ✅ |
| P3-B10 | 2026-09-06 | BOM | — | DELIVERY-P3-B10 | BOM 异常（后端） |
| P3-B11 | 2026-09-07 | 月结封账 | — | DELIVERY-P3-B11 | 月结封账 ✅ |
| P3-B12 | 2026-09-07 | 渠道账实 | — | DELIVERY-P3-B12 | 渠道账实、支付渠道 ✅ |
| P4-B13 | 2026-09-07 | 房间床位 / 设备 | — | DELIVERY-P4-B13 | 房间床位、设备台账 ✅ |
| P5-B14 | 2026-09-07 | 价目设计 | — | DESIGN/DELIVERY-P5-B14 | 价目表 ✅ |
| P5-B15 | 2026-09-07 | 卡项目录 | — | DELIVERY-P5-B15 | 卡项目录 ✅ |
| P5-B16 | 2026-09-07 | 售卡开卡 | — | DELIVERY-P5-B16 | 售卡/开卡 |
| P5-B17 | 2026-09-07 | 卡核销三路径 + 去划扣动线 | `c40f42d` | DELIVERY-P5-B17 | 整单划扣、划扣台、会员卡项 ✅ |
| P5-B18 | 2026-09-08 | 赠金 + 退卡冻结 + 双签金额分级 | `3162abf` | DELIVERY-P5-B18 | 赠金、退卡冻结、双签硬校验 |
| P5-B19 | 2026-09-08 | L3 大额三签审批（店长→区域→财务）+ sign3 | `fbe65c2` | DELIVERY-P5-B19 | 退款/退卡/审批中心 ✅ |
| P5-B20 | 2026-09-08 | 审批 SLA 超时收口（三阶段时限/催办/标记）+ 消息通知中心真实落地 + 候选人门店预过滤 + 退卡手续费收入分录 | `b9e5829` | DELIVERY-P5-B20 | 审批中心 SLA、**消息通知中心 ✅ 新闭环**、退卡手续费 |
| P5-B21 | 2026-09-08 | 审批转交/加签目标人硬校验（存在/在职/阶段角色/自转交/重复加签/加签给指派人）+ 通知偏好持久化（notify_preference 表 + GET/PUT 端点 + SLA 催办偏好免打扰）+ 审批详情指派人/会签人展示 + 退卡冻结 SLA 覆盖实证勾销 | `89c3c64` | DELIVERY-P5-B21 | 审批硬校验、通知偏好持久化 ✅（既有模块纵深增强，不新增页面） |
| P5-B22 | 2026-09-08 | 营销 9 页收口：精准推送写链路四件套（参数校验/幂等 dedupKey 60s/周频硬上限 3/违禁词四类实时校验 + 全动作审计 PUSH-SEND）+ 券核销门店三级兜底（登录门店→store-service /internal/first 内部端点→SST01 降级，FORGED 异常单也落真实门店）+ 核销链路漏斗播种器（CH-001/002/003）+ 海报推荐人接真实员工（listStaff 19 名在职，替换 mock 林晚）+ 9 页浏览器逐页真实验证零控制台报错 | `1977df4` | DELIVERY-P5-B22 | 营销 9 页 ✅（优惠券/精准推送/券核销/营销总览/素材库/海报/直播短视频/营销ROI/渠道业绩；34→43/166=26%） |
| P5-B23 | 2026-09-09 | 客户域 4 卡收口：积分商城（持久单号/双签审核/幂等下单/履约/规则四件套）、积分账户人工调分（clientToken 防重放/余额非负/流水分页/积分池真实读模型）、客户标签（五分类 CRUD/防重打标/覆盖汇总/tagId 过滤/360 打标删标/全程审计）、会员等级（中文五档/阈值权益/单行规则/手工调级/实时人数/自动升级只升不降） | `64e8fe3`、`852d2f2`、`9fd5664`、`9df9e7e` | DELIVERY-P5-B23 | 积分商城、积分账户、客户标签、会员等级 4 页 ✅（43→47/166=28%；客户域 ✅1→5、🔧4→0） |
| P5-B24 | 2026-09-09 | 「两线合并全收」5 卡：财务四页去 mock + 发票真实 CSV、卡余额时间线 + 核销双签明细服务间内部投影切真、咨询草稿/接诊端点 + ConsultationView 切真、treat-start/treat-done 治疗全链路（术前四项/治疗归档 EMR）+ 医师工作台切真、我的工作台 7 项计数真实化（5 项无后端域诚实标注「（演示）」不计总数） | `1eaef84`、`dfbe14a`、`d2d369b`、`512990d`、`50d694d` | DELIVERY-P5-B24 | 发票/预算/财务设置/资金日报、卡余额时间线/核销明细、咨询工作台、医师工作台、我的工作台 7 项 ✅（47→54/166=33%；财务 ✅7→11、🔧4→0；咨询诊疗 ✅1→3、🔧4→2） |
| P5-B25 | 2026-09-09 | 域①客户域自动化三件套：标签自动化规则引擎（TA### 规则 CRUD + TagAutoRuleJob 定时扫描 + 5 类条件 + 打/撤标 + 审计）、消费自动积分（txn 内部只读端点拉取已收款/已退款订单 + 积分规则折算 + AUTOPOINTS 幂等键发分/回退）、等级定时批处理（LevelMonthlyJob 每月1号 cron + autoUpgrade 开关收敛 + 汇总审计） | `00a06e3`（代码）、见 DELIVERY-P5-B25 §六（docs） | DELIVERY-P5-B25 | 标签自动化规则、消费自动积分、等级定时批处理 3 模块 ✅（54→57/166=34%；客户域 ✅5→8、⬜13→10） |
| P5-B26 | 2026-09-09 | 域⑦ 通知多渠道 + 域⑤ 赠金高级规则 + 外部渠道回传骨架：渠道适配层（Inbox/SMS/Wechat/Email dev 网关 POST）+ NotificationFanoutJob 定时扇出 + NotificationStreamController SSE 实时推送 + 免打扰全局配置；GrantRule/GrantService（满赠阶梯/有效期/账本/过期/报表）+ GrantExpireJob；ExternalChannelController 通用回传回调 + channel_returnback 表 + 种子联调 | 见 DELIVERY-P5-B26（docs） | DELIVERY-P5-B26 | 通知多渠道、赠金高级规则、外部渠道回传 3 模块 ✅（57→60/166=36%；营销域 ✅9→11、🔧2→1） |
| P5-B27 | 2026-09-10 | 主轴接待台/候诊分诊 + EMR 独立域（链路+完整页面交互）：arrival/triage/emr_record 三新表；到店五态队列（AH 单号、queue_no 本店当日连号、四渠道、预约签到同事务自动到店）；分诊三型同事务建 consult_plan 草稿+双向回挂+改派留痕+叫号/完成；EMR DRAFT→SIGNED→ARCHIVED+修订（-R{n}）全状态机，signEmr/treatDone 同事务幂等落病历回挂方案；客情登记真实建档+自动到店；工作台两演示项摘除；**附带修复 2 项运行态缺陷**（EMR 号池 char_length 18→17 每日重号 500；nanoTime 伪随机病历号删除收敛查库池）；前端 14 文件模板零改动 | `759990d`、`a08bf4d` | DELIVERY-P5-B27 | 接待台、客情登记、EMR 电子病历 3 模块 ✅（60→62/166=37%；预约接待 ✅3→5、⬜5→3；咨询诊疗 ✅3→4、🔧2→1）；冒烟全量还原 |
| P5-B28 | 2026-09-10 | 预约接待收口批三卡：到店核销 /m2-checkin 真实闭环（checkin_record 新表+CI 单号池、SCAN/APPOINTMENT/WALKIN 三方式、PENDING/DONE/EXCEPTION 三态+四异常码、确认核销/标记异常/解除异常+timeline 同事务流水、KPI/过滤、by-phone 本店→公海→404 锚定、当日同号 PENDING 幂等、手机号掩码出域）；客情登记十类扩展字段后端化（customer 加 9 列、白名单+过敏互斥、360 档案有值才显示 kv）；triage 改派历史时间线（triage_reassign 新表同事务追加+读模型批量富化+接待台最小 UI） | `aa87fcb`、`cac3f37`、`8ae7389` | DELIVERY-P5-B28 | 到店核销 1 模块 ✅（62→63/166=38%；预约接待 ✅5→6、⬜3→2；客情扩展/改派时间线为既有模块纵深增强）；audit 301–313 共 13 行；冒烟全量还原 |
| P5-B29 | 2026-09-10 | 预约接待尾工批三卡：排队智能候补（arrival_waitlist 新表+WL 单号池、手机号锚定会员/散客快照两态、WAITING→NOTIFIED→FULFILLED/CANCELLED 状态机、号源释放同事务 FIFO 递补首位+店长站内信幂等、/queue 第三卡+候补登记弹层+候补 KPI+30s 双刷）；候诊超时自动释放号源（ArrivalAutoReleaseJob 60s 扫描「30min 阈值+10min 宽限」、批 50、系统释放 LEFT 同事务递补、手工释放按钮接真、重载状态防重入、auto-release-enabled 开关收敛）；到店核销↔预约/划扣自动勾连（APPOINTMENT 手机号命中当日预约，同事务回写 appt_no/wd_no+预约置已到店+自动建到店号+writeoff_desk_task PENDING，核销详情透出勾连单号） | `b85ee45`、`ae646ed`、`abbe49e`、`2e4d010` | DELIVERY-P5-B29 | 排队候补 1 模块 ✅（63→64/166=39%；预约接待 ✅6→7、⬜2→1；超时释放/核销勾连为既有模块纵深增强，转化漏斗仍 ⬜）；网关 curl 五阶段 24 项全绿（含 Job 自动释放实证）+浏览器两页双轨取证 console 零错；audit 314–331 共 18 行、哈希链 17/17；冒烟全量还原（删除计数 2/2/1/1/4/3/1，审计 append-only） |
| P5-B30 | 2026-09-11 | 近线收口四卡：③ 复购提醒前端接线（RepurchaseView 全链路：客户真实检索+目标项目必填+知情同意硬前置+客户·经办·店长三方签核+RP 单号池+REPURCHASE/CREATE·TRIPLE_SIGN 审计，🔧→✅）；④ EMR 模板库/400 化（emr_template 新域+集团 4 种子 EMT-SEED-001..004 只读+本店建/停用+模板库弹层九段范文、列表真分页/统计后端聚合、ChineseValidationAdvice 框架异常中文 400）；② 建档实时写 ES（customer_search_event outbox 只存 customerId+10s 中继 Job 批 50 FIFO+SENT/RETRY/DEAD+连续失败 3 次熔断 30s 降级 DB+显式 mapping+读时 mergeRecentFromDb 融合+DataScope 过滤+新索引全量回填）；① 术后随访 SOP 引擎（followup_sop_template/node/batch/followup 四表+treat-done AFTER_COMMIT 幂等排程 1/3/7/30 天+60s 超期升级 Job 店长幂等通知免打扰+/api/txn/followup 五端点+工作台两演示项切真摘除）；**附修 2 项运行态真实缺陷**（fa549d5 afterCommit 无活动事务致 SOP 排程静默丢写→REQUIRES_NEW 物理事务；fc97d00 模板库裸 null 参数致 PG 42P18 查询 500→JPQL cast string）；样式零改动 | `7c922cb`、`5aa1e69`、`09f590b`、`40a35ad`、`fa549d5`、`fc97d00` | DELIVERY-P5-B30 | 复购提醒、术后随访 SOP 2 模块 + 建档实时写 ES/EMR 模板库 2 项 Backlog 纵深闭合（64→68/166=41%，🔧6→5、⬜96→93；咨询诊疗 ✅4→6、🔧1→0）；网关 curl 全路径（事件 SENT/7 次 RETRY 自愈/熔断/DataScope SELF 隔离 []）+浏览器 RP20260911-003194 创建→三方签核与模板库 UI 双轨取证 console 零错；audit 81–93 共 13 行九类动作 append-only 保留；冒烟全量还原（PG 当日造数零残留、customer 基线 100、ES 113→110 三历史漂移槽位如实记录） |
| P5-B31 | 2026-09-11 | 术后随访 SOP 引擎收口两卡（闭合 B30 明示 Backlog 两项）：卡A 随访工作台 /followup 整页接真（手工建随访 POST：门店强制取 JWT/客户实名解析/planDate≥serviceDate/method 白名单默认 PHONE/sopStage=MANUAL；stats 扩九键 sopPending·sopOverdue 旧键不动+pending/todayPending/overdue/done/skipped/avgSatisfaction/adverseCount；keyword 客户名/项目/关联单号 OR 模糊+固定排序分页；FollowupView 整页+BoardView sopTodos 真库；schedulePostOpSop 降 async 空 shim 防前后端重复排程；C 端 /m/followup「陈美玲」种子与 submitByCustomer 保留；**附修空库 AVG null 致 /stats 500 的 NPE→getSingleResult**）；卡B SOP 编排 /sop 整页接真（模板节点增改停删+重置五动作、dayOffset 0-365/内置可停用可改不可删（删除中文 400）、批次分页聚合未完结在前、summary 五键、一键升级超期节点，FollowupSopEscalator 抽为 DueJob/一键升级共享组件+escalate 后 save 防标记丢失，SOP_TEMPLATE 五动作审计，前端删 SOP mock 双 tab/KPI/warnbar·infobar）；样式零改动、router/nav 零改动 | `3825a48`、`4492da4`、`d130dfc`、`70d2b3e`、`53901f6` | DELIVERY-P5-B31 | 随访工作台、SOP 编排 2 页 ✅（68→70/166=42%，⬜93→91、🔧5 不变；咨询诊疗 ✅6→8）；网关 curl 全路径（空体 Bean Validation 422 中文原文、内置删除 400、他店批次 404、一键升级 escalated 计数）+浏览器两页双轨取证（KPI 2/5/2/1、reqid 104→105/106 静默刷新 infobar、console 仅 1 a11y 零 error/warn）；**偏差如实披露：全量重建经 setup-seed-db.sh TRUNCATE audit_log，本批冒烟审计不存活，终态 36 行全 CONSUMABLE 播种基线，已登记「脚本增补保留 audit 选项」Backlog**；冒烟全量还原（followup=0、template=1、node=4 全启用、SOP-ESC 幂等键=0） |
| P5-B32 | 2026-09-11 | 客户搜索 outbox「DEAD 事件处置台」两卡（闭合 B30 遗留 Backlog）：卡A 后端（customer_search_event 扩 DISCARDED+resolve_note/resolved_by/resolved_at，CustomerSearchEventAdminService 三动作状态机：retry 仅 DEAD→PENDING rc 清零/last_error 清空、PENDING 幂等、终态中文 400；replay PENDING/DEAD 当场同步投递 ES、ES 不可用保持 PENDING 走 RETRY；discard note trim≥2 字+处置三字段；GET search-events 三过滤+event_id 倒序真分页+/stats 四键；reindex 换权 customer:search:admin；CUSTOMER_SEARCH_EVENT 四 action MANUAL_RETRY/REPLAY/DISCARD/REINDEX 合法 JSON 审计；权限码仅挂 REGION_MGR；附修 GlobalExceptionHandler 漏接 ResponseStatusException 中文 400/404 被兜 500、audit_log.biz_type varchar(16)→(32)）；卡B 前端（/search-events 处置台整页：KPI 四宫格点击筛选/筛选条事件ID 正整数校验/左表右详情双栏/选中粉行/last_error 红块/三按钮按状态机禁用/丢弃 modal 行内校验/写后 list+stats 两连刷新/全量重建 toast，nav 客情洞察组+路由守卫） | `f02384b`、`dbf8158` | DELIVERY-P5-B32 | DEAD 事件处置能力+处置台 2 项 Backlog 闭合（70→72/166=43%，⬜91→89、🔧5 不变；预约接待 ✅7→8）；网关 curl 全矩阵（中文 400/404/401/SE001 403/reindex=100/审计 JSON）+浏览器双轨取证（中继 Job 活行为：PENDING 经 RETRY rc 封顶 20 转 DEAD；丢弃 1 字行内报错/三 toast；network 23 个全 200、console 零 error/warn、双截图；SE001 菜单隐藏+守卫 /no-auth 403）；造数外科手术式还原（删 4 事件+setval 归 1+按 biz_type 删测试审计 id42-49，他类审计完好 max=36）；ES reindex 对账治理仍留 Backlog |
| P5-B33 | 2026-09-11 | 组织树写能力整建两卡（闭合大表「组织树 🔧 待接线收口」；**本行为 B34 补登，B33 当批时间线漏登**）：卡A 后端（OrgUnit 扩 leader_name/headcount/remark/inactive_reason 四列 ddl-auto；OrgAdminController 三写端点全挂 org:manage——新建仅门店下建部门、parentCode 非门店中文 400、orgCode 服务端 toUpperCase 归一+唯一校验、orgType 固定「部门」不收前端值、region/store_code 由父门店继承推导；编辑三态语义（缺省=保持/空串=清空/非空=更新）、orgCode/orgType 传了也忽略、parentCode 仅部门可下发、跨店移动联动改写 store_code+region；启停 reason trim 非空必填、启用强制清空 inactive_reason、同态幂等成功且不重复写审计；DataScope 走 assertManageable **越权中文 404 而非 403** 防编码枚举探测；ORG 四 action 合法 JSON 审计；GET /api/org/tree 补四富化字段，该端点仅 org:view 不做 DataScope（组织目录全局可见为既有设计，隔离由写端点兜底））；卡B 前端（T1OrgView 去 mock 接真实 API 并收窄写边界：树头「新建部门」、上级下拉仅列 6 门店、类型 disabled、编辑态编码/类型置灰、全程无状态字段、状态只走启停弹窗且原因非空才解锁「确认停用」、启用弹窗无原因字段、后端中文 message 直出、提交期禁用防重复；m1Org store 重写 205 行：/tree 裸根 flatten、三命令 async 且成功后强制回源刷新不做乐观更新、仅部门下发 parentCode）；`<style scoped>` diff 0 行 | `c257165`、`079d4b9` | DELIVERY-P5-B33 | 组织树 1 模块 🔧→✅（72→73/166=44%，🔧5→4；组织树大表已有独立行，本批为状态跃迁不加行）；网关 curl 19 项断言（编码大写归一/重复编码 400/非门店父 400/三态编辑/跨店联动 region/停用缺因 400/启用清因/同态幂等审计不增/SE001 越权两条中文 404）+浏览器 6 组断言（写边界收窄/必填校验/小写 d-b33ui→D-B33UI/编辑置灰/跨店移动 SST01→SST06/停用带因→启用清因整块消失）双轨全绿；造数全量还原（删 2 部门+带 max(id) 保护谓词删链尾 ORG 审计 id79-88，精确回 org_unit=11/audit_log=36/max=36/ORG=0，verify `{ok:true,total:36}`）；门店/大区/集团层级写能力与兼岗上级路由留 Backlog |
| P5-B34 | 2026-09-12 | 技术债清偿两卡（**夜间自主决策窗 02:0x–03:0x 执行，逐条理由见 DELIVERY-P5-B34 §5**）：卡A BOM 扣料异常缺料明细全链路贯通（store-service `InsufficientStockException` 携 `shortages[]`（skuCode/skuName/needQty/stockQty/unit）→ `@ExceptionHandler` 转 **422** `{message,shortages[]}`，选 422 而非 400 以区分「业务前置条件不满足」与「参数校验失败」；txn-service `extractShortages` 解析写 `detail_json` + 审计 `shortageLines`；InventoryView 异常详情渲染缺料明细行；**附修 `substring(exc_id from 13)` 起始位错致当日序号恒为 1**——BEX+8 位日期+连字符共 12 字符前缀）；卡B 券核销兜底门店配置化（marketing_cfg 新增 `writeoff_fallback_store_code varchar(32)` 可空列（ddl-auto）+ 写入前门店存在性校验中文 400 / 空白归一 null / 同值不写冗余审计；`CouponWriteoffService.currentStore()` 改三级链（登录门店→配置兜底门店→store-service `/internal/first` 首家真实门店→**三级落空抛 503 拒绝核销**），**消灭 core 库不存在的硬编码 SST01 幽灵门店码**（历史脏数据 WR20260908-000004~000007 为证）；`MarketingCfgDataInitializer` 刻意不回填（core/seed 编码体系不同，任何播种值必有一侧对不上）；前端 /m5-settings 审批流分组新增「券核销兜底门店」CSelect（选项取真实门店表，首项「未配置（自动取门店表首家）」），m5Settings store 六处同步保证 dirty diff 与审计卡一致）；`<style scoped>` diff 0 行 | `73e4b7c`、`9a82af6` | DELIVERY-P5-B34 | BOM 异常 1 模块 🔧→✅（73→**74/166=45%**，🔧4→3；域④ 收银与交易 ✅10→11、🔧1→0 **交易域 🔧 清零**；卡B 为券核销/营销设置两行纵深不加行）；网关 curl 轨（422 含 shortages 五字段、BEX20260911-000001/000002 序号递增、假门店码 400、真码 changed:true、同值 changed:false 且 3 次 POST 仅 2 条审计、三级链分别落 ST-SH-002/ST-SH-001/E005 本店、E005 与 E014 多门店隔离）+浏览器轨（/m5-settings 保存 ST-SH-003 二次确认→saveDisabled 回 true→审计卡「核销兜底门店 未配置→ST-SH-003」→落库对账→再核销得 WR20260911-000004 上海虹桥店 status=DUPLICATE，**证明三级链在正常态与异常态 saveAbnormal() 两条写路径上均生效**）双轨全绿；配置项经 API 复原为 NULL（让复原也留审计），4 条核销流水与 4 条审计属真实业务写入 append-only 如实登记不删；**偏差如实披露：take_screenshot 的 filePath 受 workspace roots 白名单限制、docs/ 不在其中，本批无法产出截图，改用可访问性快照文本存档**；SST01 其余 5 处残留与 GET/POST 字段名不对称（weeklyPushLimit vs weeklyLimit）登记 Backlog |
| P5-B35 | 2026-09-12 | 充值赠金收口一卡（闭合大表域⑤「充值赠金 🔧」自 B18 起的下游缺口与 B26 明示 Backlog「赠金收银台抵扣 VALID→USED」，用户拍板三决策：UI=支付方式增一项磁贴/不足处理=本批做混合支付/造数=走真实端点）。marketing-service：新增 grant_deduction 实体+仓储（change_type DEDUCT/REFUND、balance_after_fen、biz_ref 锚订单、idem_key 唯一），CustomerGrant 增 USED 状态，GrantService.deduct FIFO 按到期先后悲观锁带锁扣减（多笔赠金按到期早→晚分摊）、idemKey 幂等重放、超额中文 422、GRANT_DEDUCT 合法 JSON 审计；新增 InternalGrantController（/internal/grants/balance、/deduct，X-Internal-Token，不对外）。txn-service：PaymentService METHODS 增 grant（散客单中文 400、同单第二笔 grant 中文 409 限一单一次、失败透传营销域中文 message、幂等键=订单号+支付序号），M4FlowController 增 GET /order/{no}/grant-balance（**cashier:view** 权限经交易域转发，FRONT_DESK 无 marketing:view；散客/营销不可用降级 available:false 不阻断收款），新增 MarketingGrantClient（RestTemplate、marketing.service.url 配置化、internal token、异常降级）。前端 OrderView **template/style 零改动仅 script 7 处**：order.ts 增 getOrderGrantBalance/GrantBalanceDTO，PAY_METHODS 增「营销赠金」磁贴复用 finance 图标，选中查余额 toast 引导「不足部分用其他方式补收」，提交前零余额/超额两道拦截，混合支付明细/横幅/KPI/tab 计数全联动。基础设施附修 docker-compose.app.yml+seed.yml 漏配 MARKETING_SERVICE_URL（txn 按默认 127.0.0.1 调用 Connection refused 的运行态真实缺陷） | `21d90df` | DELIVERY-P5-B35 | 充值赠金 1 模块 🔧→✅（74→**75/166=45%**，🔧3→2；域⑤ 表头 11✅1🔧→12✅0🔧 **营销域 🔧 清零**；大表已有独立行状态跃迁不加行，沿用历史计数差口径）。三轨全绿：marketing 84 单测全绿；网关 curl 轨（M003 OD20260820-000002 ¥1580 grant500+wxpay1080、超额 422、散客 400、同单第二笔 409、幂等重放、VALID→USED 余额 0、四表+审计对账）；浏览器轨 E006（M001 OD20260912-000001 ¥2000：网络 reqid=53 grant-balance 200 {balanceFen:60000,available:true}→赠金 600「部分收款成功，待收 ¥1,400」→微信 1400「收款完成」，KPI 待收 1→0/已完成 3→4/今日已收 ¥2,880→¥4,880，PM...003 grant60000+PM...004 wxpay140000、grant_deduction DEDUCT 60000/余 0、审计 6 条含 GRANT_DEDUCT/FUND_ENTRY system 流水）。**偏差披露**：TRAE-browseruse 扩展 Connection refused（os error 61）改用 Chrome DevTools MCP；截图 filePath 受 workspace roots 限制无法落盘，以工具内目视+快照文本+网络 reqid 存档；两笔已收款验证单据与规则 GRT20260912-000001 属真实业务写入 append-only 保留、报用户拍板是否清理；消费满额自动发赠金（issueByRule 无消费事件调用方）与 txn 扣减侧单测覆盖登记 Backlog |
| P5-B36 | 2026-09-12 | B34 技术债清尾批四卡（**白天自主推进，纯还债不增完成模块 75/166=45%、🔧 2 不变**）：卡1 SST01 幽灵门店码 5 处残留清零（`stores/storeContext.ts` 默认兜底改读真实门店首家并更正 B34 路径误记 utils→stores、核销/资产两播种器加 seed 门店体系存在性门控、`m5Core.ts` mock 删除，4 文件 +54/-96）；卡2 `/internal/first` 排序契约化（StoreRepository `findFirstByOrderByStoreCodeAsc`，store_code 字典序升序首家+空库 404+Token 鉴权断言测试）；卡3 StoreNameResolver 故障分层（新增 StoreServiceUnavailableException，resolveNames 宽松降级/resolveNamesRequired 严格 503 vs 400 双方法，MarketingCfgService 写校验改严格版，券名回显/资产放行两处消费方经核实零改动）；卡4 txn-service 测试基建从零建立+PaymentServiceTest 7 例（散客 400/同单第二笔 409/售卡 400/deduct 先于落库 InOrder/422·502 不记账/混合尾款不重扣） | `8ddac05`、`3694003`、`a43d299`、`c0d3583` | DELIVERY-P5-B36 | 纯技术债/测试债，无模块状态跃迁（75 不变；勾销 §④ Backlog 4 行：SST01 残留清理、WR 脏数据登记更正、StoreNameResolver 分层、/internal/first 契约）。marketing **93 测试全绿**+store-service 卡2 测试全绿+txn **7 测试全绿**；卡3 真实容器三分支（假码 400/真码 200 changed=true/停 store-service 503 旧逻辑误报 400 实证），验证后 store-service 重启 healthy、fallback 配置 API 复原 null；**存量污染更正**：WR20260908 实测 7 条 000001~000007（B34 误登 4 条 000004~000007）+WR-SEED 4 条+marketing_asset SST 2 条，append-only 待业务确认不擅删 |
| P5-B37 | 2026-09-12 | 营销设置收口批两卡（**白天自主推进，75→76/166=46%**）：卡1 marketing_cfg 读写 DTO 契约统一（GET 不再直出 JPA 实体，新增 ConfigView 与写命令 ConfigCmd 字段/类型完全对称：周频键统一 weeklyPushLimit 废弃写命令旧 weeklyLimit 破坏性改名、前后端同卡同步，defaultPushChannels/defaultAdChannels 服务层解析 JSON 两端同为数组，视图物理剔除 cfgId/referralArrivedReward/referralDealReward/commissionRate 四个与 M5-15 无关的多泄遗留列，保留内部 get() 供核销兜底/频控/quota 读取，审计快照随写链改新键+前端对 B37 前 append-only 旧快照 weeklyLimit 读时归一，quota `/push/quota` 出参 weeklyLimit 为独立前端契约保持不动；6 文件 +219/-38，Controller 切片+Service 坏 JSON 等单测、vue-tsc/vitest 50 绿、容器 GET/POST/幂等/硬约束/落库/审计新键取证）；卡2 消费满额自动发赠金（闭合 B35 登记 Backlog「issueByRule 无消费事件调用方」：**路径选择轮询而非 ROADMAP 原文的 FUND_ENTRY 事件链路**——复刻 B25 AutoPoints 同款 txn `/internal/paid-orders` 拉取范式，新增 AutoGrantService/AutoGrantJob（fixedDelay 5min initialDelay 45s）/TxnInternalClient（X-Internal-Token）/auto_grant_state 单行游标表（ddl-auto 建表，NULL 首跑自 2000-01-01 UTC 全量回填，yyyy-MM-dd 日粒度邻轮重叠窗口）/TxnServiceUnavailableException；CONSUME_THRESHOLD 启用规则按 priority 升序首条命中一单不叠多档，applicableStores 空白=全部门店否则精确匹配；**双层幂等** idem_key `RULE:{ruleId}:{orderNo}` + existsBySourceBizRef 订单级跨规则锚点；故障分层 txn 不可用/客户域 502 整轮中止不推进游标、客户不存在 400 跳过推进、CARD_SALE/空客户/非正额跳过；明细审计复用 issueByRule（GRANT_ISSUE/RULE，调度线程 actor 回落 system）+Job 仅 granted>0 补 AUTO-GRANT/SYSTEM 汇总；**附修既有跨域潜伏缺陷 audit_log.txn_no VARCHAR(24)→128**：规则明细审计号 `RULE:{ruleId}:{orderNo}` 实测 40 字符致 audit-service PG 22001 insert 500（RestAuditRecorder 仅 log.error 不阻断主流程故赠金落账但明细审计静默丢失，B35 短手工号从未触发），实体 length+现网 ALTER TABLE+基线 db/schema.sql 三处修复；10 文件 +703/-1，AutoGrantServiceTest 11 例+AutoGrantJobTest 3 例） | `828ac6d`、`7552665` | DELIVERY-P5-B37 | **营销设置 🔧→✅**（75→**76/166=46%**；营销设置行原游离于仪表盘 🔧 计数差中，随本批「随营销规则引擎整体收口」条件满足正式勾销；卡2 为「充值赠金」既有 ✅ 行纵深闭合不加行，沿用历史计数差口径；域⑤ 12✅ 不变）。marketing+audit 全量单测绿；**容器端到端真验**：现存百万门槛规则首跑扫描 5 发放 0 零资金风险→真实端点造临时规则 GRT20260912-000002（满 50000 赠 2000 限 ST-SH-001）→全量回填**扫描 5 发放 4**（M001×2/M002/M003 命中，50000 分边界 `>=` 命中，M005 ST-SH-002 被门店限定排除，expire_at +3 月正确）→明细 RULE/system 4 条+AUTO-GRANT/SYSTEM 汇总双审计齐全→三轮自然调度 scanned=1/granted=0 幂等零新增、赠金与 RULE 审计均不增→基线恢复（临时规则 DISABLED 行保留、4 笔测试赠金删除、audit_log append-only 凭证留存、auto_grant_state 存活）。**退款回加赠金明确不做仍 Backlog**（需定义已抵扣/已过期语义）；marketing_cfg/auto_grant_state/audit_log 版本化 Flyway 基线留中期治理 |
| P5-B38 | 2026-09-12 | Flyway 基线批（**白天用户拍板「B38 Flyway 基线批（推荐）」后执行，纯基建还债，完成模块数不变 76/166=46%、🔧 2 不变**）：卡1 audit-service 接入 Flyway（`6f2e734`，pom flyway-core 走 BOM 不写版本/Community 9.22.3、application.yml 同款共享链配置 enabled·locations classpath:db/migration·baseline-on-migrate·baseline-version 0·validate-on-migrate·ignore *:missing+*:future，**自主增开 out-of-order:true** 共享链补洞防 V11 先部署高版本占位卡死 V10；V10__audit_log_baseline.sql 以 `ALTER TABLE IF EXISTS ... ALTER COLUMN TYPE` 补登 B32 biz_type 16→32、B37 txn_no 24→128 两次历史手动 ALTER，现网重设同宽 no-op、全新库表不存在 NOTICE skipping 留给 Hibernate 按实体 length 建；db/schema.sql biz_type 漂移校准）；卡2 marketing-service 接入 Flyway（`2b03c46`，同款 pom/yml 无 out-of-order；V11__marketing_cfg_baseline.sql 以 `CREATE TABLE IF NOT EXISTS` 纳管 marketing_cfg 15 列含 cfg_id PK default 1 + auto_grant_state 5 列两表，现网 42P07 already exists skipping，其余 16 张营销属表维持 ddl-auto=update 后续按域分批纳管；db/schema.sql marketing_cfg 全列+主键、auto_grant_state 缺失两处校准，db/ 被 gitignore 校准不进提交） | `6f2e734`、`2b03c46` | DELIVERY-P5-B38 | 纯基建还债无模块状态跃迁（76 不变、🔧 2 不变；勾销 §④ Backlog 两行：marketing_cfg 等无 Flyway 迁移脚本、audit_log biz_type 基线漂移；新增「其余 16 张营销属表分批纳管」一行）。四轨真验：①共享链扩至 **V0(baseline)–V11 共 12 行全 success**（V10 rank11 19ms、V11 rank12 18ms）；②数据无损 audit_log 387 行/marketing_cfg 1 行 15 列/auto_grant_state 1 行；③两服务各自重启与同时重启均 validated 12 migrations、up to date、healthy（跨服务 *:future 忽略实证：audit 类路径只持 V10 仍通过含 V11 全链校验）；④全新库探针 b38_fresh_probe 实测 V10 两条 NOTICE skipping、V11 建出两表 \d 结构与现网 pg_dump 完全一致，验完 DROP DATABASE；audit/marketing mvn test 全绿。**踩坑**：backend/Dockerfile 只 COPY 宿主 fat-jar 镜像内不跑 Maven，改 pom 后须先 `mvn -pl <svc> package` 再 compose build（卡1 只 compile 致镜像旧 jar 迁移未执行，已踩已修） |
| P5-B39 | 2026-09-12 | 退款终审联动赠金逆向回加一卡（**白天自主推进，勾销 B37 登记 Backlog「退款回加赠金（消费满额规则的逆向链路）」，76→77/166=46%**。用户先拍板三决策：范围=仅链路①付款回加（链路②满赠门槛追回留 Backlog）、过期券=回加但保持 EXPIRED、分摊=赠金→卡本金→法币。marketing：GrantDeduction 增 origin_biz_ref 列（ddl-auto，随 B41 V12 Flyway 纳管），仓储增 findByBizRefOrderByIdAsc/sumRefundedByOriginGroupByGrant/findByIdInForUpdate（PESSIMISTIC_WRITE）；GrantService.refund() 六步：原单累计封顶校验（锁前 422「原订单可回赠金不足：可回 X 元，本次需回加 Y 元」）→悲观锁逆向 FIFO（USED→VALID 复活/EXPIRED 保态/VALID 累加）→REFUND 流水（balance_after 双锚 biz_ref=RF、origin_biz_ref=原单）→saveAll 唯一冲突重查幂等→GRANT_REFUND 审计 actor=system/payload operator=真实终审人；InternalGrantController 增 POST /internal/grants/refund（X-Internal-Token，返回 bizRef/refundedFen/grantCount/balanceFen）。txn：FinanceEventPublisher 增 refundSplitForOrder 三段拆分（grantPart=min(原单 grant 实付,退款额)、balancePart=min(卡本金实付,余)、cashPart 余数，与 outbox emitRefundConfirmed 共用口径，grant 段空 cmds 跳过不产生 finance_event）；TxnService.confirmRefund 终审事务内 grantClient.refund→cardClient.refundForOrder 先于置 REFUNDED/审计/outbox（InOrder），远程全在置态前失败整笔回滚单据停留 PENDING_FINANCE；MarketingGrantClient.refund 4xx 透传中文 message/5xx·IO 转 502「本笔操作已回滚，未扣款未记账」） | `5353bd9` | DELIVERY-P5-B39 | **退款回加赠金 Backlog ⬜→✅**（76→**77/166=46%**，⬜ 88→87；域⑤ 12✅6⬜→13✅5⬜；大表已有独立 Backlog 行勾销不新增行，沿用历史计数差口径）。单测 **13 例全绿**（GrantServiceTest 8：全退复活/部分退累计/EXPIRED 保态/超额 422/RF 幂等/多券 FIFO/无赠金原单 422；TxnServiceTest 5：三段拆分/纯 grant 零事件/终审 InOrder/停营销回滚/4xx 透传）。容器真验四组全过：①RF20260912-000001 全额混合退（grant id=3 复活 60000 VALID、deduction id=4 REFUND 双锚、finance_event 仅 1 条 wxpay 140000 RF-REFUND、CONFIRM 审计 grantRefund 60000/cashRefund 140000）；②RF20260912-000002 纯赠金段退 50000（grant id=1 复活、max(event_id)=13 零新增、grantRefund 50000/cashRefund 0）；③内部接口重放 RF...001 原样返回不双加（流水/余额/审计三查）+RF20260912-CAP1 同 origin 请 100 分 422「可回 0.00 元，本次需回加 1.00 元」；④docker stop marketing 终审 RF...003 →502 回滚文案，单据停留 PENDING_FINANCE/无 REFUND 流水/事件不增/仅 SUBMIT 审计四项零写入，恢复 18s healthy 后重试得 422（故障窗口重试安全、单据仍悬挂可驳回）。**真验数据待拍板**：RF...001/002 REFUNDED 与回加赠金是否冲正、RF...003 建议 reject 结案留故障证据。**余 Backlog**：链路②满赠门槛追回、卡 gift_balance RF 不回补登记、B35 grant 渠道正向 DEAD 事件、origin_biz_ref 随 B41 纳管 |
| P5-B40 | 2026-09-12 | 存量 SST 脏数据全库确认与处置一卡（**白天自主推进，四问请示被 skip 后按铁律 -1 采纳全部推荐项，纯数据治理/技术债，完成模块数不变 77/166=46%、🔧 2 不变**）。全库 PL/pgSQL DO 块遍历 information_schema 字符/JSON 列扫描，定性污染 **11 表 175 行+audit_log 36 行取证快照**：①根因修复 `5ef75db`——store-service 三个 ApplicationRunner（StoreBomDataInitializer @Order50 耗材 36 店×6 SKU 副作用写 stock+PURCHASE、StoreMasterDataInitializer @Order60 9 房 16 床 8 设备 11 维保、StorePriceCatalogDataInitializer @Order70 SST01 11 价目）自 2026-09-06 19:11 起无门控全环境播种（该服务无 Flyway、ddl-auto=update、默认数据源即 meiyun_core；B36 仅修 marketing 两侧漏了 store），按 CouponWriteoffDataInitializer 范式注入 spring.datasource.url 补 meiyun_seed 早退（门控位于 count 幂等判断之前），集团项目配方/品牌品类 SKU 目录/卡项目录保持全栈播种；②数据处置——8 表 162 行幽灵数据零外键零预约零订单引用，删前 8 份 CSV+全表 pg_dump 留档（scripts/backup-b40-sst-20260912/，backup-*/ gitignored），单事务物理删除，audit_log 36 行 append-only 保留；2 海报 AS-SEED-002/009 保留本体改 scope=ALL/store_codes=[]（原值 JSONL 留档）；11 脏核销全保留（8 条 0 元 FORGED/DUPLICATE 风控证据、3 条 OK 1140 元挂账待业务定门店）。**踩坑**：Dockerfile 仅 COPY 宿主 fat-jar，首镜像构建于 mvn package 之前，`compose restart` 不换镜像，空表反被旧播种器重播 55 行（room9/bed16/equip8/maint11/price11）；修复链 mvn clean package→javap 解压 BOOT-INF/classes 复核三类含 meiyun_seed（macOS strings 对 .class 假阴性）→重建镜像→单事务二次删 55 行→force-recreate | `5ef75db` | DELIVERY-P5-B40 | 纯数据治理无模块状态跃迁（77 不变；更新 §④ Backlog「存量脏数据」行：162 行已物理清除+门控修复、资产 2 条转 ALL、1140 元 3 单挂账待业务）。容器真验：三条「非种子库…跳过」日志齐出（耗材/房间床位设备/SST01 价目），五表重启后零重播，audit_log 保持 36 行重播不增审计实证，品牌 9/项目配方 9/catalog_product 8 集团数据完好，store-service healthy；consumable/stock/movement 各仅剩 2 套 ST-SH-001 B34 复验真实行 |
| P5-B41 | 2026-09-12 | 16 张营销属表 Flyway 纳管一卡（**白天自主推进，B38 通道接通后的纵深收口，纯基建还债，完成模块数不变 77/166=46%、🔧 2 不变**。勾销 B38 登记 Backlog「其余 16 张 JPA 属表分批纳管（首个版本号 V12）」）。以现网 `pg_dump --schema-only` 物理结构（留档 gitignored b41-16tables-schema.sql 712 行）为唯一依据 1:1 转写、不按 JPA 实体理想态重画，保留中文枚举 CHECK（coupon_writeoff_chain.segment 正常/异常/待处理）、live_session.start_time 无时区 TIMESTAMP、push_record FK→customer+表注释、GrantRule.applicable_stores TEXT、MarketingAsset tags/store_codes VARCHAR512 存 JSON 文本非 jsonb、uk_cg_idem/uk_crb_chan_bizref/uk_forbidden_word_category_word/uk_gd_ref_grant 四唯一约束与 4 索引；B39 新增 origin_biz_ref 随 V12 grant_deduction 收录。**V12** 券赠金条线 8 表（coupon_template/coupon_grant/campaign/coupon_writeoff_record/coupon_writeoff_chain/customer_grant/grant_rule/grant_deduction）；**V13** 渠道素材违禁词 3 表（channel_returnback/marketing_asset/forbidden_word）；**V14** 海报短视频直播触达 5 表（poster_template/poster_record/short_video/live_session/push_record，PushSchemaInitializer 历史自愈的 push_type 扩宽/drop 旧 CHECK/补 dedup_key 直接固化目标态）。一律 CREATE TABLE/INDEX IF NOT EXISTS，不对存量表 ALTER（缺列交 ddl-auto=update 净兜底，本批比对无缺列） | `3166d97` | DELIVERY-P5-B41 | 纯基建无模块状态跃迁（77 不变；勾销 §④ Backlog B38「16 张属表分批纳管」行，新增 ddl-auto update→validate 中期评估行）。双场景真验：①现网——backend/ 目录 `mvn -pl marketing-service clean package -DskipTests`（规避仓库根带 -am 的 MavenExecutionException 旧坑；fat-jar unzip 实证含 V12~V14，57,474,678 字节）→compose build→force-recreate，日志「Successfully applied 3 migrations … now at version v14」，共享链扩至 **V0–V14 共 15 行全 success**（rank13/14/15 各 33/11/12ms），16 表全 NOTICE skipping，迁移前后真实 count 逐表对照零变更（template8/grant4/campaign3/writeoff16 含 SST 11/chain3/customer_grant2/rule2/deduction4/channel0/asset10/word24/poster_tpl6/poster6/video5/live7/push10；n_live_tup 为陈旧估计须以 count(*) 为准），origin_biz_ref 列在，marketing healthy；②全新库——临时库 b41_fresh_scratch 裸回放 V1→V3（customer 历史 DROP+建券三表）→V12（3 表跳过+5 表新建，实证 customer V3 与 marketing V12 IF NOT EXISTS 共存、执行顺序无关）→V13→V14：首报 relation "customer" does not exist 为预期非缺陷（customer 表由 customer-service Hibernate 建不在 V1 init，生产 compose 已固化 marketing depends_on customer healthy），补 customer_id VARCHAR16 PK stub 重放 EXIT=0，16 表+FK+注释核对齐全后 DROP DATABASE。marketing 全 18 张属表 100% 纳管 |
| P5-B42 | 2026-09-12 | 后台 AI 中心模型配置地基一卡（**用户拍板火山方舟 ARK 四要素：OpenAI Chat Completions 兼容格式、base_url `https://ark.cn-beijing.volces.com/api/coding/v3`（不以斜杠结尾，/chat/completions 自动补尾）、模型 ID `ark-code-latest`、展示名 `ark-code-latest-coding`；Key 用户自持不入库代码，页面录入 AES-GCM 密文落库**；一期薄适配零重型 AI SDK）。后端新建第 8 个服务 **ai-service**（Spring Boot 3.2.5/Java17/端口 8089/包名 com.meiyun.ai，reactor 纳 backend/pom.xml）：LlmClient 基于 RestClient 的 OpenAI 兼容薄适配（chat 拼 /chat/completions+Bearer+解析 choices[0].message.content 与 usage 三 token；ping 单条 "ping"、temperature 0、maxTokens 8）；共享 Flyway 链 **V15__ai_model_platform.sql** 七表（ai_provider/ai_model/ai_feature_binding/ai_feature_role/ai_invoke_log/ai_approval/ai_global_cfg，CREATE IF NOT EXISTS，双栈双库均已应用）；六组 REST 端点（providers 5、models 6 含 POST /{id}/test 真实连通性、features 4、cfg GET/PUT、logs/logs/bill/logs/kpi、approvals 分页+decide）写接口四件套齐全；ApiKeyCipher AES-GCM（主密钥 env AI_CONFIG_SECRET，密文/明文不进审计与日志，页面只回掩码 sk-p****rify 与 hasApiKey）；审批联动 MODEL 通过→enabled=t、BINDING 驳回→linkage=NONE、重复决策 400 中文；FeatureCatalog 六功能×五角色矩阵启动播种。网关 routeTable 增第 8 前缀 `/api/ai`→AI_SERVICE_URL（默认 127.0.0.1:8089），docker-compose.app.yml+seed.yml 双栈补 ai-service 与 gateway depends_on/env。前端：新增 /ai/providers 模型接入页（供应商/模型 CRUD、Key 掩码录入、真实连通性测试按钮、六功能绑定与角色灰度矩阵）+api/ai.ts 19 端点，nav.ts/router/PAGE_TITLES 三处注册；A1Gateway/A1Admin/A1Govern 三页全部去 mock 接真实端点；PermissionMatrix 给 REGION_MGR 补 aiAdmin:edit+aiGovern:approve（org 改权限须重签 JWT）。**踩坑**：api/client.ts 响应拦截器 `(resp)=>resp` 不剥 data（与 api/request.ts 不同），api/ai.ts 19 端点必须统一 `.then(r=>r.data)`，漏解包运行时报 map is not a function | `7ea662d` | DELIVERY-P5-B42 | **模型接入+网关监控+管理配置+审批治理 4 页 ⬜→✅**（77→**81/166=49%**，⬜ 87→83；域⑧ 平台与基建 4✅→**8✅**、62⬜→**58⬜**；大表原「A1 AI 中心（15 页）⬜」单行细化为 4✅+余 11 页⬜）。双栈真验：curl 经网关 8443 六组端点+PG 七表数据对账+AI 审计哈希链完整；四页面经 8080 浏览器真验零 console error/warn（MCP Chrome 无法过 8443 自签证书 ERR_CERT_AUTHORITY_INVALID，走 nginx 8080）；占位数据（假 Key 供应商 FAIL 态/6 绑定/5 矩阵/配置/2 已完结审批）保留 app 库不删、seed 库干净；真实 ARK Key 的 SUCCESS 连通待用户在 /ai/providers 自行录入后复核（不索要明文）；ai_invoke_log 真实调用沉淀待 B43 |
| P5-B43 | 2026-09-12 | 六功能真实 invoke 出口闭环一卡（B42 四个 ✅ 页面的纵深闭合，无新页面；用户已在 /ai/providers 录入真实火山方舟 Key 并连通通过）。后端 ai-service：新增 FeatureInvokeService+FeatureInvokeController——**POST /api/ai/features/{featureCode}/invoke** 无类级 @RequirePerm，服务内逐功能角色矩阵鉴权（六功能各自角色 Map，未授权 401/403 中文），前置链：功能存在 404→绑定存在且 enabled 409→输入非空 400→门店灰度 ALL/SPECIFIED 域校验→绑定模型存在/enabled/含 CHAT 能力→Key 引用可解密→参数覆盖（binding.paramOverrides 的 temperature/maxTokens 合并默认值）；LlmClient.chat 真实 OpenAI 兼容调用，**成功与失败均沉淀 ai_invoke_log**（model/snippet 200 字/tokens 三量/cost_fen/耗时/error_code）；**invoke 方法刻意无方法级 @Transactional**，保证出站调用失败时失败日志在独立事务提交、不随 502 回滚。迁移 **V16__ai_invoke_log_error_code_extend.sql**（共享 Flyway 链现网已应用，DO 块 information_schema 判宽幂等 ALTER error_code→VARCHAR(512)，纳下 ARK 上游长错误消息）。实体两处可写性修复：AiInvokeLog.error_code length 512；AiFeatureBinding.updated_at 去掉 insertable=false/updatable=false，FeatureService.saveBinding 仅在逐字段 diff changed=true 时显式 setUpdatedBy/setUpdatedAt+save+审计（全相等 `{"changed":false}` 幂等短路、不刷时间不写审计）。前端：api/ai.ts 增 invokeFeature（timeout 60s）；A1AdminView 绑定抽屉增「试运行」区（输入框+发起真实调用+调用中置灰，结果胶囊状态/模型/耗时/token 三量/费用+content `<pre>` 回显），成功后 Promise.allSettled 刷新 KPI 与本月账单；A1GatewayView KPI/API 目录今日调用/调用日志全部真实联动；AiProvidersView 计费口径 3 处文案统一「元/百万 tokens」。**运行态修复第四缺陷**：bindRows computed 曾在 `...f` 展开后用同名 `storeScope:'全部门店'` 中文串覆盖原始枚举，CTable 作用域插槽把污染行传 openBind 致门店灰度 CSelect 严格 === 匹配 ALL 失败回显「请选择」——展示字段改名 storeScopeText（列 key 同步），原始 storeScope 随展开保留 | `70cac29` | DELIVERY-P5-B43 | **六功能真实 invoke 出口 ⬜ 纵深→✅**（81→**82/166=49%**，⬜ 83→82；域⑧ 8✅ 不变、58⬜→**57⬜**；无新页面，B42 页面行纵深更新不新增行，沿用历史计数差口径）。三轨真验全绿：①curl 经 8443 网关全状态码矩阵 401（无 token）/400（空输入）/403（角色不匹配）/404（功能不存在）/409（绑定停用）/502（ARK 上游 400 temperature 长消息）+两次真实成功，失败独立落库不回滚实证；②PG ai_invoke_log 4 行（3 成 1 败，snippet/error_code 长消息/token/cost_fen 齐全）；③Chrome 经 8080 页面真实试运行成功（ark-code-latest、13013ms、提示 83/补全 414/合计 497 tokens、¥0.00、三条中文画像标签），KPI 本月用量调用后 3→4 实时联动，网关页 KPI 今日 4/成功率 75%/P99 38391ms/活跃告警 0（硬编码待 B44）、API 目录 invoke 今日 4 正常、调用日志 4 行含失败上游消息；storeScopeText 修复后三 CSelect 全部正确回显；changed:true 真实保存实证 updated_at 刷新（DB 23:14:23.54801+08/updated_by E011，验证后参数还原 maxTokens=500）。**观察项登记 B46**：ark-code-latest 为推理模型不完全遵守 max_tokens（497 tokens 超出 414 补全上限口径需计费评估）；后端 OffsetDateTime 序列化 UTC，前端 updatedAt 直接字符串切割显示 15:14 未转本地 +08 时区 |
| P5-B44 | 2026-09-12 | AI 出站治理二期闭环一卡（配额限流+敏感词/越权过滤+监控告警，B42 四个 ✅ 页面内新增 tab，无新页面）。迁移 **V17** 三表：ai_quota（scope FEATURE/MODEL/GLOBAL+target 唯一，日/月额度 NULL=不限，**只存额度不设计数列**，用量以 append-only ai_invoke_log 按北京时间日/月窗实时 count 为唯一真相，MODEL 无 DB 行返回 quotaId=null 虚拟默认视图首次 save 建行、跨供应商同 modelCode 合并计数）、ai_sensitive_word（BANNED/INJECTION 两类 13 词种子）、ai_alert_rule（LATENCY_P99/ERROR_RATE/CALL_COUNT/SUCCESS_RATE/QUOTA_WATERMARK 五规则种子，compare_op `>`/`<`）。出站拦截链在 Key 解密后 prompt 组装前：**灰度 403 → 敏感词 screen 400（命中词首尾脱敏回显，总开关缺省开）→ 配额 FEATURE→MODEL→GLOBAL 三级 429 中文「已用 X/Y 次」**，被拦截不写日志不占用量不产生费用；告警刻意无 @EnableScheduling，请求时按当日日志实时评估、三态（已停用/告警/正常），activeCount 联动网关 KPI activeAlerts。前端 A1Admin 增第 6 个「配额管理」tab（三级 8 行+水位三档色+调整抽屉留空=不限），A1Gateway 告警 tab 五规则真实化 | `336b7f8` | DELIVERY-P5-B44 | **纯纵深治理无模块状态跃迁（82/166=49%、⬜82、域⑧ 8✅2🔧57⬜ 均不变）**，勾销 §④ B44 Backlog。三轨真验：curl 敏感词两类 400 脱敏、灰度 403 先于敏感词、配额置 0 触发 429 且零落日志、恢复后真实 200（54736ms/2097 tokens）、水位 100% active；PG 三表+AI_QUOTA 审计对账；Chrome 网关告警红 P99+KPI activeAlerts=1、管理页 6 tab/8 行配额。观察项：P99 阈值 200ms 对推理模型偏严（阈值分级登记）、告警事件持久化与真实通知留后续 |
| P5-B45 | 2026-09-12 | 审批申请入口+联动启用+效果评估/A·B 实验实体化一卡（A1GovernView 页内纵深闭合，无新页面）。后端 ai-service：**POST /api/ai/approvals** apply（类型白名单 PROVIDER/MODEL/BINDING、内容非空≤512、MODEL/BINDING 目标必须存在且未启用、同目标 PENDING 幂等 400 中文，aiAdmin:edit），审批通过联动 MODEL→ai_model.enabled=t / BINDING→ai_feature_binding.enabled=t（审计 linkage=MODEL_ENABLED/BINDING_ENABLED，决策仍 aiGovern:approve 权限分离）。迁移 **V18** 两表：ai_eval_task（FEATURE/MODEL 维度+窗口 1~90 CHECK+RUNNING/DONE+指标 JSON 快照+结论）、ai_experiment（对照/实验双分组+**CHECK 强制对照≠实验**+双侧指标快照+lift_pp+RUNNING/FINISHED+结论≤512）；EvalService 八端点（evals/experiments 各 list/create/refresh/conclude + eval-stats），指标按 append-only ai_invoke_log **北京时间窗口**聚合调用量/成功率/P99/token/成本，lift=实验成功率−对照成功率 pp，KPI 平均提升仅统计已结案实验，全动作审计、结案冻结刷新。前端：新增 AiApplyDrawer 共享申请抽屉（模型行「上架申请」、绑定抽屉「提交审批」），A1Govern 效果评估 tab（新建/刷新/`window.prompt` 录结论结案）+A·B 实验 tab（models<2 拦截、对照≠实验拦截、双侧刷新、prompt 结案）全部实体化。**真验修复一处响应式缺陷**：KPI 卡片 kpis 原为 loadKpi 内 toKpis 一次性快照、结案只刷 evalStats 不重算致「平均效果提升」停留旧值（接口实回 0.00），拆 baseKpi ref+kpis computed 派生计时联动 | `f74d9c8` | DELIVERY-P5-B45 | **纯纵深闭合无模块状态跃迁（82/166=49%、⬜82、域⑧ 8✅2🔧57⬜ 均不变）**，勾销 §④ B45 Backlog。三轨真验：Chrome 两类申请→审批通过→联动启用（审批待办清零、本月审批 4、benchmark/churn enabled=t 且按钮联动消失），评估 91 天拦截/7 天创建指标快照（5 调用/80%/54.09s/5284 tokens）/prompt 结案冻结，实验同模型拦截/异模型创建（-80 与 +80 两案）/双侧刷新/prompt 结案、KPI 均值 0pp；PG ai_approval #5#6 APPROVED、audit 452–460 含 linkage 且 AI 区段 79 条哈希链全连续（仅 id=380 旧遗留断链无关）；curl 登录与模型价格置空 changed:true。另证伪「价格清空失败」为 MCP fill 空串不触发 v-model 的工具限制（产品 numOrNull→BigDecimal null 差量置空正确，真实键盘清空落 NULL）。业务转化/ROI 无埋点不伪造、前端 UTC→本地时区统一转 B46 |
| P5-B46 | 2026-09-13 | 敏感词词库管理+命中留痕落库+敏感词检测页去 mock/前端时区统一一卡。迁移 **V19** 两动作：新建 ai_sensitive_hit 命中表 14 列（hit_id 序列/hit_at timestamptz default now()/word_id 可空/word·category/feature_code/staff_id·staff_name/store_code/context_snippet(500)/false_positive default f/marked_by/marked_at，CHECK category IN BANNED/INJECTION，hit_at DESC·word_id·false_positive 三业务索引）+ai_sensitive_word 补 updated_at；函数 ai_set_updated_at() 为 8 张 AI 表（provider/model/alert_rule/feature_role/global_cfg/eval_task/experiment/sensitive_word）挂 UPDATE 触发器（同值更新亦刷新；quota/feature_binding 不挂）。**SensitiveWordController 六端点**（类级 /api/ai/sensitive+aiAdmin:view）：GET /words 全量 List（命中数 countGroupByWord 内存聚合，非 Spring Page、无 keyword）、POST /words 与 POST /words/{id}（edit，重复词中文 400、changed=false 空操作幂等不写审计）、GET /hits 真分页（category 三态过滤、size 夹 1~200、hit_id 倒序）、GET /hits/stats（今日按北京时间日界）、POST /hits/{id}/mark-fp（幂等重复标注不写审计）；AI_SENSITIVE_WORD CREATE/UPDATE、AI_SENSITIVE_HIT MARK_FALSE_POSITIVE 全动作审计 payload 合法 JSON。拦截链接入 FeatureInvokeService **L142**（Key 解密 L140 后、quota check L143 前）：BANNED/INJECTION 两类 400 中文拦截语+命中词首尾脱敏（≤2 字保留首字加 *），SensitiveHitRecorder @Transactional(REQUIRES_NEW)+snippet 截断 500，保证 400 同时命中行独立提交；总开关读 ai_global_cfg id=1 sensitive_check 缺省开。前端新增 utils/datetime.ts（UTC OffsetDateTime→浏览器本地时区，分钟/秒级两格式化，null/非法显 —），AI 中心 4 文件 7 处时间 slice 替换；A1SensitiveView 整页去 mock（4 KPI、命中 tab 词类三态过滤+分页+误报标注、词库 tab 抽屉 CRUD、重复词 400 保留），api/ai.ts 增 6 client | `d944a01` | DELIVERY-P5-B46 | **敏感词检测 1 整页 ⬜→✅**（82→**83/166=50%**，⬜82→81；域⑧ 8✅→**9✅**、57⬜→**56⬜**；大表新增独立 1 行；时区工具/触发器为既有模块纵深增强不加行）。三轨真验全绿：①curl 经 8443——无 token 401、重复词 400 中文、changed=false 空操作不写审计、hits 分页+stats 今日 2/总量 2、两类敏感词 400 脱敏拦截语、mark-fp 重复标注幂等；②PG——V19 共享链应用成功、命中行 400 时 REQUIRES_NEW 独立提交可查、updated_at 触发器同值更新亦刷新、audit_log payload word/category/enabled 合法 JSON 对账；③Chrome 经 8080——KPI 2/2/2、词库 15 词启用 13、词类过滤三态、时间列按浏览器本地时区显示。测试数据留存（word14/15 停用、hit1/2 均 false_positive=t，append-only 不删）。**页数口径差**：历史盘点余 11 页与实测 nav 中 12 个 mock A1 视图差 1（视图/页面粒度），后续按 12 视图切页，详见 §7；EMBEDDING/VISION 多能力、多供应商、token 账单环比、marketing ddl-auto validate、告警事件持久化、全站时区推广（余约 21 处 slice）登记 §④ |
| P5-B46 卡2 | 2026-09-13 | AI 智能中心首页整页去 mock 一卡（**纯前端切真，无新后端端点、无迁移、样式零改动**）。A1HomeView 原四 KPI/能力卡调用量/待办告警全为硬编码假数，onMounted 重构为**按权限分组门控**：aiGateway:view 组 Promise.allSettled 并发 logs/kpi、logs/bill（无参，后端默认当月）、alerts 三请求；aiGovern:view 组 approvals?status=PENDING&page=0&size=5（同一响应 **content 供前 5 待办列表、totalElements 供待审批 KPI**，一次请求双用，不依赖网关 KPI 端点、消除越权 403）；aiAdmin:view 组 sensitive/hits/stats（今日拦截/总量）。无权限分组整体不发请求、对应 KPI 显「—」。12 张能力卡保留产品元数据（名称/图标/跳转/生命周期标签），capCalls computed 按 monthlyBill rows 的 feature 键映射，仅六功能显「本月 N 次调用」、敏感词卡显「今日拦截 N 次」，无日志功能诚实隐藏不补假数；模板同步删除假数据里的 cap.calls 字段改 capCalls[cap.key]。待办流 todos computed=待审批前 5（**appliedAt** 经新增 fmtAgo 显相对时间，点击跳 /ai/govern）+敏感词今日命中条+active=true 告警前 5，汇总后 slice(0,6)，无数据显空态文案。**utils/datetime.ts 增 fmtAgo**（刚刚/N 分钟前/N 小时前/N 天前，≥30 天回落 fmtDateTime(iso,true) 本地时区绝对时间）。修复两处既有缺陷：onMounted 删除 govern-only 视图也无条件调 logKpi 的越权兜底；Promise.allSettled 任一端点失败不阻断其余分组渲染 | `8bee59d` | DELIVERY-P5-B46-2 | **智能中心首页 1 整页 ⬜→✅**（83→**84/166=约51%**，⬜81→80；域⑧ 9✅→**10✅**、56⬜→**55⬜**；大表新增「A1 · 智能中心首页」独立 1 行；A1 页数 5/15→**6/15**，实测 nav mock 视图 12→10）。三轨真验全绿：①curl 经 8443 五端点（E011 三权限齐备）logs/kpi 今日 2 调用/成功率 50.0/P99 60959ms/activeAlerts 3、logs/bill 当月仅 profile 6 次 5284 tokens/cost 0、approvals PENDING totalElements 0、sensitive/hits/stats 今日 2/总量 2（词库 15 词启用 13）；②PG 按 invoked_at/hit_at 北京时间日界八项对账一致；③Chrome 经 8080 E011 首页四 KPI=今日 2/今日拦截 2/成功率 50%/待审批 0，画像能力卡「本月 6 次调用」、敏感词卡「今日拦截 2 次」、待办 4 条（敏感词条+3 活跃告警），Network 9 个 XHR 全 200 无 403、console 零 error/warn，全页截图留档。npm run build exit 0，双 compose 文件重建 frontend 静态镜像。A1ContentView（渠道内容生成）因公众号/海报/短信渠道后端 invoke 无对应字段口径，移出本卡放下一卡做透，余 10 个 mock 视图登记 §④ |

---

## ④ 后续规划 Backlog

> 登记「本批做不了、涉及后续模块/阶段」的事项，逐条注明**来源**与**依赖/前置**与**建议批次**。能收口的在当前批次收口，不入此表。

### B20 已收口（2026-09-08 交付，详见 DELIVERY-P5-B20）

L3 审批 SLA 超时扫描+催办（ApprovalSlaJob 60s/三阶段时限 24h-8h-4h/120min 节流/幂等）、消息通知中心最小落地（notification 表 + 4 真实端点 + 铃铛角标/跳转已读）、审批中心候选人门店预过滤、退卡手续费 RF-REVENUE/IN 收入分录对账、sign_tier 双库回溯（无空值/在途全对）——全部闭合。

### B21 已收口（2026-09-08 交付，详见 DELIVERY-P5-B21）

审批转交/加签目标人硬校验（guardTargetApprover：工号存在/在职/主角色+兼岗并集按阶段校验 STORE_MGR/REGION_MGR/FINANCE/自转交拦截/重复加签拦截/加签给当前指派人拦截，网关硬校验矩阵 10/10 全中、中文报错原文留档；审计历史带 toName/whoName 中文姓名）、通知偏好持久化（notify_preference 表 + uk 唯一约束 + GET/PUT /api/txn/notifications/preferences 端点，五类×四渠道校验，前端 Pinia 乐观更新+snapshot 回滚、UI 零改动）、SLA 催办偏好免打扰（ApprovalSlaJob 收件人偏好过滤，关订阅→跳过1人/通知0人、恢复→通知1人，双向实证）、审批详情指派人/会签人展示（ApprovalView 模板补「当前指派人/会签人」）、退卡冻结 SLA 超时提醒（**实证勾销**：Job 仅按 status=PENDING 扫描不分 bizType + seed 库 CARD_CANCEL 6 单全走 approval_todo 办结，B20 已无差别覆盖）——全部闭合。

### B22 已收口（2026-09-08 交付，详见 DELIVERY-P5-B22）

营销 9 页收口全部闭合：**精准推送写链路四件套**（PushService：Bean Validation 参数校验 422、dedupKey=SHA-256(customerId|pushType|content) 前 16 位 60s 窗口幂等重放返回同一 pushId 不重复落库/不占频控/不重复审计、DbRateLimiter 周频硬上限 3 超限 400 中文报错、违禁词四类实时校验 400；全动作 audit_log bizType=PUSH/action=SEND，网关矩阵 7/7）、**券核销门店三级兜底**（currentStore：登录人 storeCode→store-service 新增 GET /api/stores/internal/first 内部端点（X-Internal-Token，服务间走 HTTP 不直读别域表）→FALLBACK SST01 降级；门店字段在三态分支之前设置，FORGED 异常单也落真实门店；E014 无门店上下文实证返回 ST-SH-001/上海旗舰店）、**核销链路漏斗播种器**（CouponWriteoffChainDataInitializer @Order(33)：CH-001 正常核销 774 / CH-002 异常核销 9 / CH-003 待处理 59，count>0 skip 幂等）、**海报推荐人接真实员工**（m5Poster store loadReferrers 改 listStaff，filter 非离职 map staffName/role.roleName，zh-Hans-CN 排序；Pinia 直查 19 名真实在职员工，mock「林晚」已替换）、**9 页浏览器逐页真实验证**（优惠券 8 券/1742 领取/3 核销、推送 13 客户/周频提示、核销今日 12/正常 3/异常 9/抵扣 ¥1,140、海报 6 模板/佣金 ¥9,370、素材库 10 素材、直播 7 场/挂链 ¥471,400、ROI 发券核销真实、渠道业绩 7 渠道卡片、总览 651,030 触达漏斗，控制台零报错）。核销三态对账：audit_log COUPON_WRITEOFF 8 条（WRITEOFF 1：VIP85 discountFen=55200；BLOCK 7：FORGED×4/DUPLICATE×3）。

### B23 已收口（2026-09-09 交付，详见 DELIVERY-P5-B23）

客户域 4 卡全部闭合：**积分商城**（商品/规则/兑换/双签审核/拒绝/履约/KPI，持久单号、幂等下单、审核通过同事务扣积分扣库存、履约幂等）、**积分账户人工调分**（客户 360「档案」tab、流水分页、积分池真实读模型、非零整数/原因长度/余额非负校验、clientToken 部分唯一索引防重放、全动作审计）、**客户标签**（消费/肤质/行为/价值/医疗五分类 CRUD、标签名全局唯一、重复打标 409、删除定义级联解绑、覆盖汇总、客户列表 tagId exists 过滤、360 打标/删标）、**会员等级**（普通/银卡/金卡/钻石/黑卡中文五档、阈值/折扣/权益、单行等级规则、手工调级、自动升级只升不降、客户表实时人数聚合）。prod 当前 B23 审计 33 条、17 个动作分组、null_payload=0；seed 重置后客户 100、标签 12、打标 265、商城商品 7、兑换单 8、B23 审计 0，四页浏览器回归无 JS 报错。

### B24 已收口（2026-09-09 交付，详见 DELIVERY-P5-B24）

「两线合并全收」5 卡全部闭合：**财务四页去 mock + 发票 CSV**（发票/预算/财务设置/资金日报接真实端点，`/api/finance/export/invoices.csv` 真实导出）、**卡余额时间线 + 核销双签明细**（customer-service `GET /internal/cards/{cardNo}/ledger` 与 txn-service `GET /internal/finance/writeoff-details` 两个 X-Internal-Token 内部投影，服务间走 HTTP 不跨库直读；finance 聚合暴露卡时间线/核销明细/卡台账 CSV/核销 CSV，四类枚举中文映射）、**咨询草稿/接诊**（consult-plan draft/start 端点 + ConsultationView 会话区切真实方案单）、**treat-start/treat-done 治疗全链路**（方案单状态机 PENDING→ACTIVE→PENDING_REVIEW→APPROVED→READY_PAY→PAID→TREATING→DONE 贯通，TreatStartCmd 扁平四项布尔术前核对、treat-done 治疗小结必填并生成 treatEmrId，权限 consult:review+emr:edit 双栈硬校验）、**我的工作台 7 项计数真实化**（审批/待收款/今日预约/方案单四态，每域 auth.can 门控 + 独立容错；候诊接待/病历草稿/术后回访/SOP 超期/复诊提醒 5 项无后端域保留演示并显式标注「（演示）」、`todoTotal` 只累加真实项）。prod 终验：E003 陈医生真实待办 2（ACTIVE=1/PENDING_REVIEW=1，无权限项隐藏，order 403）、E005 李店长待收款 1、SE101 真实待办 3，/doctor 刘女士 M002 ¥2280 真实方案单，三页 console 零错；prod 凭证澄清（E001–E014/SE101–SE105 + meiyun123，SE001 仅 seed 库）。seed 端到端造数 TREATING 链路实证（江医生 5/许店长 11），测试残留单号已在交付文档 §5.2 诚实登记。样式零改动。

### 移交后续（Backlog）

| 事项 | 来源 | 依赖 / 前置 | 建议批次 |
|---|---|---|---|
| SLA 超时自动升级/自动代办 | B20/B21 移交 | 组织树兼岗模型完善（兼岗上级路由）；现仅标记+催办+偏好免打扰。**B33 复核：本批只补齐部门层级写能力（建/改/启停），未引入兼岗上级字段与路由算法，本项维持 Backlog** | 组织树二批（兼岗模型） |
| 通知多渠道（短信/企微/邮件/WebSocket 实时推送） | B20/B21 移交 | 渠道适配层；notify_preference.channels 已存渠道位（INBOX/SMS/WECHAT/EMAIL）不返工；现为系统内通知+轮询 | B22+ |
| 审批 REGION 阶段兼岗路由（指派人/会签按兼岗上级） | B19/B21 移交 | 组织树兼岗模型完善；硬校验 B21 已收口（主角色+兼岗并集校验），缺兼岗上级自动路由。**B33 复核：org_unit 已有 leader_name 但仅为文本展示字段，未与 employee 建立外键与兼岗关系表，路由仍无数据基础** | 组织树二批（兼岗模型） |
| 组织树门店/大区/集团层级写能力 | B33 新增 | B33 三写端点仅放行 org_type='部门'（新建父级必须为门店、编辑不可改类型/编码、跨店移动联动 store_code+region）；门店/大区/集团节点的增改停仍需手工改库或走 store 域，缺层级重挂与子树级联规则 | 组织树二批 |
| 组织节点物理删除与编码回收 | B33 新增 | B33 只做停用（status 启停 + 停用原因必填），不提供 delete；物理删需先定义引用检查（employee.org_code / 审批链 / 历史单据）与 org_code 回收策略，否则会产生悬挂引用 | 组织树二批 |
| 免打扰时段策略（静默时间段/加急例外） | B21 新增 | notify_preference 已就绪，需加时段字段与 Job 判断 | B22+ |
| prod 8 月已办结单 sign_tier 旧阈值错配 15 笔 | B20 回溯 | refund 4 + card_cancel 11（8/16、8/20 旧阈值遗留）；改已办结单与审批轨迹矛盾，需历史迁移专项连同轨迹一并处理 | 数据治理专项（不紧急） |
| 赠金高级规则（满赠阶梯/有效期/报表） | B18 移交 | 营销规则引擎 | 营销收口批 |
| 86 张卡开卡首笔流水修复 | B17 移交 | 售卡开卡链路排查 | B21（数据修复） |
| 去划扣项目智能预填 | B17 移交 | 卡项-项目映射 | 交易收口批 |
| 撞单合并 | business-flows | 客户查重/合并规则 | 客户域批 |
| 资产转移 / 合同 | business-flows | 卡资产模型 | 交易域批 |
| 超管 Impersonate（ impersonation 登录） | business-flows | RBAC 扩展 + 审计 | 平台批 |
| ~~排队智能候补~~ | business-flows | **B29 已闭合（2026-09-10，arrival_waitlist 新域：WL 单号/手机号锚定会员·散客快照两态/WAITING→NOTIFIED→FULFILLED·CANCELLED、号源释放同事务 FIFO 递补+店长站内信幂等、/queue 第三卡+登记弹层+KPI，同批附带候诊超时自动释放与核销↔预约/划扣勾连，网关 24 项+浏览器两页取证 audit 314–331，详见 DELIVERY-P5-B29）** | ✅ B29 |
| ~~转介绍到期重分配~~ | business-flows | 营销渠道 + 定时任务；**B22 复核：转介绍页 /m5-referral 不在 9 页内仍为 mock，老带新数据未接真实客户关系，维持 Backlog** | 营销二批 |
| 私域自动化 Flow（随访/SOP/关怀/召回） | business-flows | 流程引擎 + 营销；术后随访 SOP 引擎及随访工作台/SOP 编排两整页（诊疗侧）B30/B31 已闭合，本行余营销侧关怀/沉睡唤醒/复诊召回 Flow | 远期营销阶段 |
| 角色管理页（RBAC 可视化配置） | permission-matrix | 后端 RBAC 已就绪，缺前端管理页 | 组织收口批 |
| ~~营销 9 页交付验证收口~~ | 盘点 | **B22 已闭合（2026-09-08，9 页逐页网关+浏览器验证、零控制台报错，详见 DELIVERY-P5-B22）** | ✅ B22 |
| 外部广告渠道接入（美团/抖音/小红书/大众点评/新氧投放回传） | B22 移交 | m5-channel 7 平台卡片 + m5-roi 渠道区现为前端 mock；**B26 已落通用回传骨架**（ExternalChannelController：HMAC-SHA256 签名/±300s 窗口/限流 60/min/幂等 bizRef/客户硬校验/channel_returnback 落库，双栈冒烟取证）；仍需各平台 OAuth 对接 + 投放 spend 落库 + ROI 联调 | 外部集成批（T3） |
| 营销归因模型（首次触点/末次触点/多触点加权） | B22 移交 | m5-roi 归因区现为 mock；依赖客户触点事件流（浏览/咨询/到店/成交）埋点归集，现触点数据未采集 | 数据仓库批（T2） |
| 素材库对象存储（S3/MinIO 替换本地/静态资源） | B22 移交 | m5-assets 10 素材现为种子静态数据；需文件上传服务 + 桶策略 + 素材审核流 | 平台批 |
| 海报图片真实渲染（模板+二维码+推荐人工号合成） | B22 移交 | m5-poster 6 模板为静态展示；需后端渲染服务（如 html2image/画布合成）+ 分销二维码绑定 referrer | 营销二批 |
| 直播/短视频写接口与成交回写 | B22 移交 | m5-live 7 场/挂链成交 ¥471,400 为种子只读数据；需直播排期写接口 + 挂链订单成交回写 + 审计四件套 | 营销二批 |
| 营销总览导出报告 / 订阅周报 | B22 移交 | m5-dashboard 651,030 触达漏斗真实只读；需报表导出（Excel/PDF）+ 订阅定时推送（可复用 B20 通知中心） | 营销二批 |
| ~~营销设置页 /m5-settings 接线~~ | B22 盘点 | **B37 已闭合（2026-09-12，`828ac6d`，详见 DELIVERY-P5-B37）**：卡1 统一 marketing_cfg 读写 DTO 契约（ConfigView/ConfigCmd 11 字段对称、weeklyPushLimit 统一、渠道数组化、剔除多泄列），全 12 键真实页面+容器端点管理（周频上限/审批/免打扰/渠道/兜底门店等；违禁词库另走 /marketing/forbidden-words，B34 已配置化兜底门店），营销设置 🔧→✅ | ✅ B37 |
| 赠金高级规则（满赠阶梯/有效期/报表） | ✅ B26 | GrantRule/GrantService/GrantExpireJob 真实闭环（规则 CRUD + 发赠金账本 + 过期批处理 + 报表）；收银台抵扣列下游 Backlog | 营销二批 |
| ~~赠金「收银台抵扣」（status VALID→USED）~~ | B26 新增 | **B35 已闭合（2026-09-12，grant_deduction 流水+FIFO 带锁扣减/幂等/超额 422/GRANT_DEDUCT 审计+marketing internal 端点+txn grant 支付+收银台磁贴混合支付，84 单测+curl+浏览器三轨取证，详见 DELIVERY-P5-B35）**；消费满额自动发赠金触发见下方 B35 新增行 | ✅ B35 |
| ~~消费满额自动发赠金触发（issueByRule 接消费事件）~~ | B35 新增 | **B37 卡2 已闭合（2026-09-12，`7552665`，详见 DELIVERY-P5-B37）**：实现路径经自主决策改为**轮询拉取**而非本行原记的 FUND_ENTRY 事件链路——复刻 B25 AutoPoints 同款范式，AutoGrantJob 5 分钟轮询 txn `/internal/paid-orders`（X-Internal-Token，与自动积分复用同一事件源），auto_grant_state 单行游标日粒度窗口+NULL 首跑全量回填；CONSUME_THRESHOLD 规则 priority 首条命中一单不叠多档、门店限定；**双层幂等**（idem_key `RULE:{ruleId}:{orderNo}` + customer_grant.source_biz_ref 订单级跨规则锚点，防同一订单重复发赠）；txn/客户域故障分层（502 整轮中止不推进游标、客户不存在 400 跳过）；明细 RULE/system + AUTO-GRANT/SYSTEM 汇总双审计。容器真验全量回填扫描 5 发放 4、50000 分边界 `>=` 命中、ST-SH-002 门店限定排除、二轮幂等零新增。附修 audit_log.txn_no 24→128 跨域缺陷。**退款回加赠金不做，见下方 B37 新增行** | ✅ B37 |
| ~~退款回加赠金（消费满额规则的逆向链路）~~ | B37 新增 | **B39 已闭合（2026-09-12，`5353bd9`，详见 DELIVERY-P5-B39）**：用户拍板三决策——①仅链路①付款回加（终审事务内同步逆向回加，远程失败 502 整笔回滚）；②过期券回加但保持 EXPIRED；③部分退款按**赠金→卡本金→法币**级联。marketing 侧 GrantService.refund() 约 145 行：RF 单号幂等（uk_gd_ref_grant 防双加）+ origin_biz_ref 原单锚点累计封顶（同原单多张 RF 合计不得超原扣，超额锁前 422）+ 悲观锁逆向 FIFO + USED 复活/EXPIRED 保态 + saveAll 冲突重查 + GRANT_REFUND(system/operator 双锚) 审计；grant_deduction 增 origin_biz_ref 列（随 B41 V12 纳管）。txn 侧 RefundSplit 三段拆分与 outbox 资金分录共用口径，grant 段**不产生 finance_event**（赠金是营销负债非资金），终审 grant→card 顺序 InOrder 固化，远程全在置态前、失败单据停留 PENDING_FINANCE 可安全重试/驳回。13 单测全绿（marketing 8 + txn 5），容器真验四组：全退 grant600+wxpay1400（券复活、仅 1 条 wxpay 140000 分录）、纯 grant 段退 50000（零资金事件）、RF 重放原样返回不双加+累计封顶 422、营销停服终审 502 四项零写入+恢复后 422 业务拦截。**余 Backlog：链路②满赠门槛追回、卡 gift_balance RF 不回补、B35 grant 渠道正向 DEAD 事件** | ✅ B39 |
| ~~txn-service 扣减侧单测零覆盖~~ | B35 新增 | **B36 卡4 已闭合（2026-09-12，`c0d3583`）**：pom 增 starter-test 建立 txn-service 首个测试工程，PaymentServiceTest 7 例覆盖 grant 散客 400/同单第二笔 409/售卡禁令 400/成功路径 deduct 先于 order_payment 落库（InOrder）/营销域 422·502 透传不记账/grant+现金混合尾款不重扣；幂等重放已由 marketing 域单测+网关实证覆盖。**余：M4FlowController grant-balance 端点「降级 available:false」与 MarketingGrantClient HTTP 层尚无服务内单测**（curl 轨已实证），随后续控制器层测试批补 | ✅ B36（主体） |
| 通知真实运营商网关（短信/企微/邮件） | B26 新增 | 现 dev 网关未配时诚实 SKIPPED、配置 URL+密钥后 POST 联调（冒烟以容器化 mock 取证 200/4xx/拒连三路径）；缺各厂商签名/模板审核/回执与投递状态回写 | 外部集成批（T3） |
| 通知免打扰员工个人级时段 | B26 新增 | B26 实现全局配置版（meiyun.notify.quiet.*）；个人级需 notify_preference 加时段字段 + 前端设置页 | 组织/通知收口批 |
| 标签自动化规则（按消费/肤质/行为/价值/医疗规则自动打标、摘标与规则审计） | ✅ B25 | 标签定义/人工打标已闭环；B25 完成规则引擎+定时扫描+审计（TA### 规则 / TagAutoRuleJob / 5 类条件），详见 DELIVERY-P5-B25 | 客户运营自动化批 |
| 消费自动积分与退款回退 | ✅ B25 | 积分账户/流水已闭环；B25 接入 txn 内部只读端点拉取已收款/已退款订单，按积分规则幂等发分与回退（AUTOPOINTS 幂等键），详见 DELIVERY-P5-B25 | 交易-客户联动批 |
| 等级定时批处理（每月 1 号扫描、执行报告、通知、批处理幂等记录） | ✅ B25 | 自动升级服务与规则已就绪；B25 新增 LevelMonthlyJob（每月1号 cron、autoUpgrade 开关收敛、汇总审计），详见 DELIVERY-P5-B25 | 客户运营自动化批 |
| 自动降级引擎（连续未达标/保护期/客户通知/审批策略） | B23 移交 | 当前只升不降；需计算周期事实表、保护月逻辑、降级审计与 KPI 口径；引擎上线前本月降级 KPI 仅统计手工降级 | 客户运营自动化批 |
| 客户详情会员权益真实联动（discount 实际计价 + benefits 可享权益） | B23 新增 | 等级阈值/权益配置已就绪；需订单计价与权益核销链路接入 | 交易计价收口批 |
| ~~积分/标签/会员等级/积分商城接线~~ | 盘点 | **B23 已闭合（2026-09-09，4 卡网关+PG+浏览器验证，详见 DELIVERY-P5-B23）** | ✅ B23 |
| ~~财务边角页（发票/预算/税/日结/核销预收）接线~~ | 盘点 | **B24 已闭合（2026-09-09，财务四页去 mock + 发票 CSV、卡余额时间线/核销双签明细内部投影切真，详见 DELIVERY-P5-B24）** | ✅ B24 |
| ~~工作台/咨询/医师/EMR 会话区接线~~ | 盘点 | **B24 大部分闭合（2026-09-09，工作台 7 项计数真实、咨询草稿接诊、医师 treat 全链路切真；EMR 独立域仍留 Backlog），详见 DELIVERY-P5-B24** | ✅ B24（EMR 除外） |
| ~~EMR 电子病历独立域（病历书写/模板/审签/归档实体与页面）~~ | B24 移交 | **B27 已闭合（2026-09-10，emr_record 独立域全状态机+修订+signEmr/treatDone 联动+EmrView 浏览器全链路取证，工作台「病历草稿」演示项摘除，详见 DELIVERY-P5-B27）**；余 EMR 列表分页/缺参 400 化/模板库见下方新增行 | ✅ B27 |
| ~~EMR 病历模板库 / 列表分页 / 缺参 400 化~~ | B27 新增 | **B30 已闭合（2026-09-11，emr_template 新域+集团 4 种子只读+本店建/停用+模板库弹层九段范文、EMR 列表真分页默认 20/stats 后端聚合、ChineseValidationAdvice 框架异常中文 400，附修 fc97d00 PG 42P18，详见 DELIVERY-P5-B30）**；集团模板运营管理（上架/版本/审核）留 Backlog | ✅ B30 |
| ~~建档实时写 ES + 客户搜索 DB 融合~~ | B27 新增 | **B30 已闭合（2026-09-11，customer_search_event outbox+10s 中继 Job SENT/RETRY/DEAD+熔断 30s 降级 DB+显式 mapping+新索引全量回填+读时 mergeRecentFromDb 融合+DataScope 过滤，网关事件 SENT/7 次 RETRY 自愈/SELF 隔离 [] 实证，详见 DELIVERY-P5-B30）**；**DEAD 事件处置台 B32 已闭合**（2026-09-11，retry/replay/discard 三动作状态机+/search-events 处置台整页+全量重建入口，customer:search:admin 仅授 REGION_MGR，附修异常处理透传+biz_type 扩宽，`f02384b`/`dbf8158`，详见 DELIVERY-P5-B32）；ES reindex 对账治理（ES110 vs PG100 历史漂移 10 文档：ES/PG 文档级 diff 对账与孤儿文档清理）仍留 Backlog | ✅ B30/B32 |
| ~~客情登记扩展字段后端化~~ | B27 新增 | **B28 已闭合（2026-09-10，customer 加 9 列落十类扩展字段：年龄/肤质/诉求/过敏史三列/意向项目/意向等级/预算/沟通要点，白名单+过敏互斥，360 档案 tab 有值才显示 kv 回显，详见 DELIVERY-P5-B28）** | ✅ B28 |
| ~~triage 改派时间线~~ | B27 新增 | **B28 已闭合（2026-09-10，triage_reassign 历史表+reassign 同事务追加+读模型批量富化+接待台最小 UI 时间线，详见 DELIVERY-P5-B28）** | ✅ B28 |
| ~~术后随访 SOP 引擎（随访计划编排/超期扫描/回访记录）~~ | B24 移交 | **B30 已闭合引擎（2026-09-11，followup_sop_template/node/batch/followup 四表+treat-done AFTER_COMMIT(REQUIRES_NEW) 幂等排程 1/3/7/30 天节点+FollowupSopDueJob 60s 超期升级店长幂等通知+/api/txn/followup 五端点+工作台两演示项摘除，附修 fa549d5，详见 DELIVERY-P5-B30）**；**B31 已闭合整页与手工建（2026-09-11，/followup 随访工作台整页接真+手工建随访端点+stats 九键+keyword 模糊，/sop SOP 编排整页+模板节点五动作+批次聚合看板+summary 五键+一键升级，FollowupSopEscalator 共享组件，附修 d130dfc 空库 AVG NPE，详见 DELIVERY-P5-B31）** | ✅ B30/B31 |
| 沉睡客户唤醒 / 复诊召回自动化 | B24 移交 | 工作台「复诊待提醒」演示项依赖营销自动化 Flow，仍远期；**其中「复购提醒前端未接」已由 B30 闭合**（RepurchaseView 全链路+三方签核，`7c922cb`，ApprovalService 多级审批见末行） | 远期营销自动化批 |
| ~~接待台 / 候诊接待域~~ | B24 移交 | **B27 已闭合（2026-09-10，到店五态队列+分诊建 CP 草稿+预约/客情自动到店+工作台「候诊待接待」演示项摘除，详见 DELIVERY-P5-B27）**；余排队智能候补/超时自动释放**已由 B29 闭合**（ArrivalAutoReleaseJob 30min+10min 宽限 60s 扫描、系统释放同事务 FIFO 递补，详见 DELIVERY-P5-B29）；到店核销见下条 B28 已闭合 | ✅ B27/B29 |
| ~~到店核销 /m2-checkin~~ | B27 明确 | **B28 已闭合（2026-09-10，checkin_record 独立域 CI 单号池+三方式/三态/四异常码、确认核销/标记异常/解除异常+timeline+KPI/过滤、by-phone 本店→公海→404 锚定、当日同号 PENDING 幂等、手机号掩码出域，网关+浏览器全链路取证 audit 301–313，详见 DELIVERY-P5-B28）**；与预约/writeoff 自动勾连**已由 B29 闭合**（APPOINTMENT 命中当日预约同事务四连写 appt/wd/ci/arrival+核销详情透出勾连单号，audit 314–331，详见 DELIVERY-P5-B29） | ✅ B28/B29 |
| 通用异常中心（跨域异常单据统一归集与处置工作台） | B24 移交 | BOM 异常等现为各域分散后端 only；财务税/日结/异常页 B24 已切真但无统一处置中心 | 平台/交易收口批 |
| 异常账务处置登记（长短款/错账登记与审批闭环） | B24 移交 | 财务台账真实但缺异常账务专门登记审批流 | 财务收口批 |
| 预收合规监控（合规指标/挂账时长监控预警） | B24 移交 | 核销/预收明细 B24 已切真可读，缺合规监控规则与预警 | 财务收口批 |
| 进项税抵扣链路 | B24 移交 | 当前发票/税页仅覆盖销项与台账视角，进项抵扣诚实未实现 | 财务收口批 |
| M4Repurchase 复购方案签核 / 多级审批 | B24 移交 | **单据内客户（预填）/经办/店长三方签核 B30 已闭合**（RepurchaseView 知情同意硬前置+三方签核填齐解禁生效+REPURCHASE/TRIPLE_SIGN 审计，`7c922cb`，详见 DELIVERY-P5-B30）；医师/店长/财务 ApprovalService 多级审批流仍留 Backlog | 诊疗域收口批 |
| seed 端到端造数/清理脚本工具化（幂等） | B24 移交 | B24 手工造数残留（CP…004/005/000006、OD…000003、PM…000004、SC026）已在 DELIVERY-P5-B24 §5.2 登记 | 测试体系批（不紧急） |
| setup-seed-db.sh 全量重建保留 audit_log 选项 | B31 新增 | 脚本约 L674-675 TRUNCATE 清单含 audit_log，B31 全量重建致冒烟审计不存活（终态 36 行全 CONSUMABLE 播种基线），audit append-only 取证口径受影响；需增补「保留 audit」开关（如按 bizType 导出/跳过 TRUNCATE） | 测试体系批（不紧急） |
| SOP 多模板 / 门店级模板 / 节点拖拽排序 | B31 新增 | 现集团通用单模板 SPT-SEED-001（store_code 空），节点 lineNo 按 dayOffset 自动 renumber；门店自建模板、模板上下架/版本管理、手工拖拽排序未做 | 诊疗域后续批 |
| 随访结果结构化分析（满意度趋势 / 不良反应专项处置台） | B31 新增 | stats 已提供 avgSatisfaction/adverseCount 实时计数，趋势分析与不良反应（adverse）专项处置工作台未做 | 数据/诊疗后续批 |
| ~~SST01 幽灵门店码残留清理批~~ | B34 新增 | **B36 卡1 已闭合（2026-09-12，`8ddac05`）**：前端兜底常量改读真实门店表首家（实际路径为 `frontend/src/stores/storeContext.ts`，B34 登记误写 `utils/`，本批一并更正；失实注释同步修正）；`CouponWriteoffDataInitializer` 与 `MarketingAssetDataInitializer` 两处 SST 种子加「仅 seed 库门店体系存在时播种」门控，core 库不再产生幽灵码；`m5Core.ts` mock 值删除。**注：db/01_master.sql 为 seed 库脚本，SST 系门店在 seed 体系内真实存在，不属残留，未动**；历史已写入的 SST 脏数据见下方「存量脏数据」行 | ✅ B36 |
| ~~marketing_cfg GET/POST 字段名不对称~~ | B34 新增 | **B37 卡1 已闭合（2026-09-12，`828ac6d`）**：GET 改独立只读视图 ConfigView（不再直出 JPA 实体），与写命令 ConfigCmd 字段名/类型完全对称——周频键统一 `weeklyPushLimit`（废弃写命令旧 `weeklyLimit`，破坏性改名前后端同卡同步）、渠道字段两端同为 List（前端不再 parse JSON 字符串）、视图剔除 cfgId/referralArrivedReward/referralDealReward/commissionRate 四个多泄遗留列；旧 append-only 审计快照的 weeklyLimit 由前端读时归一；quota `/push/quota` 的 weeklyLimit 是独立前端契约刻意不动。容器 GET/POST/幂等/硬约束/落库/审计新键取证，详见 DELIVERY-P5-B37 | ✅ B37 |
| ~~marketing_cfg 等营销/审计表无 Flyway 迁移脚本~~ | B34 新增 | **B38 已闭合（2026-09-12，`6f2e734`/`2b03c46`，详见 DELIVERY-P5-B38）**：audit/marketing 两服务均接入全库共享 Flyway 版本链（flyway-core 走 BOM，Community 9.22.3；yml 与 customer/finance 同款 enabled/baseline-on-migrate/baseline-version 0/validate-on-migrate/ignore *:missing+*:future，audit 额外 out-of-order:true 共享链补洞）；V10 以 ALTER TABLE IF EXISTS 补登 audit_log 两次历史手动 ALTER（biz_type 16→32、txn_no 24→128），V11 以 CREATE TABLE IF NOT EXISTS 纳管 marketing_cfg 全 15 列+主键与 auto_grant_state 全 5 列；共享链实测 V0–V11 共 12 行全 success。**余：marketing-service 其余 16 张 JPA 属表仍靠 ddl-auto=update，见下方 B38 新增行** | ✅ B38 |
| ~~audit_log 基线 schema 与实体/现网的历史漂移~~ | B37 新增 | **B38 卡1 已闭合（2026-09-12，`6f2e734`）**：V10 迁移以 `ALTER TABLE IF EXISTS audit_log ALTER COLUMN biz_type TYPE VARCHAR(32)` 补登 B32 历史手动 ALTER（现网重设同宽 no-op，全新库表不存在时 NOTICE skipping 留 Hibernate 按实体 length=32 建）；gitignored 基线 db/schema.sql 的 biz_type VARCHAR(16) 同步校准为 VARCHAR(32)（db/ 不入库），漂移消解 | ✅ B38 |
| ~~marketing-service 其余 16 张 JPA 属表分批纳管 Flyway~~ | B38 新增 | **B41 已闭合（2026-09-12，`3166d97`，详见 DELIVERY-P5-B41）**：V12 券/赠金域 8 表（coupon_template、coupon_grant、campaign、coupon_writeoff_record、coupon_writeoff_chain、customer_grant、grant_rule、grant_deduction）、V13 渠道/素材/违禁词 3 表（channel_returnback、marketing_asset、forbidden_word）、V14 内容/触达 5 表（poster_template、poster_record、short_video、live_session、push_record，含 push_record→customer FK），全部 pg_dump 现网物理结构 1:1 转写、CREATE TABLE/INDEX IF NOT EXISTS 基线（不对存量表 ALTER）；现网重放全 NOTICE skipping、16 表 count 前后零变更，共享链 V0–V14 共 15 行全 success；scratch 全新库裸回放 V1→V14 全链通过（push_record 依赖的 customer 表由 customer-service Hibernate 建，compose 已固化 depends_on healthy 时序）。marketing 全 18 张属表达成 100% Flyway 纳管 | ✅ B41 |
| marketing-service ddl-auto 由 update 改 validate 的评估 | B41 新增 | 18 张属表全部纳管后，ddl-auto=update 仍在跑（基线迁移只建结构不 ALTER，历史缺列仍靠 update 净兜底）；后续需逐表核对 Flyway 基线与 JPA 实体完全一致后，把 marketing-service 的 ddl-auto 切到 validate 并做一次全新库+现网双场景验证，彻底关闭「JPA 自动改表」口子 | 中期治理 |
| 存量脏数据核销流水/营销资产/门店主数据 SST 处置（**B40 全库扫描闭环，B36 实测更正**） | B34 新增/B36 更正/B40 处置 | **B40（2026-09-12，`5ef75db`，详见 DELIVERY-P5-B40）全库字符/JSON 列扫描定性污染 11 表 175 行+audit_log 36 行快照**：①门店运营域 8 表 162 行幽灵主数据（consumable 36/consumable_stock 36/consumable_movement 35/equipment 8/equipment_maintenance 11/treatment_room 9/treatment_bed 16/store_price 11，2026-09-06 19:11 system 播种、零外键零预约零订单引用）已 CSV 8 份+全表 pg_dump 留档（scripts/backup-b40-sst-20260912/，gitignored）后单事务**物理删除**，audit_log 36 行 CONSUMABLE 建档快照 append-only 保留；根因 3 个 store-service 播种器无门控已修并经重建镜像 force-recreate 实证零重播。②`marketing_asset` 2 条（AS-SEED-002 投 SST01/02、AS-SEED-009 投 SST03/04；**B36 仅登 SST01 本批更正为 SST01~04**）保留资产本体，UPDATE scope=ALL、store_codes=[]，原值 JSONL 留档。③**核销流水 11 条全部保留不擅改**：8 条 0 元（WR-SEED-0003/0004 + WR20260908-000001/002/004~007，FORGED/DUPLICATE）为风控证据不动；**3 条 OK 共 114000 分=1140 元（WR20260908-000003 55200、WR-SEED-0001 50000、WR-SEED-0002 8800）store_code=SST01 挂账，需业务指定真实归属门店后定向回填**。**B36 勘误**：WR20260908 前缀实 8 条——000001~000007 共 7 脏（B36 登 7 条正确但 B34 曾误登 4 条），000008 为 ST-SH-001 干净复验记录不属脏数据 | 1140 元 3 单待业务确认门店；余闭环 |
| ~~StoreNameResolver 不可达与不存在不可区分~~ | B34 新增 | **B36 卡3 已闭合（2026-09-12，`a43d299`）**：新增 StoreServiceUnavailableException，拆为 `resolveNames`（宽松：故障降级空 Map，仅供券名回显/「不可用即放行」）与 `resolveNamesRequired`（严格：连接/超时/4xx/5xx 抛专用异常→写链路 **503**；name-map 200 子集缺码=真不存在仍 **400**），fetchNames 唯一取数出口；MarketingCfgService 兜底门店保存改严格版，store-service 宕机不再误报 400。真实容器三分支取证（假码 400/真码 200/停服 503）+93 营销单测全绿 | ✅ B36 |
| ~~`/internal/first` 首家门店排序口径未文档化~~ | B34 新增 | **B36 卡2 已闭合（2026-09-12，`3694003`）**：StoreRepository 增 `findFirstByOrderByStoreCodeAsc`，契约明确为 **store_code 字典序升序首家**（不再依赖无 ORDER BY 的 JPA 默认），空库 404/空；StoreControllerTest 断言多门店固定首家、空库、X-Internal-Token 鉴权，javadoc 写明排序口径 | ✅ B36 |
| BOM 缺料明细 → 申购单联动 | B34 新增 | B34 已把缺料明细（skuCode/needQty/stockQty）结构化贯通到页面，具备一键生成申购单的数据基础；申购域属 M2 门店运营平台，待该阶段一并实现 | 远期 M2 |
| M2 门店运营平台（13 页） | 远期阶段 | 排班/工单/日结/申购/报损/绩效等 | 远期 M2 |
| M3 客户运营平台（10 页） | 远期阶段 | 分群/旅程/自动化 | 远期 M3 |
| M1 集团管控（14 页） | 远期阶段 | 集团-门店多租户 | 远期 M1 |
| ~~A1 AI 中心模型配置地基（模型接入/网关监控/管理配置/审批治理 4 页）~~ | B42 新增 | **B42 已闭合（2026-09-12，`7ea662d`，详见 DELIVERY-P5-B42）**：ai-service 第 8 服务+V15 七表+/api/ai 网关前缀+火山方舟 OpenAI 兼容薄适配（LlmClient RestClient）+供应商/模型 CRUD 与 AES-GCM Key 落库+真实连通性测试+六功能绑定/角色灰度矩阵+调用日志/KPI/账单+全局配置+审批分页决策四页真实闭环。真实 ARK Key 由用户在 /ai/providers 录入后 SUCCESS 复核（不索要明文） | ✅ B42 |
| ~~B43 真实功能 invoke 闭环~~ | B42 新增 | **B43 已闭合（2026-09-12，详见 DELIVERY-P5-B43）**：POST /api/ai/features/{code}/invoke 六功能角色矩阵鉴权+门店域+模型 enabled/CHAT 校验+参数覆盖+LlmClient.chat 真实火山方舟 ark-code-latest 调用，成败均沉淀 ai_invoke_log（V16 error_code 扩 512、去方法级事务保失败日志独立提交）；网关页 KPI/API 目录/调用日志与管理页试运行抽屉全部真实联动；curl 全状态码矩阵+DB+Chrome 三轨真验（497 tokens/13013ms 真实中文画像）。四项运行态修复：error_code 列宽、失败日志事务、updated_at 显式刷新、storeScopeText 展示映射污染 | ✅ B43 |
| ~~B44 配额限流与安全告警~~ | B42 新增 | **B44 已闭合（2026-09-12，`336b7f8`，详见 DELIVERY-P5-B44）**：V17 三表（ai_quota/ai_sensitive_word/ai_alert_rule）+种子（13 敏感词/5 告警规则）；配额只存额度不设计数列，用量以 append-only ai_invoke_log 按北京时间日/月窗实时 count，出站前 **FEATURE→MODEL→GLOBAL 三级校验**超限 **429** 中文、被拦截不落日志不占量；敏感词总开关+BANNED 违禁/INJECTION 越权（prompt injection）小写 contains 命中 **400** 且命中词首尾脱敏回显；告警五规则（P99/错误率/调用量/成功率/配额水位）无定时任务、请求时实时评估，activeAlerts 联动网关 KPI；A1Admin 新增第 6 个「配额管理」tab（8 行三级配额+水位三档色+调整抽屉），A1Gateway 告警 tab 真实化。curl（400/429 脱敏与用量对账/真实 2097 tokens 54736ms）+PG+Chrome 三轨真验 | ✅ B44 |
| ~~B45 审批申请与效果评估实体化~~ | B42 新增 | **B45 已闭合（2026-09-13，详见 DELIVERY-P5-B45）**：POST /api/ai/approvals apply 申请端点（类型白名单/目标存在且未启用/同目标 PENDING 幂等 400 中文，aiAdmin:edit 与决策 aiGovern:approve 权限分离）+AiApplyDrawer 前端申请 UI（模型上架/功能绑定两类申请单），审批通过后端联动 MODEL/BINDING enabled=t（linkage 审计）；V18 ai_eval_task/ai_experiment 两表三 CHECK，EvalService 八端点按 append-only ai_invoke_log 北京时间窗口聚合调用量/成功率/P99/token/成本，lift pp 与已结案均值，评估/A·B 实验两 tab 从占位实体化（新建/刷新/prompt 录结论结案冻结）。真验修复 KPI kpis 一次性快照不刷新的 Vue 响应式缺陷（baseKpi ref+kpis computed），curl+PG+Chrome 三轨证据齐全 | ✅ B45 |
| ~~B46 卡1 敏感词词库管理+命中落库+敏感词检测页去 mock~~ | B44 新增（敏感词二期纵深） | **B46 卡1 已闭合（2026-09-13，`d944a01`，详见 DELIVERY-P5-B46）**：V19 新建 ai_sensitive_hit 命中表 14 列（CHECK category+三业务索引）+ai_sensitive_word 补 updated_at、ai_set_updated_at() 为 8 张 AI 表挂 UPDATE 触发器；SensitiveWordController 六端点（词库全量 List/新建/编辑重复词 400、命中真分页+词类三态过滤+北京时间 stats/误报标注，changed=false 与重复标注幂等不写审计，全动作审计 payload 合法 JSON）；FeatureInvokeService L142 出站拦截链接入（Key 解密后/配额前，BANNED/INJECTION 400 中文脱敏，SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库、snippet 截断 500）；前端 utils/datetime.ts 时区统一工具（AI 中心 4 文件 7 点位先落地），A1SensitiveView 整页去 mock。curl+PG+Chrome 三轨真验全绿 | ✅ B46 |
| ~~B46 卡2 A1HomeView 智能中心首页去 mock~~ | B42/B46 更新 | **B46 卡2 已闭合（2026-09-13，`8bee59d`，详见 DELIVERY-P5-B46-2）**：纯前端切真无新端点，四 KPI/12 能力卡调用量/待办告警按 aiGateway:view、aiAdmin:view、aiGovern:view 三权限分组门控全部接真（无权限不发请求 KPI 显「—」杜绝越权 403），待审批数取审批分页 totalElements、能力卡按 monthlyBill 诚实显隐、待办流 fmtAgo 相对时间+空态，datetime.ts 补 fmtAgo；curl 五端点+PG 八项+Chrome（9 XHR 全 200、console 零报错）三轨真验全绿 | ✅ B46 |
| A1 AI 中心其余页（Prompt 编排/知识库/Embedding/向量/Agent/效果实验等） | B42 更新（原「A1 AI 中心 15 页」行）/B46 更新 | B42 交付 4/15、B46 卡1 敏感词检测页、卡2 智能中心首页闭合后 **6/15 页真实闭环**（历史口径余 9 页；实测 nav 中 mock A1 视图 12→10 个，视图/页面粒度口径差见 DELIVERY-P5-B46 §7，后续按实测余 10 视图逐一切页；下一卡优先 A1ContentView 渠道内容生成——需先定公众号/海报/短信渠道 invoke 字段口径）：余页逐页去 mock 接真；扩展 LlmClient 支持 EMBEDDING/VISION 等多能力与多供应商（非 OpenAI 兼容协议适配）；token 计费核算与账单环比闭环；A1Gateway 告警规则写端点小补丁；全站时区工具推广（AI 中心外余约 21 处 slice） | B46 起分批 |
| T4 AI 算力（4 页） | 远期阶段 | GPU/算力资源调度与监控，独立于 A1 应用层 | 远期 T4 |
| T2 数据分析（4 页）/ T3 外部集成 | 远期阶段 | 数据仓库 / 开放 API | 远期 T2/T3 |
| C 端移动端（20 页） | 远期阶段 | mp-uniapp，2 页已真实 | 远期移动端 |

---

> **更新约定（铁律 9）**：每批 commit+push 后、汇报前，更新 §① 仪表盘数字、§② 相关模块状态与批次、§③ 时间线行、§④ Backlog 增删；交付汇报必带「整体完成度 X/Y 模块（Z%），本批后新增完成 N 个」。
