# DELIVERY-P5-B54 · 排班真实化大批（L144 医生/房间班次表，6 卡）（2026-09-16）

> **批次性质**：用户对 A/B/C/D 四方向正式拍板后承接 B 方向的业务落地大批——04-backlog L144「M1 调度医生/房间班次表（资源可用时间现固定 09:00-20:00）」全行勾销：org-service 新建 `staff_shift` 实体+全员周排班维护+周例铺底，internal resolve 供 txn 派单/改期/resources 三处改排班真源，M2 `/m2-schedule` 排班页整页接真（M2 13 页中**首个真实闭环页**），M1 调度时间轴硬编码改后端动态轴，请假申请-批准-驳回与排班 LEAVE/OVERRIDE 权威联动+复制上周。
> **用户拍板（B 方向原文照录）**：「B. L144 医生/房间班次表（资源真实可用时间）1、建实体+维护界面+后端接线；2、全员排班；3、周例，排班维度你看看我给的线上截图，按这种维度就行；早中全，改为上午、下午、全天、休息、请假；4、前端时间格以后端为准；其他的问题你可以先做竞品调研，然后根据调研结果，推荐一个最合适我们的」；总授权「剩下的我没有决定的你按照推荐来就行」。（同批另三方向：A=L141 已在 B53 以 A3 诚实空态闭合、C=L133 impersonate→P5-B55、D=L123 报告哈希验真 UI→P5-B56，本批均不触碰。）
> **提交清单（5 代码 commit 全 push origin/main）**：
> - 卡1 `bbcdb46` feat(org)：staff_shift 实体+Repo+ScheduleController 周视图/改班/周例，4 files +347/-21
> - 卡2 `e2da7d7` feat(txn)：org internal resolve+OrgScheduleClient 软降级+DispatchService 动态班次窗，4 files +223/-17
> - 卡3 `2e1602b` feat(m2)：排班页接真 api/schedule+store mock 换真源（视图零改），3 files +198/-111
> - 卡4 `fc5c2e7` feat(m1)：调度时间轴按排班动态化 ResourceView 四字段+axisFor 并集轴，5 files +154/-57
> - 卡5 `9f44eb1` feat(m2)：请假申请/批准闭环+复制上周 LeaveRequest/LeaveController/ShiftUpsertService+前端接真，9 files +860/-25
>
> 合计原始 **25 files +1782/-231**（各 commit 含当卡 HANDOFF 哨兵回写；去重唯一代码文件 16 个）；卡6 零代码为本批 docs 原子提交。
> **铁律对齐**：0 先查再写（卡0 全站侦察 org 无任何既有排班实现、PermissionMatrix 三码已预置）、-1 mock 即活规格/诚实空态（NONE 不造假班、考勤无源恒空、无排班/故障软降级、degraded「未排班」小标）、6 多栈 RestTemplate+X-Internal-Token 软降级（OrgScheduleClient 永不抛异常）、7 三轨真验（构建+PG+curl+审计+浏览器 UI 全绿）、8 一卡一 commit 紧跟 push、9/10 台账六册原子提交回写通读勾稽、11 哨兵 HANDOFF-AUTO 全程心跳。

## 1. 背景与记账口径

- **L144 登记实质**：B49 卡12 M1 调度中心切真时，医生/房间资源的「可派时间」无任何排班数据源，被迫写死固定营业窗 09:00-20:00（DispatchService WORK_START/WORK_END 常量）；B51 用户拍板④「都保留诚实空态」时登记在案，卡5 改期 422 班次校验引用同一固定窗，注明「班次表落地后两处一并改真源」。本批即该缺口的真实落地。
- **落地范围（对用户四点需求逐一兑现）**：
  1. **建实体+维护界面+后端接线**：org-service `staff_shift` 表（JPA ddl-auto=update，无 Flyway）+周视图/改班/周例/复制上周端点+M2 排班页整页维护界面；
  2. **全员排班**：周视图按数据域内在职员工（DataScope）逐人出班，generate-week 全员铺底；
  3. **周例+五态**：照用户线上截图维度（员工×7 天网格），POST generate-week 周一至六全天/周日休息 TEMPLATE 铺底（已存在手工行一律跳过不覆盖）；早/中/全 改名 **上午/下午/全天**，加 **休息/请假** 共 5 态（MORNING 09:00-14:00、MID 14:00-20:00、FULL 09:00-20:00 可派；OFF/LEAVE 不可派）；
  4. **前端时间格以后端为准**：M1 删除写死 SLOTS，改按 resources 返回窗并集动态成轴；M2 五态钟点镜像后端字典。
  - 另按总授权补齐竞品调研后的推荐闭环：请假申请-批准-驳回单据流（leave_request，单号 LV+yyyyMMdd+三位序号）批准时逐日权威覆盖 LEAVE/OVERRIDE 保证「请假不可派」，复制上周（跳已有手工行/不复制 LEAVE/落 TEMPLATE）。
- **完成度记账（本批有真实模块落地，数字跃迁）**：M2 `/m2-schedule` 由 mock 页（⬜）变为实体+真实端点+整页接真的真实闭环（✅ 三条件齐备），是 **M2 门店运营 13 页中首个切真页（M2 1/13）**：
  - 总数 **✅108→109/166=约66%、🔧1 不变、⬜56→55（约33%）；新增完成模块 1 个**；勾销 04-backlog **L144 一行**。
  - **域归属口径（三证合一）**：新完成页计入**域⑦ 组织与权限**而非域⑧——①02-modules「M2 门店运营（…13 页）」聚合行物理位于域⑦表末行（域⑦表头 L104、域⑧表头 L121）；②01-dashboard 域⑦ 14⬜ 恰容纳 M2 13 页+交接班 1，叙事含「M2 运营为远期」；③域⑧ ⬜31 构成注（M1 0/A1 0/移动端20/T2·T4等12）不含 M2。故 **域⑦ 10✅→11、14⬜→13；域⑧ 34✅2🔧31⬜ 完全不动**。聚合行照 A1（L164）范式保持 ⬜、备注列明 M2 1/13，另起「M2 · 排班」明细 ✅ 行。
  - M1 调度中心（卡2/卡4 的 txn 真源+动态轴）与 M2 排班页请假闭环（卡5 前端）均为既有 ✅/新页纵深挂载，不另计完成数。

## 2. 影响面只读核实（卡0 侦察结论）

- org-service **无任何既有排班实现**（无 shift/duty/roster 表与端点）；权限矩阵 `schedule:view/edit/approve` 三码已预置——view 八角色全有、edit/approve 仅 REGION_MGR/STORE_MGR/SUPER_ADMIN，**无需新播种**。
- org 业务表一律 JPA `ddl-auto=update`（org 无 Flyway，V1 仅 sys_ 表）；实体审计字段惯例仅 createdAt，操作人/动作走 AuditRecorder；范式=Controller 直注 Repository、无 Service、裸 Map 出参、中文 ResponseStatusException、DataScope 门店越权 404、internal 走 X-Internal-Token（internal:name-map，网关 Go isInternalPath 外网裸 404）。
- 网关是 gateway/ 下 **Go 服务**（非 Java），`/api/<svc>/internal/**` 硬编码外网一律裸 404 隐身；服务间 compose 内直连服务名（org 8086/txn 8083）不经网关。
- internal token 鉴权链：meiyun-security AuthInterceptor 常量时间比 X-Internal-Token，命中→LoginUser system/系统服务/GROUP/List.of("*")，任意 @RequirePerm 通过。
- 前端 M2 排班页 ScheduleView.vue 视图与样式完备（卡3 mock 即活规格），接真须模板/样式零改、只换 api+store；M1 M1DispatchView.vue 时间轴原为写死 9-20 点 SLOTS。

## 3. 代码改动（卡1–卡5，按 commit）

### 卡1 `bbcdb46`（4 files +347/-21）org 员工周排班后端

- 新建 `StaffShift.java`：id IDENTITY PK、staff_id(16)+shift_date date 唯一键 `uk_staff_shift_date`、shift_code(8) 五态、source(8)=TEMPLATE/OVERRIDE、created_at。
- 新建 `StaffShiftRepository.java`：区间按日期+工号排序查询、单日 findById 锚点。
- 新建 `ScheduleController.java` 四端点：
  - `GET /api/org/schedule?weekStart=`（默认本周、任意日期归一化到周一；返 weekStart/weekEnd/staff[]/shifts[]，DataScope 数据域）；
  - `GET /api/org/shift-codes`（MORNING 上午/MID 下午/FULL 全天 assignable=true、OFF 休息/LEAVE 请假 assignable=false）；
  - `PUT /api/org/schedule/shift`（schedule:edit；落 OVERRIDE；同码幂等不重复审计；离职/越权 404；非法码/日期 400；SET 审计）；
  - `POST /api/org/schedule/generate-week`（周一至六 FULL/周日 OFF TEMPLATE，已存在跳过不覆盖手工行，GENERATE_WEEK 一条审计）。
- 编译坑留证：`DataScope.storeSpec(...).and(lambda)` 链式推断退化为 Specification<Object>，抽 activeStaffInScope()+显式 witness `DataScope.<Staff>storeSpec(...)` 修复。

### 卡2 `e2da7d7`（4 files +223/-17）org internal resolve + txn 派单真源

- ScheduleController 追加 `GET /api/org/internal/schedule/resolve?staffId=&date=`（X-Internal-Token，@RequirePerm("internal:name-map")，网关外裸 404）返 ShiftWindow record（present/assignable/windowStart/End/source）；钟点窗 org 侧单点计算（FULL 09:00-20:00、MORNING 09:00-14:00、MID 14:00-20:00、OFF/LEAVE assignable=false、无行 present=false 不打窗）。
- 新建 txn `OrgScheduleClient.java`（RestTemplate+X-Internal-Token，**永不抛异常**：无行/故障/空窗→degraded 回 WORK 09:00-20:00 营业窗 log.warn；OFF/LEAVE 权威返回不可派不降级）。
- `DispatchService.java` 五处改：resources DOCTOR 循环逐人 resolve 动态窗+ON/OFF 态；dispatch 医生取真窗，越窗 422 动态中文文案「派单时段 X 超出班次 start-end」、OFF/LEAVE「当日休息/请假，不可派单」；followReschedule 校验下沉为每条派单按自身资源取窗（改期 422「该预约已派单，新时段…请先释放派单再改期」）；WORK_START/END 常量语义改营业窗；构造器注入第 4 依赖。房间/DEVICE 不排班恒营业窗。

### 卡3 `2e1602b`（3 files +198/-111）M2 /m2-schedule 前端接真（视图零改）

- 新建 `frontend/src/api/schedule.ts`：四端点薄封装+DTO（走 api/client.ts baseURL '/api'）。
- 重写 `stores/schedule.ts`：mock 全换真源；load() adapt 员工（roleCode→本地 8 角色中文名，不调需 role:view 的 /org/roles）；setShift 乐观更新+失败回滚+toast 后端中文，PUT 落 OVERRIDE 成功以返回 source 覆盖；watch(weekOffset) 上下周自动重载（loadedWeekKey 防重）。
- SHIFTS 五态改名+真钟点镜像后端（上午 09:00-14:00/下午 14:00-20:00/全天 09:00-20:00/休/假）+**NONE 空态**（未排日后端不返行，不造假班；NONE 不进 shiftCycle、不入后端、todayOnDuty 仅计三可派态）；考勤/请假后端当时无端点→attendance/leaves 恒空、KPI 迟到/缺勤/待批恒 0（视图既有空态文案自然呈现）。
- `ScheduleView.vue` 模板/样式零改；零改兼容修两处均在 store 侧（setShift 形参类型含 NONE 防御、NONE time '' 消图例重复）。

### 卡4 `fc5c2e7`（5 files +154/-57）M1 调度时间轴按排班动态化

- 后端 `DispatchService.java`：ResourceView record 增 shiftCode/shiftLabel/source/degraded 四字段随读模型透出；医生 OFF/LEAVE 的 workStart/workEnd 改 **null**（消灭假营业窗，透传真中文态/source/degraded）；房间/设备恒营业窗 source="BUSINESS_HOURS"/degraded=false；shiftLabel() 扩 FULL/MORNING/MID 中文（全天/上午/下午）、未知码返 null 不伪造中文态。
- 前端删写死 SLOTS（9-20 点 23 刻度）：store 改 buildAxis 30 分钟步长+axisFor(type) 取当前 tab 有窗资源最早开工/最晚收工**并集动态成轴**（全无窗如全休日/无资源回落营业窗，网格恒可渲染）；DTO workStart/workEnd 改 string|null；utilization/canDispatch 空窗防御。
- `M1DispatchView.vue`：axis 四 computed 驱动 ruler/cells 内联 gridTemplateColumns、块定位与班次遮罩共用轴边界；OFF/LEAVE 行无遮罩+全行斜纹 row--off+副标题「休息/请假」，degraded 行挂橙色「未排班」小标（title 提示软降级）。

### 卡5 `9f44eb1`（9 files +860/-25）请假申请/批准闭环 + 复制上周

- 新建 `LeaveRequest.java`（leave_no 唯一 LV+yyyyMMdd+三位序号、staff_id/type/start_date/end_date/reason、status PENDING/APPROVED/REJECTED、reviewer/reject_review_at/createdAt）；`LeaveRequestRepository.java`（同员工区间 PENDING 冲突检测、门店数据域列表）。
- 新建 `LeaveController.java` 三端点（311 行）：POST /api/org/leaves（schedule:edit 申请，区间重叠 PENDING→409，LEAVE_APPLY 审计）、GET /leaves（schedule:view 数据域）、POST /leaves/{id}/approve|reject（schedule:approve，非 PENDING→409/越权→404）。
- 新建 `ShiftUpsertService.java` 抽共享件：applyLeaveOverride 批准权威动作逐日覆盖 LEAVE/OVERRIDE（TEMPLATE 铺底与手工行一律改 LEAVE 保证请假不可派，已 LEAVE 幂等，返改动天数，LEAVE_APPROVE days 审计）；copyWeek 复制上周（目标日已有行跳过保留手工/请假、LEAVE 行不复制防无单请假、复制落 TEMPLATE，COPY_WEEK 一条审计）。ScheduleController 追加 POST /api/org/schedule/copy-week（schedule:edit，返 created/skipped/staffCount）。
- 前端：api/schedule.ts 五端点+DTO；store leaves 换真源（替换卡3 诚实恒空）、submitLeave/approveLeave/rejectLeave/copyWeek 真调用+toast+activity，权限静默门，批准后自动重载周网格；ScheduleView 请假登记抽屉（员工/类型 CSelect+起止 date+事由+前端校验链）+审批卡（批准/驳回+单号、reviewer 回显）+KPI 待批联动+复制上周按钮；CInput type 联合补 date。

## 4. 三轨真验（全绿，逐卡留证）

1. **构建轨**：卡1 `mvn -pl org-service package` 二次通过（泛型坑修复后 0 错）；卡2 `mvn package -pl org-service,txn-service -am` EXIT=0（fat jar org 47,182,659/txn 47,788,818）；卡3/卡5 `pnpm build`（vue-tsc --noEmit && vite build）EXIT=0；卡4 pnpm build+mvn package 双 EXIT=0（txn fat jar 47,789,156，md5 e858e133…）。
2. **PG/建表轨**：staff_shift 由 ddl-auto 在 meiyun_core/meiyun_seed 双库建成（id IDENTITY PK、uk_staff_shift_date UNIQUE(staff_id,shift_date) 已 \d 核对）；leave_request 双库建成。
3. **curl/HTTP 轨（seed 18443 为主，core 8443 软降级对照）**：
   - 卡1 core 17 项+seed 12 项（周视图/字典/默认本周归一/PUT 改班落 OVERRIDE 同 id 幂等/非法码 400 中文允许值/外域 404/无 token 401/generate-week 跳过手工行）；
   - 卡2 resolve 四分支（FULL TEMPLATE/MID OVERRIDE/OFF present=true assignable=false/无工号 present=false）+无 token/错 token 双 401+非法日期 400+双网关外 internal 裸 404；派单 SE003 09-15 MID：回落 09:30→422「超出班次 14:00-20:00」、14:00→200、重叠→409、贴尾 19:00-20:00→200；core（staff_shift 0 行）全 degraded 软降级，PUT OFF→422「当日休息，不可派单」、LEAVE→「当日请假」、MORNING 热玛吉 90min 结束越窗→422、贴尾→200；followReschedule 活跃改期越窗 422 提示先释放、改无行日 200 派单跟随移动、改 OFF 日 422；
   - 卡4 resources DOCTOR 六场景（MID/FULL/OFF null/degraded/ROOM/DEVICE 恒营业窗+source=BUSINESS_HOURS）+core 软降级+写侧窗外 422/窗内 200；
   - 卡5 申请/同区间重叠 409 带 LV 冲突单号/批准/驳回/列表+PG LEAVE OVERRIDE 覆盖原 FULL。
4. **审计轨（append-only 哈希链）**：core audit_log 602-611（DISPATCH/RELEASE/RESCHEDULE_FOLLOW/SCHEDULE SET，422 不落账）；seed audit_log 290-301（290-298 curl 九条+299 APPLY/300 APPROVE/301 LEAVE_APPROVE days=2）哈希链 broken=0、中文 payload。
5. **浏览器 UI 轨（Chrome DevTools MCP）**：
   - 卡3 /m2-schedule：KPI 今日在岗 4/迟到/缺勤/待批 0、周范围 9/14-9/20、江医生周二下午、周日四休、图例「未排」干净、点格五态循环、下周全员「未排」空态、控制台零报错；
   - 卡4 /m1-dispatch：今日 FULL 23 刻度动态轴、09-15 并集轴 09:00-20:00 江医生遮罩 left45.45%/width54.55% 几何精确、班外格斜纹、09-20 全休 row--off+回落轴不塌、09-21 degraded 橙「未排班」、治疗室 5 室全宽遮罩、零 error/warn；
   - 卡5 完整闭环：抽屉提交 LV20260916003（苏治疗 年假 10-01~02）→KPI 待批 0→1→UI 批准→KPI 归 0+审批人许店长回显→9/28 周网格 10/1、10/2 自动显「假」覆盖原 FULL（console 仅人为探针 3 条，非应用异常）。
6. **双栈部署轨**：org/txn fat jar docker cp+restart 四容器（meiyun-org/txn-service、meiyun-seed-org/txn-service）均 healthy；前端 dist 两栈干净换载（先 rm -rf 再 cp，避旧 hash 残留），8080/18080 HTTP 200、网关 18443/8443 验活；部署指纹：org 卡5 jar md5=b1bb05d220a1…、txn 卡4 jar md5=e858e133…、前端 bundle=index-C2UsCTr4.js。
7. **提交轨**：bbcdb46/e2da7d7/2e1602b/fc5c2e7/9f44eb1 逐一 commit 紧跟 push，ls-remote 与本地一致。

## 5. 如实说明

- **房间/设备不排班**：本批真实排班仅覆盖 DOCTOR（员工）；ROOM/DEVICE 恒营业窗 09:00-20:00（source=BUSINESS_HOURS），与 L144「医生/房间班次表」中房间维度的关系如实标注——房间可用时间仍走营业窗，未造房间排班实体（无业务源，避免臆造）。
- **软降级是设计行为非故障掩盖**：员工无排班行或 org 服务不可用时，txn 回退营业窗并以 degraded=true+橙色「未排班」小标诚实透出；OFF/LEAVE 为权威态**绝不降级**（即使 org 超时，OFF/LEAVE 走已取到的权威响应；仅无行/故障才降级）。
- **考勤仍诚实空态**：M2 排班页 KPI 的迟到/缺勤/考勤明细仍无后端源，保持恒 0/空态（卡5 仅接通请假，未造考勤数据）。
- **M2 余 12 页未动**：工单/日结/申购/报损/绩效/周报/巡检/拓客/唤醒/异常等仍 ⬜ Backlog，聚合行保持 ⬜ 并注明 M2 1/13。
- **core 生产库零污染**：所有写测试数据已还原（见下），meiyun_core staff_shift=0、leave_request=0。

**测试数据还原（铁律：测试数据必须还原）**：
- seed staff_shift 回到 **28 行基线**（id1-28=27 TEMPLATE+1 OVERRIDE：id1 SE003 09-15 MID）；卡5 测试 id≥56 共 56 行删净、_b54_restore_backup 备份表 DROP（ON_ERROR_STOP=1 事务性）；
- seed leave_request 3 行删净回 **0**，GET /leaves→[]、09-28 周 staff 无班次；
- core 手工排班行 DELETE 回 0（audit 602-611 append-only 留痕）、预约 000004 改回 09-16 14:00、派单恢复测试前两活跃态（id2 E001 14:00-15:00/id5 E003 14:00-15:30 SCHEDULED）；seed 测试派单 id19/id20/id21 均 RELEASED。

## 6. 进度落账

**完成度数字跃迁**：✅ **108→109**/166=约**66%**、🔧 **1** 不变、⬜ **56→55**（约33%）；**域⑦ 组织与权限 10✅0🔧14⬜ → 11✅0🔧13⬜；域⑧ 平台与基建 34✅2🔧31⬜ 完全不动**。新增完成模块 **1 个**（M2 · 排班 /m2-schedule，M2 13 页首切真 1/13），勾销 04-backlog **L144 一行**。

- **04-backlog**：L144 全行勾销——首列标题打删除线（原文零丢失），注记列保留 B51 登记原文+追加加粗 B54 闭合注记（五 commit、staff_shift 实体+全员周排班+周例+internal resolve 三处真源+M2 整页接真+M1 动态轴+请假 LEAVE 联动+复制上周），末列改 `✅ B54`。L141（B53 A3）保持不勾销。
- **03-timeline**：表底追加 P5-B54 六列行（commits 列五短 hash、文档列 DELIVERY-P5-B54-2026-09-16、末列写数字跃迁 108→109/域⑦ 10→11/L144 勾销/M2 1/13）。
- **02-modules**：域⑦表 M2 聚合行照 A1（L164）范式保持 ⬜、备注改「排班页 B54 已切真=M2 1/13，余 12 页 Backlog」；其后新增明细行「M2 · 排班（/m2-schedule）✅ P5-B54」；M1·调度中心既有 ✅ 行尾接卡2/卡4 纵深（txn 排班真源+动态轴）。
- **01-dashboard**：L12 108→109/约66%、L14 56→55/约33%、L18 口径流水尾接 B54 条、L22 全量页面口径约65%→约66%（叙事补 M2 1/13）、L35 域⑦ 10→11/14→13+M2 叙事更新；L36/L38 域⑧ 数字与构成不动。
- **00-history**：顶部「最近更新」整段替换为 B54 简报（109/166 约66%、🔧1、⬜55、域⑦ 11/13、五 commit、M2 1/13、DELIVERY 指引），原 B53 段加「> 上一批 B53：」前缀下沉。
- 索引+五分册+本 DELIVERY+哨兵同一 commit 原子提交，提交后通读勾稽（①完成度=②状态计数=③时间线批次；109/55/166、域⑦ 11/13 三处一致）。

## 7. 下一批

- **P5-B55（C=L133 合并 L46 真实 impersonate）**：JWT act/realSub 双 claim+meiyun-security 全服务回归、审计 realSub·act 双留痕、短时效切换 token+版本号回收、前端换 token+强制横幅+一键退出；授权矩阵 超管→店长→员工、总部员工可跨店、门店员工不可跨店（DataScope 403）。
- **P5-B56（D=L123 报告哈希验真 UI）**：先冻结规范化字节（UTF-8 BOM+CRLF、文件名去下载时刻）→finance report_job 加 content_hash（SHA-256 落库原始字节）→验真端点 report:verify+前端验真卡；注意现有 finance/ai 两套体系。
- 无用户新指令前不擅自提前开工；哨兵批末置 DORMANT。
