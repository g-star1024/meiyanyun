# DELIVERY-P5-B55 · 真实 impersonate 纵深安全批（C 方向 L133 合并 L46，JWT act/realSub 双 claim+授权矩阵，3 代码 commit）（2026-09-16）

> **批次性质**：用户 A/B/C/D 四方向拍板之 C 方向纵深安全批——04-backlog **L133「M1 合规 impersonate 真实身份切换」与 L46「超管 Impersonate（impersonation 登录）」合并项全行勾销**：把 B49 卡9 仅审计留痕真实化的「内存假会话」升级为真实身份切换——JWT 增 `realSub`（真实超管）/`act`（被切换人工号）双 claim，meiyun-security 共享库全服务无状态解析；审计 actor 双通道口径经 AuditBoundary 单点收敛（业务身份=sub、审计真实人=realSub，payload 双标记注入）；org-service 签发 30min 短时效切换 token（不滑动、不续期）+exit 重签常规 token；前端换发 token+全局危红强制横幅（真实人/目标人/事由+一键退出）+刷新快照本地还原+代操作态 401 自动退回真实人；授权矩阵 超管→店长→员工（禁链式/切超管/切本人/离职目标，理由必填）、DataScope 随目标人收窄（总部跨店放行、门店跨店 403/404）。
> **用户拍板（C 方向原文照录）**：「P5-B55（C 方向 L133 真实 impersonate：JWT act/realSub 双 claim+授权矩阵）与 P5-B56（D 方向 L123 报告哈希验真 UI）开工吧」；范围锁定（B54 存档）：「JWT act/realSub 双 claim+meiyun-security 全服务回归、审计 realSub·act 双留痕、短时效切换 token+版本号回收、前端换 token+强制横幅+一键退出；授权矩阵 超管→店长→员工、总部员工可跨店、门店员工不可跨店（DataScope 403）」；总授权「剩下的我没有决定的你按照推荐来就行」（据此定案：TTL=1800s 不滑动、HS256 共享密钥无状态不引入会话中心/版本号载体、刷新放弃代操作走快照本地还原）。（同批 D 方向 L123→P5-B56 本批不触碰。）
> **提交清单（3 代码 commit 全 push origin/main）**：
> - 卡1-2 `8f78169` feat(b55)：meiyun-security 双 claim+AuditBoundary 审计边界收敛+org 切换/退出端点+5 业务服务审计口径统一，12 files +300/-19
> - 卡3 `557a20a` feat(frontend)：真实 impersonate 换发——auth store 换 token/快照还原、client 401 自动退回真实人、全局危红横幅/一键退出、合规页删内存假会话接真实端点，7 files +337/-85
> - 真验修复 `1331a21` fix(org)：IMPERSONATE_START/END 审计 payload 显式携带 realSub/act 双标记（START 请求线程仍是原超管 token，控制层显式写入，审计自证不依赖线程态），1 file +7/-4
>
> 合计原始 **20 files +644/-108**（各代码 commit 含当卡哨兵回写；去重唯一代码文件 20 个）；卡5 零代码为本批 docs 原子提交。
> **铁律对齐**：0 先查再写（卡0 全站侦察 JWT 自包含被 7+ 服务本地验签无会话中心、43f009b 双通道 actor 语义可复用、前端 m1Compliance 内存假会话雏形）、-1 不臆造（旧 token 无 claim 兼容普通身份、门店静态下拉甄别为非缺陷、刷新无 END 审计为设计行为）、6 多栈双栈换载 16 容器 fat-jar 冒烟、7 三轨真验（构建+193 单测+curl 授权矩阵+PG 审计+浏览器 UI-01/UI-02 全绿）、8 一卡一 commit 紧跟 push、9/10 台账六册原子提交回写通读勾稽、11 哨兵 HANDOFF-AUTO 全程心跳。

## 1. 背景与记账口径

- **L133/L46 登记实质**：B49 卡9 M1 合规中心切真时，页面「代操作」能力被收窄为**仅审计留痕真实化**——start/end 经 POST /api/audit append 真实落库，但 activeSession 只是前端内存单会话，并未真正置换登录身份；B50 评估为「远期平台/安全专项大项」登记 L133 四要件：①JWT 增 act/realSub claim 并 meiyun-security 全服务解析回归；②全服务审计 actor 口径全局统一（B50 卡8c `43f009b` 已先行收敛 audit append 双通道 actor，可直接复用通道语义）；③org-service 签发切换态 token 与回收/超时；④前端 auth store 换 token 与切换态横幅。L46「超管 Impersonate（impersonation 登录）」为同一事项的平台批登记。本批即四要件的真实落地，两行合并勾销。
- **落地范围（对四要件逐一兑现）**：
  1. **双 claim 全服务解析**：HS256 共享密钥无状态验签架构不变（不引入会话中心/吊销表）；JWT claim `sub`=目标人工号、`realSub`=真实超管工号、`act`=被切换人工号，`scope/stores/perms` 按**目标人**收窄重签；`LoginUser` 增 12 参构造（新增 realSub/act/issuedAt，保留 9 参兼容），旧 token 无新 claim 时自然退化为普通身份；
  2. **审计 actor 口径统一**：`DataScope` 拆 `currentActor`（sub 业务身份，数据域判定用）与 `currentRealActor`（realSub 审计真实人）+impersonating/currentAct；新建 `AuditBoundary` 单点收敛审计 actor 选取与 payload 双标记（realSub/act/impersonating）注入；ai/customer/marketing/org/txn 五服务 RestAuditRecorder 与 audit-service JWT 直连通道、outbox 补偿链统一口径；
  3. **短 token 签发/回收/超时**：org-service `POST /api/org/auth/impersonate`（目标工号+理由）签发 TTL=**1800s（30min，不滑动不续期，SecurityProperties.impersonateTtl）**短 token，常规 token TTL=43200s；`POST /api/org/auth/impersonate/exit` 重签常规 token；无状态故「回收」=exit 主动重签+短 TTL 自然过期双通道；
  4. **前端换 token+强制横幅+一键退出**：auth store 换发短 token 并落 sessionStorage 快照；CShellDesktop 危红常驻横幅（不可关）真实人/目标人/事由+一键退出；整页刷新消费快照本地还原为真实人（不调 exit）；axios 代操作态 401 本地还原+warning toast，不重试写请求防提权。
- **授权矩阵（门控在 org 控制层）**：仅真实 **SUPER_ADMIN**（realSub 为空且本人为超管，禁链式）可发起；禁切超管目标、禁切本人、禁切离职目标、理由必填；切换后权限/数据域完全以目标人计算——超管→店长→员工逐级、总部员工（REGION/GROUP 域）跨店放行、门店员工跨店 DataScope 403/404（沿用越权 404 隐身范式）。
- **完成度记账（纵深安全批，数字一律不变）**：本批无新页面、无模块 ⬜→✅ 状态跃迁、无 🔧 成因消解，能力挂载于既有 ✅ 模块（登录/JWT、RBAC、M1 合规中心、审计链）：
  - 总数 **✅109/166=约66%、🔧1、⬜55（约33%）全部不变**；勾销 04-backlog **L133+L46 两行**。
  - 域归属：**域⑦ 组织与权限 11✅0🔧13⬜ 不变**（登录/JWT、RBAC 行纵深尾接）；**域⑧ 平台与基建 34✅2🔧31⬜ 不变**（M1 合规中心行纵深尾接，不新增模块行）。三证勾稽（完成度=状态计数=时间线批次），同 B50/B51/B52/B53 纵深批先例。

## 2. 影响面只读核实（卡0 侦察结论）

- **JWT 无状态验签是全平台基座**：meiyun-security 共享库 `JwtTokenUtil` HS256 共享密钥签发/验签，被 org/txn/customer/marketing/store/finance/ai/audit 等 **7+ 服务本地验签**，**无会话中心、无服务端 token 存储**——故真实切换不能走「服务端置换会话」路线，只能在自包含 JWT 内增 claim 并全服务解析；「回收/版本号吊销」若引入黑名单需新增状态载体（Redis/DB 表）与每请求查问，破坏无状态架构，按推荐定案为 **30min 短 TTL 不滑动 + exit 重签常规 token**，不引入版本号载体。
- **审计双通道 actor 已有收敛基座**：B50 卡8c `43f009b` 已让 audit append 五通道 actor 按通道强制（P1 伪造 HACK→SE101），本批直接复用该通道语义：真实 actor 从 JWT realSub 取，业务 actor 从 sub 取，单点在 AuditBoundary 收敛，五业务服务 RestAuditRecorder 与 audit-service 直连/outbox 链路统一注入。
- **前端 impersonate 现存雏形=内存假会话**：B49 卡9 在 m1Compliance store 内维护内存 activeSession + 页内假 imp-bar + `appendComplianceAudit` 前端直调 audit append——仅留痕不换身份；CShellDesktop 全局壳四门控（B50 卡5 `7f08103`）、auth store、api/client.ts 401 拦截是真实换发与全局横幅的承接外壳。
- **权限种子零新增**：impersonate 端点门控限定真实 SUPER_ADMIN（硬门控而非 PermissionMatrix 业务权限点），不新增权限码、不播新种子；DataScope 门店越权 404 范式沿用。
- 网关是 gateway/ 下 Go 服务，本批端点全部 `/api/org/auth/**` 正常业务路由，不涉及 internal 与 Go 侧改动。

## 3. 代码改动（按 commit）

### 卡1-2 `8f78169`（12 files +300/-19）meiyun-security 双 claim+审计边界+org 端点+5 服务统一

- **meiyun-security 共享库（5 文件）**：
  - `LoginUser.java`（+18）：新增 12 参全参构造（realSub/act/issuedAt），保留 9 参构造向后兼容；增 realSub/act/issuedAt 字段与 impersonating() 判定（realSub 非空即代操作态）。
  - `JwtTokenUtil.java`（+19）：签发/解析支持 realSub/act claim；常规登录不携带两 claim；切换态 token 按目标人 scope/stores/perms 重签并带 realSub/act；解析侧旧 token 缺 claim 时填 null 自然退化为普通身份。
  - `SecurityProperties.java`（+16）：新增 `impersonateTtl` 配置，默认 **1800s（30min）**，常规 token TTL 43200s 不变。
  - `DataScope.java`（+35）：拆 currentActor（sub 业务身份，门店数据域 Specification 判定用）与 **currentRealActor**（realSub 审计真实人）；增 impersonating()/currentAct() 访问器；数据域计算严格按目标人 sub（门店员工跨店被收窄，总部域 GROUP/REGION 放行）。
  - `AuditBoundary.java`（**新建 66 行**）：审计 actor 单点收敛——真实 actor 取 realSub（无则 sub），payload 注入 `realSub`/`act`/`impersonating` 三标记；五业务服务与 audit 直连/outbox 统一经此边界，杜绝各通道口径漂移。
- **org-service（1 文件）**：`AuthController.java`（+131）新增两端点：
  - `POST /api/org/auth/impersonate`：门控=请求人须真实 SUPER_ADMIN（realSub 为空+本人超管，禁链式）；校验目标存在且在职、非超管、非本人、reason 非空（否则中文 400/403/404）；按目标人 scope/stores/perms 签发 30min 短 token（sub=目标、realSub=超管、act=目标工号）；落 IMPERSONATE_START 审计。
  - `POST /api/org/auth/impersonate/exit`：代操作态可调，按 realSub 重签常规 token（TTL 43200s、无 realSub/act）；落 IMPERSONATE_END 审计。
- **5 业务服务审计统一（ai/customer/marketing/org/txn 各 RestAuditRecorder.java，各 +4）**：审计 actor 与 payload 双标记改经 AuditBoundary 注入，共 5 文件 +20。
- **audit-service（1 文件）**：`AuditController.java`（+14）：JWT 直连 append 通道识别 realSub/act，落库 actor 取真实人、payload 保留双标记，与 RestAuditRecorder 通道口径一致。
- 构建：8 服务 mvn package 全 EXIT=0，**193 个测试全绿**；双栈 16 容器 fat-jar 换载冒烟 healthy。

### 卡3 `557a20a`（7 files +337/-85）前端真实换发+全局横幅+合规页接真

- `api/auth.ts`（+22）：impersonate/exit 两端点薄封装+DTO。
- `stores/auth.ts`（+153）：`startImpersonate` 成功后先 `saveImpSnapshot`（sessionStorage 键 `meiyun_imp_snapshot` 存真实人 token/session）再换发短 token；`exitImpersonation` 走后端 exit 重签常规 token；`restoreSession` 启动最优先消费快照——整页刷新时代操作态**本地还原**为真实人（不调 exit、不跳登录）；localStorage `meiyun_token`（裸 JWT）/`meiyun_session`（JSON：staffId/name/impersonating/realSub/act）。
- `api/client.ts`（+60）：代操作态遇 401（短 token 过期）本地还原快照+warning toast「代操作已超时，已退回真实身份」，**不重试写请求**（防短 token 失效后以真实人身份误提交提权/越权写）。
- `components/CShellDesktop.vue`（+88）：`.imp-banner` 危红常驻横幅（**不可关闭**）展示真实人→目标人+事由+一键退出按钮；代操作态禁用顶栏 dev-login 角色切换、「超管代操作」入口按钮随 `v-if auth.isSuper`（代操作态 isSuper=false=目标人权限）消失。
- `api/compliance.ts`（+18/-）、`stores/m1Compliance.ts`（+41/-）、`views/M1ComplianceView.vue`（+40/-）：删除内存假 activeSession/页内假 imp-bar/`appendComplianceAudit` 前端直调，代操作弹层改调真实 org `/auth/impersonate` 端点，审计统一后端落库，视图接 store 真状态。
- 构建：`pnpm build`（vue-tsc --noEmit && vite build）EXIT=0。

### 真验修复 `1331a21`（1 file +7/-4）START/END 审计双标记显式化

- 真验 PG 取证发现：IMPERSONATE_START 请求到达控制层时，**线程内仍是原超管常规 token**（impersonating=false，短 token 尚未签发），AuditBoundary 按线程态判定不会注入 realSub/act；END 同理存在线程态与语义不一致。
- 修复（`AuthController.java` +7/-4）：START/END 两条审计在控制层**显式写入** realSub/act 双标记，actor 显式传真实超管（START）/realSub（END），使审计自证不依赖线程态；业务写请求（持短 token）仍走 AuditBoundary 自动注入。审计 618（END）起、619（START）起双标记完整（见 §4/§5 演进留痕）。

## 4. 三轨真验（全绿，逐轨留证）

1. **构建/单测轨**：卡1-2 8 服务 `mvn package` 全 EXIT=0、**193 测试全绿**，双栈 16 容器 fat-jar docker cp+restart 全 healthy；卡3 `pnpm build` EXIT=0（vue-tsc+vite）；1331a21 org-service 重 package+双栈 org 两容器换载 healthy。
2. **PG/审计轨（meiyun_core audit_log append-only）**：真验共留 **id 615-626 全 12 行**，biz_type 全 COMPLIANCE/IMPERSONATE_*、actor 全 SE101（周岚真实超管），哈希链连续 broken=0：
   - 615 START E005「P5-B55 真验：排障授权代操作」、616/617 START E007/E007「跨店收窄真验」——**此三条为 1331a21 修复前所记，payload 仅 {ip,risk,detail,target}，无 realSub/act 双标记**（演进留痕，append-only 不改写）；
   - 618 END E007（会话 0 分钟，**首次带 act/realSub**）、619 START E005「P5-B55 复验双标记」（**START 首次双标记完整**）、620 END E005；621 START E001 刘治疗师「数据域真验-员工」、622 END E001；623 END E005；624 START E005 工单号 VERIFY-B55-UI-01、625 END E005 会话时长 2 分钟、626 START E005 VERIFY-B55-UI-02（**无 END=整页刷新放弃代操作**，设计行为）。
3. **curl/HTTP 授权矩阵轨（core 8443，真超管 SE101）**：
   - 正向链：超管 SE101→店长 E005→员工 E001 逐级切换 200，短 token 解码 sub=目标/realSub=SE101/act=目标工号/scope·stores·perms 按目标人/ttl=1800；exit 200 重签 sub=SE101、realSub/act=null、scope=GROUP、ttl=43200；
   - 数据域：切总部域（REGION/GROUP）目标后跨店查询放行；切门店员工 E001（STORE 域）后访问外店数据 403/404（DataScope 按目标 sub 收窄），本店数据 200；
   - 门控负向：非超管发起 403、链式（代操作态再发起）403、切超管目标 400/403、切本人 400、切离职目标 404、理由空 400（全中文文案）；
   - 短 token **手工置过期**后业务请求 401（30min 不滑动不续期实证）；**旧常规 token 无 realSub/act claim** 全服务解析兼容（impersonating=false、现行权限不受影响）；无 token 401。
4. **浏览器 UI 轨（Chrome DevTools MCP）**：
   - **UI-01**：SE101 登录→合规页对 E005 发起代操作（事由 VERIFY-B55-UI-01）→全局**危红横幅**出现并常驻不可关，显示真实人 周岚(SE101)→目标人 李店长(E005)+事由；顶栏身份变「李店长」；「超管代操作」按钮随 `v-if isSuper` 消失；顶栏 dev-login 角色切换被拦截并 toast；点一键退出→调后端 exit、横幅消失、身份/权限还原 SE101；
   - **UI-02**：再次代操作 E005 后**整页刷新**→restoreSession 最优先消费 sessionStorage 快照本地还原：不调 exit、不跳登录，直接回到真实人 SE101；复查快照已消费为 null、JWT sub=SE101/realSub=null/act=null/scope=GROUP/ttl=43200、session.impersonating=false（该次对应审计 626 有 START 无 END，短 token 自然过期，设计一致）；
   - 代操作态 401 钩子（短 token 超时）本地还原+warning toast、不重试写请求，控制台无应用 error。
5. **双栈部署轨**：meiyun-security 升级后依赖它的 8 服务 fat-jar 双栈换载（org/txn/customer/marketing/store/finance/ai/audit，共 16 容器）均 healthy；前端 dist 双栈干净换载，8080/18080 200、网关 8443/18443 验活。
6. **提交轨**：8f78169/557a20a/1331a21 逐一 commit 紧跟 push origin/main，ls-remote 与本地一致，HEAD=1331a21。

## 5. 如实说明

- **门店「23 家下拉」非 DataScope 缺陷**：浏览器真验中曾见代操作目标选择下拉列出 23 家门店，经核实系前端静态展示数据（门店字典），**不代表数据域放行**；DataScope 收窄以后端 API 为准——门店员工 E001 经真实接口访问外店=0 条/403/404、本店 9 条，静态下拉与越权判定是两回事，不据此修代码。
- **刷新放弃代操作无 END 审计是设计行为非漏记**：整页刷新走快照**本地还原**（不调 exit 端点），故后端无 IMPERSONATE_END；30min 短 token 不滑动自然过期，他人无法复用；START（626）已留痕且 actor=真实超管可完整追责。若需「刷新仍保持代操作」反而要把短 token 持久化/续期，削弱安全性，故按推荐取本地还原。
- **审计演进诚实留痕（不改写历史）**：615-617 三条早期 START 落于 1331a21 修复前，payload 无双标记；618（END）起、619（START）起双标记完整。audit_log 为 append-only 哈希链，**不回填、不删除**，在本 DELIVERY 与台账如实区分修复前后口径。
- **不引入吊销列表/版本号是有意取舍**：无状态 JWT 架构下做主动吊销需 Redis/DB 黑名单+每请求查问，违背基座设计；以「30min 短 TTL 不滑动 + exit 重签 + 前端短 token 不落地长期存储（快照仅存真实人常规 token）」组合控制风险窗口，已在哨兵/本文明示。
- **ETEST 测试数据零残留**：真验临时员工 ETEST 行已 DELETE 并连续复查 `count=0`；除 append-only 审计 615-626 外，meiyun_core 无其他测试污染。

**测试数据还原（铁律：测试数据必须还原）**：
- meiyun_core `staff` 表 ETEST 临时员工删除，复查 `SELECT count(*) ... ETEST = 0`；
- audit_log 615-626 为 append-only 合规凭证**保留**（哈希链不可删），已在台账注明；
- 代操作仅切换既有在职真实账号（E005 李店长/E007/E001 刘治疗师/SE101 周岚），未造业务单据，无业务表数据需还原；双栈服务换载后均为正式产物运行。

## 6. 进度落账

**完成度数字（纵深批不变）**：✅ **109**/166=约**66%**、🔧 **1**、⬜ **55**（约33%）全部不变；**域⑦ 组织与权限 11✅0🔧13⬜ 不变；域⑧ 平台与基建 34✅2🔧31⬜ 不变**。新增完成模块 **0 个**，勾销 04-backlog **L133+L46 两行**。

- **04-backlog**：
  - L133 全行勾销——首列标题打删除线（原文零丢失），注记列保留 B49/B50 登记原文+追加加粗 B55 闭合长注（三 commit、realSub/act 双 claim 全服务解析、AuditBoundary 双通道 actor 收敛、org impersonate/exit 端点、30min 短 token 不滑动、授权矩阵、前端换发/全局危红横幅/一键退出/刷新快照还原/401 本地还原、三轨证据、审计 615-626 演进留痕），末列 `远期（平台/安全专项大项）` 改 `✅ B55`；
  - L46「超管 Impersonate（impersonation 登录）」全行勾销——首列删除线，末列 `平台批` 改 `✅ B55（与 L133 合并项）`。
- **03-timeline**：表底追加 P5-B55 六列行（commits 列 8f78169/557a20a/1331a21、文档列 DELIVERY-P5-B55-2026-09-16、末列写明纵深批数字不变+勾销 L133/L46+审计 615-626 留档）。
- **02-modules**：L108「登录 / JWT」✅ 行缺口/备注列尾接 B55 纵深注记（realSub/act 双 claim、30min 短 token、AuditBoundary 审计双标记、org impersonate/exit、全局横幅/一键退出/刷新快照还原，三 commit，数字不变）；L142「M1 · 合规中心」✅ 行尾接一句（代操作弹层由内存假会话改真实 org 端点、拆页内假 imp-bar、审计统一后端落库）；L110 角色 RBAC 行保持（授权矩阵门控在 org 控制层，不新增权限码）。
- **01-dashboard**：L12-15 状态表数字一字不动；L18 口径流水尾接 B55 条；L22 全量页面口径约66% 不变（无新页面）；L35 域⑦行 11/0/13 数字不动、叙事尾接 B55 impersonate 双 claim+AuditBoundary+全局横幅纵深注记；L36/L38 域⑧ 34/2/31 与构成注一字不动。
- **00-history**：顶部「最近更新」整段替换为 B55 简报（数字不变 109/166 约66%、🔧1、⬜55、域⑦ 11/0/13、域⑧ 34/2/31、三 commit、勾销 L133/L46、审计 615-626 留痕、DELIVERY-P5-B55 指引），原 B54 段加「> 上一批 B54：」前缀下沉。
- 索引+五分册+本 DELIVERY+哨兵同一 commit 原子提交，提交后通读勾稽（①完成度=②状态计数=③时间线批次；109/55/166、域⑦ 11/13、域⑧ 34/31 一致；04 L133/L46 双勾销）。

## 7. 下一批

- **P5-B56（D=L123 报告哈希验真 UI，仅第①类）**：先冻结规范化字节（UTF-8 BOM+CRLF、下载文件名去下载时刻，保证同数据两次导出字节一致）→finance report_job 加 `content_hash`（SHA-256 落库原始字节）→`report:verify` 验真端点（重算比对）+前端 M1 报表中心验真卡（上传/粘贴哈希或重下比对，出真/伪+生成时刻）；**注意现有 finance/ai 两套报表体系**，先卡0 只读侦察确认落点（B49 卡11 report 域 report_job content BYTEA/ReportCsvBuilder 为 finance 主线），第②类 DSAR/consent/巡检合规远期大项不并入。
- 无用户新指令外不扩大范围；哨兵批末置 DORMANT，B56 开工再翻 ACTIVE。
