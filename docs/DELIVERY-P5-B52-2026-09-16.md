# DELIVERY-P5-B52 · seed 栈别名层纵深加固 + M1 派单自由时段开放批（2026-09-16）

> **批次性质**：B51 批末新增登记两行（04-backlog L146 派单自由时段产品决策、L147 seed 栈同名服务别名 DNS 轮询串扰环境隐患）经用户 2026-09-16 凌晨拍板后即刻施工的小型纵深批——卡0 侦察激活 + 卡1 环境治理 + 卡2 业务纵深 + 卡3 批末落账。
> **用户拍板（原文照录）**：「1、优先处理 seed 栈 DNS 轮询串扰隐患，②seed 容器服务别名加 seed- 前缀；2、M1 调度派单自由时段（start 不锚定 apptTime）：卡5 已闭合改期联动半项，此半项属产品决策，开放；3、完成后开工 P5-B52」。
> **提交清单**：fix `0ea8983`（卡1 seed 栈别名层纵深，05:45 已 push）、feat `cc00948`（卡2 派单自由时段，06:23 已 push）；docs 心跳 `17ac5cf`（06:11 接力会话哨兵）+ 批末本批 docs 原子提交。卡0/卡3 零代码无 commit。
> **铁律对齐**：0 先查再写（卡0 双网/别名/nginx/marketing 只读侦察、接力会话法医鉴识+独立复验）、1 API 三处一致（DispatchCmd/前端类型/store/view 全链路 start 透传）、5 前端真实 API（无 mock，点击格带 slot 真派单）、6 双栈构建部署（txn 镜像+frontend dist 双栈换载）、7 三轨真验（curl 五分支+PG 派单/审计哈希链+Playwright Chrome 轨）、8 一卡一 commit 紧跟 push、9/10 台账分册原子提交、11 哨兵 HANDOFF-AUTO 全程心跳与接力。

## 1. 背景与目标

| 登记行 | 内容 | 卡序 | 结果 |
|---|---|---|---|
| L147（环境治理） | seed 栈与正式栈同 Docker 网络+同名服务别名致 DNS 轮询串扰（txn 服务间调用随机命中 seed 栈，seed 库客户号 SC 前缀查 M002 返回 200 空 Map，间歇性空数据而非报错） | 卡1 | ✅ 闭合（拍板方案②：seed 十服务键加 seed- 前缀） |
| L146（业务纵深） | M1 调度派单自由时段（start 不锚定 apptTime，手工指定班次内任意时段）——B51 卡5 仅闭合改期联动半项 | 卡2 | ✅ 闭合（DispatchCmd 可选 start，缺省回落 apptTime） |

## 2. 卡0 哨兵激活+只读侦察（05:10-05:11，零代码）

- 六册台账开工前全读；双栈运行态实证：10-_meiyun 网络 15 容器与 meiyun-seed_seed-net 14 容器零跨网附着——别名前缀系**别名层纵深加固而非现网抢修**（网络层本已隔离，串扰发生在共享同一 compose 网络的历史窗口与同名别名解析层）。
- seed marketing 容器 Exited(1) 31h：Flyway V11-14 乱序 pending、application.yml 缺 `spring.flyway.out-of-order: true`（B38 audit 同款修法）。
- frontend nginx 配置 4 处裸别名 `proxy_pass http://gateway/...`：双栈共享同一 frontend 镜像，裸别名在 seed 网络解析存在隐患，需 envsubst 模板按栈注入上游。
- 卡2 全链路五环节锚定：Controller 透传零改 / Service start 锚定 apptTime 且 DispatchCmd record 无 start / api 类型无 start / store 请求体漏传 / view 格子点击不带 slot。

## 3. 卡1 seed 栈服务别名层纵深加固（fix `0ea8983`，05:45 已 push）

**缘起**：B51 卡6 真验破获「name-map 空 Map 之谜」——同名服务在共享网络内 Docker DNS 轮询，服务间调用随机命中异栈容器，表现为间歇性空数据而非报错，极难排查。用户拍板方案②。

**改动（六个子步全闭合）**：

- a. seed compose 十个服务键统一加 `seed-` 前缀（seed-gateway/seed-org/seed-txn/seed-marketing/seed-ai/seed-customer/seed-audit/seed-finance/seed-store/seed-frontend），两处 depends_on 同步更名；容器名与网络别名随服务键根治，DNS 轮询不再跨栈。
- b. frontend nginx 改 envsubst 模板：4 处 `proxy_pass http://gateway/...` 改 `${GATEWAY_UPSTREAM}` 变量注入；Dockerfile COPY templates 并以 envsubst 渲染启动；**正式栈默认 GATEWAY_UPSTREAM=gateway 保 app 栈零影响，seed 栈注入 meiyun-seed-gateway**，双栈继续共享同一前端镜像（铁律 -1 样式/现网保护）。
- c. seed marketing 修复：application.yml 加 Flyway `out-of-order: true`，新增 V32 迁移对 schema drift 做幂等 ALTER 补列（历史乱序迁移一次性对齐）。
- d. seed 栈 down/up 重建、网络重接、双栈镜像与 dist 部署。
- e. 三轨真验全绿：双向 DNS 隔离实证（异栈别名 NXDOMAIN）、双栈 nginx 上游渲染正确、8080 与 18080 双前端 200、seed marketing V11-14+V32 迁移全部生效且 healthy。
- f. fix commit + push origin/main。

## 4. 卡2 M1 派单开放自由时段（feat `cc00948`，5 files +46/-25，06:23 已 push）

**缘起**：B49 卡12 调度中心初版派单 start 强制锚定预约 apptTime；B51 卡5 闭合改期联动半项后，自由时段半项以产品决策转登记 L146。用户拍板「开放」。

**全链路五环节改动（铁律 1/5）**：

- 后端 `DispatchService`：`DispatchCmd` record 增第四字段 `String start`；新增 `HHMM = ^([01]\d|2[0-3]):[0-5]\d$` 正则——`start` 为 null/空白时回落预约 apptTime（老客户端不传行为不变，兼容 B49/B51 全部既有调用方），非空 trim 后正则校验，非法格式 422「派单时段格式应为 HH:mm（如 09:30）」；随后统一进入既有班次窗 09:00-20:00 校验（越界 422 文案改「请选择班次内的空闲时段」）、同资源同日重叠 409、DOCTOR 与预约指定医生一致性、SKU duration_min 真源时长、审计 DISPATCH/RELEASE——既有校验与审计链零旁路复用。
- 后端 `DispatchController`：仅 javadoc 同步透传 start?（请求体绑定零逻辑改动）。
- 前端 `api/dispatch.ts`：派单 DTO 加 `start?: string`，注释三处一致（自由时段/缺省回落）。
- 前端 `stores/m1Dispatch.ts`：dispatch 请求体透传 start。
- 前端 `M1DispatchView.vue`：`slotClick(resourceId, slot)` 点击具体时间格、start 取该格 slot 而非 job.apptTime；选单后该资源班次内**全部非占用空闲格可点**（原仅 apptTime 等值格可点）；点击时 canDispatch 预检（格 busy+班次窗）+后端 422/409 兜底，冲突 toast 中文；文案/图例改自由选时口径；cell 加 `data-slot` 测试锚点；**CSS 零改动**（铁律 -1 样式保护）。

**三轨真验**：

- *curl + PG 轨（06:30 全绿，seed SST01 18443）*：①自由时段 000007 start=10:00（异于 apptTime 16:00）→200 落 id14 A01 10:00-11:00；②越班次 08:00 与 19:30（60min→20:30）→422；③非法格式 9:60→422「HH:mm」；④重叠 000008 同 10:00→409、10:30 半重叠→409、相邻 11:00→200 id15（边界正确）；⑤空串/纯空白/不传三态→均回落 apptTime 17:30 落 id16/17/18。PG 对账 dispatch_assignment id14-18 start/end/biz_date=2026-09-16 全一致；audit_log DISPATCH 链 id234-243 payload 均含 start/end、actor=E011、prev/cur 哈希连续；core 栈补验医生保护未破坏（无医生预约派 E001 无论是否带 start 均 422「未指定医生」，医生校验在先）。
- *Chrome 轨（Playwright，证据随 cc00948 提交说明留档）*：22 格放开可点、非预约时段 14:00 派单成功、释放后格子复位、治疗室/设备 tab 正常、FINANCE 角色守卫、console 0 error。
- *构建部署轨*：frontend dist 05:57（含「可自由选时」文案实证）+ txn-service 镜像 05:58:40 双栈换载；双栈 txn/frontend healthy。

**接力会话独立复验（07:09-07:13，非变更只读）**：seed 18443 E011 登录成功（JWT 6516 字符）；`GET /api/txn/dispatch/resources?date=2026-09-16&storeCode=SST01` →200，12 资源（DOCTOR/ROOM/DEVICE，workStart 09:00/workEnd 20:00）；`GET /api/txn/dispatch/jobs` →200，2 个 PENDING（AP20260915-000007 16:00、000008 17:30），与 PG `appointment` 两单「已预约」状态对账一致；dispatch_assignment id14-18 全 RELEASED append-only 留档（id14 即自由 10:00 实证）；audit_log id234-243 DISPATCH/RELEASE payload 含 start/end、actor=E011、哈希链连续；cc00948 完整 diff 逐行复核无误。

## 5. 接力与中断恢复留档（铁律 11）

前实例完成卡2 代码+构建+双栈部署后中断于三轨真验/提交前的窗口；06:11 接力会话法医鉴识（git show/diff、docker 现场、无活跃 mvn/compose 进程、哨兵心跳停滞 ≥15 分钟排除并发）后接管，完成 curl+PG 五分支、Playwright Chrome 轨、`cc00948` feat 提交+push；07:14 第二次接力（本会话）再次心跳判定+独立复验，确认 cc00948 实质全闭合（哨兵文字曾滞后标 🔄，本批已订正为 ✅），随后完成批末落账。全程无并发踩车、无验证跳过。

## 6. append-only 留档专章（不可删）

- seed 库 `dispatch_assignment` id14-18：全 RELEASED 保留行（id14=自由 10:00 派单、id15=相邻边界 11:00、id16/17/18=三态回落 17:30）。
- seed 库 `audit_log` id234-243：DISPATCH/RELEASE 完整哈希链，payload 含 start/end/resourceType/resourceName，actor=E011。
- seed marketing Flyway V11-14 + V32 迁移历史、双栈别名/DNS 隔离验证输出随 `0ea8983` 提交说明留档。

## 7. 如实说明

- 本批为**纯纵深/环境治理批**：卡2 是既有 ✅ 模块 M1 调度中心（B49 卡12）的纵深挂载，卡1 是联调环境加固，无新页面、无 ⬜→✅ 模块跃迁、无 🔧 成因消解；闭合的是 04-backlog L146/L147 登记行。
- 卡1 别名前缀是别名层纵深：侦察实证双网网络层本已隔离，本卡根治的是同名别名解析隐患并顺带修复 seed marketing 崩溃，不夸大为现网故障抢修。
- 自由时段开放后，班次表（L144，仍固定 09:00-20:00）与 URGENT 加急源（L141）两项无源诚实空态继续保留登记，本批不触碰。
- 接力会话对卡2 的写操作真验未重复执行（append-only 数据与 cc00948 提交说明证据链完整），独立复验严格限定登录+GET 只读端点+PG 只读查询，未产生任何新派单/释放写。

## 8. 进度落账

**完成度数字不变**：✅ **108**/166=约65%、🔧 **1**、⬜ **56**（约34%）；域⑧ 34✅2🔧31⬜ 不变。口径同 B44/B45/B50/B51 先例（纵深/还债闭合不动数字）——本批纯纵深+环境治理，新增完成模块 **0** 个，勾销 Backlog 登记行 **2** 行。

- **04-backlog**：勾销 L146（cc00948）、L147（0ea8983）两行（~~删除线~~+闭合详情+「✅ B52」列）；L141/L144/L133/L123 等保留登记不动。
- **02-modules**：L144 M1·调度中心行尾接 B52 卡2 自由时段纵深（卡1 环境治理在 04/03/本文档体现，不挂业务模块行）。
- **03-timeline**：表底追加 P5-B52 行。
- **00-history**：顶部新增 B52 简报，B51 条降级「上一批 B51」。
- **01-dashboard**：长备注段尾追加 B52 段（数字不变口径说明）。

## 9. 下一批

- **04 存量登记待拍板**：L141 URGENT 加急标记源（依赖预约/工单优先级字段产品拍板）、L144 医生/房间班次表（依赖排班数据源）；L133 impersonate 大项（JWT act/realSub claim+meiyun-security 全服务回归+前端换 token，与 L46 合并）、L123 报告哈希验真 UI（content_hash+规范化字节口径，DSAR/consent 远期）。
- 域⑦ M2 门店运营 / 域⑧ T2 数据分析等远期阶段页面群仍按整体规划待机；无用户新拍板前自主队列不再自行开工新批次。
