# DELIVERY-P5-B63 卡2（2026-09-19）· 通用异常中心三源只读归集（04-backlog L83）

> **批次性质**：第二层近线**财务收口纵深批第二卡**（P5-B63 四卡之卡2，非零代码批）。04-backlog L83 登记的「通用异常中心（跨域异常单据统一归集与处置工作台）」经卡0 只读侦察确认为**真实缺口**：异常能力六处分散（txn `bom_deduct_exception` 真实端点仅服 BOM 域、checkin/writeoffDesk/bom 三处异常登记各管一段、`writeoff_record` 表 ABNORMAL 三字段全 txn 零写入方、卡1 后财务侧已有 `fin_abnormal_bill` 真实单据源但无统一归集面）。本卡全部能力**纵深挂载既有 ✅ 模块与既有页面**：聚合层落 txn-service 扁平包（非 exceptioncenter 子包）、财务源经 finance 既有 service＋internal 只读端点供给、前端挂载既有 ExceptionView 工作台页（样式零改只换数据源）。**无新页面、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解、全程纯只读零写入，数字一律不变**。
>
> **决策依据（用户四阶段授权逐字）**：「1、先处理第二层近线，可以全部开始；2、然后处理core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径 / 状态机 / 选型决策 按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入C. 大阶段 / 专项开工」。本卡为第①阶段第二层近线第四批 B63 第二卡（B60/B61/B62/卡1 之后），「可以全部开始」覆盖开工无需再请示；统一读模型字段口径、三源状态/级别/处置路由映射、财务源系统身份拉取＋逐行 DataScope 二次收敛、软降级策略全部按「按推荐来」授权由卡0 侦察与卡2 施工定案，零返工。
>
> **卡0 裁断（2026-09-19 07:17 闭合，硬结论，卡序＝卡1 L84→卡2 L83→卡3 L85→卡4 L86，风险递增＋依赖）**：
> ① **异常能力六处分散无归集面**：txn `bom_deduct_exception` 有真实端点但只服 BOM 域；checkin/writeoffDesk/bom 三处异常登记各有真实端点互不相通；`writeoff_record` 表已有 ABNORMAL 状态＋abnormal_reason＋sign 字段但**全 txn 零写入方**（字段悬空，诚实空态不得造数）；卡1 已交付 `fin_abnormal_bill` 单据模型与状态机，卡2 可直接只读归集、处置仍走卡1 既有闭环不搬移。
> ② **可复用底座全部实证存在**：txn 扁平包可直接追加聚合控制器/服务；finance `FinAbnormalBillService.list` 即为财务源只读供给，双端 yml internal URL 与 X-Internal-Token 范式（卡1 StoreConsumableClient/FinanceAbnormalClient 双向 RestTemplate）现成；Go 网关对 `/api/<svc>/internal/**` 外网一律裸 404；前端 ExceptionView 工作台页与 exception store/api 槽位存在（原 mock 本地派生），样式零改只换数据源即可。
> ③ **卡序依赖已满足**：卡1 fin_abnormal_bill 单据模型与状态机即财务来源，卡1 先行闭合（`3d9d399`）后卡2 只读归集无阻塞；卡3 L85 首引 @EnableScheduling 中风险置后；卡4 L86 进项税抵扣全链新建最大置后。卡2 契约六条存哨兵 MACHINE:CARD 行。
>
> **提交清单（代码侧，一 feat commit，已 push origin/main）**：
> - 卡2 `d413186`（2026-09-19）`feat(P5-B63 卡2): 通用异常中心三源只读归集 L83`——**8 files +730/-194**（txn-service 4 新、finance-service 1 新、frontend 3 改）。
> - 卡2 过程心跳 docs commit `53cb13b`/`9445a24`/`4391772`/`24f0c0b`（非业务代码，不计入 03-timeline commit 列）。
>
> **铁律对齐**：-1 不臆造（三源表结构/端点/悬空字段经双库 psql 与全仓 grep 实证，writeoff ABNORMAL=0、fin_abnormal_bill=0 诚实空态逐条坐实，不造数）；0 开工先读哨兵＋六册；1 API 三处一致（三源 source 枚举 BOM_DEDUCT/WRITE_OFF/FIN_ABNORMAL、状态 PENDING/CLOSED、级别 HIGH/MEDIUM/LOW 在 txn 聚合契约、finance internal 视图、前端 exception.ts 类型三处对齐）；2 DataScope/权限不回退（本地两源 DataScope、FIN 系统身份拉全量后逐行 canReadStore 二次收敛、越权/不存在统一 404、E001 DOCTOR 无 exception:view 实测 403）；3 中文错误（403「无操作权限：需要 exception:view」、404「异常单据不存在或无权查看」「数据不存在或无权查看」全中文）；4 数字不虚增（纯纵深批数字一律不变）；5 前端真 API（ExceptionView/exception store 消灭全部本地 mock 派生，三源全换真源，样式零改）；6 双栈构建部署（mvn package finance/txn -am＋pnpm build＋双栈真名容器换载 healthy）；7 三轨真验（curl 五账号矩阵＋PG 只读基线＋Chrome 双隔离会话＋docker stop finance 软降级专项，§4 全绿）；8 一卡一 commit＋紧跟 push（d413186 已在 origin/main）；9/10 台账六册只追加不改写历史（02 旧注保留尾接、04 L83 整行勾销）；11 哨兵 ACTIVE/心跳/批末存档；安全：internal 端点 X-Internal-Token 网关外裸 404、直连无 token 401、视图剔除 idemKey/outboxId/disposeFundEntryId 内部列、财务源系统身份不拉 DataScope 但聚合层逐行收敛绝不越级、纯 GET 全链路无任何写操作。

## 1. 背景与记账口径

04-backlog L83 登记：「通用异常中心（跨域异常单据统一归集与处置工作台）｜异常能力六处分散无统一归集面｜平台/交易收口批」。卡1 DELIVERY §7 逐字预告卡2 施工口径：「建统一异常单据模型收口三类来源——BOM 异常（bom_deduct_exception）、核销异常（writeoff_record ABNORMAL 三字段接线写入方）、卡1 异常账务（fin_abnormal_bill 只读归集，处置仍走卡1 既有闭环不搬移）；统一列表/筛选/状态口径与处置跳转，前端挂载既有工作台页面（样式零改只换数据源）」。卡0 三路侦察（txn 异常端点＋writeoff 悬空字段／finance 卡1 单据源／前端工作台槽位）于 2026-09-19 07:17 零代码闭合；卡2 当日 13:29 代码单 commit `d413186` push 完成。

**记账口径（数字一律不变，纵深批，纯只读）**：本卡无新页面（ExceptionView 为既有工作台页）、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解；全链路仅 GET 端点，PG 零写入、零测试数据制造，故**无数据还原动作**。仪表盘维持 **✅109/🔧1/⬜55=166（约66%）**；域⑥ 财务维持 **11✅/0🔧/1⬜**。台账处置仅一项：04-backlog **L83 整行勾销**（删除线＋闭合长注＋末列 ✅ B63）；L85/L86 两行保持开放，分别为卡3/卡4 目标。三证勾稽（完成度＝状态计数＝时间线批次）一致：本卡不产生 ✅ 增量，只闭合一条既有登记。

## 2. 影响面只读核实（卡0/施工前侦察硬结论）

- **BOM 源有真实端点但只服一域**：txn `bom_deduct_exception` 表与真实 CRUD 端点存在，单号 BEX+yyyyMMdd-六位序号，字段含 store_code、fail_count、status（PENDING/RESOLVED）、异常原因与时间线；检查/核销/BOM 三处登记端点互不相通，无跨域列表。
- **核销异常字段悬空**：`writeoff_record` 表已有 ABNORMAL 状态＋abnormal_reason＋sign 三字段，但全 txn 代码库**零写入方**——即当前 15 行核销记录 ABNORMAL 计数为 0 是真实基线。卡2 只建归集读模型与来源映射，不补写入链路（写入方接线不在 L83 范围，且严禁为演示造数）。
- **财务源卡1 已就绪**：卡1 交付的 `fin_abnormal_bill`（V37 正式 schema，双库已迁移）与 `FinAbnormalBillService.list` 即财务异常单据源；卡2 经 finance internal 只读端点系统身份取数，处置仍跳卡1 /m6-abnormal 既有闭环，不搬移、不复制任何写能力。当前 core/seed 双库 fin_abnormal_bill 均为 0 行（卡1 验毕已全量还原，V37 纯 DDL 无种子），财务源在卡2 验证中呈诚实空态。
- **聚合挂载点现成**：txn-service 扁平包 com.meiyun.txn 可直接追加控制器/服务/读模型/跨服务客户端，无需新建子包；双端 yml internal URL、X-Internal-Token 校验范式、RestTemplate bean 均沿用卡1 既有底座。
- **前端槽位真实但全 mock**：ExceptionView.vue 工作台页与 stores/api exception 槽位存在，原数据全为本地 mock 派生（假 KPI、假列表、假详情）；本卡消灭 mock 接真源，scoped style 平移零新增类。
- **权限码**：统一归集门控使用 `exception:view`；财务 internal 供给端使用 `internal:abnormal-read` 系统身份码（X-Internal-Token），不面向登录用户。网关 Go 路由对 /api/finance/internal/** 与 /api/txn/internal/** 外网一律裸 404。

## 3. 施工（单卡三段，8 files +730/-194，commit `d413186`）

### 3.1 txn-service 聚合层（扁平包 4 新，共 504 行）

- **ExceptionCenterItem（28 行新）**：统一读模型 record，15 个组件（业务口径习称「14 字段」）：id、no、source、storeCode、storeName、type、level、title、description、status、assignee、occurredAt、closedAt、timeline、disposeRoute；内嵌 `Node(by, text, at)` 时间线记录。三源原始模型经各自 mapper 收敛为此扁平形状，前端只认这一个模型。
- **ExceptionCenterController（45 行新）**：两端点——`GET /api/txn/exceptions`（@RequirePerm("exception:view")，可选参 source/status/storeCode，分页与 KPI 汇总同口出）、`GET /api/txn/exceptions/{id}`（详情，按 id 前缀分发：`BEX` 切 4 位走 BOM、`WO` 切 3 位走核销、`AB` 切 4 位走财务）。
- **ExceptionCenterService（326 行新）**：三源并行归集＋统一口径。**状态映射**：BOM PENDING→PENDING、RESOLVED→CLOSED；WRITEOFF ABNORMAL→PENDING（无终态写入方，恒待处理）；FIN REJECTED/DISPOSED→CLOSED，其余→PENDING。**级别映射**：BOM failCount≥3 为 HIGH 否则 MEDIUM；WRITEOFF 恒 HIGH；FIN 金额绝对值≥¥20000 为 HIGH、≥¥5000 为 MEDIUM、否则 LOW。**type 三源恒 BUSINESS**。**处置路由**：BOM→/m2-inventory、WRITEOFF→/writeoff、FIN→/m6-abnormal（跳既有视图，零写操作）。**安全**：BOM/WRITEOFF 两本地源查询即带 DataScope；FIN 源经 internal 系统身份拉全量后**逐行 DataScope.canReadStore 二次收敛**；列表 storeCode 越权返回 404「数据不存在或无权查看」；详情越权/不存在/错前缀统一 404「异常单据不存在或无权查看」（不泄露单据是否存在）。
- **FinanceAbnormalBillClient（105 行新）**：RestTemplate 调 finance `/api/finance/internal/abnormal/bills`，带 X-Internal-Token；**软降级**：任何异常（连接拒绝/5xx/超时/反序列化失败）一律 log.warn 后返回空 List，绝不外抛——finance 宕机时异常中心列表仍 200，仅缺财务源。

### 3.2 finance-service 只读供给（1 新 62 行）

- **InternalAbnormalReadController（62 行新）**：`GET /api/finance/internal/abnormal/bills`，权限码 `internal:abnormal-read`＋X-Internal-Token 系统身份；直接委托既有 `FinAbnormalBillService.list`（**不做 DataScope**——系统身份跨店取数，收敛职责在 txn 聚合层逐行执行）；返回视图剔除 idemKey、outboxId、disposeFundEntryId 等内部列，仅暴露归集所需单据字段。纯 GET，零新增 service/Repository/写路径。

### 3.3 前端三件（样式零改，只换数据源）

- **api/exception.ts（55 行新）**：两端点薄封装＋ExceptionCenterItem/ExceptionNode/三源 source 枚举与筛选类型，走 api/client 的 /api base。
- **stores/exception.ts（164 行 +/- 混合）**：删除全部本地 mock 派生（假 KPI、假列表、假详情），换真源拉取＋三源 KPI 汇总、source/status/storeCode 筛选透传、详情按 id 拉取；保留既有 return 形状，ExceptionView 零结构改动。
- **ExceptionView.vue（139 行 +/- 混合）**：KPI 卡、来源筛选、状态筛选、列表卡片、详情抽屉全部绑真源字段（14 字段全展示含内嵌时间线）；「前往处置」按钮按 disposeRoute 跳三视图（BOM→/m2-inventory、WRITEOFF→/writeoff、FIN→/m6-abnormal）；空源诚实空态（按钮 disabled、文案「暂无异常单据」）；scoped style 平移零新增类。
- pnpm build（vue-tsc --noEmit && vite build）EXIT=0。

## 4. 三轨真验（全绿，逐轨留证）

### 4.1 构建部署

mvn package finance/txn -am BUILD SUCCESS；pnpm build EXIT=0；scripts/deploy.sh 双栈换载（meiyun-finance/txn-service、meiyun-seed-finance/txn-service 四容器），换载后四容器 healthy。验证途中发现并修复 detail 真 bug（见 §5.5），txn fat jar 终版 **47,840,326 字节**，13:17 换载，新指纹 **146deb44e9b4**。

### 4.2 curl 轨（正式栈 8443，五账号权限/隔离矩阵）

登录人：E005 李店长（上海旗舰店 STORE_MGR）、E009 吴店长（北京 ST-BJ-001 STORE_MGR）、E011 冯区域（华东 REGION_MGR）、SE101（超管全量）、E001（DOCTOR 无 exception:view 码）。

- **数据可见性矩阵**：E005 列表见 BOM 2 单（BEX20260911-000001、BEX20260911-000002，均上海旗舰店）；E009 北京店列表为 []；SE101 全量；E011 华东区只见上海两单（DataScope 区域边界正确）。
- **登录根因如实查清**：首批用大写工号（E005/E009）登录取 token 曾出现可见性异常，逐路排查后确认登录名大小写须与库中工号一致，换小写工号重登后矩阵全部符合预期——非代码缺陷，已在哨兵与本 DELIVERY 如实钉死。
- **筛选器**：source=BOM_DEDUCT→2 单、source=WRITE_OFF→[]（诚实空态）；status=PENDING→2、status=DONE→[]（BOM 两单均 PENDING，RESOLVED=0）。
- **详情前缀分发**：三前缀详情 200（BEX/WO/AB 路由分发正确，14 字段齐）；错前缀、跨店越权详情统一 **404「异常单据不存在或无权查看」**。
- **权限闸**：E001 DOCTOR 无码访问列表→**403「无操作权限：需要 exception:view」**；无/错 token→401。
- **internal 安全**：网关外访问 /api/finance/internal/abnormal/bills→裸 **404**；直连服务无 token→401；带 X-Internal-Token 系统身份→200。

### 4.3 PG 轨（双库只读基线核实，零写入）

- meiyun_core：`bom_deduct_exception`=2 行（PENDING 2/RESOLVED 0），与列表/KPI 完全一致；`writeoff_record`=15 行其中 ABNORMAL=0（悬空字段零触发，WRITE_OFF 源诚实空态）；`fin_abnormal_bill` core=0（卡1 还原基线，V37 无种子）。
- meiyun_seed：fin_abnormal_bill=0（seed 栈纯结构迁移，零业务数据）。
- 全卡验证仅执行 SELECT/COUNT，无任何 INSERT/UPDATE/DDL，**零污染、无需还原**。

### 4.4 Chrome 轨（8080 正式栈，双隔离会话）

- **会话 b63excE005（李店长/上海）**：工作台 KPI 2/0/0/0（待处理 2、核销 0、财务 0、总数 2）；列表 2 张 BOM 卡，文案「耗材扣料 中 待处理 09-12 01:38」（级别 MEDIUM＝failCount<3 映射正确）；详情抽屉 14 字段齐全，含上海旗舰店门店名与系统时间线（Node by/text-at 渲染）；「前往处置」跳 /m2-inventory 正确；切到 WRITE_OFF/FIN_ABNORMAL 源显诚实空态、按钮 disabled。
- **会话 b63excE009（吴店长/北京）**：KPI 全 0、列表「暂无异常单据」，与 curl [] 一致（跨店隔离 UI 实证）。
- 双会话 console error/warn 均 **0 条**。

### 4.5 软降级专项（docker stop finance）

`docker stop meiyun-finance` 后：E005 重新请求列表→**仍 200**，仅含 BOM 两单（FIN 源缺席由空 List 顶替，FinanceAbnormalBillClient log.warn 不外抛）；FIN 前缀详情→安全 **404**（不泄露、不 500）。`docker start` 复启后四探活 healthy，财务源自动恢复。

### 4.6 数据零污染终检

验毕双库全表 COUNT 与卡前基线逐一对齐：bom_deduct_exception 仍 2、writeoff_record 仍 15（ABNORMAL 仍 0）、fin_abnormal_bill core/seed 仍 0；无审计新增（纯 GET 链不写 audit_log）。本卡无备份/还原脚本（无写入即无还原对象），这是纯只读归集卡的诚实形态。

## 5. 如实说明

### 5.1 财务源零行是诚实基线非缺陷

V37 为纯 DDL 迁移无种子，卡1 验毕又已全量物理还原测试账单，故卡2 验证时 fin_abnormal_bill core/seed 均为 0。FIN_ABNORMAL 源在三轨中呈诚实空态（列表 0、筛选 0、UI 空态），通过 internal 端点 200＋卡1 既有单测/交付链路证明供给健康，**不为演示凑数造账单**。

### 5.2 核销 ABNORMAL 悬空字段不补写入方

writeoff_record 的 ABNORMAL/abnormal_reason/sign 三字段全 txn 零写入方是侦察实证的既有现状。卡2 只收口「归集与展示」语义（映射恒 HIGH、PENDING、/writeoff 路由），**不擅自新建核销异常写入链路**（超出 L83 契约，且涉及核销业务状态机决策）；该接线留存后续 backlog 决策。

### 5.3 大写工号登录根因

验证早期 E005/E009 大写工号取得的 token 可见性异常，经逐路排查确认为登录名大小写须与库中工号严格一致，换小写重登后五账号矩阵全部正确。非产品缺陷，过程心跳已如实记录，最终证据全部以小写工号会话为准。

### 5.4 处置只跳转不搬移

三源「前往处置」均跳既有业务视图（/m2-inventory、/writeoff、/m6-abnormal），异常中心自身零写端点；财务异常处置严格保留卡1 登记→审批→ADJUST 动账闭环，卡2 不复制任何处置/动账能力。

### 5.5 附修真实缺陷：detail 不可变 List 500

联调中发现 `detail()` 初版以 `List.of(item)` 构造单元素 holder，而 enrich 流程对聚合结果执行 `items.set(...)` 就地补门店名，不可变 List 触发 UnsupportedOperationException 致详情 500。已改为 `List<ExceptionCenterItem> holder = new ArrayList<>(1); holder.add(item); enrich(holder); return holder.get(0);`，复测三前缀详情全 200。修复计入 d413186，终版 txn jar 47,840,326 字节、指纹 146deb44e9b4。

### 5.6 无测试数据还原

本卡全程纯 GET/SELECT，不产生任何业务或审计写入，故无 §5.4（卡1）式单事务物理删除与备份动作；token 临时文件（/tmp/tok_*.txt）为登录凭证留痕，非业务数据。

## 6. 进度落账

批末六册＋本 DELIVERY＋哨兵同一 docs 原子提交（docs/ 被 gitignore：已跟踪 `git add -u docs/`，本新文件 `git add -f docs/DELIVERY-P5-B63-卡2-2026-09-19.md`）：

- **04-backlog L83**：整行加删除线，闭合长注（三源只读归集/聚合两端点/14 字段/finance internal 系统身份＋逐行 DataScope/软降级/状态级别路由映射/前端三件样式零改/detail ArrayList 修复/三轨五账号矩阵/PG 2-15-0 纯只读零还原/Chrome 双会话），末列改「✅ B63」；L84（卡1 已勾销）、L85/L86 两行零改动保持开放。
- **02-modules L100（域⑥「税 / 卡余额 / 日结 / 异常」行）**：状态保持 ✅、批次列不动，尾句删去「通用异常中心仍列 Backlog（卡2 L83）」，卡1 句号收口后尾接 P5-B63 卡2 闭合长注（d413186/8 files/聚合层/internal/软降级/安全/零写跳转/detail bug 修复/三轨/纯只读/L83 收口/数字不变）。不新增行、不翻状态。
- **00-history**：顶部「最近更新」换卡2 巨型行（d413186 战果＋L83 勾销＋数字不变）；原卡1 行首加「> 上一批 B63 卡1：」前缀下沉（内容全保留）。
- **01-dashboard**：卡1 bullet 前倒序插入卡2 bullet；数字格 ✅109/🔧1/⬜55=166 与域⑥ 11/0/1 一字不动。
- **03-timeline**：卡1 行（L94，尾句「转卡2」原文保留不改写历史）后正序追加 P5-B63 卡2 六列行（L95；commit 列仅 d413186；docs 心跳 53cb13b/9445a24/4391772/24f0c0b 注明不计；交付文档 DELIVERY-P5-B63-卡2-2026-09-19；末列 L83 勾销＋数字不变＋卡序卡1✅→卡2✅→卡3 L85→卡4 L86＋哨兵转卡3）。
- **索引 DEVELOPMENT-ROADMAP.md**：通读确认纯索引无随动。
- **HANDOFF-AUTO.md**：STATUS 保持 ACTIVE（队列未全闭合）；HEARTBEAT 取批末实时北京时间；CARD 由卡2 L83 改写为卡3 ⬜ L85 预收合规监控（卡2 标 ✅＋d413186）；PREV 加卡2 锚；人读区顶部换卡2 闭合 bullet；在「## P5-B63 卡1 闭合存档（2026-09-19）」前插入「## P5-B63 卡2 闭合存档（2026-09-19）」八要素块。

## 7. 下一批

**卡3 = 04-backlog L85 预收合规监控（prepay 账龄/超额/长期未核销预警）**，卡0 已完成只读侦察、契约要点存档哨兵 MACHINE:CARD：

- **建设目标**：finance-service 首引 `@EnableScheduling` 账龄扫描 Job＋预警规则实体（阈值/级别/启用开关），扫描 prepay 预收池按账龄与余额派生合规预警；复用 txn 既有 internal 告警通路 `/api/txn/internal/compliance-alert` 落合规告警单。
- **分级与派单**：WARN→STORE_MGR、CRITICAL→REGION_MGR＋SUPER_ADMIN；幂等键前缀 `COMPLIANCE:{bizRef}:{staffId}:{level}` 防重复告警；复用 txn NotificationFanoutJob 60s 扇出既有通知管道。
- **前端接真**：闲置端点 `GET /finance/prepay-pool` 与既有 FinPrepayView 页当前三类本地派生告警换真源（样式零改）。
- **中风险点**：首引定时调度（卡0 卡序据此置后于卡1/卡2），须验证双栈调度互斥（core/seed 不重复扫不重复告警）与 Job 开关默认态；施工前精读 @EnableScheduling 既有引用面与双栈 profile。
- 卡4 L86 进项税抵扣（进项发票实体/认证/抵扣/申报全链新建，V37 后续版迁移）契约随卡4 开工细化，为四卡中最大全链新建、队列末卡。

按「卡0 硬结论直接取用→一卡一 commit+push→前端样式零改只换数据源→curl+PG+Chrome 三轨真验→测试数据单事务还原（若产生写入）→批末六册落账」继续施工。队列顺序：卡1 L84✅→卡2 L83✅→**卡3 L85（下一张）**→卡4 L86，四卡全闭合后 B63 批置 DONE 并转 B64。
