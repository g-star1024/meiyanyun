# DELIVERY-P5-B51 · M1 调度 Backlog 纵深缺口批（2026-09-15）

> **批次性质**：B49 卡12 登记的调度六缺口（04-backlog L140-145）纵深闭合批——用户拍板顺序②（①平台治理批 B50 已闭合 → ②本批）。
> **施工顺序（用户拍板①「按建议序」）**：卡3 L143 DONE 联动 → 卡4 L140 DEVICE 接真 → 卡5 L145 改期联动 → 卡6 L142 sku_code；前置卡1 六缺口只读侦察 + 卡2 汇报拍板（零代码）。
> **四项拍板（卡2 AskUserQuestion，2026-09-15 21:00）**：①按建议序开工；②同时放开 DEVICE 派单写；③appointment 加 sku_code 列；④L141 URGENT / L144 班次表都保留诚实空态。
> **提交清单**：feat `79a8ae4`（卡3）+ fix `2714287`（卡3 真验顺手修）、feat `86d4a65`（卡4）、feat `50a7eb7`（卡5）、feat `777d456`（卡6）；docs `65a343a`（开工哨兵）/`b0e8aeb`（卡1 勾销）/`2fa8d5e`（卡3 勾销）/`ce07b0f`（卡4 勾销）/`53946fe`（卡5 勾销）/`23e8d00`（卡6 勾销）+ 批末本批 docs 原子提交。卡2 零代码无 commit。
> **铁律对齐**：0 先查再写（卡1 全只读侦察）、7 三轨真验（每卡 API+PG+前端/审计三轨）、7.5 同类全站扫、8 一卡一 commit 紧跟 push、9/10 台账分册原子提交（本文档+五分册同批）、11 哨兵 HANDOFF-AUTO 全程心跳。

## 1. 背景与目标

B49 卡12 调度中心整页切真时在 04-backlog L140-145 登记六项纵深缺口（均标「远期 M1」）。本批逐项闭合其中四项真实有源缺口，两项经侦察确认无源缺口按拍板④保留诚实空态不伪造：

| 缺口行 | 内容 | 卡序 | 结果 |
|---|---|---|---|
| L140 | DEVICE 设备档案台账域待建（resources 三源之一诚实空态） | 卡4 | ✅ 闭合（复用既有 equipment 域接真+放开 DEVICE 派单写） |
| L141 | URGENT 加急标记源 | — | ⏸ 保留诚实空态（拍板④，appointment 无 priority/urgent 列） |
| L142 | durationMin 真实时长（固定 end=start+60min） | 卡6 | ✅ 闭合（appointment.sku_code 列+SKU duration_min 真源） |
| L143 | assignment DONE 完成态（仅 SCHEDULED/IN_PROGRESS/RELEASED） | 卡3 | ✅ 闭合（treatDone AFTER_COMMIT 联动） |
| L144 | 医生/房间班次表（固定 09:00-20:00） | — | ⏸ 保留诚实空态（拍板④，无 shift/duty/roster 数据源） |
| L145 | 派单时段自由度（start 锚定 apptTime，不支持自由时段/改期） | 卡5 | ✅ 改期联动半项闭合；自由时段派单半项系产品决策转登记 |

## 2. 卡1 六缺口数据源只读侦察（20:25 闭合，零代码，docs `b0e8aeb`）

铁律 -1-A 链路先行、铁律 0 先查再写，六缺口逐一实证数据源就绪度：

- **L140 DEVICE 数据源就绪可施工**：B13 equipment 域完整（equipment 表 store_code+asset_no 唯一、category 六类、status 四态 NORMAL/CALIBRATING/REPAIRING/DISABLED；公开端点 /api/stores/equipments 挂 equipment:view/edit+DataScope）；seed SST01 实证 8 设备（5N/1C/1R/1D）+11 维保、prod 0/0；缺 InternalEquipmentController（复刻 InternalRoomController 范式）+txn 侧 StoreEquipmentClient（复刻 StoreRoomClient 软降级）+DispatchService DEVICE 分支。
- **L141 URGENT 无源**：appointment 无 priority/urgent 列（CHECK 仅 source/status）→ 继续诚实空态或拍板加列。
- **L142 durationMin 半就绪需拍板**：product_sku.duration_min integer not null 真实存在（90/70/60/45/40/30/15 分钟档），但 appointment.project 12 个中文项目名 vs SKU 品牌商品名字面匹配≈0%、无关联键；选项 (a) appointment 加 sku_code 列 (b) project→SKU 人工映射表 (c) 保持固定 60min；seed appointment 共 166 行存量。
- **L143 DONE 数据源完全就绪**：联动链 dispatch_assignment.appt_no→arrival.appt_no→consult_plan.arrival_id→ConsultPlanService.treatDone（TREATING→DONE+EM 治疗记录+revision TREAT_DONE+audit+AFTER_COMMIT FollowupScheduler 先例）全在 txn-service 同服务同库，事务内直接联动零跨服务。
- **L144 班次表无源**：库内仅 ai_scheduling_plan/ai_scheduling_slot（B47 卡6 证实 AI 草稿非现排），无 shift/duty/roster 表 → 继续固定 09:00-20:00 诚实空态。
- **L145 改期半就绪**：AppointmentController.reschedule（仅已预约态+HH:mm 校验+audit RESCHEDULE）真实存在，但不联动 dispatch_assignment——改期后 SCHEDULED 派单滞留旧时段；可施工小项=reschedule 同事务联动跟随（同服务同库）；自由时段派单为产品决策需拍板。

## 3. 卡2 侦察汇报与四项拍板（零代码无 commit）

侦察结论汇报用户，AskUserQuestion 四项拍板（21:00）：①施工顺序=按建议序（DONE→DEVICE→改期→sku_code）；②L140 范围=同时放开 DEVICE 派单写；③L142 方向=appointment 加 sku_code 列（schema 变更+预约创建选 SKU+durationMin 从 SKU duration_min 真源取，替代固定 60min）；④无源缺口=L141/L144 都保留诚实空态。拍板全文补记哨兵（docs `2fa8d5e`）。

## 4. 卡3 L143 assignment DONE 完成态联动（feat `79a8ae4` + fix `2714287`，9 文件 +150/-25）

**缘起**：治疗完成（treatDone）后派单单据滞留 IN_PROGRESS/SCHEDULED，资源时段被幽灵占用、jobs 不重现、无法体现「已做完」。

**改动**：txn-service 新建 DispatchCompletion——ConsultPlanService.treatDone 事务提交后（AFTER_COMMIT，REQUIRES_NEW 新事务照 FollowupScheduler 先例）把该预约活跃派单置 DONE+done_at；DONE 为终态：resources 回显不占时段、release/再派 422、jobs 不重现；前端时间轴 DONE 绿块+图例、isSlotBusy 排除 DONE。**顺手修既有 bug**（fix `2714287`）：预约号当日序号统计排除 DEMO 等非数字后缀，避免 cast bigint 22P02 致创建预约 500（真验中发现）。

**三轨真验 8 项全绿**：PG done_at 列置值 / REQUIRES_NEW 日志「置DONE 1条」/ resources DONE 回显 / jobs 不重现 / release 422 / 再派 422 / DONE 不占时段（id=7 同医生同时段派单成功）/ audit DISPATCH id=6 DONE actor=SE004。

## 5. 卡4 L140 DEVICE 设备档案接真+放开 DEVICE 派单写（feat `86d4a65`，8 文件 +179/-21）

**缘起**：调度 resources 三源之一 DEVICE 长期诚实空态（「设备档案为远期能力」），而侦察实证 equipment 域早已存在，缺口只是未接线。

**改动**：store-service 新建 InternalEquipmentController（/api/stores/internal/equipments，internal:catalog-read，复刻 InternalRoomController 范式）；EquipmentService.listDispatchBriefs 轻量六键清单（规避 toView 逐台查维保记录 N+1）。txn-service 新建 StoreEquipmentClient（软降级空列表，复刻 StoreRoomClient）；DispatchService resources() DEVICE 分支（仅 NORMAL 态）+ dispatch() 放开 DEVICE 写（拍板②：资源类型白名单/三分支校验/404 中文文案）；DispatchAssignment.RES_DEVICE 常量，resourceId=资产编号 assetNo（店内唯一 uk_eq_store_asset，length 32 匹配）；409 重叠检测按 resourceType+resourceId 天然复用，DEVICE 零额外改动获得时段占用与冲突校验。前端 adaptAssignment 显式三态（防 DEVICE 错映射 DOCTOR）；空态统一三态文案，「设备档案为远期能力」空态退役。

**三轨真验全绿**：读侧 resources?type=DEVICE 返回 5 台 NORMAL（校准/停用/维修 3 台过滤）；写侧派 EQ-L001 成功 id=8 SCHEDULED、同设备同时段 409「皮秒激光治疗仪 在 14:00-15:00 已有排单」、派校准中 EQ-R002 404「所选设备不存在或不可用（仅正常状态设备可派单）」；resources 占用块回显 14:00-15:00 唐玉兰；PG dispatch_assignment DEVICE 行+audit_log DISPATCH payload resourceType=DEVICE 双落库。

## 6. 卡5 L145 预约改期同事务联动 SCHEDULED 派单跟随（feat `50a7eb7`，2 文件 +56/-3）

**缘起**：reschedule 端点真实存在但不联动派单——改期后 SCHEDULED 派单滞留旧时段，资源占用失真（卡1 侦察坐实）。

**改动**：DispatchService.followReschedule——预约改期时同事务把其 SCHEDULED 派单移动到新日期时段（派单 start 锚定 apptTime 语义延伸，跨日 bizDate 一并跟随）；新时段越出固定班次窗 09:00-20:00 → 422「请先释放派单再改期」；与同资源活跃占用重叠（排除自身）→ 409 冲突文案；任一校验失败整体回滚，预约与派单不产生中间态；每条跟随派单记 audit RESCHEDULE_FOLLOW（调度域动作调度域审计，payload 含新时段完整快照）。AppointmentController.reschedule 注入 DispatchService+@Transactional，保存预约前先跟随校验。Repository 零改动（findByApptNoAndStatusIn 复用卡3 遗产）；已到店预约被 getActive 天然拦截，IN_PROGRESS 不进跟随；无派单预约改期空列表早退零影响。

**三轨真验全绿**：改期 000007→16:00 派单 id=8 跟随 16:00-17:00 resources 回显 / 改期 16:30 与既有排单重叠 409「皮秒激光治疗仪 在新时段 16:30-17:30 与已有排单 16:00-17:00 冲突」且预约回滚仍 14:30 / 改期 19:30 越班次 422 / 改期 17:30 成功跟随 EQ-L001 双块回显 / 无派单预约改期回归 200 / PG 两行派单 bizDate+start/end 跟随正确 + audit 双轨 RESCHEDULE×3+RESCHEDULE_FOLLOW×2（拒绝两次无 FOLLOW 记录）。

**残留半项如实标注**：L145 的「自由时段派单（start 不锚定 apptTime）」经卡1 侦察认定为产品决策（涉及派单与预约解耦的产品形态），本批不施工，转 04-backlog 登记待拍板。

## 7. 卡6 L142 appointment 加 sku_code 列+派单时长真源化（feat `777d456`，7 文件 +130/-18）

**缘起**：派单时长固定 end=start+60min 与项目真实时长脱节（热玛吉 90min 也按 60min 排）；侦察实证 product_sku.duration_min 真源存在但预约与 SKU 无关联键。拍板③选定 appointment 加 sku_code 列方案。

**改动**：Appointment 加 sku_code varchar(40)（ddl-auto 自动建列，166 行存量 NULL 不受影响）；StoreProjectClient 新增 activeSkuDurationMap 第二缓存（60s TTL，失败沿用旧缓存软降级）；AppointmentController.create 带 SKU 校验（`skuCode != null && !activeSkuDurationMap().containsKey(skuCode)` → 400「SKU 不存在或已停用: 」），audit CREATE payload 含 sku；DispatchService.resolveDurationMin——绑 SKU 取 duration_min 真源，未绑/查无/为 0 回落 60min（三消费点：jobs 预览/dispatch 派单/followReschedule 改期跟随时长一致）；前端 appointment.ts 两 interface 加 skuCode、AppointmentCreateView SKU 下拉（403 无 catalog 权限降级手输）+watch 回填。

**三轨真验 7 项全绿**：①绑 SKU（ZH-THERMAGE-FL，duration_min=90）派单 E003 → 14:00→15:30（API+PG 双轨）；②未绑 SKU 派单 E001 → 14:00→15:00（回落 60 回归）；③伪 SKU FAKE-999 → HTTP 400「SKU 不存在或已停用」；④PG appointment.sku_code 有/空对照落列；⑤audit CREATE payload 含 `"sku":"ZH-THERMAGE-FL"`/`""`；⑥jobs durationMin: 90；⑦schema sku_code varchar(40) ddl-auto 自动建列。

## 8. L141/L144 诚实空态保留（拍板④）

- **L141 URGENT 加急标记源**：appointment 无 priority/urgent 列，jobs 加急标记继续无源；前端 B49 卡12 已删 stats.urgent 死代码，无伪造展示。04-backlog 行保留不勾销，追加拍板④保留登记。
- **L144 医生/房间班次表**：库内无 shift/duty/roster 数据源（ai_scheduling_* 为 AI 草稿非现排），资源可用时间继续固定 09:00-20:00；卡5 改期 422 班次校验引用同一固定窗，班次表落地后改真源。04-backlog 行保留不勾销，追加拍板④保留登记。

## 9. append-only 留档专章

真验过程产生的业务/审计数据全部 append-only 保留、不清理（可复查）：

- **卡3（seed 栈）**：dispatch_assignment id=6 DONE（done_at 置值，actor=SE004）、id=7（同医生同时段派单成功，DONE 不占时段实证）。
- **卡4（seed 栈）**：dispatch_assignment id=8（EQ-L001 皮秒激光治疗仪 DEVICE 派单，SCHEDULED）。
- **卡5（seed 栈）**：预约 000007 改期链路（14:30→16:00 等多次）；audit RESCHEDULE×3+RESCHEDULE_FOLLOW×2（两次拒绝改期无 FOLLOW 记录，回滚无副作用实证）。
- **卡6（prod 栈）**：预约 AP20260915-000001~000005（含绑/未绑 SKU 对照）；dispatch_assignment id=1（E003 14:00-15:30，SKU 90min 真源）/id=2（E001 14:00-15:00，回落 60min）SCHEDULED；audit CREATE×4（payload sku 有/空对照）。

## 10. seed 栈同网络别名 DNS 轮询隐患（环境层登记，上报用户拍板）

卡6 真验期间破获「name-map 空 Map 之谜」：正式栈与 seed 栈容器同处一个 Docker 网络且后端服务名相同 → Docker DNS 轮询把 txn 服务间调用随机打到 seed 栈（seed 库客户号 SC 前缀，查 M002 返回 200 空 Map，表现为间歇性空数据而非报错，极难排查）。实证 seed 栈 10 容器长期在跑（meiyun-seed-customer-service Up 26h 等）。**此为环境层历史遗留，非本批代码缺陷**；已登记 04-backlog，建议二选一：①seed 栈迁移独立 Docker 网络；②seed 栈容器服务别名加 seed- 前缀。**待用户拍板后施工**。

## 11. 如实说明

- 本批四卡均为**既有 ✅ 模块纵深**（M1 调度中心 B49 卡12 已 ✅、新建预约 M4 已 ✅），无新页面、无模块状态跃迁、无 🔧 成因消解；闭合的 L140-145 是 04-backlog 登记行而非仪表盘 ⬜ 独立行。
- L145 仅「改期联动」半项真实闭合，「自由时段派单」半项系产品决策未施工、转登记，不夸大整行闭合。
- L141/L144 按拍板④保留诚实空态，未伪造数据源。
- 卡3 顺手修的预约号 22P02 bug（fix `2714287`）系真验中发现的既有缺陷，非本卡引入，单独 fix commit 如实拆分。
- name-map 空 Map 现象系 seed 栈 DNS 轮询环境隐患（§10），非本批代码缺陷；真验最终结论以隔离后双轨复核为准，全部 7 项全绿。

## 12. 进度落账

**完成度数字不变**：✅ **108**/166=约65%、🔧 **1**、⬜ **56**（约34%）；域⑧ 34✅2🔧31⬜ 不变。口径同 B44/B45（纵深闭合不加页不动数字）/B50（纯还债不动数字）先例——本批无新页面、无 ⬜→✅ 跃迁，四卡均为既有 ✅ 行的纵深增强，沿用历史计数差口径。

- **04-backlog**：勾销 L140/L142/L143/L145 四行（~~删除线~~+闭合详情+「✅ B51」列，L145 注明改期半项闭合+自由时段转登记）；L141/L144 行保留不勾销，追加拍板④保留登记；新增登记两行（seed 栈 DNS 轮询隐患、派单自由时段产品决策）。
- **02-modules**：L144 M1·调度中心行尾接 B51 四卡纵深挂载；L29 新建预约行挂卡6 sku_code 纵深。
- **03-timeline**：表底追加 P5-B51 行。
- **00-history**：顶部新增 B51 简报，B50 行降级「上一批 B50」。
- **01-dashboard**：L18 口径流水尾接 B51 段（数字不变口径说明）；L36 域⑧行尾接 B51 段。

## 13. 下一批

- **04 存量登记**：L141 URGENT 加急标记源（待 appointment 优先级字段产品拍板）；L144 医生/房间班次表（待排班数据源）；新增两项——seed 栈 DNS 轮询隐患治理（§10，待拍板）、派单自由时段（start 不锚定 apptTime，产品决策待拍板）。
- 域⑦ M2 门店运营/域⑧ T2 数据分析等远期阶段页面群仍按整体规划待机。
