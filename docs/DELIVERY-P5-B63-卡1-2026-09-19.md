# DELIVERY-P5-B63 卡1（2026-09-19）· 异常账务处置登记全链（04-backlog L84）

> **批次性质**：第二层近线**财务收口纵深批首卡**（P5-B63 四卡之卡1，非零代码批）。04-backlog L84 登记的「异常账务处置登记（长短款/错账登记与审批闭环）」经卡0 只读侦察确认为**真实缺口**（finance 后端零实体零端点、前端 342 行假处置）。本卡全部能力**纵深挂载既有 ✅ 模块**：复用既有财务页 FinAbnormalView（/m6-abnormal，342→417 行接真）、txn 既有审批中心（新增 FIN_ADJUSTMENT bizType）、FundEntryService 既有 ADJUST 白名单动账范式、已播种但零引用的 finance:abnormal:view/dispose 悬空权限码。**无新页面、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解，数字一律不变**。
>
> **决策依据（用户四阶段授权逐字）**：「1、先处理第二层近线，可以全部开始；2、然后处理core 库 24 行营销演示残留仿 B57 备份+物理删除；3、B. 产品口径 / 状态机 / 选型决策 按照你推荐的来就可以，做好竞品调研选择最合适的就行；以上全部完成后转入C. 大阶段 / 专项开工」。本卡为第①阶段第二层近线第四批 B63 首卡（B60/B61/B62 之后），「可以全部开始」覆盖开工无需再请示；异常单据模型（三型登记/WRONG 选向）、审批分级（沿用既有 ¥5000/¥20000 金额档）、动账口径（ADJUST/RF-REVENUE/长短款方向映射）、状态机（PENDING_APPROVAL→APPROVED/REJECTED→DISPOSED）全部按「按推荐来」授权由卡0 侦察与卡1 施工定案，零返工。
>
> **卡0 裁断（2026-09-19 07:17 闭合，硬结论，定卡序＝卡1 L84→卡2 L83→卡3 L85→卡4 L86，风险递增＋依赖）**：
> ① **L84 后端零实体零端点**：finance 扁平包无任何异常账务实体/Repository/Service/Controller；下版 Flyway 为 **V37**（V33 财务列之后 customer V36 已占号）；FinAbnormalView 342 行 dispose() 纯本地 state、回单金额恒 null，finReports.ts 以假 seedOutbox 派生长短款 KPI。
> ② **可复用底座全部实证存在**：悬空权限码 finance:abnormal:view/dispose 已在 PermissionMatrix 播种且授 FINANCE/REGION_MGR/STORE_MGR、全后端零引用；txn approval_todo「审批单即业务单」范式（REQUISITION/LOSS_REPORT：bizNo=todoNo、payload JSON、终审 deductOnFinalApprove 跨服务回调），biz_type varchar(16) 无 CHECK、四类型为硬分支可扩 FIN_ADJUSTMENT；FundEntryService ADJUST 白名单＋idemKey 幂等（L323 人工 MATERIAL/LOSS 必拒＝损失必挂审批的天然红线）；adjustOutbox ADJUST 范式 L511、V4 建表模板、StoreConsumableClient 双向 RestTemplate 客户端、双端 yml internal URL、TxnApplication RestTemplate bean 均现成。
> ③ **卡序依赖**：卡2 L83 通用异常中心须收口卡1 异常账务单据，故卡1 先行；卡3 L85 首引 @EnableScheduling 中风险置后；卡4 L86 进项税抵扣全链新建最大置后。卡1 契约草案六条存哨兵 MACHINE:CARD 行。
>
> **提交清单（代码侧，一 feat commit，已 push origin/main）**：
> - 卡1 `3d9d399`（2026-09-19）`feat(finance): B63 卡1 L84 异常账务处置登记全链——长短款/错账登记＋txn 三级审批闭环＋终审 ADJUST 动账，前端接真`——**15 files +1294/-249**（finance-service 5 新 2 改、txn-service 1 新 2 改、frontend 5 改）。
> - 卡0 只读侦察 docs commit `76c5dcf`（非业务代码，不计入 03-timeline commit 列）。
>
> **铁律对齐**：-1 不臆造（卡0 硬结论经双库 psql/全仓 grep 实证，零实体/悬空权限码/审批可扩展性逐条坐实）；0 开工先读哨兵＋六册；1 API 三处一致（FIN_ADJUSTMENT 在 txn 枚举、finance 回调契约、前端 approval.ts 三处枚举＋ApprovalView 本地副本同步补齐）；2 DataScope/权限不回退（登记门店 DataScope、越权阶段 403、五权限码等价门控为真验重点）；3 中文错误（400/403/422 全中文文案，驳回空原因 400、已被驳回处置 400）；4 数字不虚增（纵深批数字一律不变）；5 前端真 API（消灭 seedOutbox 假长短款与 dispose 本地态，五文件全换真源）；6 双栈构建部署（mvn package finance/txn -am＋pnpm build＋双栈真名容器换载 healthy）；7 三轨真验（curl 金额分级矩阵＋PG 落库＋Chrome 三角色 UI，§4 全绿）；8 一卡一 commit＋紧跟 push（3d9d399 已在 origin/main）；9/10 台账六册只追加不改写历史（02 旧注保留尾接、04 L84 整行勾销）；11 哨兵 ACTIVE/心跳/批末存档；安全：internal 端点 X-Internal-Token 网关外裸 404、终审事务内同步回调失败硬回滚不产生孤儿账单。

## 1. 背景与记账口径

04-backlog L84 登记：「异常账务处置登记（长短款/错账登记与审批闭环）｜财务台账真实但缺异常账务专门登记审批流｜平台/交易收口批」。B62 §7 逐字预告 B63 施工口径：「L84 异常账务处置登记（长短款/错账登记与审批闭环）：财务台账真实但缺异常账务专门登记审批流——盘点台账与审批四类型复用面」。卡0 三路侦察（finance 后端／txn 审批中心＋权限／前端 17 财务视图）于 2026-09-19 07:17 零代码闭合（docs commit `76c5dcf`），07:49 完成 15a 精读锚点进施工，当日 09:51 代码单 commit `3d9d399` push 完成。

**记账口径（数字一律不变，纵深批）**：本卡无新页面（FinAbnormalView 为 B24 既有 ✅ 页）、无 ⬜→✅ 模块状态跃迁、无 🔧 成因消解。仪表盘维持 **✅109/🔧1/⬜55=166（约66%）**；域⑥ 财务维持 **11✅/0🔧/1⬜**。台账处置仅一项：04-backlog **L84 整行勾销**（删除线＋闭合长注＋末列 ✅ B63）；L83/L85/L86 三行保持开放，分别为卡2/卡3/卡4 目标。三证勾稽（完成度＝状态计数＝时间线批次）一致：本卡不产生 ✅ 增量，只闭合一条既有登记。

## 2. 影响面只读核实（卡0/15a 侦察硬结论）

- **finance 后端零实现**：com.meiyun.finance 扁平包下无异常账务相关实体/表/端点；无进项/监控实体（后者属卡3/卡4）；FinanceController 既有 11 依赖可直接追加；Flyway 下版号 V37（V33 财务列后 customer 已占 V36）；V4 建表迁移为 ADD/DDL 双库可重入范式模板。
- **前端假处置**：FinAbnormalView.vue 342 行 dispose() 为纯本地 state 翻转、回单金额恒 null；stores/finReports.ts 内置假 seedOutbox 本地派生长短款 KPI 与待处置列表——登记/审批/入账/分录号回显全部无真源。
- **审批中心可扩展**：txn approval_todo 表 biz_type varchar(16) 无 CHECK 约束；既有四类型 REFUND/CARD_CANCEL/REQUISITION/LOSS_REPORT 为 ApprovalService 硬分支，「审批单即业务单」范式（bizNo=业务单号、payload JSON 明细、终审 finalStage 触发 deductOnFinalApprove 跨服务回调）可直接新增 FIN_ADJUSTMENT；审批端点为类级基路径单数 `/api/txn/approval`（复数 approvals 实测 404）。
- **权限码悬空待接**：finance:abnormal:view / finance:abnormal:dispose 已在 PermissionMatrix 播种，PG role_permission 实测连同 approval:view/refund:approve/cardcancel:approve 五码均授 FINANCE/REGION_MGR/STORE_MGR 三角色，全后端零引用——本卡接线，不新增权限码。
- **动账红线与范式**：FundEntryService L323 人工 MATERIAL/LOSS 分录必拒（损失必须挂审批），ADJUST 在白名单内；adjustOutbox（L511）为 ADJUST 动账既成范式：subject=RF-REVENUE、source=MANUAL、ref_type/biz_type=ADJUST、idemKey 幂等短路、outbox 落 PENDING 由既有管道消费。异常账务入账只走终审回调，禁止绕过审批直接写分录。
- **跨服务通路现成**：StoreConsumableClient 提供双向 RestTemplate＋X-Internal-Token 范式；finance/txn 双端 yml internal URL 与 TxnApplication RestTemplate bean 均现成；internal 落点锚定 txn `/api/txn/internal/finance/abnormal-approvals` 与 finance `/api/finance/internal/abnormal/approval-callback`，网关 Go 路由对 /api/<svc>/internal/** 外网一律裸 404。

## 3. 施工（单卡四段，15 files +1294/-249，commit `3d9d399`）

### 3.1 15b finance-service 后端（5 新 2 改）

- **Flyway V37**：新建 `fin_abnormal_bill` 表，bill_no（AB+yyyyMMdd+三位序号）、store_code、type（SHORT 短款/LONG 长款/WRONG 错账）、amount_fen bigint（长短款=差额绝对值、错账=涉及金额，单位分）、direction（WRONG 登记时必填调整方向 OUT/IN）、source（MANUAL/RECONCILE）、outbox_id（关联对账差异可空）、reason、status（PENDING_APPROVAL/APPROVED/REJECTED/DISPOSED）、approval_no、dispose_fund_entry_id、dispose_outbox_id、created_by/reviewer 工号＋时间戳列；**idem_key 列＋唯一索引**保证登记幂等，ADD/DDL 写法双库（meiyun_core/meiyun_seed）可重入。
- **FinAbnormalBill 实体＋Repository**：AB 序号用 nativeQuery 取号（Specification 支撑状态/类型筛选）。
- **FinAbnormalBillService（约 315 行，五段）**：登记（金额>0/门店 DataScope/idemKey 幂等 duplicated 短路、落 FIN_ABNORMAL_CREATE 审计、同步提交 txn 审批）、列表、详情、终审回调（approved=true/false 双分支，独立 FIN_ABNORMAL_APPROVAL 审计 bizType）、处置（仅 APPROVED 可处置、幂等、ADJUST 白名单入账、DISPOSED 回写分录号/outbox 号、FIN_ABNORMAL_DISPOSE 审计）。**方向映射：SHORT→OUT（冲减）、LONG→IN（补收）、WRONG→取登记时所选方向**。
- **TxnApprovalClient**：POST txn internal `/api/txn/internal/finance/abnormal-approvals`，登记事务内同步调用、失败硬回滚（不产生无审批孤儿账单）。
- **FinanceController（+57）**：四端点——POST `/api/finance/abnormal/bills` 登记（finance:abnormal:dispose）、GET 列表（finance:abnormal:view，状态/类型筛选）、GET `/{billNo}` 详情、POST `/{billNo}/dispose` 终审后入账（finance:abnormal:dispose，幂等）。
- **InternalFundWriteController（+20）**：POST `/api/finance/internal/abnormal/approval-callback`（X-Internal-Token，收审批结果六字段，billNo 幂等）。

### 3.2 15c txn-service 审批接入（1 新 2 改）

- **FinanceAbnormalClient（新）**：POST finance internal approval-callback，**终审事务内同步硬失败**：4xx 透传、5xx/网络故障转 502 回滚审批动作；billNo 幂等。
- **InternalFinanceController（+49）**：第 9 依赖注入 ApprovalService，POST `/api/txn/internal/finance/abnormal-approvals`（@RequirePerm("internal:finance-flow")），六字段基础校验 400 中文文案，返回 {approvalNo}。
- **ApprovalService（+99）**：第 8 依赖；`submitFinAdjustment`——bizType=FIN_ADJUSTMENT、bizNo=billNo、findByBizNo 同单幂等回返既有单号、tierFor 金额分级、payload JSON 明细、applicant 透传空回落 system、title truncate 128、abnormalTypeLabel 短/长/错中文；approve 终审 finalStage 回调 approved=true，reject **任一阶段**回调 approved=false。
- **金额分级（沿用既有 TxnService 口径，不改码）**：L2_MIN=500_000 分（¥5000）、L3_MIN=2_000_000 分（¥20000）——**<¥5000 L1 财务单签；¥5000–20000 L2 店长初审→财务终审；≥¥20000 L3 店长初审→区域复审→财务终审**。L1_MIN=100_000（¥1000）系定义但 tierFor 未引用的死常量，¥3000 走 L1 是退款/退卡/核销/报损共用的既有正确行为，非 bug 不动。
- **顺带修复真实缺陷（L3 REGION 漏分支）**：原 ApprovalService L451-452 店长 REVIEW 通过后插 REGION 阶段的条件写死仅 REFUND/CARD_CANCEL，致 FIN_ADJUSTMENT 的 L3 单店长初审后直跳 FINANCE、漏掉区域复审。已加 FIN_ADJUSTMENT 分支；mvn package txn -am EXIT=0，txn fat jar 47,822,743 字节，双栈四容器换载后新指纹 `f71e4afba47c`，四探活均 healthy。

### 3.3 16 前端五改（样式零新增，只换数据源）

- **api/finance.ts**：四端点薄封装＋FinAbnormalBillDTO 等类型（走 api/client 的 /api base）。
- **stores/finReports.ts**：Store2 删除假 seedOutbox 与 dispose() 本地 state，异常列表/KPI/处置全换真源（保 KPI/五标签 return 形状不变；修两处隐患：值/type import 补三函数三类型、纯 re-export 改 import type＋export type 本地别名）。
- **views/FinAbnormalView.vue（342→417 行整体重写）**：登记 modal（门店/类型/金额/事由，选 WRONG 后动态出现「调整方向」必填下拉 OUT/IN）、四态状态卡 PENDING_APPROVAL/APPROVED/REJECTED/DISPOSED、**仅 APPROVED 渲染处置按钮**、处置双确认弹窗（列明账单/方向金额/幂等键 ABNORMAL:&lt;billNo&gt;/RF-REVENUE/MANUAL/不可重复动账）、ADJUST 分录号回显、空态接真；style scoped 平移零新增类。
- **stores/approval.ts**：ApprovalBizType/BIZ_LABEL/BIZ_PERM 三处枚举加 FIN_ADJUSTMENT（「异常账务调整」/finance:abnormal:dispose），铁律 1 三处一致。
- **views/ApprovalView.vue**：vue-tsc 逼出文件内 BIZ_LABEL/BIZ_PERM 本地副本同样漏 FIN_ADJUSTMENT，已同步补齐。
- pnpm build（vue-tsc --noEmit && vite build）EXIT=0。

## 4. 三轨真验（全绿，逐轨留证）

### 4.1 构建部署

mvn package finance/txn -am BUILD SUCCESS；pnpm build EXIT=0；scripts/deploy.sh 双栈换载（meiyun-finance/txn-service、meiyun-seed-finance/txn-service 四容器），V37 在 meiyun_core/meiyun_seed 双库建成（fin_abnormal_bill 表/uk 幂等索引 \d 核对），换载后四容器 healthy。

### 4.2 curl 轨（正式栈 8443，金额分级矩阵，账单 001-006）

登录人：E012 褚财务（FINANCE）、E005 李店长（STORE_MGR）、E011 冯区域（REGION_MGR）、E009 吴店长（ST-BJ-001 门店）。

- **001 SHORT ¥800 L1 正向**：店长抢 FINANCE 阶段 403→E012 终审通过，txn 待办与账单双 APPROVED、reviewer/approvedAt 落值→dispose 成功 DISPOSED，entryId=35/outboxId=40→同单重放 duplicated=true 幂等短路。
- **004 SHORT ¥0.01 L1 驳回**（E009 登 ST-BJ-001）：驳回空原因 400→E012 reject 后审批与账单双 REJECTED→对已驳回报单调 dispose 返回 400「账单已被驳回」。
- **005 LONG ¥6000 L2**：E005 店长 REVIEW 初审→E012 财务终审→dispose 成功，IN 补收 entryId=36。
- **006 WRONG ¥25000 L3（修复复测）**：REVIEW 阶段财务抢跑 403、REGION 阶段店长抢跑 403→店长初审→区域复审通过→财务终审回调→dispose OUT 冲减 entryId=37，三阶段完整，坐实 L3 REGION 缺陷修复。
- **002 LONG ¥3000 L1**：E012 终审＋dispose，IN entryId=38。
- 旁路：无/错 token 401、网关外访问 /api/finance/internal/** 裸 404、登记参数校验 400/422 均符合预期。

### 4.3 PG 轨（meiyun_core，curl 四笔）

fund_entry id 35-38 四笔全 biz_type/ref_type=ADJUST、subject=RF-REVENUE、source=MANUAL、真实门店、idem_key=`ABNORMAL:<billNo>`：方向金额 **80000 OUT（001）/600000 IN（005）/2500000 OUT（006）/300000 IN（002）** 全对；outbox_record id 40-43 amount 带符号（OUT 负 IN 正）、txn_no=billNo、全 PENDING。审计 id 660-697 共 38 条全部归属本卡六单：FIN_ABNORMAL_CREATE 9、APPROVAL SUBMIT 5/APPROVE 9/REJECT 1、FIN_ABNORMAL_APPROVAL APPROVE 5/REJECT 1、DISPOSE 4、FUND_ENTRY POST 4。

### 4.4 Chrome 轨（8080 正式栈，三角色三会话 page 7/8/9）

- **登记**：009 错账 ¥25000（选 WRONG 后动态出「调整方向」必填下拉，选冲减 OUT）→ L3；另登 007 短款 ¥800（L1）、008 长款 ¥6000（L2）。
- **多阶段审批 UI**：E012 褚财务 /approval 终审 007 L1 单签（待办 4→3、办结 10→11）；E005 李店长（isolatedContext=b63mgr）通过 008 L2/009 L3 初审，008 流转财务、009 流转区域——越权阶段打开详情显「当前阶段需 X 审批，当前角色无权处理」且加签/转交/驳回/同意按钮全隐藏（阶段闸 UI 实证）；E011 冯区域（b63region）复审 009 通过（L3 REGION 修复 `f71e4afba47c` UI 生效）；E012 再终审 008（双签留痕）、009（三签留痕：提交→李店长→冯区域）。
- **处置闭环**：回 /m6-abnormal 三单处置，双确认弹窗列明账单/方向金额/幂等键/RF-REVENUE/MANUAL/不可重复动账；确认后 **009→分录 #39（OUT 冲减 ¥25000，01:34）、007→#40（OUT 冲减 ¥800，01:39）、008→#41（IN 补收 ¥6000，01:39）**，待处置 KPI 4→3→2→1（仅剩 003 缺陷证据单）；错账金额正确排除在长短款 KPI 外（登记后长款 ¥15000/短款 ¥1601 不变）。
- 三角色页面 list_console_messages types=error（含 preserved）全量 **0 条**。

### 4.5 PG 复核（UI 三笔＋终态全量）

fund_entry 35-41 七条 ADJUST 全齐（#39 OUT 2500000/#40 OUT 80000/#41 IN 600000，idem_key/subject/ref_type/biz_type/source 全对）；outbox 40-46 七条符号正确（#44 009 -2500000/#45 007 -80000/#46 008 +600000，全 PENDING）；账单 9 行终态＝**7 DISPOSED**（001/002/005/006 curl＋007/008/009 UI）**＋003 APPROVED 无分录（修复前缺陷版证据单）＋004 REJECTED**，dispose_fund_entry_id 35-41 全回填；审批 9 单 AP20260919-000001~009（**8 APPROVED＋1 REJECTED**，stage 均 FINANCE 终态、assignee 空）；审计 698-718 共 21 条 UI 留痕（SUBMIT/CREATE 各 3＋三角色 APPROVE＋FIN_ABNORMAL_APPROVAL 3＋FUND_ENTRY POST 3＋DISPOSE 3）。本卡审计合计 id 660-718 共 **59 条**。

### 4.6 审计哈希链

id≥660 区间排除末行 NULL 假阳性后 58 链点零断链；全表 5 处断链（247→258/300→302/378→380/518→520/646→650）均位于 id&lt;660 历史缺号区间（与 B62 brokenAtId=258 同口径，B27/B29 冒烟窗口历史删行），append-only 只登记不修，本卡零新增断链。

## 5. 如实说明

### 5.1 L1_MIN=100000 死常量

TxnService 中 L1_MIN=100000（¥1000）定义存在但 tierFor 未引用，实际分级阈值只有 L2_MIN=500000/L3_MIN=2000000，¥3000 走 L1 财务单签是退款/退卡/核销/报损共用的既有正确行为。本卡不改该常量（避免波及四类既有审批），仅在 DELIVERY 与台账中如实钉死口径。

### 5.2 outbox ADJUST 无消费者

七笔 outbox（id 40-46）落库后全 PENDING 是**既有现状**：ADJUST bizType 历来无 outbox 消费者（adjustOutbox 范式如此，残留历史种子 id=9 同为 PENDING）。动账的权威结果是 fund_entry 分录（已真实落库且处置回写分录号），outbox 仅为既有管道留痕。接入 ADJUST 消费者不属于 L84 范围，留存后续。

### 5.3 003 缺陷证据单

账单 003 为 L3 REGION 缺陷修复**前**登记的 APPROVED 单：审批显示终审通过但因漏 REGION 分支时期的回调/状态时序问题无分录、无处置，作为缺陷真实存在过的证据保留至验证结束；数据还原时连同 AP20260919-000003 物理清理，其审计留痕 append-only 保留。

### 5.4 测试数据全还原（单事务物理删除，双份备份）

验毕对 meiyun_core 单事务 `psql -v ON_ERROR_STOP=1` 精准 DELETE（禁 TRUNCATE）：fin_abnormal_bill AB20260919-000001~009 **9 行**、approval_todo biz_type=FIN_ADJUSTMENT 且 biz_no AB20260919-% **9 行**、fund_entry idem_key ABNORMAL:AB20260919-%（entry 35-41）**7 行**、outbox_record txn_no AB20260919-%/biz_type=ADJUST（id 40-46）**7 行**；审计 audit_log id 660-718 共 59 条 append-only **全部保留不删**。

备份双份留档 `/tmp/b63-card1-backup-20260919/`：fin_abnormal_bill.csv（9 行）、approval_todo_fin_adjustment.csv（9 行）、fund_entry_abnormal.csv（7 行）、outbox_record_abnormal.csv（7 行，均 CSV HEADER）＋fin_abnormal_bill_dump.sql（pg_dump --data-only --table=fin_abnormal_bill，2889 字节，COPY 9 行）。

### 5.5 还原后基线与空态复核

共享表精确回落卡前基线：fin_abnormal_bill total=0、FIN_ADJUSTMENT 审批 total=0、**fund_entry 21 行/max entry_id=34、outbox_record 26 行/max outbox_id=39**。残留唯一 biz_type=ADJUST 的 outbox id=9/txn_no=OUTBOX-4/2026-09-05/-468000/PENDING 系历史种子（既有 ADJUST 无消费者现状），非本卡产物，保留不动。

空态三探：curl E012 GET /api/finance/abnormal/bills=[]；GET /api/txn/approval?bizType=FIN_ADJUSTMENT 全量与 tab=todo/tab=done 均 []（不带 bizType 对照全量审批 8 条历史，证明端点健康）；Chrome E012 token 过期重登 /m6-abnormal：KPI 异常笔数 0/长款 ¥0.00/短款 ¥0.00/待处置 0、列表「暂无异常账单」、详情区「当前无异常账单」、console 0 error。

### 5.6 seed 栈零污染

meiyun_seed 全程仅做 V37 结构迁移与换载探活，未造任何业务数据：fin_abnormal_bill=0、ABNORMAL 分录=0、FIN_ADJUSTMENT 审批=0，无需还原。双库保留 V37 正式 schema 与新端点。

## 6. 进度落账

批末六册＋本 DELIVERY＋哨兵同一 docs 原子提交（docs/ 被 gitignore：已跟踪 `git add -u docs/`，本新文件 `git add -f docs/DELIVERY-P5-B63-卡1-2026-09-19.md`）：

- **04-backlog L84**：整行加删除线，依赖列改「**✅ P5-B63 卡1 已闭合（2026-09-19，3d9d399，详见 DELIVERY-P5-B63-卡1-2026-09-19）**」闭合长注（V37/三型/三级审批含 L3 REGION 修复/ADJUST 动账/状态机/前端接真/三轨/9-9-7-7 还原），末列改「✅ B63」；L83/L85/L86 三行零改动保持开放。
- **02-modules L100（域⑥「税 / 卡余额 / 日结 / 异常」行）**：状态保持 ✅、批次 P5-B24、依据列不动，「通用异常中心列 Backlog」原文保留，备注列尾接 P5-B63 卡1 闭合长注（挂载既有 FinAbnormalView 无新页面、V37/三型/三级审批/L3 修复/动账/状态机/三轨/还原基线/数字不变/通用异常中心仍列 Backlog 卡2）。不新增行、不翻状态。
- **00-history**：顶部插 B63「最近更新」巨型行（卡1 3d9d399 战果＋L84 勾销＋数字不变）；B62 行首加「> 上一批 B62：」前缀下沉（内容全保留）。
- **01-dashboard**：B62 bullet 前倒序插入 B63 bullet；数字格 ✅109/🔧1/⬜55=166 与域⑥ 11/0/1 一字不动。
- **03-timeline**：B62 行（L93）后正序追加 P5-B63 卡1 六列行（commit 列仅 3d9d399；卡0 docs commit 76c5dcf 不计；交付文档 DELIVERY-P5-B63-卡1-2026-09-19；末列 L84 勾销＋数字不变＋卡2-4 卡序＋哨兵转卡2）。
- **索引 DEVELOPMENT-ROADMAP.md**：通读确认纯索引无随动。
- **HANDOFF-AUTO.md**：STATUS 保持 ACTIVE（遵从自主队列第5步「卡片闭合后改写为下一张卡并保持 ACTIVE；全队列闭合才 DONE」）；HEARTBEAT 取批末实时北京时间；CARD 由卡1 L84 改写为卡2 ⬜ L83 通用异常中心（卡1 标 ✅＋3d9d399）；PREV 加卡1 锚；人读区顶部加卡1 闭合 bullet；在「## P5-B62 闭合存档」前插入「## P5-B63 卡1 闭合存档（2026-09-19）」七要素块。

## 7. 下一批

**卡2 = 04-backlog L83 通用异常中心（跨域异常单据统一归集与处置工作台）**，卡0 已完成只读侦察、硬结论存档于哨兵 MACHINE:BATCH，契约要点：

- **缺口实证（异常能力六处分散）**：txn `bom_deduct_exception` 有真实端点（BOM 异常后端 only）；checkin/writeoffDesk/bom 三处异常登记各有真实端点；`writeoff_record` 表已有 ABNORMAL 状态＋abnormal_reason＋sign 字段但**全 txn 零写入方**（字段悬空）；财务侧 FinAbnormalView 原仅镜像 outbox（卡1 后已有 fin_abnormal_bill 真实单据源）。无统一归集面与处置工作台。
- **建设目标**：建统一异常单据模型收口三类来源——BOM 异常（bom_deduct_exception）、核销异常（writeoff_record ABNORMAL 三字段接线写入方）、卡1 异常账务（fin_abnormal_bill 只读归集，处置仍走卡1 既有闭环不搬移）；统一列表/筛选/状态口径与处置跳转，前端挂载既有工作台页面（样式零改只换数据源）。
- **依赖已满足**：卡1 fin_abnormal_bill 单据模型与状态机即为财务来源，卡2 可直接只读归集。
- 卡3 L85 预收合规监控（finance 首引 @EnableScheduling 账龄 Job＋规则实体＋复用 txn internal /compliance-alert，prepay_pool/FinPrepayView 接真）、卡4 L86 进项税抵扣（进项发票实体/认证/抵扣/申报全链新建，V37 后续版迁移）契约随各卡开工细化。

按「卡0 硬结论直接取用→一卡一 commit+push→前端样式零改只换数据源→curl+PG+Chrome 三轨真验→测试数据单事务还原→批末六册落账」继续施工。队列顺序：卡2 L83（下一张）→卡3 L85→卡4 L86，四卡全闭合后 B63 批置 DONE 并转 B64。
