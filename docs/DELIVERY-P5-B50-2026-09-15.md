# P5-B50 交付：平台治理/还债批九卡——采购/耗材 DataScope 越权收窄 + 大屏时区 + 目标行级域 + 登录四门控 + 审计断链清单 + 网关真实 IP + actor 通道收敛

- **交付日期**：2026-09-15
- **批次性质**：**平台治理/纯还债批**——九卡（含 1 卡零代码）全部勾销 04-backlog 既有观察项，无新页面/新模块切真，**完成度数字不变：108/166=约65%、🔧 1、⬜ 56，域⑧ 34✅2🔧31⬜**
- **feat 提交**：卡2 `da3d812`（采购 DataScope，3 files +116/-14）、卡3 `cbd6692`（大屏 shTimeStr，2 files +9/-2）、卡4 `3a263ab`（canReadTarget，6 files +206/-5）、卡5 `7f08103`（登录四门控，4 files +52/-23）、卡6 `aecf97f`（verifyChain 清单，5 files +201/-8）、卡7 `5251306`（网关真实 IP，7 files +364/-7）、卡8a `dd4307a`（耗材 storeSpec+库存 watch，5 files +179/-39）、卡8b **零代码无 commit**、卡8c `43f009b`（actor 通道收敛，1 file +9/-2）
- **铁律对齐**：铁律 1（既有契约零破坏：verifyChain brokenAtId 旧口径保留、审计 List 端点不动、api/* 只加不改）、铁律 5（无 mock 回填，DEVICE 等无源项继续诚实空态）、铁律 6（后端 mvn package+docker cp/网关交叉编译 recreate、前端 pnpm build 含 vue-tsc）、铁律 7（curl 经网关+PG+Chrome/构建三轨真验，纯后端卡按双轨取证）、铁律 8（一卡一 feat commit 紧跟 push，docs 批末原子提交）、铁律 9/10（本交付文档 + 5 分册回写）、铁律 -1-D（跨店例外域 DataScope.storeSpec 基座）

## 1. 背景与目标

用户拍板「① 平台治理批（登录页 401 门控 + 历史断链 #380 全量断链清单 + 各卡平台类小项）；② M1 Backlog 纵深缺口；先 1 后 2」。本批为①，把 M1 集团屏批（B49）各卡三轨真验中登记的平台治理/越权/观察项一次清零：

| 卡 | 勾销位置 | 事项 | commit |
|---|---|---|---|
| 卡1 | 侦察（无代码） | 全平台越权/观察项盘点，定性九卡范围与顺序 | — |
| 卡2 | 04 L127（B49 卡5 新增） | M1 采购组合域列表 REGION 无参跨区越权 | `da3d812` |
| 卡3 | 04 L129（B49 卡7 新增） | 数据大屏成交流时间显示 UTC 未转北京 | `cbd6692` |
| 卡4 | 04 L132（B49 卡8 新增） | target 列表行级 DataScope 未收窄 | `3a263ab` |
| 卡5 | 04 L146（B49 卡12 新增） | 登录页无 token 全局壳预拉四路 401 | `7f08103` |
| 卡6 | 04 L122（B49 卡2 新增） | verifyChain 只报首处断链，需全量清单 | `aecf97f` |
| 卡7 | 04 L134（B49 卡9 新增） | 审计真实客户端 IP 采集（去硬编码 "web"） | `5251306` |
| 卡8a | 04 L147（本批新增随勾） | 耗材档案/流水 REGION 无参跨区越权+库存页切店不重拉 | `dd4307a` |
| 卡8b | 04 L124（B49 卡4 新增） | 无 GROUP 集团全域账号（全量域真验缺口） | **零代码** |
| 卡8c | 04 L148（本批新增随勾） | audit append 通道 actor 自报可伪造 | `43f009b` |

保留登记不动工两行：04 L123（哈希验真 UI 中项 + DSAR/consent/巡检合规远期大项）、04 L133（JWT impersonate act/realSub 平台/安全大项，卡8c 已先行收敛 audit actor 通道语义供其未来复用）。

## 2. 卡2 采购列表 DataScope 越权收窄（`da3d812`，3 files +116/-14）

**缘起**：B49 卡5 权限三角断言发现，PurchaseOrderRepository 的 search JPQL 带 `cast(null as string) is null` 退化谓词，REGION 多店账号无参调用时 Controller 传 null→门店过滤整体失效→全量泄露非本区采购单。

**改动**（store-service procurement 域）：

1. PurchaseOrderRepository 增继承 JpaSpecificationExecutor，删除 cast null 全量退化 JPQL；
2. PurchaseOrderService.listPurchaseOrders 改以 DataScope.storeSpec("storeCode") 为不可绕过查询基座，显式 storeCode/status 作叠加谓词、Sort id desc，"__NONE__" 哨兵短路空列表；
3. Controller 显式参守卫保留，Specification 层作纵深防御兜底；
4. PurchaseOrderServiceTest 三契约用例（哨兵短路不触库/findAll(spec,sort)/不退回无域 findAll）。

**三轨真验**（seed 18443，E011=REGION_MGR 华东 stores=[SST01,02,06]）：无参采购单 6→4（SST03 两单消失，仅 SST01×2/SST02×2）、显式越界 SST03 返空、本域 SST01=2、status/组合谓词正确、id desc、无 token 401；PG 域内 count=4 对账一致；双栈 docker cp 部署 healthy。

## 3. 卡3 数据大屏 shTimeStr 成交时间锚定 Asia/Shanghai（`cbd6692`，2 files +9/-2）

**缘起**：B49 卡7 Chrome 轨登记：m1Screen order-paid 顶插流水时间直接 `slice(11,16)` 截 UTC ISO 字符串，北京环境显示差 8 小时（05:26 实为 13:26）。

**改动**（frontend）：utils/datetime 新增 `shTimeStr`（复用既有 SHANGHAI_TIME_FMT，Intl 显式锚定 Asia/Shanghai），shDateTimeStr 同步复用；m1Screen 成交流时间改走北京时区转换，非法/空值兜底「—」。

**三轨真验**：造 now() 新支付→SSE 真实扇出 paidAt=05:26:00Z→大屏显 13:26/13:28，与 PG `AT TIME ZONE 'Asia/Shanghai'` 零误差；前端构建通过。

## 4. 卡4 DataScope.canReadTarget 详情/动作级越权收窄（`3a263ab`，6 files +206/-5）

**缘起**：B49 卡8 权限三角断言：target 列表仅校验 target:view 功能权限，有视图权限即可读全量集团/异区目标行，缺行级数据域。

**改动**（finance-service target 域）：

1. DataScope 新增 canReadTarget：GROUP 行各域共享；REGION 行按 JWT 中文 region 对 ownerName contains 匹配；STORE 行复用 canReadStore；super/GROUP/BRAND/匿名既有口径放行；
2. BizTargetService.list 循环首行叠加 canReadTarget 行级过滤（ownerType 等可选过滤仍生效）；
3. 种子 STORE 归属归一真实门店码（T01 杭州西湖旗舰院→SST06、T02 上海静安分院→SST01）；
4. BizTargetServiceTest 四角契约（REGION 华东/华北、STORE、超管/匿名、ownerType 叠加）共 5 用例全绿；
5. M1TargetView 树渲染过滤数据域外悬空 children，消除空白 0% 占位行。

**三轨真验（四角真验 7/5/5/10）**：E011 华东可见 7 行（G1-E+SST06+SST01）、SE102 华北 5 行（G1-N）、SE001 SST01 5 行（T02-R）、SE101 超管 10 行全量；Chrome E011 页面零空白行；PG 逐行对账吻合。

## 5. 卡5 登录页全局壳四门控（`7f08103`，4 files +52/-23）

**缘起**：B49 卡12 三轨真验观察项：/login 挂全局壳 CShellDesktop.vue，未登录 onMounted 即无条件预拉 permissions/stores×2/notifications 四路请求全 401（非卡12 引入的既有现象，拦截器虽对 /login 豁免跳转但控制台噪声+无效流量）。另发现 main.ts 与 App.vue 双 init 并发窗口内 /stores 双发。

**改动**（frontend，门控收口在数据层——任何调用方都受保护，main.ts/App.vue 调用点不动）：

1. auth.ts loadMatrix 首行无 token 直接 return false、applySession 建会话后补拉矩阵；
2. storeContext.ts loadStores 三重闸门（无 token resolve／loaded resolve／inflight 复用同一在途 Promise，finally 清零）根治双 init 双发；
3. CShellDesktop.vue 壳仅认证后挂载、onMounted 作登录后统一补拉点；
4. notification.ts fetch/fetchPreferences 无 token 不发、旁路失败静默。

**三轨真验**（seed 18080，新 bundle index-SOVxdEZ6.js 双栈部署）：无 token 访问 /login 及受保护路由 XHR 零请求、console 零报错、守卫正确跳 /login?redirect；E011 真实登录 permissions/stores/notifications 各 1 次全 200、门店切换器徐汇店/通知 9 条/业务请求带 storeCode=SST01；已登录硬刷新 restoreSession 后三路各 1 次 200 无双发；pnpm build exit 0。

## 6. 卡6 verifyChain 全量断链清单（`aecf97f`，5 files +201/-8）

**缘起**：B49 卡2 /verify 实测 {"ok":false,"brokenAtId":380}，verifyChain 首处失配即返回，无法回答「到底还有几处断链」；#380 全量清单登记为 Backlog。

**改动**（audit-service + 前端）：

1. AuditService.verifyChain 由「首处失配即返回」改为遍历全程收集全部失配节点，新增 ChainBreak 明细（id/期望前驱/存储前驱/curHash/action/actor/createdAt）与 ChainVerifyResult.breaks 数组；
2. **关键口径**：失配后 expectedPrev 始终沿**存储的 node.curHash** 推进而非重算值，才能独立检出每一处历史断链、不把断点后正常节点误报；
3. brokenAtId 首处口径保留旧契约兼容（m5Settings 等既有消费方零破坏），append-only 存量不回改；
4. AuditServiceVerifyChainTest 三用例（篡改+重链同型、断链在下游暴露）；M1AuditLogView 断链清单卡渲染。

**三轨真验**：prod breaks=[380,520]/569 两处历史断链全量暴露——#380（2026-09-12 11:49）与 #520（2026-09-13 20:01）均早于 B48 修复 `4bfb658` 部署窗口（2026-09-14 09:41），修复后无新增断链；seed breaks=[]/total=117（卡8c 真验后 total=124，新行正常延链）。

## 7. 卡7 Go 网关真实客户端 IP 采集（`5251306`，7 files +364/-7）

**缘起**：B49 卡9 合规复检审计 payload.ip 如实写死 "web"（后端在网关后拿不到真实对端），登记「审计真实客户端 IP 采集」观察项。

**改动**（gateway + meiyun-security + audit-service）：

1. Go 网关 router.go 增 clientIPFromRequest/remoteHost + Director 规范化：X-Real-IP=解析出的客户端、XFF 追加直接对端 peer、直连采 RemoteAddr；router_test.go 9 用例；
2. meiyun-security 新增 ClientIp 工具（XFF 首段→X-Real-IP→remoteAddr→"unknown"）+ClientIpTest 6 用例（JDK 动态代理 fake，零新增依赖）；
3. ComplianceController 注入 HttpServletRequest、ComplianceService.recheck 加 clientIp 参数去硬编码 "web"，ComplianceServiceRecheckIpTest 2 用例；共 20 单测全绿。

**双栈部署**：交叉编译重建 meiyun/gateway:latest 双栈 recreate、audit 胖包 docker cp 双栈重启均 healthy。

**三轨真验**：curl 四场景（经 nginx/直连/伪造 XFF 203.0.113.77/无头兜底，seed 审计 id180-182、prod id588-589）、PG payload.ip 全部真实非 web；Chrome seed id183 actor=冯区域 ip=192.168.65.1，审计页渲染 121 条「哈希链完整」；新行均正常延链。

## 8. 卡8a 耗材 storeSpec 越权收窄 + 库存页切店重拉（`dd4307a`，5 files +179/-39）

**缘起**：卡2 同型还债排查耗材域发现同型 cast null 全量退化 JPQL；另前端 InventoryView 漏挂门店切换 watch（OrderView/Writeoff/AppointmentBoard 均有范式），切店后不重拉。

**改动**（store-service consumable 域 + frontend）：

1. ConsumableRepository/ConsumableMovementRepository 改继承 JpaSpecificationExecutor 并删除 cast null 全量退化 JPQL；
2. ConsumableService 两列表方法以 DataScope.storeSpec("storeCode") 为基座（"__NONE__" 哨兵短路、显式参叠加、档案 Sort ASC skuCode/流水 Sort DESC id），写路径未动；
3. ConsumableServiceScopeTest 6 用例，store-service 全量 12 绿；
4. InventoryView 补门店切换 watch。

**双栈部署**：docker cp+restart healthy。**三轨真验**：curl seed E011（REGION stores=[SST01,02,06]）档案无参 36→18、越界 SST03=0、本域 SST01=6；流水无参 38→18（5+7+6）、越界=0、本域 SST02=7；无 token 401；prod E011 档案 2/流水 4 均 ST-SH-001；PG 对账全符；Chrome 库存页切换器仅现在域三店，切徐汇→浦东无刷新即重拉、四路 XHR 全带新店码 200、console 零告警。

## 9. 卡8b SE101 GROUP 全域口径核对（零代码闭合，无 commit）

**缘起**：B49 卡4 登记「无 GROUP 集团全域账号，集团全量域真验缺口」，原拟新增 SE106。

**核对结论（走 backlog 原文第二路径「明确 SUPER_ADMIN 全量域口径」，不新增账号）**：PG 双库核实既有播种账号 **SE101 周岚**（RbacDataInitializer.EXTRA_STAFF）在 meiyun_core/meiyun_seed 均为 SUPER_ADMIN/store_code=null/region=null/在职；登录实测 scope=GROUP、stores=[]、perms=\*（AuthController ROLE_SCOPE SUPER_ADMIN→GROUP；resolveVisibleStores 对 GROUP/BRAND 返空列表，DataScope 遇空＝全量）。不新增 SE106：既有账号已合格，增账号既冗余又扩大弱口令面。另 E014 蒋IT 同为超管可备。

**三口径真验**（/finance/group-overview DataScope.storeSpec 基座对齐 PG 基线）：seed SE101 全域 revenue_monthly 7 行/4 店（SST01/02/03/06，2026-08 四店 ¥1,259,000,000）vs E011 华东 6 行/3 店（SST01/02/06，8 月 ¥260,000,000，差额恰为越界 SST03 的 ¥999,000,000）；prod SE101 7 行/5 店 vs E011 2 行/仅 ST-SH-001；双栈匿名 401。

## 10. 卡8c AuditController actor 按通道强制收敛（`43f009b`，1 file +9/-2）

**缘起**：卡8b 侦察 ComplianceService/前端 impersonate 留痕链路时发现：员工持 audit:view 经网关直调 POST /api/audit/append 时请求体 actor 字段原样落库，可伪造任意操作人，破坏审计不可抵赖性。

**改动**（audit-service AuditController.append，按鉴权通道强制 actor）：

1. `"system".equals(DataScope.currentActor())`（X-Internal-Token 系统身份，业务服务已在本地 SecurityContext 代填真实操作人）→沿用 req.actor() 且空值兜底 "system"；
2. 否则（员工 JWT 直连，合规 impersonate 留痕）→强制取 JWT staffId，忽略请求体 actor；
3. AppendRequest.actor 去 @NotBlank。

mvn package audit-service 胖包双栈 docker cp+restart healthy。

**五通道真验**（B50_8C_TEST append-only 永久留痕）：T1 伪造 actor=FORGED_E011→落 SE101（seed id184）、T2 不传 actor→SE101（id185）、T3 匿名→401、T4 内部 token 代填 E011→沿用 E011（id186，宿主 18084 绕网关）、T5 错内部 token→401（user==null 拦截器口径）；prod 同构 P1 伪造 HACK→落 SE101（id590）；seed verifyChain ok total=124，prod 断链仍仅历史 #380/#520（卡6 已治理为清单展示，新行正常延链）。

## 11. append-only 留档专章（不可删/不可改）

本批真验在生产库（meiyun_core）与种子库（meiyun_seed）产生的永久留痕数据，按审计/业务 append-only 原则不回改、不清理：

| 库 | 对象 | 内容 | 产生卡 |
|---|---|---|---|
| meiyun_core | compliance_check id=1 | 手插 PRIVACY「L134 平台治理验真临时项」，现 **FAIL**（无真实业务含义，仅验真载体，留存） | 卡7 |
| meiyun_core | audit_log id588/589 | IP 采集四场景中经网关两场景，payload.ip 为真实客户端 | 卡7 |
| meiyun_core | audit_log id590 | B50_8C_TEST P1：JWT 直调伪造 actor=HACK 被强制落 SE101 | 卡8c |
| meiyun_seed | audit_log id180-182 | IP 采集 curl 三场景（经 nginx/直连/伪造 XFF） | 卡7 |
| meiyun_seed | audit_log id183 | Chrome 合规复检 actor=冯区域 ip=192.168.65.1 | 卡7 |
| meiyun_seed | audit_log id184/185/186 | B50_8C_TEST T1 伪造 FORGED_E011/T2 不传 actor/T4 内部 token 代填 E011 | 卡8c |

历史断链 #380/#520 为 B48 修复前历史窗口产物，append-only 不补写、不回改，由卡6 verifyChain 清单如实展示。

## 12. 如实说明（不夸大闭环）

1. **纯还债无完成度增量**：本批九卡无新页面/无模块状态跃迁，✅108/166=约65%、🔧1、⬜56、域⑧ 34✅2🔧31⬜ 一律不变；价值在越权面收窄（卡2/4/8a 三个 DataScope 缺口同型治理）、审计可信链增强（卡6 清单/卡7 IP/卡8c actor）与平台噪声治理（卡3/卡5）。
2. **断链是「查清」不是「修复」**：prod #380/#520 按 append-only 不可篡改原则保持原状，卡6 交付的是全量暴露能力（清单+前端卡），防复发靠 B48 outbox+中继+对账三道防线。
3. **compliance_check id=1 FAIL 永久留存**：手插验真项无业务含义但不删除，任何合规看板统计需知悉该行为平台治理验真载体。
4. **卡8b 零代码**：GROUP 全量域缺口以「明确既有 SE101 口径」闭合，未新增账号、未改代码，无 commit；未来若需职责分离的独立集团审计账号，另开卡评估。
5. **保留登记两行**：L123 哈希验真 UI（中项，需先定义 CSV 规范化字节口径否则验真必失败）/DSAR/consent/巡检（合规远期大项）、L133 JWT impersonate act/realSub（7+ 服务本地验签，平台/安全专项大项）均不在本批动工。
6. **卡7 前端面**：仅合规复检链路去除 "web"，其余业务服务审计的 ip 字段随网关 X-Real-IP 与 meiyun-security ClientIp 落地后自然获得真实值，未逐服务审计 payload 改造（写侧各服务本地代填链路保持不变）。

## 13. 进度落账

- 完成度 **108/166=约65% 不变**、🔧 **1**（C 端移动端远期行）不变、⬜ **56** 不变；域⑧ **34✅2🔧31⬜ 不变**；
- 04-backlog 勾销九行：L122 断链清单（✅B50 卡6）、L124 GROUP 全域（✅B50 卡8b）、L127 采购越权（✅B50 卡2）、L129 大屏 UTC（✅B50 卡3）、L132 target 行级域（✅B50 卡4）、L134 审计 IP（✅B50 卡7）、L146 四路 401（✅B50 卡5）、L147 耗材越权+切店（✅B50 卡8a，新增随勾）、L148 actor 伪造（✅B50 卡8c，新增随勾）；
- 02-modules 十处挂载行备注：L108 登录/卡5、L115 耗材/卡8a、L125 审计链/卡6+卡8c、L127 网关/卡7、L131 M1审计日志/卡6、L137 采购/卡2、L139 大屏/卡3、L140 目标/卡4、L141 合规/卡7+id=1 留档、L144 调度四路 401 闭合措辞；
- 03-timeline 表底 P5-B50 单行（8 commit+卡8b 零代码）；00-history 顶部 B50 简报、B49卡12 降级「上一批」；01-dashboard L18 流水尾接+L36 域⑧行尾接，数字表不动。

## 14. 下一批

**P5-B51 M1 Backlog 纵深缺口批**（用户拍板顺序②）：首组为 B49 卡12 登记的调度六缺口——DEVICE 设备档案台账域（04 L140）、URGENT 加急标记源（L141）、durationMin 真实时长（L142，依赖 appointment 关联 SKU/时长字段）、assignment DONE 完成态（L143，依赖服务完成事件源）、医生/房间班次表（L144）、派单时段自由度/改期（L145）；多依赖跨域新数据源，动工前逐卡侦察数据源就绪度，无源项继续诚实空态不伪造。
