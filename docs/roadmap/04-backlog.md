# 台账分册 · ④ 后续规划 Backlog

> 属于美研云整体开发台账分册；索引、状态判定口径与读写规则见 [../DEVELOPMENT-ROADMAP.md](../DEVELOPMENT-ROADMAP.md)（铁律 9 / 10）。
> 登记「本批做不了、涉及后续模块/阶段」的事项，逐条注明来源、依赖/前置与建议批次；收口的条目整段保留并标注已收口。

## ④ 后续规划 Backlog

> 登记「本批做不了、涉及后续模块/阶段」的事项，逐条注明**来源**与**依赖/前置**与**建议批次**。能收口的在当前批次收口，不入此表。

### B20 已收口（2026-09-08 交付，详见 DELIVERY-P5-B20）

L3 审批 SLA 超时扫描+催办（ApprovalSlaJob 60s/三阶段时限 24h-8h-4h/120min 节流/幂等）、消息通知中心最小落地（notification 表 + 4 真实端点 + 铃铛角标/跳转已读）、审批中心候选人门店预过滤、退卡手续费 RF-REVENUE/IN 收入分录对账、sign_tier 双库回溯（无空值/在途全对）——全部闭合。

### B21 已收口（2026-09-08 交付，详见 DELIVERY-P5-B21）

审批转交/加签目标人硬校验（guardTargetApprover：工号存在/在职/主角色+兼岗并集按阶段校验 STORE_MGR/REGION_MGR/FINANCE/自转交拦截/重复加签拦截/加签给当前指派人拦截，网关硬校验矩阵 10/10 全中、中文报错原文留档；审计历史带 toName/whoName 中文姓名）、通知偏好持久化（notify_preference 表 + uk 唯一约束 + GET/PUT /api/txn/notifications/preferences 端点，五类×四渠道校验，前端 Pinia 乐观更新+snapshot 回滚、UI 零改动）、SLA 催办偏好免打扰（ApprovalSlaJob 收件人偏好过滤，关订阅→跳过1人/通知0人、恢复→通知1人，双向实证）、审批详情指派人/会签人展示（ApprovalView 模板补「当前指派人/会签人」）、退卡冻结 SLA 超时提醒（**实证勾销**：Job 仅按 status=PENDING 扫描不分 bizType + seed 库 CARD_CANCEL 6 单全走 approval_todo 办结，B20 已无差别覆盖）——全部闭合。

### B22 已收口（2026-09-08 交付，详见 DELIVERY-P5-B22）

营销 9 页收口全部闭合：**精准推送写链路四件套**（PushService：Bean Validation 参数校验 422、dedupKey=SHA-256(customerId|pushType|content) 前 16 位 60s 窗口幂等重放返回同一 pushId 不重复落库/不占频控/不重复审计、DbRateLimiter 周频硬上限 3 超限 400 中文报错、违禁词四类实时校验 400；全动作 audit_log bizType=PUSH/action=SEND，网关矩阵 7/7）、**券核销门店三级兜底**（currentStore：登录人 storeCode→store-service 新增 GET /api/stores/internal/first 内部端点（X-Internal-Token，服务间走 HTTP 不直读别域表）→FALLBACK SST01 降级；门店字段在三态分支之前设置，FORGED 异常单也落真实门店；E014 无门店上下文实证返回 ST-SH-001/上海旗舰店）、**核销链路漏斗播种器**（CouponWriteoffChainDataInitializer @Order(33)：CH-001 正常核销 774 / CH-002 异常核销 9 / CH-003 待处理 59，count>0 skip 幂等）、**海报推荐人接真实员工**（m5Poster store loadReferrers 改 listStaff，filter 非离职 map staffName/role.roleName，zh-Hans-CN 排序；Pinia 直查 19 名真实在职员工，mock「林晚」已替换）、**9 页浏览器逐页真实验证**（优惠券 8 券/1742 领取/3 核销、推送 13 客户/周频提示、核销今日 12/正常 3/异常 9/抵扣 ¥1,140、海报 6 模板/佣金 ¥9,370、素材库 10 素材、直播 7 场/挂链 ¥471,400、ROI 发券核销真实、渠道业绩 7 渠道卡片、总览 651,030 触达漏斗，控制台零报错）。核销三态对账：audit_log COUPON_WRITEOFF 8 条（WRITEOFF 1：VIP85 discountFen=55200；BLOCK 7：FORGED×4/DUPLICATE×3）。

### B23 已收口（2026-09-09 交付，详见 DELIVERY-P5-B23）

客户域 4 卡全部闭合：**积分商城**（商品/规则/兑换/双签审核/拒绝/履约/KPI，持久单号、幂等下单、审核通过同事务扣积分扣库存、履约幂等）、**积分账户人工调分**（客户 360「档案」tab、流水分页、积分池真实读模型、非零整数/原因长度/余额非负校验、clientToken 部分唯一索引防重放、全动作审计）、**客户标签**（消费/肤质/行为/价值/医疗五分类 CRUD、标签名全局唯一、重复打标 409、删除定义级联解绑、覆盖汇总、客户列表 tagId exists 过滤、360 打标/删标）、**会员等级**（普通/银卡/金卡/钻石/黑卡中文五档、阈值/折扣/权益、单行等级规则、手工调级、自动升级只升不降、客户表实时人数聚合）。prod 当前 B23 审计 33 条、17 个动作分组、null_payload=0；seed 重置后客户 100、标签 12、打标 265、商城商品 7、兑换单 8、B23 审计 0，四页浏览器回归无 JS 报错。

### B24 已收口（2026-09-09 交付，详见 DELIVERY-P5-B24）

「两线合并全收」5 卡全部闭合：**财务四页去 mock + 发票 CSV**（发票/预算/财务设置/资金日报接真实端点，`/api/finance/export/invoices.csv` 真实导出）、**卡余额时间线 + 核销双签明细**（customer-service `GET /internal/cards/{cardNo}/ledger` 与 txn-service `GET /internal/finance/writeoff-details` 两个 X-Internal-Token 内部投影，服务间走 HTTP 不跨库直读；finance 聚合暴露卡时间线/核销明细/卡台账 CSV/核销 CSV，四类枚举中文映射）、**咨询草稿/接诊**（consult-plan draft/start 端点 + ConsultationView 会话区切真实方案单）、**treat-start/treat-done 治疗全链路**（方案单状态机 PENDING→ACTIVE→PENDING_REVIEW→APPROVED→READY_PAY→PAID→TREATING→DONE 贯通，TreatStartCmd 扁平四项布尔术前核对、treat-done 治疗小结必填并生成 treatEmrId，权限 consult:review+emr:edit 双栈硬校验）、**我的工作台 7 项计数真实化**（审批/待收款/今日预约/方案单四态，每域 auth.can 门控 + 独立容错；候诊接待/病历草稿/术后回访/SOP 超期/复诊提醒 5 项无后端域保留演示并显式标注「（演示）」、`todoTotal` 只累加真实项）。prod 终验：E003 陈医生真实待办 2（ACTIVE=1/PENDING_REVIEW=1，无权限项隐藏，order 403）、E005 李店长待收款 1、SE101 真实待办 3，/doctor 刘女士 M002 ¥2280 真实方案单，三页 console 零错；prod 凭证澄清（E001–E014/SE101–SE105 + meiyun123，SE001 仅 seed 库）。seed 端到端造数 TREATING 链路实证（江医生 5/许店长 11），测试残留单号已在交付文档 §5.2 诚实登记。样式零改动。

### 移交后续（Backlog）

| 事项 | 来源 | 依赖 / 前置 | 建议批次 |
|---|---|---|---|
| SLA 超时自动升级/自动代办 | B20/B21 移交 | 组织树兼岗模型完善（兼岗上级路由）；现仅标记+催办+偏好免打扰。**B33 复核：本批只补齐部门层级写能力（建/改/启停），未引入兼岗上级字段与路由算法，本项维持 Backlog** | 组织树二批（兼岗模型） |
| 通知多渠道（短信/企微/邮件/WebSocket 实时推送） | B20/B21 移交 | 渠道适配层；notify_preference.channels 已存渠道位（INBOX/SMS/WECHAT/EMAIL）不返工；现为系统内通知+轮询 | B22+ |
| 审批 REGION 阶段兼岗路由（指派人/会签按兼岗上级） | B19/B21 移交 | 组织树兼岗模型完善；硬校验 B21 已收口（主角色+兼岗并集校验），缺兼岗上级自动路由。**B33 复核：org_unit 已有 leader_name 但仅为文本展示字段，未与 employee 建立外键与兼岗关系表，路由仍无数据基础** | 组织树二批（兼岗模型） |
| 组织树门店/大区/集团层级写能力 | B33 新增 | B33 三写端点仅放行 org_type='部门'（新建父级必须为门店、编辑不可改类型/编码、跨店移动联动 store_code+region）；门店/大区/集团节点的增改停仍需手工改库或走 store 域，缺层级重挂与子树级联规则 | 组织树二批 |
| 组织节点物理删除与编码回收 | B33 新增 | B33 只做停用（status 启停 + 停用原因必填），不提供 delete；物理删需先定义引用检查（employee.org_code / 审批链 / 历史单据）与 org_code 回收策略，否则会产生悬挂引用 | 组织树二批 |
| 免打扰时段策略（静默时间段/加急例外） | B21 新增 | notify_preference 已就绪，需加时段字段与 Job 判断 | B22+ |
| prod 8 月已办结单 sign_tier 旧阈值错配 15 笔 | B20 回溯 | refund 4 + card_cancel 11（8/16、8/20 旧阈值遗留）；改已办结单与审批轨迹矛盾，需历史迁移专项连同轨迹一并处理 | 数据治理专项（不紧急） |
| 赠金高级规则（满赠阶梯/有效期/报表） | B18 移交 | 营销规则引擎 | 营销收口批 |
| 86 张卡开卡首笔流水修复 | B17 移交 | 售卡开卡链路排查 | B21（数据修复） |
| 去划扣项目智能预填 | B17 移交 | 卡项-项目映射 | 交易收口批 |
| 撞单合并 | business-flows | 客户查重/合并规则 | 客户域批 |
| 资产转移 / 合同 | business-flows | 卡资产模型 | 交易域批 |
| 超管 Impersonate（ impersonation 登录） | business-flows | RBAC 扩展 + 审计 | 平台批 |
| ~~排队智能候补~~ | business-flows | **B29 已闭合（2026-09-10，arrival_waitlist 新域：WL 单号/手机号锚定会员·散客快照两态/WAITING→NOTIFIED→FULFILLED·CANCELLED、号源释放同事务 FIFO 递补+店长站内信幂等、/queue 第三卡+登记弹层+KPI，同批附带候诊超时自动释放与核销↔预约/划扣勾连，网关 24 项+浏览器两页取证 audit 314–331，详见 DELIVERY-P5-B29）** | ✅ B29 |
| ~~转介绍到期重分配~~ | business-flows | 营销渠道 + 定时任务；**B22 复核：转介绍页 /m5-referral 不在 9 页内仍为 mock，老带新数据未接真实客户关系，维持 Backlog** | 营销二批 |
| 私域自动化 Flow（随访/SOP/关怀/召回） | business-flows | 流程引擎 + 营销；术后随访 SOP 引擎及随访工作台/SOP 编排两整页（诊疗侧）B30/B31 已闭合，本行余营销侧关怀/沉睡唤醒/复诊召回 Flow | 远期营销阶段 |
| 角色管理页（RBAC 可视化配置） | permission-matrix | 后端 RBAC 已就绪，缺前端管理页 | 组织收口批 |
| ~~营销 9 页交付验证收口~~ | 盘点 | **B22 已闭合（2026-09-08，9 页逐页网关+浏览器验证、零控制台报错，详见 DELIVERY-P5-B22）** | ✅ B22 |
| 外部广告渠道接入（美团/抖音/小红书/大众点评/新氧投放回传） | B22 移交 | m5-channel 7 平台卡片 + m5-roi 渠道区现为前端 mock；**B26 已落通用回传骨架**（ExternalChannelController：HMAC-SHA256 签名/±300s 窗口/限流 60/min/幂等 bizRef/客户硬校验/channel_returnback 落库，双栈冒烟取证）；仍需各平台 OAuth 对接 + 投放 spend 落库 + ROI 联调 | 外部集成批（T3） |
| 营销归因模型（首次触点/末次触点/多触点加权） | B22 移交 | m5-roi 归因区现为 mock；依赖客户触点事件流（浏览/咨询/到店/成交）埋点归集，现触点数据未采集 | 数据仓库批（T2） |
| 素材库对象存储（S3/MinIO 替换本地/静态资源） | B22 移交 | m5-assets 10 素材现为种子静态数据；需文件上传服务 + 桶策略 + 素材审核流 | 平台批 |
| 海报图片真实渲染（模板+二维码+推荐人工号合成） | B22 移交 | m5-poster 6 模板为静态展示；需后端渲染服务（如 html2image/画布合成）+ 分销二维码绑定 referrer | 营销二批 |
| 直播/短视频写接口与成交回写 | B22 移交 | m5-live 7 场/挂链成交 ¥471,400 为种子只读数据；需直播排期写接口 + 挂链订单成交回写 + 审计四件套 | 营销二批 |
| 营销总览导出报告 / 订阅周报 | B22 移交 | m5-dashboard 651,030 触达漏斗真实只读；需报表导出（Excel/PDF）+ 订阅定时推送（可复用 B20 通知中心） | 营销二批 |
| ~~营销设置页 /m5-settings 接线~~ | B22 盘点 | **B37 已闭合（2026-09-12，`828ac6d`，详见 DELIVERY-P5-B37）**：卡1 统一 marketing_cfg 读写 DTO 契约（ConfigView/ConfigCmd 11 字段对称、weeklyPushLimit 统一、渠道数组化、剔除多泄列），全 12 键真实页面+容器端点管理（周频上限/审批/免打扰/渠道/兜底门店等；违禁词库另走 /marketing/forbidden-words，B34 已配置化兜底门店），营销设置 🔧→✅ | ✅ B37 |
| 赠金高级规则（满赠阶梯/有效期/报表） | ✅ B26 | GrantRule/GrantService/GrantExpireJob 真实闭环（规则 CRUD + 发赠金账本 + 过期批处理 + 报表）；收银台抵扣列下游 Backlog | 营销二批 |
| ~~赠金「收银台抵扣」（status VALID→USED）~~ | B26 新增 | **B35 已闭合（2026-09-12，grant_deduction 流水+FIFO 带锁扣减/幂等/超额 422/GRANT_DEDUCT 审计+marketing internal 端点+txn grant 支付+收银台磁贴混合支付，84 单测+curl+浏览器三轨取证，详见 DELIVERY-P5-B35）**；消费满额自动发赠金触发见下方 B35 新增行 | ✅ B35 |
| ~~消费满额自动发赠金触发（issueByRule 接消费事件）~~ | B35 新增 | **B37 卡2 已闭合（2026-09-12，`7552665`，详见 DELIVERY-P5-B37）**：实现路径经自主决策改为**轮询拉取**而非本行原记的 FUND_ENTRY 事件链路——复刻 B25 AutoPoints 同款范式，AutoGrantJob 5 分钟轮询 txn `/internal/paid-orders`（X-Internal-Token，与自动积分复用同一事件源），auto_grant_state 单行游标日粒度窗口+NULL 首跑全量回填；CONSUME_THRESHOLD 规则 priority 首条命中一单不叠多档、门店限定；**双层幂等**（idem_key `RULE:{ruleId}:{orderNo}` + customer_grant.source_biz_ref 订单级跨规则锚点，防同一订单重复发赠）；txn/客户域故障分层（502 整轮中止不推进游标、客户不存在 400 跳过）；明细 RULE/system + AUTO-GRANT/SYSTEM 汇总双审计。容器真验全量回填扫描 5 发放 4、50000 分边界 `>=` 命中、ST-SH-002 门店限定排除、二轮幂等零新增。附修 audit_log.txn_no 24→128 跨域缺陷。**退款回加赠金不做，见下方 B37 新增行** | ✅ B37 |
| ~~退款回加赠金（消费满额规则的逆向链路）~~ | B37 新增 | **B39 已闭合（2026-09-12，`5353bd9`，详见 DELIVERY-P5-B39）**：用户拍板三决策——①仅链路①付款回加（终审事务内同步逆向回加，远程失败 502 整笔回滚）；②过期券回加但保持 EXPIRED；③部分退款按**赠金→卡本金→法币**级联。marketing 侧 GrantService.refund() 约 145 行：RF 单号幂等（uk_gd_ref_grant 防双加）+ origin_biz_ref 原单锚点累计封顶（同原单多张 RF 合计不得超原扣，超额锁前 422）+ 悲观锁逆向 FIFO + USED 复活/EXPIRED 保态 + saveAll 冲突重查 + GRANT_REFUND(system/operator 双锚) 审计；grant_deduction 增 origin_biz_ref 列（随 B41 V12 纳管）。txn 侧 RefundSplit 三段拆分与 outbox 资金分录共用口径，grant 段**不产生 finance_event**（赠金是营销负债非资金），终审 grant→card 顺序 InOrder 固化，远程全在置态前、失败单据停留 PENDING_FINANCE 可安全重试/驳回。13 单测全绿（marketing 8 + txn 5），容器真验四组：全退 grant600+wxpay1400（券复活、仅 1 条 wxpay 140000 分录）、纯 grant 段退 50000（零资金事件）、RF 重放原样返回不双加+累计封顶 422、营销停服终审 502 四项零写入+恢复后 422 业务拦截。**余 Backlog：链路②满赠门槛追回、卡 gift_balance RF 不回补、B35 grant 渠道正向 DEAD 事件** | ✅ B39 |
| ~~txn-service 扣减侧单测零覆盖~~ | B35 新增 | **B36 卡4 已闭合（2026-09-12，`c0d3583`）**：pom 增 starter-test 建立 txn-service 首个测试工程，PaymentServiceTest 7 例覆盖 grant 散客 400/同单第二笔 409/售卡禁令 400/成功路径 deduct 先于 order_payment 落库（InOrder）/营销域 422·502 透传不记账/grant+现金混合尾款不重扣；幂等重放已由 marketing 域单测+网关实证覆盖。**余：M4FlowController grant-balance 端点「降级 available:false」与 MarketingGrantClient HTTP 层尚无服务内单测**（curl 轨已实证），随后续控制器层测试批补 | ✅ B36（主体） |
| 通知真实运营商网关（短信/企微/邮件） | B26 新增 | 现 dev 网关未配时诚实 SKIPPED、配置 URL+密钥后 POST 联调（冒烟以容器化 mock 取证 200/4xx/拒连三路径）；缺各厂商签名/模板审核/回执与投递状态回写 | 外部集成批（T3） |
| 通知免打扰员工个人级时段 | B26 新增 | B26 实现全局配置版（meiyun.notify.quiet.*）；个人级需 notify_preference 加时段字段 + 前端设置页 | 组织/通知收口批 |
| 标签自动化规则（按消费/肤质/行为/价值/医疗规则自动打标、摘标与规则审计） | ✅ B25 | 标签定义/人工打标已闭环；B25 完成规则引擎+定时扫描+审计（TA### 规则 / TagAutoRuleJob / 5 类条件），详见 DELIVERY-P5-B25 | 客户运营自动化批 |
| 消费自动积分与退款回退 | ✅ B25 | 积分账户/流水已闭环；B25 接入 txn 内部只读端点拉取已收款/已退款订单，按积分规则幂等发分与回退（AUTOPOINTS 幂等键），详见 DELIVERY-P5-B25 | 交易-客户联动批 |
| 等级定时批处理（每月 1 号扫描、执行报告、通知、批处理幂等记录） | ✅ B25 | 自动升级服务与规则已就绪；B25 新增 LevelMonthlyJob（每月1号 cron、autoUpgrade 开关收敛、汇总审计），详见 DELIVERY-P5-B25 | 客户运营自动化批 |
| 自动降级引擎（连续未达标/保护期/客户通知/审批策略） | B23 移交 | 当前只升不降；需计算周期事实表、保护月逻辑、降级审计与 KPI 口径；引擎上线前本月降级 KPI 仅统计手工降级 | 客户运营自动化批 |
| 客户详情会员权益真实联动（discount 实际计价 + benefits 可享权益） | B23 新增 | 等级阈值/权益配置已就绪；需订单计价与权益核销链路接入 | 交易计价收口批 |
| ~~积分/标签/会员等级/积分商城接线~~ | 盘点 | **B23 已闭合（2026-09-09，4 卡网关+PG+浏览器验证，详见 DELIVERY-P5-B23）** | ✅ B23 |
| ~~财务边角页（发票/预算/税/日结/核销预收）接线~~ | 盘点 | **B24 已闭合（2026-09-09，财务四页去 mock + 发票 CSV、卡余额时间线/核销双签明细内部投影切真，详见 DELIVERY-P5-B24）** | ✅ B24 |
| ~~工作台/咨询/医师/EMR 会话区接线~~ | 盘点 | **B24 大部分闭合（2026-09-09，工作台 7 项计数真实、咨询草稿接诊、医师 treat 全链路切真；EMR 独立域仍留 Backlog），详见 DELIVERY-P5-B24** | ✅ B24（EMR 除外） |
| ~~EMR 电子病历独立域（病历书写/模板/审签/归档实体与页面）~~ | B24 移交 | **B27 已闭合（2026-09-10，emr_record 独立域全状态机+修订+signEmr/treatDone 联动+EmrView 浏览器全链路取证，工作台「病历草稿」演示项摘除，详见 DELIVERY-P5-B27）**；余 EMR 列表分页/缺参 400 化/模板库见下方新增行 | ✅ B27 |
| ~~EMR 病历模板库 / 列表分页 / 缺参 400 化~~ | B27 新增 | **B30 已闭合（2026-09-11，emr_template 新域+集团 4 种子只读+本店建/停用+模板库弹层九段范文、EMR 列表真分页默认 20/stats 后端聚合、ChineseValidationAdvice 框架异常中文 400，附修 fc97d00 PG 42P18，详见 DELIVERY-P5-B30）**；集团模板运营管理（上架/版本/审核）留 Backlog | ✅ B30 |
| ~~建档实时写 ES + 客户搜索 DB 融合~~ | B27 新增 | **B30 已闭合（2026-09-11，customer_search_event outbox+10s 中继 Job SENT/RETRY/DEAD+熔断 30s 降级 DB+显式 mapping+新索引全量回填+读时 mergeRecentFromDb 融合+DataScope 过滤，网关事件 SENT/7 次 RETRY 自愈/SELF 隔离 [] 实证，详见 DELIVERY-P5-B30）**；**DEAD 事件处置台 B32 已闭合**（2026-09-11，retry/replay/discard 三动作状态机+/search-events 处置台整页+全量重建入口，customer:search:admin 仅授 REGION_MGR，附修异常处理透传+biz_type 扩宽，`f02384b`/`dbf8158`，详见 DELIVERY-P5-B32）；~~ES reindex 对账治理（ES110 vs PG100 历史漂移 10 文档）~~**已由 B48 卡2 闭合**（2026-09-14，`9c1fd6a`，文档级 diff 对账：missing M001–M003 自动补齐 es110→113 二跑幂等、orphan 100 条仅报告 warn 待人工绝不删除（SC* seed/app 共享索引拓扑预期）+6h 定时巡检；原「漂移 10」系只比 seed 库口径误判，权威源 seed∪core=113，详见 DELIVERY-P5-B48） | ✅ B30/B32 |
| ~~客情登记扩展字段后端化~~ | B27 新增 | **B28 已闭合（2026-09-10，customer 加 9 列落十类扩展字段：年龄/肤质/诉求/过敏史三列/意向项目/意向等级/预算/沟通要点，白名单+过敏互斥，360 档案 tab 有值才显示 kv 回显，详见 DELIVERY-P5-B28）** | ✅ B28 |
| ~~triage 改派时间线~~ | B27 新增 | **B28 已闭合（2026-09-10，triage_reassign 历史表+reassign 同事务追加+读模型批量富化+接待台最小 UI 时间线，详见 DELIVERY-P5-B28）** | ✅ B28 |
| ~~术后随访 SOP 引擎（随访计划编排/超期扫描/回访记录）~~ | B24 移交 | **B30 已闭合引擎（2026-09-11，followup_sop_template/node/batch/followup 四表+treat-done AFTER_COMMIT(REQUIRES_NEW) 幂等排程 1/3/7/30 天节点+FollowupSopDueJob 60s 超期升级店长幂等通知+/api/txn/followup 五端点+工作台两演示项摘除，附修 fa549d5，详见 DELIVERY-P5-B30）**；**B31 已闭合整页与手工建（2026-09-11，/followup 随访工作台整页接真+手工建随访端点+stats 九键+keyword 模糊，/sop SOP 编排整页+模板节点五动作+批次聚合看板+summary 五键+一键升级，FollowupSopEscalator 共享组件，附修 d130dfc 空库 AVG NPE，详见 DELIVERY-P5-B31）** | ✅ B30/B31 |
| 沉睡客户唤醒 / 复诊召回自动化 | B24 移交 | 工作台「复诊待提醒」演示项依赖营销自动化 Flow，仍远期；**其中「复购提醒前端未接」已由 B30 闭合**（RepurchaseView 全链路+三方签核，`7c922cb`，ApprovalService 多级审批见末行） | 远期营销自动化批 |
| ~~接待台 / 候诊接待域~~ | B24 移交 | **B27 已闭合（2026-09-10，到店五态队列+分诊建 CP 草稿+预约/客情自动到店+工作台「候诊待接待」演示项摘除，详见 DELIVERY-P5-B27）**；余排队智能候补/超时自动释放**已由 B29 闭合**（ArrivalAutoReleaseJob 30min+10min 宽限 60s 扫描、系统释放同事务 FIFO 递补，详见 DELIVERY-P5-B29）；到店核销见下条 B28 已闭合 | ✅ B27/B29 |
| ~~到店核销 /m2-checkin~~ | B27 明确 | **B28 已闭合（2026-09-10，checkin_record 独立域 CI 单号池+三方式/三态/四异常码、确认核销/标记异常/解除异常+timeline+KPI/过滤、by-phone 本店→公海→404 锚定、当日同号 PENDING 幂等、手机号掩码出域，网关+浏览器全链路取证 audit 301–313，详见 DELIVERY-P5-B28）**；与预约/writeoff 自动勾连**已由 B29 闭合**（APPOINTMENT 命中当日预约同事务四连写 appt/wd/ci/arrival+核销详情透出勾连单号，audit 314–331，详见 DELIVERY-P5-B29） | ✅ B28/B29 |
| 通用异常中心（跨域异常单据统一归集与处置工作台） | B24 移交 | BOM 异常等现为各域分散后端 only；财务税/日结/异常页 B24 已切真但无统一处置中心 | 平台/交易收口批 |
| 异常账务处置登记（长短款/错账登记与审批闭环） | B24 移交 | 财务台账真实但缺异常账务专门登记审批流 | 财务收口批 |
| 预收合规监控（合规指标/挂账时长监控预警） | B24 移交 | 核销/预收明细 B24 已切真可读，缺合规监控规则与预警 | 财务收口批 |
| 进项税抵扣链路 | B24 移交 | 当前发票/税页仅覆盖销项与台账视角，进项抵扣诚实未实现 | 财务收口批 |
| M4Repurchase 复购方案签核 / 多级审批 | B24 移交 | **单据内客户（预填）/经办/店长三方签核 B30 已闭合**（RepurchaseView 知情同意硬前置+三方签核填齐解禁生效+REPURCHASE/TRIPLE_SIGN 审计，`7c922cb`，详见 DELIVERY-P5-B30）；医师/店长/财务 ApprovalService 多级审批流仍留 Backlog | 诊疗域收口批 |
| seed 端到端造数/清理脚本工具化（幂等） | B24 移交 | B24 手工造数残留（CP…004/005/000006、OD…000003、PM…000004、SC026）已在 DELIVERY-P5-B24 §5.2 登记 | 测试体系批（不紧急） |
| setup-seed-db.sh 全量重建保留 audit_log 选项 | B31 新增 | 脚本约 L674-675 TRUNCATE 清单含 audit_log，B31 全量重建致冒烟审计不存活（终态 36 行全 CONSUMABLE 播种基线），audit append-only 取证口径受影响；需增补「保留 audit」开关（如按 bizType 导出/跳过 TRUNCATE） | 测试体系批（不紧急） |
| SOP 多模板 / 门店级模板 / 节点拖拽排序 | B31 新增 | 现集团通用单模板 SPT-SEED-001（store_code 空），节点 lineNo 按 dayOffset 自动 renumber；门店自建模板、模板上下架/版本管理、手工拖拽排序未做 | 诊疗域后续批 |
| 随访结果结构化分析（满意度趋势 / 不良反应专项处置台） | B31 新增 | stats 已提供 avgSatisfaction/adverseCount 实时计数，趋势分析与不良反应（adverse）专项处置工作台未做 | 数据/诊疗后续批 |
| ~~SST01 幽灵门店码残留清理批~~ | B34 新增 | **B36 卡1 已闭合（2026-09-12，`8ddac05`）**：前端兜底常量改读真实门店表首家（实际路径为 `frontend/src/stores/storeContext.ts`，B34 登记误写 `utils/`，本批一并更正；失实注释同步修正）；`CouponWriteoffDataInitializer` 与 `MarketingAssetDataInitializer` 两处 SST 种子加「仅 seed 库门店体系存在时播种」门控，core 库不再产生幽灵码；`m5Core.ts` mock 值删除。**注：db/01_master.sql 为 seed 库脚本，SST 系门店在 seed 体系内真实存在，不属残留，未动**；历史已写入的 SST 脏数据见下方「存量脏数据」行 | ✅ B36 |
| ~~marketing_cfg GET/POST 字段名不对称~~ | B34 新增 | **B37 卡1 已闭合（2026-09-12，`828ac6d`）**：GET 改独立只读视图 ConfigView（不再直出 JPA 实体），与写命令 ConfigCmd 字段名/类型完全对称——周频键统一 `weeklyPushLimit`（废弃写命令旧 `weeklyLimit`，破坏性改名前后端同卡同步）、渠道字段两端同为 List（前端不再 parse JSON 字符串）、视图剔除 cfgId/referralArrivedReward/referralDealReward/commissionRate 四个多泄遗留列；旧 append-only 审计快照的 weeklyLimit 由前端读时归一；quota `/push/quota` 的 weeklyLimit 是独立前端契约刻意不动。容器 GET/POST/幂等/硬约束/落库/审计新键取证，详见 DELIVERY-P5-B37 | ✅ B37 |
| ~~marketing_cfg 等营销/审计表无 Flyway 迁移脚本~~ | B34 新增 | **B38 已闭合（2026-09-12，`6f2e734`/`2b03c46`，详见 DELIVERY-P5-B38）**：audit/marketing 两服务均接入全库共享 Flyway 版本链（flyway-core 走 BOM，Community 9.22.3；yml 与 customer/finance 同款 enabled/baseline-on-migrate/baseline-version 0/validate-on-migrate/ignore *:missing+*:future，audit 额外 out-of-order:true 共享链补洞）；V10 以 ALTER TABLE IF EXISTS 补登 audit_log 两次历史手动 ALTER（biz_type 16→32、txn_no 24→128），V11 以 CREATE TABLE IF NOT EXISTS 纳管 marketing_cfg 全 15 列+主键与 auto_grant_state 全 5 列；共享链实测 V0–V11 共 12 行全 success。**余：marketing-service 其余 16 张 JPA 属表仍靠 ddl-auto=update，见下方 B38 新增行** | ✅ B38 |
| ~~audit_log 基线 schema 与实体/现网的历史漂移~~ | B37 新增 | **B38 卡1 已闭合（2026-09-12，`6f2e734`）**：V10 迁移以 `ALTER TABLE IF EXISTS audit_log ALTER COLUMN biz_type TYPE VARCHAR(32)` 补登 B32 历史手动 ALTER（现网重设同宽 no-op，全新库表不存在时 NOTICE skipping 留 Hibernate 按实体 length=32 建）；gitignored 基线 db/schema.sql 的 biz_type VARCHAR(16) 同步校准为 VARCHAR(32)（db/ 不入库），漂移消解 | ✅ B38 |
| ~~marketing-service 其余 16 张 JPA 属表分批纳管 Flyway~~ | B38 新增 | **B41 已闭合（2026-09-12，`3166d97`，详见 DELIVERY-P5-B41）**：V12 券/赠金域 8 表（coupon_template、coupon_grant、campaign、coupon_writeoff_record、coupon_writeoff_chain、customer_grant、grant_rule、grant_deduction）、V13 渠道/素材/违禁词 3 表（channel_returnback、marketing_asset、forbidden_word）、V14 内容/触达 5 表（poster_template、poster_record、short_video、live_session、push_record，含 push_record→customer FK），全部 pg_dump 现网物理结构 1:1 转写、CREATE TABLE/INDEX IF NOT EXISTS 基线（不对存量表 ALTER）；现网重放全 NOTICE skipping、16 表 count 前后零变更，共享链 V0–V14 共 15 行全 success；scratch 全新库裸回放 V1→V14 全链通过（push_record 依赖的 customer 表由 customer-service Hibernate 建，compose 已固化 depends_on healthy 时序）。marketing 全 18 张属表达成 100% Flyway 纳管 | ✅ B41 |
| ~~marketing-service ddl-auto 由 update 改 validate 的评估~~ | B41 新增 | 18 张属表全部纳管后，ddl-auto=update 仍在跑（基线迁移只建结构不 ALTER，历史缺列仍靠 update 净兜底）；后续需逐表核对 Flyway 基线与 JPA 实体完全一致后，把 marketing-service 的 ddl-auto 切到 validate 并做一次全新库+现网双场景验证，彻底关闭「JPA 自动改表」口子。**B48 卡3 已闭合（2026-09-14，`188c90b`）**：逐表核对基线与实体后切 validate，现网+scratch 全新库双场景零 drift 一次通过，详见 DELIVERY-P5-B48 | ✅ B48 |
| 存量脏数据核销流水/营销资产/门店主数据 SST 处置（**B40 全库扫描闭环，B36 实测更正**） | B34 新增/B36 更正/B40 处置 | **B40（2026-09-12，`5ef75db`，详见 DELIVERY-P5-B40）全库字符/JSON 列扫描定性污染 11 表 175 行+audit_log 36 行快照**：①门店运营域 8 表 162 行幽灵主数据（consumable 36/consumable_stock 36/consumable_movement 35/equipment 8/equipment_maintenance 11/treatment_room 9/treatment_bed 16/store_price 11，2026-09-06 19:11 system 播种、零外键零预约零订单引用）已 CSV 8 份+全表 pg_dump 留档（scripts/backup-b40-sst-20260912/，gitignored）后单事务**物理删除**，audit_log 36 行 CONSUMABLE 建档快照 append-only 保留；根因 3 个 store-service 播种器无门控已修并经重建镜像 force-recreate 实证零重播。②`marketing_asset` 2 条（AS-SEED-002 投 SST01/02、AS-SEED-009 投 SST03/04；**B36 仅登 SST01 本批更正为 SST01~04**）保留资产本体，UPDATE scope=ALL、store_codes=[]，原值 JSONL 留档。③**核销流水 11 条全部保留不擅改**：8 条 0 元（WR-SEED-0003/0004 + WR20260908-000001/002/004~007，FORGED/DUPLICATE）为风控证据不动；**3 条 OK 共 114000 分=1140 元（WR20260908-000003 55200、WR-SEED-0001 50000、WR-SEED-0002 8800）store_code=SST01 挂账，需业务指定真实归属门店后定向回填**。**B36 勘误**：WR20260908 前缀实 8 条——000001~000007 共 7 脏（B36 登 7 条正确但 B34 曾误登 4 条），000008 为 ST-SH-001 干净复验记录不属脏数据 | 1140 元 3 单待业务确认门店；余闭环 |
| ~~StoreNameResolver 不可达与不存在不可区分~~ | B34 新增 | **B36 卡3 已闭合（2026-09-12，`a43d299`）**：新增 StoreServiceUnavailableException，拆为 `resolveNames`（宽松：故障降级空 Map，仅供券名回显/「不可用即放行」）与 `resolveNamesRequired`（严格：连接/超时/4xx/5xx 抛专用异常→写链路 **503**；name-map 200 子集缺码=真不存在仍 **400**），fetchNames 唯一取数出口；MarketingCfgService 兜底门店保存改严格版，store-service 宕机不再误报 400。真实容器三分支取证（假码 400/真码 200/停服 503）+93 营销单测全绿 | ✅ B36 |
| ~~`/internal/first` 首家门店排序口径未文档化~~ | B34 新增 | **B36 卡2 已闭合（2026-09-12，`3694003`）**：StoreRepository 增 `findFirstByOrderByStoreCodeAsc`，契约明确为 **store_code 字典序升序首家**（不再依赖无 ORDER BY 的 JPA 默认），空库 404/空；StoreControllerTest 断言多门店固定首家、空库、X-Internal-Token 鉴权，javadoc 写明排序口径 | ✅ B36 |
| BOM 缺料明细 → 申购单联动 | B34 新增 | B34 已把缺料明细（skuCode/needQty/stockQty）结构化贯通到页面，具备一键生成申购单的数据基础；申购域属 M2 门店运营平台，待该阶段一并实现 | 远期 M2 |
| M2 门店运营平台（13 页） | 远期阶段 | 排班/工单/日结/申购/报损/绩效等 | 远期 M2 |
| M3 客户运营平台（10 页） | 远期阶段 | 分群/旅程/自动化 | 远期 M3 |
| M1 集团管控（14 页） | 远期阶段 | 集团-门店多租户 | 远期 M1 |
| ~~A1 AI 中心模型配置地基（模型接入/网关监控/管理配置/审批治理 4 页）~~ | B42 新增 | **B42 已闭合（2026-09-12，`7ea662d`，详见 DELIVERY-P5-B42）**：ai-service 第 8 服务+V15 七表+/api/ai 网关前缀+火山方舟 OpenAI 兼容薄适配（LlmClient RestClient）+供应商/模型 CRUD 与 AES-GCM Key 落库+真实连通性测试+六功能绑定/角色灰度矩阵+调用日志/KPI/账单+全局配置+审批分页决策四页真实闭环。真实 ARK Key 由用户在 /ai/providers 录入后 SUCCESS 复核（不索要明文） | ✅ B42 |
| ~~B43 真实功能 invoke 闭环~~ | B42 新增 | **B43 已闭合（2026-09-12，详见 DELIVERY-P5-B43）**：POST /api/ai/features/{code}/invoke 六功能角色矩阵鉴权+门店域+模型 enabled/CHAT 校验+参数覆盖+LlmClient.chat 真实火山方舟 ark-code-latest 调用，成败均沉淀 ai_invoke_log（V16 error_code 扩 512、去方法级事务保失败日志独立提交）；网关页 KPI/API 目录/调用日志与管理页试运行抽屉全部真实联动；curl 全状态码矩阵+DB+Chrome 三轨真验（497 tokens/13013ms 真实中文画像）。四项运行态修复：error_code 列宽、失败日志事务、updated_at 显式刷新、storeScopeText 展示映射污染 | ✅ B43 |
| ~~B44 配额限流与安全告警~~ | B42 新增 | **B44 已闭合（2026-09-12，`336b7f8`，详见 DELIVERY-P5-B44）**：V17 三表（ai_quota/ai_sensitive_word/ai_alert_rule）+种子（13 敏感词/5 告警规则）；配额只存额度不设计数列，用量以 append-only ai_invoke_log 按北京时间日/月窗实时 count，出站前 **FEATURE→MODEL→GLOBAL 三级校验**超限 **429** 中文、被拦截不落日志不占量；敏感词总开关+BANNED 违禁/INJECTION 越权（prompt injection）小写 contains 命中 **400** 且命中词首尾脱敏回显；告警五规则（P99/错误率/调用量/成功率/配额水位）无定时任务、请求时实时评估，activeAlerts 联动网关 KPI；A1Admin 新增第 6 个「配额管理」tab（8 行三级配额+水位三档色+调整抽屉），A1Gateway 告警 tab 真实化。curl（400/429 脱敏与用量对账/真实 2097 tokens 54736ms）+PG+Chrome 三轨真验 | ✅ B44 |
| ~~B45 审批申请与效果评估实体化~~ | B42 新增 | **B45 已闭合（2026-09-13，详见 DELIVERY-P5-B45）**：POST /api/ai/approvals apply 申请端点（类型白名单/目标存在且未启用/同目标 PENDING 幂等 400 中文，aiAdmin:edit 与决策 aiGovern:approve 权限分离）+AiApplyDrawer 前端申请 UI（模型上架/功能绑定两类申请单），审批通过后端联动 MODEL/BINDING enabled=t（linkage 审计）；V18 ai_eval_task/ai_experiment 两表三 CHECK，EvalService 八端点按 append-only ai_invoke_log 北京时间窗口聚合调用量/成功率/P99/token/成本，lift pp 与已结案均值，评估/A·B 实验两 tab 从占位实体化（新建/刷新/prompt 录结论结案冻结）。真验修复 KPI kpis 一次性快照不刷新的 Vue 响应式缺陷（baseKpi ref+kpis computed），curl+PG+Chrome 三轨证据齐全 | ✅ B45 |
| ~~B46 卡1 敏感词词库管理+命中落库+敏感词检测页去 mock~~ | B44 新增（敏感词二期纵深） | **B46 卡1 已闭合（2026-09-13，`d944a01`，详见 DELIVERY-P5-B46）**：V19 新建 ai_sensitive_hit 命中表 14 列（CHECK category+三业务索引）+ai_sensitive_word 补 updated_at、ai_set_updated_at() 为 8 张 AI 表挂 UPDATE 触发器；SensitiveWordController 六端点（词库全量 List/新建/编辑重复词 400、命中真分页+词类三态过滤+北京时间 stats/误报标注，changed=false 与重复标注幂等不写审计，全动作审计 payload 合法 JSON）；FeatureInvokeService L142 出站拦截链接入（Key 解密后/配额前，BANNED/INJECTION 400 中文脱敏，SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库、snippet 截断 500）；前端 utils/datetime.ts 时区统一工具（AI 中心 4 文件 7 点位先落地），A1SensitiveView 整页去 mock。curl+PG+Chrome 三轨真验全绿 | ✅ B46 |
| ~~B46 卡2 A1HomeView 智能中心首页去 mock~~ | B42/B46 更新 | **B46 卡2 已闭合（2026-09-13，`8bee59d`，详见 DELIVERY-P5-B46-2）**：纯前端切真无新端点，四 KPI/12 能力卡调用量/待办告警按 aiGateway:view、aiAdmin:view、aiGovern:view 三权限分组门控全部接真（无权限不发请求 KPI 显「—」杜绝越权 403），待审批数取审批分页 totalElements、能力卡按 monthlyBill 诚实显隐、待办流 fmtAgo 相对时间+空态，datetime.ts 补 fmtAgo；curl 五端点+PG 八项+Chrome（9 XHR 全 200、console 零报错）三轨真验全绿 | ✅ B46 |
| ~~B46 卡3 A1ContentView 渠道内容生成去 mock~~ | B42/B46 更新 | **B46 卡3 已闭合（2026-09-13，`609ca2f`，详见 DELIVERY-P5-B46-3）**：V20 ai_content_record 17 列（双 CHECK+三业务索引+触发器，DDL-only），ContentController 四端点（三渠道生成/真分页/北京日界六指标/幂等下发审计），generate 复用 featureInvokeService.invoke 全治理链+InvokeView.logId，长文案读超时 60s→180s 三处根治（yml/LlmClient cause 链/axios），createdAt findById 回读；curl（sms 83.7s/3410、wechat 136.7s/4854、敏感词 400 脱敏）+PG（5 行/采纳率 40%）+Chrome（KPI 20%→40% 联动、console 零报错）三轨真验全绿 | ✅ B46 |
| ~~B46 卡4 A1ScriptsView 话术库去 mock~~ | B42/B46 更新 | **B46 卡4 已闭合（2026-09-13，`46cacc6`，详见 DELIVERY-P5-B46-4）**：V21 ai_script 15 列（scene icebreak/followup/objection+source MANUAL/AI 双 CHECK、三业务索引、updated_at 触发器，DDL-only 无 seed），ScriptController 七端点（库真分页+场景/关键词过滤、北京日界四指标 stats、generate、新增/编辑、adopt、feedback），三场景差异化系统指令真实调用 ark-code-latest，**generate 只出站不落库、保存带 invokeLogId 才 source=AI** 关联 ai_invoke_log，复用 featureInvokeService.invoke 全治理链（角色/灰度/敏感词脱敏/配额/计费/日志），保存前敏感词兜底；采纳计数/好评差评滚动均分，AI_SCRIPT CREATE/UPDATE/ADOPT/FEEDBACK 全动作审计；create/update 无方法级事务+findById 回读 DB 默认时间戳。真验修复两缺陷：JPQL null 参数被 PG 推断 bytea 致 lower() 500（空串归一化+`:param = ''` 守卫）、新建话术 createdAt=null（一级缓存坑）；curl（invokeLogId=21/1615 tokens、第二轮 198 字、负例 400/404）+PG（3 行/7 审计/stats 3·2·33%·100%）+Chrome（轮询 170s、KPI 联动、console 零报错）三轨真验全绿 | ✅ B46 |
| ~~B47 卡1 A1ProfileView 客户画像引擎去 mock~~ | B42/B47 更新 | **B47 卡1 已闭合（2026-09-13，`c116096`，详见 DELIVERY-P5-B47-1）**：V22 新建 ai_customer_profile 表（客户价值分/所属分群/画像标签/应用分群标记/invoke 关联/原始 JSON，CHECK+业务索引+updated_at 触发器，DDL-only 无 seed），ProfileController 八端点（客户候选搜索、真实生成、最近画像、历史列表、stats 四 KPI、特征权重 SHAP、近 4 周效果回看、应用到分群），客户候选/档案上下文/消费指标**跨域**经 customer-service `/internal/profile-context*` 内部端点（X-Internal-Token 常量时间比对）富化、远程失败统一降级空态不阻断画像出站，generate 为 feature_code=**profile** 第七个注册功能复用 featureInvokeService.invoke 全治理链（角色/门店灰度/敏感词脱敏/配额/计费/ai_invoke_log），apply 幂等置位 applied_to_segment+APPLY_SEGMENT 审计。真验修复两真实缺陷：**RestTemplate 中文 query 按 ISO-8859-1 编码致客户名搜索静默 200 返空**（UriComponentsBuilder 显式 `.encode(UTF_8).build().toUri()` + exchange(URI) 重载，分层隔离定位缺陷纯在 ai→customer 一跳）、**效果回看周聚合只有下界变累计计数**（新增 `aggregateBetween` 含下界不含上界按周边界分桶）。curl（张女士/张/手机号中文搜索、真实 ark-code-latest 13.9s/683 tokens、latest 两态、apply changed:true/false 幂等、review W4 71.43%）+PG（画像 1 行/invoke_log profile 7 条 5 成 2 败/审计哈希链 GENERATE+APPLY_SEGMENT）+Chrome（真实 KPI、画像输出候选下拉详情、特征权重 8 行 SHAP 合计 100%、效果回看 W1~W3 空态 W4 71.4%）三轨真验全绿 | ✅ B47 |
| ~~B47 卡2 A1RepurchaseView 复购预测引擎去 mock~~ | B42/B47 更新 | **B47 卡2 已闭合（2026-09-13，`fc9f508`，详见 DELIVERY-P5-B47-2）**：V23 新建 ai_repurchase_prediction 表 27 列（period week/month/quarter、project_code skin/inject/anti/body/other 双 CHECK、prob 0~100 CHECK、四索引含 followup 部分索引+updated_at 触发器，DDL-only 无 seed、bindingId=7 运行时录入），RepurchaseController 八端点（/run 批次推理、/list、/batch、/stats、/factors、/{id}/followup、/{id}/push、/batch-followup，校验/幂等/限流配额/审计四件套），复购候选与近期成交信号 ai→txn、卡余额 ai→customer 双跨域 X-Internal-Token 富化失败降级不阻断 AI 出站，run 为 feature_code=repurchase 第八功能复用 invoke 全治理链逐客户串行真实 ark-code-latest 推理，无批次中文 404 转引导空态、因子不可用 available=false 诚实置灰；curl+PG（RP2026091301/4 人/约4分09秒/均值 25%/预计客单价 ¥1,241、invoke 25-28 tokens 6840/1566/1497/917 cost_fen=0、审计 500 RUN_PREDICTION/501/502/503 BATCH affected=3、幂等重放零审计）+Chrome（月/季引导空态、console 零 error/warn）三轨真验全绿 | ✅ B47 |
| ~~B47 卡3 A1ChurnView 流失预警引擎去 mock~~ | B42/B47 更新 | **B47 卡3 已闭合（2026-09-13，`4e05215`，详见 DELIVERY-P5-B47-3）**：V24 新建 ai_churn_prediction 表 26 列（batch_no CH+yyyyMMdd+两位序号、risk_score/risk_level 双 CHECK、四索引含 intervene 部分索引+updated_at 触发器，DDL-only 无 seed），ChurnController 七端点（/run 无 period 最近一批即当前榜、/list?riskLevel、/batch、/stats、/factors、/{id}/intervene、/batch-intervene，校验/幂等/限流配额/审计四件套），流失候选与近期消费信号 ai→txn 单跨域 X-Internal-Token 富化（SCAN_LIMIT=3000、recency 倒序）失败降级 List.of() 不阻断出站，run 为 feature_code=churn 第九功能复用 invoke 全治理链逐客户串行真实 ark-code-latest 评分（invoke 超时 180000ms）、风险阈值服务端重算 ≥85 high/≥60 mid 不信模型自报等级，客诉/互动两因子 available=false 诚实置灰；真验修复一前端真实缺陷——风险等级列 key='level' 撞会员等级字段致三行全显低风险（改 #col-level 作用域插槽取 row.riskLevel）；curl+PG（CH2026091301/3 人全 mid/均 60 分/约53秒、invoke 30 benchmark 404 失败 append-only 留档+31-33 成功 762/856/1220 tokens、12678/16270/24224ms、cost_fen=0、UPDATE model_id=1 修正绑定、审计 505 RUN_SCORING/506 REGISTER_INTERVENE/507 BATCH affected=3、幂等重放零审计）+Chrome（中风险标签正确、console 零 error/warn）三轨真验全绿 | ✅ B47 |
| ~~B47 卡4 A1DailyView AI 经营日报去 mock~~ | B42/B47 更新 | **B47 卡4 已闭合（2026-09-13，`1282769`，详见 DELIVERY-P5-B47-4）**：V25 新建 ai_daily_report/ai_daily_suggestion/ai_daily_subscription 三表（建议类别 CHECK core·anomaly·action、subscription staff_id PK 四渠道+updated_at 触发器，DDL-only 无 seed），DailyController 九端点（当日/重发生成、history 按日期归并最新版、建议列表/逐条采纳幂等、订阅四渠道读写），经营指标+前一日基线 ai→txn /api/txn/internal/daily-metrics（X-Internal-Token internal:finance-flow）远程失败 available=false 降级不阻断出站，generate 为 feature_code=daily 第十功能（bindingId=8 运行时录入，角色仅 SUPER_ADMIN/REGION_MGR）复用 invoke 全治理链真实生成三段十条建议，同日重发多版本留存；真验修复两缺陷——nginx 默认 60s 致 86.9s 生成 504（正则 location `~ ^/api/ai/(daily/generate\|churn/run)$` 180s 四跳对齐 axios 180000/Go 网关不截断/LLM 读超时 180000）、JPA timestamptz UTC 差 8 小时（DailyService.bjTime()）；curl+PG（报告 v1/v2 两版本、建议 1-10 中 id=2/9 由 E011 采纳、订阅四渠道登记、invoke 34 为 86.9s/4523 tokens、35 为 118.0s/5801 tokens、cost_fen=0、审计 508 GENERATE_REPORT/509-510 ADOPT/511-514 SUBSCRIPTION、幂等重放零审计）+Chrome（14 请求全 200、唯一 404 为设计内当日空态探测、console 零 error）三轨真验全绿 | ✅ B47 |
| A1 AI 中心其余页（Prompt 编排/知识库/Embedding/向量/Agent/效果实验等） | B42 更新（原「A1 AI 中心 15 页」行）/B46/B47 更新 | B42 交付 4/15、B46 卡1 敏感词检测页、卡2 智能中心首页、卡3 渠道内容生成、卡4 话术库、**B47 卡1 客户画像引擎、卡2 复购预测引擎、卡3 流失预警引擎、卡4 AI 经营日报、卡5 AI 客服、卡6 智能排班、卡7 AI 知识库**（`8ac9372`，V28 两表十端点头法加权非向量/引用溯源/反馈，详见 DELIVERY-P5-B47-7）、**卡8 隐私合规**（`a86a643`，V29 脱敏规则/等保项/导出报告三表、7 端点 aiPrivacy:view 类级+aiPrivacy:edit 三写读写分离超管/区域经理可写店长只读、四 KPI 8/88%/2/6 与审计区间 SHA-256 全链哈希导出 canonical 北京时区/366 天/空区间只哈希头、审计 AI_PRIVACY 545–550 共 6 条哈希链 544→550 全 OK、A1PrivacyView 三 tab 只读降级，详见 DELIVERY-P5-B47-8）闭合后 **15/15 页全部真实闭环**（历史口径余 0 页；实测 nav 中 mock A1 视图 12→9→8→7→6→5→4→3→2→1→**0** 个（B47 卡8 隐私合规切真，A1 全去 mock），视图/页面粒度口径差见 DELIVERY-P5-B46 §7）：扩展 LlmClient 支持 EMBEDDING/VISION 等多能力与多供应商（非 OpenAI 兼容协议适配）；token 计费核算与账单环比闭环；A1Gateway 告警规则写端点小补丁；~~全站时区工具推广（AI 中心外余约 21 处 slice）~~（**B48 卡4 已闭合**（2026-09-14，`14cdd8f`）：实际推广 **56 处** slice 统一锚定 Asia/Shanghai，登记口径 21 处系低估本批更正；max_tokens 遵从度检测同卡闭合，详见 DELIVERY-P5-B48）；**经营日报定时生成 Job 与订阅四渠道真实触达（站内信/邮件/短信/企微 M5-03·M2-17）远期**，当前订阅仅登记；**内容生成结果真实跨渠道推送（微信公众号/短信网关接入）M5 远期**，当前 deploy 仅登记下发；**话术 adopt 真实推送至 M4 工作台/企业微信会话侧边栏 M5 远期**，当前仅登记采纳计数；话术 rating 使用后回访入口（当前仅库内即时好评/差评）；**画像分群/标签跨域自动回推客户域 M3 远期**，当前 apply 仅 ai 侧登记；**标签准确率回流评估体系**（当前效果回看为调用成功率口径，业务标签准确率待评估体系建设）远期 | B46 起分批 |
| T4 AI 算力（4 页） | 远期阶段 | GPU/算力资源调度与监控，独立于 A1 应用层 | 远期 T4 |
| T2 数据分析（4 页）/ T3 外部集成 | 远期阶段 | 数据仓库 / 开放 API | 远期 T2/T3 |
| C 端移动端（20 页） | 远期阶段 | mp-uniapp，2 页已真实 | 远期移动端 |
| ~~append-only 审计「业务成功但审计静默丢失」监测/告警~~ | B47 卡7 新增（观察项，详见 DELIVERY-P5-B47-7 §6/§8） | AI_KNOWLEDGE 审计 525–544 共 20 行哈希链连续，但 544 REINDEX 对应的 KB-DOC-7（「FAILED重试验证条目」，并发实例创建）**无对应 CREATE 审计行**——推测 RestAuditRecorder 远程记录失败静默不阻断主流程的偶发（REINDEX 544 正常落链，排除审计服务整体不可用）；需补审计写失败补偿/对账监测。**B48 卡1 已闭合（2026-09-14，`4bfb658`）**：审计写失败补偿（outbox+中继）+对账监测落地，RestAuditRecorder 远程失败不再静默丢失；KB-DOC-7 幻影审计观察项复核登记（历史断链 id=380 append-only 不可篡改存量保留），详见 DELIVERY-P5-B48 | ✅ B48 |
| ~~审计哈希链全量断链清单返回（verifyChain 目前只报首处断链）~~ | B49 卡2 新增（观察项，详见 DELIVERY-P5-B49 §3 卡2） | **B50 卡6 已闭合（2026-09-15，`aecf97f`，5 files +201/-8，详见 DELIVERY-P5-B50）**：AuditService.verifyChain 由「首处失配即返回」改为遍历全程收集全部失配节点，新增 ChainBreak 明细（id/期望前驱/存储前驱/curHash/action/actor/createdAt）与 ChainVerifyResult.breaks 数组；关键口径——失配后 expectedPrev 始终沿**存储的 node.curHash** 推进而非重算值，才能独立检出每一处历史断链、不把断点后正常节点误报；brokenAtId 首处口径保留旧契约兼容，append-only 存量不回改。AuditServiceVerifyChainTest 三用例（篡改+重链同型、断链在下游暴露）；M1AuditLogView 断链清单卡。三轨真验：prod breaks=[380,520]/569 两处历史断链全量暴露（均早于 B48 `4bfb658` 部署窗口，修复后无新增），seed breaks=[]/117 | ✅ B50 |
| 隐私合规延展：个人数据请求工单流（DSAR）/授权同意（consent）管理/合规口径定期巡检/导出报告哈希验真 UI | B47 卡8 新增（详见 DELIVERY-P5-B47-8 §8）/ **B50 卡8d 复核登记（2026-09-15，本批不动工）** | 当前 /ai/privacy 覆盖脱敏字段规则配置（8 类）、等保 2.0 达标台账（8 项）、审计区间 SHA-256 全链哈希导出三 tab。**B50 拆为两类登记**：① 导出报告哈希验真 UI＝**中项**（report_job 加 content_hash 列+生成端 SHA-256 落库+上传/录入 hash 验真端点+前端验真卡；关键坑——CSV 带 BOM、时间戳列随生成时刻变化，同参数重导字节不同，须先定义规范化字节口径再哈希，否则验真永远失败）；② DSAR 个人数据查阅/复制/删除工单流、consent 授权同意生命周期、等保/脱敏口径定期巡检与差异告警＝**合规远期大项**，独立批次不并入验真 UI | 远期（合规专项） |
| ~~无 GROUP 集团全域账号（集团全量域真验缺口）~~ | B49 卡4 新增（详见 DELIVERY-P5-B49 §3 卡4） | **B50 卡8b 已闭合（2026-09-15，零代码，详见 DELIVERY-P5-B50）**：走本行原文第二路径「**明确 SUPER_ADMIN 全量域口径**」——PG 双库核实既有播种账号 **SE101 周岚**（RbacDataInitializer.EXTRA_STAFF）在 meiyun_core/meiyun_seed 均为 SUPER_ADMIN/store_code=null/region=null/在职，登录实测 scope=GROUP、stores=[]、perms=*（AuthController ROLE_SCOPE SUPER_ADMIN→GROUP；resolveVisibleStores 对 GROUP/BRAND 返空列表，DataScope 遇空＝全量）。不新增 SE106：既有账号已合格，增账号既冗余又扩大弱口令面。/finance/group-overview（DataScope.storeSpec 基座）三口径真验对齐 PG 基线：seed SE101 全域 revenue_monthly 7 行/4 店（SST01/02/03/06，2026-08 四店 ¥1,259,000,000）vs E011 华东 6 行/3 店（SST01/02/06，8 月 ¥260,000,000，差额恰为越界 SST03 的 ¥999,000,000）；prod SE101 7 行/5 店 vs E011 2 行/仅 ST-SH-001；双栈匿名 401。另 E014 蒋IT 同为超管可备 | ✅ B50 |
| 新客/复购/治疗人次/满意度/活跃客户/利用率/mom/yoy 等月度指标无数据源 | B49 卡4 新增 | 集团屏三页该等指标当前 null 显「—」不伪造；依赖 txn/org 域月度聚合投影（事实表或物化视图）落地后方可接真，属月度聚合专项 | 数据仓库批（T2） |
| revenue_monthly 覆盖不足（仅 7 行 5 店、无 2026-08） | B49 卡4 新增 | 月报行依赖月结流程（P3-B11）产出，当前仅 2026-07 五店+2026-09 两店；默认期「已出月报门店数最多·并列取最新，随数据域而定」已兜底正确，但覆盖率不足致集团趋势/对标样本偏薄，随月结流程推广自然消解 | 随月结流程推广 |
| ~~M1 采购 /m1-procurement 组合域列表按请求头上下文过滤、未强制校验门店归属（越权访问观察项）~~ | B49 卡5 三轨真验（权限三角断言） | **B50 卡2 已闭合（2026-09-15，`da3d812`，3 files +116/-14，详见 DELIVERY-P5-B50）**：PurchaseOrderRepository 增 JpaSpecificationExecutor，删除 `cast(null as string) is null` 退化为全量的 search JPQL（REGION 多店无参时 Controller 传 null→无门店过滤→全量泄露 SST03 等非本区单）；PurchaseOrderService.listPurchaseOrders 改以 DataScope.storeSpec("storeCode") 为不可绕过查询基座，显式 storeCode/status 作叠加谓词、Sort id desc，"__NONE__" 哨兵短路空列表；Controller 显式参守卫保留、Specification 层纵深防御兜底；PurchaseOrderServiceTest 三契约用例（哨兵短路不触库/findAll(spec,sort)/不退回无域 findAll）。三轨真验（seed 18443，E011=REGION 华东 SST01/02/06）：无参 6→4（SST03 两单消失，仅 SST01×2/SST02×2）、显式越界 SST03 交空、本域 SST01=2、status/组合谓词正确、id desc、无 token 401；PG 域内 count=4 对账一致；双栈 docker cp 部署 healthy | ✅ B50 |
| ai-service pg-ai seed 库 Flyway V17-V29 脚本悬置（未纳入部署链路，表结构已在镜像内建） | B49 批前侦察 | seed 库版本化时对齐共享版本链 | 后续批次（seed 库版本化专项） |
| ~~M1 数据大屏成交流时间显示 UTC 原文未转 Asia/Shanghai~~ | B49 卡7 三轨真验（Chrome 轨观察项，详见 DELIVERY-P5-B49 §卡7） | **B50 卡3 已闭合（2026-09-15，`cbd6692`，2 files +9/-2，详见 DELIVERY-P5-B50）**：utils/datetime 新增 shTimeStr（复用既有 SHANGHAI_TIME_FMT），shDateTimeStr 同步复用；m1Screen order-paid 时间原直接 slice(11,16) 截 UTC ISO，现转北京时区，非法/空值兜底「—」。三轨真验：造 now() 新支付→SSE 真实扇出 paidAt=05:26:00Z→大屏 13:26/13:28 与 PG `AT TIME ZONE` 零误差 | ✅ B50 |
| M1 目标管理 REVENUE 实际值自动聚合 RevenueMonthly（首卡收窄手工维护） | B49 卡8 新增（详见 DELIVERY-P5-B49 §卡8） | target 域 progress 当前经 PUT /{id}/progress 手工维护；REVENUE 类目标实际值可由 revenue_monthly 月报按 ownerType 三级自动聚合回写，依赖月报覆盖率（当前仅 7 行 5 店）与聚合投影落地 | 后续批次（目标域增强） |
| M1 目标管理 REJECTED 终态不可再提交 | B49 卡8 新增（详见 DELIVERY-P5-B49 §卡8） | 状态机当前 REJECTED 为终态（submit 仅 DRAFT→PENDING）；业务上驳回后通常允许修改重报，需 REJECTED→DRAFT 回转或允许直接再提交，涉状态机扩展与审计口径 | 后续批次（目标域增强） |
| ~~target 列表行级 DataScope 未收窄（有 target:view 即可读全量）~~ | B49 卡8 三轨真验（权限三角断言） | **B50 卡4 已闭合（2026-09-15，`3a263ab`，6 files +206/-5，详见 DELIVERY-P5-B50）**：DataScope 新增 canReadTarget——GROUP 行各域共享；REGION 行按 JWT 中文 region 对 ownerName contains 匹配；STORE 行复用 canReadStore；super/GROUP/BRAND/匿名既有口径放行；BizTargetService.list 循环首行叠加 canReadTarget 行级过滤（ownerType 等可选过滤仍生效）；种子 STORE 归属归一真实门店码（T01 杭州西湖旗舰院→SST06、T02 上海静安分院→SST01）；BizTargetServiceTest 四角契约（REGION 华东/华北、STORE、超管/匿名、ownerType 叠加）共 5 用例全绿；M1TargetView 树渲染过滤数据域外悬空 children 消除空白 0% 占位行。三轨真验：E011 华东 7 行/G1-E+SST06+SST01、SE102 华北 5 行/G1-N、SE001 SST01 5 行/T02-R、SE101 10 行全量；Chrome E011 页零空白 | ✅ B50 |
| M1 合规 impersonate 真实身份切换（org-service 会话置换） | B49 卡9 新增（详见 DELIVERY-P5-B49 §卡9）/ **B50 卡8d 复核登记（2026-09-15，本批不动工，远期独立大项）** | 卡9 收窄为仅审计留痕真实化（start/end 经 POST /api/audit append 真实落库），activeSession 保持前端内存单会话。**B50 评估为大项**：JWT 自包含被 7+ 服务本地验签（无会话中心），真实切换需——① JWT 增 act/realSub claim（操作者/被切换人）并在 meiyun-security 全服务解析回归；② 全服务审计 actor 口径全局统一（卡8c 已先行闭合 audit append 双通道 actor 收敛，`43f009b`，本项落地时可直接复用通道语义）；③ org-service 签发切换态 token 与回收/超时；④ 前端 auth store 换 token 与切换态横幅；与「超管 Impersonate」平台批同项合并拍板 | 远期（平台/安全专项大项） |
| ~~审计真实客户端 IP 采集（网关注入 X-Real-IP→audit append 链路）~~ | B49 卡9 新增 | **B50 卡7 已闭合（2026-09-15，`5251306`，7 files +364/-7，详见 DELIVERY-P5-B50）**：Go 网关 router.go 增 clientIPFromRequest/remoteHost+Director 规范化（X-Real-IP=解析客户端、XFF 追加直接对端 peer，直连采 RemoteAddr），router_test.go 9 用例；meiyun-security 新增 ClientIp（XFF 首段→X-Real-IP→remoteAddr→"unknown"）+ClientIpTest 6 用例（JDK 动态代理 fake，零新增依赖）；ComplianceController 注入 HttpServletRequest、ComplianceService.recheck 加 clientIp 参去硬编码 "web"，ComplianceServiceRecheckIpTest 2 用例，共 20 用例全绿。双栈部署：交叉编译重建 meiyun/gateway:latest 双栈 recreate、audit 胖包 docker cp 双栈重启均 healthy。三轨真验：curl 四场景（经 nginx/直连伪造 XFF 203.0.113.77/无头兜底，seed id180-182、prod id588-589）、PG payload.ip 全部真实非 web、Chrome seed id183 actor=冯区域 ip=192.168.65.1，审计页渲染 121 条「哈希链完整」；新行均正常延链。**prod 留临时验真数据（append-only 不回改）：meiyun_core.compliance_check 手插 id=1（PRIVACY「L134 平台治理验真临时项」，现 FAIL）、审计 id588/589** | ✅ B50 |
| M1 报表 R03-R09 七模板数据源待建（现 422 收窄，前端常驻 gen--note 提示块） | P5-B49 卡11 | 各域业务表/聚合服务就绪后逐一接真 | 远期 M1 |
| M1 报表 XLSX·PDF 导出格式（首卡 CSV-only，ReportCsvBuilder 单格式） | P5-B49 卡11 | 生成引擎（POI/PDF）选型评估 | 远期 M1 |
| M1 报表 R02 区域+项目品类维度扩展（现 RevenueMonthlyRepository 直读，区域 join 已补、品类收窄） | P5-B49 卡11 | revenue_monthly 维度字段补齐 | 远期 M1 |
| mapPayMethod transfer 渠道直通（现归「其他」保守口径，影响 R01 支付方式分布） | P5-B49 卡11 | 支付渠道枚举扩充与历史口径确认 | 远期 M1 |
| R02 毛利率 % 格式化（gross_rate 小数输出 vs 表头（%）） | P5-B49 卡11 | 前端或 SQL 层统一 ×100 口径 | 远期 M1 |
| M1 调度 DEVICE 设备档案台账域待建（GET /resources DOCTOR/ROOM/DEVICE 三源之一现诚实空态） | P5-B49 卡12（详见 DELIVERY-P5-B49 §卡12） | 设备档案主数据域落地后接真，当前资源面板设备组显空态不伪造 | 远期 M1 |
| M1 调度 URGENT 加急标记源（jobs 加急标记现无源，前端已删 stats.urgent 死代码） | P5-B49 卡12 | 依赖预约/工单优先级字段落地 | 远期 M1 |
| M1 调度 durationMin 真实时长（派单时长现固定 end=start+60min） | P5-B49 卡12 | 依赖 appointment 增 itemCode 关联 SKU 或时长字段（现 SKU↔project 名匹配率 0%） | 远期 M1 |
| M1 调度 assignment DONE 完成态（现仅 SCHEDULED/IN_PROGRESS/RELEASED） | P5-B49 卡12 | 依赖服务完成事件源/疗程消耗联动 | 远期 M1 |
| M1 调度医生/房间班次表（资源可用时间现固定 09:00-20:00） | P5-B49 卡12 | 需真实排班数据源 | 远期 M1 |
| M1 调度派单时段自由度（start 现锚定 apptTime，不支持自由时段/改期） | P5-B49 卡12 | 依赖预约管理域改期能力 | 远期 M1 |
| ~~登录页无 token 预拉取四路 401 观察项（/login 挂全局壳 CShellDesktop.vue，onMounted 无条件 notification.fetch 致 permissions/stores×2/notifications 401）~~ | P5-B49 卡12 三轨真验（观察项，详见 DELIVERY-P5-B49 §卡12） | **B50 卡5 已闭合（2026-09-15，`7f08103`，4 files +52/-23，详见 DELIVERY-P5-B50）**：门控收口在数据层（任何调用方都受保护，main.ts/App.vue 调用点不动）——auth.ts loadMatrix 首行无 token 直接 return false、applySession 建会话后补拉矩阵；storeContext.ts loadStores 三重闸门（无 token resolve／loaded resolve／inflight 复用同一在途 Promise finally 清零），根治 main.ts 与 App.vue 双 init 并发窗口内 /stores 双发；CShellDesktop.vue 壳仅认证后挂载、onMounted 作登录后统一补拉点；notification.ts fetch/fetchPreferences 无 token 不发、旁路失败静默。三轨真验（seed 18080，新 bundle index-SOVxdEZ6.js 双栈部署）：无 token 访问 /login 及受保护路由 XHR 零请求 console 零报错、守卫正确跳 /login?redirect；E011 真实登录 permissions/stores/notifications 各 1 次全 200、门店切换器徐汇店/通知 9 条/业务请求带 storeCode=SST01；已登录硬刷新 restoreSession 后三路各 1 次 200 无双发；pnpm build exit0 | ✅ B50 |
| ~~store-service 耗材档案/流水 REGION 无参跨区越权 + 库存页切店不重拉~~ | B50 卡8a 三轨真验新增（同源还债，详见 DELIVERY-P5-B50） | **B50 卡8a 已闭合（2026-09-15，`dd4307a`，5 files +179/-39）**：ConsumableRepository/ConsumableMovementRepository 改继承 JpaSpecificationExecutor 并删除 `cast null` 全量退化 JPQL；ConsumableService 两列表方法以 DataScope.storeSpec("storeCode") 为基座（"__NONE__" 哨兵短路、显式参叠加、档案 Sort ASC skuCode/流水 Sort DESC id），写路径未动；ConsumableServiceScopeTest 6 用例，store-service 全量 12 绿。双栈 docker cp+restart healthy。三轨：curl seed E011（REGION stores=[SST01,02,06]）档案无参 36→18、越界 SST03=0、本域 SST01=6；流水无参 38→18（5+7+6）、越界=0、本域 SST02=7；无 token 401；prod E011 档案 2/流水 4 均 ST-SH-001；PG 对账全符；Chrome 库存页切换器仅现在域三店。附带修复前端陈旧缺陷：InventoryView 漏挂门店切换 watch（OrderView/Writeoff/AppointmentBoard 均有范式），补 watch 后 Chrome 实测切徐汇→浦东无刷新即重拉、四路 XHR 全带新店码 200 console 零告警 | ✅ B50 |
| ~~audit append 通道 actor 自报可伪造（持 audit:view 员工请求体 actor 原样落库破坏不可抵赖性）~~ | B50 卡8c 新增（侦察 ComplianceService/前端 impersonate 留痕链路时发现，详见 DELIVERY-P5-B50） | **B50 卡8c 已闭合（2026-09-15，`43f009b`，1 file +9/-2）**：AuditController.append 按鉴权通道强制 actor——`"system".equals(DataScope.currentActor())`（X-Internal-Token 系统身份，业务服务已在本地 SecurityContext 代填真实操作人）沿用 req.actor() 且空值兜底 "system"；否则（员工 JWT 直连，合规 impersonate 留痕）强制取 JWT staffId 忽略请求体 actor；AppendRequest.actor 去 @NotBlank。mvn package audit-service 胖包双栈 docker cp+restart healthy。五通道真验（B50_8C_TEST append-only 永久留痕）：T1 伪造 actor=FORGED_E011→落 SE101（seed id184）、T2 不传 actor→SE101（id185）、T3 匿名→401、T4 内部 token 代填 E011→沿用 E011（id186，宿主 18084 绕网关）、T5 错内部 token→401（user==null 拦截器口径）；prod 同构 P1 伪造 HACK→落 SE101（id590）；seed verifyChain ok total=124，prod 断链仍仅历史 #380（卡6 已治理为清单展示，新行正常延链） | ✅ B50 |
