# 台账分册 · ② 模块清单大表

> 属于美研云整体开发台账分册；索引、状态判定口径与读写规则见 [../DEVELOPMENT-ROADMAP.md](../DEVELOPMENT-ROADMAP.md)（铁律 9 / 10）。
> 「完成批次」列对应 §③ 时间线（03-timeline.md）；「真实依据」列给出可核验的交付文档或代码位置。

## ② 模块清单大表

> 「完成批次」列对应 §③ 时间线；「真实依据」列给出可核验的交付文档或代码位置。

### 域① 客户与会员（customer-service）

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 客户档案 | ✅ | M4/P3 | 客户视图直连 @/api，交付文档 | —；**P5-B58（2026-09-17）**：客户档案承载 DSAR 个人数据主体权利工单流（ACCESS/DELETE/RECTIFY/PORTABILITY 四类型，DsarRequestController 五端点）与 consent 同意生命周期（PIPL 13-17，三列四场景，ConsentController grant/withdraw/get）主数据，全落 customer-service（com.meiyun.customer），consent 撤回经 InternalConsentController+marketing CustomerConsentClient 联动营销 PushService 门控，详见 DELIVERY-P5-B58-2026-09-17；**P5-B59 卡2（2026-09-17 夜间）**：DSAR DELETE 履约定案为 PIPL 第 73 条不可逆匿名化脱敏（DsarRequestService.anonymizeCustomer：name→匿名客户、phone→00000000000、gender→其他（过 varchar(4) CHECK 女/男/其他）、birthDate/age/skinType/concerns/allergies 等直接/间接标识字段清空、置 customer.anonymized_at；保留 customer_id 关联键与 level/storeCode/channel/totalSpend/visitCount/ownerStaffId/points/status/consent 非标识字段，维系下游十余张表 FK/快照与 append-only 审计哈希链，幂等可重入），履行第 47 条删除权——非物理删除、订正 B58 软删标记语义，`1a2a3b2`＋`deb44e8`，详见 DELIVERY-P5-B59-2026-09-17 |
| 客户标签 | ✅ | P5-B23 | DELIVERY-P5-B23，`9fd5664` | 五分类 CRUD、覆盖汇总、防重打标、客户 360 打标/删标、tagId exists 过滤真实闭环；标签自动化规则列 Backlog |
| 会员等级 | ✅ | P5-B23 | DELIVERY-P5-B23，`9df9e7e` | 五档中文等级、阈值/权益/规则、手工调级、自动升级只升不降、实时人数；定时批处理与自动降级列 Backlog。**P5-B62（2026-09-19）卡1 自动降级引擎已闭合 `003d71e`**：V36 customer_monthly_spend/customer_level_history/monthly_spend_state 三表+auto_downgrade 列，UTC 闭合月游标事实聚合、连续3月低于保级线/保护期锚最近调级月默认3月/每次只降一级/幂等重放短路，autoUpgrade 切事实表累计 net（total_spend 死字段停用），降级告知 internal 端点（按店 STORE_MGR/idemKey/公海只审计）+手动 level:edit 双端点，seed 预演降级78人=history=通知=唯一键=审计/升级38人/重放0/全还原；**卡2 会员等级折扣计价联动 `59a79f8`**：internal level-discount（批量≤50/缺失兜底/fail-closed）＋member-levels/catalog，TxnOrder 5 列＋OrderItem 2 列等级快照，createOrder/createRetailOrder/signEmr 三计价点 BigDecimal HALF_UP（CARD_SALE 不参与、不与门店会员价叠加），前端折后预估/原价划线＋360 权益区接真，黑卡 SC001 20999→16798 五价档；**P5-B93（2026-09-24）benefits 权益核销链路已闭合（卡1 `95e9ea3`/卡2 `d9ffd8d`）**：member_level free_care_times 次数列＋member_benefit_wallet/writeoff 双表（懒发放 UK＋核销幂等三态＋BENEFIT 审计）＋GET benefits 读模型＋benefit:writeoff 权限点＋360 权益卡＋LevelsView 次数编辑，04 L73 整行勾销（原注记「04 L70」系行号漂移按册实况改 L73，同 B92 L78/L79 先例）；详见 DELIVERY-P5-B62-2026-09-19／DESIGN-P5-B93-2026-09-24 |
| 积分账户 | ✅ | P5-B23 | DELIVERY-P5-B23，`852d2f2` | 客户 360「档案」tab 流水与人工调分；clientToken 幂等、余额非负、全动作审计；消费自动积分列 Backlog |
| 积分商城 | ✅ | P5-B23 | DELIVERY-P5-B23，`64e8fe3` | 商品/规则/兑换/双签审核/拒绝/履约/KPI 真实闭环；持久单号、幂等下单、同事务扣积分扣库存 |
| 撞单合并 | ✅ | P5-B84 | feat `5cb3ec1`＋feat `ffc60dd`＋fix `95d2c35`＋chore(seed) `a3bea33` | 依赖客户查重规则，Backlog。**◑ P5-B62 卡3 期1 候选发现只读端点已就位（2026-09-19，`34bcfeb`，详见 DELIVERY-P5-B62-2026-09-19）**：GET /api/customer/merge-candidates（customer:merge，三层手机号归一/POOL·SAME_STORE·CROSS_STORE 分型/pair 双侧 canReadOwned/掩码前3后4/score 0.95/owner 富化降级/8 纯单测），prod 七角 401×2·200×3·403×2、seed 四视角 3/2/1/0 pair、造数备份双份全还原 core 零污染；**✅ P5-B84 合并写期全闭合（2026-09-22）**：V40 双表（customer_merge/customer_merge_snapshot）＋customer 扩 merged_into/merged_at、单事务八步（锁序/SAME_STORE·CROSS_STORE 闸门/409 拒已匿名/8 张物理 FK 子表改挂/loser merged_into 留痕/快照留档/审计 JSON/ES 失效）、5 写端点（propose/approve/execute/dismiss/history，customer:merge 店长起跳）、merged_into 全站横切拦截、前端 api/store/MergeView 全接真（template/style 零改动）、KPI D6 适配层消化、写轨回验三轨全绿（MG20260922-000001 MERGED/MG20260922-000002 NOT_DUPLICATE/UI KPI 2/2/2→1/3/1）、种子幂等造数 SC-M1/M2 合并对＋SC-M3/M4 常态对 |
| 公海客户 | ⬜ | — | — | 远期 |
| 客诉管理 | ⬜ | — | — | 远期 |
| M3 客户运营平台（旅程/分群/任务/关怀/流失/NPS/导入导出/风控/洞察/设置 10 页） | ⬜ | — | — | **远期独立阶段 M3**，Backlog |

### 域② 预约与接待

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 预约看板 | ✅ | M4 | 视图直连 @/api | — |
| 新建预约 | ✅ | M4 | 视图直连 @/api | **B51 卡6 `777d456` 纵深挂载**（DELIVERY-P5-B51）：appointment 加 sku_code 列+创建选 SKU（product_sku 下拉，无 SKU 权限 403 降级可空过）+durationMin 从 SKU duration_min 真源取（绑 SKU 90/70/60/45/40/30/15 分钟档、未绑回落 60min，替代原固定 60min） |
| 我的工作台 | ✅ | P5-B24/B27/B30 | DELIVERY-P5-B24，`50d694d`；B27 `a08bf4d`；B30 `40a35ad` | 7 项计数真实；B27 摘除「候诊待接待」「病历草稿」两演示项（改 listArrivals(WAITING)/listEmr(DRAFT) 真实计数）；**B30 再摘除「术后回访」「SOP 超期」两演示项**（/api/txn/followup/stats 真实 sopPending/sopOverdue 计数，60s 超期升级 Job 驱动）；仅剩复诊提醒 1 演示项，依赖营销自动化 Flow |
| 排队候补 | ✅ | P5-B29 | DELIVERY-P5-B29，`b85ee45`、`2e4d010` | arrival_waitlist 新域：WL 单号查库号池、手机号锚定会员/散客快照两态、WAITING→NOTIFIED→FULFILLED/CANCELLED 状态机；号源释放（手工/超时）同事务 FIFO 递补首位+店长站内信幂等（idemKey `WL:<wlNo>:<staffId>`）；/queue 第三卡+候补登记弹层+候补 KPI；超时自动释放见接待台行 |
| 接待台 | ✅ | P5-B27/B28/B29 | DELIVERY-P5-B27，`759990d`、`a08bf4d`；B28 `cac3f37`；B29 `ae646ed`、`2e4d010` | arrival 独立域：AH 单号/queue_no 本店当日连号/四渠道/WAITING→TRIAGED→CALLING→DONE 五态；分诊（CONSULT/MEDICAL/SERVICE）同事务建 consult_plan 草稿双向回挂、叫号/完成；预约签到同事务自动到店；审计 ARRIVAL 全动作；**B28 改派历史时间线（triage_reassign 同事务追加、正序富化、右栏最小内联改派 UI，同资质候选池剔除当前负责人）**；**B29 候诊超时自动释放（ArrivalAutoReleaseJob 60s 扫描「30min 阈值+10min 宽限」、批 50、系统释放 LEFT 同事务 FIFO 递补、手工释放按钮接真、重载状态防重入、`meiyun.queue.auto-release-enabled` 开关收敛）** |
| 客情登记 | ✅ | P5-B27/B28/B30 | DELIVERY-P5-B27，`a08bf4d`；B28 `aa87fcb`；B30 `09f590b` | GuestReg 手机号 watch 查重（searchCustomers+hydrate）、真实建档成功后自动 checkIn 到店并跳接待台；**B28 十类扩展字段后端化（customer 加 9 列：年龄/肤质/诉求/过敏史三列/意向项目/意向等级/预算/沟通要点，白名单+过敏互斥，客户 360 档案 tab 有值才显示 kv 回显）**；**B30 建档实时写 ES 已交付**（customer_search_event outbox 只存 customerId+10s 中继 Job 批 50 FIFO，SENT/RETRY/DEAD 三态，连续失败 3 次熔断 30s 降级 DB，显式 mapping name 分词+keyword 子字段，读时 mergeRecentFromDb 融合+DataScope 数据域过滤；**B32 DEAD 事件处置台已交付**（/search-events retry·replay·discard 三动作+全量重建入口，customer:search:admin 仅授 REGION_MGR，`f02384b`/`dbf8158`，见下「检索事件处置台」行）；~~ES reindex 对账治理（ES110 vs PG100 漂移 10 文档）~~**已由 B48 卡2 闭合**（2026-09-14，`9c1fd6a`，文档级 diff 对账：missing M001–M003 自动补齐 es110→113 二跑幂等、orphan 100 仅报告 warn 待人工绝不删除（SC* 共享索引拓扑预期）+6h 定时巡检；原「漂移 10」系 seed-only 口径误判，权威源 seed∪core=113，详见 DELIVERY-P5-B48）） |
| 到店核销 | ✅ | P5-B28/B29 | DELIVERY-P5-B28，`8ae7389`；B29 `abbe49e`、`2e4d010` | checkin_record 独立域：CI 单号查库号池、三方式（SCAN/APPOINTMENT/WALKIN）、三态（PENDING/DONE/EXCEPTION）+四异常码（NOT_SELF/ALREADY_DONE/NO_APPOINTMENT/INFO_MISMATCH）、timeline JSON 同事务流水、手机号掩码出域；by-phone 内部目录端点（本店→公海→404）锚定会员/散客快照两态；当日同号 PENDING 幂等；**B29 APPOINTMENT 手机号命中当日预约后同事务四连写（回写 ci.appt_no/wd_no、预约置已到店、自动建到店号 AH、生成 writeoff_desk_task PENDING），核销详情透出勾连单号** |
| 检索事件处置台 | ✅ | P5-B32 | DELIVERY-P5-B32，`f02384b`、`dbf8158` | /search-events：KPI 四宫格点击筛选/状态·客户ID·事件ID 筛选/左表右详情双栏，retry·replay·discard 三动作状态机（DEAD→PENDING rc 清零、当场重放、丢弃 note≥2 字+处置三字段）+中文 400/404+CUSTOMER_SEARCH_EVENT 四 action 审计+全量重建索引入口；customer_search_event 扩 DISCARDED（ddl-auto）；权限 customer:search:admin（仅 REGION_MGR），无权限菜单隐藏+路由守卫 /no-auth；附修异常处理透传、audit biz_type varchar(32) |
| 转化漏斗 | ⬜ | — | — | 依赖数据分析 |

### 域③ 咨询与诊疗

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 开方开单 | ✅ | M4/P3 | 视图直连 @/api | — |
| 咨询工作台 | ✅ | P5-B24 | DELIVERY-P5-B24，`d2d369b` | 方案单草稿保存/接诊真实端点（draft/start），会话区接真实方案单；EMR 独立域列 Backlog |
| 医师工作台 | ✅ | P5-B24/B30 | DELIVERY-P5-B24，`512990d`；B30 `40a35ad` | treat-start/treat-done 全链路（PAID→TREATING→DONE，术前四项/治疗归档），治疗队列切真；**B30 起 treat-done AFTER_COMMIT 幂等触发术后随访 SOP 排程**（物理事务 REQUIRES_NEW，见术后随访 SOP 行） |
| EMR 电子病历 | ✅ | P5-B27/B30 | DELIVERY-P5-B27，`759990d`、`a08bf4d`；B30 `5aa1e69`、`fc97d00` | emr_record 独立域：EM 单号查库号池（修订号 源号-R{n}）、DRAFT→SIGNED→ARCHIVED 全状态机、修订 parent_id/version、FIRST_VISIT/TREATMENT 两型；signEmr/treatDone 同事务幂等落病历并回挂 plan.emr_id/treat_emr_id；门店域全见越权 404；全链路网关+浏览器取证（audit 276–300）；**B30 补齐 EMR 模板库（emr_template 新域+集团 4 种子 EMT-SEED-001..004 只读+本店建/停用两态+模板库弹层九段范文，权限 emr:view/create 复用）、列表真分页（默认 20）/stats 后端聚合、ChineseValidationAdvice 框架异常中文 400；附修 fc97d00（JPQL cast(:type as string) 解决 PG 42P18 查询 500）；集团模板运营管理留 Backlog** |
| 复购提醒 | ✅ | P5-B30/B64 | DELIVERY-P5-B30，`7c922cb`；B64 卡1 `52feb20` | RepurchaseView 全链路切真：客户 /api/customer/search 真实检索（DataScope 数据域隔离）、目标项目必填+知情同意硬勾选前置、待签核统计实时、客户（预填）/经办/店长三方签核填齐解禁、RP 单号查库序号不回退（冒烟 RP20260911-003194 已还原）、REPURCHASE/CREATE+TRIPLE_SIGN 审计；**✅ P5-B64 卡1 已闭合（2026-09-20，`52feb20` 11 files +511/-148，详见 DELIVERY-P5-B64-卡1-2026-09-20）**：医师/店长/财务 ApprovalService 多级审批流接入既有 ApprovalService 无新表/无 Flyway/无新权限码/无新页面——开工三项只读实测修正 DESIGN（repurchase.status CHECK 实测仅 3 值幂等 ALTER DO 块扩「审批中」4 值，DESIGN 误判该 CHECK 不存在；approval_todo 本无 CHECK、REPURCHASE 仅新增 biz_type 常量零 DDL；list 端点加可选 bizNo 非新端点、REGION_MGR 四重证据 L3 无阻断），金额唯 tierFor 是瞻（L1 ≥¥1000 直达 FINANCE/L2 ≥¥5000 REVIEW→FINANCE/L3 ≥¥20000 REVIEW→REGION→FINANCE HIGH），小额三签即同事务并账、大额三签后进审批中、终审 APPROVED 同 JVM 同事务回调 RepurchaseTransferExecutor 并账（仅资产转移搬账，来源卡不足抛 400 回滚）、驳回不并账、审批中再签 409、bizNo 幂等、hasText 空签拦截（真验缺陷同卡修复）；后端 5＋前端 6 改，curl T1-T7＋n10/n12/n15＋PG 双库＋Chrome 双栈三轨全绿，L3 终审 400 负向回滚＋L2 ¥6000 单 200 同事务并账等价覆盖（L3 正向 seed 物理不可达诚实说明），seed 单事务物理还原 0/0/110（审计 436-448 共 13 行保留）、prod 5/8/8 零污染＋8080 只读 Chrome 轨，OCR 511 新增行零命中 |
| 术后随访 SOP | ✅ | P5-B30/B31 | DELIVERY-P5-B30，`40a35ad`、`fa549d5`；DELIVERY-P5-B31，`3825a48`、`4492da4`、`d130dfc`、`70d2b3e`、`53901f6` | followup_sop_template/node/batch/followup 四表；treat-done AFTER_COMMIT→REQUIRES_NEW 物理事务幂等排程（source_plan_id uk+exists 双幂等），默认 1/3/7/30 天节点（微信/电话），种子 SPT-SEED-001 @Order(62) 仅 seed 库；batch_no 号池 `SOP{yyyyMMdd}-%` maxSeqOfDay 不回退；FollowupSopDueJob 60s 扫描批 50 FIFO 超期升级店长幂等通知（idemKey `SOP-ESC:{followupNo}:{staffId}`，免打扰）；/api/txn/followup 五端点（list/stats/get/complete/skip，followup:view/edit）+工作台两演示项摘除；附修 fa549d5（afterCommit 无活动事务致排程静默丢写）；**B31 纵深增强（不加行）：手工建随访 POST（门店强制取 JWT/客户实名解析/planDate≥serviceDate/method 白名单/sopStage=MANUAL/FOLLOWUP·CREATE 审计）、stats 扩九键（+pending/todayPending/overdue/done/skipped/avgSatisfaction/adverseCount，空库 AVG null 修 getSingleResult 的 d130dfc）、keyword 三字段 OR 模糊+固定排序分页；SOP 侧九端点（模板查改/节点增改停删/重置/批次分页聚合/summary 五键/一键升级），SOP_TEMPLATE 五动作审计，FollowupSopEscalator 抽为 DueJob 与一键升级共享组件（escalate 后 save 防标记丢失）** |
| 随访工作台 | ✅ | P5-B31 | DELIVERY-P5-B31，`3825a48`、`4492da4` | /followup 整页接真：FollowupView 列表（九键 KPI/keyword/分页/手工建弹层/完成·跳过流转）+ BoardView 流水牌 sopTodos 真库计数；schedulePostOpSop 降 async 空 shim 防前后端重复排程；C 端 /m/followup「陈美玲」2 条演示种子与 submitByCustomer 保留 |
| SOP 编排 | ✅ | P5-B31 | DELIVERY-P5-B31，`70d2b3e`、`53901f6` | /sop 整页接真：SopManagementView 双 tab（模板节点五动作+增改 dayOffset 0-365/method 白名单/内置可停用可改不可删（删除中文 400）/reset 同构重建；批次看板未完结在前+节点内嵌随访 24 字段）、五键 KPI、超期 warnbar 一键升级（v-perm followup:edit，成功 infobar 静默刷新）；删 SOP mock，router/nav 零改动（既有占位直接接真） |
| 复诊召回 | ✅ | P5-B90 | DESIGN-P5-B90-MARKETING-FLOW-CARE-RECALL-2026-09-24，RecallService（VISIT_GAP_DAYS 规则），V48/V49 | 依赖营销自动化，远期；**B90 已闭合（2026-09-24，卡1 `07c6b65`/卡2 `cf45578`）**：VISIT_GAP_DAYS 复诊召回规则随营销侧随访 Flow 引擎落地——RecallService 种子 90 条召回单（89 条 rule_no=AR-SEED-VISITGAP 系 SYSTEM_AUTO 自动生成）、五 tab 88/0/0/1/1、KPI 待提醒 88/今日 88/转化率 100%/即将到期 88，/recall 整页切真＋工作台复诊待提醒 88 真实计数（第 5 个演示项摘除） |

### 域④ 收银与交易（txn-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 订单收款 | ✅ | M4/P3 | 网关 200 + 交付文档 | — |
| 整单划扣 | ✅ | B17 | DELIVERY-P5-B17 | 三路径打通 |
| 划扣执行台（双签） | ✅ | B17/B18 | WriteoffDeskService 硬闸门 | L1 双签 |
| 退款（L3 三签） | ✅ | B19 | DELIVERY-P5-B19，fbe65c2 | 店长→区域→财务 |
| 退卡（冻结/终审） | ✅ | B18/B19/B20 | DELIVERY-P5-B18/B19/B20 | 手续费 RF-REVENUE/IN 收入分录 B20 收口；退卡 SLA 经 B21 实证由无差别 SLA Job 覆盖 |
| 审批中心 | ✅ | B19/B20/B21 | ApprovalService 状态机，DELIVERY-P5-B20/B21 | SLA/候选过滤 B20 收口；转交/加签目标人硬校验（存在/在职/阶段角色/自转交/重复加签/加签给指派人）+ 详情指派人/会签人展示 B21 收口；REGION 兼岗路由留 Backlog |
| 会员卡项 / 疗程 | ✅ | B16/B17 | member_card 真实 | — |
| 卡项目录 | ✅ | B15 | DELIVERY-P5-B15 | — |
| 支付渠道 | ✅ | P3 | 渠道账实 B12 | — |
| BOM 异常 | ✅ | B10/B34 | DELIVERY-P5-B34，BomDeductService/InventoryView | **B34 收口**：库存不足 422 `{message,shortages[]}` 结构化贯通（skuCode/skuName/needQty/stockQty/unit）→ detail_json + 审计 shortageLines + 页面缺料明细；附修 exc_id substring 起始位致序号恒为 1 |
| 疗程跟踪 | ✅ | P5-B83 | feat `91f1921`＋fix `f9a9e09`（11＋1 文件） | **B83 卡1 收口**：G1 GET /api/customer/cards/course-track（customer-service，course:view 宽码⊃course:track 菜单窄码、DataScope 三守卫越权返空、trackStatus 未开始/进行中/即将到期/已完结四态服务端推导、缺 storeCode 400 中文）＋G2 GET /api/txn/writeoffs/by-card/{cardNo}（txn-service，越权/不存在统一 404）；种子 110 卡 expires_at 回填＋304 划扣流水＋304 核销；前端 CourseTrackView 整页接真（KPI 18/7/2/57＋21 行＋弹层 5 核销，只换数据源模板/style 零改动） |
| 资产转移 / 合同 | ✅ | P5-B85 | feat 卡1 `7380c88`（6 files +252/-2）＋卡2 `80c6517`（4 files +121/-86）＋卡3 `ee8c8eb`（5 files +351）＋卡4 `ffebe89`（8 files +668/-351） | **B85 四卡收口**：D1 卡转移动账权威收口 customer 域（POST /api/customer/internal/cards/transfer）＋D2 transferGift 赠金随转＋D3 toCustomerId 跨客户强制审批＋D4 V43 card_ledger CHECK 加 TRANSFER＋D5 次数转移同 productCode＋D6 contractNo 合同联动（contract 实体 contract_no HT+yyyyMMdd-6位 PK、total_amount 分、penalty_rate 基点 2000=20%、状态机 草稿→生效中→已履行\|已终止、/api/txn/contracts 六端点）；前端合同/资产转移/复购三页接真（api/contract.ts 93 行六端点、stores/contract.ts 306 行重写、stores/transfer.ts 426 行重写，三 View template/style 零改） |

### 域⑤ 营销与留存（marketing-service）

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 优惠券 | ✅ | B22 | DELIVERY-P5-B22 | 券模板/发放/核销真实闭环；8 券种网关+浏览器验证 |
| 精准推送 | ✅ | B22 | DELIVERY-P5-B22，PushService | 写链路四件套：幂等(SHA-256 dedupKey 60s)/审计(PUSH/SEND)/周频控(7天≤3)/违禁词；13 客户真实 |
| 券核销 | ✅ | B22 | DELIVERY-P5-B22，CouponWriteoffService | 三态(FORGED/DUPLICATE/EXPIRED)+折扣(分)+审计(WRITEOFF/BLOCK)；**B34 修正**门店三级兜底为(登录门店→营销设置配置的兜底门店→/internal/first 首家真实门店→三级落空抛 503)，消灭原硬编码 SST01 幽灵码(core 库无此门店，历史脏数据 WR20260908-000004~000007 为证) |
| 营销总览 | ✅ | B22 | DELIVERY-P5-B22 | 触达/漏斗/趋势/渠道排行真实；**B94 已闭合**导出报告（CSV 七节同源导出＋marketing:export 方法级权限＋MARKETING_DASH EXPORT 审计）与订阅周报（weekly-sub 双端点 upsert＋周一 9:00 Job＋通知中心 INBOX 触达 idemKey 双防重，2026-09-25 卡1 `abca319`/卡2 `dbaf8f4`）；XLSX/PDF 格式增强、M5RoiView 同构导出桩、订阅渠道实名化三项留 Backlog（04 表尾） |
| 素材库 | ✅ | B22 | DELIVERY-P5-B22 | 10 素材元数据/标签/授权门店/引用计数；文件二进制(S3/MinIO) Backlog |
| 海报 | ✅ | B22 | DELIVERY-P5-B22，m5Poster | 推荐人下拉接 listStaff 真实在职员工(19名)；模板/漏斗/佣金真实；图片渲染 **B92 已闭合 2026-09-24（卡1 `9ec476a`/卡2 `e369ceb`，详见 DESIGN-P5-B92-内容生产纵深设计-2026-09-24）**：html2canvas 纯前端导出 PNG（预览 DOM 快照→dataURL→a[download] 不入库，平台无图像/对象存储基建 B69 内建轻量先例）＋qrcode 真码协议串 MEIYUN:POSTER:{posterId}:REF:{工号}（确定性生成无需后端存储）＋referrer_name 语义升级工号零 DDL＋员工选择器（listStaff 43 在职「姓名·角色·工号」）＋展示姓名解析回落工号；模板 CRUD/五级埋点/POSTER 成交来源/AI 文案/后端渲染五项移交 04 表尾 |
| 直播/短视频 | ✅ | B22 | DELIVERY-P5-B22 | 7 场直播/挂链成交/漏斗真实；开播写接口/成交回写 **B92 已闭合 2026-09-24（卡1 `9ec476a`/卡2 `e369ceb`，详见 DESIGN-P5-B92-内容生产纵深设计-2026-09-24）**：注——直播开播/结束三写端点开批前已闭合（系原文档滞后）；本批落地＝短视频发布/编辑/上下架三写端点（V51 short_video ADD status DEFAULT 'PUBLISHED'＋chk PUBLISHED\|OFFLINE，live:edit，审计 SHORT_VIDEO CREATE/UPDATE/TOGGLE）＋四 GET 方法级 @RequirePerm OR 放开零新码＋txn source_type/source_id 可空双列与可选来源（词表 NONE\|LIVE_SESSION\|SHORT_VIDEO）＋V51 mkt_deal_attribution/deal_backfill_state 双表＋DealBackfillJob 正反双段（paid 增量/refund 冲销 floor 0）＋前端售卡成交来源选择器 |
| 营销 ROI | ✅ | B22 | DELIVERY-P5-B22，m5Roi | 活动维度 ROI/发券核销统计真实(GET /stats/overview)；渠道区 7 平台+归因模型为外部依赖 Backlog（**B57 卡2 已实施闭合 2026-09-17 `fc97b37`**：三渠道 HMAC 密钥/免签开关收口 external_integration 窗口，回传接收链路密钥来源 env→库窗口，见 DESIGN-P5-B57-EXTERNAL-INTEGRATION；OAuth/spend/ROI 联调仍 Backlog） |
| 渠道业绩 | ✅ | B22 | DELIVERY-P5-B22，m5Channel | 7 渠道卡片/接入状态/对账日真实渲染；外部广告平台(美团/抖音/小红书/大众点评/新氧)数据接入 Backlog（**B57 卡2 已实施闭合 2026-09-17 `fc97b37`**：回传密钥 env→external_integration 窗口录入即生效，见 DESIGN-P5-B57-EXTERNAL-INTEGRATION；平台 spend/投放数据拉取与对账仍 Backlog） |
| 营销设置 | ✅ | B34/B37 | DELIVERY-P5-B37，`828ac6d`；B34 `9a82af6` | 不在 B22 9 页内；**B34 纵深**：新增「券核销兜底门店」配置项(marketing_cfg.writeoff_fallback_store_code，写入前门店存在性校验中文 400、空白归一 null、同值不写冗余审计)，前端 /m5-settings 审批流分组真实门店下拉；**B37 卡1 收口（2026-09-12，`828ac6d`）**：GET 改独立 ConfigView 与写命令 ConfigCmd 字段/类型完全对称（周频键统一 weeklyPushLimit 废弃旧 weeklyLimit 破坏性改名前后端同卡同步、渠道字段两端同为数组、视图剔除 cfgId/referral*/commissionRate 四个多泄遗留列、旧审计快照 weeklyLimit 读时归一、quota weeklyLimit 独立契约不动），marketing_cfg 全部 12 键（周频/免打扰/节假日豁免/大额券阈值/推送审批层级/默认推送与投放渠道/核销兜底门店）均由 /m5-settings 真实页面+真实端点管理，容器 GET/POST/幂等/硬约束/落库/审计取证，原「缺管理页」「GET/POST 字段名不对称」两条 🔧 理由均消除 |
| 充值赠金 | ✅ | B18/B26/B35/B37 | DELIVERY-P5-B35/B37，GrantService/InternalGrantController/PaymentService/OrderView/AutoGrantService | B18 账本+发赠金、B26 满赠阶梯/有效期/报表；**B35 收口**：grant_deduction 扣减流水+FIFO 带锁扣减/幂等/超额 422/GRANT_DEDUCT 审计，marketing internal 端点（X-Internal-Token），txn 转发查余额（cashier:view，散客/不可用降级 available:false）+grant 支付（散客 400/同单第二笔 409），收银台「营销赠金」磁贴与混合支付（template/style 零改动）；**B37 卡2 收口（2026-09-12，`7552665`）**：消费满额自动发赠金——AutoGrantJob 5 分钟轮询 txn `/internal/paid-orders`（复刻 B25 AutoPoints 拉取范式，未走 FUND_ENTRY 事件链路）、CONSUME_THRESHOLD 规则 priority 首条命中（一单不叠多档）、双层幂等（idem_key `RULE:{ruleId}:{orderNo}` + source_biz_ref 订单级跨规则锚点）、单行游标 auto_grant_state 日粒度窗口/NULL 首跑全量回填、txn 不可用整轮中止不推进/客户不存在 400 跳过的故障分层、明细 RULE/system + AUTO-GRANT/SYSTEM 汇总双审计，容器真验全量回填扫描 5 发放 4、二轮幂等零新增；**B39 退款回加赠金已闭合（终审联动逆向回加，`5353bd9`，详见 DELIVERY-P5-B39）** |
| 转介绍 | ✅ | P5-B86 | DESIGN-P5-B86-转介绍设计-2026-09-23，ReferralService/ReferralExpireJob，V44/V45 双表 | **B86 三卡收口（2026-09-23，卡1 `6d1aa00`/卡2 `a52363b`/卡3 `ff019c8`）**：referral_binding/referral_reward 双表＋绑定/解绑/我的绑定/我的奖励等七端点＋ReferralExpireJob 到期 EXPIRED 释放绑定＋手动登记奖励三端点＋/m5-referral 前端接真（D1-A 仅本页）；活动配置/阶梯奖励/层级奖励/邀请排行四 mock 块留 Backlog（04 表尾新登记行）；/m3-referral 旧页本批不动（mock 现状） |
| 落地页 / 日历 | ✅ | P5-B88 | DESIGN-P5-B88-落地页日历设计-2026-09-23，LandingService/CalendarService，V46 三表 | **B88 三卡收口（2026-09-23，卡1 `b4a31a7`/卡2 `a70ee99`/卡3 `d225be8`）**：V46 一次建 landing_page/calendar_node/calendar_schedule 三表＋落地页六端点（列表/详情/新建/编辑/上下线/删除）＋日历八件套（节点 CRUD/排期/月历/D6 敏感词双道校验/CalendarScheduleJob）＋种子库门控（5 落地页＋23 节点＋4 排期，meiyun_core 跳过）＋/m5-landing /m5-calendar 前端两页接真（四定案全 A：D1-A 两模块同批/D2-A 排期带门店可空 NULL=全连锁/D4-A visits/leads/abEnabled/variants 入库采集 v2/D5-A DataInitializer 播种）；拆自原「落地页 / 日历 / 转介绍」联合行（B86 D1-A 定案仅转介绍一页闭合），本批落地页/日历闭合 |
| 随访 / SOP / 关怀 / 召回 | ✅ | P5-B90 | DESIGN-P5-B90-MARKETING-FLOW-CARE-RECALL-2026-09-24，MarketingFlowService/CareService/RecallService，V48/V49 | **B90 两卡收口（2026-09-24，卡1 `07c6b65`/卡2 `cf45578`，开工拍 docs `d982be8`）**：B69 选型定案落地——内建轻量 Trigger-Condition-Action 引擎（不引入 LiteFlow/Drools）＋V48 automation_rule/automation_log 双表＋V49 care_templates 补登＋三条默认规则播种（BIRTHDAY/DORMANT_DAYS/VISIT_GAP_DAYS）＋MarketingFlowJob 每 5 分钟扫描＋Care/Recall 读端点＋/m3-care /recall 前端两页切真＋工作台复诊待提醒演示项摘除（88 真实计数）；权限码零新增（marketing:view/edit、care:view/edit/send、recall:view/create/edit 七码既有）；三轨真验全绿（curl/PG/浏览器三页 console 0 error）；术后随访 SOP 引擎及随访工作台/SOP 编排两整页诊疗侧仍见域③ B30/B31 行 |

### 域⑥ 财务（finance-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 资金台账（fund_entry） | ✅ | B1/P3 | FundEntryService.postEntries 幂等 | — |
| 三方对账 | ✅ | B6 | DELIVERY-P3-B6 | — |
| 渠道账实 | ✅ | B12 | DELIVERY-P3-B12 | — |
| 月结封账 | ✅ | B11 | DELIVERY-P3-B11 | 封账月禁重算 |
| 成本结转 | ✅ | B8 | CostCarryService（COST 分录） | — |
| 提成 | ✅ | B9 | DELIVERY-P3-B9 | — |
| CSV 导出 | ✅ | B7 | DELIVERY-P3-B7 | — |
| 发票管理 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84` | 发票列表去 mock 接真实端点，发票 CSV 真实导出（/api/finance/export/invoices.csv） |
| 预算 / 财务设置 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84` | 预算与财务设置页去 mock 切真；进项税抵扣列 Backlog。**P5-B63 卡4（2026-09-20）进项税抵扣链路已闭合 `4dcfcb6`（16 files +2806/-6）＋真验后独立 CSS 缺类修复 `03cead4`（1 file +9）**：进项发票登记簿＋增值税申报期全链挂载既有 finance 服务与既有 FinInvoiceView/FinTaxView 页无新页面——Flyway V39 fin_input_invoice/fin_tax_period 双表纯 DDL 零种子（CHECK＋业务索引，双库 CREATE IF NOT EXISTS 可重入），追平 org 服务 V35 external_integration prod 历史漏迁（rank40 目录行，checksum -1460755500 保留）；状态机 UNCONFIRMED→CONFIRMED→DEDUCTED→TRANSFERRED_OUT 旁路 NON_DEDUCTIBLE（撤销确认仅 CONFIRMED 可撤、FILED 申报期锁抵扣、不设 360 天硬卡、七类不得抵扣发票强制 NON_DEDUCTIBLE 不入抵扣）；权限 finance:input:view/edit/confirm 三码仅授 FINANCE（org PermissionMatrix），14 端点（InputInvoiceController 9＋TaxPeriodController 5）全挂 /api/finance，系统间 /internal 网关外网裸 404 仅 8087/18087 服务间 X-Internal-Token 直连；审计 FIN_INPUT_INVOICE/FIN_TAX_PERIOD append-only 不引告警、失败走 audit_outbox 中继；前端三收口（FinInvoiceView 进项 tab 登记簿、FinTaxView 申报期、stores/finInputInvoice 287 行＋api/finance.ts +219），销项/台账样式零改；真验发现进项 tab 七类 scoped 样式类缺失（scope 1290d492：tabs/tab/tab--active/inv__output/filters--2 含 1024px 窄屏/pager/pager__info）致裸排版，照 RefundView 母本 03cead4 补齐（构建产物 FinInvoiceView-CR1qSBj2.js 44.34kB＋DqSG-ZY8.css，双前端容器 docker cp 换载免重启，品牌色 rgb(255,107,158) computed-style 实测＋页面上下文 fetch 样式表原文 indexOf 双证，绕 Vue scoped [data-v-1290d492] 后缀精确匹配坑；a11y 文本树替代截图取证未覆盖样式系本次缺类事故漏检根因，已补 computed style＋fetch CSS 原文取证范式）；三轨真验全绿（01:01 curl 经网关状态机全流转/三权限码三角色矩阵 E012 褚财务可编辑确认·E005 李店长 view 只读写门 403·E001 无码 403·无 token 401、/internal 外网 404·直连带 token 200＋PG 双表落库与审计哈希链、01:13 Chrome 三角色双栈三收口真实渲染 console 0 error），01:17 测试数据单事务物理精准还原（禁 TRUNCATE，双份备份留盘 scripts/backup-b63-input-test-20260920/：prod/seed fin_input_invoice、fin_tax_period 四 CSV＋prod_audit_log_733_748 共五 CSV（含表头 5/4/2/2/17 行＝数据 4/3/1/16）＋prod/seed two_tables 双 pg_dump，prod 删 fin_input_invoice id 8/9/10/12 与 fin_tax_period id 4/5/6、seed 各 1 行，审计 prod id733–748 共 16 行 append-only 保留、audit_outbox 无 FIN* 残留），01:22 OCR 替代门禁（ocr/gitleaks/trufflehog CLI 本机不可用，16 文件 8 类敏感信息正则全扫零命中＋人工逐文件目检）；纵深批数字一律不变（域⑥ 11✅/0🔧/1⬜），详见 DELIVERY-P5-B63-卡4-2026-09-20 |
| 税 / 卡余额 / 日结 / 异常 | ✅ | P5-B24 | DELIVERY-P5-B24，`1eaef84`、`dfbe14a` | 资金日报去 mock；卡余额时间线切真（customer-service 内部投影 /internal/cards/{cardNo}/ledger）；通用异常中心列 Backlog。**P5-B63 卡1（2026-09-19）异常账务处置登记已闭合 `3d9d399`**：本页 FinAbnormalView（/m6-abnormal）342→417 行纵深挂载、无新页面——Flyway V37 fin_abnormal_bill（idem_key 唯一索引双库可重入）＋SHORT/LONG/WRONG 三型登记（WRONG 登记时选方向）＋txn FIN_ADJUSTMENT 三级审批闭环（TxnService 金额分级 L2_MIN=500000 分/L3_MIN=2000000 分：<¥5000 L1 财务单签、¥5000–20000 L2 店长→财务、≥¥20000 L3 店长→区域→财务，附修 L3 REGION 阶段漏 FIN_ADJUSTMENT 分支缺陷）＋终审回调 ADJUST/RF-REVENUE 动账（SHORT→OUT、LONG→IN、WRONG 取登记方向，subject=RF-REVENUE/source=MANUAL/ref_type=ADJUST/biz_type=ADJUST/idemKey `ABNORMAL:<billNo>` 幂等短路）＋状态机 PENDING_APPROVAL→APPROVED/REJECTED→DISPOSED＋处置回写 outbox/分录号，前端登记 modal/四态 KPI/仅 APPROVED 可处置/分录号回显/空态接真，删假 seedOutbox 本地态；三轨真验全绿（curl 金额分级矩阵 E012 褚财务/E005 李店长/E011 冯区域/E009 吴店长＋PG 007/008/009 处置落 fund_entry #39/#40/#41 与 outbox #40–46、审计 660–718 共 59 条哈希链＋Chrome 三账号 UI 闭环 console 0 error、空态 bills=[]＋KPI 四 0），测试数据 9 账单/9 审批/7 分录/7 outbox 单事务物理全还原（双份备份留档 /tmp/b63-card1-backup-20260919/：4 CSV＋pg_dump 2889 字节），审计 append-only 保留，共享表 fund_entry 21 行/max=34、outbox 26 行/max=39 精确回落卡前基线（残留 outbox id=9 系 2026-09-05 历史种子非本卡产物）；纵深批数字一律不变（域⑥ 11✅/0🔧/1⬜），详见 DELIVERY-P5-B63-卡1-2026-09-19。**P5-B63 卡2（2026-09-19）通用异常中心已闭合 `d413186`（8 files +730/-194）**：txn-service 扁平包聚合层三源只读归集（BOM_DEDUCT/WRITEOFF/FIN_ABNORMAL）——`GET /api/txn/exceptions`（exception:view，source/status/storeCode 可选参）＋`GET /api/txn/exceptions/{id}`（BOM:/WO:/FIN: 前缀分发）统一 14 字段读模型，finance 新增 internal 只读端点 `/api/finance/internal/abnormal/bills`（X-Internal-Token 系统身份委托既有 FinAbnormalBillService.list 不做 DataScope，txn 逐行 canReadStore 内存二次收敛，视图剔除 idemKey/outboxId/disposeFundEntryId 内部列），FinanceAbnormalBillClient 软降级（任何异常→空 List＋log.warn 绝不外抛，finance 宕机列表不挂）；本地两源 DataScope、detail 越权/不存在/错前缀统一 404 中文、storeCode 越店 404、E001 DOCTOR 无码 403；处置按来源跳既有视图（/m2-inventory、/writeoff、/m6-abnormal）零写操作不新建处置链；状态/级别映射（BOM failCount≥3 HIGH、FIN 金额绝对值≥¥20000 HIGH、WRITEOFF 恒 HIGH），type 三源统一 BUSINESS；前端既有 ExceptionView 页去 mock 接真（api/exception.ts 薄封装＋stores/exception.ts 删 seed 与四写换真源＋ExceptionView.vue 零写三态「前往处置」跳三视图），样式零改；附修 detail `List.of` 不可变致 enrich `List.set` 抛 UnsupportedOperationException 500 真 bug（改 `new ArrayList<>(1)` holder 承载，jar 47840326 指纹 146deb44e9b4 换载复测），curl+PG+Chrome 三轨真验全绿，纯只读零写入零污染无需还原；通用异常中心 Backlog 项收口（04 L83 整行勾销），纵深批数字一律不变（域⑥ 11✅/0🔧/1⬜），详见 DELIVERY-P5-B63-卡2-2026-09-19。**P5-B63 卡3（2026-09-19）预收合规监控已闭合 `c958169`（16 files +1327/-3）**：finance-service 首引 @EnableScheduling 定时栈——Flyway V38 prepay_monitor_rule/prepay_monitor_event 两表纯 DDL（CHECK＋idem 全唯一＋OPEN 部分唯一索引 uk(rule_code,store_code) WHERE status='OPEN'，双库 CREATE IF NOT EXISTS 可重入零 INSERT）＋PrepayMonitorJob 每日 02:20 Asia/Shanghai（cron 全键 meiyun.security.prepay-monitor.cron，env MEIYUN_PREPAY_MONITOR_CRON 覆盖，与 customer 合规巡检 02:00 错峰）＋DEPOSIT_AGE 预收沉淀账龄（fund_entry subject='RF-DEPOSIT' 净额 native 投影最宽阈值单扫＋各规则二次过滤）/REFUND_PENDING 待退款积压（txn finance-flows refunds 按店聚合）/DORMANT_CARD 沉睡卡挂账（customer 卡投影中文状态非在用/退卡中即休眠，仅计本金不含赠金）三类规则扫描＋OPEN 幂等新开/不再命中批量 RESOLVED/回转后可再开事件生命周期（idem_key `PM:{code}:{store}:R{轮次}`、bizRef `PM{yyMMdd}:...` 日粒度≤32）＋新发 OPEN 经 txn POST /internal/compliance-alert 站内信分级（HIGH→CRITICAL 区域+超管、MEDIUM/LOW→WARN 全量店长）软降级不回滚事件＋每轮（含 0 命中）COMPLIANCE/PREPAY_MONITOR 与规则增改 PREPAY_RULE_CREATE/UPDATE 审计 append-only；规则管理仅后端端点（GET overview finance:prepay:view＋GET/POST/PUT rules finance:settings:view/edit，四件套：code 重复 409/非法入参 422/门店越域 404 不泄露存在性/type 不可变 422），系统间触发走 internal POST /api/finance/internal/prepay-monitor/run（internal:prepay-monitor-run＋X-Internal-Token，网关 /internal/** 裸 404 仅服务间直连）；首启安全三层：V38 纯 DDL 零 INSERT＋规则 enabled 默认 false＋store_code 白名单灰度（NULL=全店）；前端守「无新页面」契约——FinPrepayView 既有页告警流并入 PM- 真实事件（4 KPI/5 checks/三告警形状零改、style scoped 零改、prepayPool 绝不灌入 KPI），规则管理不接管理 UI；三轨真验全绿（curl 规则 CRUD 边界→OPEN 事件/站内信→幂等→门店域→RESOLVED 回转＋PG 事件/通知落库与审计哈希链＋Chrome 既有页真实告警渲染 console 0 error），测试数据单事务物理精准还原（禁 TRUNCATE，双份 CSV 备份留盘 scripts/backup-b63-prepay-20260919/ 共 9 CSV，按 .gitignore backup-*/ 不入库），审计 append-only 保留；纵深批数字一律不变（域⑥ 11✅/0🔧/1⬜），详见 DELIVERY-P5-B63-卡3-2026-09-19 |
| 财务核销 / 预收 | ✅ | P5-B24 | DELIVERY-P5-B24，`dfbe14a` | 核销双签明细切真（txn-service /internal/finance/writeoff-details，X-Internal-Token），卡台账/核销 CSV 导出；预收合规监控列 Backlog |
| 经营毛利 | ⬜ | — | — | 依赖成本+收入全量，远期 |

### 域⑦ 组织与权限（org-service）— 核心闭环 ✅

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 登录 / JWT | ✅ | M7 | DELIVERY-M7，/api/org/auth/login；B50 卡5 `7f08103` | B50 卡5 登录页全局壳四门控（未登录不预拉 permissions/stores×2/notifications，闭合 B49 卡12 四路 401 观察项，4 files +52/-23，前端 bundle index-SOVxdEZ6.js）；**P5-B55（2026-09-16）纵深安全批**：JWT 增 realSub/act 双 claim 代操作短 token（TTL 1800s 不滑动不续期，常规 token 仍 43200s）、AuditBoundary 单点收敛审计 actor（currentActor/currentRealActor 双通道）且 payload 显式携带 realSub/act/impersonating 双标记、org 新增 /impersonate 与 /impersonate/exit 两端点+授权矩阵（仅真实 SUPER_ADMIN 发起，禁链式/切超管/切本人/切离职，理由必填）、五业务服务 RestAuditRecorder+audit-service JWT 直连+outbox 统一、前端全局危红横幅常驻/一键退出/刷新快照本地还原/代操作态 401 本地退回不重试写（三 commit `8f78169`、`557a20a`、`1331a21`，20 files +644/-108，勾销 04-backlog L133+L46，audit_log 615-626 append-only 留档区分修复前后，进度数字不变，详见 DELIVERY-P5-B55-2026-09-16） |
| 员工管理 | ✅ | M7 | staff/staff_role 真实 | — |
| 角色 RBAC / 权限矩阵 | ✅ | M7/B19 | PermissionMatrix | 角色管理页待补（Backlog）；**P5-B61 订正（2026-09-18，零代码纵深，旧注作为当时时点事实保留不抹）**：前端角色/权限矩阵/员工/组织四页 T1 共 3219 行＋t1Rbac.ts 609 行五真实端点＋router L170-175＋nav L353-362 权限中台分组＋后端 RbacAdminController 全套均真实存在，三轨三角实证（334/686/48、无 parent_id、四角门控），数字不变，详见 DELIVERY-P5-B61-2026-09-18 |
| 房间床位 | ✅ | B13 | DELIVERY-P4-B13 | — |
| 设备台账 | ✅ | B13 | DELIVERY-P4-B13 | — |
| 项目 / 品牌 | ✅ | P4 | 主数据真实 | — |
| 价目表 | ✅ | B14 | DELIVERY-P5-B14 | — |
| 耗材库存 | ✅ | B8/B13 | 库存真实；B50 卡8a `dd4307a` | B50 卡8a 耗材档案/库存两 Repository 改 JpaSpecificationExecutor 删 cast null JPQL+ConsumableService storeSpec 越权收窄基座+库存页切店 watch 重拉（5 files +179/-39，seed 档案 36→18/流水 38→18） |
| 组织树 | ✅ | B33 | DELIVERY-P5-B33，OrgAdminController 三写端点 + T1OrgView 去 mock | 部门层级写闭环真实（仅门店下建部门/编辑三态语义/部门跨店移动联动 store_code+region/启停带停用原因，越权中文 404）；**门店/大区/集团层级写能力与兼岗上级路由留 Backlog**，部门只停用不物理删；**补正（2026-09-22）**：层级写放开 大区/集团已由 B87 卡2 `a8d6817` 闭合、物理删除＋tombstone 由卡3 `339391c` 闭合、兼岗上级路由由卡1 `4f67922`＋卡2 闭合（详见 04 L36/L38/L39/L40 勾销注记）；**B87 补卡·方案B `0afeef2`（2026-09-22）**：by-role 查询由「按人互斥」改设计 §4.1「主角色∪兼岗并集」（主角色命中 OR 兼任范围命中，主角色双写 '' 行仅对纯兼任者算全局，防借 '' 绕行），摘除拦截收窄至 orgCode=''（'' 锚点行仍 400 保护、范围行可摘＝授摘对称），前端 T1StaffView 授摘对齐（含模板摘除按钮渲染条件收窄），三轨全绿、staff_role 48 行全 '' 零残留 |
| 消息通知中心 | ✅ | B20/B21 | DELIVERY-P5-B20/B21，NotificationController 6 端点 | 系统内通知真实闭环（铃铛/列表/已读）；通知偏好持久化（notify_preference 表 + GET/PUT 端点 + SLA 催办偏好免打扰双向实证）B21 收口；**多渠道（短信/企微/邮件 dev 网关 + SSE 实时推送 + 免打扰 + 失败重试/死信）B26 收口**，真实运营商网关接入留 Backlog（**B57 卡2 已实施闭合 2026-09-17 `fc97b37`**：三通道网关 URL 收口 external_integration 窗口、录入即生效零重启、未配保持 SKIPPED 诚实降级，见 DESIGN-P5-B57-EXTERNAL-INTEGRATION；运营商账号开通/工号-手机号映射仍 Backlog）；**P5-B59 卡1（2026-09-17 夜间）**：合规巡检告警 CRITICAL/WARN 跨服务站内信联动——customer ComplianceInspectionJob 巡检分级后经 RestTemplate 携 X-Internal-Token 调 txn 新增内部端点 `POST /api/txn/internal/compliance-alert`（@RequirePerm("internal:notify") system 身份），OrgStaffClient.listStaffByRole 角色路由（WARN→门店经理 STORE_MGR、CRITICAL→区域经理 REGION_MGR+超管 SUPER_ADMIN、INFO 仅审计不发信），idemKey=`COMPLIANCE:{bizRef}:{staffId}:{level}` 幂等、notify_preference SYSTEM 类免打扰、NotificationFanoutJob fixedDelay 60s 扇出 INBOX、远程失败软降级不阻断巡检，`5706efa`；**铃铛实时性事实澄清（前端实证，订正旧文"SSE 实时推送"表述）**：CShellDesktop.vue/NotificationView.vue onMounted 单次 fetch 拉取，无 EventSource/SSE、无轮询，实时性＝后端 60s fanout 落库＋前端下次挂载拉取；EventSource 仅 m1Screen.ts 集团大屏使用，详见 DELIVERY-P5-B59-2026-09-17）；**P5-B60 卡3 再订正（2026-09-18，`51c6628`，旧 B59 表述作为当时时点事实保留不抹）**：上述「非 SSE」事实已被本卡改变——stores/notification.ts 新增 connectStream/disconnectStream 已接 EventSource 订阅 /api/txn/notifications/stream，监听 "notification" 增量事件按 id 去重头插 items+刷未读（守卫防重、无 token 不握手、ready 置位、401 类坏 token 未握手即 CLOSED 主动摘除防死循环、其余交浏览器原生重连），CShellDesktop onMounted connect/onUnmounted disconnect 挂载清理；铃铛无刷新实时更新（Chrome 实证角标 1→2、store 头插 [87,86,42]、reload 无重复、curl ready/实体帧+:hb），实时性＝服务端 fanout 落库后 streamRegistry.push 秒级推送，详见 DELIVERY-P5-B60-2026-09-18。**P5-B60 卡4 再补（`3a9a308`）**：全局免打扰管理端可配（external_integration NOTIFY_GLOBAL_QUIET/QUIET_WINDOW），个人级免打扰由卡1/2 落地（notify_preference quiet 三列+设置页），免打扰策略全量收口 |
| 交接班 | ✅ | 全门店交班/接班链路（当前班次/交班快照：资金+客情+预约+库存异动四域聚合/差异核对/异常落库/交班记录） | 读：当前交班会话/会话列表/快照/交班记录 四端点 | **P5-B83 卡2（2026-09-21）交接班已闭合 `dbbb8bb`（11 files +1139/-205）**：store-service handover 包八文件（HandoverSession/HandoverSnapshot/HandoverException 三实体＋四仓储＋HandoverService 聚合服务）＋读端点四件套（GET /api/store/handover/current-session·/sessions·/sessions/{id}/snapshot·/records），快照四域聚合＋差异阈值核对＋异常落库；前端 api/handover.ts＋stores/handover.ts＋HandoverView 全量切真（template/style 零改动，mock 全废），金额 bigint 分，三轨真验（网关 curl ✅/PG 对账 ✅/浏览器会话·快照·核对·记录四区全渲染 ✅）；**纵深跃迁**（占位页→全链路读通）；详见 DELIVERY-P5-B83-近线补点-2026-09-21.md（卡2 章） |
| M2 门店运营（排班/工单/日结/申购/报损/绩效/周报/巡检/拓客/唤醒/异常等 13 页） | ✅ | P5-B54/B80/B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`7111d37` | **M2 13/13 全部真实闭环**：排班（P5-B54）＋物料申领、损耗报损（P5-B80）＋服务工单、营业日报、员工绩效、经营周报、巡店检查、拓客活动、沉睡唤醒（P5-B81，`e4f059d`/`6d421f6`/`f0a5a28`/`547a6dd`/`540ea7f`/`872ea89`/`7111d37`）；另房间床位、设备仪器、异常中心 3 页经三轨核实属事实已真（room/equipment 域早已接真、异常中心 B63 卡2 闭合），不另计新建行 |
| M2 · 排班（/m2-schedule） | ✅ | P5-B54 | DELIVERY-P5-B54-2026-09-16，`bbcdb46`、`e2da7d7`、`2e1602b`、`fc5c2e7`、`9f44eb1` | org-service staff_shift 实体（uk staff_id+shift_date、source TEMPLATE/OVERRIDE、五态上午/下午/全天可派·休息/请假 assignable=false）+周视图/改班/生成周例/复制上周四端点+internal resolve 供 txn 派单/改期/resources 三处真源替换固定 09:00-20:00 窗（X-Internal-Token 软降级、越窗 422 中文动态文案、OFF/LEAVE 权威不可派）；leave_request 请假申请-批准-驳回闭环+批准逐日 LEAVE/OVERRIDE 联动；M2 排班整页接真（NONE 空态不造假班、考勤恒空诚实标注）、M1 调度时间轴改后端班次并集动态轴；**M2 13 页首个切真页（1/13）**，余 12 页远期 |
| M2 · 物料申领（/m2-requisition） | ✅ | P5-B80 | DELIVERY-P5-B80-REQUISITION-WASTAGE-2026-09-21，`7c5cd8b`、`0aab59f` | store-service requisition 域 10 文件：Requisition/RequisitionItem/RequisitionNote 三表（JPA ddl-auto=update 零 Flyway）、RqNoGenerator（RQ 单号）、Repository×3、RequisitionService 315 行（五态 DRAFT→SUBMITTING→APPROVED/REJECTED，驳回回 DRAFT，APPROVED 出库联动扣减耗材库存＋移动均价）、RequisitionController 157 行七端点挂 /api/stores/requisitions、RequisitionDataInitializer 种子 6 单/13 明细/15 备注（meiyun_seed 门控）；前端 api/requisition.ts 77 行＋stores/requisition.ts 重写 170 行切真、RequisitionView.vue 微调；**M2 2/13**，余 11 页远期；L103「BOM 缺料明细→申购单一键联动」仍留 Backlog |
| M2 · 损耗报损（/m2-wastage） | ✅ | P5-B80 | DELIVERY-P5-B80-REQUISITION-WASTAGE-2026-09-21，`0ec3a55`、`0e82133` | store-service wastage 域 8 文件：Wastage/WastageNote 两表（ddl-auto 零 Flyway）、WsNoGenerator（WS 单号）、Repository×2、WastageService 281 行（五态 DRAFT→SUBMITTING→APPROVED/REJECTED，APPROVED 按损耗明细逐项扣减耗材库存）、WastageController 155 行端点挂 /api/stores/wastages、WastageDataInitializer 种子 7 单/17 备注；前端 api/wastage.ts 75 行＋stores/wastage.ts 重写 155 行切真、WastageView.vue 微调；**M2 3/13**，余 10 页远期 |
| M2 · 拓客活动（/m2-acquisition） | ✅ | P5-B81 | DELIVERY-P5-B81-CARD6-ACQUISITION-2026-09-21，`872ea89` | store-service acquisition 域 7 文件：AcquisitionCampaign 单表（ddl-auto 零 Flyway）、AqNoGenerator（AQ-yyyyMMdd-000001 全局日序号）、Repository、AcquisitionService 209 行（状态机 DRAFT→launch→ONGOING→end→ENDED、金额元↔分换算、end 置 end_date 当天）、AcquisitionController 122 行五端点挂 /api/stores/acquisitions（DataScope 三守卫）、AcquisitionDataInitializer @Order75 种子 7 单（3 ONGOING/3 ENDED/1 DRAFT，业务种子 seed() 不写 audit_log）；前端 api/acquisition.ts 49 行新建＋stores/acquisition.ts 208 行重写切真（DTO adapt/KPI 聚合、rank、can('acquisition:edit')、写后 await load()）、AcquisitionView.vue 微调；**M2 4/13**，余 9 页远期；误伤第 7 条 DRAFT 经 restore-seed-draft.sql 单事务补回 |
| M2 · 服务工单（/m2-workorder） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`e4f059d` | store-service workorder 域 9 文件：WorkOrder/WorkOrderNote 两表（ddl-auto 零 Flyway）、WoNoGenerator（WO 单号）、Repository×2、WorkOrderService 293 行（状态机 PENDING→start→IN_PROGRESS→complete→DONE，PENDING/IN_PROGRESS→escalate→ESCALATED；type REPAIR/INSPECTION/CUSTOMER/CONSULT；create 默认 deadline=now+4h、assignee 缺省待分配、首条 note「创建工单」；notes append-only 四动作）、WorkOrderController 136 行挂 /api/stores/work-orders（view/create/edit/close 四权限码）、WorkOrderDataInitializer 种子 6 单；附修 GlobalExceptionHandler 收敛中文错误；前端 api/workorder.ts 69 行＋stores/workorder.ts 246 行重写切真（DTO adapt/KPI pending/inProgress/done/overdue）、WorkOrderView.vue 仅 script async；**M2 5/13**，余 8 页远期 |
| M2 · 营业日报（/m2-daily） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`6d421f6` | store-service daily 域 9 文件：DailyReport/DailyTodo 两表（ddl-auto 零 Flyway）、Repository×2、DailyService 347 行（一天一份、DRAFT→submit→SUBMITTED 终态锁定；footfall/orders/services/hourly[12]（10:00~21:00）/todos/exceptions/timeline；setHourly 时 footfall=hourly 求和；todo kind TASK/CUSTOMER/ISSUE）、DailyController 152 行挂 /api/stores/daily-reports（view/edit/submit 三权限码，GET /today 不存在服务端兜底建 DRAFT）、DailyDataInitializer 启动幂等保证昨日 SUBMITTED＋今日 DRAFT 两条基线；IntListJsonConverter/TimelineJsonConverter 两 JSON 转换器；前端 api/daily.ts 79 行＋stores/daily.ts 303 行重写切真（ensureToday 改同步从已加载列表命中、空壳不写库）、DailyView.vue 仅 script；**M2 6/13**，余 7 页远期 |
| M2 · 员工绩效（/m2-performance） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`f0a5a28` | store-service performance 域 4 文件：PerfStaff 实体（ddl-auto 零 Flyway）、Repository、PerformanceService 153 行（name/role CONSULTANT/DOCTOR/BEAUTICIAN、target/actual 元、orders、commissionRate、status ON_DUTY/LEAVE/PROBATION、trend[6]；COMMISSION_TIERS 6/8/10/12% 四档门槛）、PerformanceController 91 行挂 /api/stores/perf-staff（GET ?period=THIS_MONTH/LAST_MONTH、updateTarget performance:edit）、PerformanceDataInitializer 种子 7 员工；前端 api/performance.ts 30 行＋stores/performance.ts 127 行重写切真（simulateCommission 保持纯前端试算无网络契约、trendLabels 随 period 动态生成）、PerformanceView.vue 仅 script；**M2 7/13**，余 6 页远期 |
| M2 · 经营周报（/m2-weekly） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`547a6dd` | store-service weekly 域 4 文件：WeeklyReport 实体（ddl-auto 零 Flyway）、Repository、WeeklyService 218 行（weekNo/startDate/endDate/revenue/prevRevenue/footfall/orders/newCustomers/repurchaseRate/highlights/issues/nextWeekPlan；DRAFT/SUBMITTED；**ISO-8601 周年周号** WeekFields.ISO 修正 mock 非 ISO 口径，create 取最新周 startDate+7 派生、prevRevenue 取上一张）、WeeklyController 121 行挂 /api/stores/weekly-reports（新建/保存/提交统一 weekly:submit）、WeeklyDataInitializer 种子 6 条（最新 2026-W34）；前端 api/weekly.ts 52 行＋stores/weekly.ts 198 行重写切真（createWeekly async、wowRevenue KPI）、WeeklyView.vue 仅 script；**M2 8/13**，余 5 页远期 |
| M2 · 巡店检查（/m2-inspection） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`540ea7f` | store-service inspection 域 10 文件：Inspection/InspectionItem/RectifyIssue 三表（ddl-auto 零 Flyway）、InsNoGenerator、Repository×3、InspectionService 322 行（type ENV/SERVICE/COMPLIANCE；totalScore=Σscore/(n*10)*100、item score<7 记 issue；issue 状态 OPEN→DOING→DONE、dueAt +7 天；无 issue create 即 DONE、有 issue PENDING，全 issue DONE 单据 DONE）、InspectionController 122 行挂 /api/stores/inspections（**storeCode 数据域列跨店**，view/create/edit 三权限码）、InspectionDataInitializer 种子 6 单跨三店；前端 api/inspection.ts 59 行＋stores/inspection.ts 298 行重写切真（store 展示名 adapt、monthCount/avgScore/待整改/overdue KPI）、InspectionView.vue 仅 script；**M2 9/13**，余 4 页远期 |
| M2 · 沉睡唤醒（/m2-reactivate） | ✅ | P5-B81 | DELIVERY-P5-B81-M2-SEVEN-PAGES-2026-09-21，`7111d37` | store-service reactivate 域 8 文件：ReactivateCustomer/ReactivateLog 两表（uk rc_no，ddl-auto 零 Flyway）、RcNoGenerator（RC-yyyyMMdd-%06d 上海时区）、Repository×2、ReactivateService 264 行（tier T30/T60/T90 按 lastVisitDays 分档；状态机 PENDING→assign→ASSIGNED→logVisit→VISITED→recovered→RECOVERED 不回退；channel PHONE/WECHAT/SMS；assign nextFollowAt=now+2 天；ASSIGNEE_FULL 全名映射）、ReactivateController 98 行五端点挂 /api/stores/reactivates（统一 reactivate:edit、REACTIVATE 全动作审计）、ReactivateDataInitializer @Order76 种子 8 客户；前端 api/reactivate.ts 53 行＋stores/reactivate.ts 176 行重写切真（DTO adapt、total/t30/t90/monthRecovered KPI、写后 await load()）、ReactivateView.vue 仅 script；**M2 10/13**（连同房间/设备/异常 3 事实翻转页，M2 13/13 全闭环） |

### 域⑧ 平台与基建

| 模块 | 状态 | 完成批次 | 真实依据 | 缺口 / 备注 |
|---|---|---|---|---|
| 审计链（audit-service） | ✅ | M7 | 审计落库真实；B50 卡6 `aecf97f`、卡8c `43f009b` | B50 卡6 verifyChain 增强返全量断链清单（prod breaks=[380,520] 均 B48 修复前历史窗口、seed []/117、seed verify ok total=124，5 files +201/-8）；B50 卡8c AuditController actor 按通道强制收敛（X-Internal-Token 通道沿用请求体 actor 空兜底 system、员工 JWT 直调强制 staffId，1 file +9/-2；B50_8C_TEST seed audit id184-186/prod id590 append-only 留档） |
| 字典管理 | ✅ | M7 | 字典端点真实 | — |
| 国密网关双栈（Go） | ✅ | M7 | 18443/8443 双栈；B50 卡7 `5251306` | B50 卡7 真实客户端 IP 采集（clientIPFromRequest/Director 注入 X-Real-IP：XFF→X-Real-IP→remoteAddr→unknown，下游审计去硬编码 "web"，20 单测，7 files +364/-7） |
| 测试 / 双栈验证体系 | ✅ | 全程 | 每批 DoD（mvn+vue-tsc+双栈+PG 对账） | — |
| M1 集团（brand/procurement/marketing 3 页真实） | ✅ | P5-B49/B82 | 3 页直连 | 审计日志 B49 卡2、区域+门店主数据卡3、概览/矩阵/对标卡4、采购补全卡5、标准作业SOP卡6、数据大屏卡7、目标管理卡8、合规中心卡9、健康度巡检卡10、报表中心卡11、调度中心卡12 接真（见下行拆分），其余 0 页 mock，**M1 17/17 页全切真（3 真实+14 已切真+0 mock，M1 批目标达成）**；**P5-B82（2026-09-21）计数校准**：M1 聚合行随 17/17 全切真由 🔧 翻 ✅（模块行口径，本行为一个模块行；下十四行为 M1 拆分行，聚合/拆分并存但计数以模块行为准、拆分行不另入总） |
| C 端移动端（packages/coupons 2 页真实） | 🔧 | — | mp-uniapp 2 页 | 其余 20 页 mock，**远期移动端** |
| M1 · 审计日志（/m1-audit-log） | ✅ | P5-B49 卡2 | DELIVERY-P5-B49，`43f52ca` | audit-service +GET /page 五过滤服务端分页+/facets 两新端点，原全链 List 端点零改动；KPI/过滤/分页/详情侧栏全真，/verify 如实显历史断链 #380；B50 卡6 `aecf97f` verifyChain 增强返全量断链清单（prod breaks=[380,520] 均 B48 修复前历史窗口、569 行/seed [] total=124），#380 全量清单观察项闭合 |
| M1 · 区域管理（/m1-region） | ✅ | P5-B49 卡3 | DELIVERY-P5-B49，`494c272` | org/tree+regions/dist 双源只读，六区 KPI/卡体统计全真，写侧收窄 Backlog 只读 |
| M1 · 门店主数据（/m1-tenant） | ✅ | P5-B49 卡3 | DELIVERY-P5-B49，`494c272` | /api/stores 接真只读（DataScope 随登录人数据域），7 列+动态筛选+KPI 含已关店+三态，写侧收窄只读 |
| M1 · 集团经营概览（/m1） | ✅ | P5-B49 卡4 | DELIVERY-P5-B49，`f672a80` | finance-service 新增 GET /finance/group-overview 集团聚合端点（铁律 -1-D 跨店例外域，DataScope.storeSpec("storeCode") LinkedHashMap 聚合 parallel 空安全），hero/KPI/月度趋势全真，null 显「—」不伪造，默认期「已出月报门店数最多月份·并列取最新，随数据域而定」 |
| M1 · 指标矩阵（/m1-matrix） | ✅ | P5-B49 卡4 | DELIVERY-P5-B49，`f672a80` | 同 group-overview 端点接真，门店×指标矩阵全真，fen2wan 分→万换算、score 权重归一+负截 0，null 显「—」 |
| M1 · 门店对标（/m1-compare） | ✅ | P5-B49 卡4 | DELIVERY-P5-B49，`f672a80` | 同端点门店横向对比接真，E011 华东域默认期 2026-09 系域收窄正确非 bug（华东仅 ST-SH-001 有月报），脚注 4 处硬编码月份自发现自修复→generic 规则化 |
| M1 · 采购管理（/m1-procurement） | ✅ | P5-B49 卡5 | DELIVERY-P5-B49，`a108203` | store-service 新建 procurement 域（供应商+采购单四表+收货），六态状态机/审批阈值三级/库存联动移动均价 1348 分/批次号幂等，view 零改动切真；B50 卡2 `da3d812` 采购列表 DataScope 越权收窄（3 files +116/-14，seed E011 采购单 6→4、SST03 行消失） |
| M1 · 标准作业SOP（/m1-sop） | ✅ | P5-B49 卡6 | DELIVERY-P5-B49，`d4c6453` | store-service 新建 sop 域（模板+步骤+任务三表 ddl-auto 建表+种子），SopController 八端点（模板列表/新建/发布、任务按模板生成/开始/步骤逐项勾选/完成状态机），三轨真验全绿 |
| M1 · 数据大屏（/m1-screen） | ✅ | P5-B49 卡7 | DELIVERY-P5-B49，`815f006` | txn-service 新建 screen 包（GET /screen/overview 六 KPI+hourly+品类占比+门店 top5 快照、GET /screen/stream SSE 流、ScreenOrderFanoutJob 3s 轮询 order_payment 扇出广播 order-paid、StoreProjectClient SKU→品类映射 X-Internal-Token 60s 缓存），P0 SSE 认证串扰+nginx SSE 缓冲双缺陷修复，三轨真验全绿；B50 卡3 `cbd6692` shTimeStr 成交流时间锚定 Asia/Shanghai（2 files +9/-2，UTC 05:26:00Z→北京 13:26 显示修复，闭合 B49 卡7 登记观察项） |
| M1 · 目标管理（/m1-target） | ✅ | P5-B49 卡8 | DELIVERY-P5-B49，`1f432a2` | finance-service 新建 target 域（biz_target 表 metric 五枚举×ownerType 三级×approval 四态+审批状态机+idemKey 幂等+BIZ_TARGET 审计留痕），TargetController 七端点（列表四过滤/新建/进度/提交/批准/驳回），种子 10 行三级分解树四态覆盖；前端 api/target.ts+m1Target 重写切真，修复 GROUP 行重复渲染遗留缺陷，三轨真验全绿；B50 卡4 `3a263ab` DataScope.canReadTarget 详情/动作级越权收窄（6 files +206/-5，四角真验 7/5/5/10，闭合 B49 卡8 登记「target 列表行级 DataScope 未收窄」） |
| M1 · 合规中心（/m1-compliance） | ✅ | P5-B49 卡9 | DELIVERY-P5-B49，`b7baee5` | audit-service 平铺新增 5 文件+V31 迁移（compliance_check 表 category 六枚举×status 四态 CHECK），GET /checks 类级 compliance:view+POST /checks/{id}/recheck 方法级 compliance:edit（body={pass 必填,remark?}），同事务 RECHECK 审计（bizType=COMPLIANCE、payload 八键、risk=FAIL→HIGH/PASS→LOW、ip "web"），audit_log 不扩列；取操作人 SecurityContext.currentStaffName() 中文姓名；ComplianceDataInitializer @Order(45) 种子 12 条照 mock+6 条审计真实写入保哈希链；前端 api/compliance.ts+m1Compliance 重写切真（impersonate 仅审计留痕真实化收窄，view 零改动），curl 权限四角+PG 验链+Chrome 三轨真验全绿；B50 卡7 `5251306` 后审计 IP 改由 Go 网关注入 X-Real-IP（XFF→X-Real-IP→remoteAddr→unknown）不再硬编码 "web"，B49 卡9 登记「审计真实客户端 IP 采集」观察项闭合；prod compliance_check 手插 id=1 PRIVACY「L134 平台治理验真临时项」FAIL append-only 留档；**P5-B55（2026-09-16）**：合规中心代操作弹层由 m1Compliance store 内存假会话改接 org 真实 /impersonate、/impersonate/exit 端点（store -41 行、M1ComplianceView -40 行），页内假 imp-bar 拆除改由 CShellDesktop 全局危红横幅承载，删除前端 appendComplianceAudit 直调（审计统一经后端 RestAuditRecorder→audit-service 落库，前端不再旁路写审计），详见 DELIVERY-P5-B55-2026-09-16；**P5-B58（2026-09-17）**：ComplianceInspectionJob 每日合规巡检（@Scheduled 02:00 Asia/Shanghai、actor=SYSTEM、WARN/CRITICAL/INFO 三级、无命中也落审计 bizType=COMPLIANCE action=INSPECT、payload inspectedAt+三级计数+清单上限 20）落 customer-service（com.meiyun.customer），巡检审计经既有审计通道落 audit-service，与合规中心同链衔接，详见 DELIVERY-P5-B58-2026-09-17；**P5-B59 卡3（2026-09-17 夜间）**：巡检执行日按 Asia/Shanghai 业务时区取日（`@Scheduled(...zone="Asia/Shanghai")`＋`now.atZoneSameInstant(BIZ_ZONE).toLocalDate()`），修复凌晨 02:00 触发时按 UTC 取到前一日的日期错位，bizRef 与审计 action 共用 `INSPECT-<北京日期>` 保证巡检记录/站内信/审计三处同日勾稽，`1b5760d`，详见 DELIVERY-P5-B59-2026-09-17 |
| M1 · 健康度巡检（/m1-health） | ✅ | P5-B49 卡10 | DELIVERY-P5-B49，`177a32e` | store-service 新建 health 包 9 文件（三表 ddl-auto：health_check store_code 作 PK+health_score 六维行 uk(storeCode,dimension)+health_issue 状态机），六端点挂 /api/stores/health 网关零改动，rerun 未决高/中危扣分算法服务端化（clamp[40,98] 双向实证）+复用 ConsumableAuditRecorder 落审计，种子 5 头+30 分+7 issue；前端 api/health.ts+m1Health 重写切真+M1HealthView selId 一行微调，curl 权限四角+PG+Chrome 三轨真验全绿 |
| M1 · 报表中心（/m1-report） | ✅ | P5-B49 卡11 | DELIVERY-P5-B49，`01c7af1` | finance-service 平铺新建 report 域 10 文件（report_template 9 模板+report_job content BYTEA 真实 CSV 落库可重放），ReportController 挂 /api/finance/report 类级 report:view 七端点（templates/jobs/subscribe/generate/retry/download/preview），ReportAsyncRunner @Async 真实生成；R01→FinanceAggregationService.ledger 真实流水门店×支付方式聚合、R02→RevenueMonthlyRepository 直读 join 补区域+环比（项目品类维度收窄）、R03-R09 七模板 422 收窄登记 backlog；种子 9 模板+5 job content NULL 不伪造；审计复用 FinanceAuditRecorder bizType=REPORT；前端 api/report.ts+m1Report 重写切真（剔双 setTimeout 改真实 POST+1s×10 轮询、剔假 preview/mock blob download、format 仅 CSV），三轨真验全绿；**P5-B56（2026-09-16）**：报告哈希验真 UI 纵深——V33 report_job 双列（content_hash varchar(64)/file_name varchar(128)，ADD COLUMN IF NOT EXISTS 双库可重入）、哈希源定案含 UTF-8 BOM 的 content 原始字节 SHA-256 小写 hex+RFC4180 CRLF（JDK 原生 MessageDigest 无新依赖）、下载文件名锚 createdAt 北京时区 yyyyMMdd-HHmmss 冻结消解同参重导漂移（历史 NULL 回退 createdAt 同名规则不回填）、GET /jobs/{id}/verify 四态 MATCH/MISMATCH/HASH_NOT_RECORDED/HISTORICAL_NOT_RETAINED（GENERATING/FAILED 422、404、401 负向齐备、沿用类级 report:view 不新增权限码、MessageDigest.isEqual 常量时间比较）、审计第五 action VERIFY append-only（payload ok/reason）、前端验真卡（verifyResults 按 jobId 留存/verifyingIds 防重入/短指纹 12 位 title 全量/MATCH 单指纹 MISMATCH 双指纹三色调/READY 即显不受 report:export 门控）；后端 `3b34214`、前端 `68d01cd` 共 11 files +340/-14（含 4 单测），卡5 curl 九角+PG（三 hash 实证、VERIFY 24 条 id315-345 交错占号四态 12/4/4/4、全表链 broken=0、V33 双库 success）+Chrome（三色/SE001 仅 view 可见/0 error 0 warn）三轨真验全绿、测试数据零污染还原（seed report_job 回 11 行基线、双列非空 0、core 0 行），34✅2🔧31⬜ 数字不变，详见 DELIVERY-P5-B56-2026-09-16 |
| M1 · 调度中心（/m1-dispatch） | ✅ | P5-B49 卡12 | DELIVERY-P5-B49，`0b3dfb0` | txn-service 平铺新建 dispatch 域 6 文件（DispatchAssignment 实体 dispatch_assignment 表 ddl-auto=update 建表无 Flyway 照卡10 先例、Repository、DispatchController 挂 /api/txn/dispatch 四端点、DataInitializer @Order(86) 空表门控不伪造、DispatchService 353 行、StoreRoomClient 70 行读侧软降级），store-service +1（InternalRoomController 36 行 /api/stores/internal/rooms 治疗室过滤+X-Internal-Token internal:catalog-read），org-service 零改动复用 OrgStaffClient.listStaffByRole("DOCTOR")；GET /resources（dispatch:view，医生/房间/设备三源+当日活跃占用内联，DEVICE 设备档案无源诚实空态）、GET /jobs（已预约/已到店+当日+无活跃占用，已到店排前）、POST /dispatch（dispatch:edit，start 锚定 apptTime/end+60min，已预约=SCHEDULED/已到店=IN_PROGRESS，改派医生 422/重叠 409/重复派单 409 全中文）、POST /assignments/{id}/release（RELEASED+released_at 保留行）；审计 bizType=DISPATCH action=DISPATCH/RELEASE；权限零改动 PermissionMatrix 预置 dispatch:view/dispatch:edit；前端 api/dispatch.ts+m1Dispatch 重写切真（删 urgentJobs/stats.urgent 死代码）+M1DispatchView 接线；三轨真验全绿（curl 权限四角 E011 全通/E002 双 403/无端 401/SE101·E014 通、写路径 15 分支、PG dispatch_assignment 5 行终态 RELEASED+审计哈希链、jobs pending=3 DEMO01-03、Chrome UI 零 warn、mvn+vue-tsc+vite build）；观察项登录页全局壳无 token 四路 401 已由 B50 卡5 `7f08103` 四门控闭合（未登录不预拉 permissions/stores×2/notifications）；M1 17 页全切真（3 真实+14 切真+0 mock）；**B51 四卡纵深挂载（2026-09-15，完成度数字不变，详见 DELIVERY-P5-B51）**：卡3 DONE 完成态联动 `79a8ae4`+fix `2714287`（9 文件 +150/-25，DispatchCompletion AFTER_COMMIT REQUIRES_NEW 联动 treatDone、终态 422、isSlotBusy 排除 DONE 不占时段）；卡4 DEVICE 接真+放开派单写 `86d4a65`（8 文件 +179/-21，store InternalEquipmentController+txn StoreEquipmentClient 软降级——上文「DEVICE 设备档案无源诚实空态」已闭合为读+写全闭环，仅 NORMAL 可派 404/同时段 409）；卡5 改期联动 `50a7eb7`（2 文件 +56/-3，followReschedule 同事务跟随/409 回滚/422 班次/audit 双轨 RESCHEDULE+RESCHEDULE_FOLLOW——上文「start 锚定 apptTime」改期半项已闭合，自由时段系产品决策转 04 登记）；卡6 sku_code 时长真源 `777d456`（7 文件 +130/-18，appointment 加 sku_code 列+activeSkuDurationMap 缓存+伪 SKU 400+resolveDurationMin 三消费点——上文「end+60min」已闭合为绑 SKU 按 duration_min 真源、未绑回落 60min）；**B52 卡2** 派单自由时段开放 `cc00948`（5 files +46/-25，DispatchCmd 增可选 start（HH:mm 正则）缺省/空白回落 apptTime、非法 422、越班次 422、重叠 409 复用，前端 slotClick 带 slot、班次内全空闲格可点、data-slot 锚点、style 零改——04 L146 闭合；纯纵深无模块状态跃迁；同批卡1 seed 别名层环境治理 `0ea8983` 不挂业务模块行，勾 04 L147）；**P5-B53（2026-09-16）`fb16b53`** 删除零引用死 CSS `.job--urgent` 一行（L141 加急源 A3 拍板不造字段、继续诚实空态，仅清死码无功能/契约变更，加急源缺口保留 04 L141 登记不勾销，纯死码清理不影响模块状态） |
| T2 数据分析（4 页） | ⬜ | — | — | 远期 T2 |
| T3 外部集成 | ⬜ | — | — | 远期 T3（**B57 卡2 已实施闭合 2026-09-17 `fc97b37`**：external_integration 表＋/integrations 窗口先行建成，3 通知 URL/3 广告密钥/免签开关录入即生效零重启，见 DESIGN-P5-B57-EXTERNAL-INTEGRATION；连接器本体/Outbox/T+1 对账仍远期 T3，状态不跃迁） |
| T4 AI 算力（4 页） | ⬜ | — | — | 远期 T4 |
| A1 · 模型接入（/ai/providers 供应商与模型配置） | ✅ | P5-B42/B43 | DELIVERY-P5-B42，ai-service | 供应商/模型 CRUD、ApiKey AES-GCM 密文落库+掩码回显、真实连通性测试（火山方舟 OpenAI 兼容）、六功能绑定与角色灰度矩阵；B42 末真实 Key 录入后连通待复核项**已由 B43 真实 invoke 成功闭合**（ark-code-latest，497 tokens）；价格口径统一为元/百万 tokens |
| A1 · AI 网关监控（/ai/gateway 调用日志/KPI） | ✅ | P5-B42/B43/B44 | DELIVERY-P5-B42，A1GatewayView 去 mock；DELIVERY-P5-B43 真实联动；DELIVERY-P5-B44 告警真实化 | KPI/调用日志/账单接 /api/ai/logs* 真实端点；**B43 起 POST /features/{code}/invoke 真实出口贯通，ai_invoke_log 成败全量沉淀，今日调用/成功率/P99/日志全部真实（ark-code-latest 真实 ARK 497 tokens 实证）**；**B44 拦截链接敏感词(400)与三级配额(429)、告警五规则实时评估，告警 tab 与 KPI 活跃告警真实联动** |
| A1 · AI 管理配置（/ai/admin 全局配置/矩阵/账单） | ✅ | P5-B42/B43 | DELIVERY-P5-B42，A1AdminView 去 mock | 全局配置 GET/PUT、功能角色矩阵、KPI/账单真实；B43 功能绑定抽屉新增真实试运行（invoke 结果/token/费用回显，调用后本月用量实时联动）；**B44 新增第 6 个「配额管理」tab（三级配额 8 行+日/月用量额度+水位三档色+调整抽屉，FEATURE→MODEL→GLOBAL 出站前 429 中文）** |
| A1 · AI 审批与效果评估（/ai/govern） | ✅ | P5-B42/B45 | DELIVERY-P5-B42，A1GovernView 去 mock；DELIVERY-P5-B45 申请入口+评估实验实体化 | 审批 Spring Page 真分页+状态过滤+通过/驳回决策（MODEL 通过联动 enabled、BINDING 驳回联动 NONE、重复决策 400 中文），REGION_MGR aiGovern:approve；**B45 补 POST /api/ai/approvals apply 申请入口（模型上架/功能绑定两类申请单，AiApplyDrawer 共享抽屉）+审批通过联动 enabled=t（linkage 审计），效果评估/A·B 实验从占位实体化（V18 两表、八端点、北京时间窗口聚合、lift pp、结案冻结，KPI computed 修复）** |
| A1 · 敏感词检测（/ai/sensitive 词库治理/命中留痕） | ✅ | P5-B46 | DELIVERY-P5-B46，A1SensitiveView 去 mock | 词库 CRUD+命中分页/词类三态过滤/统计/误报标注（SensitiveWordController 六端点）、V19 ai_sensitive_hit 命中落库 CHECK+三索引、六功能出站链 L142 拦截 BANNED/INJECTION 400 中文脱敏、SensitiveHitRecorder REQUIRES_NEW 保证拦截亦独立落库、8 张 AI 表 updated_at 触发器、前端时区工具 datetime.ts |
| A1 · 智能中心首页（/ai 四 KPI/能力卡/待办告警） | ✅ | P5-B46 卡2 | DELIVERY-P5-B46-2，A1HomeView 去 mock，`8bee59d` | 三权限分组门控（aiGateway:view 并发 logKpi/monthlyBill/listAlerts、aiGovern:view approvals?status=PENDING 取分页 totalElements、aiAdmin:view sensitive/hits/stats），无权限 KPI 显「—」且不发请求杜绝越权 403；12 能力卡 capCalls 按 billMap 仅六功能显本月调用量、敏感词卡显今日拦截，无日志功能诚实隐藏；待办=审批前 5（fmtAgo 相对时间，跳 /ai/govern）+敏感词今日命中条+active 告警前 5，空态文案；datetime.ts 增 fmtAgo；纯前端切真无新端点、样式零改动 |
| A1 · 内容生成（/ai/content 三渠道生成/落表/下发） | ✅ | P5-B46 卡3 | DELIVERY-P5-B46-3，A1ContentView 去 mock，`609ca2f` | V20 ai_content_record 17 列（channel/status 双 CHECK+三业务索引+updated_at 触发器，DDL-only）；ContentController 四端点（/generate、/records 真分页、/stats 北京日界六指标、/records/{id}/deploy 幂等审计）；公众号 wechat/海报 poster/短信 sms 三渠道差异化系统指令真实调用 ark-code-latest，generate 复用 featureInvokeService.invoke 全治理链（角色/灰度/敏感词 400 脱敏/配额/计费/日志）+InvokeView.logId 精确关联；读超时 60s→180s 三处对齐（yml/LlmClient cause 链识别/前端 axios）；save 后 findById 回读 DB 默认 createdAt；一键登记下发 GENERATED→DEPLOYED 联动采纳率；真实跨渠道推送（微信/短信网关）M5 远期 |
| A1 · 话术库（/ai/scripts 破冰/跟进/异议三场景生成/落库/采纳反馈） | ✅ | P5-B46 卡4 | DELIVERY-P5-B46-4，A1ScriptsView 去 mock，`46cacc6` | V21 ai_script 15 列（scene icebreak/followup/objection+source MANUAL/AI 双 CHECK、业务索引、updated_at 触发器，DDL-only）；ScriptController 七端点（库真分页+场景/关键词过滤、stats 北京日界四指标、generate、新增/编辑、adopt、feedback）；三场景差异化系统指令真实调用 ark-code-latest，generate 只出站不落库、保存带 invokeLogId 才 source=AI 关联 ai_invoke_log，复用 featureInvokeService.invoke 全治理链（角色/灰度/敏感词 400 脱敏/配额/计费/日志），保存前敏感词兜底；采纳计数/好评差评滚动均分，AI_SCRIPT CREATE/UPDATE/ADOPT/FEEDBACK 全动作审计；create/update 无方法级事务+findById 回读 DB 默认时间戳；真实推送至 M4 工作台/企业微信会话侧边栏为远期 |
| A1 · 客户画像引擎（/ai/profile 候选搜索/大模型画像/分群标签/权重回看） | ✅ | P5-B47 卡1 | DELIVERY-P5-B47-1，A1ProfileView 去 mock，`c116096` | V22 ai_customer_profile 表（价值分/分群/标签/应用标记/原始 JSON，CHECK+索引+updated_at 触发器，DDL-only）；ProfileController 八端点（客户候选搜索、真实生成、最近画像、历史、stats、特征权重、近 4 周回看、应用到分群）；客户候选/档案上下文/消费指标跨域经 customer-service 内部端点（X-Internal-Token）富化、失败降级空态不阻断；generate 为 feature_code=profile 第七个注册功能，复用 featureInvokeService.invoke 全治理链（角色/灰度/敏感词 400 脱敏/配额/计费/日志）；apply 幂等置位+APPLY_SEGMENT 审计，特征权重 SHAP Top8/效果回看按 ai_invoke_log 北京周边界聚合；真实 ark-code-latest 生成（683 tokens）；修复 RestTemplate 中文 query ISO-8859-1 编码静默返空、周聚合缺上界累计计数两缺陷；画像跨域自动推送分群/标签准确率回流评估体系为远期 |
| A1 · 复购预测引擎（/ai/repurchase 批次预测/真实推理/登记闭环） | ✅ | P5-B47 卡2 | DELIVERY-P5-B47-2，A1RepurchaseView 去 mock，`fc9f508` | V23 ai_repurchase_prediction 27 列（period week/month/quarter、project_code skin/inject/anti/body/other 双 CHECK、prob 0~100 CHECK、四索引含 followup 部分索引+触发器，DDL-only）；RepurchaseController 八端点（/run 真实批次、/list、/batch、/stats、/factors、/{id}/followup、/{id}/push、/batch-followup，校验/幂等/配额/审计四件套）；复购候选/近期成交信号跨域 txn-service、卡余额跨域 customer-service（X-Internal-Token）失败降级；repurchase 第八功能复用 invoke 全治理链逐客户串行真实 ark-code-latest 推理（RP2026091301/4 人/invoke 25-28/审计 500-503）；无批次中文 404 转引导空态、因子 available=false 诚实置灰；跟进任务真实下发/营销推送真实触达 M3-08·M5-03 远期 |
| A1 · 流失预警引擎（/ai/churn-model 批次评分/真实推理/干预闭环） | ✅ | P5-B47 卡3 | DELIVERY-P5-B47-3，A1ChurnView 去 mock，`4e05215` | V24 ai_churn_prediction 26 列（batch_no CH 规则/risk_score·risk_level 双 CHECK/四索引含 intervene 部分索引/触发器，DDL-only）；ChurnController 七端点（/run 无 period 最近一批即当前榜、/list?riskLevel、/batch、/stats、/factors、/{id}/intervene、/batch-intervene，四件套）；流失候选/近期消费信号 ai→txn X-Internal-Token 富化（SCAN_LIMIT=3000）失败降级；churn 第九功能复用 invoke 全治理链逐客户串行 ark-code-latest 真实评分、阈值服务端重算（CH2026091301/3 人全 mid 60 分/invoke 30 benchmark 失败留档+31-33 成功/审计 505-507）；客诉/互动两因子诚实置灰；字段撞会员等级名缺陷修复；干预真实下发 M3-10·M2-17·M5-03 远期 |
| A1 · AI 经营日报（/ai/daily-report 按日生成/多版本/建议采纳/订阅） | ✅ | P5-B47 卡4 | DELIVERY-P5-B47-4，A1DailyView 去 mock，`1282769` | V25 三表 ai_daily_report/ai_daily_suggestion/ai_daily_subscription（建议类别 CHECK core·anomaly·action、subscription staff_id PK+updated_at 触发器，DDL-only）；DailyController 九端点（当日报告/重发生成/history 按日期归并最新版/建议列表/逐条采纳/订阅四渠道读写）；经营指标+前一日基线 ai→txn /api/txn/internal/daily-metrics（X-Internal-Token internal:finance-flow）失败 available=false 降级；daily 第十功能（bindingId=8，角色仅 SUPER_ADMIN/REGION_MGR）复用 invoke 全治理链真实生成核心结论/异常提醒/行动建议三段十条建议，同日重发多版本留存（报告 v1/v2、invoke 34/35：86.9s/4523、118.0s/5801 tokens、cost_fen=0），建议 id=2/9 由 E011 采纳幂等、订阅翻转才审计（审计 508-514）；金额分→元/环比 null 文案/当日 404 静默空态/四渠道置灰；修复 nginx 60s 504（正则 location `~ ^/api/ai/(daily/generate|churn/run)$` 180s 四跳对齐）与 JPA timestamptz UTC 差 8 小时（bjTime）两缺陷；四渠道真实触达 M5-03·M2-17、日报定时生成 Job 远期 |
| A1 · AI 客服（/ai/chatbot 真实会话/AI 回复/转人工/人工座席） | ✅ | P5-B47 卡5 | DELIVERY-P5-B47-5，A1ChatbotView 去 mock，`405397a` | V26 两表 ai_chat_session（CS 单号/channel CHECK wechat·miniapp·web·h5、status CHECK ACTIVE·AI·HUMAN·CLOSED、转人工四列+触发器，DDL-only）/ai_chat_message（sender CHECK customer·ai·staff、AI 沉淀六列 invoke_log_id/tokens 三量/延迟/模型、双索引+触发器，DDL-only）；ChatbotController 七端点（类级 /api/ai/chatbot+aiChatbot:view：GET /sessions?channel、POST /sessions、GET /sessions/{id}、POST /sessions/{id}/messages 顾客消息携最近 6 轮上下文+顾客称呼、POST /sessions/{id}/transfer 幂等、POST /sessions/{id}/staff-reply 人工座席非人工态 409、GET /stats）；chatbot 继 daily 之后新接线业务功能码（bindingId=9 运行时录入 model_id=1/ALL/enabled）复用 invoke 全治理链真实出站，AI→人工单向状态机翻转后顾客消息落库不触发 AI；三次真实 invoke log37-39（1058/1615/618 tokens、21.9/33.9/14.4s、cost_fen=0），审计 516 CREATE_CHAT_SESSION/517 TRANSFER_HUMAN/518 CREATE_CHAT_SESSION 哈希链连续；真验修复 staffReply 挂方法级事务致 created_at（insertable=false PG now()）一级缓存未回填、人工消息 time="" 一缺陷（去事务+新持久化上下文回读）；三次均 <60s 本卡不改 nginx；转人工工作台 M4-09 真实联动、知识 RAG 增强远期 |
| A1 · 智能排班（/ai/scheduling 真实员工池/到店指标/规则矩阵/AI 解读/采纳） | ✅ | P5-B47 卡6 | DELIVERY-P5-B47-6，A1SchedulingView 去 mock，`e1bbadf` | V27 两表 ai_scheduling_plan（status CHECK DRAFT·ADOPTED、UK(week_start,store_code,version)、编制三列/cost_fen/zero_arrival/cost_basis_note/AI 五列/采纳三列+触发器，DDL-only）/ai_scheduling_slot（FK CASCADE、shift CHECK MORNING·MIDDLE·EVENING、4 裸码岗位）；SchedulingController 五端点（类级 /api/ai/scheduling+aiScheduling:view：POST /generate、GET /plan?weekStart&storeCode、POST /plans/{id}/adopt 幂等、GET /stats、GET /history）；OrgStaffClient RestTemplate 调 org-service 拉真实在职 4 裸码岗位（店长/咨询师/医生含治疗师/前台含收银）11 人池（5s/10s 超时，故障或空池 502），txn-service daily-metrics 近 14 天真实到店；规则算早/中/晚 3 班×7 天=21 槽需求矩阵与公平轮转（确定性非大模型），scheduling invoke（bindingId=4，B42 起六功能最后占位本卡首次接线）单次 ark-code-latest 仅生成解读 log41/42/44（7354/3917/5647 tokens、70~139s、页面约 125s、cost_fen=0）；到店样本 0 天 zeroArrival 诚实兜底全程标注，工资无数据源→岗位参考班薪 600/540/700/360 每班 6h 估 ¥12,080=1,208,000 分 COST_BASIS_NOTE 标注，M2-03 无现排后端显「无现排对比」诚实态；GENERATE_SCHEDULE/ADOPT_PLAN 双审计 520/521；修复两处旧 6 类岗位文案、零到店/故障分层两口径，nginx 60s→180s 正则补 scheduling/generate；清脏留 plan2 ADOPTED 样板（2026-09-07/11 人/21 槽/gap=0/zeroArrival=true/log42/E011）；M2-03 现排回填对比、员工薪酬真实数据源、客流沉淀后恢复按客流排班、M4-09 排班联动远期 |
| A1 · AI 知识库（/ai/knowledge 知识条目/词法检索/热搜/引用溯源/反馈） | ✅ | P5-B47 卡7 | DELIVERY-P5-B47-7，A1KnowledgeView 去 mock，`8ac9372` | V28 两表 ai_knowledge_item（15 列 category CHECK project·script·compliance/index_status CHECK PENDING·INDEXED·FAILED/refs_count≥0+3 索引+updated_at 触发器，DDL-only）/ai_knowledge_citation（9 列 FK CASCADE/source_feature CHECK manual_search·scripts·chatbot·content/useful 三态+2 索引，append-only）；KnowledgeController 十端点（类级 /api/ai/knowledge+aiKnowledge:view 三角色：根 GET 列表、/{id}、/stats 九键、/hot 近 30 天 top8、/search?q 词法加权召回、/{id}/citations、POST create/update/reindex/feedback）；KnowledgeService 365 行 PG ILIKE 加权（ORDER BY CASE 标题 0/标签 1/正文 2, refs_count DESC）非语义向量、热搜近 30 天北京时区日界、浏览不写引用显式检索才写 manual_search citation 同事务 refs_count+1、stats usefulRatePct 无反馈 null 显「—」、feedback/reindex 状态翻转才审计幂等、create/update 无方法级事务回读 PG now()；管理/检索面不接 FeatureCatalog 不走绑定配额计费；ai.ts 5 interface+10 函数、A1KnowledgeView 489 行（浏览检索分离/四 KPI/热搜 chips/idxPill 三态 FAILED 重试/溯源抽屉 Promise.all+反馈幂等/词法诚实注脚两处）；审计 525-544 共 20 行 CREATE×6/FEEDBACK×10/UPDATE×2/REINDEX×2 哈希链连续；真验修复 recall nativeQuery 5 处 cast(:keyword as text)、非法 category 400、列表列 key 错配 vector/refs→indexStatus/refsCount、feedbackCitation 返回类型、fat-jar 陈旧 5 项；两业务表验证后清场 0 行审计留存；语义向量 EMBEDDING+pgvector、RAG 联动 scripts·chatbot·content（枚举预留当前仅 manual_search）、pg_trgm/zhparser 分词、附件/版本/审批流/门店可见性隔离远期；~~审计静默丢失监测~~**已由 B48 卡1 闭合**（2026-09-14，`4bfb658`，outbox+中继+对账监测三道防线，详见 DELIVERY-P5-B48） |
| A1 · 隐私合规（/ai/privacy 脱敏规则/等保达标/审计哈希导出） | ✅ | P5-B47 卡8 | DELIVERY-P5-B47-8，A1PrivacyView 去 mock，`a86a643` | V29 三表 ai_privacy_mask_rule（rule_code UK/mask_type 八值 CHECK phone·idcard·name·amount·bankcard·address·medical·email/enabled 默认 true+(enabled,rule_id) 索引，8 固定码 ON CONFLICT DO NOTHING，PM-SEED-07 停用）/ai_privacy_compliance_item（item_code UK/label VARCHAR200/checked 默认 false，8 项等保 2.0 固定码 PC-SEED-07 未达标互证）/ai_privacy_export（range_from/range_to DATE+两 CHECK 区间跨度≤366 天/audit_count/report_hash VARCHAR64，export_id DESC 索引），DO 块挂两 BEFORE UPDATE 触发器复用 ai_set_updated_at()；PrivacyController 7 端点类级 aiPrivacy:view（GET stats/mask-rules/compliance-items/exports 分页 page0 size20，两 toggle+POST /exports 方法级 aiPrivacy:edit；PermissionMatrix 两处 +edit 超管/区域经理可写、STORE_MGR 仅 view、auth.ts REGION_MGR +1，403 不落审计）；PrivacyService 265 行四 KPI maskFieldCount=8/compliancePct=Math.round=88/pendingCount=停用规则+未达标项=2/auditCount=AI_PRIVACY 6+export 0=6，createExport canonical＝HEADER\|AI_PRIVACY\|from\|to\|行数+逐行 id\|prev_hash\|cur_hash\|Jackson readTree/writeValueAsString 规范化 payload（异常回退原文/null→空串），SHA-256 HexFormat 64 位小写，北京时区 UTC+8 左闭右开、RANGE_SPAN_MAX_DAYS=366、空区间只哈希头也出报告、PAGE_MAX=200；审计动作码用数字主键 PM-RULE-{id}/PC-ITEM-{id}/PM-EXPORT-{id}（种子码 PM-SEED/PC-SEED 在 payload），AI_PRIVACY 545–550 共 6 条 TOGGLE_MASK×2/TOGGLE_COMPLIANCE×2/EXPORT_REPORT×2 哈希链 544→550 七接点全 OK，双报告 hash d2df30d8…8227（323 行）/b89188f4…e59c8（空区间）；ai.ts 4 interface+7 函数、A1PrivacyView 272→453 行三 tab 只读降级（canEdit 短路/操作列灰字「只读」/日期控件 :disabled/导出按钮替换为需 edit 权限文案）+busyMaskId/busyCompId/exporting 防重入+bjDateString+shortHash 截断挂 title 全量；E005 账密登录实测三 tab 只读零越权，清场 exports=0/停用仅 PM-SEED-07/未达标仅 PC-SEED-07 零污染；个人数据请求工单流 DSAR、授权同意 consent 管理、合规口径定期巡检、导出哈希验真 UI 远期 |
| A1 AI 中心余页（Prompt 编排/Embedding/向量/Agent/效果实验等，15/15 页已全部真实闭环） | ⬜ | — | — | **远期 A1 增强池（本行一个模块行，保持 ⬜）**：B46 分批下列 15 页均已切真，**但本行不再按「页面数」计余页，仅作为 A1 纵深能力占位行**——~~真实功能 invoke+调用日志（B43 已闭合）~~、~~配额限流敏感词告警（B44 已闭合）~~、~~审批申请 UI+效果评估/A·B 实体化（B45 已闭合）~~、~~敏感词检测页（B46 卡1 已闭合）~~、~~智能中心首页（B46 卡2 已闭合）~~、~~渠道内容生成（B46 卡3 已闭合）~~、~~话术库（B46 卡4 已闭合）~~、~~客户画像引擎（B47 卡1 已闭合）~~、~~复购预测（B47 卡2 已闭合）~~、~~流失预警（B47 卡3 已闭合）~~、~~AI 经营日报（B47 卡4 已闭合）~~、~~AI 客服（B47 卡5 已闭合）~~、~~智能排班（B47 卡6 已闭合）~~、~~AI 知识库（B47 卡7 已闭合）~~、~~隐私合规（B47 卡8 已闭合）~~、余页去 mock（已交付 15/15 页全部真实闭环，历史口径余 0 页/实测 nav 中 mock A1 视图 12→7→6→5→4→3→2→1→**0** 个（B47 卡8 切真，A1 全去 mock），粒度口径差见 DELIVERY-P5-B46 §7）+EMBEDDING/VISION 多能力+多供应商+token 计费账单环比闭环 |
| 通用页（全局搜索/帮助/403/闭环演示） | ✅ | 全局搜索四源聚合（顾客/疗程卡/服务/产品，手机号掩码 A1-17）＋帮助中心三类内容真实过滤＋意见反馈跳 /workorders；403 与 /closed-loop 闭环演示此前已具备 | 读：/api/search/global?q=（customer-service） | **P5-B83 卡3（2026-09-21）通用页切真闭合 `42cfa1c`（2 files +179/-22）**：G1 全局搜索四源聚合端点（限额合并＋searchSeq 防竞态＋250ms 防抖，手机号 maskPhone 内联掩码 `***` 合规 A1-17）；G2 帮助中心 quickStart/faqs/videos 三 computed 真实过滤＋反馈入口跳 /workorders 真实工单页；GSearchView +156/-22／GHelpView +23，template/style 零改动，三轨真验（网关 curl ✅/浏览器搜索四分类·帮助过滤·反馈跳转全绿 ✅）；**纵深跃迁**；详见 DELIVERY-P5-B83-近线补点-2026-09-21.md（卡3 章） |
